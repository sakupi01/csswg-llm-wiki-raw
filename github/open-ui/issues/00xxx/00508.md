---
number: 508
title: "[popup] Consider renaming `triggerpopup`"
state: "closed"
labels: ["needs edits", "popover"]
author: "mfreed7"
created_at: "2022-03-31T19:09:34Z"
closed_at: "2022-04-21T19:06:29Z"
url: "https://github.com/openui/open-ui/issues/508"
comments: 15
has_resolution: true
synced_at: "2026-08-27T14:25:56Z"
---

To complete the trifecta of popup bikeshedding issues (along with https://github.com/openui/open-ui/issues/491 for `popup` and https://github.com/openui/open-ui/issues/500 for `initiallyopen`), let's consider renaming `triggerpopup`.

The purpose of this attribute is to link a button (or similar) element to a popup element, and enable the button to declaratively trigger (show) the popup. The functionality is described [here in the explainer](https://open-ui.org/components/popup.research.explainer#declarative-trigger-the-triggerpopup-attribute).

Suggestions appreciated.

<!-- comment id=1085206298 author=scottaohara created=2022-03-31T22:49:35Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1085206298 -->
## @scottaohara — 2022-03-31

This may be best decided once popup has been shed or shedded… whichever word is best for past tensing that. 

but, I personally like the terms trigger or controls which could be appended to whatever other word is chosen. though the latter is due to my familiarity with the aria-controls attribute

<!-- comment id=1091989883 author=mfreed7 created=2022-04-07T17:05:22Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1091989883 -->
## @mfreed7 — 2022-04-07

One more comment here, which directly relates to https://github.com/openui/open-ui/issues/486. If that issue is resolved that clicking the trigger button a second time **closes** the popup, then perhaps a better name for the attribute would be something like `togglepopup`.

<!-- comment id=1092055094 author=una created=2022-04-07T18:10:10Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1092055094 -->
## @una — 2022-04-07

+1 `togglepopup` seems like a better idea here — 

1. This makes it possible to add this attribute to the "close" button within a modal popup.
2. It also enables open/close in a trigger button such as a select menu instead of just allowing that button to open it

<!-- comment id=1092081568 author=css-meeting-bot created=2022-04-07T18:40:48Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1092081568 resolution=true -->
## @css-meeting-bot — 2022-04-07

The Open UI Community Group just discussed `508`, and agreed to the following:

* `RESOLVED: Change the name and behavior to 'togglepopup' and keep discussing the values and behaviors for only-open and only-close.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> Topic: 508<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/508<br>
&lt;hdv> una: so when you have a menu that you want to close, you may have like an X on it to close it… when masonf and I talked about it we realised togglepopup may be better<br>
&lt;hdv> una: because you usually have the same button to open and close it<br>
&lt;hdv> una: so it could make sense to have multiple attribute, but probably woudl be too many<br>
&lt;Travis> https://wicg.github.io/close-watcher/ as FYI<br>
&lt;masonf> q?<br>
&lt;dandclark> q+<br>
&lt;JonathanNeal> ack JonathanNeal<br>
&lt;hdv> una: majority of reasons seems to be opening or closing, which could be same button or two, but you could use the same attr on both<br>
&lt;hdv> dandclark: I like the idea of a single toggle attribute that can handle both<br>
&lt;hdv> dandclark: are there no use cases where you want to separate the behavior?<br>
&lt;hdv> una: because you set the ID of the popup you can have as many buttons that you could use the attribute on<br>
&lt;hdv> dandclark: what if you want two separate buttons, one is for opening one for closing<br>
&lt;hdv> ack dandclark<br>
&lt;hdv> q+<br>
&lt;JonathanNeal> q+<br>
&lt;hdv> ack hdv<br>
&lt;scotto> q+<br>
&lt;hdv> hdv: thinking of accessible names, would there be cases where you would want to have specific butons for 'open thing' and 'close thing', for that you would need mutiple buttons<br>
&lt;hdv> una: yes but thinking of use cases like tooltips or popovers you would usually use the same button<br>
&lt;JonathanNeal> ack JonathanNeal<br>
&lt;hdv> JonathanNeal: does this open the possibility to @@@<br>
&lt;hdv> masonf: great idea!<br>
&lt;hdv> scotto: there probably wouldn't be an implicit ARIA role or state associated to it, it would behave as a button with a single function, if there was a single element that opened and closed a popup, we would probably need to find another way to set expanded/collapsed states, this could maybe be inferred<br>
&lt;hdv> scotto: probably could decide on something on the platform level that could be baked into the control like the way that works in a selectg<br>
&lt;hdv> s/selectg/select<br>
&lt;JonathanNeal> The "possibility" hdv mentioned was something like `togglepopup="+id"` or `togglepopup="open:id"`.<br>
&lt;masonf> Proposed resolution: Change the name to 'togglepopup' and keep discussing the values.<br>
&lt;hdv> Travis: seems like for 508 we almost have a proposed resolution, for 500, folks can go there and continue the conversation<br>
&lt;JonathanNeal> +1 to that proposed resolution, masonf, and that the default behavior is toggle<br>
&lt;masonf> Proposed resolution: Change the name and behavior to 'togglepopup' and keep discussing the values and behaviors for only-open and only-close.<br>
&lt;hdv> una: I would like to capture which values in the proposed resolution, the default behavior is toggle<br>
&lt;JonathanNeal> +1 to that, masonf<br>
&lt;hdv> +1<br>
&lt;masonf> RESOLVED: Change the name and behavior to 'togglepopup' and keep discussing the values and behaviors for only-open and only-close.<br>
</details>

<!-- comment id=1092101702 author=mfreed7 created=2022-04-07T19:05:42Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1092101702 -->
## @mfreed7 — 2022-04-07

One proposal to highlight from the meeting just now:

```html
<button togglepopup="id">This toggles the popup</button>
<button togglepopup="+id">This can only open the popup</button>
<button togglepopup="-id">This can only close the popup</button>
```

There are issues with ids that contain +/-, and the syntax is funny. But perhaps something like this as an approach?

<!-- comment id=1092154660 author=jonathantneal created=2022-04-07T20:07:51Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1092154660 -->
## @jonathantneal — 2022-04-07

I did initially suggest symbols. A symbol would prevent conflicts with multiple IDs. This is not a direct concern of `togglepopup`, which is limited to a single ID. Avoiding ID conflicts may still be worth considering, tho, largely as a general practice to be leveraged in future APIs, and lesser as a matter of future-proofing.

However, I would not prefer a pattern using **HYPHEN MINUS** (`-`), as that character overlaps with working IDs.

**Example**:
```html
<style>[-id] { color: blue }</style>
<span -id>this text will be blue</span>
```

I would recommend a **SOLIDUS** (`/`) to separate an ID (or IDs) from additional arguments. In the case of `togglepopup`, a “force” argument.

**Example**:
```html
<button togglepopup="id">makes the popup open or close</button>
<button togglepopup="id / open">makes the popup open</button>
<button togglepopup="id / close">makes the popup close</button>
```

If the separator is acceptable, the second argument can still be bikeshed. In the example above, I set a second argument of `open` or `close`. Perhaps it could also be `toggle`. Perhaps it could be `true` or `false`, mirroring [`DOMTokenList`](https://developer.mozilla.org/en-US/docs/Web/API/DOMTokenList/toggle). Perhaps it could be `0` or `1`,  mirroring the [CSS Toggles](https://tabatkins.github.io/css-toggle/#checked-pseudoclass) proposal. I only mention this to separate the `open` and `close` keywords from the preference for a **SOLIDUS** (`/`) separator.

**Applying this pattern to some future concept:**
```html
<future-concept check="id">makes the thing checked or unchecked</future-concept>
<future-concept check="id / checked">makes the thing checked</future-concept>
<future-concept check="id / unchecked">makes the thing unchecked</future-concept>
<future-concept check="id / indeterminate">makes the thing indeterminate</future-concept>
```

<!-- comment id=1093151856 author=mfreed7 created=2022-04-08T18:09:10Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1093151856 -->
## @mfreed7 — 2022-04-08

+1 to using SOLIDUS as a separator, or at the very least not using something that can appear in IDs. I added the `+/-` to my example just because that's what I heard at the meeting. Thanks for pointing out the issue.

I kind of like your proposed syntax. Is there any equivalent API on the platform, or would this be a brand new pattern? (I think it's new.)

```html
<button togglepopup="id">makes the popup open or close (toggle)</button>
<button togglepopup="id / toggle">makes the popup open or close (toggle)</button>
<button togglepopup="id / open">makes the popup open but not close</button>
<button togglepopup="id / close">makes the popup close but not open</button>
```

Personally, I don't like `true/false` or `0/1` because it's confusing. What does `togglepopup="id / false"` mean? Don't toggle? Only close? I think your original `open/close/toggle` naming is the most clear and readable.

<!-- comment id=1105448221 author=mfreed7 created=2022-04-21T16:32:51Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105448221 -->
## @mfreed7 — 2022-04-21

In working on the Chromium prototype, I think I found an issue with the concept of allowing multiple buttons to open/close a popup. In this example:

```html
<div popup=popup id=popup>Popup</div>
<button togglepopup=popup>Click me to *open* the popup</button>
<button togglepopup=popup>Click me to *close* the popup</button>
```

Clicking the "open" button properly opens the popup, and in doing so, registers the first button as the "invoking element" for the popup. Subsequent clicks on that same button are therefore ignored in the light dismiss logic, meaning a mouse-down on the first button doesn't immediately close the popup. However, if instead, the user clicks on the second button intending to close the popup, that mouse-down *is* a light dismiss trigger, since it isn't a descendent of a) the popup, b) the anchor element, or c) the invoking element. So that mouse-down closes the popup. Then the `togglepopup` behavior happens, which sees the now-hidden popup and "toggles" it back open. So the close button just causes the popup to flicker but stay open.

I suppose this could be fixed by adding more magic to the light dismiss algorithm, to check whether the clicked element is a descendent of a button that *also* points (via `togglepopup`) to the popup element, and if so, avoid light dismissing the popup. That seems like it'll work, but I wanted to bring this up in case there are better suggestions.

One bonus, I think, is that if we go this route, we don't actually have to store the invoking element on `Element`. We can just check for `togglepopup` on ancestors as we walk up the tree, and if they point to an open popup, count them as ancestral popups.

<!-- comment id=1105624941 author=css-meeting-bot created=2022-04-21T18:41:13Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105624941 -->
## @css-meeting-bot — 2022-04-21

The Open UI Community Group just discussed `Consider renaming triggerpopup`.

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: Consider renaming triggerpopup<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/508<br>
&lt;gregwhitworth> q?<br>
&lt;flackr> masonf: Discussed before, we resolved to rename triggerpopup to togglepopup. One issue still open - since it would have an implied toggle behavior open -> close -> open, you may want a way to have a one way toggle behavior to have two buttons<br>
&lt;flackr> masonf: There's a proposed syntax, togglepopup=&lt;id>/(open|close)<br>
&lt;hdv> q+<br>
&lt;una> q+<br>
&lt;gregwhitworth> ack hdv<br>
&lt;flackr> hdv: Is there any precedent for this?<br>
&lt;flackr> masonf: Not aware of one<br>
&lt;gregwhitworth> ack una<br>
&lt;JonathanNeal> In CSS, there is a similar precedent.<br>
&lt;gregwhitworth> q+<br>
&lt;flackr> una: I'm in support of having it be togglepopup by default. Not sure about the proposed syntax where it's part of the string. I think it would make more sense if we had togglepopup=id and had openpopup and closepopup as separate values<br>
&lt;flackr> una: I think this is more clear from the developer perspective<br>
&lt;jh3y> openpop="id" closepopup="id" more declarative for people. String has some room for error. People writing "closer", etc.<br>
&lt;hdv> ack greg<br>
&lt;jh3y> +1<br>
&lt;JonathanNeal> The precedence of a second argument being a "force" is in classList / DOMTokenList.<br>
&lt;flackr> gregwhitworth: +1 to una's suggestion, we just need to figure out how to handle the collision if you put multiple on there<br>
&lt;flackr> gregwhitworth: we also need to look to the implementers for the overhead cost of this<br>
&lt;jh3y> Yeah, what takes precedence?<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack dbaron<br>
&lt;Zakim> dbaron, you wanted to comment on collision handling<br>
&lt;flackr> una: it's hopefully relatively similar to other things, just syntactic sugar<br>
&lt;una> q+<br>
&lt;flackr> dbaron: for collision handling, we can take the union of the behaviors, i.e. openpopup + closepopup is equivalent to togglepopup. openpopup + togglepopup is equivalent to toggle<br>
&lt;gregwhitworth> ack una<br>
&lt;jh3y> I'd see open/close as taking precedence over toggle.<br>
&lt;flackr> una: I think this would be a bit confusing, and only one should win<br>
&lt;flackr> masonf: It's only confusing if you also have togglepopup, but if you only have openpopup or closepopup it makes sense<br>
&lt;JonathanNeal> what if I put different ids to open &amp; close?<br>
&lt;gregwhitworth> yeah, we're discussing an error case<br>
&lt;jh3y> If you have open and toggle, it should be open IMO.<br>
&lt;gregwhitworth> flackr: it's like onmousedown and onmouseup onmouseclick<br>
&lt;JonathanNeal> openpopup="one_id" closepopup="alt_id"<br>
&lt;flackr> masonf: if we remove toggle and just have open and close it's pretty clear right?<br>
&lt;JonathanNeal> q+<br>
&lt;jh3y> Oof – the button that controls multiple popups. That would flag an a11y thing perhaps?<br>
&lt;flackr> una: I don't think it's confusing, but it's a better experience to just write toggle<br>
&lt;gregwhitworth> q?<br>
&lt;flackr> masonf: what if you write all three?<br>
&lt;flackr> una: I'm coming around to it should toggle, what if there are more ids?<br>
&lt;flackr> masonf: that's less ambiguous, specific behavior per id<br>
&lt;flackr> una: could you have opentoggle=id1 opentoggle=id2, etc<br>
&lt;hdv> or opentoggle="id1 id2 id3" like with class?<br>
&lt;flackr> masonf: I think you get the value of the last one provided?<br>
&lt;JonathanNeal> They are only supposed to support one, I asked about this.<br>
&lt;flackr> emilio: making it depend on order feels bad<br>
&lt;JonathanNeal> support one id<br>
&lt;flackr> emilio: we do have precedents with longhand and shorthand ordering in css, but it's not great<br>
&lt;gregwhitworth> q?<br>
&lt;JonathanNeal> my issue with multiple attributes are that the id must be duplicated, which means they can be different.<br>
&lt;flackr> emilio: if we want to have all three attributes, we should just define the order<br>
&lt;flackr> una: this feels like checked=false checked=true, last one wins<br>
&lt;flackr> emilio: this is a parser thing though, the html parser chooses one to win<br>
&lt;JonathanNeal> still q+<br>
&lt;flackr> emilio: having two different attributes is a perfectly syntactically valid situation, so it seems simplest to define the order in cases where the order could matter<br>
&lt;miriam> q+<br>
&lt;flackr> emilio: does the order matter? if both or all three are present, we just need to define the behavior to not do two different things<br>
&lt;masonf> q?<br>
&lt;flackr> una: if open and close are both present, does it open it after closing?<br>
&lt;flackr> emilio: right, this is what we should define<br>
&lt;JonathanNeal> Sorry, I lack the assertiveness to just jump into the conversation on Zoom. :)<br>
&lt;flackr> dbaron: I agree, you should only get one state change out of the same thing<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;flackr> JonathanNeal: I share these concerns. I had asked previously - there should only ever be one item an element with popup can control. This was the question that lead to toggleattribute being the one attribute which is why I ultimately prefer this syntax. If we duplicate the ids then we must resolve the possible difference of different ids<br>
&lt;flackr> JonathanNeal: If there was a single attribute syntax it removes this possible issue<br>
&lt;flackr> masonf: So the question is should it be possible to have one button open more than one popup?<br>
&lt;flackr> JonathanNeal: Oh, I thought it's not supposed to be possible. If it was, then a single attr is easier, but if not, then this isn't a concern<br>
&lt;gregwhitworth> ack miriam<br>
&lt;flackr> miriam: if we had 3 attrs, open|close|toggle, it makes sense with all three or any combination we get the toggle behavior because any comination adds up to this<br>
&lt;flackr> miriam: this gets more complicated with different ids, where it really shouldn't control different ids<br>
&lt;flackr> masonf: if you have togglepopup=id1 and openpopup=id2, which one opens?<br>
&lt;flackr> masonf: i realize we can define this, but it feels odd to have to reason about this<br>
&lt;gregwhitworth> that's a great point<br>
&lt;JonathanNeal> This is why I am hoping we can land on something like `toggle="id"`, `toggle="id open"`, `toggle="id close"`.<br>
&lt;flackr> masonf: this is why the single attribute suggestion is nice - in avoiding this problem<br>
&lt;masonf> q?<br>
&lt;JonathanNeal> erm, but `togglepopup`:)<br>
&lt;jh3y> Is it not the case that we might want multiple popups in some scenarios?<br>
&lt;una> q?<br>
&lt;JonathanNeal> q?<br>
&lt;JonathanNeal> q+<br>
&lt;masonf> q+<br>
&lt;flackr> gregwhitworth: based on this, I can't stand the syntax that was proposed earlier, but i see the need to force choosing a single id<br>
&lt;una> q+<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;flackr> JonathanNeal: looking at css toggles proposal, it seems that a toggle and its control are not separated by a /, which makes me think the / might separate for controlling multiple items. I see a precedent there for not needing any symbol. Does this seem better? e.g. togglepopup="id (open|close)" this acts as the one way forcing direction<br>
&lt;gregwhitworth> ack masonf<br>
&lt;flackr> masonf: one issue there is ids can contain whitespace<br>
&lt;hdv> white space separated would make me thing there is an element with the id 'open'  that would also open<br>
&lt;hdv> *think<br>
&lt;flackr> JonathanNeal: can ids support whitespace?<br>
&lt;gregwhitworth> q++ masonf<br>
&lt;JonathanNeal> We do have force on `toggle` in https://developer.mozilla.org/en-US/docs/Web/API/DOMTokenList/toggle<br>
&lt;flackr> una: with this syntax, the keywords open and close could be ids. It's weird that we're setting the attribute to toggle but the open and close don't mean toggle. It feels unusual and less normative. I still prefer multiple attributes<br>
&lt;gregwhitworth> ack +<br>
&lt;Futekov> +1 for having several attributes<br>
&lt;flackr> masonf: what if we allow all three, but define it such that one always takes precedence<br>
&lt;dbaron> +1 to just defining the precedence<br>
&lt;gregwhitworth> ack una<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> +1 to defining precedence, that way it is only complex for authors who make it complex by adding multiple attributes<br>
&lt;JonathanNeal> if we define precedence, I would expect `toggle` wins.<br>
&lt;flackr> una: I think masonf and i in agreement we like multiple attributes and we just need to define the precedence<br>
&lt;masonf> Proposed resolution: add two more attributes, 'openpopup' and 'hidepopup' which only open or close the popup. These, plus 'togglepopup' would only take an idref as a value. We would define a priority such that only one of the three takes effect.<br>
&lt;una> LGTM<br>
&lt;hdv> +1<br>
&lt;JonathanNeal> q+<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;flackr> JonathanNeal: I like this mostly. Can we punt on the last part about defining a priority, does adding one invalidate it?<br>
&lt;masonf> Proposed resolution: add two more attributes, 'openpopup' and 'hidepopup' which only open or close the popup. These, plus 'togglepopup' would only take an idref as a value. We would define how precedence works when more than one of these attributes is used.<br>
&lt;gregwhitworth> flackr: the ordering is just for the IDRef, not the behavior<br>
&lt;JonathanNeal> +1<br>
&lt;dbaron> (in hindsight, I realize I was a bit confused earlier and making suggestions that weren't helpful)<br>
&lt;Futekov> q+<br>
&lt;jh3y> ( open || close ) > toggle, open &amp;&amp; close == do nothing<br>
&lt;gregwhitworth> ack Futekov<br>
&lt;flackr> una: we could still define it being the order things appear in the dom. The resolution is just to add these attributes and figure out how to resolve these conflicts<br>
&lt;JonathanNeal> thank you for the rephrasing, masonf.<br>
&lt;emilio> q+<br>
&lt;masonf> q+<br>
&lt;flackr> Futekov: we don't necessarily  need precedence. We can just apply the same logic as when the parser sees duplicate attributes in one tag.<br>
&lt;gregwhitworth> ack emilio<br>
&lt;flackr> emilio: it's a pretty different case from having multiple classes in the class attribute or multiple class attributes?<br>
&lt;flackr> Futekov: multiple class attributes<br>
&lt;flackr> emilio: You only need to deal with that during parsing but not anywhere else, where having different attributes is a valid state that can happen from javascript. This makes it complicated to define last when it can occur later.<br>
&lt;flackr> emilio: I'd prefer not relying on attribute order<br>
&lt;gregwhitworth> ack masonf<br>
&lt;flackr> masonf: +1, this makes me nervous. We do keep track of the order, but it would be difficult to track over updates and could be complicated<br>
&lt;JonathanNeal> This is why I am glad we have rephrased the proposal.<br>
&lt;flackr> gregwhitworth: I think we're safe to resolve on modified proposed resolution, the further discussion is just about how to handle the discrepancies<br>
&lt;una> +1 thanks JonathanNeal<br>
&lt;flackr> Resolved: add two more attributes, 'openpopup' and 'hidepopup' which only open or close the popup. These, plus 'togglepopup' would only take an idref as a value. We would define how precedence works when more than one of these attributes is used.<br>
</details>

<!-- comment id=1105643383 author=mfreed7 created=2022-04-21T19:00:25Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105643383 -->
## @mfreed7 — 2022-04-21

Note the resolution from the meeting just now:

```
<flackr> Resolved: add two more attributes, 'openpopup' and 'hidepopup' which only
open or close the popup. These, plus 'togglepopup' would only take an idref as a
value. We would define how precedence works when more than one of these
attributes is used.
```

<!-- comment id=1105648992 author=mfreed7 created=2022-04-21T19:06:29Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105648992 -->
## @mfreed7 — 2022-04-21

Just to clarify what I assume to be everyone's preference: the resolution above **should have said** `showpopup` and not `openpopup`. If there are objections to that, please open a new issue to discuss, and sorry about that!

I've opened https://github.com/openui/open-ui/issues/523 to discuss the rules for precedence. Given that, I'm going to close this issue as resolved.

<!-- comment id=1105654557 author=mfreed7 created=2022-04-21T19:12:09Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105654557 -->
## @mfreed7 — 2022-04-21

> I suppose this could be fixed by adding more magic to the light dismiss algorithm, to check whether the clicked element is a descendent of a button that _also_ points (via `togglepopup`) to the popup element, and if so, avoid light dismissing the popup. That seems like it'll work, but I wanted to bring this up in case there are better suggestions.
> 
> One bonus, I think, is that if we go this route, we don't actually have to store the invoking element on `Element`. We can just check for `togglepopup` on ancestors as we walk up the tree, and if they point to an open popup, count them as ancestral popups.

I'm treating this issue like an implementation/spec issue at this point. I think it should be possible to get the behavior we want, and as I implement it in Chromium, I'll raise an issue if the above approach doesn't seem to be working.

<!-- comment id=1105659011 author=jh3y created=2022-04-21T19:17:06Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1105659011 -->
## @jh3y — 2022-04-21

Thoughts from the call:

- Is there not a use case whereby a developer could want multiple popups on the top level?
- I like the idea of "toggle", "show", and "hide". For precedence, "show" or "hide" trump "toggle" IMO as they are more specific about what they are doing. If all three exist, it's a toggle. If "show" and "hide" exist, it's a non-issue too? Only issue is with varying IDs 🤔  If a button opens one and closes another, where does a11y come into that? Does it?
- +1 for ::backdrop It's one less thing for developers to worry about creating. If they want to create custom, they can.

<!-- comment id=1106667697 author=mfreed7 created=2022-04-22T16:19:05Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1106667697 -->
## @mfreed7 — 2022-04-22

Thanks for the comments!

> * Is there not a use case whereby a developer could want multiple popups on the top level?

I view that as the primary difference between popups and dialogs. Popups are "one at a time", which allows them to be light-dismissed. If you want more, `<dialog>` is your friend. If you think there's a use cases we're missing here, please open an issue!

> * I like the idea of "toggle", "show", and "hide". For precedence, "show" or "hide" trump "toggle" IMO as they are more specific about what they are doing. If all three exist, it's a toggle. If "show" and "hide" exist, it's a non-issue too? Only issue is with varying IDs 🤔  If a button opens one and closes another, where does a11y come into that? Does it?

I [posted](https://github.com/openui/open-ui/issues/523#issuecomment-1106667339) this on the [issue](https://github.com/openui/open-ui/issues/523) for you.

> * +1 for ::backdrop It's one less thing for developers to worry about creating. If they want to create custom, they can.

👍 

<!-- comment id=1106669789 author=jh3y created=2022-04-22T16:21:44Z url=https://github.com/openui/open-ui/issues/508#issuecomment-1106669789 -->
## @jh3y — 2022-04-22

Awesome! Thanks @mfreed7 🙏 

Yeah, after discussing it some more, I think you could do a `popover` as an overlay to tackle that scenario of wanting more than one `popover` displayed at a time. The idea was like "Help" screens/tutorials where you have multiple call outs. I've yet to dig into `dialog` demos in-depth. Can you display multiple at the same time side by side then? I know you can nest them which is interesting.

Thanks for forwarding on my comments 🙏 
