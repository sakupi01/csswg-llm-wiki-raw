---
number: 491
title: "[popup] Bikeshed: popup=popup is unusual"
state: "closed"
labels: ["needs edits", "popover"]
author: "mfreed7"
created_at: "2022-03-16T23:00:21Z"
closed_at: "2022-05-19T18:54:23Z"
url: "https://github.com/openui/open-ui/issues/491"
comments: 28
has_resolution: true
synced_at: "2026-08-27T14:25:56Z"
---

This issue was reported by @domenic [here](https://github.com/mfreed7/popup/issues/2), and is being moved to the OpenUI repo.

-----

The `x=x` pattern in HTML is generally used as a way to allow XML serializations (i.e. "XHTML") to do boolean attributes. I.e. instead of the simple `<div checked>`, you'd do `<div checked="checked">`, because XML does not allow value-less attributes.

We should probably avoid using this pattern for a non-boolean attribute, as it will confuse people.

Ideas:

- Rename the attribute. (E.g. `toplayerbehavior=popup` or `toplayer=popup`.)
- Rename the value. (E.g. `popup=default`, `popup=onlyone`.)
- Have no value, and just a state. (So you'd do `<div popup>` with no value to get this behavior.)

I imagine there's going to be a general bikeshedding fest on popup/hint/async, but I just wanted to note this as a relatively high-priority consideration in that bikeshed.

<!-- comment id=1084781696 author=mfreed7 created=2022-03-31T15:57:21Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084781696 -->
## @mfreed7 — 2022-03-31

One more thing I've run into, related to this issue, as I've been implementing the Chromium prototype:

```html
<script>
function popup() {
  ...do something...
}
</script>
<button onclick="popup()">Click me</button>
```

I've seen this example in several of our web tests now, and I'm wondering if it portends Web compat issues in the future. The issue is that within the `onclick` handler, `this` is the `<button>`, so `popup()` tries to call `button.popup()`, which is now the `DOMString`-valued IDL attribute on `Element`, so this throws. Renaming `popup` to something a bit more obscure would clearly help this potential issue.

<!-- comment id=1084793987 author=domenic created=2022-03-31T16:08:39Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084793987 -->
## @domenic — 2022-03-31

I think you could fix that by adding `[Unscopable]` to the popup IDL attribute.

<!-- comment id=1084978849 author=css-meeting-bot created=2022-03-31T18:47:13Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084978849 -->
## @css-meeting-bot — 2022-03-31

The Open UI Community Group just discussed `popup=popup`.

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: popup=popup<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/491<br>
&lt;andrico1234> masonf: there are 3 bikeshedding issues, that we can't resolve, but it's worth talking about them<br>
&lt;andrico1234> masonf: the  easy one is renaming open, another doesn't have an issue, trigger popup attribute.<br>
&lt;andrico1234> masonf: the important one is, which changes the syntax is the popup attribute itself, a number of reason dominic bought up for considering why wemight want to change.<br>
&lt;bkardell_> q+<br>
&lt;andrico1234> masonf: he's in favour of changing the attribute or value it to something else, another issue i've run into is that popup is a common name<br>
&lt;andrico1234> masonf: in chromium, i've run into collisions when things are named popup, so i' in favour of renaming the attribute from popup to something else<br>
&lt;gregwhitworth> ack bkardell_<br>
&lt;andrico1234> bkardell_: I think popup as an attribute name seemed convenient at first, but might cause trouble. i support an alternative<br>
&lt;tantek> +1 bkardell_<br>
&lt;dandclark> q+<br>
&lt;una> q+<br>
&lt;gregwhitworth> ack dandclark<br>
&lt;andrico1234> dandclark: i agree, i like renaming it to toplayer as a name for the attribute, like bringing an element to the top layer.<br>
&lt;scotto_> +1 to toplayer<br>
&lt;gregwhitworth> ack una<br>
&lt;gregwhitworth> q+<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;andrico1234> una: i think toplayer is a little more explicit if popup is always toplayer, which i think it is. you could have popup inline, or in the top layer, so this could make it more clear if we want to distinguish them. i don't mind popup, but i don't mind toplayer, and it makes sense<br>
&lt;gregwhitworth> ack masonf<br>
&lt;masonf> Proposed resolution: we should rename the `popup` attribute to something else. Naming TBD.<br>
&lt;JonathanNeal> +1 for bikeshedding the name, desiring something more “oem”, namely to avoid patterns like `popup="popup"` which are uncommon (probably for the XML rationale).<br>
&lt;gregwhitworth> +1 to what everyone said<br>
&lt;andrico1234> masonf: i propose that we a resolution that we change the name, and the TBD is the name it should be. i like toplayer but we don't use dashes in attribute names so it looks like "to player". are there objections to naming it?<br>
&lt;tantek> +1 to renaming it<br>
&lt;dandclark> +1 to renaming<br>
&lt;JonathanNeal> +1 to renaming<br>
&lt;scotto_> +1 rename<br>
&lt;davatron5000> toupee-layer<br>
&lt;andrico1234> una: for me, it depends if there's a better option, if toplayer is a bad idea, then what's a name better than popup?<br>
&lt;andrico1234> masonf: we should propose to have a discussion about options for name<br>
&lt;masonf> Proposed resolution: we should rename the `popup` attribute to something else, if we can find a better name.<br>
&lt;JonathanNeal> +1 to it being 'bikeshedding' or 'consider an alternative name' :)<br>
&lt;tantek> +1<br>
&lt;andrico1234> gregwhitworth: please add what your names are so we can keep the bikeshedding discussion short next week<br>
&lt;andrico1234> gregwhitworth: can someone cover for me next week, because there's plenty to cover<br>
&lt;JonathanNeal> `pop="↑"` :P<br>
&lt;gregwhitworth> Resolved: we should rename the `popup` attribute to something else, if we can find a better name.<br>
&lt;gregwhitworth> Zakim, end meeting<br>
&lt;Zakim> As of this point the attendees have been JonathanNeal, flackr, andrico, davatron, dbaron, futekov, scotto_, kylegach, gregwhitworth, dandclark, miriam, masonf, bkardell_, battaglr,<br>
&lt;Zakim> ... una, tantek<br>
&lt;Zakim> RRSAgent, please draft minutes<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2022/03/31-openui-minutes.html Zakim<br>
</details>

<!-- comment id=1084981971 author=mfreed7 created=2022-03-31T18:50:34Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084981971 -->
## @mfreed7 — 2022-03-31

> I think you could fix that by adding `[Unscopable]` to the popup IDL attribute.

Ohh nice. Thanks for that - TIL about `[Unscopable]`. That might alleviate the pressure at least from a compat perspective. I will add that to the prototype right now.

Per the meeting just now, there's a desire to rename the attribute, **if** a better name can be found. We'd like folks to please suggest alternate names, and hopefully we can find something better. A leading contender is something like `toplayer` but the issue with that is that it looks like "To Player". Suggestions welcome.

<!-- comment id=1084984899 author=dandclark created=2022-03-31T18:53:53Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084984899 -->
## @dandclark — 2022-03-31

Maybe `toplevel`?

<!-- comment id=1084985335 author=una created=2022-03-31T18:54:27Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1084985335 -->
## @una — 2022-03-31

Not a direct opinion, just some considerations:

1. `toplayer` also describes other components (ie. `dialog` with `showModal()`). What's the difference between `showModal()` and `showPopup()`?
2. Why not `popup=initial` or `popup=default` instead of `popup=popup`

I don't mind renaming if there is something better, and `toplayer` is more clear RE: what this attribute is affecting. 

That being said, I don't mind `popup` since we have bikeshed this before and landed on it being the best descriptor for a variety of components with shared behavior.

<!-- comment id=1085000115 author=mfreed7 created=2022-03-31T19:10:51Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1085000115 -->
## @mfreed7 — 2022-03-31

While you're in a bikeshedding mood, please don't forget to also look at the other two popup-related issues:

- https://github.com/openui/open-ui/issues/500
- https://github.com/openui/open-ui/issues/508

<!-- comment id=1092063546 author=css-meeting-bot created=2022-04-07T18:19:47Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1092063546 resolution=true -->
## @css-meeting-bot — 2022-04-07

The Open UI Community Group just discussed `[popup] Carryover: popup=popup is unusual #491 (timebox to 10 mins)`, and agreed to the following:

* `RESOLVED: keep the attribute as 'popup' and bikeshed a replacement for the 'popup' *value*.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> Topic: [popup] Carryover: popup=popup is unusual #﻿491 (timebox to 10 mins)<br>
&lt;Travis> https://github.com/openui/open-ui/issues/491<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/491<br>
&lt;hdv> masonf: we discussed this last time, there are two issues we're bikeshedding, most important is the name of the attr itself<br>
&lt;hdv> masonf: suggestions include toplayer, toplevel, topmost<br>
&lt;hdv> masonf: we also should discuss attribute values. Currently a value is `popup`, that's the genesis of this isusues as it seems odd to have a value that is the same as the name of the attribute<br>
&lt;JonathanNeal> How do we request to ask a question again?<br>
&lt;hdv> masonf: another value is `hint`, behaves like tooltip, or @@@<br>
&lt;bkardell_> q followed by +<br>
&lt;hdv> JonathanNeal: my question about popup=popup, was the concern this conforming to a pattern in HTML attributes where they don't tend to mimic the value of the attr name? is that a concern?<br>
&lt;hdv> masonf: that's why this issue was raised by Domenic, generally because generally not used in HTML, doesn't seem to be a hard rule, but ideally something to avoid, is how I interpreted that<br>
&lt;Travis> q?<br>
&lt;JonathanNeal> q+<br>
&lt;flackr> +1 I like the name, and would prefer to see popup value change<br>
&lt;hdv> masonf: personally I like the word popup, it concisely and accurately describes the behavior, so perhaps the best is to find a better value for the default 'normal' popup<br>
&lt;hdv> s/the word/the attribute name<br>
&lt;hdv> q?<br>
&lt;flackr> q+<br>
&lt;hdv> ack Jon<br>
&lt;hdv> JonathanNeal: was the issue with the role and aria-modal=false still an issue?<br>
&lt;hdv> masonf: basically we sidestepped the role discussion because we moved away from choosing a specific element and towards using an attribute<br>
&lt;hdv> JonathanNeal: is the conflict resolved for hint, if aria-modal=false?<br>
&lt;hdv> masonf: could you be more specific about what the issue was?<br>
&lt;hdv> JonathanNeal: I learned the different values for popup were related to specific roles for popup, and I saw in an issue somewhere that that would not be allowed for a popup? this issue is linked from 491<br>
&lt;hdv> JonathanNeal: if I know what it reflects, I might be better at bikeshedding names<br>
&lt;JonathanNeal> it was in #﻿410<br>
&lt;hdv> masonf: sounds like that is an older, popup element discussion<br>
&lt;hdv> s/491/410<br>
&lt;hdv> scotto: I think it has been misread, we should probably review again<br>
&lt;dandclark> q+<br>
&lt;hdv> masonf: I'll close the issue where we decided not to go for a popup element<br>
&lt;hdv> q?<br>
&lt;hdv> ack flackr<br>
&lt;JonathanNeal> I am sorry if my questions did not make sense, but I am glad it helped close an issue.<br>
&lt;hdv> flackr: I like the name popup for the attribute, so I agree we probably need to find a new  name for the popup value<br>
&lt;hdv> dandclark: I would support a resolution that keeps the popup attribute name<br>
&lt;hdv> dandclark: I also like una's suggestion of popup=default<br>
&lt;masonf> or maybe 'standard'<br>
&lt;hdv> dandclark: I like framing the popup behavior as the default thing, seems reasonable<br>
&lt;masonf> q?<br>
&lt;hdv> ack dan<br>
&lt;una> I also like `popup=default` :)<br>
&lt;hdv> dbaron: just trying to think of uses for popup, or the default popup or whatever we want ot call it… I haven't been following this for long… a default kind of popup is something that is attached to some control… the others seem more loosely attached?<br>
&lt;una> q+<br>
&lt;hdv> dbaron: a tooltip doesn't feel like it is attached to any specific place? maybe this is useful in terms of naming?<br>
&lt;JonathanNeal> Wild Idea — but since I am unclear on the story where these are serialized values shipped with initial HTML, I am in favor of popup being a property or a css property.<br>
&lt;hdv> ack dbaron<br>
&lt;masonf> Proposed resolution: keep the attribute as 'popup' and bikeshed a replacement for the 'popup' *value*.<br>
&lt;Travis> aria-haspopup=true<br>
&lt;masonf> travis, that goes on the element that triggers the popup.<br>
&lt;hdv> una: there are so many use cases for popup… like you might want a standalone dialog experience, or a menu that is dismissable, some may be attached during positioning some might not … I like the popup attribute<br>
&lt;hdv> una: I could also imagine popup=true or just having popup would trigger the default behavior<br>
&lt;masonf> RESOLVED: keep the attribute as 'popup' and bikeshed a replacement for the 'popup' *value*.<br>
&lt;Travis> https://github.com/openui/open-ui/issues/500<br>
</details>

<!-- comment id=1098536551 author=flackr created=2022-04-13T22:17:28Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1098536551 -->
## @flackr — 2022-04-13

I think it would be helpful to look back to the [behavioral differences](https://open-ui.org/components/popup.research.explainer#classes-of-ui-dismiss-behavior-and-interactions)

Roughly:
* popup implies uniqueness, dismisses only on an explicit action.
* hint implies uniqueness, dismisses on action or timeout.
* async is not unique, and only timeout dismissal.

There is also the subtle difference that opening a popup dismisses popups and hints where opening a hint only dismisses hints, however it's a bit unclear to me in what use case you would be opening a hint that wasn't nested in the current popup when any interaction with this element would cause dismissal of the popup.

With this in mind, I think I would propose the main popup value be `sync` (the opposite of async). I'd also consider `modal` since while it doesn't force you to interact with the popup (due to light dismiss), you're still not able to interact with any content outside of the popup without first implicitly dismissing the popup.

<!-- comment id=1098574018 author=scottaohara created=2022-04-13T23:32:36Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1098574018 -->
## @scottaohara — 2022-04-13

FWIW, I like the `sync` suggestion.

I'd be very wary of `modal`, and to Una's question:

>What's the difference between showModal() and showPopup()

I would not consider these to be the same.  A popup doesn't / shouldn't have to be modal by default.  E.g., [the example i mention in this issue](https://github.com/openui/open-ui/issues/420), and others I can think of but don't want to bog this issue down (but would be happy to discuss if interested).   Rather, I'd expect that if someone needed a modal popup, they should use other features to do so (e.g., `inert` or maybe they're really just invoking a modal dialog at that point).

<!-- comment id=1099042571 author=zcorpan created=2022-04-14T10:33:43Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1099042571 -->
## @zcorpan — 2022-04-14

Related: https://github.com/openui/open-ui/issues/514

<!-- comment id=1100266015 author=mfreed7 created=2022-04-15T18:01:26Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1100266015 -->
## @mfreed7 — 2022-04-15

I'll leave the bikeshedding going for values, thanks for all of the suggestions! But I wanted to talk quickly about this part:

> There is also the subtle difference that opening a popup dismisses popups and hints where opening a hint only dismisses hints, however it's a bit unclear to me in what use case you would be opening a hint that wasn't nested in the current popup when any interaction with this element would cause dismissal of the popup.

The use case that this behavior has in mind is a popup (for example a select menu) that is open on one part of the page, and the user moves the mouse over another element on the page (separate from the select menu) and hovers it. That hover triggers a tooltip which is unrelated to the select menu, so it should not close the select's listbox. But it should also show up, so they can read the tooltip.

> Rather, I'd expect that if someone needed a modal popup, they should use other features to do so (e.g., `inert` or maybe they're really just invoking a modal dialog at that point).

Also a very quick +1 to this comment. I think "modal popup" should be an oxymoron. If you want something truly "modal", you should use the `<dialog>` element and its `.showModal()` invoker. Popups are **by definition** transient, and transient things can't be modal.

> Related: #514

Thanks for the reference - for context, this issue raises the possibility of allowing just `<div popup>` (without an explicit value) to signify the "normal" case. I.e. equivalent to the current proposal's `<div popup=popup>`. I like this idea for two reasons: 1) It makes the common case easier to type, and 2) it avoids what appear to be some awkward naming choices for this default behavior's value.

<!-- comment id=1112626742 author=css-meeting-bot created=2022-04-28T20:30:25Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1112626742 -->
## @css-meeting-bot — 2022-04-28

The Open UI Community Group just discussed `[popup] Bikeshed: popup=popup is unusual #491`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> Topic: [popup] Bikeshed: popup=popup is unusual #﻿491<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/491<br>
&lt;hdv> masonf: This one is bikeshedding on the popup attribute… the attribute name is popup and will stay popup, but the question is, what should the values be named?<br>
&lt;JonathanNeal> q+<br>
&lt;hdv> masonf: one is popup=popup, it's like popup with 'normal rules', see explainer, and then popup=hint and popup=async (like a toast; no light dismiss)<br>
&lt;hdv> masonf: those ar the three we've currently proposed<br>
&lt;hdv> s/ar/are<br>
&lt;hdv> masonf: naming of all three is in question, but let's start with the one for the normal behavior<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> masonf: maybe it could be leaving the popup attribute without value<br>
&lt;bkardell_> &lt;div popup="" vs &lt;div popup ?which one masonf ?<br>
&lt;hdv> JonathanNeal: had it been considered to have the empty and popup value mean the same thing? So that way the boolean continued to work and the XML variant continued to work?<br>
&lt;hdv> masonf: I don't now if it has been explicitly proposed, not sure if anyone disagrees with allowing that<br>
&lt;JonathanNeal> q?<br>
&lt;flackr> q+<br>
&lt;JonathanNeal> ack JonathanNeal<br>
&lt;hdv> [ +1 sounds good to me to have that variant as well ]<br>
&lt;JonathanNeal> ack flackr<br>
&lt;dandclark> q+<br>
&lt;bkardell_> popup="plain | extra-cheese | peperoni"<br>
&lt;hdv> flackr: I would disagree with allowing both… the popup attribute has the general attribute of producing a popup… it would be weird if the popup attribute would have a value that doesnt meant the same as the popup<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> masonf:  are you disagreeing with allowing both?<br>
&lt;hdv> flackr: just with requiring it<br>
&lt;JonathanNeal> q?<br>
&lt;bkardell_> q+<br>
&lt;hdv> dandclark: I agree with flackr on this… it's difficult to talk about, I prefer to just have the boolean attribute, not a fan of allowing both the boolean and popup=popup. Multiple ways to do the same is confusing and popup=popup seems confusing<br>
&lt;JonathanNeal> ack dandclark<br>
&lt;JonathanNeal> ack bkardell_<br>
&lt;hdv> bkardell_: to be sure I'm understanding correctly… popup with no `=` is what we're saying, right?<br>
&lt;hdv> bkardell_: what if you did empty string? `popup=""`<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> masonf: that's how we would technically have to implement as `popup` and `popup=""` are technically equivalent<br>
&lt;hdv> bkardell_: how would you remove the popupness?<br>
&lt;hdv> masonf: remove the attribute?<br>
&lt;hdv> s/?//<br>
&lt;hdv> bkardell_: what if you add anything in the value?<br>
&lt;hdv> s/bkardell_/JonathanNeal<br>
&lt;bkardell_> so that means that "" is explcitly an allowed value?<br>
&lt;hdv> JonathanNeal: was thinking when I was passing a value to something in a template, it is possible for me to pass an empty string in…<br>
&lt;hdv> flackr: we could consider explicitly supporting popup=false or popup=none<br>
&lt;hdv> JonathanNeal: am more thinking about the default value…<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> scotto: if I add required="lol" if I add it into an input it would still be required… <br>
&lt;hdv> masonf: but that's a boolean attribute<br>
&lt;hdv> scotto: right, but the same goes for the hidden attribute<br>
&lt;hdv> masonf: that was done for backwards compat, which we're not constrained by here… but if it's a pattern… <br>
&lt;flackr> q+<br>
&lt;davatron5000> reminds me of checked=checked<br>
&lt;hdv> bkardell_: I think the boolean nature is somewhat confusing on this stuff<br>
&lt;hdv> bkardell_: I would almost consider giving it a name, like popup=simple or something<br>
&lt;JonathanNeal> Is `popup=""` synonymous for `popup="default"` an option? Sorry if this is too similar to my earlier question.<br>
&lt;hdv> bkardell_: because there are parallels with like hidden that was a boolean before but no longer<br>
&lt;hdv> bkardell_: if we just give it a name all these problems go away<br>
&lt;hdv> masonf: good point…<br>
&lt;JonathanNeal> ack flackr<br>
&lt;hdv> flackr: the advantage of treating any invalid value as the default popup behavior is that even if we decided the name would be popup, or others, it would just resolve to popup behavior, you could then even do popup=popup in your XML if you needed to<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> JonathanNeal: do we have a resolution on this?<br>
&lt;hdv> masonf: I didn't hear consensus on anything specifically… but maybe if we allow anything that's not hint or async it would be the default and then most of the things we talked about would work (like popup=popup)<br>
&lt;hdv> flackr: on the issue there was 'sync', because it was kind of the opposite of 'async'<br>
&lt;bkardell_> scotto: you htink that popup="blah" should be work like current 'popup=popup', right?<br>
&lt;bkardell_> q+<br>
&lt;hdv> una: I thought the original issue was popup=popup was a problem… so popup=default instead of popup=popup could be a solution<br>
&lt;JonathanNeal> +1 to the popup="" / popup="default" option<br>
&lt;hdv> una:  and then we don't need to worry about figuring out what a no value would mean… so default could be  default<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> scotto: I like default<br>
&lt;dandclark> I like popup="default".<br>
&lt;masonf> Proposed resolution: the new value for "standard popup" should be "default", and invalid values should resolve to a "standard popup".<br>
&lt;hdv> bkardell_: I also like default… but backtracking a little… the convo we had before about what happens with another value, is pertinent to this<br>
&lt;hdv> una: I agree that needs to be sorted out<br>
&lt;dandclark> q+ to mention compatibility concern<br>
&lt;hdv> bkardell_: so you could use popup=whatever and it would be the same (even popup=false, unfortunately but pretty normal)<br>
&lt;JonathanNeal> q?<br>
&lt;masonf> q+<br>
&lt;JonathanNeal> ack bkardell_<br>
&lt;JonathanNeal> ack dandclark<br>
&lt;Zakim> dandclark, you wanted to mention compatibility concern<br>
&lt;Alexander_Futekov> q+<br>
&lt;hdv> dandclark: for the second part of that resolution… what if we're introducing a new popup type later, might that lead to web compat issues?<br>
&lt;hdv> dandclark: that might be a potential downside?<br>
&lt;JonathanNeal> q?<br>
&lt;JonathanNeal> ack masonf<br>
&lt;hdv> masonf: I was on the queue for the same… I would be a bit concerned about that… there is another issue defining the behaviors of the other types, I am not completely convinced that they're the only types we'll ever define<br>
&lt;JonathanNeal> +1 to the popup="" / popup="default" option<br>
&lt;hdv> scotto: there is a way as long as there is a default value… if it is expected that you have to use one of these values to use it, I don't have the concern for an invalid value to default to something<br>
&lt;hdv> scotto: I was concerned because with other attrs that work like defaulting to something there's an issue, was more of an author education point<br>
&lt;hdv> Alexander_Futekov: I am not convinced that we should have a default option because not sure how this will scale… if we have a popup that is async and one that is a hint, and another one that is popup, then what is popup? why not use popup=modal?<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> masonf: because it is technically not a modal<br>
&lt;hdv> Alexander_Futekov: I think we have to find a term that says more<br>
&lt;hdv> masonf:  what about default?<br>
&lt;JonathanNeal> if `popup="default"` is a default popup, then is `popup="hint"` a hint popup?<br>
&lt;JonathanNeal> q?<br>
&lt;JonathanNeal> ack Alexander_Futekov<br>
&lt;hdv> Alexander_Futekov: it's the most common pattern now, but might not be in the future?<br>
&lt;hdv> JonathanNeal: is popup=default describable as a 'default popup', or like popup=hint can be described as a 'hint popup'?<br>
&lt;bkardell_> popup="2022"<br>
&lt;hdv> masonf: the hint popup makes sense, but as Alexander_Futekov raised what is that?<br>
&lt;hdv> una: if we use another term we still need to define it<br>
&lt;hdv> scotto: I agree with that, there is a common expectation of what the default is and that can be defined<br>
&lt;hdv> scotto: if other popups come to be, we define those as well<br>
&lt;hdv> scotto: there has to be a default<br>
&lt;JonathanNeal> q?<br>
&lt;masonf> Proposed resolution: we should pick a new value for "standard popup" which isn't the word "popup", and invalid values should behave as if the `popup` attribute is not present.<br>
&lt;hdv> JonathanNeal: so does that mean your proposed resolution would stay the same?<br>
&lt;scotto> +1 to what mason just said<br>
&lt;Alexander_Futekov> +1<br>
&lt;dandclark> +1<br>
&lt;hdv> +1<br>
&lt;JonathanNeal> +1<br>
&lt;hdv> bkardell_: if we had no fallback, let's say chromium implement, say, spicy popup, but firefox only implemented bland regular popup, the end user experience would do nothing?<br>
&lt;hdv> una: I don't know if we can resolve this today<br>
&lt;hdv> JonathanNeal: as someone who likes to write polyfill things… getting the raw value and feature detecting, that's helpful… not opposed in that sense<br>
&lt;bkardell_> computedPopup :)<br>
&lt;hdv> masonf: so as long as you could feature detect you would be happy?<br>
&lt;bkardell_> that is the opposite of what most features in html do, no?<br>
&lt;hdv> JonathanNeal: it is harder when the browser throws it away and I have no access to it, than when the browser says this is what I thought I got… <br>
&lt;hdv> masonf: great feedback thanks<br>
&lt;hdv> JonathanNeal: looks like we won't achieve a resolution on that today<br>
&lt;hdv> JonathanNeal: thanks everyone for attending today!<br>
&lt;bkardell_> hdv: thanks for scribing!<br>
&lt;hdv> RRSAgent, end meeting please<br>
&lt;RRSAgent> I'm logging. I don't understand 'end meeting', hdv.  Try /msg RRSAgent help<br>
&lt;JonathanNeal> hdv: thank you!!!<br>
&lt;hdv> RRSAgent, make minutes please<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2022/04/28-openui-minutes.html hdv<br>
&lt;hdv> Zakim, end meeting<br>
&lt;Zakim> As of this point the attendees have been masonf, flackr, JonathanNeal, davatron, miriam, andrico, una, scotto, bkardell_, dbaron<br>
&lt;Zakim> RRSAgent, please draft minutes<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2022/04/28-openui-minutes.html Zakim<br>
&lt;Zakim> I am happy to have been of service, hdv; please remember to excuse RRSAgent.  Goodbye<br>
</details>

<!-- comment id=1113607241 author=mfreed7 created=2022-04-29T18:36:22Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1113607241 -->
## @mfreed7 — 2022-04-29

Just to highlight and summarize what I heard in the [discussion](https://github.com/openui/open-ui/issues/491#issuecomment-1112626742):
1. There was fairly good consensus that we should **rename the "standard popup" value** from `popup` to something else, to avoid `popup=popup`.
2. There was significant discussion about the behavior for "invalid" values. While it might be desirable to allow invalid values to default to "standard popup" behavior, that would come with fairly significant forward-compatibility issues, if we ever want to add or change values for this attribute. So I think we reached rough agreement that **invalid values should behave as if there is no `popup` attribute present**, potentially with a console warning.
3. We discussed, at length, the option to give the `popup` attribute a "boolean-like" value, where `<div popup>` would confer "standard popup" behavior. However, that came with additional complications, such as behavior in frameworks (where a value like `true` might be assigned), whether empty values (`<div popup="">`) would behave the same way, and whether there should **also** be a string name for this behavior. In the end, I think we reached rough agreement that we should **not pursue a "boolean-like" behavior**. 
4. I feel that we were close to agreement that we should simply **pick a better name for "standard popup" behavior**, perhaps one that better describes what "standard popup" behavior [actually is](https://open-ui.org/components/popup.research.explainer#classes-of-ui-dismiss-behavior-and-interactions). Some suggestions that were proposed include: `default`, `standard`, and `sync`. Other suggestions welcome.
5. There wasn't a lot of discussion about `hint` and `async` values at all.

The above is just what I heard - please feel free to chime in if I missed something or if you disagree with anything. Otherwise, suggestions for a **value** that means "standard popup" with [these behaviors](https://open-ui.org/components/popup.research.explainer#classes-of-ui-dismiss-behavior-and-interactions) would be appreciated.

<!-- comment id=1117526245 author=mfreed7 created=2022-05-04T15:58:26Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1117526245 -->
## @mfreed7 — 2022-05-04

I added agenda+ to try to resolve on the bolded phrases in [the comment above](https://github.com/openui/open-ui/issues/491#issuecomment-1113607241). Please chime in here if you disagree with those points.

Assuming general agreement, we need to pick a name for the "standard popup" value. Please either **vote with emojis**, or propose an alternative (in which case I'll edit this comment to add your suggestion):
- `popup=default` 🚀 
- `popup=standard` 😄 
- `popup=sync` 👍 
- `popup=auto` 🎉 

[Edit: based on [this comment](https://github.com/openui/open-ui/issues/495#issuecomment-1117721005), I've added another alternative, `popup=auto`. Feel free to change your vote, if you want!]

<!-- comment id=1117527686 author=domenic created=2022-05-04T15:59:42Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1117527686 -->
## @domenic — 2022-05-04

Strong -1 for `popup=standard`. All `popup=""` values will be standard, hopefully.

<!-- comment id=1118927375 author=css-meeting-bot created=2022-05-05T18:41:56Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1118927375 -->
## @css-meeting-bot — 2022-05-05

The Open UI Community Group just discussed `[popup] Bikeshed: popup=popup is unusual`, and agreed to the following:

* ``RESOLVED:  rename the "standard popup" value to something else (to be chosen), and make invalid values, including empty/boolean-like values, behave as if there is no popup attribute present. Also, the IDL reflection of the `popup` value should reflect what was used, to allow feature detection.``

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [popup] Bikeshed: popup=popup is unusual<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/491<br>
&lt;gregwhitworth> q?<br>
&lt;bkardell_> masonf: We discussed this last week and we got close but didn't reach a resolution<br>
&lt;bkardell_> masonf: we're trying to find a value for popup="the normal thing"<br>
&lt;bkardell_> masonf: we discussed boolean, we talked about what happens for invalid values - both ran into troubles.<br>
&lt;gregwhitworth> q?<br>
&lt;flackr> q+<br>
&lt;bkardell_> masonf: the rough concensus I thought I heard is we should rename with an explicit name, and make it so invalid values act as if there is no value at all and don't allow an empty boolean value<br>
&lt;gregwhitworth> ack flackr<br>
&lt;bkardell_> flackr: I generally like the proposal. Can developers test whether it resolved correctly<br>
&lt;bkardell_> masonf: yeah, we discussed that - to make the idl reflection only reflect the used value<br>
&lt;bkardell_> flackr: that sounds great<br>
&lt;masonf> Proposed resolution:  rename the "standard popup" value to something else (to be chosen), and make invalid values, including empty/boolean-like values, behave as if there is no popup attribute present.<br>
&lt;gregwhitworth> bkardell_: is there an attribute that works like that today?<br>
&lt;bkardell_> JonathanNeal: if you create an input with a type"willywonka" the .type is "text"<br>
&lt;bkardell_> bkardell_: cool<br>
&lt;bkardell_> flackr: it is consistent with what we decided<br>
&lt;bkardell_> bkardell_: yay!<br>
&lt;masonf> Proposed resolution:  rename the "standard popup" value to something else (to be chosen), and make invalid values, including empty/boolean-like values, behave as if there is no popup attribute present. Also, the IDL reflection of the `popup` value should reflect what was used, to allow feature detection.<br>
&lt;bkardell_> +1 to the resolution<br>
&lt;flackr> +1<br>
&lt;dandclark> +1<br>
&lt;bkardell_> resolved<br>
&lt;masonf> RESOLVED:  rename the "standard popup" value to something else (to be chosen), and make invalid values, including empty/boolean-like values, behave as if there is no popup attribute present. Also, the IDL reflection of the `popup` value should reflect what was used, to allow feature detection.<br>
</details>

<!-- comment id=1119094349 author=mfreed7 created=2022-05-05T22:17:12Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1119094349 -->
## @mfreed7 — 2022-05-05

Ok, per the resolution, we just need to pick a name. **[Please vote](https://github.com/openui/open-ui/issues/491#issuecomment-1117526245)!** I'd like to bring this back up next week and resolve on the name that gets the most votes here.

<!-- comment id=1120274849 author=danielfilho created=2022-05-07T19:43:37Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1120274849 -->
## @danielfilho — 2022-05-07

I like the idea of `popup=auto`, given the nature of `auto` being used as the default value for a lot of CSS attributes.

I'm aware that this is not CSS, but I like the implicit standard that it brings. It "feels familiar" :)

Thanks for the effort, anyways 🥇 

<!-- comment id=1120281343 author=getify created=2022-05-07T20:15:04Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1120281343 -->
## @getify — 2022-05-07

None of the currently proposed values make sense to me, TBH. I would gravitate to:

* `popup=basic`
* `popup=simple`
* `popup=native`

<!-- comment id=1120286046 author=AmeliaBR created=2022-05-07T20:39:58Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1120286046 -->
## @AmeliaBR — 2022-05-07

I've just read the [description of what the values should do](https://open-ui.org/components/popup.research.explainer#classes-of-ui-dismiss-behavior-and-interactions). From that, I understand that the value you want to name is an exclusive popup. It closes all other pop-ups/dialogs (except its ancestors) when it is opened; and it is automatically closed by any other pop-up or dialog opening.

I would not have guessed this based on a name like `popup`, `default`, `standard`. *Maybe* `auto` makes sense in that it triggers an auto-closing behavior, but that isn't clear; `auto` could just as easily refer to an auto-opening behavior which might be added in the future!

Of the [choices in the poll](https://github.com/openui/open-ui/issues/491#issuecomment-1117526245), I would therefore vote for `sync` because it contrasts against `async` for the non-exclusive option.  But, I don't really think that `sync` and `async` are the correct words for either.  This isn't just about time-ordering, it's about whether it interrupts other actions.

I would therefore vote for changing *both* these values to something that more clearly defines the difference between them:

- `popup=exclusive` vs `non-exclusive`
- `popup=unique` vs `multiple`
- `popup=only` vs `many`

<!-- comment id=1121494078 author=mfreed7 created=2022-05-09T19:32:00Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1121494078 -->
## @mfreed7 — 2022-05-09

Thanks for all of the great new suggestions for names here! From the [original poll](https://github.com/openui/open-ui/issues/491#issuecomment-1117526245), `popup=auto` got the most votes, with 7, followed closely by `popup=default` with 5 votes. Since there have been some new additions, I'm inclined to try to re-organize them here for a runoff poll, where I've included only `auto` and `default` from the original:

- `popup=auto` 🎉
- `popup=default` 🚀
- `popup=basic` 👍 
- `popup=simple` 😄 
- `popup=native` 😕 
- `popup=exclusive` ❤️ 
- `popup=unique` 👀 
- `popup=only` 👎  (sorry, last available emoji)

Please **vote** early and often.

<!-- comment id=1121669600 author=zcorpan created=2022-05-09T23:07:41Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1121669600 -->
## @zcorpan — 2022-05-09

I'll note that in #500 the `initiallyopen` attribute was renamed to `defaultopen`. So when used together the options here would look like:

- `<div popup=auto defaultopen>...</div>`
- `<div popup=default defaultopen>...</div>`
- `<div popup=basic defaultopen>...</div>`
- `<div popup=simple defaultopen>...</div>`
- `<div popup=native defaultopen>...</div>`
- `<div popup=exclusive defaultopen>...</div>`
- `<div popup=unique defaultopen>...</div>`
- `<div popup=only defaultopen>...</div>`

<!-- comment id=1130422995 author=mfreed7 created=2022-05-18T19:28:38Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1130422995 -->
## @mfreed7 — 2022-05-18

Reminder: I'm looking for a final resolution on a name here at the meeting **tomorrow**.

Please **[vote](https://github.com/openui/open-ui/issues/491#issuecomment-1121494078)**!

<!-- comment id=1130444626 author=mfreed7 created=2022-05-18T19:36:31Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1130444626 -->
## @mfreed7 — 2022-05-18

Also note that https://github.com/openui/open-ui/issues/533 reopens a part of [our last resolution on this issue](https://github.com/openui/open-ui/issues/491#issuecomment-1118927375). But it contains some potentially-new context, and is worth a read.

<!-- comment id=1132078846 author=mfreed7 created=2022-05-19T18:53:21Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1132078846 -->
## @mfreed7 — 2022-05-19

The Open UI Community Group just discussed `[popup] Bikeshed: popup=popup is unusual`, and agreed to the following:

* `` RESOLVED:  Rename `popup=popup` to `popup=auto`. ``

<details>
<summary>The full IRC log of that discussion</summary>
<pre>
[11:36] <hdv> masonf: another bikeshedding issue… we talked about it many times. For the value we used to call popup=popup, we want to replace the popup value with something else.
[11:36] <hdv> masonf: this one got a bit more voting… it got down to two: popup=auto and popup=default… currently 'auto' is winning by 1
[11:37] <gregwhitworth> q+
[11:37] * Zakim sees gregwhitworth on the speaker queue
[11:37] <hdv> masonf: multiple people voted for multiple options
[11:37] <hdv> masonf: resolution would be to go for 'auto'… 
[11:37] <gregwhitworth> ack gregwhitworth
[11:37] * Zakim sees no one on the speaker queue
[11:37] <masonf> q+
[11:37] * Zakim sees masonf on the speaker queue
[11:37] <bkardell_> I feel the opposite for the same reasons :)
[11:37] <hdv> gregwhitworth: I like auto it feels it describes well what it tries to do
[11:38] <gregwhitworth> ack masonf
[11:38] * Zakim sees no one on the speaker queue
[11:38] <JonathanNeal> +1 to `auto` if we wanted to resolved.
[11:38] <hdv> masonf: there is a separate discussion too about what to do with 'no value'  situation
[11:38] <JonathanNeal> q+
[11:38] * Zakim sees JonathanNeal on the speaker queue
[11:39] <gregwhitworth> ack JonathanNeal
[11:39] * Zakim sees no one on the speaker queue
[11:39] <hdv> JonathanNeal: I like all of this… my question is since top voted things were `auto` and `default`… we have been discuss `default` as a different meaning for things in a reflection sense… do we think that should influence our decision here? Sometimes in attribute name default takes on a different meaning, which is why I preferred auto
[11:40] <hdv> JonathanNeal: what do others think?
[11:40] <masonf> q+
[11:40] * Zakim sees masonf on the speaker queue
[11:40] <hdv> JonathanNeal: I wanted to avoid defualt as it started to take on meaning on the web that would not be useful here
[11:40] <gregwhitworth> ack masonf
[11:40] * Zakim sees no one on the speaker queue
[11:40] <hdv> masonf: I like that point, default does have some other meaning… but auto also has a different meaning in CSS, auto is the default
[11:40] <JonathanNeal> +1 to mason maybe +1’ing me :)
[11:40] <hdv> gregwhitworth: was my point too, 'do the thing you're supposed to do'
[11:41] <masonf> Proposed resolution: Rename `popup=popup` to `popup=auto`.
[11:41] <JonathanNeal> +1
[11:41] <flackr> +1
[11:41] <hdv> [ fantasai did a fantastic talk on `auto` in CSS https://vimeo.com/134597090 ]
[11:41] <hdv> [ matches what gregwhitworth and others say about it ]
[11:42] <masonf> RESOLVED: Rename `popup=popup` to `popup=auto`.
[11:42] <JonathanNeal> up next: rename space to tabs
[11:42] <hdv> noooo
</pre>
</details>

<!-- comment id=1132079610 author=mfreed7 created=2022-05-19T18:54:23Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1132079610 -->
## @mfreed7 — 2022-05-19

(Above manually posted from [these minutes](https://www.w3.org/2022/05/19-openui-minutes.html#t03).)

I'm going to close this issue! Thanks everyone for the great contributions and discussion around this overall topic.

<!-- comment id=1132165724 author=css-meeting-bot created=2022-05-19T20:18:08Z url=https://github.com/openui/open-ui/issues/491#issuecomment-1132165724 -->
## @css-meeting-bot — 2022-05-19

The Open UI Community Group just discussed `[popup] Bikeshed: popup=popup is unusual #491`, and agreed to the following:

* ``RESOLVED: Rename `popup=popup` to `popup=auto`.``

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> Topic: [popup] Bikeshed: popup=popup is unusual #﻿491<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/491<br>
&lt;hdv> masonf: another bikeshedding issue… we talked about it many times. For the value we used to call popup=popup, we want to replace the popup value with something else.<br>
&lt;hdv> masonf: this one got a bit more voting… it got down to two: popup=auto and popup=default… currently 'auto' is winning by 1<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> masonf: multiple people voted for multiple options<br>
&lt;hdv> masonf: resolution would be to go for 'auto'… <br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;masonf> q+<br>
&lt;bkardell_> I feel the opposite for the same reasons :)<br>
&lt;hdv> gregwhitworth: I like auto it feels it describes well what it tries to do<br>
&lt;gregwhitworth> ack masonf<br>
&lt;JonathanNeal> +1 to `auto` if we wanted to resolved.<br>
&lt;hdv> masonf: there is a separate discussion too about what to do with 'no value'  situation<br>
&lt;JonathanNeal> q+<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;hdv> JonathanNeal: I like all of this… my question is since top voted things were `auto` and `default`… we have been discuss `default` as a different meaning for things in a reflection sense… do we think that should influence our decision here? Sometimes in attribute name default takes on a different meaning, which is why I preferred auto<br>
&lt;hdv> JonathanNeal: what do others think?<br>
&lt;masonf> q+<br>
&lt;hdv> JonathanNeal: I wanted to avoid defualt as it started to take on meaning on the web that would not be useful here<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> masonf: I like that point, default does have some other meaning… but auto also has a different meaning in CSS, auto is the default<br>
&lt;JonathanNeal> +1 to mason maybe +1’ing me :)<br>
&lt;hdv> gregwhitworth: was my point too, 'do the thing you're supposed to do'<br>
&lt;masonf> Proposed resolution: Rename `popup=popup` to `popup=auto`.<br>
&lt;JonathanNeal> +1<br>
&lt;flackr> +1<br>
&lt;hdv> [ fantasai did a fantastic talk on `auto` in CSS https://vimeo.com/134597090 ]<br>
&lt;hdv> [ matches what gregwhitworth and others say about it ]<br>
&lt;masonf> RESOLVED: Rename `popup=popup` to `popup=auto`.<br>
&lt;JonathanNeal> up next: rename space to tabs<br>
&lt;hdv> noooo<br>
&lt;gregwhitworth> Zakim, end meeting<br>
&lt;Zakim> As of this point the attendees have been hdv, gregwhitworth, miriam, JonathanNeal, tantek, scotto, bkardell_, emilio, flackr, masonf, dbaron<br>
&lt;Zakim> RRSAgent, please draft minutes<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2022/05/19-openui-minutes.html Zakim<br>
&lt;Zakim> I am happy to have been of service, gregwhitworth; please remember to excuse RRSAgent.  Goodbye<br>
&lt;masonf> nice job minuting as always hdv<br>
&lt;JonathanNeal> Yes! thank you so much, hdv.<br>
&lt;masonf> It doesn't appear to have copied the minutes or the resolution to #﻿491. I'll copy/paste them...<br>
</details>
