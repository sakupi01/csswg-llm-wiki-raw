---
number: 321
title: "[Popup] Should light dismiss be cancellable on popup?"
state: "closed"
labels: ["popover"]
author: "melanierichards"
created_at: "2021-04-09T21:36:37Z"
closed_at: "2022-05-26T19:31:13Z"
url: "https://github.com/openui/open-ui/issues/321"
comments: 7
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

On [MS Edge Explainers #457](https://github.com/MicrosoftEdge/MSEdgeExplainers/issues/457), @mfreed7 asked:

> During implementation, the question has come up about whether the Escape key light dismiss behavior (or really, any of the light dismiss behavior such as clicking outside) should be author-cancellable. I.e. user hits Escape, but author has added 'keydown' event handler on document.body, and does preventDefault(). Should the currently-top popup be hidden?

@domenic said:

> First, I think the platform needs to have both author-cancelable and non-cancelable close signals. Notably, exiting fullscreen cannot be author-cancelable. But, exiting a `<dialog>` is (and we have some strong feedback that it should continue to be).
> 
> Second, I think that the behavior should be uniform. So, you can prevent dialog closing by `preventDefault()`ing <kbd>Esc</kbd> on `document`. It also has an explicit `cancel` event which can be `preventDefault()`ed. (But, its `close` cannot be `preventDefault()`ed; that happens after the fact.) This means that, IMO, anything with cancelable close behavior should behave the same way, both in terms of `keydown` event listeners (on desktop) and also having a `cancel` event where applicable.
> 
> (Note: the modal close signals explainer was written before I realized this about dialog. Per https://github.com/slightlyoff/history_api/issues/13, we're probably going to update the event name for `ModalCloseWatcher` in the way the above paragraph discusses, i.e. change `beforeclose` to `cancel`, to match `<dialog>`. Maybe we'll even rename it to `ModalCancelWatcher`.)
> 
> I don't have a strong opinion on which category `<popup>` should go in. @melanierichards mentioned in #455 that the current plan is for it to be in the uncancelable category. If so, then I think it should behave like fullscreen and keydown listeners shouldn't be able to stop it, and it shouldn't have a `cancel` event, etc.
> 
> Now, there's an interesting wrinkle for author-cancelable close signals regarding the Android back button, which is that canceling them can be used to trap the user by disabling the back button. Our modal close signals explainer has some mitigations for this, and for platform predictability, it applies those mitigations on all platforms.

<!-- comment id=816986040 author=melanierichards created=2021-04-09T21:38:51Z url=https://github.com/openui/open-ui/issues/321#issuecomment-816986040 -->
## @melanierichards — 2021-04-09

My personal opinion remains: I recognize authors will want to cancel dismissal, but I think this could open up a vector for abuse. I would rather we try and explore solutions to the legitimate use cases for cancelling popup dismissal, e.g. an event or pseudo selector authors can hook into in order to style popups / animate them out as they are dismissed.

<!-- comment id=820516498 author=flackr created=2021-04-15T15:23:52Z url=https://github.com/openui/open-ui/issues/321#issuecomment-820516498 -->
## @flackr — 2021-04-15

FYI I started #335 to brainstorm / discuss how we could animate popups appearance / dismissal.

<!-- comment id=1072903391 author=github-actions[bot] created=2022-03-19T00:31:47Z url=https://github.com/openui/open-ui/issues/321#issuecomment-1072903391 -->
## @github-actions[bot] — 2022-03-19

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=1082522262 author=mfreed7 created=2022-03-30T01:25:59Z url=https://github.com/openui/open-ui/issues/321#issuecomment-1082522262 -->
## @mfreed7 — 2022-03-30

We need to re-think the requirements for this issue, in the context of the new [Popup API](https://open-ui.org/components/popup.research.explainer). In the new explainer, hiding the popup can be overridden via developer CSS (e.g. set `display:block` to override the UA's `display:none` rule), with the exception of top layer status. That last bit might make animations tough, though, since the element will immediately be removed from the top layer, and that alone might make it obscured by another element, so that the animation can't be seen.

<!-- comment id=1093294353 author=mfreed7 created=2022-04-08T19:41:44Z url=https://github.com/openui/open-ui/issues/321#issuecomment-1093294353 -->
## @mfreed7 — 2022-04-08

Increasingly, I think this is a key point of difference between popups and `<dialog>`. A dialog is a more "permanent" piece of UI that is around until explicitly dismissed. In contrast, a popup is more "transient", e.g. because it light dismisses when you leave it. As such, it feels like a dialog should be able to cancel its "hide" event (and it [can](https://html.spec.whatwg.org/multipage/interactive-elements.html#the-dialog-element:event-cancel)). However, a popup should not be cancelable. In particular, there are some actions that would close a popup, such showing a modal dialog or a fullscreen element, and those would **need** to not be cancelable. So really only the light dismiss or programmatic (`hidePopup()`) closing might be cancelable. I wonder what the (good) use cases are.

As I mentioned in https://github.com/openui/open-ui/issues/335#issuecomment-1093277873, I think this issue of cancelability can be separated from the ability to animate show/hide. So the only question (IMHO) is whether there's a use case for cancelling the hide of popups. 

<!-- comment id=1126461742 author=mfreed7 created=2022-05-13T20:34:15Z url=https://github.com/openui/open-ui/issues/321#issuecomment-1126461742 -->
## @mfreed7 — 2022-05-13

Increasingly, it seems to me like an anti-pattern to allow canceling the `hide` event for popups. Much of the machinery (and machinations on OpenUI issues) for popup is really geared toward doing the right thing for each type of popup. For example, [only allowing one `popup=hint` at a time](https://github.com/openui/open-ui/issues/525#issuecomment-1119093412), or hiding `popup=hint`s when `popup=popup`s are shown, or just generally [handling light dismiss correctly](https://open-ui.org/components/popup.research.explainer#light-dismiss). Allowing developers to override all of that behavior sounds like mostly a footgun for what is likely to be corner-case use-cases. Perhaps, as pointed out in https://github.com/openui/open-ui/issues/495, for those use-cases, `<popup=async>` is the right solution?

<!-- comment id=1138893574 author=css-meeting-bot created=2022-05-26T18:38:16Z url=https://github.com/openui/open-ui/issues/321#issuecomment-1138893574 -->
## @css-meeting-bot — 2022-05-26

The Open UI Community Group just discussed `[Popup] Should light dismiss be cancellable on popup?`, and agreed to the following:

* ``RESOLVED: the `show` and `hide` events should not be cancelable.``

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [Popup] Should light dismiss be cancellable on popup?<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/321<br>
&lt;gregwhitworth> masonf: the question is there are shown and hide events<br>
&lt;gregwhitworth> masonf: should they be canceleable<br>
&lt;una> q+<br>
&lt;gregwhitworth> masonf: currently they are not - my opinion is that they should not be canceleable<br>
&lt;gregwhitworth> masonf: we've spent a ton of time trying to get all of the behaviors to get right and allowing an easy developer override will end up being a footgun<br>
&lt;gregwhitworth> masonf: they may result in a11y issues<br>
&lt;gregwhitworth> masonf: there is an out, if you want to more easily control that then popup=async give you the control you may be looking for<br>
&lt;gregwhitworth> masonf: there are other more complicated cases, such as a full screen element is shown we need to force close the popups we would have to make the events not canceleable<br>
&lt;gregwhitworth> ack una<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> una: it seems like it doesn't bring up any usecases, I'm inclined to not rock the boat<br>
&lt;gregwhitworth> una: I'm not seeing anything and we can re-visit this<br>
&lt;JonathanNeal> +1 to making it not-cancel-able. As popups contrast with `&lt;dialog>`, popups feel more “temporary”, and I don’t see a use-case to make popups more like dialogs.<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> masonf: one more thing, there is a potential for a usecase for animations<br>
&lt;gregwhitworth> masonf: we have an issue opened to solve animations being better<br>
&lt;una> gregwhitworth: when i was doing early mockups on web components, i found being able to cancel the events valuable so I could hijack the behaviors and do something different<br>
&lt;JonathanNeal> +1 to what una says. I would prefer animations be handled in a different way than by allowing programatic cancellation.<br>
&lt;una> gregwhitworth: the capability of doing slotting in a lot of ways and the async approach is much more elegant<br>
&lt;JonathanNeal> As the issue mentions, authors could do “bad” things by using CSS to force the display of the popup, or potentially in the future by creating a never ending animation that _stalls_ the popup from closing.<br>
&lt;una> gregwhitworth: i didnt like it because i was undoing alot of the logic and had to re-author it, so i would +1 to the earlier a11y point because they're so intertwined, you end up having to cancel a lot of the events<br>
&lt;una> gregwhitworth: and I'm wanting to not cancel them all so +1 to not making them cancelable<br>
&lt;una> gregwhitworth: to provide the use case, I wanted to pivot from just down to showing hte scrollwheel touch event on mobile (inertia on scrolling)<br>
&lt;una> gregwhitworth: definitely required different logic, but again doesnt have to do with these exact events<br>
&lt;gregwhitworth> masonf: just to throw it out there, there may be usecases like that and I think popup=async without actually breaking the more defined usecases<br>
&lt;gregwhitworth> q?<br>
&lt;masonf> Proposed resolution: the `show` and `hide` events should not be cancelled.<br>
&lt;JonathanNeal> +1 to the proposed resolution<br>
&lt;masonf> Proposed resolution: the `show` and `hide` events should not be cancelable.<br>
&lt;JonathanNeal> +1 to the fixed proposed resolution.<br>
&lt;dandclark> +1<br>
&lt;masonf> RESOLVED: the `show` and `hide` events should not be cancelable.<br>
&lt;masonf> thanks tantek<br>
&lt;tantek>  is here for your W3C obscura<br>
&lt;gregwhitworth> Zakim, end meeting<br>
&lt;Zakim> As of this point the attendees have been scotto_, miriam, JonathanNeal, masonf, bkardell_, stephstimac, una, tantek, flackr, dandclark<br>
&lt;Zakim> RRSAgent, please draft minutes<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2022/05/26-openui-minutes.html Zakim<br>
</details>
