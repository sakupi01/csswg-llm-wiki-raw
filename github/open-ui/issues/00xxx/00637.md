---
number: 637
title: "SelectMenu placeholder capability?"
state: "closed"
labels: ["agenda+", "select"]
author: "jh3y"
created_at: "2022-11-16T12:18:00Z"
closed_at: "2024-04-25T18:37:28Z"
url: "https://github.com/openui/open-ui/issues/637"
comments: 30
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

In some of the *popular* select menu offerings out there, it's often the case that you can have a default "clean" state where no value is selected: https://react-select.com/home#async

The button part tends to have some form of "placeholder" content and the options list doesn't have to contain that placeholder visually as an option.

I tried to implement this behavior with the current API: https://codepen.io/web-dot-dev/pen/qBKjYKy

The question is "How do we allow people to set a placeholder without weird hacks?"

I tried a couple of "hacks" but they break the current implementation.

- Have an option that's hidden to "trick" the button part. This works but breaks the functionality as expected. Opening the dropdown doesn't focus on an option in the dropdown.
- Using `disabled` on the first option also breaks the functionality but is a common route people take: https://stackoverflow.com/questions/5805059/how-do-i-make-a-placeholder-for-a-select-box

Do we see this as a bug in the implementation? Or should we have some way to define a placeholder? Technically allowing a SelectMenu to be in a non-selected state.

cc @mfreed7 

<!-- comment id=1317043620 author=scottaohara created=2022-11-16T13:41:57Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1317043620 -->
## @scottaohara — 2022-11-16

Agree this is a common thing people want to do, and should probably be supported.  Problem with this though, and with the hacks people do to simulate it, is that it creates situations where a user cannot return to a no value selected state without resetting the form. 

If supporting a placeholder for selectmenu, it'd be nice to also account for users wanting to return to an unselected state without them having to reset the form or refresh the web page to do so.

<!-- comment id=1317048330 author=jh3y created=2022-11-16T13:45:27Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1317048330 -->
## @jh3y — 2022-11-16

Totally agree! 💯 

Side note:: Why is there not a "💯" reaction for comments?! 😅 

<!-- comment id=1317053230 author=scottaohara created=2022-11-16T13:49:18Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1317053230 -->
## @scottaohara — 2022-11-16

There are quite a number of reactions that I'd like to see as well... though, most of them fall into sarcastic/snark territory, and it's best I do not have easy access to them.

<!-- comment id=1317274515 author=mfreed7 created=2022-11-16T16:15:03Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1317274515 -->
## @mfreed7 — 2022-11-16

I agree that this feels like something we should handle with the new element. Are there standard ways that existing frameworks expose this functionality to developers? Just no `<option selected>` means no selection? And how is the ability to clear the selection exposed to users? It'd be good to follow cow paths for this.

<!-- comment id=1372617296 author=smhigley created=2023-01-05T19:04:34Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1372617296 -->
## @smhigley — 2023-01-05

+1 to having a `placeholder` prop and `::placeholder` style on selectmenu. I think it'd be good to separate out clearing selection to a different issue. While `placeholder` makes the lack of clearing selection more obvious, I don't think they're otherwise related.

<!-- comment id=1372659701 author=css-meeting-bot created=2023-01-05T19:48:51Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1372659701 resolution=true -->
## @css-meeting-bot — 2023-01-05

The Open UI Community Group just discussed `SelectMenu placeholder capability?`, and agreed to the following:

* `RESOLVED: Add placeholder functionality of some sort`
* ``RESOLVED: `<selectmenu>` should support placeholder functionality in some form``

<details><summary>The full IRC log of that discussion</summary>
&lt;una> Sarah: in favor of this<br>
&lt;JonathanNeal> q+<br>
&lt;masonf> q+<br>
&lt;JonathanNeal> Scott is addressing my question.<br>
&lt;JonathanNeal> Decorations.<br>
&lt;una> scotto: you mention placeholder prop like placeholder attribute like selectmenu, makes me think this would just accept text, are we saying people wouldn't be abel to have placeholder icons?<br>
&lt;una> Sarah: was actually going to ask and discuss that, my personal opinion is text-only is fine<br>
&lt;una> masonf: we haven't resolved what we're doing with non-text-only options in button anyway<br>
&lt;una> masonf: that would affect how we support placeholder<br>
&lt;una> masonf: if we're going just text as current protoype does, then text-only seems fine to me<br>
&lt;una> JonathanNeal: the proposal has pseudo element that doesnt force you to make a descision. But on the attribute that's where we'd need to decide in the API<br>
&lt;dandclark> q?<br>
&lt;una> JonathanNeal: might end up with the issue that have an attribute that also needs to be a slot<br>
&lt;una> ack JonathanNeal<br>
&lt;JonathanNeal> ack JonathanNeal<br>
&lt;JonathanNeal> +1 to having placeholder functionality<br>
&lt;una> masonf: not only that we want to do this, but we need to also discuss how to do it<br>
&lt;JonathanNeal> +1 to allowing `::placeholder`, too.<br>
&lt;dandclark> +1 to supporting it in some way<br>
&lt;una> scotto: would be nice to come up with an actual solution instead of people faking it like they need to do now<br>
&lt;una> scotto: let's definitely do it, people want it<br>
&lt;una> Brecht: the only thing i'm thinking about is using the pseudo would complicate things for multiple languages<br>
&lt;una> dandclark: so far solution has been to make everything a part<br>
&lt;JonathanNeal> I’m hoping both selectmenu values and selectmenu placeholders can share a mostly symmetry API.<br>
&lt;JonathanNeal> s/selectmenu values/selectmenu options<br>
&lt;una> dandclark: seems like the natural way to solve this as well<br>
&lt;JonathanNeal> s/symmetry/symmetric<br>
&lt;una> dandclark: maybe we should move forward with a resolution on if we should support this somehow before we determine how we should do it<br>
&lt;una> scotto: _1<br>
&lt;JonathanNeal> Agreed with dandclark to support the concept, bikeshed the how separately.<br>
&lt;una> s/_1/+1<br>
&lt;JonathanNeal> +1 to keep talking about it after the initial resolution<br>
&lt;una> masonf: any objections to adding a placeholder functionality of some form?<br>
&lt;una> - no objections -<br>
&lt;una> RESOLVED: Add placeholder functionality of some sort<br>
&lt;dandclark> RESOLVED: `&lt;selectmenu>` should support placeholder functionality in some form<br>
&lt;una> masonf: agree with dan re: using slots<br>
&lt;una> masonf: my guess is placeholder slot would be inside the button slot<br>
&lt;una> masonf: may be confusing if you replace the button element, all the placeholder functionality would go away, but maybe this isn't an issue<br>
&lt;JonathanNeal> My question about this is whether the `::part` targets the shadow `&lt;slot>` or the thing I added the `slot` to. It’s the former, technically, I think. But it kinda feels like I would want the later.<br>
&lt;una> JonathanNeal: when i added slot attribute to my own button for decorations, when i add the attribute, it felt a little strange that the part API is not targetting the element that i've assigned<br>
&lt;una> JonathanNeal: i didn't want it to not work that way at first but when I was using it, I wanted the part to style my element and not the display-contents slot container<br>
&lt;una> masonf: this is the same issue in that the part is exposed in the shadow root and not on the slotted content itself - have done this manually in the implementation of selectmenu<br>
&lt;una> masonf: the part is technically targetting the slot<br>
&lt;masonf> q?<br>
&lt;masonf> ack masonf<br>
&lt;una> masonf: to push things along, any objections to move things along to design a slot-based placeholder functionality?<br>
&lt;una> scotto: I do question how someone would do this if they were completely replacing the button and needing to add everything on their own<br>
&lt;una> JonathanNeal: when im using this API and I'm using native attributes then its going to be a little confusing if i'm adding decorations but i'm hiding them by adding a text value to that, unless I use ARIA text-value?<br>
&lt;una> JonathanNeal: example - I have rich content technically, but i want to represent that with a value, i.e. hex code of blue or word "five", but i want the value to be the color or number 5. I want a way to express that in my slotted content<br>
&lt;una> JonathanNeal: would we expect that I use another global attribute like aria-value-text? Or another special prop<br>
&lt;una> Sarah: I think this goes to what I brought up before because options can take richtext and images, we still need a plaintext attribute for them<br>
&lt;una> Sarah: for stuff like type to jump to an option, we need to give options a way to define a plaintext alternative<br>
&lt;una> masonf: the current prototype keeps using option element<br>
&lt;una> Sarah: I don't thihnk we can do that with value - the form uses option value instead of human submitted value<br>
&lt;una> Sarah: i.e. team wanted non-human-readable value for the DB, but wanted a human-readable value string text that had to be different than DB value<br>
&lt;una> masonf: is there an issue open for that?<br>
&lt;JonathanNeal> q?<br>
&lt;una> Sarah: not sure if there's another issue open for that<br>
&lt;una> masonf: please look for or open that so we can discuss<br>
&lt;JonathanNeal> I want to set something like `aria-valuetext` but also the thing that would be `setFormValue`<br>
&lt;una> scotto: i can't navigate by options by typing any of the values (just stays on first option), so its not supported yet<br>
&lt;una> Sarah: the one other consideration is if we have an editable selectmenu, i.e. another element that has a typeable combobox or autofill capability, would require string-only values<br>
&lt;JonathanNeal> Is my audio working?<br>
&lt;una> Related issue: https://github.com/openui/open-ui/issues/571#issuecomment-1293921725<br>
&lt;una> JonathanNeal: we have a JS API for this with setFormValue<br>
&lt;una> JonathanNeal: I also want this bc the need has been recognized for custom elements in the JS API<br>
&lt;una> masonf: might copy paste that link into a new issue because this is related but it's a seperate topic<br>
&lt;una> masonf: agreed to move forward with some sort of placeholder, tentative to use parts and slots, any objection to move along that route?<br>
&lt;JonathanNeal> +1 to prototyping<br>
&lt;una> dandclark: intermediate step can be to explore some options in protoype<br>
&lt;una> masonf: i meant to comment in code snippets<br>
&lt;una> dandclark: sounds good, agree<br>
&lt;JonathanNeal> +1 to code snippets, too; armchair prototyping.<br>
&lt;una> - no objections to exploring code snippets -<br>
&lt;una> masonf: one more thing - we discussed issue #﻿578 before - how the event works when popover shows and hides<br>
</details>

<!-- comment id=1405746658 author=josepharhar created=2023-01-26T22:18:07Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1405746658 -->
## @josepharhar — 2023-01-26

> masonf: agreed to move forward with some sort of placeholder, tentative to use parts and slots, any objection to move along that route?

So it sounds like we should use parts and slots to put some elements in as a placeholder and style it, but how should the selectmenu know if you want to opt into having a default unselected state with a placeholder in the first place?

> Agree this is a common thing people want to do, and should probably be supported. Problem with this though, and with the hacks people do to simulate it, is that it creates situations where a user cannot return to a no value selected state without resetting the form.

What about pressing backspace or escape? I suppose that wouldn't work on a phone when you don't have a keyboard though... perhaps an "x" button? Or an entry in the list?

<!-- comment id=1483517344 author=josepharhar created=2023-03-24T22:33:38Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1483517344 -->
## @josepharhar — 2023-03-24

The existing `<select>` element supports clearing the selected value by doing `element.value = ''` or `element.value = null`. I think this is something we could easily copy for selectmenu.

As for having a placeholder...
We currently set the selected value's textContent to an empty string when there is no selected value: https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/html/forms/html_select_menu_element.cc;l=864-865;drc=1fa8c431e4d96c382156f7831e5246a427754705
This matches what `<select>` does, it just becomes empty when there is no selected value.
We could easily add an attribute like `placeholder` to replace that empty string with an actual string specified by the author. This attribute already exists for other elements and I think would be perfect for selectmenu: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#placeholder

If you want to do something really fancy like have the placeholder have a different style than the other selected options, I believe that falls into something that you could do with a custom placeholder element and script. It wouldn't be too different than the resolution we already made for deciding not to copy innerHTML of the selected option: https://github.com/openui/open-ui/issues/571

I propose that we support `selectmenu.value = ''` and `selectmenu.value = null` for clearing the selected value and add the `placeholder` attribute as a way of setting the textContent of the selected value element to something other than an empty string for this case.

I also propose that pressing the escape key on the button while the listbox is closed should clear the selected value because thats what aria practices says to do: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/

<!-- comment id=1485582153 author=scottaohara created=2023-03-27T17:56:24Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1485582153 -->
## @scottaohara — 2023-03-27

>I also propose that pressing the escape key on the button while the listbox is closed should clear the selected value because thats what aria practices says to do: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/

that's only for text-editable comboboxes though.  that'd be out of scope for selectmenu 

<!-- comment id=1489550595 author=josepharhar created=2023-03-30T01:21:03Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1489550595 -->
## @josepharhar — 2023-03-30

Thanks for looking scott, I retract my proposal for the escape key behavior

<!-- comment id=1490456050 author=josepharhar created=2023-03-30T14:57:51Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1490456050 -->
## @josepharhar — 2023-03-30

I think my proposal still skips over an important use case where the author wants the initial state of the selectmenu to not have a selected value, which isn't possible with the current implementations of select or selectmenu.

Input elements have a "default value" concept, where you can specify a default value with the `value` attribute or the `defaultValue` script IDL. Maybe we could do a similar thing for selectmenu, where the string you pass in is the value of the option you want to be selected by default, and if its null or an empty string then it starts out unselected?

<!-- comment id=1490520016 author=css-meeting-bot created=2023-03-30T15:38:16Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1490520016 -->
## @css-meeting-bot — 2023-03-30

The Open UI Community Group just discussed `SelectMenu placeholder capability? #637`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> topic: SelectMenu placeholder capability? #﻿637<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/637<br>
&lt;dandclark> jarhar: THis was a request to be able to have a placeholder for selectmenu. By default, the first element is selected. But authors might want to have nothing selected, and show placeholder text.<br>
&lt;flackr> q+<br>
&lt;dandclark> ...: this would be pretty easy to implement. Propose that we add the placeholder attribute. More tricky is to make it not have t placeholder value to by default. Look at input text for a way to this. Add a value or default-value to selectmenu, have the value of the attr be the value that you want to be selected by default.<br>
&lt;una> q+<br>
&lt;dbaron> i/Agenda/ScribeNick: dandclark/<br>
&lt;hdv> ack flackr<br>
&lt;JakeA> q+<br>
&lt;dandclark> flackr: I think we have to decide what selectedIndex returns as well.<br>
&lt;dandclark> ...: At what point does the user get a value selected where they can no longer unselect a value?<br>
&lt;jarhar> q+<br>
&lt;dandclark> ...: Presumably opening the select doesn't select an optoin. There's some point where you get a selected value and can no longer be unselected<br>
&lt;hdv> ack una<br>
&lt;dandclark> JakeA: COuld also look at radio buttons<br>
&lt;dandclark> una: ??? The second is that you don't have a placeholder, show the value by default. Third is you have a placeholder that's not a selected value, it's only presentational. Fourth is that you have a placeholder that's also the selected value. What you send to the form might be different than what you show on the screen. Proposal should cover all 4 of these use cases.<br>
&lt;scotto> q+<br>
&lt;dandclark> una: First is you have no placeholder, but don't want to show anything in selectmenu.<br>
&lt;hdv> ack JakeA<br>
&lt;dandclark> ...: Do not have an initial value for that<br>
&lt;dandclark> JakeA:  Part of the reason for this is you can put richer contetn in options. Would be weird to regress on that for placeholder. Sounds like we were looking at it as only text, not rich. Would it make more sense to tag an option as a placeholder? Just doesn't take place in index of options, and can't be selected.<br>
&lt;scotto> +1 to jake's comment<br>
&lt;dandclark> una: good point, placeholder needs to be rich content<br>
&lt;bkardell_> JakeA: so its selectedIndex is -1?<br>
&lt;bkardell_> that would be weird<br>
&lt;hdv> ack jarhar<br>
&lt;una> q+<br>
&lt;dandclark> jarhar: We've already talked about richer contnt in selected value, decided should only be text for auto showing of the selected value. If show richer content, or mirror the content of the selected option into the selected value, it has to be rendered in 2 places at once. Gets error prone.<br>
&lt;dandclark> JakeA: svg use already has concept of rendering content in 2 places at once<br>
&lt;dandclark> una: THis is something I've struggled with when using selectmenu, can't reflect options in placeholder easily<br>
&lt;una> q-<br>
&lt;dandclark> jarhar: I can look into svg use<br>
&lt;dandclark> flakr: when we decided not to reflect content, was because we wanted to wait until we come up with a way to do this. WOuld make sense to have a placeholder option even if now we can only copy the value and authorss have to use script to add rich content. Then later we can do the better thing.<br>
&lt;dandclark> ...: though placeholder is easier since it's not showing in 2 places at once. But we have a longer term plan to address that concern<br>
&lt;bkardell_> q+<br>
&lt;hdv> ack scotto<br>
&lt;bkardell_> q-<br>
&lt;dandclark> scotto: To echo some of that, one of the issues with reflection is that because anyone can add arbitrary content, links buttons and stuff, you don't want that reflected in the trigger for hte selectmenu. E.g. an option with a link, paragraph, delete button, don't want that reflected in the triggering element. Can't just whole-hog copy the innerHTML of the option back to the triggering option<br>
&lt;bkardell_> q+<br>
&lt;dandclark> ...: I like the idea of identifying particular option in the set as the placholder<br>
&lt;dandclark> ...: make the option you want to be the placeholder, and you have the safe default.<br>
&lt;dandclark> ...: question I have for this approach is that because triggering element can be swapped out, how do we make that accessible by default. Someone can take out the default combobox element and put something else there. Using a placeholder on selectmenu element, I don't know how that would go into any element that people put in there.<br>
&lt;dandclark> ...: I'm still not sure how we'd make that not create problems<br>
&lt;hdv> ack bk<br>
&lt;dandclark> bkardell: Have to flesh out the idea of tagging one of them with follow up questions. Is it still an option if it's a placeholder? By selecting it, do you have none selected? SelectedIndex is -1 then? It's weird because you have option, option, defaultOption, and the indexes of those feel like it hsould skip 1.<br>
&lt;dandclark> ...: For multiselect, can you select placeholder and then something else? That's what people do now with select, you just have to fake it. Should be able to do that. It's going to be highly styleable anyway.<br>
&lt;dandclark> ...: This part of select is particularly broken anyway, so feel like it's an ok solutionl.<br>
&lt;dandclark> s/solutionl/solution<br>
&lt;dandclark> hidde: Sounds like placeholder string would not work, want to have rich content, but not sure how<br>
&lt;bkardell_> s/This part of select is particularly broken/This part of select seems not particularly broken<br>
&lt;dandclark> una: We'd rely on placeholder value to be that string that's preserved, but rich content is important for this feature<br>
&lt;dandclark> ...: it's what separates it from current select<br>
&lt;dandclark> jarhar: Agree, fact that we decided on having a string in selectedValue is not good enough. Idea was if you want to display something more rich, use script. But if that's not good enough, having just the placeholder text isn't good enough.<br>
&lt;dandclark> ...: Also, rob talked about going from selected to non-selected state. Not sure if makes sense to have option for that, or use backspace etc.<br>
&lt;dandclark> ...: Any other ideas to clear selected value?<br>
&lt;flackr> q+<br>
&lt;dandclark> scotto: Worry is that escape key is used to close listbox, I would hate for someone to open it, close it, and lose the value<br>
&lt;dandclark> ...: get form error because it's not filled out, have to go back and find it and the work is lost<br>
&lt;dandclark> ...: Could come up with way to make this more intuitive, but why change the behavior?<br>
&lt;dandclark> jarhar: Could slot something into button to clear the selectedvalue<br>
&lt;dandclark> scotto: If placehodler option is still selectable, someone could return to that<br>
&lt;dandclark> s/placehodler/placeholder<br>
&lt;dandclark> scotto: Do we really need this? Placeholder in text inputs is also big a11y issue<br>
&lt;dandclark> scotto: Already have solution for this now, use an option, don't give it a value. In validation, reject if no value<br>
&lt;hdv> ack flackr<br>
&lt;dandclark> flackr: Wanted to say thing. If you want to allow users to go back to unselected state, make the placholder selectable<br>
&lt;hdv> /me input type=reset<br>
&lt;dandclark> ...: LIke with radio buttons, if you want a way to reset form, add a button with script to do that. But dont' normally have a way to unselect radio buttons, dont' see why we need one for select either<br>
&lt;dandclark> jarhar: Don't think we can resolve, need a better way to render options into the selectedvalue to make more progress.<br>
&lt;dandclark> flackr: Aren't we saying this can be done by having an option that doesn't show up, that serves as your placeholder?<br>
&lt;dandclark> ...: By default we render text of option in the button, that satisfies the requirement for now. When we have a better solution for showing rich content, we can use that<br>
&lt;dandclark> ...: It's the default by being first. Just need way to indicate that it's not in the dropdown.<br>
&lt;dandclark> JakeA: use the attribute<br>
&lt;bkardell_> q+<br>
&lt;dandclark> flakr: It could be that there's a placeholder attr on the select that targets the option, ensures there's only one<br>
&lt;hdv> ack bk<br>
&lt;dandclark> bkardell: Could we do something to say if the 1st option has value of emptystring, it's the placeholder?<br>
&lt;dandclark> JakeA: If you use attr to indicate placeholder, it only applies to the 1st option. Worried about empty value because that's not how it behaves on select.<br>
&lt;dandclark> bkardell: Not showing is already not the way it behaves on select<br>
&lt;dandclark> flackr: Need a way for option to not be selectable in list<br>
&lt;dandclark> jarhar: Want to either render or not in the dropdown list depending on what author wants<br>
&lt;dandclark> jarhar: seems like there's 2 different controls here. Ability to not render should be on the option, and placeholder should be on the selectmenu itself?<br>
&lt;dandclark> flackr: To not have it render, seems like using the placeholder attribute is sufficent for that<br>
&lt;bkardell_> so we add an attribute like "resettable"?<br>
&lt;scotto> this is how someone would do this today<br>
&lt;scotto> &lt;select> 	&lt;option selected hidden>foo&lt;/option> 	&lt;option>boo&lt;/option> &lt;/select><br>
&lt;dandclark> flackr: We dont' need a separate feature for placeholder, as long as we have feature to prevent it from being in selectable options when we expand the menu.<br>
&lt;bkardell_> oh wow, does that work?<br>
&lt;dandclark> ...: Authors can build the equivalent of a placeholder<br>
&lt;scotto> https://codepen.io/scottohara/pen/eYLaWbL<br>
&lt;flackr> I suspect option:first-child { display: none; } might work<br>
&lt;dandclark> jarhar: What if on option element you say slot=placeholder.<br>
&lt;dandclark> ...: then it doesn't get slotted into the list<br>
&lt;bkardell_> scotto: that doesn't seem to work for me on safari at least<br>
&lt;dandclark> hidde: seems magic<br>
&lt;dandclark> una: so it's not selectable?<br>
&lt;dandclark> jarhar: yes, it's not rendered<br>
&lt;scotto> safari thinks different then<br>
&lt;dandclark> una: don't hate it but does seem magic<br>
&lt;flackr> scotto: nice demo!<br>
&lt;dandclark> ...: unexpected DOM result. But see how it would work for dev ergonomics. But then why not have the value in where you put the placeholder. In the dropdown slot.<br>
&lt;flackr> q+ can we just use hidden like scotto's demo?<br>
&lt;dandclark> jarhar: Una was saying we could make a special string that would go in option's value attr to prevent it from being rendered<br>
&lt;dandclark> una: Why not have attr that could live where the slot is for the placeholder, if you don't want it to be a selectable option, but want to put rich content inside it. The value of the placeholder attr is the value for screenreaders, visible to AT.<br>
&lt;dandclark> jarhar: attr goes on which el?<br>
&lt;dandclark> una: goes in slot where you put the button<br>
&lt;flackr> q+<br>
&lt;dandclark> jarhar: ON button element or selected-value element<br>
&lt;dandclark> una: On one of those<br>
&lt;dandclark> jarhar: It targets an option, or it's just text<br>
&lt;dandclark> una: not option, because option isnt' selectable<br>
&lt;dandclark> una: *works on providing code sample*<br>
&lt;dandclark> flackr: Could we do the thing that works with select today?<br>
&lt;dandclark> scotto: Doesnt' work on safari<br>
&lt;dandclark> jarhar: If put hidden on it, makes it not shown in the list. In selectmenu it also removes it from list<br>
&lt;dandclark> s/hidden/hidden attribute<br>
&lt;dandclark> flackr: Not sure you'd copy the options content itself, but we can figure that out<br>
&lt;dandclark> hidde: Want to come back to this?<br>
&lt;dandclark> ...: Seems like we're not sure if we need something specific for this or can be done with existing stuff<br>
&lt;flackr> q-<br>
&lt;hdv> q?<br>
</details>

<!-- comment id=1499170285 author=josepharhar created=2023-04-06T14:37:17Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1499170285 -->
## @josepharhar — 2023-04-06

At the meeting last week, people pointed out that having a placeholder attribute with a text value isn't good enough because if we find a way to display richer values in the selected value slot then simply having a text value wouldn't be as good as having all the rich content that you could put in an option element.

Scott pointed out that if you display:none or set the hidden attribute on an option, it won't show up in the dropdown, so the placeholder could simply be the first option in the list:
```html
<selectmenu>
  <option hidden>--choose an option--</option>
  <option>one</option>
  <option>two</option>
</selectmenu>
```

If you want the user to be able to go back to the unselected state, you could simply remove the hidden attribute on the first option.
Does everyone think this is a good enough solution?

<!-- comment id=1499370320 author=jh3y created=2023-04-06T17:08:10Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1499370320 -->
## @jh3y — 2023-04-06

Hey 👋 

Does this work though? Setting `[hidden]` *kinda* works. The issue is that the value of the `selectmenu` becomes whatever that `indeterminate` option's value is. Which could be fine. But, the DX side likely expects something along the lines of `null`, `undefined`, `""`, etc.

Also, a `hidden` option [breaks the keyboard controls](https://codepen.io/jh3y/pen/oNaNqGQ/1dc404d031f4f578f0dc5b913d9a3ca2).

```
<selectmenu>
  <option value="indeterminate" hidden>Indeterminate</option>
  <option value="one" autofocus>One</option>
  <option value="two">Two</option>
  <option value="three">Three</option>
</selectmenu>
```

Overriding the `selected-value` isn't terrible depending on the design. You would check against the selectmenu value and if it's other than `null`, you update accordingly. This is purely based on the fact that `selected-value` rich content hasn't been settled on yet so assuming long term that it'll be down to the developer to manage this which isn't "terrible", jus' a little tedious.

With that said, because a `selectmenu` assumes a value of the first option, setting the value to `null` or something other than an option value breaks the browser in certain scenarios 😅 

Check [this demo](https://codepen.io/jh3y/pen/RwewMxz/3acc4c2fc01b5ff6787c4574e6a6ccfb) and tab into the control and hit up/down. Figured this was a "fun" find ha. But, this happens I believe because we've broken the tabbing controls by setting the value. 

Either of these two approaches break the keyboard tabbing. I'm all for the "indeterminate" value route as mentioned. But, the tabbing controls need to skip to the first available(visible) option 👍 

<!-- comment id=1500774686 author=scottaohara created=2023-04-08T03:11:15Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1500774686 -->
## @scottaohara — 2023-04-08

The point of showing that hidden is how someone can hack this now was not to imply that it’s a perfect solution -now-, but to show that it’s a way it could be done and if it was specified to work this way, then unwanted behaviors could be mitigated. But if there is a better idea for how to do this, then we should go with that. 

<!-- comment id=1507113572 author=josepharhar created=2023-04-13T14:52:20Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1507113572 -->
## @josepharhar — 2023-04-13

> But, the DX side likely expects something along the lines of null, undefined, "", etc.

If you set the value of the placeholder option to an empty string, then at least assigning to an empty string works: https://jsfiddle.net/jarhar/3boetfum/
Assigning to null or undefined makes it look like it has no selected value at all, which we could change to make select the first option instead.

> Check [this demo](https://codepen.io/jh3y/pen/RwewMxz/3acc4c2fc01b5ff6787c4574e6a6ccfb) and tab into the control and hit up/down

Thanks for finding this, I filed a bug to get it fixed: https://bugs.chromium.org/p/chromium/issues/detail?id=1432986

> The point of showing that hidden is how someone can hack this now was not to imply that it’s a perfect solution -now-, but to show that it’s a way it could be done and if it was specified to work this way, then unwanted behaviors could be mitigated. But if there is a better idea for how to do this, then we should go with that.

Ok, what if we put a "placeholder" attribute on the placeholder option? I forget if this was thought of during the last meeting. @jh3y do you have any ideas?

<!-- comment id=1507150545 author=css-meeting-bot created=2023-04-13T15:13:30Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1507150545 -->
## @css-meeting-bot — 2023-04-13

The Open UI Community Group just discussed `SelectMenu placeholder capability? #637`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> topic:  SelectMenu placeholder capability? #﻿637<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/637<br>
&lt;hdv> jarhar: this is about having a placeholder for &lt;selectmenu><br>
&lt;hdv> jarhar: I suggested a placeholder attribute on &lt;selectmenu> itself<br>
&lt;hdv> jarhar: then last time we discussed we talked about adding an attribute to the &lt;option> element, but then we discussed last time that it could work today already if you have the first &lt;option> element as a placeholder and add hidden attr to it<br>
&lt;hdv> jarhar: it seems like we need a better way to do it<br>
&lt;una> q+<br>
&lt;masonf> q?<br>
&lt;hdv> jarhar: maybe we can add a placeholder attribute to the &lt;option> element? does anyone have thoughts?<br>
&lt;masonf> ack una<br>
&lt;hdv> una: it sounds like this could work with another attribute, but I'm not sure if `placeholder` is it, if it's not going to be a string of text<br>
&lt;hdv> una: but then we'd also need to find out what to submit<br>
&lt;scotto> q+<br>
&lt;hdv> una: would suggest something very specific like optionlist-placeholder, though that's a bit long<br>
&lt;masonf> ack scotto<br>
&lt;hdv> scotto: I agree, reusing placeholder attribute would not be good here.<br>
&lt;hdv> scotto: when I mentioned using the hidden attribute… it was more to show _how_ it could be done today, I didn't want to suggest it was an ideal solution to this problem<br>
&lt;masonf> q+<br>
&lt;hdv> scotto: maybe it could have some attribute on an option that can indicate that a specific option is the placeholder, or maybe even an element like &lt;placeholderoption><br>
&lt;hdv> scotto: if we could identify an option as being the placeholder, or create an element to allow authors to specify the placeholder they want, that would be a less hacky than my previous suggestion<br>
&lt;hdv> s/suggestion/suggestion of using the `hidden` attribute<br>
&lt;masonf> ack dbaron<br>
&lt;hdv> dbaron: last time I asked about it, i18n working group prefers presentable text to not be an attribute<br>
&lt;hdv> dbaron: for things like directionality<br>
&lt;hdv> ack mason<br>
&lt;dbaron> s/directionality/directionality or ruby/<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: we already have a placeholder attribute that is familiar to people… using that word might be useful. What if we did use the `placeholder` attribute or something like it but have it contain the ID of an option, like placeholder="foo" to indicate `&lt;option id="foo">` is the placeholder<br>
&lt;masonf> q?<br>
&lt;hdv> Keith: there are cases where you want the currently selected<br>
&lt;hdv> jarhar: so it sounds like we have multiple options… maybe we can post those on the GitHub issue and discuss more there?<br>
</details>

<!-- comment id=1507556195 author=jh3y created=2023-04-13T20:13:32Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1507556195 -->
## @jh3y — 2023-04-13

> Ok, what if we put a "placeholder" attribute on the placeholder option? I forget if this was thought of during the last meeting. @jh3y do you have any ideas?

To be honest, if I was going with an explicit placeholder that isn't an option, then perhaps it becomes another optional piece associated with a `behavior`. I gather the `slot`, `behavior`, etc. part was being discussed again. But, it could make sense as its own designated element. That's how I've gone about implementing it in some demos where I've needed it. That way you could have some logic such as if a placeholder is present, don't set a default value of the `:selected` or first option. And once there is a selected value, the placeholder loses visibility or something. In some cases to get around this, I have to manually create a `selected-value` element and control it myself.

<!-- comment id=1673657555 author=una created=2023-08-10T17:53:24Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1673657555 -->
## @una — 2023-08-10

I chatted with @mfreed7 and @josepharhar about a potential placeholder solution which is relatively simple and solves a lot of open questions. 

**TLDR; Align with what `select` does for placeholders: put `value=""` on an option to indicate a placeholder**

Here's what this might look like:

Text-only placeholder:

```html
<selectlist>
  <option value="">
    Select a country
  </option>
  <option value="south-africa">
    <img src="s-a-flag.png" />
    <p>South Africa</p>
  </option>
  <option>
    ...
  </option>
</selectlist>
```

Placeholder with an image:

```html
<selectlist>
  <option value="">
    <img src="placeholder-flag.png" />
    <p>Select a country</p>
  </option>
  <option value="south-africa">
    <img src="s-a-flag.png" />
    <p>South Africa</p>
  </option>
  <option>
    ...
  </option>
</selectlist>
```

<!-- comment id=1681251438 author=brechtDR created=2023-08-16T20:48:24Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1681251438 -->
## @brechtDR — 2023-08-16

Interesting idea @una 

Few questions come to mind

1) does this mean that the empty valued option should also be the first option?

2) what if multiple empty values are provided?

3) should we consider the "selected" and "disabled" attributes for this as well? As I believe an empty value could potentially be a choice (seen this in the past with other selects(

Would love to hear your thoughts. 

<!-- comment id=1681262821 author=una created=2023-08-16T20:58:41Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1681262821 -->
## @una — 2023-08-16

1. Yes, it should. Currently, select will show "Disco" as the placeholder here:

```
<select> 
  <option value="disco">Disco</option>
  <option value="">Select a value</option>
</select>
```

2. Currently, select will choose the first option (if there isn't a `selected` attribute on it). I.e. the placeholder in the below example is "Select a value"

```
<select>
  <option value="">Select a value</option>
  <option value="">Select a value 2</option>
  <option value="disco">Disco</option>
</select>
```

3. Yes, good call -- that would be the ideal, just like with select:

```
<select> 
  <option value="" selected disabled>Select a value</option>
  <option value="disco">Disco</option>
</select>
```

So to put it all together:
```
<selectlist>
  <option value="" selected disabled>
    <img src="placeholder-flag.png" />
    <p>Select a country</p>
  </option>
  <option value="south-africa">
    <img src="s-a-flag.png" />
    <p>South Africa</p>
  </option>
  <option>
    ...
  </option>
</selectlist>
```

<!-- comment id=1681629999 author=brechtDR created=2023-08-17T05:19:26Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1681629999 -->
## @brechtDR — 2023-08-17

@una sounds about right. 👍 

It's consistent with the current select implementation. I do remember that in the past a question was raised in a telecon with "can we do better?" 

The use case for that is that people might want to use a placeholder attribute on a select as well just as they can do with an input element. 

However, this use case doesn't have the ability to use an image as in your proposal. Especially for custom styling, I'm more into what you are proposing.

It does make me wonder: should we also allow a placeholder attribute? And what would happen if we have a placeholder combined with the empty value solution, but maybe this is beside the question. 

<!-- comment id=1682540235 author=una created=2023-08-17T16:00:21Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1682540235 -->
## @una — 2023-08-17

@brechtDR I recall we've talked about a `placeholder` attribute before but it didn't really get anywhere. I wouldn't be against it, but would need to define its utility and if its worth adding a new attribute to HTML, plus how it would interact with the existing `select` options, etc. This solution also doesn't disallow `placeholder` in the future, it could work with it. i.e.


```
<selectlist>
  <option value="" placeholder>
    <img src="placeholder-flag.png" />
    <p>Select a country</p>
  </option>
  <option value="south-africa">
    <img src="s-a-flag.png" />
    <p>South Africa</p>
  </option>
  <option>
    ...
  </option>
</selectlist>
```

where `placeholder` would replace `selected disabled`

<!-- comment id=1682604658 author=josepharhar created=2023-08-17T16:32:14Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1682604658 -->
## @josepharhar — 2023-08-17

Considering that option elements already support `selected` and `disabled`, I think that we can just keep using those instead of making `placeholder` replace them. Besides, what if someone wants to make a placeholder than the user can go back to and select again? In this case, they could use `selected` without `disabled`.

I'd like to propose these three behaviors, aligning with @una, all three of which are already match the existing `<select>` element:
1. `<option value="">` in a `<selectlist required>` prevents form submission when selected.
2. `<option disabled>` prevents the option from being selected by the user. It can still be the default initially selected option.
3. `<option selected>` makes the option the default initially selected option. If there is no `<option selected>`, then the first child option in the DOM is the default initially selected option. If there are multiple `<option selected>`s, then the first one in the DOM wins.

Given all these behaviors, I think that authors should be able to do any placeholder use case they want.

EDIT:

I just re-read the issue description and realized this is missing the point that jhey brought up - there should be a way to also hide the placeholder from the option list completely.

> Have an option that's hidden to "trick" the button part. This works but breaks the functionality as expected. Opening the dropdown doesn't focus on an option in the dropdown.

If we could make `<option hidden>` not break focus, then would the `hidden` attribute be good enough?

EDIT 2:

imo `<option hidden>` should be fine. I don't see any reason why it should break focus because I just tested `<button hidden>` and verified that it isn't keyboard focusable. I'd like to propose making `<option hidden>` the preferred way of preventing an option from being shown in the listbox until there is a compelling reason to do otherwise.

I don't think that just signifying an option as a "placeholder" to opt into these four different behaviors is a good idea when we could instead let authors opt into them separately to build exactly what they want.

<!-- comment id=1682682545 author=brechtDR created=2023-08-17T17:25:43Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1682682545 -->
## @brechtDR — 2023-08-17

> imo `<option hidden>` should be fine. 

In combination with this, it sounds really powerful. Absolutely great idea!

<!-- comment id=1682780451 author=css-meeting-bot created=2023-08-17T18:38:51Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1682780451 resolution=true -->
## @css-meeting-bot — 2023-08-17

The Open UI Community Group just discussed `SelectMenu placeholder capability?`, and agreed to the following:

* `RESOLVED: Support the value="" attribute, selected attribute, disabled attribute, and hidden attribute on option elements in selectlist. For v2, consider a placeholder attribute or element that implies <option value="" disabled hidden>.`

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: SelectMenu placeholder capability?<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/637#issuecomment-1673657555<br>
&lt;gregwhitworth> jarhar: we've arleady discussed this once before<br>
&lt;gregwhitworth> jarhar: libs allow placeholder and it won't show up in the options list<br>
&lt;gregwhitworth> jarhar: I discussed having another element and that was deemed not a good idea but una noted supporting value=""<br>
&lt;gregwhitworth> jarhar: I looked at what select already supports and I think that we should support four different behaviors on an option element<br>
&lt;gregwhitworth> jarhar: value="" it will be an empty string but it will make the list has a required attribute it will keep the form from being submitted<br>
&lt;gregwhitworth> jarhar: second thing, if it is disabled it won't be selected by the user but it will be in the list but not selectable<br>
&lt;gregwhitworth> jarhar: we support the selected attribute we'll select the default<br>
&lt;gregwhitworth> jarhar: fourth we should support the hidden attribute on the option that will make it so that it won't show up in the listmenu at all; but this may have some a11y issues<br>
&lt;gregwhitworth> jarhar: this is all currently supported in the current select element<br>
&lt;masonf> q?<br>
&lt;gregwhitworth> masonf and argyle +1<br>
&lt;gregwhitworth> argyle: if you want examples of this I use gradient styles to make this possible<br>
&lt;jarhar> Proposed resolution: Support the value="" attribute, selected attribute, disabled attribute, and hidden attribute on option elements in selectlist<br>
&lt;bkardell_> +1<br>
&lt;flackr> +1<br>
&lt;masonf> q+<br>
&lt;scotto_> q+<br>
&lt;brecht_dr> gregwhitworth: In the current select when i put the hidden attribute in the option, it renders it in the box, but not in the popup. That seems odd to me.<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> masonf: it gets exactly the behavior that we want and it achieves the goal<br>
&lt;gregwhitworth> masonf: please choose one of these options, so this is nice<br>
&lt;gregwhitworth> scotto_:<br>
&lt;gregwhitworth> since I was the one that mentioned hidden was an example of what you can do today not that what we should do<br>
&lt;argyle> hrm https://codepen.io/argyleink/pen/PoXowLb<br>
&lt;gregwhitworth> scotto_: this should come with author guidance; it would be the random middle option that is hidden and multiple value=""; which one of those becomes the placeholder<br>
&lt;gregwhitworth> scotto_: as long as what's resolved will get the placeholder<br>
&lt;flackr> +1 it should be the first option that shows up in the select which you can use to implement placeholder like behavior<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> ack scotto_<br>
&lt;gregwhitworth> scotto_: I don't really follow the proposed resolution as it relates to the placeholder<br>
&lt;bkardell_> this was my understanding too<br>
&lt;gregwhitworth> masonf: these four things, when put together they can make a placeholder<br>
&lt;gregwhitworth> masonf: this allows it to be a placeholder with these four options and also we planned to add these support<br>
&lt;gregwhitworth> masonf: do some of those scenarios cause a11y issues?<br>
&lt;gregwhitworth> scotto_: if it's disabled then it shouldn't be selected<br>
&lt;gregwhitworth> scotto_: if something's disabled; there is a reason for that<br>
&lt;gregwhitworth> scotto_: on ebay for example; there will be X variants but it is sold out so it's disabled but you want it rendered to the user<br>
&lt;gregwhitworth> scotto_: is it the combination of these things, one of them, etc that makes it a placeholder?<br>
&lt;gregwhitworth> masonf: I guess maybe my point is this proposal there is no such thing that is a placeholder<br>
&lt;gregwhitworth> masonf: they can be hidden, disabled, selected, the first one of those it will show up in the button<br>
&lt;gregwhitworth> scotto_: sorry, the one of these it needs to be the first option in the list<br>
&lt;brecht_dr> q+<br>
&lt;gregwhitworth> flackr: not if it's selected<br>
&lt;gregwhitworth> masonf: if it's a selected disabled in the middle but it will not be submittable<br>
&lt;gregwhitworth> scotto_: interesting, unsure what hidden would be<br>
&lt;gregwhitworth> scotto_: I'm not opposed to this, but it seems like a lot to do<br>
&lt;gregwhitworth> scotto_: or do we just want to have a placeholder attribute<br>
&lt;gregwhitworth> flackr: if it's the first option then it's selected<br>
&lt;gregwhitworth> jarhar: if we have a placeholder option, then we need to be both in the list<br>
&lt;bkardell_> could we have a placeholder idref to get around the "dont be an attribute" issues?<br>
&lt;gregwhitworth> masonf: it also allows the other usecases, such as wanting to show a selected but not available usecase<br>
&lt;gregwhitworth> scotto_: I wouldn't have considered that a placeholder<br>
&lt;gregwhitworth> scotto_: thank you for taking me on that journey<br>
&lt;gregwhitworth> q?<br>
&lt;jarhar> ill scribe<br>
&lt;jarhar> greg: when i was asking are these things defined, but scotts confusion is what i want to ensure is defined somewhere<br>
&lt;jarhar> greg: heres what happens with hidden, selected, etc.<br>
&lt;jarhar> greg: so selectlist needs to have a definition with what its going to do with those<br>
&lt;jarhar> greg: i think that the DX is super weird to me<br>
&lt;jarhar> greg: theres value to all of them existing, when you bring up the "please choose an option", that is such a placeholder and reaching for hidden is odd to me but its not something im going to fight for<br>
&lt;jarhar> greg: as long as we go through heres how it goes and author guidance apply hidden and everyone will move forward with life<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> brecht_dr: from the prior convo, two things came to mind<br>
&lt;gregwhitworth> brecht_dr: the benefit of this is that you can use the complex content in the option element; beyond text<br>
&lt;gregwhitworth> brecht_dr: another thing is that selected option being a placeholder that may not be the first option<br>
&lt;gregwhitworth> brecht_dr: they maybe selected somewhere down the list, but these do happen and it's perfectly understandable that the "placeholder" should be supported<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;gregwhitworth> argyle: shared a codepen in IRC and started making weird selects and added multiple and none of the placeholders show up<br>
&lt;bkardell_> q+<br>
&lt;gregwhitworth> argyle: I kind of agree with scott on this that it feels very hacky; it's not an option. It's a label<br>
&lt;gregwhitworth> argyle: seems odd to keep this going forward<br>
&lt;gregwhitworth> argyle: we can make it very explicit<br>
&lt;gregwhitworth> ack dbaron<br>
&lt;gregwhitworth> dbaron: one brief comment that is similar to brecht's first comment is that i18n will remind groups of presentable text in attributes so that you can handle localization<br>
&lt;gregwhitworth> ack bkardell_<br>
&lt;brecht_dr> q+<br>
&lt;dbaron> s/groups of/groups of avoiding/<br>
&lt;scotto_> q+<br>
&lt;gregwhitworth> bkardell_: wanted to double on a couple things that others have said, if this is how we need to do it then sure, it's odd and hacky and I understand the desire to fix selectlist?<br>
&lt;gregwhitworth> bkardell_: can we give something for both select and selectlist<br>
&lt;gregwhitworth> bkardell_: all the reasons why it's not great to have a single string, bidi, complex content etc<br>
&lt;gregwhitworth> bkardell_: could we do an idref for that?<br>
&lt;scotto_> &lt;option placeholderoption>whatever you want here&lt;/option><br>
&lt;gregwhitworth> bkardell_: what is really the placeholder element<br>
&lt;flackr> q+<br>
&lt;gregwhitworth> masonf: one thing to point out is a few alternatives and one is attribute and another element like &lt;placeholder> and that would solve all of the issues. ID ref is a third but feels a bit odd<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;gregwhitworth> brecht_dr: I was thinking of a third; someone speaking about a placeholder attribute on the options that would allow you to select which option denotes the option<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> masonf: the idea would be the placeholder attribute on the option?<br>
&lt;gregwhitworth> brecht_dr: yes<br>
&lt;gregwhitworth> ack scotto_<br>
&lt;gregwhitworth> that's what I was going to say, when I was talking about placeholder that we would actually re-use the placeholder attribute with text string and arbitrary content and i18n issues<br>
&lt;masonf> So &lt;option placeholder> is like syntactic sugar for &lt;option value="" disabled hidden>.<br>
&lt;gregwhitworth> scotto_: I think there is value in doing this as it could take precedance over an initially selected option and they end up with selected onto a selected option and what happens due to hacky stuff occurring<br>
&lt;flackr> and default selection - if you don't have a selected option<br>
&lt;gregwhitworth> scotto_: bugs sneak in<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack flackr<br>
&lt;gregwhitworth> flackr: I think the discussion has gotten to where I've been going<br>
&lt;gregwhitworth> flackr: we have to suppor this anyways but I do like the proposal as it solves what we want<br>
&lt;jarhar> ill scribe<br>
&lt;jarhar> greg: im really like placeholder element given the plethora of elements we are introducing. it feels like we are doing old school flash days actionscript where everything is concretely defined<br>
&lt;jarhar> greg: i dont disagree from an implementation perspective that is so fast<br>
&lt;jarhar> greg: in a utopia world, i would just have a placeholder element<br>
&lt;jarhar> greg: like other issues with errors that we are going to have to generally solve to ensure that people dont rebuild their old stuff<br>
&lt;jarhar> greg: +1 to what rob just summed up<br>
&lt;jarhar> greg: kind of reached consensus on yes just go solve all of them<br>
&lt;jarhar> greg: seems to be general support, addendum to supposed resolution - placeholder attribute equivalent to value=""<br>
&lt;jarhar> flackr: should we do an element instead of an attribute?<br>
&lt;jarhar> greg: i care more about implementation time than whether is an element or an attribute<br>
&lt;scotto_> +1 to either attribute or optionplaceholder element<br>
&lt;jarhar> greg: take the whatwg spec time. do we envision this being faster as an attribute vs an element? i dont want to get bogged down<br>
&lt;jarhar> masonf: one important point is that we need to support these attributes anyway<br>
&lt;jarhar> masonf: they make it look like a placeholder<br>
&lt;jarhar> masonf: maybe we could do this in two versions. start with the option attributes, then do the element later<br>
&lt;jarhar> flackr: thats what i was getting towards<br>
&lt;jarhar> flackr: you can still build a placeholder in the meantime<br>
&lt;jarhar> masonf: i can see standardization issues with placeholder element<br>
&lt;jarhar> greg: that latter part is exactly what i want to avoid<br>
&lt;jarhar> greg: it seems pretty straightforward, it only works in selectlist<br>
&lt;jarhar> greg: it could work later on other elements<br>
&lt;scotto_> also agree to the idea that this could be a version 2 feature<br>
&lt;masonf> Proposed resolution: Support the value="" attribute, selected attribute, disabled attribute, and hidden attribute on option elements in selectlist. For a v2, consider a placeholder attribute or element that implies &lt;option value="" disabled hidden>.<br>
&lt;jarhar> greg: what you just said is fine rob, im just not passionate enough about the placeholder thing. worthwhile resolution<br>
&lt;scotto_> +1<br>
&lt;argyle> +1<br>
&lt;jarhar> brecht: shouldnt that be option value="" selected hidden?<br>
&lt;masonf> RESOLVED: Support the value="" attribute, selected attribute, disabled attribute, and hidden attribute on option elements in selectlist. For v2, consider a placeholder attribute or element that implies &lt;option value="" disabled hidden>.<br>
</details>

<!-- comment id=1817496250 author=lukewarlow created=2023-11-18T12:29:32Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1817496250 -->
## @lukewarlow — 2023-11-18

For the placeholder aspect (wouldn't cover the reset one).

Could we allow `<selectedoption>Placeholder content goes in here<selectedoption>` kinda like how a slot takes default content?

<!-- comment id=1843775330 author=josepharhar created=2023-12-06T22:15:00Z url=https://github.com/openui/open-ui/issues/637#issuecomment-1843775330 -->
## @josepharhar — 2023-12-06

> Could we allow `<selectedoption>Placeholder content goes in here<selectedoption>` kinda like how a slot takes default content?

If we end up cloning into the user-agent shadowroot of the selectedoption element then I suppose that's a possibility. However, the way its currently prototyped and the way that some of us are pushing for is to replace the light-dom contents of the selectedoption element when cloning the selected option element, meaning that the "Placeholder content goes ..." text child of the selectedoption element will always get removed by the browser.

<!-- comment id=2077748338 author=josepharhar created=2024-04-25T16:57:52Z url=https://github.com/openui/open-ui/issues/637#issuecomment-2077748338 -->
## @josepharhar — 2024-04-25

Is all we need to discuss here the idea of putting content inside the selectedoption element? I still think it would be better to follow the paradigm that the existing select element has with its options to do placeholders, especially now that we are re-using the select element.

<!-- comment id=2077928559 author=css-meeting-bot created=2024-04-25T18:37:20Z url=https://github.com/openui/open-ui/issues/637#issuecomment-2077928559 resolution=true -->
## @css-meeting-bot — 2024-04-25

The Open UI Community Group just discussed `SelectMenu placeholder capability?`, and agreed to the following:

* `RESOLVED: Keep the previous resolution which reuses the existing APIs for placeholders for select elements`

<details><summary>The full IRC log of that discussion</summary>
&lt;masonf> q+<br>
&lt;brecht_dr> jarhar We discussed this in the past and we resolved on supporting the bahavior that the select element already has. Where we can use the disabled and selected, etc attributes<br>
&lt;brecht_dr> jarhar the issue was never closed<br>
&lt;dandclark> q+<br>
&lt;brecht_dr> Luke Thinking of the selectedopion as a slot. which it kinda is. Should we just put placeholder in there? then you would not need the empty options<br>
&lt;gregwhitworth> q+<br>
&lt;brecht_dr> q+<br>
&lt;gregwhitworth> ack masonf<br>
&lt;brecht_dr> masonf We are now re-using the select element, I think we should re-use those paradigms<br>
&lt;una> +1 to masonf<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> +1 to masonf<br>
&lt;brecht_dr> masonf as much as possible, It would get confusing otherwise<br>
&lt;gregwhitworth> ack dandclark<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;brecht_dr> dandclark When we started from a clean slate that sounded good<br>
&lt;gregwhitworth> brecht_dr: I do agree that this was something we discussed with another element, but when I saw the &lt;selectedoption> capability I do think it's really neat<br>
&lt;gregwhitworth> brecht_dr: it could be really powerful and it wouldn't be breaking for existing &lt;select>s<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> masonf: my fear would be that we just resolved on now makes this really brittle if we did that<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;brecht_dr> gregwhitworth I agree with the spirit of what you said brecht_dr<br>
&lt;brecht_dr> But now that we are using select, this is not a technological usecase battle we need to have<br>
&lt;una> +1<br>
&lt;brecht_dr> gregwhitworth But now that we are using select, this is not a technological usecase battle we need to have<br>
&lt;Luke> +1 that's a fair summary<br>
&lt;jarhar> Proposed resolution: Keep the previous resolution which reuses the existing APIs for placeholders for select elements<br>
&lt;Luke> +1<br>
&lt;brecht_dr> +1<br>
&lt;masonf> +1<br>
&lt;una> +1<br>
&lt;dandclark> +1<br>
&lt;flackr> +1<br>
&lt;dbaron> +1<br>
&lt;jarhar> RESOLVED: Keep the previous resolution which reuses the existing APIs for placeholders for select elements<br>
</details>
