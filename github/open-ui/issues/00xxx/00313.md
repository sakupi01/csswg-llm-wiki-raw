---
number: 313
title: "[checkbox/switch] unchecked value behaviour"
state: "open"
labels: ["WHATWG", "checkbox"]
author: "nuxodin"
created_at: "2021-04-06T19:50:43Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/313"
comments: 17
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

## Discussion Summary

Currently checkboxes (and switches) have an odd behaviour around their value property and the forms submission behaviour.

Take `<input type="checkbox" name="a">` this element will reflect .value as "on", but when a parent form is submitted it won't include any entry for "a".

As the checkbox is unchecked it's quite common to want false submitted when unchecked and true when checked. You can do the latter part by adding `value="true"` but when unchecked it still won't be included in the form submission (and .value will still return "true".)

There's workarounds using JS where you can listen for the `formdata` event and manually add the entry when unchecked, but it would be nice if we had a HTML solution to this.

Possibly a new attribute (name tbd) such as `uncheckedvalue="false"` which includes the input in the form submission automatically and perhaps updates the value getter to return the uncheckedvalue attribute's value when unchecked.

### Original comment

I would really like it that when a checkbox is unchecked its value is an empty string.

I don't know about others, but i have to do this hack all the time:
```js
let value = el.value; // other input
if (el.type==='checkbox') { // checkboxes
    value = el.checked ? el.value : '';
}
```

<!-- comment id=814897480 author=nuxodin created=2021-04-07T13:06:02Z url=https://github.com/openui/open-ui/issues/313#issuecomment-814897480 -->
## @nuxodin — 2021-04-07

I also use this "hack" to send an "unchecked" checkbox in a form:

```html
<input type='hidden' value='' name='accepted'>
<input type='checkbox' value='1' name='accepted'>
```

This hack has a rating of 592 at stackoverflow 
https://stackoverflow.com/a/1992745/4865307

And it is mentioned at MDN:
https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Input/checkbox

I think if we have the possibilities to redesign the checkbox, we should improve it to this behavior.

```html
<checkbox value="1" name=accepted checked> <!-- checkbox.value === '1' -->
<checkbox value="1" name=accepted> <!-- checkbox.value === ''  -->
```

Conceivable would also be a future expansion with an attribute like "falsy".
```html
<checkbox value="on" falsy="off" name=accepted>
```

<!-- comment id=822961551 author=gregwhitworth created=2021-04-20T04:16:05Z url=https://github.com/openui/open-ui/issues/313#issuecomment-822961551 -->
## @gregwhitworth — 2021-04-20

@nuxodin thank you so much for filing this. This is the exact same issue that we just discussed in #311 - @domenic actually hits on the confusion of the current checkbox in this [comment](https://github.com/openui/open-ui/issues/311#issuecomment-816248592).

Likewise this issue you raised was brought up to the TAG [here](https://github.com/w3ctag/design-principles/issues/279). Ultimately the group seems to be in agreement that there need to be better cohesion in how the initial states relate to that of the IDL values themselves.

As far as your proposals are concerned they need to follow the proposal for `open` in `<popup>` in that we should be clear in what is controlling the initial state vs reflection in the IDL. Effectively the groups seem to be leaning towards making it clear in the name that you're setting the initial state & value rather than that content attribute reflecting the value. @domenic do you want to add any additional thoughts here?

<!-- comment id=1072903416 author=github-actions[bot] created=2022-03-19T00:31:52Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1072903416 -->
## @github-actions[bot] — 2022-03-19

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=1112161685 author=nuxodin created=2022-04-28T12:48:04Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1112161685 -->
## @nuxodin — 2022-04-28

My final solution would be:

```html
<checkbox on=yes off=no name=accepted>
```
```js
checkbox.value // no
checkbox.checked = true;
checkbox.value // yes
```

Advantages: 
- Also unchecked checkboxes sends something (as default an empty string)
- the property "value" no longer incorrectly reflects the attribute "value" (the attribute no longer exists)
- "on" and "off" are short and clear attributes

<!-- comment id=1291292929 author=github-actions[bot] created=2022-10-26T00:51:53Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1291292929 -->
## @github-actions[bot] — 2022-10-26

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=1673908856 author=nuxodin created=2023-08-10T20:57:52Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1673908856 -->
## @nuxodin — 2023-08-10

This suggestion was discussed and it was concluded that traditional check boxes should also be given an off value (as I understand it)
https://github.com/whatwg/html/issues/4180#issuecomment-1645108696

This will probably look something like this:

```html
<input type=checkbox value=yes name=accepted **offvalue=no**>.
```

Unfortunately this will result in `checkbox.value` not being able to have the selected value, but always the on-state value, as this needs to remain compatible :(

So we still have to do something like this:

```js
let value = el.value; // other input
if (el.type==='checkbox') { // checkboxes
    // value = el.checked ? el.value : ''; // old
    value = el.checked ? el.value : el.offValue;
}
```
I would rather there were new checkboxes that had a better API.

<!-- comment id=1683665636 author=annevk created=2023-08-18T09:54:34Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1683665636 -->
## @annevk — 2023-08-18

I don't think that's necessarily true @nuxodin. With an opt-in attribute we could make `el.value` take that into account. The `value` IDL attribute and content attribute are different after all. `defaultValue` is the one that reflects the `value` content attribute.

<!-- comment id=1683683095 author=nuxodin created=2023-08-18T10:08:40Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1683683095 -->
## @nuxodin — 2023-08-18

Do you mean something like that?

Current behavior

```html
<input type=checkbox value="true" checked> 
// .value == "true"
<input type=checkbox value="true">
// .value == "true" // i think .value should be "" (empty string)
```

Future

```html
<input type=checkbox value="true" offvalue="false" checked>
// .value == "true"
<input type=checkbox value="true" offvalue="false">
// .value == "false"
```

<!-- comment id=1683757405 author=annevk created=2023-08-18T11:11:00Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1683757405 -->
## @annevk — 2023-08-18

Yes, something like that. What would help motivate such a feature would be more evidence that this is a problem web developers frequently run into. Stack Overflow questions might help with that as well as JS libraries that work around the issue.

<!-- comment id=1945173348 author=github-actions[bot] created=2024-02-15T00:34:07Z url=https://github.com/openui/open-ui/issues/313#issuecomment-1945173348 -->
## @github-actions[bot] — 2024-02-15

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2869680138 author=lukewarlow created=2025-05-11T10:40:34Z url=https://github.com/openui/open-ui/issues/313#issuecomment-2869680138 -->
## @lukewarlow — 2025-05-11

As far as evidence the Vue.js framework has a special API specifically for checkboxes.

They have a true-value and false-value binding. https://vuejs.org/guide/essentials/forms#checkbox-1

Something like a new uncheckedvalue attribute set to false would seem to be in a similar space.

Fwiw this is something I commonly see, people often want checkboxes (and switches) to be a Boolean value rather than submitting conditionally.

<!-- comment id=3505511108 author=github-actions[bot] created=2025-11-08T00:51:27Z url=https://github.com/openui/open-ui/issues/313#issuecomment-3505511108 -->
## @github-actions[bot] — 2025-11-08

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=4624910599 author=keithamus created=2026-06-04T18:13:42Z url=https://github.com/openui/open-ui/issues/313#issuecomment-4624910599 -->
## @keithamus — 2026-06-04

I need to WHATWG this in some way.

<!-- comment id=4993615374 author=lukewarlow created=2026-07-16T15:22:06Z url=https://github.com/openui/open-ui/issues/313#issuecomment-4993615374 -->
## @lukewarlow — 2026-07-16

Just went to open a new issue but see this one exists so we'll keep this one open. This came up as a passing comment on my review of #1470 I think this is something that would be good to address. The behaviour of an unchecked checkbox (and now switch) not submitting any value has just never sat right with me. I think a new attribute like `uncheckedvalue="some other value"` (name to be bikeshedded to oblivion I'm sure), that changes the submission behaviour and then also making it so that .value reflects this value when unchecked would be great.

I guess a further question I have is what about indeterminate. Do we need a third value available for that state? How does indeterminate work today regarding form submission? I know indeterminate isn't mutually exclusive with checked/unchecked (as much as css might try to say otherwise).

<!-- comment id=4993951113 author=frehner created=2026-07-16T15:56:49Z url=https://github.com/openui/open-ui/issues/313#issuecomment-4993951113 -->
## @frehner — 2026-07-16

>How does indeterminate work today regarding form submission? I know indeterminate isn't mutually exclusive with checked/unchecked (as much as css might try to say otherwise).

from https://html.spec.whatwg.org/multipage/input.html#the-input-element

> Each [input](https://html.spec.whatwg.org/multipage/input.html#the-input-element) element has an indeterminateness, a boolean which is initially false. It only has an effect on [checkbox](https://html.spec.whatwg.org/multipage/input.html#checkbox-state-(type=checkbox)) controls, including their rendering and accessibility semantics. An [input](https://html.spec.whatwg.org/multipage/input.html#the-input-element) element's indeterminateness is independent of its [checkedness](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#concept-fe-checked).

So it doesn't affect the form submission in any way is my understanding.

Relatedly, it would be nice to add an attribute for indeterminate -- it's annoying that it's only a property. :) 

<!-- comment id=4994121630 author=lukewarlow created=2026-07-16T16:15:01Z url=https://github.com/openui/open-ui/issues/313#issuecomment-4994121630 -->
## @lukewarlow — 2026-07-16

Yeah an `initiallyindeterminate` attribute (name tbd) would be neat. I'm guessing an issue for that already exists in whatwg/html, but perhaps if not worth opening one here or there.

<!-- comment id=4994564554 author=frehner created=2026-07-16T17:04:43Z url=https://github.com/openui/open-ui/issues/313#issuecomment-4994564554 -->
## @frehner — 2026-07-16

This is tracking the attribute proposal I believe. https://github.com/whatwg/html/issues/6578
