---
number: 809
title: "Should we worry about existing button behavior?"
state: "closed"
labels: ["select"]
author: "bkardell"
created_at: "2023-08-21T22:03:43Z"
closed_at: "2023-08-29T17:00:48Z"
url: "https://github.com/openui/open-ui/issues/809"
comments: 7
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

Opening this out of an abundance of caution, but I noticed only in the past few days that 3 weeks ago as part of #702 it was suggested that we use a `<button type="_something_">` and I just want to make sure that we're ok with that specific detail. 

I think the idea here would allow an author to supplant this and if we do use that approach, it seems to me that in any unsupported browser it would result in someone clicking a button to, say, expand the list, and instead it submits the form.

Maybe everyone is ok with that on purpose somehow, but I'd really like to just make sure it didn't get overlooked (I overlooked it) because there is _a lot_ happening in that issue.  It seems that this detail [originated](https://github.com/openui/open-ui/issues/702#issuecomment-1654256533) as one of many detail during the call by [josepharhar](https://github.com/josepharhar)  who then did a lot of real work to [come up with an actual proposal for _all of the things being discussed there_](https://github.com/openui/open-ui/issues/702#issuecomment-1662708138).


Everyone had positive responses, including @domenic (who opened the issue with concerns from the HTML POV), so that's good - but re-reading it I'm not entirely confident that wasn't mostly about the nice shift in all of the other moving parts (which I also like).

<!-- comment id=1687601989 author=hidde created=2023-08-22T07:10:28Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1687601989 -->
## @hidde — 2023-08-22

Hm, now that you point it out… it does seem worrisome that authors could not progressively enhance the `type="_something_"` button, as the not-submitting-a-form bit would depend on the browser fully/properly supporting `selectlist` (and undoing that as part of their support).

<!-- comment id=1692167822 author=mfreed7 created=2023-08-24T17:55:08Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1692167822 -->
## @mfreed7 — 2023-08-24

Perhaps an alternative is to use a new attribute instead, and require `type=button` to avoid form submissions on non-supported browsers?

```html
<selectlist>
  <button type="button" selectlistbutton>Pick a value</button>
</selectlist>
```

That new attribute would also have to be added to the `HTMLInputElement`:

```html
<selectlist>
  <input type="button" value="Pick a value" selectlistbutton>
</selectlist>
```

But that'd do the trick, right? The name needs bikeshedding, of course. Perhaps `activationbutton` or something semi-generic?

The other approach, which I'd **really** like to avoid, is inventing a new element for this button:

```html
<selectlist>
  <selectlistbutton> Pick a value </selectlistbutton>
</selectlist>
```

That's terrible by itself, because now you have yet another platform thing that is a button, but also, each new form control that we tackle will likely need its own set of new buttons. Think `<datepickertodaybutton>` and `<fileinputpickafilebutton>`, etc.

<!-- comment id=1692173801 author=josepharhar created=2023-08-24T17:59:44Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1692173801 -->
## @josepharhar — 2023-08-24

> it was suggested that we use a `<button type="_something_">` and I just want to make sure that we're ok with that specific detail.

I think that reusing `<button>` will just make things easier, but if there's anything that `<button>` can't do I'm all ears

> I think the idea here would allow an author to supplant this and if we do use that approach, it seems to me that in any unsupported browser it would result in someone clicking a button to, say, expand the list, and instead it submits the form.

In an unsupported browser, `<selectlist>` will already act weird anyway right? I feel like the form submission of a type=selectlist button isn't a big deal in comparison. If I wanted to use selectlist and support an unsupported browser, then I'd probably not use selectlist until its supported or not use selectlist when running in that browser or use a polyfill that would take care of this issue.

<!-- comment id=1692178188 author=domenic created=2023-08-24T18:03:09Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1692178188 -->
## @domenic — 2023-08-24

I agree with Joey's position. If you're running any sort of script, e.g. a polyfill, it's easy to set up a `preventDefault()` handler in that script to prevent incorrect behavior in downlevel browsers.

<!-- comment id=1692188496 author=lukewarlow created=2023-08-24T18:11:42Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1692188496 -->
## @lukewarlow — 2023-08-24

Could unknown elements that aren't valid custom element (single word name) be changed to disable default behaviour of buttons submitting forms? Probably not web compatible but just an idea.

<!-- comment id=1692191406 author=css-meeting-bot created=2023-08-24T18:14:01Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1692191406 resolution=true -->
## @css-meeting-bot — 2023-08-24

The Open UI Community Group just discussed `[selectlist] Should we worry about existing button behavior? #809`, and agreed to the following:

* `RESOLVED: Keep the current <button type=selectlist> proposal the way it is because polyfills would be required to make selectlist work anyway`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> topic: [selectlist] Should we worry about existing button behavior? #﻿809<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/809<br>
&lt;hdv> masonf: the issue brought up here is about the danger that browsers that don't support selectlist would also not support the button behaviour, which would then make that those buttons trigger form submission as is the default for buttons in forms<br>
&lt;hdv> masonf: I agree it's an issue and we should think about it<br>
&lt;hdv> masonf: two options: one is to create a new element instead of an attribute… I don't really like it: at best confusing, at worst… terrible<br>
&lt;jarhar> q+<br>
&lt;hdv> masonf: another is don't use the type attribute but a different attribute, that we could bikeshed<br>
&lt;hdv> q+<br>
&lt;masonf> ack jarhar<br>
&lt;hdv> jarhar: I posted a comment on the issue… I don't think form submission behaviour is something to worry about… if you're in a browser that doesn't support selectlist, having selectlist wouldn't really make sense anyway<br>
&lt;hdv> jarhar: I don't think it's worth trying to address it… in those browsers you would probably do something completely different anyway<br>
&lt;hdv> masonf: on the other hand it's worse if it does something else and renders the form<br>
&lt;masonf> ack hdv<br>
&lt;scotto_> q+<br>
&lt;dbaron> hdv: Is the idea that developers would use the type attribute in addition to the other attribute?<br>
&lt;dbaron> masonf: yes, they'd need to use type=button along with the new attribute<br>
&lt;hdv> lukew: one way you could do this is @@@<br>
&lt;masonf> ack scotto_<br>
&lt;hdv> masonf: interesting suggestion, but could cause the opposite problem<br>
&lt;hdv> scotto_: that could also break custom elements as they are all unknown elements<br>
&lt;dbaron> s/@@@/make &lt;button> elements inside of unknown elements not submit forms/<br>
&lt;hdv> scotto_: instead of using type= or another attribute… we would still run in the same issue if some developer _doesn't_ add the type attribute, if they forget it<br>
&lt;hdv> scotto_: as jarhar mentioned before… if someone uses this without a polyfill, many more things would break… a polyfill should address this case<br>
&lt;hdv> scotto_: I don't know how someone would use this without a polyfill<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: the more I think about it, the more I agree… you could get lots of other funny business when you try to do this<br>
&lt;hdv> masonf: sounds like we're heading to the conclusion that this issue is moot<br>
&lt;hdv> masonf: any objections to resolving that<br>
&lt;hdv> +1 that makes sense<br>
&lt;dbaron> +1<br>
&lt;jarhar> Proposed resolution: Keep the current &lt;button type=selectlist> proposal the way it is because polyfills would be required to make selectlist work anyway<br>
&lt;scotto_> +1<br>
&lt;jarhar> RESOLVED: Keep the current &lt;button type=selectlist> proposal the way it is because polyfills would be required to make selectlist work anyway<br>
&lt;hdv> github: Naming of the selected value element<br>
</details>

<!-- comment id=1697824966 author=josepharhar created=2023-08-29T17:00:48Z url=https://github.com/openui/open-ui/issues/809#issuecomment-1697824966 -->
## @josepharhar — 2023-08-29

I created another issue which poses similar questions to this one: https://github.com/openui/open-ui/issues/821
