---
number: 939
title: "[combobox] Is type=\"selectlist\" correct for the input?"
state: "closed"
labels: ["Combobox", "needs-triage"]
author: "lukewarlow"
created_at: "2023-11-07T23:52:12Z"
closed_at: "2024-10-18T22:29:43Z"
url: "https://github.com/openui/open-ui/issues/939"
comments: 9
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

I feel like adding a new input type doesn't really add anything?

Search feels appropriate?

<!-- comment id=1800694129 author=lukewarlow created=2023-11-08T00:17:12Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1800694129 -->
## @lukewarlow — 2023-11-08

Alternatively I reckon at least call it combobox not selectlist. While they're similar they are distinct components.

<!-- comment id=1802312758 author=sudheer-gowrigari created=2023-11-08T17:09:33Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1802312758 -->
## @sudheer-gowrigari — 2023-11-08

@gregwhitworth Let's use this issue for discussing all elements of combobox anatomy.?

<!-- comment id=1840787175 author=lukewarlow created=2023-12-05T13:23:46Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1840787175 -->
## @lukewarlow — 2023-12-05

Thinking on this some more I think combobox should allow any type the user wants inside of its input. As the type attribute provides relevant validation.

If I want a telephone input that drops down with previously saved examples I want to get the free validation? (Might be a bad example I don't think telephone actually provides validation but you get the general idea)

<!-- comment id=1841579527 author=sudheer-gowrigari created=2023-12-05T20:40:56Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1841579527 -->
## @sudheer-gowrigari — 2023-12-05

@lukewarlow In the case of `selectlist` the `type=selectlist` attribute indicates to the browser that this button should open the listbox. We need some kind of similar thing(either a unique attribute or type) even for combobox no? 

<!-- comment id=1841593523 author=lukewarlow created=2023-12-05T20:52:01Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1841593523 -->
## @lukewarlow — 2023-12-05

The key difference is selectlist is designed to support multiple buttons (split button design)
Whereas if someone puts 2 inputs into combobox they're asking for shenanigans and that's exactly what they'll get 😂. 

<!-- comment id=1910837275 author=css-meeting-bot created=2024-01-25T19:22:08Z url=https://github.com/openui/open-ui/issues/939#issuecomment-1910837275 resolution=true -->
## @css-meeting-bot — 2024-01-25

The Open UI Community Group just discussed `[combobox] Is type="selectlist" correct for the input?`, and agreed to the following:

* `RESOLVED: the first button inside <select> triggers the popover. If there is more than one, use <button type=popover> to be the one that triggers the popover.`

<details><summary>The full IRC log of that discussion</summary>
&lt;jarhar> luke: current explainer for combobox uses type=selectlist for the input<br>
&lt;jarhar> luke: selectlist is no more so that name is incorrect regardless<br>
&lt;masonf> q+<br>
&lt;jarhar> luke: if select still exists then thats wrong because its not a select or selectlist<br>
&lt;jarhar> luke: so i thought it should be type combobox, but the more i thought about it, the idea of requiring a certain type on the input actually makes sense<br>
&lt;gregwhitworth> q+<br>
&lt;jarhar> luke: it has builtin stuff like there are separate attributes that do this<br>
&lt;jarhar> luke: you might have a dropdown combobox and its for urls, and you want it type=url, or telephone numbers or anything like that where you want to use a builtin input type, so prescribing one doesnt make sense<br>
&lt;gregwhitworth> ack masonf<br>
&lt;brecht_dr> q+<br>
&lt;scotto> q+<br>
&lt;jarhar> masonf: we should follow the model of form in two ways. if you put a button in a form, then that button doesnt need a type and it will submit the form for you. if you have multiple and theres ambiguity then you need to put type on one. in this case i would say type=popover since you get a picker<br>
&lt;jarhar> gregwhitworth: i agree<br>
&lt;jarhar> gregwhitworth: were going to have this same problem for date, color, all of them<br>
&lt;jarhar> gregwhitworth: i like popover as well<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;dbaron> i/luke: current explainer for combobox/ScribeNick: jarhar<br>
&lt;jarhar> brecht_dr: i wonder if it is confusing in some way, select in some way also triggers a popover, or is that too simplistic<br>
&lt;dbaron> Regrets: hdv<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;jarhar> masonf: precisely, in a select youre going to trigger a popover, so lets use it there too<br>
&lt;gregwhitworth> ack scotto<br>
&lt;jarhar> scotto: in the future, html will tell us we already have a combobox, why cant you just put this inside a wrapper and you already have a datalist you can style it, conversation about select now<br>
&lt;gregwhitworth> q+<br>
&lt;luke> q+<br>
&lt;jarhar> scotto: do we need type on input elements or can we just use the list attribute to point to a popover, is that what were doing here?<br>
&lt;jarhar> gregwhitworth: thats a valid argument. it is accomplishing the same thing. less the combobox scenario, but what we were trying to do - select is going to be the same way, so what if we have a date input or color that are extensible<br>
&lt;jarhar> gregwhitworth: i agree with you on the combobox one, i wonder if the same statement holds true for select<br>
&lt;jarhar> masonf: the problem with the input is that you cant have children inside the input<br>
&lt;jarhar> masonf: i like that were thinking forward, but i like crossing one bridge at a time<br>
&lt;jarhar> masonf: lets add an attribute to input that allows it to have children<br>
&lt;jarhar> scotto: but does it need to have children? depending on the platform, select is a combobox<br>
&lt;bkardell_> masonf: is there any precedent for parsing differently based on an attribute?<br>
&lt;jarhar> scotto: inserting a button or datalist in select, what the accessibility mappings for that is ignore the select element and go to its children in those cases because you cant have a combobox that has a button inside of it - theyre siblings, not descendants<br>
&lt;jarhar> masonf: the accessibility mappings can be done either way though<br>
&lt;jarhar> scotto: but even for the inputs that do have browser nested buttons inside of them, theres buttons inside the shadowdom<br>
&lt;jarhar> masonf: youre right, the input element has shadowdom and it contains buttons in the same way that were proposing the select element<br>
&lt;jarhar> masonf: the weird one is where theres an idref and youre connecting things in the dom<br>
&lt;gregwhitworth> q+<br>
&lt;jarhar> scotto: do we need to undo conventions and allow for nesting things that up to this point werent allowed when we could just have them be siblings instead<br>
&lt;jarhar> masonf: one is backwards compatability<br>
&lt;jarhar> masonf: one thing that nesting datalists is that it allows old browsers to still work<br>
&lt;gregwhitworth> ack luke<br>
&lt;jarhar> luke: my dream would be that html doesnt have void elements, thats not going to be a thing<br>
&lt;jarhar> luke: its weird that you cant nest children inside certain elements<br>
&lt;bkardell_> xml5?<br>
&lt;jarhar> luke: it would make lots of styling things easier<br>
&lt;masonf> &lt;input allowschildren type=text><br>
&lt;jarhar> luke: if we could have an attribute that lets you nest stuff inside of input, that would be great<br>
&lt;jarhar> luke: i dont know if you can switch something being void or not based on an attribute<br>
&lt;jarhar> luke: your original response mason is that if we have the type be popover, that then limits stuff like the ability to do type email<br>
&lt;jarhar> luke: you want the combobox to be a dropdown of emails, you cant have builtin validation and native keyboard @ symbol<br>
&lt;jarhar> luke: which is something you do get when you allow any type<br>
&lt;jarhar> luke: today, you can have any type you want and attach it to a datalist<br>
&lt;jarhar> luke: i dont think the combobox should remove that capability<br>
&lt;jarhar> luke: you would get styling but then have to do more stuff yourself<br>
&lt;gregwhitworth> q?<br>
&lt;jarhar> luke: if you could have input type date with suggestions in combobox that would be a good thing<br>
&lt;jarhar> masonf: we are talking about select with a button and the type of that button<br>
&lt;jarhar> masonf: the second thing is customizing an input like a type email<br>
&lt;jarhar> masonf: youre right you cant use type popover and type email<br>
&lt;jarhar> masonf: i wonder if the solution to that is to have input type email with an attribute that allows it to have children. the button would have type popover<br>
&lt;jarhar> masonf: i wonder if we're crossing two things<br>
&lt;jarhar> gregwhitworth: we are, and that is the problem with the issue<br>
&lt;jarhar> gregwhitworth: combobox was very much 90ish percent of a combobox is an extension of a dropdown or select<br>
&lt;jarhar> gregwhitworth: we just copy the shadowdom and paste it in<br>
&lt;jarhar> gregwhitworth: at the time it was that selectlist and thats how the invoker happened<br>
&lt;jarhar> gregwhitworth: i would prefer to just resolve on select since its a known quantity at the moment<br>
&lt;jarhar> gregwhitworth: this has a title of combobox, it was trying to carry over the learnings of select. if we resolve on select, then as we work on combobox we can account for the other things<br>
&lt;jarhar> gregwhitworth: theres a lot of things untangled, we can set the judicial precedent<br>
&lt;jarhar> gregwhitworth: this issue may not be relevant when we do combobox<br>
&lt;jarhar> luke: i worry that type=popover may be confusing<br>
&lt;jarhar> luke: button popovertarget is a standalone thing, and now youve got button type=popover which is changing the type attribute in this one specific place<br>
&lt;jarhar> luke: kind of makes you wonder why does button type=popover - is that not how popovers worked in the first place<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;jarhar> luke: for now we could resolve on type popover and we could come back to it<br>
&lt;jarhar> q+<br>
&lt;jarhar> masonf: we have to rename everything 3 or 4 times, this will get one of them out of the way<br>
&lt;gregwhitworth> ack jarhar<br>
&lt;dbaron> ScribeNick: gregwhitworth<br>
&lt;gregwhitworth> jarhar: I was going to say, I agree that we'll need to pick this then take it up to WHATWG and the way that I'm going to tackle it is we need split button<br>
&lt;gregwhitworth> jarhar: and we need to be able to differentiate between the two. Then people will want to do it another way and I can see things going that<br>
&lt;dbaron> ScribeNick: jarhar<br>
&lt;jarhar> masonf: we might need to back off on split button and then type select goes away entirely<br>
&lt;dbaron> s/we might need to/jarhar was saying that we might need to/<br>
&lt;masonf> Proposed resolution: the first button inside &lt;select> triggers the popover. If there is more than one, use &lt;button type=popover> to be the one that triggers the popover.<br>
&lt;luke> +1<br>
&lt;keithamus> +1<br>
&lt;masonf> RESOLVED: the first button inside &lt;select> triggers the popover. If there is more than one, use &lt;button type=popover> to be the one that triggers the popover.<br>
</details>

<!-- comment id=2058145958 author=sudheer-gowrigari created=2024-04-16T03:02:13Z url=https://github.com/openui/open-ui/issues/939#issuecomment-2058145958 -->
## @sudheer-gowrigari — 2024-04-16

> The Open UI Community Group just discussed `[combobox] Is type="selectlist" correct for the input?`, and agreed to the following:
> 
> * `RESOLVED: the first button inside <select> triggers the popover. If there is more than one, use <button type=popover> to be the one that triggers the popover.`
> 
> The full IRC log of that discussion

We would like to reopen this discussion with reference to [this earlier comment](https://github.com/openui/open-ui/issues/839#issuecomment-1758381233). We believe the `combobox` should include:

- An input field with a "text" type for user entries.
- A button to display the selected option.
- An optional button to trigger a popover when clicked ⬇️.
```
<input type="text"> 
    <button><selectedoption></selectedoption></button>
    <button type=popover>⬇️</button>
</input>
```
![comobobox-filter (1)](https://github.com/openui/open-ui/assets/11438997/e5c8fbc9-5bdc-41a0-9dea-18e975a58e69)


Examples from Twitter and Facebook, where typing/clicking in the input field automatically triggers a popover.

https://github.com/openui/open-ui/assets/11438997/da9f1063-b57f-4791-9167-912599b9ce69



https://github.com/openui/open-ui/assets/11438997/59bf40dc-f3b2-4f25-9b17-6e9e65f06807

<!-- comment id=2064923649 author=css-meeting-bot created=2024-04-18T18:48:20Z url=https://github.com/openui/open-ui/issues/939#issuecomment-2064923649 resolution=true -->
## @css-meeting-bot — 2024-04-18

The Open UI Community Group just discussed `[combobox] Is type="selectlist" correct for the input?`.

* `RESOLVED: A combobox will have a button so that the datalist is shown. Extend datalist to have programmatic support for showPicker() to allow for hiding of the button when desired.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> gregwhitworth: Luke was updating the explainer for selectlist… this intertwines with other issues in invoker, that we only support buttons and anchors<br>
&lt;hdv> gregwhitworth: the problem that we found when we looked at a bunch of comboboxesis that many of them don't have buttons… like Facebook and Gmail… they have buttons but those have different functions<br>
&lt;hdv> gregwhitworth: so sidestepping thinking about HTML… technically you would want input type=text with children available, but what we're trying to put forward is that input type=text is the one opening the popup within the combobox<br>
&lt;keithamus> q+<br>
&lt;hdv> gregwhitworth: TLDR what type should this be?<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;brecht_dr> q+<br>
&lt;hdv> keithamus: we already have input with datalist… I wonder if that's something to explore…are they two different patterns? I genuinely don't know the answer to that question<br>
&lt;hdv> gregwhitworth: we cover that at the base of the doc… happy to have this debate as we'll probably have it elsewehre… can you do this today, yes you can bind two elements today, but the combobox can do it out of the box?<br>
&lt;scotto> q+<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> keithamus:  looking at it through a different lense… it's turning text input into a combobox vs doing it with buttons… eg what does this new thing do that input and datalist don't? I get it is terrible to style today, but is trying to improve how that works worth exploring?<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;hdv> brecht_dr: I think datalist has been mentioned a few times more in the group… that we want more style functionality… I would be all for that<br>
&lt;keithamus> q+<br>
&lt;hdv> brecht_dr: with combobox you're searching for something, would a search type input make sense? eg on mobile device in some cases you would get a specific type of keyboard?<br>
&lt;gregwhitworth> ack scotto<br>
&lt;hdv> scotto: re what Keith was saying before… seeing the the way that selectlist element is now just select element… I could see that might happen with this too<br>
&lt;hdv> scotto: if we really want HTML to allow for better styling, we need to threaten them that we want a new element<br>
&lt;hdv> scotto: I like the idea of having a combobox element<br>
&lt;hdv> scotto: but you can really have comboboxes that have different purposes<br>
&lt;hdv> scotto: browsers right now treat a lot of native text fields as comboboxes because of autocompletion that pops up in a lot of elements<br>
&lt;hdv> scotto: having the new combobox element would be a good way of mitigating the potential conflict between browser comboboxes in today's text inputs vs the new thing<br>
&lt;hdv> gregwhitworth: good points<br>
&lt;hdv> gregwhitworth: the anatomy gets drastically updated, basically like a copy of select<br>
&lt;hdv> gregwhitworth: I'l argue in many instances this is the most commonly used element; you can type but you can't see hwat you're typing<br>
&lt;jarhar> q?<br>
&lt;hdv> gregwhitworth: HTML is an implementation detail… the key things of why we need this: irrespective of what type we set this to, we need to be able to set children for a variety usecases, eg email addresses in email client or labels on GitHub<br>
&lt;hdv> gregwhitworth: we expect datalist to become more styleable because it being used in select<br>
&lt;hdv> gregwhitworth: so now a complex textfield with contenteditable and children below it… how do we invoke the popup?<br>
&lt;hdv> gregwhitworth: whether it's type search or type text… for the holy grail of being able to set children, that's a long way<br>
&lt;hdv> gregwhitworth: I'd need something beyond button or anchor to cover these kinds of use cases<br>
&lt;hdv> keithamus: I think input is a non starter, because it is not backwards (or forwards) compatible…<br>
&lt;hdv> keithamus: having input type=whatever, the shape of that HTML is not really tenable…however… the screen recordings attached to this issue are of searches… the presented dropdowns are effectively autocompletes<br>
&lt;hdv> keithamus: which are different from a bounded box of options of which you must select one<br>
&lt;hdv> keithamus: vs search is looking through potential searches to complete<br>
&lt;hdv> gregwhitworth: that takes me back full circle… if you'd build this, what would it be if not input type=whatever?<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;brecht_dr> q+<br>
&lt;hdv> keithamus: sounds fun if invokers invoke datalist… seems like a reasonable extension… I'm fuzzy on it but there is a valid diference between a select and a datalist that does the autocomplete aspect<br>
&lt;gregwhitworth> q+<br>
&lt;hdv>  keithamus: the search example is the only presented that doesn't have a button to open it… are there others, or if it's just search can we find some other solution to it?<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;hdv> brecht_dr: a new element seems like a more logical approach because there are more options and possibilities in what people want from a combobox, eg the way it searches and behaves, there are many more edge cases in a combobox than there are in a styleable select<br>
&lt;hdv> brecht_dr: this seems to be as one of those things that should be a new element<br>
&lt;hdv> gregwhitworth: I feel like we're starting to determine how this implemented in the browser, and that's the part we'er starting to struggle<br>
&lt;keithamus> q?<br>
&lt;hdv> gregwhitworth: do you think we can resolve this with invoker on an input type=text that invokes a datalist?<br>
&lt;hdv> keithamus: doesn't even need to be invokers, not sure if it exists today, is there a way to programmatically open a datalist?<br>
&lt;hdv> keithamus: if that doesn't exist that seems like a great first thing to loko at<br>
&lt;hdv>  s/loko/look<br>
&lt;hdv> keithamus: people don't use datalist today because it's not great to style… but that doesn't really help solving the combobox use case<br>
&lt;hdv> keithamus: you would need some kind of attribute to say this element is open and takes descendance, to not trip up the parser?<br>
&lt;hdv> keithamus: a combobox is a lot of things smushed together, A B and C… the search has A B but doesn't make it a combobox?<br>
&lt;scotto> q+<br>
&lt;hdv> gregwhitworth: a set of options plus a text field but now I can see what I'm typing?<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> keithamus: there's two things… one is here's the thing you're allowed to pick from,  but combobox is very free form<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> keithamus: so I'm not compelled enough to feel like presenting search as the smoking gun that needs to change the design<br>
&lt;hdv> keithamus: are there similar form designs that aren't search that don't have the button<br>
&lt;gregwhitworth> ack scotto<br>
&lt;hdv> s/button/button?<br>
&lt;hdv> scotto: the reason I went on the queue … why does the invoker need to be on the input… why could it not by default have a button?<br>
&lt;hdv> scotto: like with popover, authors may in the future be using it without having to use it themselves<br>
&lt;hdv> scotto: for most things authors want to do we want them to use buttons to invoke<br>
&lt;hdv> gregwhitworth: seems we're a friction point… what author can build vs HTML<br>
&lt;hdv> gregwhitworth: if this could use invokers under the hood without allowing authors to use invokers on inputs… I think I'm a bit more ok with it, but tricky to put in the explainer document like that<br>
&lt;hdv> gregwhitworth: now that you explain it like a spectrum, keithamus, I understand where you're coming from<br>
&lt;scotto> just as a related concept - in ARIA there are a number of abstract roles that are useful for browser implementors - but are never meant to be used by authors<br>
&lt;hdv> gregwhitworth: key difference is that it's going to be unbounded content… but am willing to putn on that… let's initially put a button in there, I don't think it would become backwards incompatble if we did<br>
&lt;keithamus> q+<br>
&lt;hdv> gregwhitworth: but raises accessibility problems if we remove that button…<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> keithamus: more thoughts on hiding the button… adding the button and setting it to display none…if there's something the browser can do that platforms cannot, don't see why we coulnd't add programmatic API<br>
&lt;hdv> keithamus: it doesn't need to be declarative, that's an enhancement we can do with JavaScript… there's showPicker() today<br>
&lt;gregwhitworth> Proposed Resolution: A combobox will have a button so that the datalist is shown. Extend datalist to have programmatic support for showPicker() to allow for hiding of the button when desired.<br>
&lt;hdv> keithamus: do we want to talk about combobox?<br>
&lt;keithamus> +1<br>
&lt;hdv> gregwhitworth: this is assuming prior resolution that Open UI want a combobox<br>
&lt;scotto> +1<br>
&lt;brecht_dr> +1<br>
&lt;gregwhitworth> Resolved: A combobox will have a button so that the datalist is shown. Extend datalist to have programmatic support for showPicker() to allow for hiding of the button when desired.<br>
</details>

<!-- comment id=2418257245 author=github-actions[bot] created=2024-10-17T00:45:20Z url=https://github.com/openui/open-ui/issues/939#issuecomment-2418257245 -->
## @github-actions[bot] — 2024-10-17

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.
