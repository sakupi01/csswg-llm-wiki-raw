---
number: 324
title: "[Popup spec] Specify hide event sequence"
state: "closed"
labels: ["needs-triage", "popover", "popover-spec"]
author: "melanierichards"
created_at: "2021-04-09T22:04:18Z"
closed_at: "2023-08-24T16:34:06Z"
url: "https://github.com/openui/open-ui/issues/324"
comments: 2
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

On https://github.com/MicrosoftEdge/MSEdgeExplainers/issues/450, @JanMiksovsky said:

> I think it'd be helpful to add an example describing the sequence of events raised for the hide operation, in the same way the [KeyboardEvent sequence](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key#keyboardevent_sequence) documents the result of a key press. The event sequence could indicate:
> 
> * Relative ordering of blur and hide events. For popups that can have the focus, I assume they'd see `blur` occur first, then `hide`?
> * Relative ordering of events across a stack of popups being dismissed. I assume the topmost popup gets the (non-bubbling?) `hide` event first, then the next one, up the hierarchy.
> * Relative ordering of `hide` events and the actual removal of the popup from view: do all popups being hidden get the `hide` event, then the popups disappear?
> 
> On this topic, I notice that `HTMLDialogElement` doesn't define a `show` event, but that doesn't mean one wouldn't be useful. `Document`, for example, defines events like `load` and `visibilitychange` that can be used when a document is opened, so I thought some consideration could be given to defining a `show` event for popups.

<!-- comment id=1072903371 author=github-actions[bot] created=2022-03-19T00:31:42Z url=https://github.com/openui/open-ui/issues/324#issuecomment-1072903371 -->
## @github-actions[bot] — 2022-03-19

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=1692037027 author=mfreed7 created=2023-08-24T16:34:06Z url=https://github.com/openui/open-ui/issues/324#issuecomment-1692037027 -->
## @mfreed7 — 2023-08-24

I believe this issue has been handled via the [landed popover spec](https://html.spec.whatwg.org/multipage/popover.html). I'd like to close it. If you disagree, please feel free to re-open with a comment about what's still missing.
