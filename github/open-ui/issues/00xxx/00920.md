---
number: 920
title: "[dialog] Declarative indicator for dialog modality"
state: "open"
labels: ["needs-triage"]
author: "lukewarlow"
created_at: "2023-10-31T13:49:29Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/920"
comments: 18
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

The invokers proposal is a brilliant addition and really helps solve some of the developer ergonomic issues with dialogs (along with much more).

However, it misses one key case as I mentioned in https://github.com/whatwg/html/issues/3567, you can't have a modal dialog load open with just HTML, you need JS to call showModal() or a user to click an invoker button.

My use case is that I sometimes have modal dialogs that are linked to a client side router where the URL controls their visibility. I currently call .showModal() on component mount. But if I could instead just do this declaratively that would remove the requirement for client JS execution (think SSR scenarios)

`<dialog open>` works for non-modals but there's no equivalent for modal dialogs.

<!-- comment id=1787264926 author=lukewarlow created=2023-10-31T13:52:48Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787264926 -->
## @lukewarlow — 2023-10-31

Happy to bikeshed etc but my proposal would be a new `modal` attribute. It wouldn't change anything by default it just acts as an indicator when used alongside the open attribute.

My thoughts on what `<dialog modal>` would do when called with JS.

`show()` - Throw an exception (I'm not fully set on this so could probably be persuaded either way).

`showModal()` - Work as expected.

`<dialog popover modal>` would be an author error and would throw in some cases. Not sure which one should win though.

This could also simplify the invoker proposal. Currently we have a [toggleModal](https://open-ui.org/components/invokers.explainer/#defaults) action, and Keith has added an additional `toggle` action inside of his Chromium CL.

If we added a new `modal` attribute, we could just have a single `toggle` action that would just work as expected?

Depending on the behaviour of `show()` JS function, we could even just have a `show` action and forget `showModal`?

<!-- comment id=1787266365 author=lukewarlow created=2023-10-31T13:53:35Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787266365 -->
## @lukewarlow — 2023-10-31

Adding to agenda as it might help simplify the invokers work that's ongoing. (Would obviously need WHATWG buy in too)

<!-- comment id=1787267657 author=YummyBacon5 created=2023-10-31T13:54:18Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787267657 -->
## @YummyBacon5 — 2023-10-31

> If we added a new modal attribute

Could `<dialog open="modal">` not work?

<!-- comment id=1787271512 author=lukewarlow created=2023-10-31T13:56:19Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787271512 -->
## @lukewarlow — 2023-10-31

> Could <dialog open="modal"> not work?

It would potentially for the open as modal on page load. Potentially as I'm not sure on changing boolean attribute to take values. I guess it was done with hidden so could be fine?

It wouldn't, however, additionally remove ambiguity around invoker actions though.

<!-- comment id=1787521525 author=keithamus created=2023-10-31T16:03:10Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787521525 -->
## @keithamus — 2023-10-31

> Potentially as I'm not sure on changing boolean attribute to take values. I guess it was done with hidden so could be fine?

Yes this is fine as it will become an enumerated attribute where the invalid/empty states map to &lt;whatever the current behaviour is&gt;. The problem is that the current behaviour will need a name. 

<!-- comment id=1787590730 author=lukewarlow created=2023-10-31T16:42:24Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1787590730 -->
## @lukewarlow — 2023-10-31

Naming things is hard, could that be another reason to use a new attribute for the modality instead. Then we get potential invoker benefits *and* we don't have to think of naming <whatever the current behaviour is>

<!-- comment id=1804346716 author=mfreed7 created=2023-11-09T18:32:05Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1804346716 -->
## @mfreed7 — 2023-11-09

Some quick references for naming and behavior, since a declarative "open when loaded" feature was initially part of Popover but was removed.

- https://github.com/openui/open-ui/issues/631
- https://github.com/whatwg/html/issues/7785#issuecomment-1294296444
- https://github.com/openui/open-ui/issues/500

<!-- comment id=1804534179 author=css-meeting-bot created=2023-11-09T19:47:06Z url=https://github.com/openui/open-ui/issues/920#issuecomment-1804534179 -->
## @css-meeting-bot — 2023-11-09

The Open UI Community Group just discussed `[dialog] Declarative indicator for dialog modality`.

<details><summary>The full IRC log of that discussion</summary>
&lt;jarhar> Luke: this is twofold. its related to invokers, but also a separate thing that it poentially important<br>
&lt;masonf> q+<br>
&lt;jarhar> Luke: invokers solves problems about dialog showmodal without js<br>
&lt;jarhar> Luke: doesnt solve problem that its opened by default on page load<br>
&lt;jarhar> Luke: generally not something you need to do, but using frameworks is something that they might want to do<br>
&lt;jarhar> Luke: for example, my edit root is a modal dialog that shows on top and thats rooted with a separate url<br>
&lt;jarhar> Luke: if i want to do that with ssr, you still need to run js on the frontend to showmodal<br>
&lt;brecht_dr> q+<br>
&lt;jarhar> Luke: discussed in whatwg issue, i think that the twofold benefit of this is that we have the modal attribute and the dialog is modal when opened<br>
&lt;jarhar> Luke: could simplify showmodal and togglemodal<br>
&lt;jarhar> Luke: if we made the modality is decided by the dialog rather than the action that could be simple<br>
&lt;jarhar> Luke: what are peoples thoughts?<br>
&lt;jarhar> Luke: modal attribute wouldnt open the dialog - separate open attribute, modal would just indicate that this will become a modal dialog<br>
&lt;jarhar> masonf: there was a feature for this for popover called defaultopen<br>
&lt;jarhar> masonf: there are some tricky bits. naming, there was a lot of conversation about naming. attribute name needs to communicate that it is currently modal<br>
&lt;jarhar> masonf: even having the open attribute on the dialog is a mistake<br>
&lt;jarhar> q+<br>
&lt;jarhar> masonf: put it back in the page, then its modal but not open anymore, and attributes cant be added while inserting or removing from the page<br>
&lt;jarhar> masonf: tricky with state changes<br>
&lt;jarhar> masonf: i think you want an attribute that means on first load only<br>
&lt;jarhar> masonf: other thing is that timing is a bit tricky. you have to precisely define the timing. if you have two things that are initially modal you have to decide what order they go in, what if theres a script in the middle, etc.<br>
&lt;masonf> ack mason<br>
&lt;masonf> ack brecht<br>
&lt;jarhar> brecht_dr: i do come in a lot of situations where i need this<br>
&lt;jarhar> brecht_dr: for example, a user agreement has changed for a certain platform when a user logs in<br>
&lt;jarhar> brecht_dr: you want to show a modal right away that says this information<br>
&lt;jarhar> brecht_dr: another one is ?? on platforms, not sure if this is a modal, but is a use case<br>
&lt;jarhar> brecht_dr: not sure if cookie banner should be a modal<br>
&lt;jarhar> brecht_dr: something thats also open by default when the page loads<br>
&lt;Luke> q+<br>
&lt;jarhar> brecht_dr: theres certainly a big use case for these kind of things<br>
&lt;jarhar> brecht_dr: already had the same thing for popovers as well<br>
&lt;jarhar> brecht_dr: should be something thats out there<br>
&lt;jarhar> brecht_dr: i do understand theres a lot of trickiness involved<br>
&lt;jarhar> brecht_dr: especially when loading by default, the last thing in your dom that has open will be open in the end<br>
&lt;jarhar> brecht_dr: in favor of having something like this<br>
&lt;masonf> jarhar: If we get rid of non-modal dialogs, we don't need a modal attribute any more?<br>
&lt;scotto_> q+<br>
&lt;masonf> jarhar: if not, then I agree with defaultopen - open when the page loads. No tracking of current state.<br>
&lt;masonf> jarhar: Original spec PR had defaultopen and we removed it - might be hard to spec.<br>
&lt;jarhar> masonf: defaultopen - there were a lot of open questions so we removed it<br>
&lt;jarhar> Luke: two points of clarification: i would foresee this as not being removed, so the attribute would always exist on the dialog<br>
&lt;jarhar> Luke: modal would just indicate that this is a modal dialog<br>
&lt;keithamus> q+<br>
&lt;jarhar> Luke: if you want something thats modal, its not also going to want to be non-modal at some time<br>
&lt;jarhar> Luke: regarding making open just mean open by default isnt plausible<br>
&lt;jarhar> Luke: for backwards compat reasons<br>
&lt;jarhar> Luke: deprecating the show function doesnt...<br>
&lt;jarhar> Luke: depends on meaning of deprecation<br>
&lt;jarhar> Luke: if its a thing thats deprecated then you should use popover but you cant get rid of it<br>
&lt;jarhar> Luke: but yeah potentially it is a thing - if we get rid of non-modal dialogs then open without popover means modal, and open with popover means non-modal<br>
&lt;jarhar> Luke: also solves invoker thing because youd use modal actions or popover actions<br>
&lt;masonf> ack jarhar<br>
&lt;masonf> ack luke<br>
&lt;jarhar> Luke: to me the modal attribute would stick around<br>
&lt;masonf> ack scott<br>
&lt;jarhar> scotto_: touching on what we were talking about with modals and show, people still need non-modal dialogs and they shjouldnt go away - its just how are they shown - popovers?<br>
&lt;jarhar> scotto_: .show() thats going away, not non-modal<br>
&lt;jarhar> scotto_: dialogs cant be modal by default, you dont have to put a close button in them. someone could show one and now you cant use anything because you dont have access to an escape key if youre on mobile<br>
&lt;jarhar> scotto_: if dialog is modal by default, youre going to need some kind of invoking action - the thing is just there and the entire page is inert<br>
&lt;jarhar> scotto_: this needs to be something that someone explicitly does, you cant just make a dialog modal by default<br>
&lt;jarhar> scotto_: this would break those pages, there doesnt have to be a close button so how are people going to close these<br>
&lt;jarhar> Luke: mobile platforms do have back gestures<br>
&lt;brecht_dr> q+<br>
&lt;jarhar> scotto_: i dont like the back gesture thing<br>
&lt;jarhar> masonf: its shipping<br>
&lt;jarhar> scotto_: i hate it<br>
&lt;masonf> ack keith<br>
&lt;jarhar> keithamus: i want to say -1 to this based on everything. theres a lot of complexity in the spec and what mason mentioned around timing, and user complexity<br>
&lt;jarhar> keithamus: luke in the comment you mentioned dialog modal open is invalid and an authoring error<br>
&lt;jarhar> keithamus: im sure that people have examples<br>
&lt;jarhar> keithamus: to scotts point, im concerned about user interaction<br>
&lt;jarhar> keithamus: if you want to present someone with a modal dialog on page load, thats a page. a modal dialog is like a mini page, and its alleviating page navigations<br>
&lt;jarhar> keithamus: if youre introducing that, then thats just the page<br>
&lt;masonf> keep in mind, you can currently do &lt;script>window.onload = () => mydialog.showModal()&lt;/script><br>
&lt;jarhar> keithamus: i struggle to see a compelling use case for that as well<br>
&lt;jarhar> keithamus: im interested if there are any<br>
&lt;brecht_dr> q-<br>
&lt;jarhar> Luke: to keiths point, yeah it is bad but a design question as whether you want something to be a modal or its own page<br>
&lt;jarhar> Luke: if you have a data table and are renaming something, its nice to have it be a small modal rather than another page<br>
&lt;jarhar> Luke: not necessarily a good design, but a design question<br>
&lt;jarhar> Luke: if we have the modal attribute, it would simplify the toggles<br>
&lt;jarhar> Luke: invoker would decide whether its modal<br>
&lt;jarhar> keithamus: im wondering if the justification is in reverse. better to focus energy on do we need .show() and then the two types of dialogs are showmodal with script or invoker, or you have a dialog popover, and those are the two<br>
&lt;jarhar> keithamus: right now there are three and its making this complicated<br>
&lt;jarhar> keithamus: stepping further into adding another thing to increase complexity might not solve the problem<br>
&lt;jarhar> masonf: on the invokers - complexity is around toggle and close. auto makes sense and is modal<br>
&lt;jarhar> masonf: so maybe a way around this is to pull out the toggle stuff from the invokers and punt this down the road<br>
&lt;jarhar> keithamus: i think we have that conversation coming up<br>
&lt;jarhar> Luke: im happy to leave this and move on to the next one<br>
&lt;jarhar> masonf: i didnt hear consensus, is there a resolution?<br>
&lt;jarhar> Luke: for now, if you have opinions then leave them on the issue<br>
&lt;jarhar> Luke: dont have a strong resolution to go with<br>
</details>

<!-- comment id=2099519305 author=github-actions[bot] created=2024-05-08T00:29:31Z url=https://github.com/openui/open-ui/issues/920#issuecomment-2099519305 -->
## @github-actions[bot] — 2024-05-08

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2455992105 author=github-actions[bot] created=2024-11-05T00:44:38Z url=https://github.com/openui/open-ui/issues/920#issuecomment-2455992105 -->
## @github-actions[bot] — 2024-11-05

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2528673200 author=lukewarlow created=2024-12-09T16:46:48Z url=https://github.com/openui/open-ui/issues/920#issuecomment-2528673200 -->
## @lukewarlow — 2024-12-09

> Could <dialog open="modal"> not work?

Coming back to this since my original reason against this no longer holds (invoker commands aren't ambiguous anymore). I think this could be a neat way to do this for dialogs. `<dialog open>` would still be parsed as an open non-modal dialog. But `<dialog open="modal">` would be parsed as an open modal dialog.

We could maybe even make `dialog.showModal()` set it as `<dialog open="modal">` as far as I know that wouldn't break any code, especially if we make `dialog.open` always return a boolean value? `dialog[open]` would still match in CSS correctly for example.

We obviously still need the discussion on whether this is a good thing to have or not, but if we decide it is that seems like a fine API? 

<!-- comment id=2953330878 author=github-actions[bot] created=2025-06-08T01:00:50Z url=https://github.com/openui/open-ui/issues/920#issuecomment-2953330878 -->
## @github-actions[bot] — 2025-06-08

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2953854202 author=ryantownsend created=2025-06-08T10:00:40Z url=https://github.com/openui/open-ui/issues/920#issuecomment-2953854202 -->
## @ryantownsend — 2025-06-08

@lukewarlow I could very much use this value existing.

I’m currently achieving it via just setting `open` in the HTML and then having JavaScript immediately call `close()` and `showModal()` during page load – it’s not ideal because you can see a flicker.

If it helps, my primary use-cases are:

1. A welcome modal triggered when a user first enters a section of an admin panel they’ve never visited previously, providing a dismissible explanation of the section.
2. More in-depth confirmation messages where a small toast-style notification won’t cut it, e.g. generating a new API key needs to present the one-time-visible key, provide context that other keys have been set to expire and link to documentation.

<!-- comment id=3621443148 author=github-actions[bot] created=2025-12-07T01:02:06Z url=https://github.com/openui/open-ui/issues/920#issuecomment-3621443148 -->
## @github-actions[bot] — 2025-12-07

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=3621724373 author=mayank99 created=2025-12-07T06:47:34Z url=https://github.com/openui/open-ui/issues/920#issuecomment-3621724373 -->
## @mayank99 — 2025-12-07

This might sound like theoretical purity but… I think a separate `modal` attribute makes more sense than overloading the `open` attribute. Modality is a _property_ of the dialog, while openness is a _state_.

I don't expect the same dialog to be opened as non-modal _and_ modal during its lifecycle. I've always found it strange that it's not possible to determine at a glance whether a `<dialog>` will be modal when opened.

Another thing to consider: making the `open` attribute diverge from the `open` DOM property will cause a potential problem for JSX frameworks that work primarily with properties, i.e. `<dialog open="modal">` would typically translate to `dialog.open = "modal"` rather than `dialog.setAttribute("open", "modal")`. I'm sure frameworks can adapt, but it might take them a while if they deem this to be a breaking change for whatever reason.

<!-- comment id=3795190936 author=oliverjam created=2026-01-24T17:30:40Z url=https://github.com/openui/open-ui/issues/920#issuecomment-3795190936 -->
## @oliverjam — 2026-01-24

This is actually quite annoying when building server-driven apps. Modern HTML/CSS is _so_ close to being able to create really nice interactive experiences without any client-side JS.

But I just hit this modal issue with form validation. If you submit a form from a dialog it's important to be able to redirect back with visible validation errors from the server. As far as I can tell there's no way to do this. I'd need to intercept the submission with client-side JS and render messages inline to avoid losing the open state on the page by redirecting.

<!-- comment id=3796881492 author=lukewarlow created=2026-01-25T16:12:20Z url=https://github.com/openui/open-ui/issues/920#issuecomment-3796881492 -->
## @lukewarlow — 2026-01-25

> I'd need to intercept the submission with client-side JS and render messages inline to avoid losing the open state on the page by redirecting.

Alternatively you'd have to have some mechanism to launch the dialog on page reload after a failed submission to the server.

<!-- comment id=3796907727 author=ryantownsend created=2026-01-25T16:28:55Z url=https://github.com/openui/open-ui/issues/920#issuecomment-3796907727 -->
## @ryantownsend — 2026-01-25

@lukewarlow I think @oliverjam's point is that if you have a form in a modal dialog, you can't have that hard-submit and SSR a response with the validation errors in the same `<dialog>` element and have it **declaratively** open on the next page load (hence needing/supporting the change described in this issue).

I have this same problem in one of my products. For now, I've 'hacked' around this with JavaScript as described above: my page will render the `<dialog>` with an `open` attribute, then JavaScript will `close()` and `showModal()` the element on page load to promote it to a modal dialog.

@oliverjam as an aside, you might want to follow @noamr's [Declarative Partial Updates proposal](https://github.com/WICG/declarative-partial-updates), I hope we'll get some form handling with that spec (I've written up this use-case in [this comment](https://github.com/WICG/declarative-partial-updates/issues/32#issuecomment-3696522405)). I wouldn't expect to see DPU support before this issue is resolved, but I think it suits your use-case nevertheless.
