---
number: 143
title: "<select> anatomy seems Windows-centric"
state: "closed"
labels: ["agenda+", "select"]
author: "dandclark"
created_at: "2020-07-23T23:45:41Z"
closed_at: "2023-02-23T19:45:50Z"
url: "https://github.com/openui/open-ui/issues/143"
comments: 8
has_resolution: true
synced_at: "2026-08-27T14:25:56Z"
---

In [MicrosoftEdge/MSEdgeExplainers#361](https://github.com/MicrosoftEdge/MSEdgeExplainers/issues/361) @othermaciej raised the following [issue](https://github.com/MicrosoftEdge/MSEdgeExplainers/issues/361):

> The proposed anatomy for a includes a part named listbox. <select> elements in macOS Safari (and in the closest matching native macOS controls) don't have a listbox, except in the <select multiple> state; they have a menu. On iPadOS, there's a popover instead. On iOS, it's a list wheel. On iOS and iPadOS, this is true even for the <select multiple> state; they use a checkable list wheel and a checkable popover respectively. I am not sure if the anatomy is meant to encompass the <select multiple> case. Can we name this part of the anatomy something that is suitable for all the different ways this control is presented on different platforms?

The open-ui [anatomy](https://open-ui.org/components/select) is intended to encompass the `<select multiple>` case, but I don't think it's been fully fleshed-out yet; I just opened #142 .

Perhaps we can come up with a term that better encompasses all use cases while still being somewhat descriptive.  Something like "popup" could describe a lot of these cases, but doesn't fit with `<select multiple>` on Windows.

Thoughts?

<!-- comment id=664690025 author=dandclark created=2020-07-27T23:29:42Z url=https://github.com/openui/open-ui/issues/143#issuecomment-664690025 -->
## @dandclark — 2020-07-27

Thinking about this more, one observation is that the goal isn't necessarily to encompass all the ways that a `<select>` controls are currently presented on different platforms.  There will be some OS-native controls that won't fit the anatomy defined in OpenUI, but web developers creating a custom control based on the OpenUI anatomy probably weren't going to be using the native controls anyway.  Control usage by developers could be divided into two distinct categories:

A) The developer wants to specify the appearance of their control, e.g. to match the styles on the site or match the guidelines of a particular design system.  The developer wants it to look the same across different browsers and OS platforms.  Currently a developer in this category would use a component framework, or just roll their own control.  They aren't using the native controls because those might have drastically different appearances and structures, e.g. the big differences of `<select multiple>` on Windows vs iOS.
B) The developer doesn't need to specify the appearance of the control.  They are fine with it having potentially significant differences in appearance and behavior on different platforms, as long as it still fulfills its basic function.  The cross-platform differences could even be considered an advantage for this developer because they know that the control will have an experience tailored for each platform without any work on their part.

Proposals based on OpenUI like [Enabling Custom Control UI](https://github.com/MicrosoftEdge/MSEdgeExplainers/blob/main/ControlUICustomization/explainer.md) target only the first category, where the goal is to make life easier for developers who want to specify how their controls look.  Developers in the second category will continue using the default non-customizable `<select>`.

Given that the target audience of [Enabling Custom Control UI](https://github.com/MicrosoftEdge/MSEdgeExplainers/blob/main/ControlUICustomization/explainer.md) and, I believe, OpenUI as a whole, is developers who are using controls from component frameworks or are writing their own, rather than developers who are using native controls, it need not be a requirement that the OpenUI anatomy encompass all OS-native control implementations.

That said, the `<select>` anatomy shouldn't be overly biased towards any particular current `<select>` implementation; it should instead be a distillation of widely agreed-upon ideas of how a `<select>` should work.   This considers not just OS implementations but browser implementations and those of popular component frameworks like Bootstrap, Ant Design, etc etc.  If folks feel that we're steering the anatomy too close to any given implementation at the expense of others then we should be prepared to correct for this.

<!-- comment id=665531858 author=othermaciej created=2020-07-29T08:53:33Z url=https://github.com/openui/open-ui/issues/143#issuecomment-665531858 -->
## @othermaciej — 2020-07-29

Perhaps `<select>` and `<select multiple>` (and also `<select size=n>` where n > 1, since that also generally has a listbox style presentation) should have distinct anatomy? There doesn't seem to be that much call to style them the same, and the `button` and `selected-value` portions of the anatomy are inapplicable to `<select multiple>`. Even worse, `<select multiple>` does have one or more select values, but they are just different stylings of items in the list, not a distinct slot.

Alternately, or in addition, perhaps the `listbox` portion of the anatomy could just be called `list`, as that is relative neutral to what form of presentation is used, whether a listbox, a popup menu, a popover, a wheel, or something else entirely.

<!-- comment id=696462980 author=gregwhitworth created=2020-09-22T01:19:16Z url=https://github.com/openui/open-ui/issues/143#issuecomment-696462980 -->
## @gregwhitworth — 2020-09-22

> Perhaps `<select>` and `<select multiple>` (and also `<select size=n>` where n > 1, since that also generally has a listbox style presentation) should have distinct anatomy? There doesn't seem to be that much call to style them the same, and the button and selected-value portions of the anatomy are inapplicable to `<select multiple>`. Even worse, `<select multiple>` does have one or more select values, but they are just different stylings of items in the list, not a distinct slot.

I agree with this completely. I have it on my list to investigate multiple and can agree that the anatomy should be distinct. I have shown that this model can achieve a multiple looks including multiple using Safari's OSX design you can play with here: https://epic-cray-0896e3.netlify.app/src/select/demos/safari-desktop/

**PLEASE NOTE:** That demo is merely testing the general model for styling and behavior/state testing of the anatomy. There are NUMEROUS gaps between a truly complete `select`. Hopefully over time we can get this WC to be a solid complete `<select>` that can be used in production.

> Alternately, or in addition, perhaps the listbox portion of the anatomy could just be called list, as that is relative neutral to what form of presentation is used, whether a listbox, a popup menu, a popover, a wheel, or something else entirely.

Possible. This discussion is happening in the [breadcrumb proposal](https://github.com/WICG/open-ui/pull/165#pullrequestreview-485379830) as the part of breadcrumbs is actually a wrapper on a nav. Would be cool to generalize this in some meaningful way.

<!-- comment id=1073136929 author=github-actions[bot] created=2022-03-20T00:33:37Z url=https://github.com/openui/open-ui/issues/143#issuecomment-1073136929 -->
## @github-actions[bot] — 2022-03-20

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=1438974586 author=josepharhar created=2023-02-21T19:16:49Z url=https://github.com/openui/open-ui/issues/143#issuecomment-1438974586 -->
## @josepharhar — 2023-02-21

> Alternately, or in addition, perhaps the listbox portion of the anatomy could just be called list, as that is relative neutral to what form of presentation is used, whether a listbox, a popup menu, a popover, a wheel, or something else entirely.

Shall we rename `listbox` to `list` as suggested?
Adding agenda+

<!-- comment id=1442215580 author=josepharhar created=2023-02-23T18:08:37Z url=https://github.com/openui/open-ui/issues/143#issuecomment-1442215580 -->
## @josepharhar — 2023-02-23

> whether a listbox, a popup menu, a popover, a wheel, or something else entirely.

If `<selectmenu multiple>` doesn't have the same named parts then I think that keeping it as `listbox` is fine because it will always be a popover.

<!-- comment id=1442324035 author=css-meeting-bot created=2023-02-23T19:36:52Z url=https://github.com/openui/open-ui/issues/143#issuecomment-1442324035 resolution=true -->
## @css-meeting-bot — 2023-02-23

The Open UI Community Group just discussed `[selectmenu] <select> anatomy seems Windows-centric`, and agreed to the following:

* `RESOLVED: Keep the part name "listbox" instead of changing it`

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [selectmenu] &lt;select> anatomy seems Windows-centric<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/143<br>
&lt;masonf> Side note: resolutions must start with "RESOLVED" all caps or they don't minute correctly.<br>
&lt;gregwhitworth> joey: it was pointed out that the listbox named part in iOS and mac they aren't used and there was a back-and-forth discussion across platforms<br>
&lt;gregwhitworth> joey: maybe it should be a list rather than listbox<br>
&lt;gregwhitworth> joey: in my opinion we should just keep it called listbox<br>
&lt;gregwhitworth> q+<br>
&lt;masonf> ack masonf<br>
&lt;sarah_h> q+<br>
&lt;jhey> +1 Everything is a box regardless of whether it's in the top layer or not<br>
&lt;una> gregwhitworth: argument that it being a popover is irrelevant bc it's still a listbox, just happens to be inset instead of popped up<br>
&lt;una> gregwhitworth: we did research the part names and listbox is a very common component when folks are building their own<br>
&lt;una> gregwhitworth: commonly used in the wild and has nothing to do with it being a popup<br>
&lt;masonf> q+<br>
&lt;una> gregwhitworth: why people added box isn't clear, but it's used as a list of options<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> ack sarah_h<br>
&lt;gregwhitworth> sarah_h: I was in favor of changing in that listbox is the aria role name that is used for that part of the component but as the OP defined it isn't the case for Mac, iOS or Android<br>
&lt;gregwhitworth> sarah_h: we may want to change the role for select multiple, there is an argument to change the role to menu. So having the name tied so closely to the aria-role to changing it<br>
&lt;gregwhitworth> sarah_h: it just may be a little confusing<br>
&lt;scotto_> can we just call it "box"?<br>
&lt;gregwhitworth> q?<br>
&lt;scotto_> or boxy-mcboxface?<br>
&lt;gregwhitworth> ACK masonf<br>
&lt;gregwhitworth> masonf: I want to clarify, the correct aria-role is listbox but only in some cases?<br>
&lt;gregwhitworth> sarah_h: yeah, on Windows the correct role is listbox but not on other platforms<br>
&lt;jhey> q+<br>
&lt;gregwhitworth> sarah_h: the only changes would be the role and some states on the options<br>
&lt;gregwhitworth> sarah_h: there may be some situations where different roles may be a good idea<br>
&lt;gregwhitworth> masonf: you think the most likely role would be menu?<br>
&lt;scotto_> q+<br>
&lt;gregwhitworth> masonf: I would be in favor of making the roles the same rather than different<br>
&lt;JonathanNeal> I would be in favor of a rename, especially if it describes what it targets more realistically.<br>
&lt;gregwhitworth> ack scotto_<br>
&lt;gregwhitworth> scotto_: I would have been far more in favor of listbox before, but if they can have all the arbitrary content thrown into it I'm not always in favor of that<br>
&lt;gregwhitworth> scotto_: boxymcboxface the slot name and whatever role it needs to be may vary<br>
&lt;gregwhitworth> masonf: can I clarify one thing<br>
&lt;JonathanNeal> q+<br>
&lt;gregwhitworth> masonf: we have strongly discouraged all the other things so that you're doing a selectmenu and thus it IS a listbox<br>
&lt;gregwhitworth> sarah_h: is the role and platform dependant or multiple dependant. Even if we do a seperate element it is menu on others and listbox on Windows<br>
&lt;masonf> let's call it "menulistbox"<br>
&lt;gregwhitworth> scotto_: similar to that, I have the issue on making a checkbox group<br>
&lt;gregwhitworth> scotto_: commonly people use checkbox as it is selected and they have much better support than options<br>
&lt;gregwhitworth> scotto_: it's very odd but if the same underlying code was used for multiselect would it still be a listbox part?<br>
&lt;gregwhitworth> ack jhey<br>
&lt;gregwhitworth> jhey: I was going to say, I think if you think about it from a devs perspective. They're going to think of it as a list of options<br>
&lt;masonf> q?<br>
&lt;scotto_> agreed with not calling it just "list"<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> jhey: and they'll need more clarification than just list which they may put unordered or ordered list within<br>
&lt;gregwhitworth> ack dbaron<br>
&lt;gregwhitworth> dbaron: I think most of what I was going to say was covered by sarah_h and scotto_<br>
&lt;sarah_h> My preference also isn't super strong, if people who are not constantly stewing their brains in the ARIA spec think that "listbox" is generally understandable enough as a concept separate from ARIA, keeping it also seems fine to me<br>
&lt;gregwhitworth> dbaron: when I was talking to Aaron L. on CSS toggles, multiple selects are awful and they should be like checkbox groups instead<br>
&lt;gregwhitworth> dbaron: I heard folks want to see the roles evolve for multiple selects<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> JonathanNeal: what I wanted to say was that I had looked at the definition in the list<br>
&lt;gregwhitworth> JonathanNeal: if the options will not be a listbox then should options be an appropriate name for this area<br>
&lt;gregwhitworth> JonathanNeal: just because it's in the name, if we're describing it by its content and not by its role<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> masonf: both the comments from JonathanNeal and jhey<br>
&lt;dbaron> I have mixed feelings about two element names that differ only by plural vs. singular...<br>
&lt;gregwhitworth> masonf: we're going to handle the roles<br>
&lt;sarah_h> q+<br>
&lt;gregwhitworth> masonf: what will developers expect then we should call it a listbox<br>
&lt;scotto_> so divbox?<br>
&lt;gregwhitworth> masonf: I'm happy to go with whatever way that people are most familiar with<br>
&lt;masonf> "box"<br>
&lt;JonathanNeal> I would agree that `listbox` would work, if it were intuitive. I haven’t thought of ‘listbox’ as a kind of descriptor of content, tho.<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;una> gregwhitworth: agree with the path forward for multiselect. everywhere you look it's not what multiselect does today<br>
&lt;tantek> +1 masonf to hopefully not needing explicit ARIA roles<br>
&lt;una> gregwhitworth: the way JonathanNeal described it, it's how we started sketching anatomies (what is the *purpose* of this section)<br>
&lt;una> gregwhitworth: list is actually higher than listbox in the research<br>
&lt;una> gregwhitworth: we didnt have formal groupings setup at the time, list might have been too generic<br>
&lt;una> gregwhitworth: supportive of masonf just said and what scotto_  and sarah_h just raised - we may end up with a completely different element than an attribute on selectmenu<br>
&lt;una> gregwhitworth: may need a control group<br>
&lt;una> gregwhitworth: i.e. checkboxgroup that is a component that becomes a child of selectmultiple<br>
&lt;una> gregwhitworth: in favor of keeping it as it, a lot of support for not just tacking in attr on, anatomy will have to change bw the two<br>
&lt;gregwhitworth> q?<br>
&lt;una> gregwhitworth: tldr; agree with mason<br>
&lt;gregwhitworth> ack sarah_h<br>
&lt;gregwhitworth> sarah_h: I wanted to clarify that I'm not opposed to keeping it, but if it's understandable enough to regular devs then I'm fine with it<br>
&lt;gregwhitworth> sarah_h: we have a button slot and it's not the role so we're not aligning with that<br>
&lt;jarhar> Proposed resolution: Keep the part name "listbox" instead of changing it<br>
&lt;JonathanNeal> I did not think listbox was the name of this thing. But I haven’t used ‘box’ to describe any other part or slot thingies.<br>
&lt;masonf> +1<br>
&lt;gregwhitworth> RESOLVED: Keep the part name "listbox" instead of changing it<br>
&lt;JonathanNeal> Aww, thanks gregwhitworth for being awesome.<br>
&lt;tantek> +1 gregwhitworth — the positive culture of this group is quite rare and appreciated<br>
&lt;gregwhitworth> Zakim, end meeting<br>
</details>

<!-- comment id=1442334989 author=josepharhar created=2023-02-23T19:45:50Z url=https://github.com/openui/open-ui/issues/143#issuecomment-1442334989 -->
## @josepharhar — 2023-02-23

Closing per resolution
