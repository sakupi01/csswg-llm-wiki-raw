---
number: 561
title: "[popup] Standardize the basic CSS for an element with the `popup` attribute"
state: "closed"
labels: ["popover"]
author: "mfreed7"
created_at: "2022-07-06T18:59:35Z"
closed_at: "2022-10-05T21:59:20Z"
url: "https://github.com/openui/open-ui/issues/561"
comments: 44
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

Akin to the [`<dialog>` element's standardized styling](https://html.spec.whatwg.org/multipage/rendering.html#flow-content-3), I think it'd be helpful to authors to get a very basic set of standardized styles that the UA applies to pop-ups by default. Otherwise, `<div popup>Hello!</div>` is just the word "Hello", and isn't set apart from the page at all, which can be a bit confusing.

The default styles can be almost identical, I think, to the ones for dialog, perhaps only changing `position:absolute` to `position:fixed`:

```css
[popup="" i],
[popup=auto i],
[popup=hint i],
[popup=manual i] {
  position: fixed;
  inset-inline-start: 0;
  inset-inline-end: 0;
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  padding: 1em;
  background-color: Canvas;
  color: CanvasText;
}
```

It would seem to make sense for this UA rule to come before any rules that match specific elements, so that element-specific CSS overrides these.

Generally, thoughts? Objections?

<!-- comment id=1176578859 author=mfreed7 created=2022-07-06T19:11:38Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1176578859 -->
## @mfreed7 — 2022-07-06

Similarly, the fullscreen spec contains [this set of standard styles that apply to the ::backdrop pseudo element](https://fullscreen.spec.whatwg.org/#user-agent-level-style-sheet-defaults):

```css
::backdrop {
  position:fixed;
  top:0; right:0; bottom:0; left:0;
}

*|*:not(:root):fullscreen::backdrop {
  background:black;
} 
```

As written, and apparently as implemented in at least Chromium, the above CSS is used for both fullscreen elements **and** modal `<dialog>` elements.

For a pop-up element, I think the most common usage pattern would be to **not** style the `::backdrop`, because the pop-up isn't modal and doesn't want to obscure the page. But it seems helpful to at least include the sizing rules, and perhaps manage pointer events more carefully? How about this?

```css
[popup="" i]::backdrop,
[popup=auto i]::backdrop,
[popup=hint i]::backdrop,
[popup=manual i]::backdrop {
  position: fixed;
  top:0; right:0; bottom:0; left:0;
  background: transparent;
  pointer-events: none !important; /* So that clicks on the page can light-dismiss the pop-up */
}
```

<!-- comment id=1176667891 author=scottaohara created=2022-07-06T20:14:43Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1176667891 -->
## @scottaohara — 2022-07-06

I think the thing I'm most unsure of is the initial positioning. 
ideally these popups would be positioned based on their anchoring element, where applicable.  They wouldn't necessarily be fixed all the time?

Other question, so long as the attribute can be used on any element, i would expect some of these UA styles to be rather weak - e.g., the `hidden` attribute's `display: none`, to ensure that the base UA styles of a specific element (let's say `blockquote`) or other author styles take priority.  The latter not so much being a question as an expectation, but just trying to cover all the basis.

<!-- comment id=1185047527 author=mfreed7 created=2022-07-15T00:22:33Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1185047527 -->
## @mfreed7 — 2022-07-15

> I think the thing I'm most unsure of is the initial positioning. ideally these popups would be positioned based on their anchoring element, where applicable. They wouldn't necessarily be fixed all the time?

Hmm, interesting point. I think the problem is that technically you don't need to use the Anchor Positioning API to position a pop-up - you can put it top-left, or centered on the screen or whatever. I'm not sure what default style we'd apply that would "typically work" in the case that the pop-up has an `anchor` attribute. Perhaps a rule like:

```css
[popup][anchor] {
  top: anchor(bottom);
  left: anchor(left);
}
```

That feels odd to add to the standardized CSS though.


> 
> Other question, so long as the attribute can be used on any element, i would expect some of these UA styles to be rather weak - e.g., the `hidden` attribute's `display: none`, to ensure that the base UA styles of a specific element (let's say `blockquote`) or other author styles take priority. The latter not so much being a question as an expectation, but just trying to cover all the basis.

Totally agree - I'm not exactly sure how to specify that though. But that's what I meant by this sentence:
> It would seem to make sense for this UA rule to come before any rules that match specific elements, so that element-specific CSS overrides these.

<!-- comment id=1185462651 author=scottaohara created=2022-07-15T11:41:29Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1185462651 -->
## @scottaohara — 2022-07-15

yeh, thinking about it more, maybe if those anchoring styles were put in place (and if an rtl style where it was `right: anchor(right)` instead of `left`), then it would be on the author to do the rest.  that seems like the best reasonable default.

and sorry, don't know how that last sentence you had in there about UA style positions didn't register for me.  

<!-- comment id=1201766064 author=mfreed7 created=2022-08-01T21:59:48Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1201766064 -->
## @mfreed7 — 2022-08-01

I think we likely also need to pull in a few more lines from the The [rendering section of the spec for `<dialog>`](https://html.spec.whatwg.org/multipage/rendering.html#rendering) under the "when its is modal flag is true" section, particularly these:

```css
  inset-block-start: 0;
  inset-block-end: 0;
  overflow: auto;
```

The `<dialog>` rendering section also includes these two rules, which seem like they must have some history. Not sure whether they should be ported to `<div popup>` or not:

```css
  max-width: calc(100% - 6px - 2em);
  max-height: calc(100% - 6px - 2em);
```

I'm sure @domenic knows where these come from and whether they are required here.

<!-- comment id=1213315341 author=tbondwilkinson created=2022-08-12T16:44:25Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1213315341 -->
## @tbondwilkinson — 2022-08-12

For simplicity, I would remove border and padding from the default stylings and set background color to transparent. I can imagine most people overriding this.

I agree that you'd only want anchor positioning when you have an anchor element, but I'm not sure how you would style that from the UA.

<!-- comment id=1213508572 author=mfreed7 created=2022-08-12T20:47:00Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1213508572 -->
## @mfreed7 — 2022-08-12

> For simplicity, I would remove border and padding from the default stylings and set background color to transparent. I can imagine most people overriding this.

I just copied those nearly verbatim from [`<dialog>`'s styling](https://html.spec.whatwg.org/multipage/rendering.html#flow-content-3). The rationale, I imagine, was to set the `<dialog>` off from the page at least a little, by default. If you remove the border and make the background transparent, when you first start playing with `<div popup>Hello!</div>`, it can be confusing, since you can barely find the word "Hello!" overlaying other page content somewhere. Adding the padding/border/background makes it very easy. And as you point out, it's very easy to override.

@domenic do you know where the conversation took place about this styling for `<dialog>`? It'd be interesting to see how we arrived here.

<!-- comment id=1213524208 author=scottaohara created=2022-08-12T21:13:08Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1213524208 -->
## @scottaohara — 2022-08-12

While likely to be overwritten, being that this content should work in situations where author styles could be removed or revised (e.g., browser reader mode), I don't understand how it would be ok for the UA default style for an element whose purpose is to overlay other content of the page could have a transparent background.

<!-- comment id=1214535929 author=domenic created=2022-08-15T02:09:10Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1214535929 -->
## @domenic — 2022-08-15

> @domenic do you know where the conversation took place about this styling for `<dialog>`? It'd be interesting to see how we arrived here.

I can't find anything. In https://github.com/whatwg/html/commit/2fb24fcf3f916236e8767e2cb72b23e5c75b77e9 they arrived in the spec with full default styling. Various Google searches with `site:w3.org bugzilla` or `site:lists.whatwg.org` don't give any leads.

I personally like the idea of following the weak precedent provided by `<dialog>`. At least the background and border. I could be swayed to omit the padding.

<!-- comment id=1214541005 author=domenic created=2022-08-15T02:17:32Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1214541005 -->
## @domenic — 2022-08-15

Regarding `::backdrop` styling, I'm not sure many additional styles are necessary? You already get `position`, `top`, `right`, `bottom`, and `left` from the fullscreen spec's general `::backdrop`. `background` should default to `transparent`. So what's left is just adding `pointer-events: none !important;` for the popup cases.

<!-- comment id=1216971826 author=mfreed7 created=2022-08-16T18:01:05Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1216971826 -->
## @mfreed7 — 2022-08-16

@domenic thanks for digging, and too bad there's not much context. Also thanks for pointing out that the fullscreen spec already mandates most of the `::backdrop` styling. Chromium's implementation is individual per API, which is why I didn't even check.

If I keep the background/border because I heard several supportive comments, leave out the padding, and remove the overlap on `::backdrop` styling, here's what's left:

```css
[popup="" i],
[popup=auto i],
[popup=hint i],
[popup=manual i] {
  position: fixed;
  inset-inline-start: 0;
  inset-inline-end: 0;
  inset-block-start: 0;
  inset-block-end: 0;
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  overflow: auto;
  color: CanvasText;
  background-color: Canvas;
}

[popup="" i]::backdrop,
[popup=auto i]::backdrop,
[popup=hint i]::backdrop,
[popup=manual i]::backdrop {
    pointer-events: none !important;
}

[popup]:-internal-popup-hidden {
  display: none;
}
```

Objections?

<!-- comment id=1217325602 author=domenic created=2022-08-17T00:52:59Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1217325602 -->
## @domenic — 2022-08-17

> `[popup]:-internal-popup-hidden`

This seems bad?

<!-- comment id=1218596257 author=mfreed7 created=2022-08-17T23:03:57Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1218596257 -->
## @mfreed7 — 2022-08-17

> > `[popup]:-internal-popup-hidden`
> 
> This seems bad?

Sorry, in what sense? The behavior is bad? Or using an internal pseudo notation isn't the right way to express this in specs? (If the former, help me understand why it's bad? If the latter, what's the right way to specify this?)

<!-- comment id=1218798495 author=domenic created=2022-08-18T00:48:40Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1218798495 -->
## @domenic — 2022-08-18

I don't know what that's expressing, but I sure know that it's not something specs do. What is the goal of that Chromium-specific syntax? Then maybe we can talk about how to express it in specs.

<!-- comment id=1219637377 author=mfreed7 created=2022-08-18T15:31:30Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1219637377 -->
## @mfreed7 — 2022-08-18

> I don't know what that's expressing, but I sure know that it's not something specs do. What is the goal of that Chromium-specific syntax? Then maybe we can talk about how to express it in specs.

Ahh ok. So a `<div popup>` is `display:none` before it is shown. This rule makes that happen, via an internal CSS pseudo state (`:-internal-popup-hidden`) that matches when the pop-up shouldn't be showing. In [this section of the explainer](https://open-ui.org/components/popup.research.explainer#animation-of-pop-ups), step <span>#</span>1 of `.showPopUp()` is this:

> 1. Move the pop-up to the top layer, and **remove the UA display:none style**.

The CSS rule in question is how that is implemented. Suggestions appreciated (either here or on the upcoming spec PR, whichever you prefer) for how to write that in specs.

<!-- comment id=1219836523 author=css-meeting-bot created=2022-08-18T18:59:05Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1219836523 -->
## @css-meeting-bot — 2022-08-18

The Open UI Community Group just discussed `[popup] Standardize the basic CSS for an element with the popup attribute`, and agreed to the following:

* ``RESOLVED: The standard UA CSS for the pop-up API should be as written in https://github.com/openui/open-ui/issues/561#issuecomment-1216971826, but with simple [popup] selectors instead of [popup=value], etc. We still need to resolve the `display:none` selector.``

<details><summary>The full IRC log of that discussion</summary>
&lt;masonf> Topic: [popup] Standardize the basic CSS for an element with the popup attribute<br>
&lt;masonf> github: https://github.com/openui/open-ui/issues/561<br>
&lt;bkardell_> masonf: this is about standardizing styles for the popup - there has been some good discussion on the issue. Mostly a copy/paste of the styles for dialog..<br>
&lt;bkardell_> masonf: if you don't have those things (border, background) it is very confusing<br>
&lt;masonf> q?<br>
&lt;bkardell_> masonf: the counterpoint is that at some point it gets opinionated and there is a lot you have to set. We did some investigation on why we got those particular styles, but nothing much to say here we think they are deccent<br>
&lt;scotto_> q+<br>
&lt;masonf> ack: scotto<br>
&lt;masonf> ack scotto_<br>
&lt;bkardell_> scotto_: I will generally +1 all the things you said. Stripping them away would be confusing - there has to be a reasonable UA default. The only one I am somewhat on the fence is backdrop styling - is it default transparent?<br>
&lt;emilio> q+<br>
&lt;bkardell_> masonf: it comes as part of fullscreen and it does have default styling.. we wouldn't have to add much here - the only item left to provide is pointer-events: none<br>
&lt;bkardell_> scotto_: I think most wouldn't need a backdrop, but makes sense<br>
&lt;una> q?<br>
&lt;bkardell_> emilio: I see you mentioned the rules are ..?... is that necessary?  It seems better to have the styles be generic and applied to everything rather than every specific value? Does popup=true do something different? that would be unfortunate<br>
&lt;bkardell_> masonf: the answer to your last question is that that is an invalid value.<br>
&lt;bkardell_> masonf: any other value is not a valid popup and won't get any of the styling - that is why I put the explicit value in there<br>
&lt;bkardell_> masonf: I think it would be a mistake to make it look like it if it couldn't be it<br>
&lt;bkardell_> emilio: Is there a precedent for enumerated values where no value means a default like this<br>
&lt;bkardell_> emilio: you would need to write that 4 times, which is unfortunate<br>
&lt;bkardell_> masonf: there is nothing in there about the anchor attribute<br>
&lt;emilio> https://github.com/openui/open-ui/issues/561#issuecomment-1185047527<br>
&lt;bkardell_> emilio: maybe that comment is obsolete?<br>
&lt;bkardell_> masonf: ah... the standard styling is just fixed, centered on the screen. there was a question - there is anchor positioning that is a separate proposal but would be used here extensively. There was a question about whether we should rely on anchor positioning in the UA sheet by default<br>
&lt;bkardell_> masonf: that's the nature of the queastion here<br>
&lt;bkardell_> emilio: but my question is that it forces you to quadruplicate - at a minimum, more values in the future could make it more<br>
&lt;bkardell_> masonf: I'm not sure if there is a precent of exactly this where the missing value default is different - I do take your point about multiple selectors<br>
&lt;bkardell_> masonf: I think developers will do the simple thing<br>
&lt;bkardell_> q+<br>
&lt;masonf> ack emilio<br>
&lt;bkardell_> emilio: I'm looking at the enumerated attributes - most of them seem like if you specify an invalid attribute, there is still some reasonable default functionality<br>
&lt;bkardell_> emilio: if we have precedence -ok, but it feels a bit unfortunate.<br>
&lt;bkardell_> emilio: it's global and can apply to every element - maybe we could specify something simple... i don't know... in general I'm a bit sad about the complexity<br>
&lt;bkardell_> masonf: we should dig up the issue about why we decided this for context - the reason I recall was for forward compatibility.  If we don't do it this way we can't really add new ones easily<br>
&lt;bkardell_> emilio: I don't think that has been a real issue for input type="bazinga"<br>
&lt;bkardell_> masonf: there's also some feature detection stuff where you can check whether it is supported<br>
&lt;masonf> q?<br>
&lt;masonf> the issue is this one: https://github.com/openui/open-ui/issues/533<br>
&lt;emilio> FWIW: let input = document.createElement("input"); input.type = "bazinga"; input.type // "text"<br>
&lt;emilio> bkardell_: there's a disconnect between the two statements<br>
&lt;emilio> ... I think the UA stylesheet needs to do something useful<br>
&lt;emilio> ... I'm not sure I'm making a point other than this is worth thinking about<br>
&lt;masonf> q?<br>
&lt;emilio> masonf: I think you almost convinced me, r/n you do an invalid attribute value you get a warning, so not sure we should bend over backwards<br>
&lt;emilio> scotto_: so does this mean that we should change what happens when you have an invalid popup?<br>
&lt;emilio> scotto_: because right now popup=something shows up on the screen and isn't a popup<br>
&lt;masonf> q?<br>
&lt;emilio> masonf: right, with the simplified UA style that'd be a fixed-pos element, but you'd get a console warning<br>
&lt;emilio> q+<br>
&lt;emilio> scotto_: I don't have a strong opinion, so long as it's broken equally for everyone it seems fine<br>
&lt;bkardell_> ack BoCupp<br>
&lt;bkardell_> ack bkardell_<br>
&lt;bkardell_> scribenick: bkardell_<br>
&lt;masonf> ack emilio<br>
&lt;bkardell_> emilio: I just wanted to express a slight pref toward making an invalid value still a popup somehow - like type on input a bit... if it has been discussed and there is a counter-example I'd be interested in looking at it<br>
&lt;masonf> q?<br>
&lt;emilio> emilio: slight preference for making popup="invalid" still be a popup, seems similar to &lt;input type="invalid"><br>
&lt;bkardell_> masonf: I posted 533 above - give it a read it has that context, and add your comments - we can reconsider if there are new thoughts.<br>
&lt;masonf> q?<br>
&lt;masonf> Proposed resolution: The standard UA CSS for the pop-up API should be as written in https://github.com/openui/open-ui/issues/561#issuecomment-1216971826, but with simple [popup] selectors instead of [popup=value], etc.<br>
&lt;bkardell_> masonf:  the big diff between those imo is input is an element, but popup is an attribute<br>
&lt;una> SGTM<br>
&lt;miriam> +1<br>
&lt;bkardell_> emilio: {something}<br>
&lt;bkardell_> masonf: yes<br>
&lt;masonf> Proposed resolution: The standard UA CSS for the pop-up API should be as written in https://github.com/openui/open-ui/issues/561#issuecomment-1216971826, but with simple [popup] selectors instead of [popup=value], etc. We still need to resolve the `display:none` selector.<br>
&lt;emilio> s/{something}/we can't standardize the :-internal- pseudo but yeah :)<br>
&lt;bkardell_> +1<br>
&lt;scotto_> +1<br>
&lt;masonf> RESOLVED: The standard UA CSS for the pop-up API should be as written in https://github.com/openui/open-ui/issues/561#issuecomment-1216971826, but with simple [popup] selectors instead of [popup=value], etc. We still need to resolve the `display:none` selector.<br>
&lt;bkardell_> q+<br>
</details>

<!-- comment id=1220060058 author=mfreed7 created=2022-08-18T23:03:48Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1220060058 -->
## @mfreed7 — 2022-08-18

@emilio mentioned on https://github.com/openui/open-ui/issues/533 that the lack of `!important` on the `display:none` rule means that this CSS breaks things:

```css
[popup] {
  display: flex;
}
```

...because it overrides the UA rule that keeps non-showing pop-ups from being displayed.

The original purpose of **not** having `!important` on that rule was to make animations easier. But with https://github.com/openui/open-ui/issues/335 fixing those problems, I think we should probably add back `!important`.

<!-- comment id=1220919800 author=mfreed7 created=2022-08-19T17:30:22Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1220919800 -->
## @mfreed7 — 2022-08-19

Ok, with the resolution above plus the additional discussion, I think this is where we are:

```css
[popup] {
  position: fixed;
  inset-inline-start: 0;
  inset-inline-end: 0;
  inset-block-start: 0;
  inset-block-end: 0;
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  overflow: auto;
  color: CanvasText;
  background-color: Canvas;
}

[popup]::backdrop {
    pointer-events: none !important;
}

[popup]:{something that denotes the pop-up is in the hidden state} {
  display: none !important;
}
```

<!-- comment id=1222916773 author=mfreed7 created=2022-08-22T20:13:50Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1222916773 -->
## @mfreed7 — 2022-08-22

In my CL to implement the above changes, the reviewer called out the lack of `padding` explicitly. I don't disagree - having a border without padding looks odd to me:

<ins>with padding</ins>
<img width="129" alt="with_padding" src="https://user-images.githubusercontent.com/29559695/186000734-f0d425b7-da71-437d-8132-874fac850077.png">

<ins>no padding</ins>
<img width="125" alt="no_padding" src="https://user-images.githubusercontent.com/29559695/186000733-921ec35c-bbff-4696-9971-46c1ab029581.png">

Also, to point it out, `<dialog>` [has padding](https://html.spec.whatwg.org/multipage/rendering.html#flow-content-3):

<ins>dialog</ins>
<img width="101" alt="dialog" src="https://user-images.githubusercontent.com/29559695/186000730-f2f90555-a676-4b83-a9b9-1b47543ed4a9.png">

Are we sure we explicitly don't want to include padding in the default `popup` styles?

<!-- comment id=1222949460 author=dbaron created=2022-08-22T20:27:12Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1222949460 -->
## @dbaron — 2022-08-22

For what it's worth, "the reviewer" was me.

I think the norm is that if a default style has `border`, then it should probably have `padding` as well, since a border with `0` padding looks pretty bad.

Given that popups are often smaller than dialogs, I'm not sure that the default for padding needs to be `1em`, though.  I think `0.25em` or `2px` would be plausible defaults as well.  (Table cells default to `1px`, even.)

<!-- comment id=1222953272 author=emilio created=2022-08-22T20:28:40Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1222953272 -->
## @emilio — 2022-08-22

Yeah some padding makes sense to me. I don't have a strong preference for how much. Half or quarter of an em probably makes sense (and it's probably sensible to make it em-based to be consistent with dialog, but...)

<!-- comment id=1222954004 author=tbondwilkinson created=2022-08-22T20:28:59Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1222954004 -->
## @tbondwilkinson — 2022-08-22

I think I suggested leaving out the padding, but I meant it in the context of also leaving out the border because most people would probably override that style. People pointed out that we need the dialog to appear normal even if CSS doesn't load though, so I agree that if you have a border, some padding probably makes sense.

<!-- comment id=1223162139 author=mfreed7 created=2022-08-22T21:55:51Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1223162139 -->
## @mfreed7 — 2022-08-22

Thanks for the quick comments. Sounds like the start of consensus to make that change. While I'm here, I'll also point out this issue (https://github.com/whatwg/fullscreen/issues/201) because I think it makes sense to simplify the four `inset-*` properties into just `inset:0`. LMK if there are any disagreements with that.

Taken together, we're now here:

```css
[popup] {
  position: fixed;
  inset: 0;      /* Simplify to this */
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  padding: 0.25em;  /* Since I heard two people include 0.25em in their comments */
  overflow: auto;
  color: CanvasText;
  background-color: Canvas;
}

[popup]::backdrop {
    pointer-events: none !important;
}

[popup]:{something that denotes the pop-up is in the hidden state} {
  display: none !important;
}
```

Also for reference: 

<img width="170" alt="Screen Shot 2022-08-22 at 2 53 42 PM" src="https://user-images.githubusercontent.com/29559695/186025516-c73fc38c-9be2-450c-ac41-722a2665d254.png"> <img width="182" alt="Screen Shot 2022-08-22 at 2 54 05 PM" src="https://user-images.githubusercontent.com/29559695/186025513-fdcae294-5ef9-4549-a6fe-9daa82f9947f.png">

<!-- comment id=1223314969 author=scottaohara created=2022-08-22T23:20:17Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1223314969 -->
## @scottaohara — 2022-08-22

i personally like the .25em as a starting point.  but if people prefer the .5em, then that's absolutely fine as well.

<!-- comment id=1223848358 author=hidde created=2022-08-23T10:04:26Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1223848358 -->
## @hidde — 2022-08-23

Yay for default padding, 0.25em and 0.25em both look like a fine default to me.

<!-- comment id=1224382704 author=jh3y created=2022-08-23T17:16:31Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1224382704 -->
## @jh3y — 2022-08-23

From someone who's spent a lot of time with `popup` recently, I don't think it makes sense to set `display: none !important` in the UA stylesheet 👎 

It's kinda restricting for use cases and scenarios that might pop up (no pun intended 😅)

You can still set a layout style inside the pop-up when it is in the top layer with

`[popup]:top-layer { display: flex; }`

But, a wrapper element within the `popup` also does the trick. I can think of and have been playing with scenarios where you could have a `popup` shown in the DOM by default and then promote it to the top layer. For example, think of pop out media like a video player that's in the normal document flow that you promote to top layer when playing. Or how you might do certain picture in picture mode. Especially with shared element transitions.

Also, PWA and app like components like sliding drawers etc. You might have a tab that you press or tap to promote an action sheet or drawer to the top layer. You then get the backdrop and light dismiss which is handy. We were playing with a concept like this the other day for native feel UI. I've also built things like this in the past which can be tricky when building app like experiences on the web.

I'm not saying that having `display: none` will be a bad thing. But, blocking any sort of escape hatch for it wouldn't be ideal. Otherwise we potentially block creative and advanced scenarios we might not have even thought of yet. 

The workaround as discussed with @mfreed7 would be to have some sort of JavaScript intervention that applies the `popup` attribute and calls `showPopUp` when necessary but seems rather OTT when before there was way out with only CSS and no JavaScript required.

<!-- comment id=1224385139 author=mfreed7 created=2022-08-23T17:18:38Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1224385139 -->
## @mfreed7 — 2022-08-23

Thanks for the additional votes for `0.25em`!


> From someone who's spent a lot of time with `popup` recently, I don't think it makes sense to set `display: none !important` in the UA stylesheet 👎

I just wanted to point out also that the `<dialog>` element does not have a similar `!important` rule. E.g.:

```html
<dialog>This will be visible!</dialog>
<style>dialog {display: flex;}</style>
```

Perhaps we should revisit that [hasty decision](https://github.com/openui/open-ui/issues/561#issuecomment-1220060058) on my part.

<!-- comment id=1224440904 author=tbondwilkinson created=2022-08-23T17:42:21Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1224440904 -->
## @tbondwilkinson — 2022-08-23

I think `display: none;` is definitely a reasonable UA default, but I agree that `!important` feels a bit much.

FWIW, pop-out media players to me usually look modal in that they make the rest of the page non-interactible, see e.g. https://www.google.com/search?q=how+to+fish click on the video there, though this one completely hides the background you could imagine it graying the background instead. But since popups are intended to be modeless, those types of use cases aren't I think supported.

But I do think that people will be very tempted to use popup for things that they want to light dismiss, which may include taking parts of the DOM and showing them more prominently in the top-layer, or combining it with anchor positioning to move them out of their flow and attach to some other element.

<!-- comment id=1224459752 author=jh3y created=2022-08-23T17:49:59Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1224459752 -->
## @jh3y — 2022-08-23

I don't think it would be so bad if we were able to override the UA styles, but, we can't.

The pop-out media idea was just one of those that came to mind. FWIW, I've definitely seen them light dismissable in the past where dismissing can even pause the content. You could achieve that behavior described with `popup=manual`.

Yeah, for me, it's impossible to think of every edge case or use case that might pop up. But, allowing people to show us would be ideal 👍 

<!-- comment id=1224712134 author=gregwhitworth created=2022-08-23T19:34:31Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1224712134 -->
## @gregwhitworth — 2022-08-23

@mfreed7 I removed the agenda label for now since this was discussed in the past. Do you think you need to get another resolution given the changes?

<!-- comment id=1226017768 author=mfreed7 created=2022-08-24T17:22:52Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1226017768 -->
## @mfreed7 — 2022-08-24

> @mfreed7 I removed the agenda label for now since this was discussed in the past. Do you think you need to get another resolution given the changes?

Well, my plan was to take 5 minutes to get a quick resolution just confirming, but you're right that we probably (?) don't need to do that.

Let's do this: here's the latest all-in proposal for the UA stylesheet. Besides the spec question (denoted by `{spec language for "is hidden"}`), **are there any objections to this**?

```css
[popup] {
  position: fixed;
  inset: 0;
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  padding: 0.25em;
  overflow: auto;
  color: CanvasText;
  background-color: Canvas;
}

[popup]::backdrop {
    pointer-events: none !important;
}

[popup]:{spec language for "is hidden"} {
  display: none;
}
```

<!-- comment id=1226653853 author=domenic created=2022-08-25T01:01:28Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1226653853 -->
## @domenic — 2022-08-25

I wonder why there is no selector for the "is hidden" state? If the UA wants to style it, isn't it possible the user might as well?

<!-- comment id=1227903734 author=mfreed7 created=2022-08-26T01:04:52Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1227903734 -->
## @mfreed7 — 2022-08-26

> I wonder why there is no selector for the "is hidden" state? If the UA wants to style it, isn't it possible the user might as well?

Yeah, I was thinking it might be useful to have something like `:popup-opening`, which matches pop-ups that aren't in the hidden state. Then you could style the transitioning-open state separately from the fully-`:open` state. I'm still not clear what the use case is for that, but I've heard it requested.

The alternative is `:popup-hidden`, but that seems a bit more difficult to use for styling transitions, since you'd need to always use `:not(:popup-hidden)` for that. But maybe the real use case is styling pop-ups in the page when they're not officially showing. E.g. a small visible element that, when clicked, gets bigger and becomes a pop-up. I've also heard about that use case.

We need to better-understand the use cases, I think, in order to get the right polarity and name for this pseudo.

<!-- comment id=1230505056 author=tbondwilkinson created=2022-08-29T15:53:41Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1230505056 -->
## @tbondwilkinson — 2022-08-29

I think [an :open](https://github.com/w3c/csswg-drafts/issues/7319#issuecomment-1225988203) pseudoclass kind of implies the possibility for a `:closed`? That feels like what Domenic might be asking. I think `:opening` and `:closing` are useful additional states, where something could be still marked as `:closed` but also as `:opening` for styling transitions, perhaps.

<!-- comment id=1230697813 author=mfreed7 created=2022-08-29T18:27:16Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1230697813 -->
## @mfreed7 — 2022-08-29

> I think [an :open](https://github.com/w3c/csswg-drafts/issues/7319#issuecomment-1225988203) pseudoclass kind of implies the possibility for a `:closed`? That feels like what Domenic might be asking. I think `:opening` and `:closing` are useful additional states, where something could be still marked as `:closed` but also as `:opening` for styling transitions, perhaps.

Ahh, you might be right that `:closed` was what @domenic was requesting? I always wonder whether we really should add negated pseudo classes for everything, when we have `:not()`. But here, this is interesting, because the "open" state for a pop-up is actually tri-state (hidden, transitioning, and showing). So there is an unobservable state unless we add a specific pseudo for it. Hence the `:opening` (or `:opening-or-open`?) suggestion.

<!-- comment id=1230703148 author=tbondwilkinson created=2022-08-29T18:32:35Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1230703148 -->
## @tbondwilkinson — 2022-08-29

True, `:not(:open)` could be the thing that you specify here for `display: none;` for now?

I hadn't considered the option of a tri-state... or would it be a quad state so you could style transitioning to open and transitioning to close separately? I think people could probably argue for a while about whether to considering something that is "opening" "open" or "closed" so it might be good at least to consider side-stepping that argument by considering that a separate state. I do think people will fall into various categories:

1. Consider open/close to be a binary. Either a popup is open or its not.
2. Consider open/close to have additional edge states for opening and closing. And whether they considering opening/closing to fall under open or close may vary.

<!-- comment id=1233398943 author=mfreed7 created=2022-08-31T20:39:55Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1233398943 -->
## @mfreed7 — 2022-08-31

> True, `:not(:open)` could be the thing that you specify here for `display: none;` for now?

Can't do that - `:open` means the pop-up is in the "showing" state. So `:not(:open)` would mean *either* "hidden" or "transitioning", and we definitely only want to apply `display:none` when "hidden". Else transitions are broken.


> I hadn't considered the option of a tri-state... or would it be a quad state so you could style transitioning to open and transitioning to close separately? I think people could probably argue for a while about whether to considering something that is "opening" "open" or "closed" so it might be good at least to consider side-stepping that argument by considering that a separate state. I do think people will fall into various categories:
> 
> 1. Consider open/close to be a binary. Either a popup is open or its not.
> 2. Consider open/close to have additional edge states for opening and closing. And whether they considering opening/closing to fall under open or close may vary.

Ugh. This is why I hesitated to specify an explicit pseudo state for this. Perhaps this part of the feature needs more discussion before we introduce it. Certainly I don't think it is needed for "v1".

---

Last call for objections to this being the proposed standard CSS for `popup`:

```css
[popup] {
  position: fixed;
  inset: 0;
  width: fit-content;
  height: fit-content;
  margin: auto;
  border: solid;
  padding: 0.25em;
  overflow: auto;
  color: CanvasText;
  background-color: Canvas;
}

[popup]::backdrop {
    pointer-events: none !important;
}

[popup]:{spec language for "is hidden"} {
  display: none;
}
```

<!-- comment id=1234963692 author=mfreed7 created=2022-09-02T01:25:33Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1234963692 -->
## @mfreed7 — 2022-09-02

I'm going to close this issue as agreed, for now. I'm very happy to re-open it if something comes up.

Spec-wise, I think the `{spec language for "is hidden"}` portion is easier to write in spec prose than CSS, so the details we've been discussing above are likely implementation details. @josepharhar FYI, and hopefully I'm correct that there's an easy sentence to describe visibility. Could you please incorporate the updated CSS above into [the spec PR](https://github.com/whatwg/html/pull/8221/files)?

<!-- comment id=1235820421 author=josepharhar created=2022-09-02T19:08:14Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1235820421 -->
## @josepharhar — 2022-09-02

> Spec-wise, I think the {spec language for "is hidden"} portion is easier to write in spec prose than CSS, so the details we've been discussing above are likely implementation details

Yes, since the secret pseudo selector we're using in chromium should not be in the spec, I crafted the spec to have the same behavior without any selectors.

> Could you please incorporate the updated CSS above into [the spec PR](https://github.com/whatwg/html/pull/8221/files)?

Done: https://github.com/whatwg/html/pull/8221/files/9e0421c76e82132537c90e7da6e04885689c79de..a2cd6f0457d5d959916e1c01ba5c6a14a72160ee

<!-- comment id=1241074964 author=scottaohara created=2022-09-08T18:25:09Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1241074964 -->
## @scottaohara — 2022-09-08

@mfreed7 was playing around with popup today and noticed that the default style for the backdrop was updated so that is has a background of `rgba(0, 0, 0, 0.1)`.  That doesn't jive with what's been outlined here though, as the backdrop shouldn't have any background color to it by default.  

edit:

i realized this happened because i was using a `dialog` element that i was using `popup=manual` on.  So, even though it was non-modal, the dialog's backdrop was being made visible.

<!-- comment id=1241171781 author=mfreed7 created=2022-09-08T20:00:00Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1241171781 -->
## @mfreed7 — 2022-09-08

Re-opening this issue to discuss what we need to add to the UA stylesheet to avoid (maybe?) the `<dialog popup>` getting that background color. Then again, maybe that's the correct background color since it's a `<dialog>`?

<!-- comment id=1241223547 author=scottaohara created=2022-09-08T20:55:00Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1241223547 -->
## @scottaohara — 2022-09-08

thanks @mfreed7.  Definitely think this is something that would need to be avoided since a dialog opened via `.show()` doesn't have a backdrop - which makes sense because its modeless.   So a dialog opened via `showPopUp` - also being modeless - should also not have a backdrop by default.

<!-- comment id=1242297093 author=mfreed7 created=2022-09-09T17:54:21Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1242297093 -->
## @mfreed7 — 2022-09-09

I suppose the right thing to do here would be to modify the UA stylesheet to make sure `rgba(0,0,0,0.1)` only gets applied to `dialog:modal` and not `dialog:open`.

<!-- comment id=1269024954 author=mfreed7 created=2022-10-05T21:59:20Z url=https://github.com/openui/open-ui/issues/561#issuecomment-1269024954 -->
## @mfreed7 — 2022-10-05

> I suppose the right thing to do here would be to modify the UA stylesheet to make sure `rgba(0,0,0,0.1)` only gets applied to `dialog:modal` and not `dialog:open`.

Ok, the Chromium implementation has been modified to do this, by making the UA selector

```css
[popup]:open::backdrop {
```

and adding

```css
background-color: transparent;
```

@josepharhar  is adding the same to the spec. I'm going to close this as re-resolved.
