---
number: 804
title: "[switch]: Naming of the component and it's parts"
state: "closed"
labels: ["needs-triage", "switch"]
author: "gfellerph"
created_at: "2023-08-19T09:46:52Z"
closed_at: "2025-09-16T07:41:19Z"
url: "https://github.com/openui/open-ui/issues/804"
comments: 3
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

There is a draft pull request for adding a switch explainer: #785.

It contains assumptions for naming the element itself as well as it's inner parts. With this issue, I'd like to get feedback and clarify the naming of these things.

**Element:**
Proposed: `<switch>`
Alternatives: `<toggle>`, `<toggleswitch>`, `<lightswitch>`, `<togglebutton>`, ...

**Active state/pseudo class:**
Proposed: `<switch toggled>` and `switch:toggled { ... }`
Alternatives: `<toggle switched>`, `<switch checked>`, ...

_Do you toggle a switch or switch a toggle?_

**Anatomy:**
Proposed:
![image](https://github.com/openui/open-ui/assets/1659006/0fa69f9a-9f5c-4ff5-9780-f4e87731b373)

```html
<switch>
  <track>
    <toggled></toggled>
    <untoggled></untoggled>
  </track>
  <thumb></thumb>
</switch>
```

Alternatives: many

<!-- comment id=1692233975 author=css-meeting-bot created=2023-08-24T18:47:04Z url=https://github.com/openui/open-ui/issues/804#issuecomment-1692233975 -->
## @css-meeting-bot — 2023-08-24

The Open UI Community Group just discussed `[switch]: Naming of the component and it's parts`, and agreed to the following:

* ``RESOLVED: call the new element `<switch>`, and land that in the explainer. Discuss the sub-parts later.``

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> philippgfeller: the proposal is still in draft mode… not sure how ready we are to get a resolution on something… <br>
&lt;hdv> philippgfeller: editing the draft I made some broad assumptions on naming things, so wanted to throw it out there<br>
&lt;masonf> q?<br>
&lt;masonf> q+<br>
&lt;Luke> q+<br>
&lt;hdv> philippgfeller: to see what you think… starting with the element itself… I named it `&lt;switch>`, but could also be `&lt;toggle>` or anything else… am not a native English speaker so not sure<br>
&lt;hdv> ack mas<br>
&lt;hdv> masonf: one definite proposal that is on the table is to reuse the `&lt;input>` element<br>
&lt;hdv> masonf: it does negate some of the cases you propose because `input`s can't have children<br>
&lt;hdv> philippgfeller: I think greg proposed to have both `input` and `&lt;switch>` , `input` for simple cases and the `&lt;switch>` element with more detailed applications<br>
&lt;hdv> philippgfeller: like if you need animations to reveal something… I did analysis on the most popular design systems, many could be done with just the `input` element, but there are also some that can't, like ones that have text (and pseudo would be a bit hacky)<br>
&lt;hdv> masonf: hacky because of positioning?<br>
&lt;hdv> philippgfeller: not sure<br>
&lt;masonf> q?<br>
&lt;hdv> masonf:  I guess I'm curious… what percentage of use cases do you think we could cover with `input` rather than `&lt;switch>`<br>
&lt;scotto_> q+<br>
&lt;hdv> philippgfeller: in HTML it would be easier to do things like translation<br>
&lt;masonf> ack Luke<br>
&lt;hdv> Luke: if we were to go the new element route… `&lt;switch>` is the name I would go for because it would match with the ARIA role… it is the simplest… `&lt;toggleswitch>` is kind of superfluous<br>
&lt;hdv> Luke: as a web developer I would prefer a new element rather than having it on an `input` element… because `input` has a lot of history to it that has issues behaviour wise<br>
&lt;hdv> Luke: this new thing would not have all that history<br>
&lt;hdv> masonf: the proposal of using the `input` has the nice advantage of having a checkbox as a fallback even in browsers that don't know what the new attribute is<br>
&lt;hdv> Luke: personally… I would say no as a polyfill could relatively easily make a fallback<br>
&lt;hdv> Luke: maybe you would want to go for a completely different element anyway in that use case<br>
&lt;masonf> q?<br>
&lt;masonf> ack scott<br>
&lt;jarhar> q+<br>
&lt;hdv> scotto_: I'm on the queue to say not call this a toggle in any way<br>
&lt;hdv> scotto_: the intend of this is to make a switch<br>
&lt;hdv> scotto_: a togglebutton is a very different thing<br>
&lt;hdv> scotto_: a toggle button is like, in a text editor, to make content bold or italic or something… that would be buttons with `aria-pressed`, as opposed to `switch`… pedentically semantically there are differences in meaning, but those differences mean a lot<br>
&lt;hdv>  scotto_ they give different aural queues<br>
&lt;hdv> s/scotto_/scotto_:<br>
&lt;hdv> scotto_: I would not want people to come across `switch` and use it to create a `toggle`<br>
&lt;hdv> masonf: are there other use cases for `input` with `switch` attribute that you can think of?<br>
&lt;hdv> scotto_: people keep trying to turn it into something that is not a switch, like by adding extra things to it, like intermediate state etc<br>
&lt;hdv> masonf: proposal has no intermediate state<br>
&lt;masonf> q?<br>
&lt;hdv> scotto_: yes, and that's good<br>
&lt;masonf> ack jarhar<br>
&lt;hdv> jarhar: I just took a look at the explainer… I see there is some videos of switches that currently exist… it seems like most of them put some text or symbol on top of the thumb or one of the slider text<br>
&lt;philippgfeller> +q<br>
&lt;hdv> jarhar: that seems one of the use cases… people want to have an SVG, image, text, things like that<br>
&lt;scotto_> q+<br>
&lt;hdv> jarhar: I wonder if anyone would have anything to say if this would be dramatically worse to put that kind of content in a `content` property of a pseudo element<br>
&lt;hdv> ack phil<br>
&lt;hdv> philippgfeller: the videos are a little bit suggestive… I did an analasis… 70% of design systems I looked at don't use text or image… the others did, that's the ratio we're looking at<br>
&lt;hdv> masonf: so the majority of use cases doesn't need it… but then some of the cool/fancy ones do<br>
&lt;masonf> ack scott<br>
&lt;hdv> scotto_:  'on' and 'off' localised in lots of languages is not going to work for everything<br>
&lt;hdv> scotto_: maybe this is not something that browsers should do by default<br>
&lt;philippgfeller> Ratios of features used https://deploy-preview-785--open-ui.netlify.app/components/switch.explainer/#slots<br>
&lt;hdv>  scotto_: in many it looks like an `i` or `o`<br>
&lt;hdv> scotto_: so probably better adjusting it for the different locales<br>
&lt;philippgfeller> q+<br>
&lt;dbaron> Agree that the layout implications of localizing the Choose File button in &lt;input type=file> have caused problems in the past...<br>
&lt;masonf> Note that it looks like a 1 or 0, not an i or o. (At least that's how I remember it.)<br>
&lt;hdv> scotto_: the other thing is… whatever is in there, you don't want that exposed to assistive technologies<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> scotto_:  I'm not against this… I just think the initial proposal should make it very clear that if you're going to do it differently, you're on your own<br>
&lt;masonf> ack philip<br>
&lt;hdv> scotto_: not to say that people can't do good things with them, they can and should if they want to<br>
&lt;Luke> q+<br>
&lt;hdv> philippgfeller: the plain switch would just be a switch with no content, so if people want to put text in there they need to localise it<br>
&lt;masonf> ack greg<br>
&lt;hdv> gregwhitworth: I want to make sure we define it will initially be empty … the default should have an expectation that the majority of the use cases have designed aesthetic, we should do it in a way that has, like `aria-hidden=true` on there<br>
&lt;hdv> gregwhitworth: we should concretely that that's how it is implemented<br>
&lt;hdv> gregwhitworth: if people do want to do a localisation scenario where it's on/off, they would need to also do that themselves<br>
&lt;hdv> gregwhitworth: dev tools could even do warnings there to convey that to devs<br>
&lt;masonf> ack Luke<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> Luke: it's possible that Safari would want to have symbols in their switches, as they do on their platforms… triggered by, I think, the 'differentiate not just by colour' setting<br>
&lt;hdv> gregwhitworth: this goes back to last week's discussion re standardisation of mobile selectlist<br>
&lt;hdv> gregwhitworth: we would standardise the entire anatomy<br>
&lt;hdv> gregwhitworth: Apple would ship the same thing everybody else would ship<br>
&lt;hdv> gregwhitworth: if people put in an argument to say there should be in the anatomy what Apple has on platform today, that would be fine and we could standardise on it<br>
&lt;masonf> q?<br>
&lt;masonf> ack greg<br>
&lt;hdv> masonf: what are we looking for in terms of conclusions/resolutions?<br>
&lt;gregwhitworth> +1<br>
&lt;hdv> masonf: I think at Open UI we should be easier on landing… this one is still in draft, hope we can land it soon<br>
&lt;hdv> masonf: would like to request that the explainer mentions the other proposal (input type=checkbox with `switch` attribute), and explain how it is different from that<br>
&lt;hdv> masonf: I worry if they don't come together, neither would land<br>
&lt;hdv> masonf: I'd like to resolve let's land the explainer… and also maybe resolve to add that differentation<br>
&lt;hdv> gregwhitworth: is the goal to land this explainer?<br>
&lt;masonf> Proposed resolution: add a section to the explainer to discuss &lt;input type=checkbox switch>, and then land the explainer.<br>
&lt;hdv> philippgfeller: yes<br>
&lt;jarhar> q+<br>
&lt;hdv> philippgfeller: there is a link to the input[type=checkbox] with `switch` attr, but not a detailed description<br>
&lt;hdv> masonf: would love to see that detailed analysis of which examples, which you have many of, which is great,  need which pattern<br>
&lt;hdv> jarhar: based on what we were saying earlier, it sounds like people like `&lt;switch>` as a name, we could do a resolution for that too?<br>
&lt;masonf> Proposed resolution: call the new element `&lt;switch>`<br>
&lt;hdv> masonf: I heard `&lt;switch>` a lot and a lot of others that people didn't like<br>
&lt;hdv> gregwhitworth: we can resolve on the sub parts later<br>
&lt;hdv> masonf: I think we need a resolution that this direction is the right way to go<br>
&lt;hdv> gregwhitworth: agree<br>
&lt;masonf> Proposed resolution: call the new element `&lt;switch>`, and land that in the explainer. Discuss the sub-parts later.<br>
&lt;Luke> +1<br>
&lt;hdv> +1<br>
&lt;philippgfeller> +1<br>
&lt;masonf> RESOLVED: call the new element `&lt;switch>`, and land that in the explainer. Discuss the sub-parts later.<br>
</details>

<!-- comment id=2297753977 author=github-actions[bot] created=2024-08-20T00:40:19Z url=https://github.com/openui/open-ui/issues/804#issuecomment-2297753977 -->
## @github-actions[bot] — 2024-08-20

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=3296301685 author=gfellerph created=2025-09-16T07:41:19Z url=https://github.com/openui/open-ui/issues/804#issuecomment-3296301685 -->
## @gfellerph — 2025-09-16

I'm closing this as done, also https://github.com/openui/open-ui/issues/1282 could change the proposal to support the switch attribute on checkbox inputs instead.
