---
number: 778
title: "[exclusive accordion] how valuable is having working exclusivity in subtrees not connected to a document"
state: "closed"
labels: []
author: "dbaron"
created_at: "2023-07-05T18:43:40Z"
closed_at: "2023-08-01T18:43:24Z"
url: "https://github.com/openui/open-ui/issues/778"
comments: 2
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

The current proposal for exclusive accordion [proposes](https://open-ui.org/components/accordion.explainer/#interaction-with-open-attribute) that exclusivity is only enforced for `details` elements whose root is a `Document` or `ShadowRoot`.  This has [been discussed](https://github.com/whatwg/html/pull/9400#discussion_r1222094129) in the HTML PR for adding the feature, and based on the discussion there, I wanted to raise it explicitly here.

I made this decision because it seemed less constraining on implementations:  in particular, if implementations are going to cache information about which `details` elements are in the same group, they need to *store* that cache somewhere, and that location makes sense as `DocumentOrShadowRoot`.  It was pointed out that this behavior is inconsistent with radio buttons, however @emilio pointed out that the radio button behavior has known bugs in current implementations, so it's not perfect.

In the current Chrome implementation (since [the change](https://chromium-review.googlesource.com/c/chromium/src/+/4617028) to do notifications in tree order), I'm actually *not* taking advantage of this behavior, so it would be entirely straightforward to switch.  However, it would make it harder to reintroduce any form of caching in the future if we decided it was useful to optimize for speed.

So I'm interested in feedback on this decision.  And most specifically, I'm interested in feedback from developers or those who understand their needs as to how useful having this feature work in subtrees whose root is not a `Document` or a `ShadowRoot` would be (for example, when setting up something to be inserted later).

<!-- comment id=1634732524 author=css-meeting-bot created=2023-07-13T18:50:28Z url=https://github.com/openui/open-ui/issues/778#issuecomment-1634732524 -->
## @css-meeting-bot — 2023-07-13

The Open UI Community Group just discussed `[exclusive accordion] how valuable is having working exclusivity in subtrees not connected to a document`.

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [exclusive accordion] how valuable is having working exclusivity in subtrees not connected to a document<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/778<br>
&lt;hdv> dbaron: there are lots of ways to think about disclosing widgets and accordion widgets… HTML already has details/summary, which is like its native form of a disclosure widget<br>
&lt;hdv> dbaron: you could think of accordions as multiple details/summary widgets… but one thing with accordions is that there are some subtle behaviours people used<br>
&lt;hdv> dbaron: the most common ones would be: (a) the different parts of the accordion behave independently, (b) either zero or one of the parts is open, I call this exclusive accordion<br>
&lt;hdv> dbaron: what exclusive accordion does is to add an attribute to HTML so that you can associate multiple details/summary constructions and then have them behave exclusively.<br>
&lt;hdv> dbaron: this has some similarity to radio buttons… if you select one of them, you unselect the already selected one… in this case I made a decision for this to behave different from radio buttons, what if the element isn't actually attached to a document or shadow root… I wrote this is only supposed to work for a document or shadow root<br>
&lt;hdv> dbaron: one of the ways to track which of these elements are in the same set is by attaching them to the same document or shadowroot<br>
&lt;hdv> dbaron: the Chromium implementation doesn't depend on this at all… I'd be fine changing from Chromium implementation… but Emilio raised that radio buttons aren't working exactly as they are supposed to, there's a bunch of implementation issues with that already<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> dbaron: so the question is should this work when the exclusive accordion is not attached to a document… I should in the explainer… one of the design decisions I made, the exclusivity enforcement does not happen in all cases, only if there is a user interaction or some other cases<br>
&lt;masonf> q?<br>
&lt;hdv> dbaron: so in that case it would be possible to create exclusive accordions that have more than one open, due to what I would call developer error<br>
&lt;hdv> briankardell: as much as it is developer error, it seems bad to have exclusive accordions that could be not exclusive<br>
&lt;hdv> briankardell: otherwise it would confuse people<br>
&lt;hdv> dbaron: one of the difficulties is that details maintains its open/closed state in an attribute<br>
&lt;hdv> dbaron: there are times where it is difficult to change that. That makes it hard to do what you want<br>
&lt;hdv> gregwhitworth: I'm trying to think of a scenario for when its not attached to document or shadowroot, seems to be when I created the element but haven't inserted it into the DOM<br>
&lt;hdv> dbaron: that's basically the question… do developers need exclusivity enforcement when the element hasn't been added to the DOM yet<br>
&lt;masonf> q?<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> annevk: I'm also not sure if developers care… what I wonder about… not sure if the existence of bugs with input[type=radio] is sufficient to not continue on the same path as input[type=radio]… can't we fix those issues instead? I would favour staying consistent with what we have<br>
&lt;hdv> dbaron: at this point I would be fine going with that, but would want to run it by Emilio as he is not there<br>
&lt;hdv> bkardell: so we argue for staying with the current weird rather than adding other weird behaviour<br>
&lt;hdv> annevk: I don't necessarily think it's that weird though<br>
&lt;hdv> dbaron: one other question is… it could enforce exlusivity within its own subtree vs it could enforce within the @@@<br>
&lt;hdv> annevk: same tree!<br>
&lt;hdv> dbaron: would think same tree as well<br>
&lt;hdv> NicoleSullivan: I wonder if this could impact JavaScript frameworks or widget libraries where they are adding things to the DOM frequently and may want the consistently<br>
&lt;hdv> NicoleSullivan: like in infinite scrolling<br>
&lt;gregwhitworth> that's a great callout Nicole<br>
&lt;hdv> dbaron: frameworks were one of my worries… with infinite scrolling… maybe framework authors would need to solve the problem in their own environment<br>
&lt;hdv> NicoleSullivan: we could talk to a few frameworks authors<br>
&lt;hdv> gregwhitworth: what kind of frameworks?<br>
&lt;hdv> NicoleSullivan: those who do virtualised DOMs or equivalents with different naems<br>
&lt;Westbrook> q+<br>
&lt;hdv> dbaron: the same tree approach seems lower risk for these… if we take lower risk we probably don't need to go ask them about it<br>
&lt;gregwhitworth> ack Westbrook<br>
&lt;hdv> NicoleSullivan: it's pretty quick to ask them about it, I can help with that too… I can help formulate the question and send it out… would expect them to read your doc and give you some sentences on whether it would work or not<br>
&lt;hdv>  Westbrook: if the idea is the do 'same weird'… it's going to act the same way as checkboxes/radios , so would there be new information to be gained<br>
&lt;hdv> NicoleSullivan: good question… I know form elements get special treatment sometimes in these frameworks<br>
&lt;hdv> scotto: does it need to be the same as checkbox/radios as this is a completely different kind of thing? they're not form controls or submitting form data? they're obviously doing something very different?<br>
&lt;hdv> scotto: would this change actually be such a big deal?<br>
&lt;hdv> annevk: most other features that we have work in a disconnected subtree<br>
&lt;brecht_dr> q+<br>
&lt;hdv> annevk: from that perspective it would be weird if this would work differently from that, as far as grouping goes… it's useful to look at existing APIs for those that don't have precedence<br>
&lt;una> q+<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;hdv> brecht_dr: I'm a bit worried that we start to create attribute after attribute… scared to lose the simplicity of HTML<br>
&lt;gregwhitworth> ack una<br>
&lt;hdv> una: 100% agree, simpler the better… <br>
&lt;scotto> unsure?  maybe my comment was misunderstood as advocating for a different attribute?<br>
&lt;hdv> una: with the edge cases like infinite scroll, I am not very concerned about that<br>
&lt;hdv> dbaron: I'll take up Nicole's offer to reach out to framework folks to see what feedback they have<br>
&lt;dbaron> Proposed resolution: switch to enforcing exclusivity (within the same tree) when not attached to a Document or ShadowRoot, and try to get feedback from framework authors to get additional information<br>
&lt;masonf> +1<br>
&lt;dbaron> (so that exclusivity would then always be enforced within same tree)<br>
&lt;dbaron> Resolution: switch to enforcing exclusivity (within the same tree) when not attached to a Document or ShadowRoot (so that exclusivity would be always enforced within the same tree), and try to get feedback from framework authors to get additional information<br>
</details>

<!-- comment id=1660886498 author=dbaron created=2023-08-01T18:43:24Z url=https://github.com/openui/open-ui/issues/778#issuecomment-1660886498 -->
## @dbaron — 2023-08-01

I've now updated the PR in https://github.com/whatwg/html/pull/9400/commits/9b767879bb41a886c6c8240308b4b091bc0237fb , WPT in https://github.com/web-platform-tests/wpt/pull/41246 , and Chromium code in https://crrev.com/1177898 .
