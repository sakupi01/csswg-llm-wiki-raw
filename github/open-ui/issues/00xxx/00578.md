---
number: 578
title: "[popup] It is error prone to have both async and sync 'hide' event"
state: "closed"
labels: ["agenda+", "popover"]
author: "smaug----"
created_at: "2022-08-08T09:51:58Z"
closed_at: "2022-12-01T19:20:32Z"
url: "https://github.com/openui/open-ui/issues/578"
comments: 14
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

Mixing sync and async event dispatch is error prone.
If one removes a popup from dom (that triggers async hide) and then adds it back and opens the popup, there will be a sync show event, right? And then once the event loop spins, the async hide is dispatched even though the relevant popup is shown.

<!-- comment id=1212280829 author=mfreed7 created=2022-08-11T17:32:31Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1212280829 -->
## @mfreed7 — 2022-08-11

Hmm, this is an excellent point. Do you have suggestions for how to handle the fact that dispatching synchronous events is basically prohibited at some times, e.g. while removing the popup from the tree? The alternative (I guess?) might be to just **not** fire the event in those cases. Thoughts?

<!-- comment id=1213086296 author=smaug---- created=2022-08-12T13:00:49Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1213086296 -->
## @smaug---- — 2022-08-12

I guess similar-ish issue might happen with focus/blur.
I can't recall how the blur is defined to work in this case
`
data:text/html,<input placeholder="focus me" onfocus="setTimeout(()=> this.remove())" onblur="document.body.textContent = 'blur fired'">`

<!-- comment id=1213505899 author=mfreed7 created=2022-08-12T20:42:52Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1213505899 -->
## @mfreed7 — 2022-08-12

> I guess similar-ish issue might happen with focus/blur. I can't recall how the blur is defined to work in this case ` data:text/html,<input placeholder="focus me" onfocus="setTimeout(()=> this.remove())" onblur="document.body.textContent = 'blur fired'">`

At least in Chromium, this is a special case added to ContainerNode::RemoveChild():

https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/dom/container_node.cc;l=724;drc=34ac5694a0cf645b70049f6c691056165d7e5b41

I'd like to avoid adding another special case for this event also, if possible.

Besides the point about nodes being removed from the tree, the other case I was hoping to avoid was when a pop-up is being hidden because a modal `<dialog>` or fullscreen element is moving into the top-layer. If we fire a synchronous `hide` in those cases, the event handler could preempt or mess with the dialog/fullscreen.

<!-- comment id=1269023606 author=mfreed7 created=2022-10-05T21:57:24Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1269023606 -->
## @mfreed7 — 2022-10-05

There seem to be only (?) two potential paths here:

 1. The existing behavior: fire `hide` **asynchronously** when a) the Element is removed from the Document, or b) a higher-priority top-layer element (e.g. modal dialog or fullscreen) is being shown.
 2. **Do not fire** the `hide` event at all in the two corner cases above.

Any others? I think my preference is <span>#</span>1, just because it feels even worse to skip events entirely. Yes, <span>#</span>1 can fire `show` and `hide` out of order, but only in *very* corner case situations. A combination of one of the two corner cases above, **plus** the immediate re-show of the same pop-up. I think out-of-order events likely cause fewer problems than missing events.

<!-- comment id=1270428268 author=tbondwilkinson created=2022-10-06T17:14:25Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1270428268 -->
## @tbondwilkinson — 2022-10-06

The main use case for `hide` in the explainer is to add animations right? In the cases where `hide` is sent asynchronously, the animation use case is already invalidated.

Unlike `show`, `hide`, isn't cancellable so you can't use that to prevent closing (and even if you could, I imagine you wouldn't want to if the popup was removed from the DOM, which seems like something that shouldn't be cancellable).

In my opinion:
1. Not firing `hide` seems reasonable for when the popup is removed from the DOM. I think it's rare someone will do this, and I think it's reasonable to not get any event if it happens. Removing the element from the DOM is a different operation than "hiding" it.
2. For the top-layer UI case, I suppose it's fine to be asynchronous. I'm not really sure if I agree with the choice for hide to be asynchronous, wouldn't you might want to do simultaneous animations for opening a dialog and closing a popup? But that design decision has probably sailed.

<!-- comment id=1270861298 author=mfreed7 created=2022-10-07T00:10:42Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1270861298 -->
## @mfreed7 — 2022-10-07

Certainly one of the use cases for `hide` is starting animations. But another is saving state from the pop-up when it hides. For that reason, it would seem like we should always fire it, right?

The reason behind the asynchronous event in the “other top layer UI” case is perhaps outdated or not completely thought through. The idea was to avoid a pop-up being able to cancel a modal dialog in the `hide` event handler. But that could still happen if the event is asynch. Perhaps we should just fire it normally in that case too? Note that we also skip CSS hide animations in this case, to avoid the delay in closing. But perhaps we should just let them run also? Your example is interesting - you might want the pop-up to fade out while the modal dialog is fading in.

<!-- comment id=1310541344 author=josepharhar created=2022-11-10T16:16:36Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1310541344 -->
## @josepharhar — 2022-11-10

> If one removes a popup from dom (that triggers async hide) and then adds it back and opens the popup, there will be a sync show event, right? And then once the event loop spins, the async hide is dispatched even though the relevant popup is shown.

How about we just check some state in the async task to fire the hide event to see if it's still sane to fire the hide event...? and/or add some code in other spots to cancel any attempt to fire an async hide event. It seems like the best and easiest path forward to me.

<!-- comment id=1317909343 author=mfreed7 created=2022-11-17T01:08:36Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1317909343 -->
## @mfreed7 — 2022-11-17

Coming back to this issue, it sounds like perhaps there are three paths, not two:

1. The existing behavior: fire `hide` **asynchronously** when a) the Element is removed from the Document, or b) a higher-priority top-layer element (e.g. modal dialog or fullscreen) is being shown.
2. **Do not fire** the `hide` event at all in the two corner cases above.
3. (NEW) Fire `hide` **synchronously** when a higher-priority top-layer element (e.g. modal dialog or fullscreen) is being shown. Do **not** fire the `hide` event when the Element is removed from the Document, or perhaps fire it "later" (as discussed in the very related [focus fixup conversation](https://github.com/whatwg/html/pull/8392)).

I'm coming around to at least the first part of <span>#</span>3 - perhaps we should just fire the events synchronously. It's not like developers can't "cancel" the other top layer elements in other ways, and this would enable fading out a popover while a dialog fades in, for example.

> How about we just check some state in the async task to fire the hide event to see if it's still sane to fire the hide event...? and/or add some code in other spots to cancel any attempt to fire an async hide event. It seems like the best and easiest path forward to me.

I think we need a definitive set of logic - either we fire the event or we don't. It's hard for developers to code around an event that might or might not get fired, depending on state.

<!-- comment id=1319127435 author=css-meeting-bot created=2022-11-17T19:53:25Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1319127435 -->
## @css-meeting-bot — 2022-11-17

The Open UI Community Group just discussed `[popup] It is error prone to have both async and sync 'hide' event #578`.

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [popup] It is error prone to have both async and sync 'hide' event #﻿578<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/578<br>
&lt;hdv> masonf: the issue is: the hide event, that fires when the popover is about to hide, is fired synchronously in most normal cases, but in one particular case you can't fire synchronously when you have a popover that you want to remove from the DOM, we prefer not to fire events when things are removed from the DOM<br>
&lt;hdv> masonf: when another higher priority top layer element is added, like when something goes full screen, currently popovers would get hidden immediately, eg animations don't run<br>
&lt;hdv> masonf: so three things to discuss: asynch hide event is kind of funny (should we do it, maybe not fire event at all, should we do something else?); is it too heavy handed to make full screen higher order, if I have a popover open and a modal dialog opens, it may be nice to animate popover out before modal dialog opens<br>
&lt;JonathanNeal> q+<br>
&lt;flackr> q+<br>
&lt;hdv> masonf: so I'm thinking we could make these things that can be animated when they disappear. But then the third question is, should an event be fired, and if so, should it be sync or async?<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;hdv> JonathanNeal: based on what you said, normally if the element is not removed the event happens before the animation fires synchronously, is that right?<br>
&lt;hdv> masonf: as it currently stands, if a full screen event causes it to disappear the event is fired synchronously, but would like to change that if we can<br>
&lt;hdv> JonathanNeal: what about… that toggle event or the before event, it would only fire when the event can be tracked, which can't happen when it is removed… but the open/opened event of the other thing could always happen async, would that work at all?<br>
&lt;hdv> masonf: so you're saying, when element is removed, don't fire toggle event, but as a help, add the after toggle and fire that async?<br>
&lt;hdv> JonathanNeal: with the risk that it doesn't fire at all until we resolved on an after event<br>
&lt;hdv> JonathanNeal: that makes a lot of sense to me… would advocate for it remaining synchronous… for security and perf reason can understand it wouldn't fire if it wasn't properly closing<br>
&lt;hdv> masonf: so would be weird to fire an event async if it is normally sync?<br>
&lt;hdv> masonf: would be better not to fire it at all?<br>
&lt;hdv> JonathanNeal: yes that would be my opinon<br>
&lt;gregwhitworth> ack flackr<br>
&lt;hdv> s/opinon/opinion<br>
&lt;scotto_> q+<br>
&lt;hdv> flackr: a third option is that we could make it always asynchronous similar to animation events or requestAnimationFrame, where at the start of the next frame, before all the other stuff happens and updating life cycle, we fire the event<br>
&lt;hdv> flackr: nice thing about that would be you're more likely to have a clean style state going into this<br>
&lt;hdv> flackr: so you would have less forced style flashes<br>
&lt;hdv> masonf: so you're proposing deferring all behaviours that happen on hide, fire the event async then run the other behaviours<br>
&lt;hdv> masonf: you could run into some funny things in script when you do that<br>
&lt;gregwhitworth> ack scotto_<br>
&lt;hdv> scotto_: if the toggle button that invoked the popover, if the popover is removed will the button state be corrected?<br>
&lt;hdv> masonf: in the chromium implementation I'm almost 100% sure that aria-expanded is no longer true when the element is not in the DOM element, should add a test for that<br>
&lt;hdv> s/button state/button's aria-expanded state<br>
&lt;hdv> gregwhitworth: ok we'll add to agenda for the next time<br>
&lt;JonathanNeal> If this is async, I would expect some kind of `finished` like promise.<br>
&lt;hdv> gregwhitworth: ok we'll get 10 mins back! Remember, next week we will not meet, in two weeks we'll be in a different venue (not Zoom), I'll update Discord and the Google calendar invite<br>
</details>

<!-- comment id=1319133712 author=mfreed7 created=2022-11-17T19:59:31Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1319133712 -->
## @mfreed7 — 2022-11-17

So in our discussion just now, there weren't too many conclusions, but it was a good discussion. I heard the following viewpoints:
1. Don't fire the "hide" event in the case that the element has been removed from the DOM. It is confusing to get what is normally a synchronous event asynchronously, after the thing has happened. (Side point, you might have opinions on https://github.com/whatwg/html/pull/8392)
2. Maybe make the "hide" event *always* asynchronous, which requires deferring all of the behavior until that async event runs. (My counterpoint: this makes lots of simple Javascript code confusing. E.g. `p.showPopover(); p.matches(':open') === false`.)
3. Question about ARIA: will `aria-expanded` still be in the correct state when the popover is removed from the DOM? Answer: yes, it should still be correct. This is just about the events.

Let me know if I missed anything, and more importantly, chime in with suggestions for ways forward here. In particular, we didn't discuss my proposal above to make the events synchronous (and animations-respecting) when modal dialogs and fullscreen elements are shown. I'd love input on that too.

<!-- comment id=1332769527 author=mfreed7 created=2022-11-30T21:43:22Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1332769527 -->
## @mfreed7 — 2022-11-30

Ok, at this point, I believe these are the open questions (assuming rough agreement with [my comment above](https://github.com/openui/open-ui/issues/578#issuecomment-1319133712)):

- Is it ok to *not* fire the "hide" event if a visible popover is removed from the document?
- If the answer above is "yes", then is it ok to always make "hide" synchronous, including when fired during a `dialog.showModal()` or `requestFullscreen()`?

If the answer to both is "yes", then I think we have a resolution to this issue. Note that the second point makes it "ok" to animate the popover "hide" while a dialog/fullscreen is being shown. That actually seems nice, from a user/developer point of view. Note that this will not delay the dialog/fullscreen showing, so I don't think there is much risk of "abuse" type behavior. We can always apply UA limitations to the duration of those hide animations, if needed.

<!-- comment id=1334236879 author=css-meeting-bot created=2022-12-01T19:20:17Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1334236879 resolution=true -->
## @css-meeting-bot — 2022-12-01

The Open UI Community Group just discussed `[popup] It is error prone to have both async and sync 'hide' event #578`, and agreed to the following:

* `RESOLVED: fire the "hide" event asynchronously when the popover is removed from the document. For dialog.showModal and requestFullscreen, synchronously fire "hide" and allow animations.`

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: [popup] It is error prone to have both async and sync 'hide' event #﻿578<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/578<br>
&lt;JonathanNeal> scribe: hdv<br>
&lt;hdv> masonf: so the current behavior and current spec is that the hide event can be either sync or async, depending on what's happening / why popover is being hidden<br>
&lt;hdv> masonf: this seems funny and problematic to me<br>
&lt;hdv> masonf: for instance, it is shown and gets hidden for async reason, but then shown…<br>
&lt;hdv> masonf: there are ways to fix this<br>
&lt;hdv> masonf: two cases where async happens: one is, not okay to fire sync events when containing element is removed from the document<br>
&lt;hdv> masonf: way around it is not fire an event<br>
&lt;hdv> masonf: another place where this happens is when higher prio top layer elements are added, like dialog.showModal or full screen content, they hide popovers immediately and fire hide event async so that they don't get in the way of higher priority top layer elements<br>
&lt;hdv> masonf: we could relax that, as sometimes it might be nice, eg maybe we want to animate the popover out instead of just throwing it out immediately<br>
&lt;JonathanNeal> +1 to not fire the "hide" event if a visible popover is removed from the document<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> masonf: so proposal would be don't fire hide event when popover is thrown out, and fire sync hide events instead<br>
&lt;hdv> gregwhitworth: so the fact that you're not firing an event is what concerns me, I agree with everything you said except for the not firing the event<br>
&lt;hdv> gregwhitworth: to an extent we're assuming the author is aware why the dom node is removed… I can envision scenarios where someone writes crappy code and removes all of the popups out of the DOM tree… can imagine stuff like teaching UI wanting to reset where something was<br>
&lt;hdv> gregwhitworth: I could see that kind of scenario breaking<br>
&lt;hdv> masonf: it's a good question…there is an issue that I linked to on WHATWG, 8292<br>
&lt;hdv> masonf: there is a question about… if you remove a currently focused from the DOM, what should happen in terms of firing events?<br>
&lt;hdv> masonf: so could not fire sync event<br>
&lt;hdv> gregwhitworth: I thought that's what you suggested<br>
&lt;JonathanNeal> q+<br>
&lt;hdv> masonf: two options: no events at all vs firing an async event<br>
&lt;hdv> gregwhitworth: I would argue for the async event<br>
&lt;gregwhitworth> ack greg<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> masonf: the scenario that people bring up is… showpopover, remove the popover from the document, then add it back and show it immediately… you'll get a sync show event, and then an async hide event, which you no longer expected<br>
&lt;hdv> masonf: not sure how common it is \<br>
&lt;hdv> gregwhitworth: seems like that would be confusing… but at the same time, not providing the event would have its own problems too<br>
&lt;gregwhitworth> ack JonathanNeal<br>
&lt;hdv> gregwhitworth: it would seem to me best to fire the async event…  and if you end up on that use case… yeah, I don't know what to do then, seems like an edge case<br>
&lt;hdv> JonathanNeal: could you give us a refresh… why does a non bubbling non cancelleable event could not always be synchronous? or is that the problem?<br>
&lt;hdv> masonf: it is disallowed in Chromium, because a synchronous event can put the node back in the document again, which could lead to weird edge cases<br>
&lt;hdv> JonathanNeal: thanks<br>
&lt;hdv> masonf: eg imagine you're removing an entire tree, and then the browser does that and right in the middle of that operation you start to add it back<br>
&lt;hdv> q+<br>
&lt;gregwhitworth> ack hdv<br>
&lt;gregwhitworth> hdv: I agree with gregwhitworth, it sounds better to get the event and possibly some edge case<br>
&lt;masonf> q?<br>
&lt;gregwhitworth> hdv: but that's probably better than not having any event at all<br>
&lt;masonf> Proposed resolution: fire the "hide" event asynchronously when the popover is removed from the document. For dialog.showModal and requestFullscreen, synchronously fire "hide" and allow animations.<br>
&lt;JonathanNeal> So, synchronous is bad because we could end up writing (effectively) this with events `while (true) { child.remove(); container.append(child); }`?<br>
&lt;masonf> RESOLVED: fire the "hide" event asynchronously when the popover is removed from the document. For dialog.showModal and requestFullscreen, synchronously fire "hide" and allow animations.<br>
&lt;hdv> masonf: is there an example of showing a dialog while something else is open and animate them both, do you have one jhey by any chance?<br>
&lt;hdv> jhey: I don't have one at the moment<br>
&lt;hdv> gregwhitworth: maybe like have a teaching UI open, and then go full screen, show a popup that says 'click this  to go full screen', and it would animate aay<br>
&lt;hdv> s/aay/away<br>
&lt;JonathanNeal> I like it. The “_Chat is now available in full screen_” popup.<br>
</details>

<!-- comment id=1372671306 author=css-meeting-bot created=2023-01-05T20:00:45Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1372671306 -->
## @css-meeting-bot — 2023-01-05

The Open UI Community Group just discussed `[popup] It is error prone to have both async and sync 'hide' event`.

<details><summary>The full IRC log of that discussion</summary>
&lt;una> masonf: current behavior is beforetoggle happens mostly synchronously, and in the show case it can be canceled, can't be canceled in hide case. One case that isn't synchronous is when a popover is showing in the DOM and then that element is removed from the DOM, previously resolved that we would keep firing the event synchronously - last sticking point on the spec PR. question is - is that decision the right one? Should we fire any events?<br>
&lt;una> Should it be async? Are there demos that show this particular case and could help us understand the problems with the current behavior<br>
&lt;una> masonf: this is a very corner case, don't want to overcomplicate the API to make this one corner case easier<br>
&lt;una> masonf: trying to understand how corner of a case this is<br>
&lt;JonathanNeal> I’m not joking about having a passive removal listener.<br>
&lt;una> masonf: my pushback has been that you can with existing APIs handle this situation, it just takes more work<br>
&lt;una> masonf: i.e. mutationObserver or the existing event<br>
&lt;JonathanNeal> q?<br>
&lt;una> JonathanNeal: when i'm designing something, i might want to account for a DOM removal event separately<br>
&lt;una> JonathanNeal: a usecase is a library that adds and removes things from the DOM. Has other side effects, i.e. pausing videos<br>
&lt;una> JonathanNeal: an option being a removal listener shouldnt be thrown out<br>
&lt;una> JonathanNeal: I have to know the container, wheras a popup listener for open/close is on the element<br>
&lt;una> JonathanNeal: I want a way to listen to that element's removal and then the issue of how things fire become more moot<br>
&lt;una> masonf: you think its very important to keep firing the event is one point you're making<br>
&lt;una> masonf: that is against the case of not firing the event<br>
&lt;una> JonathanNeal: no i agree with that<br>
&lt;una> JonathanNeal: i add an eventlistener when the popup is open. if i want to handle if the popup is removed i would add a similar api<br>
&lt;una> masonf: you can use a mutationObserver on the parent element<br>
&lt;una> JonathanNeal: i've tried this in many situations, and there's something very disconecting bw switching bw an event listener on the elem itself to a mutation observer<br>
&lt;una> JonathanNeal: I would agree with your original point that it didn't need to fire if i had another way to listen to it<br>
&lt;una> masonf: maybe there are 2 events here<br>
&lt;una> masonf: it sounds like you're asking for a more general improvement to mutationobserver that watches for its removal<br>
&lt;una> JonathanNeal: when i might remove something in my experience, it felt if i had this even ti wouldnt be trying to create a popup remove event<br>
&lt;una> JonathanNeal: this one would be passive<br>
&lt;una> JonathanNeal: i have to change my paradigm of thinking for this one use case<br>
&lt;JonathanNeal> Sorry for repeating myself so much. Thank you for listening!<br>
&lt;una> dbaron: one other thought about this set of problems is the risk of getting some of these events out of order when some are sync and some are async<br>
&lt;una> dbaron: one thing that makes this much less scary is to be able to look at the state and see if the popup is open or closed<br>
&lt;una> masonf: you can do this with a matches call<br>
&lt;una> masonf: talked about making an attribute to add some syntactic sugar<br>
&lt;una> dbaron: this seems good enough<br>
&lt;dbaron> (I don't have a strong opinion)<br>
&lt;una> masonf: mental model i've had is that this is a corner case, but there needs to be a way to detect that this has happened (that you've disconnected and are no longer showing), the current ways are cumbersome but there is a way<br>
&lt;una> masonf: if there are further comments, please put them on #﻿578 - this is the main blocker to the spec<br>
</details>

<!-- comment id=1374678258 author=mfreed7 created=2023-01-08T01:14:22Z url=https://github.com/openui/open-ui/issues/578#issuecomment-1374678258 -->
## @mfreed7 — 2023-01-08

FYI @jonathantneal and others, @domenic pointed out [this feature request](https://github.com/whatwg/dom/issues/533), from 2017, for exactly the Mutation Observer addition you requested. I.e. the ability to observe removals on the element itself. Perhaps you'd like to comment there? It doesn't seem like a big implementation burden, but perhaps I'm missing something.
