---
number: 471
title: "[Popup] We need a JS method to open the popup"
state: "closed"
labels: ["popover"]
author: "mfreed7"
created_at: "2022-02-24T20:46:26Z"
closed_at: "2022-03-24T18:51:44Z"
url: "https://github.com/openui/open-ui/issues/471"
comments: 2
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

Given the [new approach for popup](https://github.com/openui/open-ui/issues/455), and the associated [resolution](https://github.com/openui/open-ui/issues/311#issuecomment-1050192589) not to have a "live" `open` content attribute, there needs to be a way to open/reopen the popup via Javascript. The most straightforward would seem to be something like:

```javascript
const myPopup = document.querySelector('div[popup]');
myPopup.show(); // or
myPopup.showPopup();
```

That would require adding a `show()` or `showPopup()` IDL method to `HTMLElement`. And then the question becomes: what happens if this is called on an element that lacks the `popup` attribute? Or for which the value of the `popup` attribute is invalid? I think it likely should do nothing, but it could also throw, and it could also add a valid `popup` attribute to the element. The last one feels odd, but I suppose it's an option.

<!-- comment id=1050301944 author=mfreed7 created=2022-02-24T21:53:22Z url=https://github.com/openui/open-ui/issues/471#issuecomment-1050301944 -->
## @mfreed7 — 2022-02-24

I should have added, we also need a corresponding way to close the popup from Javascript:

```javascript
myPopup.hide(); // or
myPopup.hidePopup();
```

That one likely should not throw and should not change any attributes. It should just be a no-op if the element isn't currently a top layer popup.

<!-- comment id=1077942265 author=mfreed7 created=2022-03-24T18:51:44Z url=https://github.com/openui/open-ui/issues/471#issuecomment-1077942265 -->
## @mfreed7 — 2022-03-24

This has been added to the explainer, [here](https://open-ui.org/components/popup.research.explainer).

```javascript
const popup = document.querySelector('[popup]');
popup.showPopup(); // Show the popup
popup.hidePopup(); // Hide a visible popup
```
