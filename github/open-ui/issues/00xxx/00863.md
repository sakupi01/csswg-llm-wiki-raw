---
number: 863
title: "selectlist: Should the \"checked\" option have a checkmark next to it?"
state: "closed"
labels: ["select"]
author: "josepharhar"
created_at: "2023-10-05T19:01:07Z"
closed_at: "2024-05-29T15:58:26Z"
url: "https://github.com/openui/open-ui/issues/863"
comments: 37
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

I touched on this while discussing https://github.com/openui/open-ui/issues/827

In order to indicate the currently selected option inside the listbox (I supposed it should also already be indicated in the button), should the currently selected/checked option in a single select selectlist have a checkbox rendered next to it?

For multi select, we would of course need this. For single select, maybe not?

The MacOS native select picker has a checkmark to the left of the selected option and also renders on top of the button which would otherwise indicate what the selected option is. The aria practices single select example has a checkmark to the right of the option: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-select-only/

If we did this, how would it be implemented? Just a user-agent style thats like `option:checked::after { content: '✓' }`? Or perhaps a new pseudo-element like `::checkmark`? Should it be an SVG? Or be some sort of native checkbox thing? Should it be to the right of the option like the aria-practices example? Or should it be on the left? If it's on the left, should there be padding there for every option?

Whatever the case is, I feel like this should be easy for the developer to override/replace/remove, right @una? Would the `content` property in `option:checked::after { content: '✓' }` be easy enough to override? Should there be an easy way to move it from the left side of the option to the right side or vice versa?

@scottaohara @smhigley 

<!-- comment id=1749495626 author=lukewarlow created=2023-10-05T19:11:42Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1749495626 -->
## @lukewarlow — 2023-10-05

My gut feeling on this is don't bother providing a check by default at all. Instead just provide UA styles of the following (or something similar). This way the default is usable but it's ultimately up to the authors to make it nicer. Because I feel like adding a checkmark is just going to create more work for ourselves making a good way to style it (which will mostly just be removing it)?

```css
option:checked {
  color: selecteditemtext;
  background-color: selecteditem;
}
```

Rendered:

![image](https://github.com/openui/open-ui/assets/32498324/580173f4-736d-417a-9d7c-683f4b0b7fce)

<!-- comment id=1749513791 author=lukewarlow created=2023-10-05T19:26:04Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1749513791 -->
## @lukewarlow — 2023-10-05

Happy to make a new issue for this but seeing as it's slightly related.

```css
selectlist {
  accent-color: blue;
}
```

Does not seem to impact on any of the default UA styles (e.g hover state) 

Should it?

<!-- comment id=1749694417 author=josepharhar created=2023-10-05T21:43:40Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1749694417 -->
## @josepharhar — 2023-10-05

> adding a checkmark is just going to create more work for ourselves making a good way to style it

Agreed, in the OP I almost said "I have so many questions that I don't feel like this can be completed before we finish single select and move on to multi select".
If this isn't absolutely necessary from an accessibility point of view, then maybe we could save this for multi select.

> Does not seem to impact on any of the default UA styles (e.g hover state)

Are these things already stylable via other CSS properties?
I created accent-color so that you could change the color of fully native form control elements like `<input type=checkbox>` which cannot be styled any other way.

<!-- comment id=1749697193 author=lukewarlow created=2023-10-05T21:45:51Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1749697193 -->
## @lukewarlow — 2023-10-05

Yeah they can be styled by doing `option:hovered` `option:focus-visible` etc so it's not big deal. Just might be something that comes up from people who do 

```css
* {
   accent-color: var(--my-very-execellent-color);
}
```

<!-- comment id=1749825505 author=scottaohara created=2023-10-06T00:21:36Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1749825505 -->
## @scottaohara — 2023-10-06

so the pro of a checkmark marker for the default selected state is that it provides an overt visual "YOU CHOSE THIS" confirmation without having to have a strong (dark) default selected option background color - which if the decision ends up being to not have selection follow focus, we need to then have a strong focus indicator vs the strong background color.  The checkmark loosens that a bit so the focus indicator could be a strong background color and then the selected state could be exactly the same as the unselected state - save for the 'checkmark' that appears.

the con then being that if no one wants that checkmark / if conflicts with people being able to identify a multiselect vs single select popup, then there's little in the way of default UA styling to fall back on for authors who just didn't want that default checkmark style - and they have to roll their own selected state and focus state.

keep in mind, that we do also need to consider how these selected vs unselected styles need to translate to windows high contrast mode - and since so many colors are kicked to the curb, a checkmark becomes another easy win for denoting the selected state vs the unselected state in that limited coloring scenario.

the above may make it seem like i'm advocating for the checkmark, but i'm personally a bit indifferent about it.  it's just sorta factually beneficial to have it as a default UA style for ticking as many a11y boxes as possible for the default.  These boxes can be ticked in other ways, it just means different things visual treatments need to be implemented/considered.

also i did not mean to make any sort of pun with talking about checkmarks and 'ticking boxes'  and yet here we are.  please clap.

<!-- comment id=1751442441 author=lukewarlow created=2023-10-06T21:54:51Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1751442441 -->
## @lukewarlow — 2023-10-06

One potential issue with the checkmark is if you copy the option to `<selectedoption>` is it gonna copy with the checkmark?

At least on macOS this isn't the behaviour of select nor would it be what I expect?

<!-- comment id=1751513879 author=scottaohara created=2023-10-07T00:02:26Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1751513879 -->
## @scottaohara — 2023-10-07

not if it's a css marker specific to the option element and not selectedoption

<!-- comment id=1760204100 author=css-meeting-bot created=2023-10-12T18:54:34Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760204100 -->
## @css-meeting-bot — 2023-10-12

The Open UI Community Group just discussed `selectlist: Should the "checked" option have a checkmark next to it?`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> jarhar: I wonder if we should show a checkmark next to items in a selectlist<br>
&lt;luke> q+<br>
&lt;hdv> jarhar: and if so there are lots of questions to be asked<br>
&lt;hdv> jarhar: scott had one comment: having a checkmark is very useful, a great way to indicate the current selection, but if we can find another way to do that, that would be fine too<br>
&lt;hdv> jarhar: some kind of styling that also works in high contrast mode, that would also be fine<br>
&lt;hdv> jarhar: not sure what that would be<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> jarhar: this is probably also something to figure out for multiselect… a checkmark could probably work in select and multiselect btoh<br>
&lt;hdv> s/btoh/both<br>
&lt;hdv> jarhar: I welcome suggestions<br>
&lt;una> q?<br>
&lt;gregwhitworth> ack luke<br>
&lt;una> q+<br>
&lt;hdv> luke: I thought about it… one of those things where it is easy in CSS to apply and unapply styles… I think most people probably would throw away what we do… not sure if adding a checkmark is much value<br>
&lt;scotto_> q+<br>
&lt;hdv> luke: a background style may be easy enough, and then we don't need to worry about SVG vs pseudo el etc<br>
&lt;keithamus> q+<br>
&lt;hdv> luke: maybe it would need slightly different styling for focus<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> gregwhitworth: I don't disagree…we can probably do a forced color scheme mq to swap out the colours<br>
&lt;hdv> gregwhitworth: agreed we probably don't want checkmarks out of the box<br>
&lt;gregwhitworth> ack una<br>
&lt;nicole> q+<br>
&lt;hdv> una: my first instinct was also… developers want to customise this, is a large part of the goal of this<br>
&lt;hdv> una: currently select has a checkmark though on Mac, and if we want to stay uniform that seems good<br>
&lt;hdv> una: I would argue against using background as a way to distinguish, could have unexpected results<br>
&lt;hdv> una: similarly to having to undo the checkmark, having the undo the background isn't great<br>
&lt;hdv> una: the more I work on selectlist demos though, the more I want the ability to strip the native UI so you can do your own thing<br>
&lt;hdv> una: regardless of our decision here, having a way to easily undo UA styles would be great<br>
&lt;gregwhitworth> ack scotto_<br>
&lt;hdv> scotto_: my comment may become a new issue…<br>
&lt;hdv> scotto_: I agree that we shouldn't not do it because we think people are going to remove it… we can have strong defaults that are easy to remove<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;hdv> keithamus: can we use accent color instead of background… and a ::marker ?<br>
&lt;gregwhitworth> q+ argyle<br>
&lt;hdv> [+1 to keithamus's suggestion!]<br>
&lt;gregwhitworth> ack nicole<br>
&lt;hdv> masonf: that works… but per the spec you can't mandate where accent colour goes (but can recommend it instead)<br>
&lt;hdv> nicole: I really like what keithamus just suggesed<br>
&lt;scotto_> to clarify further / repeat a bit from the issue.  i don't have strong opinions on the default selected state.  mac has the checkmark, windows doesn't.  eh?<br>
&lt;hdv> s/suggesed/suggested<br>
&lt;hdv> nicole: we need to realise that styles are often where we solve user problems where developers might not know they exist… they may not test it on small screens or screenreaders<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> nicole: so with default styles we can make sure it works better for usrrs<br>
&lt;hdv> s/usrrs/users<br>
&lt;hdv> nicole: don't want to avoid shipping styles because they'd be hard to overwrite<br>
&lt;luke> q+<br>
&lt;gregwhitworth> nicole just covered my point<br>
&lt;hdv> nicole: I think re what una suggested… we should have a conversation about user agent styles and the ability to strip them out. Probably not for here now but we should probably talk about it in some venue<br>
&lt;gregwhitworth> ack argyle<br>
&lt;hdv> argyle: +1 to that<br>
&lt;hdv> argyle: in my own code I had accent color for the checked option and use my own pseudo<br>
&lt;gregwhitworth> ack luke<br>
&lt;hdv> luke: I agree with nicole that the UA styles should cover all user needs<br>
&lt;gregwhitworth> q+<br>
&lt;nicole> q+<br>
&lt;hdv> luke: I think background is clearer as a way to distinguish than a checkmark<br>
&lt;hdv> luke: it could work so that it was using the ::marker pseudo that would be easy to display:none<br>
&lt;hdv> luke:  in terms of background color… I think a new system keyboard is being used on hover and focus styles<br>
&lt;hdv> luke: in forced color mode, the browser could swap them out<br>
&lt;hdv> luke: one down side for accent color is that a system color could not be easily swapped out<br>
&lt;masonf> Note on "stripping styles", this is discussed in https://github.com/w3c/csswg-drafts/issues/5998, and has been brought back up recently here: https://github.com/whatwg/html/pull/9546#issuecomment-1741629198<br>
&lt;gregwhitworth> q+ argyle<br>
&lt;hdv> luke: in webkit, accentcolor is 'fake' and set to blue<br>
&lt;nicole> q-<br>
&lt;hdv> gregwhitworth: there may be two resolutions here… one about should there be a checkmark<br>
&lt;una> q+<br>
&lt;hdv> gregwhitworth: the other about what are the styles and what are the implementation details of that selected state<br>
&lt;hdv> gregwhitworth: for that second one I would like to see a concrete proposal in the comment<br>
&lt;Cat_Johnson> q+<br>
&lt;hdv> gregwhitworth: seems like we're going into separate discussion<br>
&lt;hdv> masonf: to quickly answer that one, I'd like to answer them together because they are related in implementation<br>
&lt;hdv> gregwhitworth: I was asking from an end user perspective if the checkmark is bad to indicate rather than just colours in some form<br>
&lt;hdv> nicole: that's in WCAG, that you can't indicate with color alone<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> scotto_: I'm not super wedded to checkmark, but I brought it up because it's easier to recognise an icon than it is to recognise a change of colour<br>
&lt;keithamus> WCAG 1.4.1 https://www.w3.org/WAI/WCAG21/Understanding/use-of-color.html "Color is not used as the only visual means of conveying information"<br>
&lt;hdv> scotto_: I don't want to tell anyone they have to make a certain design, just whatever they want, it's radios or other icons in some platforms<br>
&lt;hdv> nicole: when we say checkmark we don't mean checkmark, right?<br>
&lt;hdv> scotto_: with the marker element, any UA could put whatever they want and developers could style it however they want too<br>
&lt;hdv> gregwhitworth: if we can't simplify the answer to checkbox, I'd want to see the implementation details and have it align between select and multiselect<br>
&lt;gregwhitworth> ack argyle<br>
&lt;hdv> argyle: I have a select multiple up on my screen here… multiple on mac doesn't show a checkmark currently, surprises me<br>
&lt;jarhar> select multiple on ios has nice checkmarks, and importantly is has empty circles for non-checked items<br>
&lt;gregwhitworth> Android does show them as radio options and fills in the circle on the selected option<br>
&lt;hdv> argyle: why don't we do it just like we would render a group of checkboxes, we already have accent colours there, ie let's follow the path that's already there<br>
&lt;hdv> scotto_: we would have to invent that though… visually looks like that but isn't actually what it is<br>
&lt;gregwhitworth> ack una<br>
&lt;flackr> q+<br>
&lt;hdv> scotto_: I do agree  but we would need some convention<br>
&lt;scotto_> 'invention'<br>
&lt;hdv> una: I think the analogy of checkboxes inside of a popover is a bit like a :checked rfor a select<br>
&lt;hdv> s/convention/invention<br>
&lt;scotto_> :)<br>
&lt;nicole> q+<br>
&lt;hdv> una: it feels like ::marker is the most straightforward implementation of this feature… easiest to set and easy to remove. But tricky to get fill of the svg to work, probably need to restyle the ::marker<br>
&lt;masonf> q+<br>
&lt;hdv> gregwhitworth: such a can of worms<br>
&lt;luke> q+<br>
&lt;scotto_> to clarify: when i said someone would put in an SVG, i assumed they would actually add it in their markup.  not via CSS<br>
&lt;hdv> argyle: as a user I like how checkboxes and selects take the accent color and I would like to see that here<br>
&lt;gregwhitworth> ack Cat_Johnson<br>
&lt;hdv> Cat_Johnson: gregwhitworth, you mentioned whether this should be enabled by default or not… my personal preference would be to enable by default<br>
&lt;jarhar> q+<br>
&lt;hdv> Cat_Johnson: in our design system, when other developers use our components, we enable things when we can, and make it easy to finetune the experience for developers if they want to<br>
&lt;gregwhitworth> ack flackr<br>
&lt;hdv> flackr: on Mac I see they do the background color thing, but as you hover over items it does change<br>
&lt;gregwhitworth> ack nicole<br>
&lt;hdv> flackr: talking about multiselect, I wanted to let people know, there are weird behaviours like keyboard ways to select 'items in between', something you couldn't do with a group of checkboxes, so it is different in ways like that<br>
&lt;hdv> nicole: developers are going to want to customise this… a solution that doesn't allow them to do that doesn't make sense<br>
&lt;hdv> nicole: like make icons to match their own icon libraries… so people will take this and turn it into their own things<br>
&lt;hdv> nicole: displaying a checkmark by default makes sense but we need to do it in a way where a lot of flexibility is allowed via CSS<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> flackr: I like it, similar to list item markers where you can replace those<br>
&lt;gregwhitworth> q+ to wind this issue down<br>
&lt;hdv> masonf: re flackr's point :we need to states, the selected item and the focused item<br>
&lt;hdv> s/to/two<br>
&lt;hdv> nicole: and a third, not selected?<br>
&lt;hdv> masonf: yes<br>
&lt;hdv> masonf: I see three ways forward: one is not support checkmarks, two is support via checked pseudo element, three is via some fully customisable SVG element<br>
&lt;gregwhitworth> ack luke<br>
&lt;hdv> masonf: we could try out the checked pseudo element route… I think it would work in 80% of cases and not be so easy in 20% … want to see how hard it is in those 20% of cases<br>
&lt;hdv> luke: I feel like ::marker is probably fine<br>
&lt;hdv> luke: in Chromium you can change the color of things like the calendar icon… can't remember how but you can<br>
&lt;hdv> luke: selectlist is already fully customisable… if you want an option with your fancy svg in it, you can already do it anyway<br>
&lt;hdv> luke: I don't think we need a way to replace the glyph because you could just display none the thing and do whatever you like to replace it<br>
&lt;hdv> luke: my proposal would be to use the ::marker pseudo, make it so that display:none makes it go away<br>
&lt;hdv> masonf: I meant to propose the same<br>
&lt;una> +1<br>
&lt;hdv> masonf: in developer guidance we could say you can change it like this, and you can fully replace it like this<br>
&lt;masonf> q?<br>
&lt;hdv> flackr: I heard a few times selection doesn't follow focus… it does seem to do that in some places, seems like that's on platforms that don't have checkbox<br>
&lt;gregwhitworth> ack jarhar<br>
&lt;hdv> scotto_: yes this is inconsistent between operating systems<br>
&lt;hdv> jarhar: I think I generally agree with Mason and Luke, and we need some time to investigate diffferent ideas<br>
&lt;una> q?<br>
&lt;hdv> jarhar: we should probably have people comment more on the issue with use cases<br>
&lt;hdv> gregwhitworth: I feel like we're all arriving at… solid proposal put forward, but maybe are use cases that are not covered<br>
&lt;hdv> gregwhitworth: jarhar can you do a proposal and request feedback from folks in the isuse?<br>
&lt;hdv> gregwhitworth: so that people can share use cases?<br>
&lt;hdv> gregwhitworth: jarhar or masonf ?\<br>
</details>

<!-- comment id=1760210820 author=lukewarlow created=2023-10-12T18:59:38Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760210820 -->
## @lukewarlow — 2023-10-12

https://jsfiddle.net/cdygvmeb/23/ - Excuse the awful CSS but this kinda shows my thoughts on hey I want custom markers.

Just display none the marker pseudo put your SVG in the option and control it based on `:checked` pseudo class.

<!-- comment id=1760226526 author=flackr created=2023-10-12T19:12:19Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760226526 -->
## @flackr — 2023-10-12

I just discovered that the existing `::marker` pseudo has a really limited set of properties: https://developer.mozilla.org/en-US/docs/Web/CSS/::marker#allowable_properties . I suspect some developers would want to animate transform / opacity

For a more extreme animation example, have a look at the material checkbox animation: https://material-components.github.io/material-components-web-catalog/#/component/checkbox . This would probably require a custom component.

<!-- comment id=1760231929 author=lukewarlow created=2023-10-12T19:16:17Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760231929 -->
## @lukewarlow — 2023-10-12

My gut is that that's probably fine? As they can just do what I mentioned above and get the full capabilities? The reason checkboxes specifically are such a pain is exactly because they don't have full HTML abilities?

It's also worth being aware I think there's dicussion on making :marker less restricted for the details styleability but I may be mistaken with that.

<!-- comment id=1760320346 author=mfreed7 created=2023-10-12T20:26:47Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760320346 -->
## @mfreed7 — 2023-10-12

> My gut is that that's probably fine? As they can just do what I mentioned above and get the full capabilities? The reason checkboxes specifically are such a pain is exactly because they don't have full HTML abilities?

+1. It seems like the basic use case is "just" to have a visible checkmark next to the selected option. That's all you get currently on Mac platforms:
 
![Mac_select](https://github.com/openui/open-ui/assets/29559695/7205befd-f901-4444-9f6e-daed5a06a5b2)

That's not a "checkbox" at all, it's just a "checkmark" which can be achieved (assuming we go this route) via

```css
selectlist option:checked::marker {
  content: "✓"
}
```

I think that achieves 80+% of the use cases. You can pick your own marker, you can style it appropriately, etc.

For the 20% of use cases where you want more control, you're more on your own. But you can, e.g. use SVGs instead of just `content` [via Luke's approach](https://jsfiddle.net/cdygvmeb/23/). Or if you want full interactive `<input type=checkbox>`'s inside the options, **first, you're running afoul of https://github.com/openui/open-ui/issues/540#issuecomment-1212330487**. But if you manage to make that accessible somehow, you can use a similar technique of adding the checkboxes to your options, and hooking them up correctly via JS.

<!-- comment id=1760423636 author=scottaohara created=2023-10-12T21:57:14Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1760423636 -->
## @scottaohara — 2023-10-12

Yeah. Whatever can be done to make people NOT put a checkbox inside of something that already represents means of selecting a choice would be super great. This sort of misunderstanding happens far too often. 

<!-- comment id=1761610373 author=flackr created=2023-10-13T14:24:08Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1761610373 -->
## @flackr — 2023-10-13

@scottaohara @mfreed7, to be clear I wasn't suggesting it is a checkbox or should have a checkbox put in it, but I could imagine developers wanting similar animations to the material checkbox checkmark animation, especially for the multi-select use case.

<!-- comment id=1761654292 author=scottaohara created=2023-10-13T14:53:36Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1761654292 -->
## @scottaohara — 2023-10-13

it was clear, sorry if my response made you think otherwise!

<!-- comment id=1761797162 author=mfreed7 created=2023-10-13T16:34:54Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1761797162 -->
## @mfreed7 — 2023-10-13

> @scottaohara @mfreed7, to be clear I wasn't suggesting it is a checkbox or should have a checkbox put in it, but I could imagine developers wanting similar animations to the material checkbox checkmark animation, especially for the multi-select use case.

Yep understood! I just wanted to carefully make the distinction between checkmark and checkbox, since in the live discussion it felt a bit muddled. Maybe just to me.

<!-- comment id=1761820200 author=scottaohara created=2023-10-13T16:54:25Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1761820200 -->
## @scottaohara — 2023-10-13

![fluent-example drop down that has selected state indicated by use of a 'checkbox' rather than just a 'checkmark'](https://github.com/openui/open-ui/assets/4152514/10d8d476-2f63-4b86-b74f-6124f68d012b)

when design treats checkmark and checkbox the same, it gets super muddled.

that screenshot is from fluent ui - where in the past that widget was incorrectly coded as checkboxes inside of options because "they look like checkboxes".  it has since been fixed so that these are just visual checkboxes to represent the selected state of the option, but i get questions on a bi-weekly basis if "this is ok" because it "looks like" a checkbox but doesnt behave like one.

<!-- comment id=1761824425 author=flackr created=2023-10-13T16:57:59Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1761824425 -->
## @flackr — 2023-10-13

Right, agreed! Thanks for the example. I think this is good reason to make it easy to make something that visually looks like a checkbox as the marker so that there's an easy path which is semantically correct 🙂

<!-- comment id=1765107097 author=josepharhar created=2023-10-16T19:02:38Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1765107097 -->
## @josepharhar — 2023-10-16

So should we just have a user-agent style rule that looks like this? The `visibility:hidden` would add padding so the actual content of the options is still rendered at the same horizontal alignment.
```css
selectlist option::marker {
  content: '✓';
}
selectlist option:not(:checked)::marker {
  visibility: hidden;
}
```
We could also use an svg or something instead of `content`, but that would be harder to add styling to - which might be good or bad, I'm not sure yet. It depends on whether we want to encourage people to `display:none` the `::marker` in order to style it or not. The styling of this I think is related to https://github.com/openui/open-ui/issues/881

Also, this rule would probably not apply to options which are slotted in from outside the selectlist, which is something we probably want to support: https://github.com/openui/open-ui/issues/565

<!-- comment id=1769004174 author=scottaohara created=2023-10-18T17:22:38Z url=https://github.com/openui/open-ui/issues/863#issuecomment-1769004174 -->
## @scottaohara — 2023-10-18

i like the visibility: hidden idea for the consistent padding

definitely would want that default UA style for the checkmark to be the following so we don't get an announcement of a "checkmark" along with the selected state.

```
selectlist option::marker {
  content: '✓' / '';
}
```

<!-- comment id=2071175112 author=github-actions[bot] created=2024-04-23T00:35:16Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2071175112 -->
## @github-actions[bot] — 2024-04-23

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2093446213 author=josepharhar created=2024-05-03T17:18:45Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2093446213 -->
## @josepharhar — 2024-05-03

I threw together a [work in progress patch](https://chromium-review.googlesource.com/c/chromium/src/+/5514970) to add checkmarks next to selected options by adding this to the UA stylesheet:
```css
select option::before {
  content: '';
  display: inline-block;
  inline-size: 1.4em;
  block-size: 1.0em;
  background-image: url(data:image/svg+xml,%3Csvg%20width%3D%2220%22%20height%3D%2220%22%20fill%3D%22none%22%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%3E%3Cpath%20d%3D%22m8.229%2014.062-3.521-3.541L5.75%209.479l2.479%202.459%206.021-6L15.292%207l-7.063%207.062Z%22%20fill%3D%22%23000%22%2F%3E%3C%2Fsvg%3E);
  background-origin: content-box;
  background-repeat: no-repeat;
  background-size: cover;
}
select option:not(:checked)::before {
  visibility: hidden;
}
```
Here is a screenshot of what it looks like:
![Screenshot 2024-05-03 at 9 38 47 AM](https://github.com/openui/open-ui/assets/7681467/dba6fe56-5cad-40db-8d3f-852a6162bf4c)
You can opt-out of it with `option::before { display:none; }`.


I feel like using `::before` won't make it through the standards and maybe we would have to switch to `::marker` or create a new pseudo-element, but is this what yall would like? Would having this by default get in the way of anything? Are there any improvements to the style rules I wrote that yall can think of?

<!-- comment id=2093495484 author=scottaohara created=2024-05-03T17:52:08Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2093495484 -->
## @scottaohara — 2024-05-03

the downside to using a graphic as the default is that it becomes more difficult to make sure this will stay visible if someone turns on a dark high contrast theme.  If it was a unicode character, it could just automatically update to the default color for whatever high contrast theme was chosen.  Similarly, if someone just wanted to make the dropdown have a dark background (a dark mode, or just a dark ui website in general) they'd _have_ to replace the graphic - instead of just doing something like `color: #fff`.

again, not against the graphic approach - but it will require more default work (for browser implementation and author overwriting).

<!-- comment id=2093519341 author=josepharhar created=2024-05-03T18:10:16Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2093519341 -->
## @josepharhar — 2024-05-03

I figure we should match the down arrow in the default button and use an svg, and I think that using a unicode character might have other issues. We can use light-dark() to make the checkmark white instead of black, and I believe there is a separate stylesheet for high contrast mode where we can also choose white or black.

> if someone just wanted to make the dropdown have a dark background (a dark mode, or just a dark ui website in general) they'd have to replace the graphic - instead of just doing something like color: #fff.

I see... it would be cool if we could make the svg know what color its painted on or what its computed background-color is and respond accordingly, but I don't know if that svg technology exists.

<!-- comment id=2099668687 author=gregwhitworth created=2024-05-08T03:25:19Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2099668687 -->
## @gregwhitworth — 2024-05-08

Note that this is why I noted when I proposed a new need for a `checkbox` element it was due to none of these solutions actually meeting the needs devs have for simplistic style adjustments. I cover this quite in-depth in this older proposal for an indicator pseudo:

https://github.com/salesforce/standards-explainers/blob/master/indicator-psuedo/explainer.md#other-solutions-considered--faq

Effectively, to @josepharhar point if a UA wants to have them in the non-standardized scenario and handle the a11y that comes with that (whether it being swapping sheets, etc) then that is fine but I don't think we should do this in the default styles for select. So possibly we raise this to the agenda to resolve on whether we want that as a default style or not?

<!-- comment id=2100211962 author=lukewarlow created=2024-05-08T09:58:19Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2100211962 -->
## @lukewarlow — 2024-05-08

I'd much prefer something like https://github.com/openui/open-ui/issues/863#issuecomment-1765107097 (along with using the correct alt syntax with content attribute)

The fonts might change how it's rendered but I think that's probably fine. base appearance is mostly about stylability rather than everything being 100% identical (though we should aim to get as close to that as possible). But it's much much easier for styling. It just works with the color property. forced-colors (Windows' Contrast themes) mode can choose to just remove it entirely should it wish too.

<!-- comment id=2101424212 author=josepharhar created=2024-05-08T20:52:05Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2101424212 -->
## @josepharhar — 2024-05-08

> I cover this quite in-depth in this older proposal for an indicator pseudo

Very nice! Yeah this has a lot of similarities with the checkbox we are talking about here

>  I don't think we should do this in the default styles for select

Why not? Your indicator proposal seemed to suggest that we should in fact use an svg instead of a unicode character.

> The fonts might change how it's rendered but I think that's probably fine. base appearance is mostly about stylability rather than everything being 100% identical (though we should aim to get as close to that as possible). But it's much much easier for styling. It just works with the color property. forced-colors (Windows' Contrast themes) mode can choose to just remove it entirely should it wish too.

If the unicode character is more stylable then that's a good reason. I'm not sure if I could replace the svg background-url with a full on svg element in the dom unless I add it to the option element's UA shadowroot, which currently doesn't contain anything and just slots in all content.

<!-- comment id=2103133602 author=una created=2024-05-09T17:43:47Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2103133602 -->
## @una — 2024-05-09

For single select, I don't think the checked option should have a checkmark next to it by default.

1. This adds complexity in terms of design and UA style standardization
2. While this is a relatively common UI, it's not in the majority of selects. In my research, I've found one in about 20-25% of custom selects. This means users will need to "undo" the style frequently
4. It's easy to add the checkmark with custom user styles (see all of the suggestions above)

My question is -- is it an accessibility issue to add this checkbox via a background image and pseudo element content? This is how I imagine most would do it

<!-- comment id=2103160295 author=mfreed7 created=2024-05-09T18:00:59Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2103160295 -->
## @mfreed7 — 2024-05-09

I'm actually in favor of adding the checkmark, via `::marker` or `content:'✔️'`. I've heard several times (would be great to get confirmation here) that it's an accessibility issue to only distinguish the selected vs. unselected options via color change. Having an explicit checkmark helps people who can't distinguish the colors. If that's true, I'd rather it be (just a bit!) of work for developers to remove the checkmark if they don't want it.

As a side-benefit, if we get this styling right, it'll feed naturally into the multi-select of the future, which **needs** checkmarks.

<!-- comment id=2103169676 author=josepharhar created=2024-05-09T18:07:27Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2103169676 -->
## @josepharhar — 2024-05-09

> This adds complexity in terms of design and UA style standardization

Yeah it will be hard to standardize this, but it will also be hard to standardize the other hundreds of lines of css.

> While this is a relatively common UI, it's not in the majority of selects. In my research, I've found one in about 20-25% of custom selects. This means users will need to "undo" the style frequently

>  I'd rather it be (just a bit!) of work for developers to remove the checkmark if they don't want it.

Yeah I'm hoping that it will just be `option::bikeshed { display:none; }` which would be pretty simple to turn off the checkmark

<!-- comment id=2103244160 author=css-meeting-bot created=2024-05-09T18:59:44Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2103244160 -->
## @css-meeting-bot — 2024-05-09

The Open UI Community Group just discussed `selectlist: Should the "checked" option have a checkmark next to it?`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> jarhar: there's an idea floating around that we need a visual indicator that is not just color on the currently selected option of selects<br>
&lt;hdv> jarhar: the native picker in macOS already does this<br>
&lt;hdv> jarhar: we could use ::before or other pseudos to put a checkmark there, I thought of using an svg to do that<br>
&lt;gregwhitworth> q?<br>
&lt;hdv> jarhar: sofar it seems most people in favour of doing a unicode checkmark<br>
&lt;una> q+<br>
&lt;masonf> q+<br>
&lt;sanketj_> q+<br>
&lt;Luke> q+<br>
&lt;hdv> jarhar: not sure if we want to resolve the method, but we could talk about what approach people prefer?<br>
&lt;gregwhitworth> ack una<br>
&lt;gregwhitworth> q+ naman<br>
&lt;hdv> una: thanks for bringing it up! I think this should not be part of default styling… it is very easy for developers to add this, including the two methods you've just mentioned<br>
&lt;hdv> una: second, I don't see this in many selects, looking at the Open UI research. Probably like 20%. It's out there but it's not most common<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> una: third: this will also be hard to spec for user agents, because icons are usually branded and we would probably end up in discussions about things liek spacing and more<br>
&lt;hdv> una: I think the iOS browsr is the only one that does it now<br>
&lt;hdv> una: would probably be easier to resolve on 'this is a developer addon' rather than adding it into the  default UA stylesheet  as something developers need to undo<br>
&lt;gregwhitworth> ack masonf<br>
&lt;brad_frost> q+<br>
&lt;bkardell_> on MacOS Safari there is also a checkmark<br>
&lt;brad_frost> (Am I doing that right?)<br>
&lt;hdv> masonf: I'd love confirmation… all of my opinions are based on what I heard from accessibility folks, that we need to have a way to show the current option that is not just colours<br>
&lt;scotto> q+<br>
&lt;hdv> masonf: can someone say I'm wrong if I'm wrong… but otherwise I think the accessible thing should be the thing we do by default<br>
&lt;keithamus> > SC 1.4.1:Use of Color (Level A)<br>
&lt;keithamus> > Color is not used as the only visual means of conveying information, indicating an action, prompting a response, or distinguishing a visual element.<br>
&lt;una> q?<br>
&lt;hdv> masonf: if agreeing on icon etc is difficult, I think we should make it a unicode character, there is a checkmark character, which is interoperable and understandable and would be easy to remove<br>
&lt;hdv> masonf: we are going to work on select multiple. I think everyone will agree that one needs checkmarks? so this would be a natural part<br>
&lt;hdv> una: I don't think an icon the only way to make it accessible… there could be other visible changes<br>
&lt;hdv> una: also would adding a unicode character cause it to be read out in addition to the already available selected state of the option?<br>
&lt;gregwhitworth> ack sanketj_<br>
&lt;hdv> sanketj_: una largely covered what I wanted to say<br>
&lt;hdv> sanketj_: the checkmark pattern seems to be only on specific platform. I agree with the premise that we should make these components accessible by default, but not sure this is the way<br>
&lt;hdv> sanketj_: if this is not used much in selects, I haven't seen it too much in my research to custom selects on the web, then probably opt-in  makes more sense<br>
&lt;gregwhitworth> ack Luke<br>
&lt;hdv> Luke: I don't think we should use existing customisable selects as the pinnacle of what accessible selects should look like, they generally have problems for keyboard users, sighted users, screenreaders users et<br>
&lt;hdv> Luke: but agree using colour alone isn't good enough so we need something<br>
&lt;hdv> Luke: I think disabling the marker would be quite trivial to do in CSS<br>
&lt;hdv> Luke: content does have a way that isn't read out by a screenreader, we would definitely want to use that<br>
&lt;hdv> Luke: I personally think we should have the checkmark by default on the checked option<br>
&lt;hdv> Luke: it's accessible and it gets the point across<br>
&lt;gregwhitworth> I have to drop, I have opinions on this as I don't like the default being backgrounds or unicode characters as you can't meaningfully customize them and thus pseudo solutions are not preferred<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> Luke: the main thing we want is a select that you customise, and that would very much be possible with this. From that perspective it makes sense for us to put as much accessible stuff in there by default as possible<br>
&lt;hdv> Luke: I prefer content to background… only thing maybe is that choice of font would impact how it is rendered<br>
&lt;hdv> s/thing/issue with that approach<br>
&lt;jarhar> q?<br>
&lt;hdv> ack nam<br>
&lt;hdv> naman: I think everything I was going to say has been covered<br>
&lt;hdv> naman: I think the unicode sets the wrong example, SVG background makes more sense to be… but don't have strong opinion on it<br>
&lt;una> q+<br>
&lt;hdv> flackr: doesn't it offer a more per platform customisation as to what that looks like on that platform?<br>
&lt;hdv> masonf: comes with default font<br>
&lt;hdv> flackr: yes<br>
&lt;hdv> naman: you've convinced me<br>
&lt;flackr> q+<br>
&lt;hdv> ack brad<br>
&lt;flackr> q-<br>
&lt;hdv> brad_frost: hi I'm Brad. we've had conversations about a global design system and wanted to give an update on that<br>
&lt;AzPrison> Hey flackr and hdv whats good<br>
&lt;hdv> masonf: should we put it on the agenda?<br>
&lt;hdv> brad_frost: sounds good!<br>
&lt;hdv> masonf: maybe this week… if you want to open an issue and set it to agenda+<br>
&lt;hdv> masonf: and thanks for the contribution!<br>
&lt;dbaron> I think there's already an issue for the global design system discussion at https://github.com/openui/open-ui/issues/1017<br>
&lt;hdv> scotto: una's correct… for accessibility the distinction for not relying on color alone, it doesn't have to be a checkbox<br>
&lt;AzPrison> dbaron whats up<br>
&lt;hdv> scotto: agree with a bunch of things that were said<br>
&lt;hdv> scotto: having said that, I'm more on the side of having a checkbox there by default<br>
&lt;AzPrison> yo hdv hit me back<br>
&lt;AzPrison> hdv hit me back<br>
&lt;hdv> scotto: if we end up not wanting to go with a character… we probably want a strong contrast in background to indicate current item… but then are people going to want to use it?<br>
&lt;hdv> scotto: if someone will change the background colour and it becomes something that isn't super accessible, it would be great to have the checkmark there as a fallback<br>
&lt;hdv> scotto: and if it's a unicode character it would also respect things like high contrast mode<br>
&lt;AzPrison> hdv scotto i can really help you guys with the character let me no youll want to hear my ideas<br>
&lt;masonf> q?<br>
&lt;masonf> ack scott<br>
&lt;keithamus> q+ naman<br>
&lt;masonf> ack una<br>
&lt;keithamus> q+<br>
&lt;hdv> una: to give you a few examples… on Facebook emoji picker the biggest change in what you've selected  is the selected option<br>
&lt;hdv> una: another example is the AirBnb country picker… a checkbox woulnd't fit there in the way we're thinking about it<br>
&lt;AzPrison> are you guys see my emails<br>
&lt;AzPrison> hdv una scotto: hit me back asap<br>
&lt;hdv> una: in a lot of examples of 'styleable selects' in the wild, the current item looks quite different and having a checkbox would not look appropriate, and would cause devs having to remove it<br>
&lt;masonf> q?<br>
&lt;hdv> una: having to remove the icon would be an extra step for developers I'd like to avoid if we can<br>
&lt;masonf> ack naman<br>
&lt;hdv> naman: to una's comment… I think there's two motivations here… one is can we give the most accessible default possible, and be defensive against developers doing the wrong thing… two is can we avoid pain for developers if they are going to customise and make it as easy of posible<br>
&lt;hdv> naman: when we think of defaults… do we expect developers to make the selected state accessible with every single one?<br>
&lt;hdv> naman: having an accessible default would be my preference<br>
&lt;una> but in that case, developers are still "undoing" and adding their own icon<br>
&lt;sanketj_> q+<br>
&lt;hdv> naman: in a single select you can think of a dropdown as a list of radiobuttons, whereas multi select is more like a list of checkbox, would it make sense to make it more radio-y?<br>
&lt;hdv> keithamus: I think it's probably been said enough… but wanted to state the obvious: there are discreet groups of people that we hope to anticipate the actions of: those who never style these things, vs those who always do<br>
&lt;hdv> keithamus: una had great examples of where the defaults wouldn't work<br>
&lt;brad_frost> q+<br>
&lt;masonf> ack keith<br>
&lt;hdv> keithamus: in our design system at GitHub we tweak pretty much all of the default style… and I think that's quite common<br>
&lt;masonf> ack sanket<br>
&lt;hdv> keithamus: and then maybe there's a third camp: those who customise badly and break accessibility<br>
&lt;jarhar> q+<br>
&lt;hdv> sanketj_: to address that category of those people who never customise: at least in Chromium we have a focus ring that we use on a variety of controls… <br>
&lt;hdv> sanketj_: is there really a need to bring in a default that is not commonplace?<br>
&lt;hdv> masonf: since selection doesn't follow focus, you need two patterns<br>
&lt;hdv> una: I think you run into the same problem, that people would still need to undo/redo it for their own styles<br>
&lt;jarhar> q-<br>
&lt;hdv> sanketj_: the meta point is… can we reuse existing patterns that are there<br>
&lt;brad_frost> q-<br>
&lt;hdv> masonf: yeah so bold text, other icons, etc could be other ways to do it<br>
&lt;hdv> q?<br>
&lt;hdv> masonf: is there anything we can resolve on today on this?\<br>
&lt;hdv> scotto: I kind of forgot about the selection follows focus thing and the moving away from that… I think there was going to be a change on Windows… but with that, it becomes even more important to distinguish between selected and focused, so like a checkmark that stays in place while user traverses with arrow keys<br>
&lt;hdv> una: if we do add an icon…it's not just adding the icon, it's also adding the spacing of the option to create space for the icon… that makes opt out a little harder<br>
&lt;hdv> jarhar: what I suggested doing earlier, is to set the checkmark on every single one of them and then the one's that aren't suggested are visibility hidden, then display none'ing them all gets rid of them all<br>
&lt;hdv> masonf: what I hear today is we're worried it would be hard to get rid of the default… would it be reasonable path forward for us to prototype?<br>
&lt;hdv> masonf: eg if we find a way to make it one line of code that's easier to discuss?<br>
&lt;hdv> una: having one line of code isn't necessarily my biggest concerns… it's also changes the architecture of what an option is? like the form of the option<br>
&lt;hdv> dbaron: one little thing to add to what masonf  was saying: one other thing to consider, can you do things like move the character to the other side?<br>
&lt;hdv> masonf: good point<br>
&lt;masonf> ack dbaron<br>
&lt;hdv> una: customising would get more complex as you have the think about the icon and how it relates to the option text<br>
&lt;hdv> keithamus: my point was kind of… customisers are going to customise<br>
&lt;scotto> q+<br>
&lt;hdv> keithamus: I think people would be ok to do a lot of customising<br>
&lt;hdv> una: but one of the more basic examples… a flag icon in a list of countries… would the author expect the checkmark to go away or move somewhere? what would they expect?<br>
&lt;masonf> ack scott<br>
&lt;hdv> keithamus: in our guidelines… if you have visual identifiers, you get rid of the checkmark… we had the conversation here and put it into the guidelines, and definitely noted the distinction but I don't think the issue was authors using the component necessarily<br>
&lt;masonf> q+ naman<br>
&lt;hdv> scotto: to reiterate what keithamus says… people have to remove a lot of styles anyway…and one of the things people do with styleable selects is also to add a lot of complexity<br>
&lt;masonf> ack naman<br>
&lt;hdv> naman: the regular patterns seems to be that appearance:none gets rid of default styles, isn't that something that could be done here?<br>
&lt;hdv> masonf: I think what you're requesting is already done<br>
&lt;hdv> naman: that makes I'm more in favour of checkboxes<br>
&lt;hdv> una: but you can't use appearance: base-select and appearance none at the same time<br>
&lt;hdv> una: can't have them both at once<br>
&lt;una> I appreciate your thoughts everyone!<br>
&lt;hdv> masonf: feel free to chime in if you have more thoughts on the issue<br>
</details>

<!-- comment id=2127775634 author=css-meeting-bot created=2024-05-23T18:17:38Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2127775634 resolution=true -->
## @css-meeting-bot — 2024-05-23

The Open UI Community Group just discussed `selectlist: Should the "checked" option have a checkmark next to it?`, and agreed to the following:

* `RESOLVED: support checkmark next to checked option, implemented via the content property on the ::marker pseudo element. The UA should set a Unicode character by default, which isn't read out by screen reader.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> masonf: we talked about this a few weeks ago. We had no resolutions then… question is: should there be a checkmark? and should it be there only for select multiple or also with single selects?<br>
&lt;hdv> keithamus: one issue was around: if we're going a unicode char, which one and will it be read out ok by a screenreader? and if not would it be easily customisable by devs?<br>
&lt;hdv> masonf: there is a natural unicode character that would fit here, which is called checkmark<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: but then the question remains how easy it is to replace the icon for developers?<br>
&lt;hdv> masonf: maybe it'd be helpful to write down what it would look like in CSS and what it would look like in the UA stylesheet, so that it's easier for us all to look at it<br>
&lt;hdv> keithamus: open question for me is: what selector would it use? `option::checked`?<br>
&lt;hdv> masonf: would make sense to me?<br>
&lt;keithamus> ```<br>
&lt;keithamus> select option:checked { content: ''; }<br>
&lt;keithamus> ```<br>
&lt;hdv> Luke: there was also talk of a marker pseudo el<br>
&lt;hdv> keithamus: if we're just overriding the `content` property, would `content: ''` the correct way to override it?<br>
&lt;Luke> `option:checked::marker { content: 'tick' }`<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: yes that would work, or whatever other method you could use<br>
&lt;hdv> q+<br>
&lt;brecht_dr> q+<br>
&lt;keithamus> q+<br>
&lt;hdv> Luke: this seems good to me, easy to replace it and easy to display none it<br>
&lt;hdv> Luke: and this is not hard to undo<br>
&lt;hdv> masonf: +1 to that!<br>
&lt;flackr> q+<br>
&lt;masonf> hdv: I agree with Luke. Seems like a good idea, easy to replace, easy to get rid of. Nice!<br>
&lt;masonf> hdv: content is also read out by screen readers, it's in the a11y name and description spec.<br>
&lt;masonf> Luke: there is a syntax to make that work correctly, with a trailing slash.<br>
&lt;masonf> ack hdv<br>
&lt;hdv> hdv: and we could even do in the UA stylesheet?<br>
&lt;hdv> Luke: yes<br>
&lt;hdv> brecht_dr: also +1 from me<br>
&lt;masonf> ack brecht<br>
&lt;masonf> ack keith<br>
&lt;hdv> brecht_dr: this seems like something people would want to remove and this seems to make it easy to do that<br>
&lt;hdv> keithamus: it seems straightforward<br>
&lt;hdv> keithamus: I know people found it tricky with `popover` on my team that `open` was the inverse state, but with this that doesn't seem like a big deal<br>
&lt;masonf> ack flackr<br>
&lt;hdv> flackr: looks good to me… but isn't `content: none` the way to turn it off?<br>
&lt;hdv> Luke: but would `display: none` help with also remove everything else like padding<br>
&lt;masonf> Proposed resolution: support checkmark next to checked option, implemented via the content property on the ::marker pseudo element.<br>
&lt;hdv> flackr: yes but content: none should also not draw a box<br>
&lt;keithamus> +1<br>
&lt;hdv> Luke: I think the only concern last time was 'is this a good idea to do as a default'… I think the consensus was that yes it is a good idea<br>
&lt;hdv> Luke: can it be rendered consistently and removed easily would be my two criteria<br>
&lt;hdv> masonf: yes I feel like having it on by default and easy to remove is good.<br>
&lt;hdv> masonf: when it comes to SVG or unicode char, I feel unicode char is easiest because standardised, SVG would maybe cause more bikeshedding<br>
&lt;brecht_dr> +1 to what Mason said there<br>
&lt;hdv> Luke: and good thing is if you change the font it can follow your font<br>
&lt;hdv> Luke: one advantage of SVG would be that currentcolor could be used and follow colours<br>
&lt;masonf> Proposed resolution: support checkmark next to checked option, implemented via the content property on the ::marker pseudo element. The UA should set a Unicode character by default.<br>
&lt;keithamus> +1<br>
&lt;masonf> support checkmark next to checked option, implemented via the content property on the ::marker pseudo element. The UA should set a Unicode character by default, which isn't read out by screen reader.<br>
&lt;keithamus> +1<br>
&lt;Luke> +1<br>
&lt;brecht_dr> +1<br>
&lt;hdv> flackr: another good reason for unicode char is that it would follow OS convention<br>
&lt;hdv> +1<br>
&lt;masonf> RESOLVED: support checkmark next to checked option, implemented via the content property on the ::marker pseudo element. The UA should set a Unicode character by default, which isn't read out by screen reader.<br>
</details>

<!-- comment id=2136336437 author=josepharhar created=2024-05-29T01:04:12Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2136336437 -->
## @josepharhar — 2024-05-29

I created an explainer PR here: https://github.com/openui/open-ui/pull/1059
I am implementing this in chromium here: https://chromium-review.googlesource.com/c/chromium/src/+/5578818

<!-- comment id=2136504731 author=yisibl created=2024-05-29T04:47:01Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2136504731 -->
## @yisibl — 2024-05-29

I strongly object to using `::marker` to style checkmarks, because `::marker` can only have a limited number of CSS properties(https://github.com/openui/open-ui/issues/863#issuecomment-1760226526), which severely constrains authors from customizing styles. For example, it's not easy to right-align icons(The third case in the figure).  Perhaps we could add a new pseudo-element so that it doesn't take up the `::before` or `::after` position.

![select-icon](https://github.com/openui/open-ui/assets/2784308/86c5aaa3-ff59-4224-ab0d-0029a749375a)

>  I've heard several times (would be great to get confirmation here) that it's an accessibility issue to only distinguish the selected vs. unselected options via color change. Having an explicit checkmark helps people who can't distinguish the colors. 

Yes, I'm in favor.

---- 

Regarding the cross-platform styling inconsistencies brought about by using Unicode characters, I think this can be solved by using the icon font technique, which is widely implemented for icon components on the Web. We now have the COLRv1 font format, which can even do gradient colors.

Demo: https://codepen.io/yisi/pen/NWVbZOY?editors=0100

@josepharhar One question is, can a browser use `@font-face` in a UA style to load a base64 font?

<!-- comment id=2136902635 author=lukewarlow created=2024-05-29T08:59:17Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2136902635 -->
## @lukewarlow — 2024-05-29

> I strongly object to using ::marker to style checkmarks, because ::marker can only have a limited number of CSS properties(https://github.com/openui/open-ui/issues/863#issuecomment-1760226526), which severely constrains authors from customizing styles. For example, it's not easy to right-align icons(The third case in the figure). Perhaps we could add a new pseudo-element so that it doesn't take up the ::before or ::after position.

So while this is true, it's possible that we can loosen some of these restrictions in this case, OR it's fine because for any non-standard usages of it they can just hide it and do the marker themselves. Importantly though we should be able to do right to left in the browser using this model if the marker should move in right to left contexts.

<!-- comment id=2136983976 author=yisibl created=2024-05-29T09:39:50Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2136983976 -->
## @yisibl — 2024-05-29

> Importantly though we should be able to do right to left in the browser using this model if the marker should move in right to left contexts.

Using a pseudo-element like `::before` does not affect changing the direction of the text, I added it to the demo.

![image](https://github.com/openui/open-ui/assets/2784308/84a60f5e-f55f-4993-a4a9-f5bb8ef054ad)


Using a pseudo-element like `::before` does not affect direction to change the direction of the text.

But with `::marker` the value of `display` becomes `list-item`, and we lose the possibility of using other layouts. No matter how much you add to the properties allowed by `::marker`, this cannot be changed under the current specification.

> The ::marker pseudo-element represents the automatically generated [marker box](https://drafts.csswg.org/css-lists-3/#marker) of a [list item](https://drafts.csswg.org/css-lists-3/#list-item). (See [[CSS-DISPLAY-3]](https://drafts.csswg.org/css-pseudo/#biblio-css-display-3) and [[CSS-LISTS-3]](https://drafts.csswg.org/css-pseudo/#biblio-css-lists-3).)

> A list item is any element with its [display](https://drafts.csswg.org/css-display-4/#propdef-display) property set to [list-item](https://drafts.csswg.org/css-display-4/#valdef-display-list-item). [List items](https://drafts.csswg.org/css-lists-3/#list-item) generate [::marker](https://drafts.csswg.org/css-pseudo-4/#selectordef-marker) pseudo-elements; no other elements do.

This problem already exists in `<details>`, I hope we don't have to make the same mistake.

<!-- comment id=2137022735 author=lukewarlow created=2024-05-29T09:58:37Z url=https://github.com/openui/open-ui/issues/863#issuecomment-2137022735 -->
## @lukewarlow — 2024-05-29

Sounds to me like that spec might need work regardless. But that's fair if it's very heavily specced and setup a certain way we can probably mint a new pseudo, or use before as Joey currently is.
