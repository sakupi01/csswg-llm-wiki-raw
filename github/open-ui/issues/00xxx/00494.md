---
number: 494
title: "[popup] Display Ordering: will this cause open=\"\" attributes to get removed during HTML parsing?"
state: "closed"
labels: ["popover"]
author: "mfreed7"
created_at: "2022-03-16T23:07:55Z"
closed_at: "2022-03-24T00:22:15Z"
url: "https://github.com/openui/open-ui/issues/494"
comments: 7
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

This issue was reported by @domenic [here](https://github.com/mfreed7/popup/issues/5), and is being moved to the OpenUI repo.

---

https://github.com/mfreed7/popup#display-ordering

> It is possible for an HTML document to contain multiple elements with popup=popup. In this case, only the last such element on the page will remain open when the page is fully loaded. This is because each successive popup element will force the previous one closed via the “one at a time” behavior.

The implication of this, combined with https://github.com/mfreed7/popup#dismiss-behavior, is that at the end of parsing only a single `open=""` attribute will be present, and others will be removed.

If this is the case, it'd be good to call it out explicitly. Also, there is some worry about modifications to the DOM during parsing, and the associated events; see https://github.com/whatwg/html/issues/4500 /cc @annevk @emilio. But probably it's no worse than custom elements, so I think that's not a real issue, just something we need to step lightly around...

<!-- comment id=1069728334 author=mfreed7 created=2022-03-16T23:08:23Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1069728334 -->
## @mfreed7 — 2022-03-16

@annevk said:

Note that custom elements don't end up executing script in all documents they end up in, e.g., with `XMLHttpRequest` or `DOMParser`. The problem with `<details open>` is that it does, iirc.

<!-- comment id=1069731062 author=mfreed7 created=2022-03-16T23:12:39Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1069731062 -->
## @mfreed7 — 2022-03-16

Note that the [updated explainer](https://github.com/openui/open-ui/pull/490) removes the `open` attribute completely. So I think the original issue might be moot. It adds an `initiallyopen` attribute, but I think we can specify that to just open the last such element on the page, rather than actually opening and then force-closing each subsequent popup. There might still be event-related issues such as https://github.com/whatwg/html/issues/4500. For these reasons, I'm leaving this issue open for comments.

<!-- comment id=1070898916 author=annevk created=2022-03-17T13:01:13Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1070898916 -->
## @annevk — 2022-03-17

(FWIW, I think `defaultopen` would be clearer and easier to spell.)

<!-- comment id=1070985785 author=domenic created=2022-03-17T15:32:08Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1070985785 -->
## @domenic — 2022-03-17

> I think we can specify that to just open the last such element on the page, rather than actually opening and then force-closing each subsequent popup.

How does this work in the case of slow-loading pages? E.g.

```html
<popup initiallyopen>...</popup>

<!-- network stalls for 2 seconds -->

<popup initiallyopen>...</popup>

<!-- network stalls for 2 seconds -->

<popup initiallyopen>...</popup>

<!-- network stalls for 10 seconds -->
```

<!-- comment id=1076944183 author=mfreed7 created=2022-03-24T00:22:15Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1076944183 -->
## @mfreed7 — 2022-03-24

> (FWIW, I think `defaultopen` would be clearer and easier to spell.)

The `initiallyopen` content attribute was proposed [here](https://github.com/openui/open-ui/issues/311#issuecomment-816160818), there were some good bikeshed-type comments made [here](https://github.com/openui/open-ui/issues/311#issuecomment-816248592), and resolved to be called `initiallyopen` [here](https://github.com/openui/open-ui/issues/311#issuecomment-820655960). But I'm not against reopening this can of worms, if you like. If so, would you mind opening a new issue?


> How does this work in the case of slow-loading pages? E.g.  {network stalls between `initiallyopen` elements}

Yep, this is an excellent point. I guess that puts us back to actually opening and closing each `initiallyopen` popup in sequence. Likely that's not too big of a deal, since I think the multiple `initiallyopen` elements case should be fairly corner case, right? That's also how the existing `<popup>` element prototype works.


Based on the above comments, I'm inclined to close this issue. Feel free to re-open if you disagree.

<!-- comment id=1077464767 author=annevk created=2022-03-24T10:19:09Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1077464767 -->
## @annevk — 2022-03-24

Filed https://github.com/openui/open-ui/issues/500.

<!-- comment id=1081256441 author=mfreed7 created=2022-03-28T23:41:46Z url=https://github.com/openui/open-ui/issues/494#issuecomment-1081256441 -->
## @mfreed7 — 2022-03-28

Coming back to this closed issue: I forgot one key detail in the above discussion. For `<popup>`, we actually specified that the **_first_** popup to contain `initiallyopen` "wins". That both a) makes a little more sense for a developer, since first-in-tree seems better than last-in-tree, and b) makes the implementation issues such as network loading delays mostly go away. That behavior is tested in [this WPT](https://wpt.fyi/results/html/semantics/interactive-elements/the-popup-element/popup-initiallyopen.tentative.html?label=master&label=experimental&aligned&q=popup-initiallyopen.tentative.html). I plan to just keep that behavior for the `popup` attribute.
