---
number: 970
title: "selectlist feedback from apple"
state: "closed"
labels: ["needs-triage", "select"]
author: "josepharhar"
created_at: "2023-12-04T18:48:59Z"
closed_at: "2024-07-31T02:03:46Z"
url: "https://github.com/openui/open-ui/issues/970"
comments: 7
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

I created a whatwg issue for selectlist here: https://github.com/whatwg/html/issues/9799

I have been discussing with Anne in the issue and offline, and I have agreed with him to make the following changes:
1. Reuse the `<select>` element instead of creating a new `<selectlist>` element
2. Reuse the `<datalist>` element instead of creating a `<listbox>` element for this element

Reusing the `<select>` element will work because of the new content model that we switched to in replacement of the behavior/slot/part attributes. We can update the HTML parser to allow `<button>` and `<datalist>` to be children of the `<select>` element.

I think that there are more arguments to be made for or against `<datalist>` instead of `<listbox>`, but Anne seemed more in favor of reusing `<datalist>`. Since reusing `<datalist>` can certainly fulfill all of our usecases for the select/selectlist project and would make it easier/faster to ship, I am receptive to reusing `<datalist>`.

Depending on how additional improvements for `<select>` go, I could also see us ending up with something like `<select size=n><datalist>...</datalist></select>` becoming a standalone `<listbox>`-like thing which could be the [aria-practices listbox](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) which people have been interested in. I haven't gotten feedback on this idea yet though.

I'd like to get a resolution that these two changes are ok.

<!-- comment id=1839281641 author=lukewarlow created=2023-12-04T19:00:40Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1839281641 -->
## @lukewarlow — 2023-12-04

I would want a concrete solution to the potential compat issue of people applying the multiple attribute in these case?

Would it be possible to use legacy select rendering (e.g ignore button and datalist) when multiple is set likewise for size attribute.

Would that actually solve the issue?

<!-- comment id=1839337766 author=josepharhar created=2023-12-04T19:31:27Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1839337766 -->
## @josepharhar — 2023-12-04

I think that ideally we will make this `<button>` and `<datalist>` pattern work somehow for `<select multiple>` and `<select size>`, but in any case we will have to make it work in some particular way before we get to that point and after we ship plain `<select>` improvements, so yeah I suppose there will be some compat risk in the intermediate period.

It would certainly be possible to use legacy select rendering when the multiple or size attributes are set, and I imagine that's probably what we would end up implementing.

<!-- comment id=1840793547 author=lukewarlow created=2023-12-05T13:27:54Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1840793547 -->
## @lukewarlow — 2023-12-05

So some further questions: 

- Would split button designs still work? 
- What type do we give button (type="select"?)?
- How do we handle people being people and ending up with a select inside a datalist inside a select? (Yes this is in the HTML Spec https://html.spec.whatwg.org/multipage/form-elements.html#the-datalist-element:~:text=a%20select%20element%20inside%20the%20datalist%20element.)

<!-- comment id=1843784841 author=josepharhar created=2023-12-06T22:23:39Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1843784841 -->
## @josepharhar — 2023-12-06

> Would split button designs still work?

I haven't gotten to this level of granularity with Anne yet, hopefully he and others are OK with it. The fact that github has these at the bottom of every issue and PR will hopefully make for a compelling argument.

> What type do we give button (type="select"?)?

I also haven't talked with them about this yet. Not sure what the response will be, and I'm not proposing changing our views on it.

> How do we handle people being people and ending up with a select inside a datalist inside a select? (Yes this is in the HTML Spec

Yeah this example in the HTML spec is pretty weird, not sure why it was added. So you're asking what happens if you do this?
```html
<select>
  <datalist>
    <select>
     ...
    </select>
  </datalist>
</select>
```
I don't see how switching from listbox to datalist elements changes whatever weird behavior this makes, but I suppose that you'd end up with a select element inside the listbox...?

<!-- comment id=1843788306 author=lukewarlow created=2023-12-06T22:26:46Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1843788306 -->
## @lukewarlow — 2023-12-06

> I don't see how switching from listbox to datalist elements changes...

I guess difference is listbox doesn't have historical baggage of whatever the hell that is. Whereas datalist does. Was more just a hmm this'll need accounting for maybe

<!-- comment id=1846026582 author=css-meeting-bot created=2023-12-07T20:01:11Z url=https://github.com/openui/open-ui/issues/970#issuecomment-1846026582 resolution=true -->
## @css-meeting-bot — 2023-12-07

The Open UI Community Group just discussed `selectlist feedback from apple`, and agreed to the following:

* `RESOLVED: Reuse the select element instead of creating the selectlist element and reuse the datalist element for selectlist/select instead of using a new listbox element for it`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> jarhar_: I recently created a WHATWG issue for selectlist and it got some feedback from Apple<br>
&lt;hdv> jarhar_: me and Anne from Apple discussed various things …one thing they suggested to change: to change selectlist to select, reusing the select element instead of using a new one… and instead of using the listbox element using the datalist element<br>
&lt;Luke> q+<br>
&lt;masonf> q+<br>
&lt;hdv> jarhar_: I agree with those and propose making those changes<br>
&lt;scotto_> q+<br>
&lt;hdv> q+<br>
&lt;dandclark> q+<br>
&lt;hdv> jarhar_: any questions?<br>
&lt;hdv> Luke: selectlist is nice because it has no historical baggage, but reusing select is nice because progressive enhancement… but need to be careful with multiple attr for instance, as we haven;'t figure out how multiple on selectlist would work<br>
&lt;masonf> ack Luke<br>
&lt;hdv> Luke: would we need to make a new element for multiple selectlist in case we realise it needs to something it currently can't?<br>
&lt;hdv> Luke: I like datalist instead of listbox, think it works, especially if you can combine size and multiple<br>
&lt;hdv> Luke: I do worry about the issues we could cause before it fully works<br>
&lt;hdv> Luke: if we just use HTML elements instead of slotted behaviours stuff that we had before, can it do all the things we thought it could before… also will we account for all use cases like split buttons?<br>
&lt;hdv> jarhar_: for select multiple we should be able to have the same improvements, hopefully there won't be issues with people using datalist with select multiple<br>
&lt;hdv> jarhar_: in this new proposal we would also avoid some of the parser issues that we had before with options in selectlist, where the content model was completely open ended; when we use select we would avoid those issues<br>
&lt;keithamus> q+<br>
&lt;hdv> ack mas<br>
&lt;hdv> masonf: I like this idea… was against it initially and feared it would be hard implementation wise… but it turns out there is a lot of parser behaviour that helps us a lot… the existing browsers/parsers will throw away all of the things it doesn't recognise, so we get a lot of leeway<br>
&lt;hdv> masonf: if we ship a version that doesnt support multiple for the new stuff… if you have a select multiple and throw in some of the new stuff, we could just make it fall back to an old style select multiple (it gives us great progressive enhancement)<br>
&lt;hdv> masonf: re q: if we can do the same with this new select proposal that we imagined for selectlist, the answer would be yes that should be possible (subject to jarhar_ going to be building all of this)<br>
&lt;masonf> q?<br>
&lt;masonf> ack scott<br>
&lt;hdv> scotto_: some of my questions were already answered… I think it's a good idea generally… it does make me wonder about some examples… like a split button that is a button and another button that opens a menu of sorts… in that case we could run in the situation that on macos that is mapped to a menubutton that opens a menu, but on Windows it opens a combobox… which is why it is hard to make a combobox...<br>
&lt;hdv> scotto_: ... that is consistent between Mac and Windows<br>
&lt;hdv> scotto_: this could perpetuate some of that<br>
&lt;hdv> scotto_: instead of being able to have a clean break, we may run into the issue where it is one thing in windows and another in mac<br>
&lt;hdv> masonf: the idea is that hte behaviour forks… you get the new behaviour if you do certain things, like add a datalist element in<br>
&lt;hdv> masonf: and only then<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: so when 'the new behaviour' triggers in select we could do whatever accessibility mappings  we wanted to do with selectlist<br>
&lt;masonf> q+ sasha<br>
&lt;hdv> q-<br>
&lt;hdv> scotto_: there's a lot of things a combobox can open, not sure how to do that anymore<br>
&lt;hdv> masonf: but this would be separate from combobox, more re select?<br>
&lt;hdv> scotto_: on windows  combobox = select<br>
&lt;hdv> masonf: let's open an issue re these accessibility mappings<br>
&lt;hdv> masonf: we don't want to lose track of that<br>
&lt;hdv> ack dan<br>
&lt;masonf> ack dan<br>
&lt;hdv> dandclark: I like the idea… a couple of comments: parser changes scare me a little bit, all of these old parser hacks are in the HTML spec for a reason, people are probably doing stuff that we would also need to take into account<br>
&lt;hdv> dandclark: we probably need to make sure it doesn't break stuff<br>
&lt;hdv> jarhar_: would be happy to add deprecation warnings and stuff<br>
&lt;hdv> dandclark: what about feature detection, would there be a way to do that with the select situation?<br>
&lt;hdv> jarhar_: could check if selectedoption element exists<br>
&lt;hdv> dandclark: +1 to what Luke said about multiple, and there is another bag of attributes on select, like size and others, that we would have to double check, we had decided not to include them for selectlist<br>
&lt;hdv> dandclark: type attribute is not that useful but it is there<br>
&lt;hdv> dandclark: would be weird if you could have arbitrary content in there<br>
&lt;hdv> dandclark: caveats aside…it would be pretty cool if we could make this work<br>
&lt;masonf> q?<br>
&lt;hdv> jarhar_: ideally we could have sizes attribute or something else… and could have fully styleable listbox element like listbox element would have been if we had included that<br>
&lt;hdv> jarhar_: does the parser for @@@ throw away elements?<br>
&lt;hdv> scotto_: yes also for options, this needs to be very carefully checked<br>
&lt;keithamus> s/@@@/datalist<br>
&lt;masonf> q?<br>
&lt;scotto_> q+<br>
&lt;hdv> dandclark: would be prioritising the parser stuff<br>
&lt;hdv> Zakim, please close the queue<br>
&lt;Zakim> ok, hdv, the speaker queue is closed<br>
&lt;hdv> keithamus: thing about selectlist is that it is easier to polyfill… it would throw elements out right?<br>
&lt;hdv> masonf: I think the path here is more graceful degradation than polyfill<br>
&lt;hdv> bkardell_: do they throw them away or fix up?<br>
&lt;hdv> masonf: throws them away<br>
&lt;hdv> masonf: you could lots of things in a select that never shows up in the DOM tree<br>
&lt;masonf> q?<br>
&lt;hdv> sasha: hard to polyfill in the browser, for parser and runtime engine for customised and standard element<br>
&lt;masonf> ack keith<br>
&lt;hdv> ack sasha<br>
&lt;masonf> ack sasha<br>
&lt;masonf> ack scotto<br>
&lt;hdv> scotto_: regardless of parser issues… it just becomes easier to style select elements, I think that's going to be a win<br>
&lt;hdv> scotto_: does the work currently still have legs as a new proposal for a menu element?<br>
&lt;hdv> scotto_: a lot of what people wanted to do with this is create menus<br>
&lt;hdv> scotto_: there's arguably still a reason to do that… a lot of what we're doing here is potentially reusable for menu-like element too<br>
&lt;hdv> scotto_: so if select has to be used… that could be a way to make it easier for people to make that styleable. And then what selectlist was, there was a lot of use cases that belonged in the menu category<br>
&lt;hdv> +1!<br>
&lt;keithamus> +1<br>
&lt;jarhar_> Proposed resolution: Reuse the select element instead of creating the select element and reuse the datalist element for selectlist/select instead of using a new listbox element for it<br>
&lt;jarhar_> Proposed resolution: Reuse the select element instead of creating the selectlist element and reuse the datalist element for selectlist/select instead of using a new listbox element for it<br>
&lt;keithamus> +1<br>
&lt;hdv> +10<br>
&lt;Luke> +1<br>
&lt;dandclark> +1 to trying it<br>
&lt;keithamus> +∞<br>
&lt;jarhar_> RESOLVED: Reuse the select element instead of creating the selectlist element and reuse the datalist element for selectlist/select instead of using a new listbox element for it<br>
</details>

<!-- comment id=2148630111 author=github-actions[bot] created=2024-06-05T00:36:57Z url=https://github.com/openui/open-ui/issues/970#issuecomment-2148630111 -->
## @github-actions[bot] — 2024-06-05

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.
