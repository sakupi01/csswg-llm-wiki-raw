---
number: 956
title: "[invokers] Dialog's `toggle` needs a better name"
state: "closed"
labels: ["command-invokers", "needs edits", "needs-triage"]
author: "keithamus"
created_at: "2023-11-16T20:12:41Z"
closed_at: "2024-12-07T14:46:21Z"
url: "https://github.com/openui/open-ui/issues/956"
comments: 4
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

Invokers targeting a `<dialog>` can have `invokeaction=toggle`, which will open and close the dialog as non-modal.

`toggle` is a little ambiguous, especially as all other names are very clear in their action, `togglePopover`, `playpause`, etc.

Given https://github.com/whatwg/html/issues/9376 (Should we deprecate Dialog `show()`?) perhaps we can simply _not_ implement this?

If we are to implement this, it needs a clearer name. Some bad ideas to start the bidding:

 - `toggleNonModal`
 - `toggleOpen`
 - `toggleShow`

<!-- comment id=1815258156 author=lukewarlow created=2023-11-16T20:23:34Z url=https://github.com/openui/open-ui/issues/956#issuecomment-1815258156 -->
## @lukewarlow — 2023-11-16

I vote "don't implement". ToggleOpen isn't a perfect representation of what it does (given focusing) so I would vote against that.

<!-- comment id=1845987671 author=css-meeting-bot created=2023-12-07T19:38:07Z url=https://github.com/openui/open-ui/issues/956#issuecomment-1845987671 -->
## @css-meeting-bot — 2023-12-07

The Open UI Community Group just discussed ``[invokers] Dialog's `toggle` needs a better name``, and agreed to the following:

* ``RESOLVED: Defer `toggle` and `show`, remove the explicit `toggleModal` string, and keep `auto` behaviours as is.``

<details><summary>The full IRC log of that discussion</summary>
&lt;masonf> q?<br>
&lt;hdv> keithamus: the toggle method may be ambiguous… the others are unambiguous to the target element, like showPopover<br>
&lt;Luke> q+<br>
&lt;masonf> q+<br>
&lt;hdv> keithamus: for showing and hiding a dialog for an invoke button<br>
&lt;masonf> q+ sasha<br>
&lt;hdv> Luke: details is ambiguous as well<br>
&lt;hdv> Luke: having said that, details isn't in the v1<br>
&lt;hdv> Luke: would say don't implement show() or toggle() in the initial version<br>
&lt;hdv> s/show()/show<br>
&lt;hdv> s/toggle()/toggle<br>
&lt;masonf> ack luke<br>
&lt;hdv> Luke: so if we decide to keep non modal dialog in the future for whatever reason ew could<br>
&lt;hdv> Luke: most people will likely use a modal dialog or a popover<br>
&lt;hdv> masonf: can we punt the whole toggle thing… there is no such API on dialogs now can we just leave showmodal and close and auto?<br>
&lt;hdv> masonf: I'm still questioning… if you're toggling… the use case for toggle on a modal doens't make sense to me<br>
&lt;masonf> ack mason<br>
&lt;hdv> keithamus: use case for toggle on modal is the auto action<br>
&lt;hdv> keithamus: do we have a name mapping for the auto action other than auto?<br>
&lt;hdv> keithamus: another meta question around this… do we intentionally want tot disincentivise showing non modal dialog from dialog?<br>
&lt;hdv> s/tot/to<br>
&lt;hdv> masonf: don't think we should actively disincentivise anything<br>
&lt;hdv> masonf: on most parts of the platform auto is magic<br>
&lt;keithamus> q+<br>
&lt;hdv> masonf: I don't think there's a problem with auto not mapping to a string value you can provide<br>
&lt;masonf> ack sasha<br>
&lt;hdv> sasha: I can see the reference to existing solutions here<br>
&lt;hdv> sasha: I think we need to align if it fits with the old semantics… it looks from this convo that it does, only want to display or hide<br>
&lt;hdv> sasha: not sure about the toggle as a part of the flow in the page… they usually work together with the whole page… what would happen next after triggering the dialog?<br>
&lt;masonf> q?<br>
&lt;hdv> sasha: when we do the proposal it would be nice to outline generic use cases and scope<br>
&lt;masonf> ack keith<br>
&lt;hdv> keithamus: one reason not to want auto this way is that auto is ambiguous depending on the target element<br>
&lt;hdv> keithamus: I didn't understand your last point sasha?<br>
&lt;hdv> sasha: usually they are triggering the visibility associated with the next steps in the user flow<br>
&lt;hdv> sasha: so if you toggle a dialog and dialog has an extra attribute, they would not just toggle visibility of that one, but also toggle details with open attribute after the close of this dialog<br>
&lt;hdv> keithamus: I think dialogs already have some of the behaviour that you describe<br>
&lt;hdv> masonf: there are still events, close and cancel on dailogs, that could be used to trigger the next<br>
&lt;hdv> sasha: am thinking as declarative<br>
&lt;masonf> q?<br>
&lt;scotto_> q+<br>
&lt;hdv> luke: on toggle button you have buttons that are literally toggles… but on a modal you'd have buttons that only do either, just a single action<br>
&lt;masonf> q+<br>
&lt;hdv> Luke: so I think folks would either go auto or toggle mode<br>
&lt;keithamus> q+<br>
&lt;hdv> scotto_: +1 what you mentioned Mason, Keith and Luke both know I've had a weirdness to toggling modals, you can't do that from a single button, outside of a weird use case of someone forcing a dialog to be open<br>
&lt;hdv> scotto_: so we probably don't need to add a toggle attribute for that<br>
&lt;hdv> ack scotto_<br>
&lt;hdv> masonf: not having auto for dialogs feels kind of funny<br>
&lt;hdv> masonf: so the spec would have to say if it is a popover, do this, if it's a dialog, do that… so maybe auto doesn't need to mean toggle, it can mean use as modal?<br>
&lt;masonf> ack masonf<br>
&lt;hdv> Luke: we don't have auto for everything, eg for select there isn't an auto<br>
&lt;hdv> masonf: shoulnd't there be?<br>
&lt;hdv> Luke: if we add togglepicker or closepicker… if we have auto now we couldn't add those in the future<br>
&lt;hdv> masonf: I'd argue show is a good default there for same reason as dialog<br>
&lt;hdv> Luke: I see the point<br>
&lt;hdv> q?<br>
&lt;masonf> q+ charlie<br>
&lt;hdv> keithamus: the intend of auto toggling the modal is thatyou don't have to specify the invokeaction attribute<br>
&lt;hdv> keithamus: so inside the dialog not specifying the attribute would  do the sensible thing, close the attribute… it saves devs from writing the attribute, browser could be clever enough to do the right thing<br>
&lt;hdv> keithamus: maybe the correct thing to do here… remove the aliases to auto, and auto will, as spec says today, if target el is popover, it will toggle as a popover, if the element is a dialog, it will show as modal unless dialog is open then it willl close it<br>
&lt;masonf> q?<br>
&lt;masonf> ack keith<br>
&lt;masonf> ack charlie<br>
&lt;hdv> charlie: my concern with going ahead with auto, because we don't know the full list of invokee elements right now, but want to support it for some other than popover and dialog, we probably want to keep it consistent<br>
&lt;hdv> charlie: this may give us confusing use cases<br>
&lt;masonf> q+ sasha<br>
&lt;hdv> charlie: my vote would be to defer auto until we have a better idea what would be included in invokers<br>
&lt;hdv> masonf: so you say it would feel weird for auto to do fundamentally different things for different elements so it is fair to wait until we know more?<br>
&lt;hdv> charlie: yes<br>
&lt;hdv> masonf: would cause for everyone to always add both attributes<br>
&lt;hdv> Luke: is the case for popover currently too<br>
&lt;hdv> masonf: it is nice not to have to add extra attributes<br>
&lt;hdv> sasha: some buttons could have three states, sounds like these could be subject to the mode, would be nice to provide those states<br>
&lt;masonf> q?<br>
&lt;hdv> masonf: top layer is pretty carefully controlled so you can't change popover to dialog without closing it first<br>
&lt;keithamus> PROPOSED RESOLUTION: Defer toggle, and remove `auto` aliases (such as `toggleModal`).<br>
&lt;masonf> ack sasha<br>
&lt;hdv> masonf: maybe to clarify: do you mean continue supporting auto but not having a mapping?<br>
&lt;hdv> keithamus: yes<br>
&lt;hdv> Luke: you intend to keep togglePopover as a standalone?<br>
&lt;hdv> masonf: that's pretty well defined, maps to a function<br>
&lt;keithamus> Proposed Solution: Defer `toggle`, remove the explicit `toggleModal` string, and keep `auto` behaviours as is.<br>
&lt;masonf> +1<br>
&lt;scotto_> +1<br>
&lt;hdv> +1<br>
&lt;hdv> Luke: can I clarify does this include show?<br>
&lt;hdv> masonf: would make sense to keep?<br>
&lt;keithamus> Proposed Resolution: Defer `toggle` and `show`, remove the explicit `toggleModal` string, and keep `auto` behaviours as is.<br>
&lt;masonf> +1<br>
&lt;Luke> +1<br>
&lt;hdv> +2<br>
&lt;keithamus> Resolved: Defer `toggle` and `show`, remove the explicit `toggleModal` string, and keep `auto` behaviours as is.<br>
&lt;hdv> (for British spelling)<br>
&lt;keithamus> RESOLVED: Defer `toggle` and `show`, remove the explicit `toggleModal` string, and keep `auto` behaviours as is.<br>
</details>

<!-- comment id=1845996385 author=sashafirsov created=2023-12-07T19:44:56Z url=https://github.com/openui/open-ui/issues/956#issuecomment-1845996385 -->
## @sashafirsov — 2023-12-07

The separation of concerns IMO needs separate attributes: 
* ther aspect of visibility, i.e. switch the shown/hidden state
* the mode definition of how the dialog would be shown(modal/non-modal/inline)
* what should happen after the dialog completed(triggered) 

It is important as provide each functionality separately and do not mix those. As the imperative as **declarative** syntax for all. In this case declarative can be limited by allocation of DIALOG attributes.

# Open
`open` as name for visibility to match the other HTML elements like in [details](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/details) element

# Toggle/Trigger
triggering the next UI on the `toggle` as pointer to the what should happen to element refered say by `for` attribute. Scenario:
* trigger the current dialog by setting `open=true` attribute
* get the ref element by `for` attribute
* get the target state of ref element from `toggle` attribute
* open/close the targer element, pass the focus

# Mode
the `modal` vs `popover` vs `inline` is rather the subject for `mode` attribute of Dialog.

<!-- comment id=2148630163 author=github-actions[bot] created=2024-06-05T00:37:00Z url=https://github.com/openui/open-ui/issues/956#issuecomment-2148630163 -->
## @github-actions[bot] — 2024-06-05

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.
