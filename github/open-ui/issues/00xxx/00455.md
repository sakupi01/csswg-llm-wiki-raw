---
number: 455
title: "New Approach for Popup"
state: "closed"
labels: ["popover"]
author: "mfreed7"
created_at: "2022-02-08T18:05:20Z"
closed_at: "2022-03-03T18:53:56Z"
url: "https://github.com/openui/open-ui/issues/455"
comments: 7
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

There has been extensive debate (e.g. https://github.com/openui/open-ui/issues/410 and https://github.com/openui/open-ui/issues/417, plus numerous OpenUI meeting discussions) about the [`<popup>` element proposal](https://open-ui.org/components/popup.research.explainer). That proposal got "stuck" on what appear to be some [rather fundamental questions](https://github.com/mfreed7/popup#alternative-dedicated-popup-element) about the semantics and associated AX roles for this new element.

As a result of these issues, we've put together a [fresh proposal](https://github.com/mfreed7/popup#readme) for a new way to achieve (roughly) the same list of [goals](https://github.com/mfreed7/popup#goals). This new proposal introduces an HTML attribute syntax that allows **any element** to be made into a "popup" that gets rendered on top of all other page content:

```html
<div popup=popup>I'm rendered on top!</div>
```

Here, the `popup` attribute is set to a value of `popup` (other values include `hint` and `async`) to indicate "popup" behaviors should be applied. That includes:
 * Rendering in the top layer
 * Automatically-provided light-dismiss and one-at-a-time behavior. (These depend on the value of the `popup` attribute.)
 * Accessible by default

Additionally, another HTML attribute, `triggerpopup`, can be used for declarative triggering, so that this API can be used without any JavaScript required:

```html
<button id=picker-button triggerpopup=datepicker>Pick a date</button> 
<my-date-picker role=dialog id=datepicker popup=popup hidden>
  ...date picker implementation...
</my-date-picker>
```

In this case, the `<my-date-picker`> component starts out `hidden`, but when the `<button>` is clicked, the UA removes `hidden` from `<my-date-picker`> and the date picker is shown. As with all popups, once the user clicks outside, hits Esc, etc., the UA will light-dismiss it by adding back the `hidden` attribute.

Please take a look at [the new proposal](https://github.com/mfreed7/popup#readme), and add your comments here to this issue. Pull requests for changes are also great.

<!-- comment id=1032955406 author=chrisdholt created=2022-02-08T18:59:25Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1032955406 -->
## @chrisdholt — 2022-02-08

Only note I would have is "accessible by default" seems like something that is likely within reason. A point of clarity here would be that an element with a role of `dialog` with `aria-modal="true"` and `popup="popup"` likely isn't going to have focus-trapping behavior by default nor do I think that it should. With that said, `focusgroup` may provide that for us. Just adding a note to make sure expectations are set.

All that said, I really like this @mfreed7.

<!-- comment id=1040567557 author=mfreed7 created=2022-02-15T17:31:39Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1040567557 -->
## @mfreed7 — 2022-02-15

So this new approach was [discussed last week](https://www.w3.org/2022/02/10-openui-minutes.html) at OpenUI. I generally heard supportive comments, and didn't hear any (at least major) objections to the new approach. I'd really like to kick the tires by re-implementing the Chromium `<popup>` element prototype into a new `<div popup=popup>` attribute prototype. Before I do that, however, I'd love to hear any input or feedback. I'd hate to implement something that has a fatal flaw. Let me know what you think!

<!-- comment id=1040629366 author=scottaohara created=2022-02-15T18:29:31Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1040629366 -->
## @scottaohara — 2022-02-15

@mfreed7 issues here or with your explainer doc?

from my memory, the topic of `hidden` vs `open` seemed good feedback, and making this change would help realign with the aspect of the element's initial spec that was calling out use of `open`

<!-- comment id=1040950058 author=mfreed7 created=2022-02-16T00:50:20Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1040950058 -->
## @mfreed7 — 2022-02-16

> @mfreed7 issues here or with your explainer doc?

Either way works for me! I'll try to listen to both and incorporate them back into the explainer.

> from my memory, the topic of `hidden` vs `open` seemed good feedback, and making this change would help realign with the aspect of the element's initial spec that was calling out use of `open`

Yep, this is a great point, thanks for the reminder. I had indeed taken a note to possibly change to an `open` attribute from the `hidden` attribute. There were several good comments to that effect in the meeting. Another related one was that by mapping the `open` attribute to the `visibility` CSS property, animations could still work in both directions. I'll make this change.

<!-- comment id=1041005731 author=mfreed7 created=2022-02-16T01:43:42Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1041005731 -->
## @mfreed7 — 2022-02-16

> Yep, this is a great point, thanks for the reminder. I had indeed taken a note to possibly change to an `open` attribute from the `hidden` attribute. There were several good comments to that effect in the meeting. Another related one was that by mapping the `open` attribute to the `visibility` CSS property, animations could still work in both directions. I'll make this change.

I've [updated the explainer](https://github.com/mfreed7/popup#html-content-attribute) to use `open` instead of `hidden`. It begs the question we already discussed on https://github.com/openui/open-ui/issues/311 about whether we actually need the `open` attribute at all. In contrast to the decision/discussion related to the `<popup>` element, I do think we'd need a CSS pseudo class at least, to allow styling things differently depending on whether the element is in the top layer. (That's discussed [here](https://github.com/w3c/csswg-drafts/issues/6965).) But it seems like perhaps we could get away with only something like an `initiallyopen` attribute that sets the initial state, and have the rest happen in the UA. Thoughts?

<!-- comment id=1050172067 author=css-meeting-bot created=2022-02-24T19:10:12Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1050172067 -->
## @css-meeting-bot — 2022-02-24

The Open UI Community Group just discussed `New Approach for Popup`, and agreed to the following:

* ``RESOLVED: The general shape of the `popup` content attribute proposal looks good, and we are ok to start prototyping.``

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> Topic: New Approach for Popup<br>
&lt;gregwhitworth> github: https://github.com/openui/open-ui/issues/455<br>
&lt;hdv> masonf: the main thing I want to get out of this meeting… I presented a new approach to popup that is based on a content attribute as opposed to a new element<br>
&lt;hdv> masonf: the comments I heard were generally positive, so the main thing I want to discuss is… are there any major killer issue that cause it to be not a viable, valid proposal<br>
&lt;hdv> masonf: because the next thing I'd like to do is update the prototype in Chromium<br>
&lt;hdv> masonf: a lot of people have raised great issues, they are in my repo btw, will probably move to open ui repo<br>
&lt;hdv> masonf: would love to hear more comments if people have them<br>
&lt;masonf> Proposed resolution: The general shape of the `popup` content attribute proposal looks good, and we are ok to start prototyping.<br>
&lt;davidluhr> Our work is done here, Dave gave the thumbs up.<br>
&lt;hdv> dave: do it!<br>
&lt;una> LGTM<br>
&lt;hdv> chris: I'd like to second that<br>
&lt;hdv> masonf: we're trying to get to the point where we found most issues there are with it<br>
&lt;hdv> davatron5000: are you happy with it, Mason?<br>
&lt;gregwhitworth> RESOLVED: The general shape of the `popup` content attribute proposal looks good, and we are ok to start prototyping.<br>
&lt;hdv> masonf: yes, I'm happier with it than the element and it feels very implementable to me<br>
</details>

<!-- comment id=1058379762 author=andrico1234 created=2022-03-03T18:53:56Z url=https://github.com/openui/open-ui/issues/455#issuecomment-1058379762 -->
## @andrico1234 — 2022-03-03

@mfreed7 , I'm closing this issue on account of the Telecon resolution. Feel free to re-open if you'd like to continue the discussion here 😄
