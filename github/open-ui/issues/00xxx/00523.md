---
number: 523
title: "[popup] What are the precedence rules for `togglepopup`, `showpopup`, and `hidepopup`"
state: "closed"
labels: ["popover"]
author: "mfreed7"
created_at: "2022-04-21T19:05:54Z"
closed_at: "2022-04-29T17:53:26Z"
url: "https://github.com/openui/open-ui/issues/523"
comments: 4
has_resolution: true
synced_at: "2026-08-27T14:25:56Z"
---

See https://github.com/openui/open-ui/issues/508 for context, but the popup now supports three content attributes:

- `togglepopup=popup_id`: If `popup_id` refers to a showing popup, hide it. If `popup_id` refers to a hidden popup, show it.
-  `showpopup=popup_id`: If `popup_id` refers to a hidden popup, show it.
-  `hidepopup=popup_id`: If `popup_id` refers to a showing popup, hide it.

The question becomes: what happens when one element has more than one of these attributes set? What if they point to different idrefs?

<!-- comment id=1106667339 author=mfreed7 created=2022-04-22T16:18:38Z url=https://github.com/openui/open-ui/issues/523#issuecomment-1106667339 -->
## @mfreed7 — 2022-04-22

[Posted](https://github.com/openui/open-ui/issues/508#issuecomment-1105659011) by @jh3y:

> * I like the idea of "toggle", "show", and "hide". For precedence, "show" or "hide" trump "toggle" IMO as they are more specific about what they are doing. If all three exist, it's a toggle. If "show" and "hide" exist, it's a non-issue too? Only issue is with varying IDs 🤔  If a button opens one and closes another, where does a11y come into that? Does it?

<!-- comment id=1106686358 author=mfreed7 created=2022-04-22T16:43:48Z url=https://github.com/openui/open-ui/issues/523#issuecomment-1106686358 -->
## @mfreed7 — 2022-04-22

I'd like to propose this as a behavior:

When an activating element (e.g. `<button>`) is activated:
1. Let `popup_idref` be null, and let `popup_action` be empty.
3. If the `togglepopup` attribute is present on the element:
    1. Let `popup_idref` be the value of the `togglepopup` property.
    1. Let `popup_action` be "toggle".
3. If `popup_action` is empty, and the `showpopup` attribute is present on the element: 
    1. Let `popup_idref` be the value of the `showpopup` property.
    1. Let `popup_action` be "show".
3. If `popup_action` is empty or "show", and the `hidepopup` attribute is present on the element: 
    1. If `popup_idref` is null, set it to the value of the `hidepopup` property.
    1. If the value of the `hidepopup` property is not equal to `popup_idref`, continue.
    1. If `popup_action` is "show", set `popup_action` to "toggle". Otherwise, set it to "hide".
4. If `popup_action` is empty, stop this algorithm.
5. Let `popup_target` be the result of [running getElementById()](https://dom.spec.whatwg.org/#ref-for-dom-nonelementparentnode-getelementbyid%E2%91%A1) on `popup_idref`.
6. If `popup_target` does not exist, or does not have a `popup` attribute with a valid value, end this algorithm.
7. If `popup_target` is in the "showing" state, and `popup_action` is "toggle" or "hide", then call `popup_target.hidePopup()` and end this algorithm.
8. If `popup_target` is in the "hidden" state, and `popup_action` is "toggle" or "show", then call `popup_target.showPopup()` and end this algorithm.

And that should:
1. Make `togglepopup` trump the other two.
2. Make `showpopup` "win" in terms of idrefs, in case it differs with `hidepopup`.
3. If both `showpopup` and `hidepopup` are present, they need to point to the same idref, and then you'll get `togglepopup` behavior.
4. There's only ever one `popup_target`.

Thoughts? Bugs in the algorithm?

<!-- comment id=1112523143 author=css-meeting-bot created=2022-04-28T18:20:02Z url=https://github.com/openui/open-ui/issues/523#issuecomment-1112523143 resolution=true -->
## @css-meeting-bot — 2022-04-28

The Open UI Community Group just discussed `[popup] Add ::backdrop to popup to enable modal popup #519`, and agreed to the following:

* `RESOLVED: We accept the precedence rules from https://github.com/openui/open-ui/issues/523#issuecomment-1106686358.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> Topic: [popup] Add ::backdrop to popup to enable modal popup #﻿519<br>
&lt;hdv> github: https://github.com/openui/open-ui/blob/main/meetings/telecon/2022-04-21.md<br>
&lt;hdv> github: https://github.com/openui/open-ui/issues/523<br>
&lt;jh3y> wrt #﻿519: +1 Good to match expectation from &lt;dialog> usage<br>
&lt;hdv> masonf: the idea of togglepopup is that the idref you put on a button that it toggles that, but it needs a rule for what happens when there are multiple attributes<br>
&lt;hdv> masonf: what we need to resolve on is what happens exactly so that developers know what to expect<br>
&lt;flackr> q+<br>
&lt;JonathanNeal> q?<br>
&lt;hdv> masonf: I proposed some rules on the thread, summary: if you provide togglepopup, it wins, it ignores showpopup and hidepopup. If you use showpopup and hidepopup, with different ids, showpopup wins, if they have the same idref, it behaves like togglepopup<br>
&lt;hdv> flackr:<br>
&lt;hdv> flackr: I agree high level… in the proposed algorithm, if you show ID1 and hide ID2, you get toggle behavior?<br>
&lt;hdv> masonf: should be show behavior?<br>
&lt;hdv> flackr: ok I agree, if the IDs don't match we should not modify the behavior<br>
&lt;JonathanNeal> q?<br>
&lt;JonathanNeal> ack flackr<br>
&lt;hdv> masonf: let's try today to discuss just what we want, then we'll fix the algorithm later<br>
&lt;miriam> +1 I like the intended behavior<br>
&lt;hdv> JonathanNeal: is there agreement on the intended behavior and the idea for this to be resolved ?<br>
&lt;una> SGTM<br>
&lt;JonathanNeal> +1<br>
&lt;Alexander_Futekov> +1<br>
&lt;hdv> flackr: I think the algorithm is correct actually<br>
&lt;flackr> +1<br>
&lt;masonf> Proposed resolution: We accept the precedence rules from https://github.com/openui/open-ui/issues/523#issuecomment-1106686358.<br>
&lt;JonathanNeal> +1<br>
&lt;masonf> RESOLVED: We accept the precedence rules from https://github.com/openui/open-ui/issues/523#issuecomment-1106686358.<br>
&lt;hdv> masonf: if you find a bug in the algorithm please tell me<br>
</details>

<!-- comment id=1113576471 author=mfreed7 created=2022-04-29T17:53:26Z url=https://github.com/openui/open-ui/issues/523#issuecomment-1113576471 -->
## @mfreed7 — 2022-04-29

Based on the resolution, I'm going to close this issue. If you find issues/bugs in [the proposed algorithm](https://github.com/openui/open-ui/issues/523#issuecomment-1106686358), please report them as a [new issue](https://github.com/openui/open-ui/issues/new).
