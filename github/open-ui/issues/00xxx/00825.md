---
number: 825
title: "select: Should `<selectedoption>` respond to mutations in the selected `<option>`"
state: "closed"
labels: ["agenda+", "needs edits", "select"]
author: "josepharhar"
created_at: "2023-08-31T20:25:59Z"
closed_at: "2024-10-28T20:01:33Z"
url: "https://github.com/openui/open-ui/issues/825"
comments: 12
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

Consider the following:
```html
<selectlist>
  <button>
    <selectedoption></selectedoption>
  </button>
  <option>foo</option>
</selectlist>
<script>
requestAnimationFrame(() => {
  document.querySelector('option').textContent = 'bar';
});
</script>
```

Should the `<selectedoption>`'s contents set to "bar", or should it stay as "foo"?
The equivalent use case for `<select>` will change the button's text to "bar".

@argyleink @mfreed7

We should probably wait for https://github.com/openui/open-ui/issues/571 to be resolved before getting this 100% figured out.

<!-- comment id=1701738822 author=argyleink created=2023-08-31T20:29:46Z url=https://github.com/openui/open-ui/issues/825#issuecomment-1701738822 -->
## @argyleink — 2023-08-31

i vote for the value to update to bar

<!-- comment id=1728510116 author=lukewarlow created=2023-09-20T22:31:52Z url=https://github.com/openui/open-ui/issues/825#issuecomment-1728510116 -->
## @lukewarlow — 2023-09-20

It should display whatever the form submitted value would be imo. So if that code would update the value (I think it would) then it should also update the displayed html

<!-- comment id=2005473933 author=github-actions[bot] created=2024-03-19T00:33:39Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2005473933 -->
## @github-actions[bot] — 2024-03-19

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2013199157 author=josepharhar created=2024-03-21T18:06:00Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2013199157 -->
## @josepharhar — 2024-03-21

Yeah we should probably listen to mutations, it really seems like the sane default. It will likely be expensive for the browser to do that, but there are ways that we can improve the performance: https://chromium-review.googlesource.com/c/chromium/src/+/5370555/2/third_party/blink/renderer/core/html/forms/html_selected_option_element.h

<!-- comment id=2077745929 author=josepharhar created=2024-04-25T16:56:25Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2077745929 -->
## @josepharhar — 2024-04-25

I propose that we resolve to make the selectedoption element respond to mutations inside option elements. Otherwise, we might not pick up updates that authors have made to the DOM and it will leave the selectedoption element in a broken state.

<!-- comment id=2077912960 author=css-meeting-bot created=2024-04-25T18:31:05Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2077912960 resolution=true -->
## @css-meeting-bot — 2024-04-25

The Open UI Community Group just discussed ``selectlist: Should `<selectedoption>` respond to mutations in the selected `<option>` ``, and agreed to the following:

* `RESOLVED: The selectedoption element should update its contents when the selected options DOM descendants are modified`

<details><summary>The full IRC log of that discussion</summary>
&lt;brecht_dr> jarhar From the browser engineer perspective this is an interesting one<br>
&lt;gregwhitworth> q+<br>
&lt;dandclark> q+<br>
&lt;bkardell_> q+<br>
&lt;masonf> q+<br>
&lt;brecht_dr> jarhar If you append some text in the option it would also show up in the button. is this something we should do. I think authors want to do that.<br>
&lt;Luke> +1<br>
&lt;brecht_dr> jarhar I would think we want to support that<br>
&lt;brecht_dr> gregwhitworth I agree we should. We did resolve on the inverse that the content is the innertext that is migrated.<br>
&lt;brecht_dr> gregwhitworth we should point that out in our documentation<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> ack dandclark<br>
&lt;brecht_dr> dandclark I agree with jarhar.<br>
&lt;gregwhitworth> ack bkardell_<br>
&lt;brecht_dr> bkardell_ I was curious. What is the case for somebody to do this?<br>
&lt;Luke> q+<br>
&lt;brecht_dr> jarhar Let's say you have some website where it has hydration kind of thing and your initial html you have a select element and then you have a seperate call in a database where you want to update your select options for example some imates<br>
&lt;brecht_dr> jarhar and you want to update that button as well by updating (hydrating) the options<br>
&lt;gregwhitworth> ack masonf<br>
&lt;brecht_dr> masonf I do have a fear about specifying this behaviour.<br>
&lt;gregwhitworth> q+<br>
&lt;keithamus> q+<br>
&lt;gregwhitworth> ack Luke<br>
&lt;brecht_dr> masonf I'm still wondering about real use cases<br>
&lt;brecht_dr> Luke How does a lazy loaded image cloned into the new area work?<br>
&lt;brecht_dr> masonf That should be ok? as it's loaded by its src link<br>
&lt;gregwhitworth> ack dbaron<br>
&lt;brecht_dr> masonf this seems more like a rendering question instead of cloning question<br>
&lt;brecht_dr> dbaron I find the hydration example to be convincing<br>
&lt;scotto9> q+<br>
&lt;masonf> q+<br>
&lt;brecht_dr> gregwhitworth The root scenario in the issue is the most common use case that I would expect. I'd expect that to work<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;scotto9> q-<br>
&lt;brecht_dr> keithamus If I were make this as a web component, I would make the selected option a slot. and do manual slot assignment<br>
&lt;dbaron> s/to be convincing/to be convincing, particularly for whatever is selected on initial page load/<br>
&lt;bkardell_> it does seem very similar<br>
&lt;brecht_dr> keithamus I'm sure the implementation is different, but I wonder why we shouldn't do it, because I would do it like this as a web component<br>
&lt;bkardell_> I think if you find the &lt;mirror> proposal/ideas it used a similar argument<br>
&lt;gregwhitworth> +1 to bkardell_<br>
&lt;brecht_dr> masonf You'd actually have to clone, it should be in both places<br>
&lt;gregwhitworth> q?<br>
&lt;brecht_dr> masonf It needs to be cloned as you might want to style it differntly<br>
&lt;brecht_dr> gregwhitworth The same goes for accessibility reasons<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack masonf<br>
&lt;jarhar> Proposed resolution: The selectedoption element should update its contents when the selected options DOM descendants are modified<br>
&lt;brecht_dr> una There is a gmail example where there is an icon int he button and additional text in the dropdown when opening<br>
&lt;Luke> +1<br>
&lt;dandclark> +1<br>
&lt;keithamus> +1<br>
&lt;masonf> +1<br>
&lt;brecht_dr> +1<br>
&lt;jarhar> RESOLVED: The selectedoption element should update its contents when the selected options DOM descendants are modified<br>
&lt;scotto9> caveat - if you are dynamically updating the selected option with a counter/countdown - please stop<br>
&lt;scotto9> stop coding in general<br>
&lt;scotto9> :)<br>
</details>

<!-- comment id=2421017102 author=josepharhar created=2024-10-18T01:05:29Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2421017102 -->
## @josepharhar — 2024-10-18

reopening due to https://github.com/whatwg/html/issues/10520

<!-- comment id=2423479201 author=sorvell created=2024-10-19T02:08:47Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2423479201 -->
## @sorvell — 2024-10-19

Was the following alternative considered?

If the content of the `<selectedoption>` really should be able to be different from the "selected `<option>`" then perhaps it should just not be synchronized at all other than its selection state.

1. There are 2 new elements, not one: `<selectedoptions>` is a container of `<selectedoption>` elements. 
2. When an item is selected in the `<select>`, the matching same value or child index `<selectedoption>` matches `:checked` or a new pseudo-class. 
3. Only if no matching `<selectedoption>` exists, one is cloned and inserted into the `<selectedoptions>` element. This allows users to pre-seed alternate renderings for the "selected option" via HTML or Javascript. Custom element users who want to avoid weird cloning limitations might also take advantage of this (at least until custom cloning steps are added to custom elements).
4. And the default styling is `selectedoptions :not(:checked) { display: none;}`
5. Other than that special 1x cloning no other modifications of the DOM are made. No syncing. No removal of the child `<selectedoption>` elements. The pseudoclass is sufficient for styling and events for JS modification.

```html
<select>
  <button>
    <selectedoptions>
      <selectedoption value="a">custom a</selectedoption>
    </selectedoptions>     
  </button>
  <option value="a">a</option>
  <option value="b">b (this will be cloned when selected)</option>
</select>
```

<!-- comment id=2435779841 author=josepharhar created=2024-10-24T16:49:25Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2435779841 -->
## @josepharhar — 2024-10-24

I think that for more sophisticated cases where you don't want to clone but instead want to have other content that lives in the option which gets rendered when a particular option is selected, this could be implemented by not using the `<selectedoption>` element at all, and could also be done by frameworks.

We wanted to add this element to support basic cases so that most users of this new select would not have to use javascript to copy the innerHTML on every change event, but since its a separate element that you can choose to put in the button or not, we are providing flexibility to build more sophisticated things in userland.

Based on the discussions so far, and my experimentation of implementing different things and testing out basic cases with React, I am supportive of not observing mutations inside select or option elements and only cloning during initialization (parsing) and when new options become selected (when the change event fires). This is the first option in [jake's article](https://jakearchibald.com/2024/how-should-selectedoption-work/#option-nothing-by-default-but-provide-a-way-to-trigger-an-update).

At first I thought that not observing mutations would not work in basic cases, but I have been convinced otherwise after implementing it and experimenting with React, as well as the idea of adding a resetContent method that authors can use if needed.

<!-- comment id=2436124668 author=css-meeting-bot created=2024-10-24T18:55:05Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2436124668 resolution=true -->
## @css-meeting-bot — 2024-10-24

The Open UI Community Group just discussed ``select: Should `<selectedoption>` respond to mutations in the selected `<option>` ``, and agreed to the following:

* `RESOLVED: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> JakeA: when you select an option, or the default one becomes selected, the content of the selected option is cloned and light dom inserted inside of selectedoption, which is optional<br>
&lt;hdv> JakeA: my question is… while there's content in one of the options, waht should happen if you modify one of those options<br>
&lt;hdv> JakeA: originally it would get recloned, but based on feedback from webkit, who said it was a bit weird… so next step was to do it synchronously<br>
&lt;hdv> JakeA: problem with that was… a basic modification of the dom was going to mutate many many times<br>
&lt;hdv> JakeA: that's going to happen a lot more than people expect<br>
&lt;hdv> JakeA: i think the biggest problem with it is that there's no way to stop the clone or modification happening<br>
&lt;hdv> JakeA: there will be cases where people make a change to the content of the option because they want to change the content of that option<br>
&lt;hdv> JakeA: eg we don't want hover effects to be duplicated over<br>
&lt;hdv> JakeA: what makes me worried about this is that we get more and more elements that modify their own attributes… am not thrilled about it, but it seems to be the new hotness (hidden-until-found, details/summary, I think dialog does it too)<br>
&lt;hdv> JakeA: I know we can't put details inside option, but if we could it would expend in one place and also inside of the selected option<br>
&lt;hdv> JakeA: so I wondered… what do we do… I asked for developer feedback<br>
&lt;hdv> JakeA: what I heard the most… people who are using React or another framework are not going to use this<br>
&lt;masonf> q?<br>
&lt;brecht_dr> q+<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> JakeA: one simple thing we could do…is to say… when you select the option, it clones then, andit will never reclone unless you do it manually… and we could  potentially some easier way to do that<br>
&lt;hdv> JakeA: we already have `replaceChildren()`…the bit we're missing is `cloneChildren()`, that would provide an easy way to do it… or a method on `&lt;selectedoption>`<br>
&lt;gregwhitworth> ack brecht_dr<br>
&lt;hdv> brecht_dr: +1 on not changing the selectedoption more than once<br>
&lt;hdv> brecht_dr: oh ja<br>
&lt;hdv> s/oh ja//<br>
&lt;gregwhitworth> q+ steveOrvell<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> masonf: my concern was React, but think most people in React would use a portal or something and do it themselves, but in React it would be easy anyway to do it<br>
&lt;hdv> masonf: I had been thinking about a JS API on the select element that would sync the options<br>
&lt;hdv> masonf: you brought up clonechildren or a method on the selectedoption element<br>
&lt;gregwhitworth> q+ sarah<br>
&lt;hdv> masonf: they sound fine to me too<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> masonf: there are two things that cause the clone… the user or when it is originally parsed<br>
&lt;hdv> JakeA: the use case is, you've got an app that renders the custom select, then in the use effect it is going to get the real option and call setOptions<br>
&lt;hdv> JakeA: in that case selectedoption would still display 'loading' as React is still cloning… to get that faulty case I actually had to play around with it a bit… and in some cases eslint would scream at me too<br>
&lt;hdv> JakeA: @@@,  I know Anne isn't a fan of this,  but script element, style element already do this<br>
&lt;gregwhitworth> ack steveOrvell<br>
&lt;hdv> steveOrvell: today, the non custom select behaviour, if you modify selected option, it will synchronously update the selected option<br>
&lt;hdv> JakeA: how is it observably synchronous?<br>
&lt;hdv> steveOrvell: you can look at the offset width<br>
&lt;hdv> JakeA: of course<br>
&lt;hdv> steveOrvell: another point is re a11y: there are many ways to mess up the a11y of the custom select… the goal of Open UI is to not make things that make it easy to mess up accessibility.<br>
&lt;hdv> steveOrvell: the thing that is announced is the actual selected option , so anything you do visually would cause a discrepancy between what's visual and what's the accessible name<br>
&lt;hdv> JakeA: that's an important detail we need to make sure<br>
&lt;scott> q+<br>
&lt;hdv> masonf: what you just said Steve is a bug, what's there visual should be reflected in a11y tree, I'll make sure that gets fixed<br>
&lt;gregwhitworth> ack sarah<br>
&lt;hdv> sarah: for a11y I would prefer it to not change automatically… <br>
&lt;hdv> sarah: if you change the value that the keyboard focus is on, the 'screenreader cursor' would be pulled back to that. It treats the active descendant as @@@2<br>
&lt;hdv> sarah: but in this case focus seems to be actually on the button not the options, in which case it would not be a problem, as the screenreader user would get an equivalent experience<br>
&lt;hdv> sarah: also, if somebody is mutating the contents of the option element… they're obviously using script for this, it is trivial to manage what's displayed without using selectedoption … so in this case the dev likely has some kind of render function and we don;'t worry about handling that as much in selectedoption<br>
&lt;hdv> JakeA: seems reasonable<br>
&lt;hdv> JakeA: if the dev has chosen to not use selectedoption but they choose to put something in, screenreader users should see that. But if they are using selectedoption … if they choose to show/hide parts of the tree, it's important that screenreader users see that<br>
&lt;hdv> sarah: yes. Also screenreader users see one thing at the time<br>
&lt;hdv> sarah: at each point it doesn't matter if the selectedoption and option content are equivaelnt<br>
&lt;hdv> s/equivaelt/equivalent<br>
&lt;hdv> JakeA: somebody asked on HackerNews if they wanted different content in the option and selectedoption… I answered you would probably do it manually<br>
&lt;hdv> sarah: in that case it would work because display:none affects the acc tree<br>
&lt;hdv> JakeA: but my understanding is in current implementation it is broken<br>
&lt;hdv> masonf: currently is broken, we'll fix it<br>
&lt;hdv>  s/currently is/yes, it is currently<br>
&lt;hdv> gregwhitworth: I'm confused on what the actual problem is when I select again?<br>
&lt;hdv> masonf: no if the user did it it would clone twice<br>
&lt;hdv> JakeA: this particular user wanted instead of cloning the option that there is a …<br>
&lt;hdv> gregwhitworth: oh I didn't mean that user, I mean the general issue here<br>
&lt;hdv> JakeA: if we clone the DOM whenever anything changes, we'll get a load of clones, if it is asynchronously, and authors might not want that reflection<br>
&lt;hdv> masonf: we'll clone for the change 'user clicks', but the issue JakeA is talking about is when the author does some kind of modification via script<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> JakeA: it clones whenever the option that is selected changes<br>
&lt;keithamus> q+ steve<br>
&lt;gregwhitworth> ack scott<br>
&lt;hdv> scott: some of what I was going to say was brought up<br>
&lt;hdv> scott: I see people asking similar things re modifying options or changing the content of the selected option after the fact…<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> scott: people want to do complex stuff<br>
&lt;hdv> scott: people could mock around in the past, as steve says, but with this custom select it is much easier to make this a broken experience<br>
&lt;gregwhitworth> ack steve<br>
&lt;hdv> scott: re the bug to file I'm not sure if there's a clear answer to what value should be returned or announced<br>
&lt;hdv> sorvell: to go fruther down the rabbit hole… if you're modifying the DOM inside the `&lt;selectedoption>` via script, we can say, that's bad, don't do it  … is that confusing? (1) if we really didn't want it to happen, is there a way for us to say it cannot be modified? maybe we can tell people? (2) are we potentially limiting cool interactive patterns?<br>
&lt;hdv> sorvell: the cloning behaviour means you should not mess with this stuff<br>
&lt;hdv> JakeA: if you say the behaviour is it clones… it's quite simple, there's; an event when this changes<br>
&lt;hdv> JakeA: you can do it via CSS so why not via the DOM<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> masonf: since we're defining the behaviour… it's fine if you modify the contents of selectedoption, but you know it will be blown away when someone changes the option. Am afraid of inventing some kind of unmodifyable thing<br>
&lt;sarah> q+<br>
&lt;scott> q+<br>
&lt;hdv> masonf: current behaviour is to clone the children, we should probably also clone the attrs or maybe the whole option… we should probably think about that at least<br>
&lt;JakeA> q+<br>
&lt;hdv> masonf: there's some kind of algo of what happens when it changes, the author should be able to access that exact algorithm or evnet<br>
&lt;hdv> s/evnet/event<br>
&lt;hdv> gregwhitworth: we brought up what to clone, eg styles and everything… so should we be cloning the attributes? maybe if you narrow it down to specific attributes, it would become somewhat weird DX. I think we said not to do attrs because the style attribute would cause weird edge cases<br>
&lt;hdv> gregwhitworth: we're not changing how the tree is constructed, more naming… how are we breaking accessibility?<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> sarah: re cloning attributes… my immediate reaction is 'oh god no', the content of the thing is the value of the select not the name , if we clone an aria label of an option onto the button of the select, we've wiped out the name of the button, and then is someone uses aria-checked instead of aria-selected and that gets cloned… could be a very bad idea for anything that can affect semantics<br>
&lt;keithamus> q+ steve<br>
&lt;jarhar> proposed resolution: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;hdv> ack sarah<br>
&lt;gregwhitworth> ack sarah<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> ack scott<br>
&lt;hdv> scott: to allude to the codepen I've shared… what happens is that the aria label on the option is return as the value… which is not… there are going to be use cases where we'll absolutely want that<br>
&lt;hdv> scott: should we reflect all the attributes on the selectedoption for instance, I'd say no… right now, anything you put on the button or selected option is going to be ignored, it's not where the values are computed from<br>
&lt;hdv> scott: selectedoption… I'm really starting to think we should treat it like slot… I think we shouldn't compute the value from that<br>
&lt;hdv> scott: because then the selectedoption could be modified in other ways<br>
&lt;gregwhitworth> ack JakeA<br>
&lt;hdv> JakeA: would putting a div with aria-label inside of the option, would that avoid the problem when cloned into selectedoption?<br>
&lt;hdv> scott: this is another area we should align on what the expected behaviour should be… eg a div is not supposed to be named at all in the aria spec<br>
&lt;hdv> scott: as long as the option returns its value which is the same as its name, arguably that should become a returned value<br>
&lt;hdv> scott: if it matches the visible text that's up to the developer, sometimes you might not, in the currency picker that Una made there was some good reasons to purposefully pick what's the acc name, to avoid the £ sign getting read out twice<br>
&lt;jarhar> proposed resolution: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;hdv> s/twice/many times<br>
&lt;gregwhitworth> ack steve<br>
&lt;hdv> sorvell: people say different things about accessibility… previously we talked about what's in the option should be announced the same… and others say it should not be the same<br>
&lt;hdv> gregwhitworth: if somebody sets a thing to display:none in CSS… that affects acc tree too<br>
&lt;hdv> gregwhitworth: when I select an option, focus goes back to the outer button, correct?<br>
&lt;hdv> masonf: correct<br>
&lt;hdv> keithamus: we're in unchartered territory, could go either way<br>
&lt;jarhar> proposed resolution: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;keithamus> q+<br>
&lt;hdv> masonf: these are all great issues, we need to nail down what the a11y behaviour is, though I think that's not what we're resolving here<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;masonf> q?<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> keithamus: to +1 what masonf's says… the concern in the issue is what is the DX, because there are timing issues, it doesn't really affect the observable thing for the user?<br>
&lt;jarhar> proposed resolution: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;hdv> keithamus: re the original question: should the option's contents clone into the selectedoption on the activation of the option, that seems like a reasonable thing to me<br>
&lt;hdv> sorvell: the ability to put the same DOM into multiple places … this could result in weird things we should account for, it gives a lot of responsibility to folks customising their selects<br>
&lt;hdv> sorvell: you could put iframes in etc… this is an advanced use case and it can only go so far, but we want to try to make the default behaviour coudl<br>
&lt;hdv> s/coudl/good<br>
&lt;hdv> masonf: agreed with that… we're opening up great power here and that comes with great responsibility<br>
&lt;jarhar> proposed resolution: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;keithamus> +1<br>
&lt;hdv> masonf: am on the queue to say: I would like to withdraw my proposal to @@@3 and still keep my proposal to add some kind of  'synchronise selected option' method<br>
&lt;JakeA> +1<br>
&lt;hdv> +1<br>
&lt;sorvell> +1<br>
&lt;brecht_dr7> +1<br>
&lt;sarah> +1<br>
&lt;jarhar> RESOLVED: dont observe mutations in option elements. only clone into selectedoption during parsing and when a new option becomes selected<br>
&lt;brecht_dr7> (on another name, irc did something strange, no with nr 7)<br>
&lt;keithamus> q+<br>
&lt;hdv> masonf: is #﻿825 a v2 thing?<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;hdv> masonf: I would like a JS method that does exactly what the browser does when someone selects an option<br>
&lt;hdv> keithamus: I agree with that<br>
&lt;hdv> keithamus: and also… in multiple selects the options are an array, there's something you'll have to do there anyway<br>
&lt;hdv> keithamus: probably something to resolve there<br>
&lt;hdv> JakeA:  like the proposal, what the method name is etc is up for grabs<br>
&lt;hdv> sorvell: I wanted to zero in on one thing Jake said… when cloning occurs, we won't rely on an event?<br>
&lt;hdv> JakeA: I think that was a misunderstanding of what gregwhitworth was saying… he said when there is an event when does it occur?<br>
&lt;hdv> gregwhitworth: let's resolve on the spirit, then the spec text<br>
&lt;hdv> JakeA: eg if you change checked attr on a checkbox you don't get an event either<br>
&lt;hdv> JakeA: can't remember what the programmatic ways are to make changes… eg if you set selectedindex, that is 1, to 1, would that reclone or not, because you set it to the same?<br>
&lt;hdv> sorvell: are we ok with the developer being unable to respond to that?<br>
&lt;hdv> JakeA: you could put a mutationobserver on the selected option<br>
&lt;hdv> sarah: are you saying… if you have one set of options rendered, when user clicks on the same index but it is the same option it would not fire a change event?<br>
&lt;hdv> JakeA: I don't have an answer, would be something we'd need to decide<br>
&lt;hdv> masonf: even if you don't mutate but the user clicks the same option that's already selected, feels to me it should still clone<br>
&lt;hdv> jakea: selectedindex = selectedindex  gives you that clone operation<br>
</details>

<!-- comment id=2436275443 author=josepharhar created=2024-10-24T20:25:25Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2436275443 -->
## @josepharhar — 2024-10-24

Since people sounded more interested in accessibility mappings of the button than the cloning behavior, I created a separate issue here so we can talk about it: https://github.com/openui/open-ui/issues/1116

<!-- comment id=2437402170 author=jakearchibald created=2024-10-25T10:09:44Z url=https://github.com/openui/open-ui/issues/825#issuecomment-2437402170 -->
## @jakearchibald — 2024-10-25

https://github.com/openui/open-ui/issues/1119 follow up on when the cloning should happen in relation to selecting the selected option.
