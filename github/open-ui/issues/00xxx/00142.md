---
number: 142
title: "Modify button part behavior for <select multiple>?"
state: "closed"
labels: ["needs-triage", "select"]
author: "dandclark"
created_at: "2020-07-23T23:33:11Z"
closed_at: "2025-06-09T22:41:03Z"
url: "https://github.com/openui/open-ui/issues/142"
comments: 4
has_resolution: false
synced_at: "2026-08-27T14:25:56Z"
---

In the `<select>` anatomy, there are a couple of spots where we don't modify `open` or `aria-expanded` if the `multiple` attribute is set, e.g. see [listbox](https://open-ui.org/components/select#part-listbox) `keydown(enter)`.  However for [button](https://open-ui.org/components/select#part-button) there's nothing to indicate that it should not toggle these attributes, or that it should not appear at all, if `multiple` is present.  So there's an inconsistency here.

Adding an extra layer of difficulty is the fact that for Safari/Chrome/Edge on iOS there is actually still a visible button for `<select multiple>` that opens a popover with checkable options.  So, the button not affecting the `open` state of the popup if `multiple` is set isn't even universally true if we consider iOS.

<!-- comment id=1073136937 author=github-actions[bot] created=2022-03-20T00:33:38Z url=https://github.com/openui/open-ui/issues/142#issuecomment-1073136937 -->
## @github-actions[bot] — 2022-03-20

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2013260472 author=josepharhar created=2024-03-21T18:36:59Z url=https://github.com/openui/open-ui/issues/142#issuecomment-2013260472 -->
## @josepharhar — 2024-03-21

I think that multi select will have a button and a popup, in which case we won't have to do anything differently with the button.

<!-- comment id=2357264851 author=github-actions[bot] created=2024-09-18T00:43:33Z url=https://github.com/openui/open-ui/issues/142#issuecomment-2357264851 -->
## @github-actions[bot] — 2024-09-18

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=2957200547 author=josepharhar created=2025-06-09T22:41:03Z url=https://github.com/openui/open-ui/issues/142#issuecomment-2957200547 -->
## @josepharhar — 2025-06-09

`<select multiple>` will have a replaceable button part when rendering in the `size=1` mode, and won't have a button when rendered as an in-page listbox when the size attribute is not set or has size > 1: https://github.com/openui/open-ui/issues/1102

If open or aria-expanded aren't set accordingly, then it will be a bug in the implementation.
