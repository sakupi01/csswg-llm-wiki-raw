---
number: 995
title: "[focusgroup] Interactions with native elements (that have focusgroup-like behavior built-in)"
state: "closed"
labels: ["focusgroup", "needs-triage"]
author: "travisleithead"
created_at: "2024-02-21T22:35:11Z"
closed_at: "2026-06-04T18:14:44Z"
url: "https://github.com/openui/open-ui/issues/995"
comments: 3
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

Putting some thoughts down here for discussion. This is currently an open question in the spec.
(This issue was split off #990)

How to treat native elements that already have a focusgroup-like behavior? This could go various ways. One way is to treat elements like `<select>` as if they declare their own internal focusgroup (which I guess they do, sort of), and to have author-defined focusgroups not apply in those cases.

Another direction is to try to blend the behavior or allow author-supplied focusgroups to customize (overwrite?) the pre-existing focusgroup-like behavior of native elements (like `<select>`). I can see this being helpful, for example, to apply a direction constraint (horizontal or vertical) to the native control so that the keyboard interaction of the native element fits better within an existing containing focusgroup (or to change the wrapping behavior).

With [exclusive accordions](https://open-ui.org/components/accordion.explainer/) and existing named radio buttons, there can be focusgroup-like behavior applied without a containing element. It would require a new method of attaching focusgroups to allow an author-defined focusgroup to override the arrow-key behavior of either one of these parent-less grouped controls. (Perhaps something like `focusgroupfor=name_of_radio_or_exclusive_accordion` to bind it up.)

And with all that, there are still controls like `input type=text`, `input type=number`, `input type=range` that trap arrow key behavior and should probably not be affected by focusgroups.

<!-- comment id=1984307286 author=css-meeting-bot created=2024-03-07T19:46:15Z url=https://github.com/openui/open-ui/issues/995#issuecomment-1984307286 resolution=true -->
## @css-meeting-bot — 2024-03-07

The Open UI Community Group just discussed `[focusgroup] Interactions with native elements (that have focusgroup-like behavior built-in)`, and agreed to the following:

* `RESOLUTION: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements).`
* `RESOLVED: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements).`

<details><summary>The full IRC log of that discussion</summary>
&lt;Penny> Comment<br>
&lt;masonf> scribenick: Penny<br>
&lt;Penny> scribenick: Penny<br>
&lt;argyle> test home/end/cmd+arrow behavior in a focusable scroller area in this prototype https://codepen.io/argyleink/pen/wvZMMZv<br>
&lt;philippg> q-<br>
&lt;Penny> @Travis: What do we do when author put focus groups or attempt to put focus groups on native elements that bindings to page up, page down, or others that are conflicting such as textarea, radiobuttons.<br>
&lt;masonf> q+<br>
&lt;scotto_> q+<br>
&lt;Penny> @travis: Also How do we even attach a focus group to a group of radiobuttons since there's no parent element?<br>
&lt;keithamus> q+<br>
&lt;dandclark> q+<br>
&lt;Penny> @masonf: seems like the thing to do is to have a list of elements that are not allowed<br>
&lt;Penny> @masonf: Ones that have no meaningful use case, disallow<br>
&lt;masonf> ack mason<br>
&lt;masonf> ack scott<br>
&lt;masonf> q+<br>
&lt;masonf> ack keith<br>
&lt;Penny> @scotto_: Maybe we can now more narrowly define behavior for these things?<br>
&lt;Penny> @keithamus: There is an upcoming spec change to allow a focusgroup on an element to be inclusive of the element, it's exclusive but the change would make it inclusive.<br>
&lt;masonf> ack dan<br>
&lt;dandclark> https://dom.spec.whatwg.org/#valid-shadow-host-name<br>
&lt;Penny> @dandclark: Question of whether this should be an allow list or block list. Start with a pre-existing list to be proposed<br>
&lt;bkardell_> the sanitizer api has a list of "funky elements" https://wicg.github.io/sanitizer-api/#handle-funky-elements :-p<br>
&lt;Penny> @masonf: allow list seems the better approach, we can enumerate a list on the issue<br>
&lt;masonf> ack mason<br>
&lt;scotto_> potential allow list elements - article, section, nav, aside, hgroup, header, footer, address, ol, ul, menu dl, search, div, custom elements, table, fieldset, li, dd<br>
&lt;Travis> PROPOSED RESOLUTION: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements.<br>
&lt;bkardell_> oh I wasn't proposing that list<br>
&lt;Travis> PROPOSED RESOLUTION: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements).<br>
&lt;Travis> RESOLUTION: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements).<br>
&lt;Travis> RESOLVED: propose a list of allowed elements that may have the focusgroup attribute (based on preexisting lists such as valid shadow hosts/ sanitizer funky elements).<br>
</details>

<!-- comment id=2327693907 author=github-actions[bot] created=2024-09-04T00:42:15Z url=https://github.com/openui/open-ui/issues/995#issuecomment-2327693907 -->
## @github-actions[bot] — 2024-09-04

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=4624917360 author=janewman created=2026-06-04T18:14:44Z url=https://github.com/openui/open-ui/issues/995#issuecomment-4624917360 -->
## @janewman — 2026-06-04

This is handled with the concept of "key-conflict" elements, see: https://open-ui.org/components/scoped-focusgroup.explainer/#key-conflicts
