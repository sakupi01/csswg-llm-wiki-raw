---
number: 1119
title: "select: Should `<selectedoption>` update when selecting the already-selected option"
state: "closed"
labels: ["select"]
author: "jakearchibald"
created_at: "2024-10-25T10:09:07Z"
closed_at: "2024-12-11T18:02:51Z"
url: "https://github.com/openui/open-ui/issues/1119"
comments: 7
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

This is following up on the resolution of https://github.com/openui/open-ui/issues/825, where the selected `<option>`'s content is only cloned when the selection is changed, and not when the _content_ of the selected `<option>` changes.

Which of the following cases should result in the triggering of the clone operation, even though the result is the same option being selected:

- The user opens the picker and closes it without making a selection
- The user opens the picker and clicks on the `<option>` that is already selected
- `select.value = select.value`
- `select.selectedIndex = select.selectedIndex`
- `select.selectedOptions[0].replaceWith(select.selectedOptions[0])`
- `select.append(document.createElement('optgroup'))`
- `form.reset()`

---

My opinions:

- The user opens the picker and closes it without making a selection

No change. Nothing happened.

- The user opens the picker and clicks on the `<option>` that is already selected

Now I've thought about it, I don't think this should re-clone. [The spec seems a little loose on this](https://html.spec.whatwg.org/multipage/form-elements.html#dom-select-value:~:text=If%20the%20multiple%20attribute%20is%20absent%2C%20and,and%20then%20send%20select%20update%20notifications.) but browser don't fire `change` or `input` when selecting the already selected option, so internally this seems like a no-op.

- `select.value = select.value`
- `select.selectedIndex = select.selectedIndex`

I think these should trigger the cloning operation, since it's an explicit set of the value. Although my feelings on this aren't strong.

- `select.selectedOptions[0].replaceWith(select.selectedOptions[0])`

This should clear the `<selectedoption>` when the selected `<option>` is removed, then trigger the cloning operation when the `<option>` is reinserted.

- `select.append(document.createElement('optgroup'))`

This should not trigger the cloning operation. Nor should adding an `<option>` unless it become the new selected option.

- `form.reset()`

This should trigger the cloning operation only if the selected option changes to another option.

<!-- comment id=2437982887 author=sorvell created=2024-10-25T14:32:18Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2437982887 -->
## @sorvell — 2024-10-25

Another case to consider: the select dynamically becomes rendered as a custom select via, say, adding a class to it that sets appearance.

<!-- comment id=2437986404 author=sorvell created=2024-10-25T14:33:48Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2437986404 -->
## @sorvell — 2024-10-25

> My opinions:

... seem reasonable.

<!-- comment id=2441363006 author=josepharhar created=2024-10-28T11:48:24Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2441363006 -->
## @josepharhar — 2024-10-28

> * The user opens the picker and clicks on the `<option>` that is already selected
> 
> Now I've thought about it, I don't think this should re-clone. [The spec seems a little loose on this](https://html.spec.whatwg.org/multipage/form-elements.html#dom-select-value:~:text=If%20the%20multiple%20attribute%20is%20absent%2C%20and,and%20then%20send%20select%20update%20notifications.) but browser don't fire `change` or `input` when selecting the already selected option, so internally this seems like a no-op.
> 
> * `select.value = select.value`
> * `select.selectedIndex = select.selectedIndex`
> 
> I think these should trigger the cloning operation, since it's an explicit set of the value. Although my feelings on this aren't strong.

Making a distinction between these two feels a bit inconsistent to me, but making re-assigning the value from script trigger cloning means that we don't need a separate method to do so.

It looks like the spec already says that assigning to select.value resets a bunch of stuff, so no special casing would be needed for this hopefully:

> On setting, the [value](https://html.spec.whatwg.org/multipage/form-elements.html#dom-select-value) attribute must set the [selectedness](https://html.spec.whatwg.org/multipage/form-elements.html#concept-option-selectedness) of all the [option](https://html.spec.whatwg.org/multipage/form-elements.html#the-option-element) elements in the [list of options](https://html.spec.whatwg.org/multipage/form-elements.html#concept-select-option-list) to false, and then the first [option](https://html.spec.whatwg.org/multipage/form-elements.html#the-option-element) element in the [list of options](https://html.spec.whatwg.org/multipage/form-elements.html#concept-select-option-list), in [tree order](https://dom.spec.whatwg.org/#concept-tree-order), whose [value](https://html.spec.whatwg.org/multipage/form-elements.html#concept-option-value) is equal to the given new value, if any, must have its [selectedness](https://html.spec.whatwg.org/multipage/form-elements.html#concept-option-selectedness) set to true and its [dirtiness](https://html.spec.whatwg.org/multipage/form-elements.html#concept-option-dirtiness) set to true.

<!-- comment id=2445318572 author=josepharhar created=2024-10-29T20:58:37Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2445318572 -->
## @josepharhar — 2024-10-29

Proposed resolution: adopt Jake's suggestions: Clone when re-assigning to select.value, don't clone when user selects the same option again

<!-- comment id=2450565150 author=css-meeting-bot created=2024-10-31T18:30:02Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2450565150 resolution=true -->
## @css-meeting-bot — 2024-10-31

The Open UI Community Group just discussed ``select: Should `<selectedoption>` update when selecting the already-selected option``, and agreed to the following:

* `RESOLVED: adopt Jake's suggestions: Clone when re-assigning to select.value, don't clone when user selects the same option again`

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> jarhar: Jake made this issue and listed a bunch of scenarios that may or may not clone into the selectedcontent element<br>
&lt;gregwhitworth> jarhar: I agree with him that we want to adopt them. The gist of it is when the user interacts with the select where an option is not selected it will not trigger the clone<br>
&lt;gregwhitworth> jarhar: but if it tiggers a .value change then it will fire the re-clone<br>
&lt;masonf> s/tiggers/triggers<br>
&lt;gregwhitworth> jarhar: if we trigger a clone on re-assign on .value we don't need a new method to trigger the clone. Doesn't mean we don't add a method later but we don't NEED one now<br>
&lt;masonf> q+ sorvell<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> jarhar: that's why it makes sense to me and reading the HTML spec we should get this for free<br>
&lt;masonf> ack sorvell<br>
&lt;gregwhitworth> sorvell: the platform generally does what is annoying but if you re-assign an attribute to the same element and an element is being observed then it will be observed. This would make it consistent<br>
&lt;gregwhitworth> sorvell: I agree with Jake's proposals but I added one, if you change custom to not custom then it should re-clone in that case<br>
&lt;gregwhitworth> masonf: we should list it explicitly anyways<br>
&lt;scott> q+<br>
&lt;gregwhitworth> masonf: explicitly it is taking Jake's suggestions and some of them don't trigger a clone<br>
&lt;gregwhitworth> ack masonf<br>
&lt;jarhar> q+<br>
&lt;gregwhitworth> masonf: plus Steve's proposal<br>
&lt;gregwhitworth> ack scott<br>
&lt;gregwhitworth> scott: only a question about when a clone would happen is if someone modifies the chosen option and then re-selects. I'm seeing headshake no but what would be populated in there isn't being re-cloned if it has been changed<br>
&lt;gregwhitworth> masonf: let's say that you have a RAF on the option that is changing then you would be re-cloning over and over<br>
&lt;sarah> q+<br>
&lt;gregwhitworth> scott: I agree overall, just thinking through what the dev is doing<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> masonf: the dev should blow away the options if they want to change them<br>
&lt;gregwhitworth> sorvell: I wish Jake were here, the case that would possibly be problematic is if the Option is updated. For example the first option is saying Loading and then that changes and you update them it's still going to say loading and the VDOM will change the content<br>
&lt;gregwhitworth> sorvell: The feedback was that you need to know what you're doing<br>
&lt;gregwhitworth> jarhar: I do not think the clone should trigger when the appearance property changes<br>
&lt;gregwhitworth> jarhar: the cloning is going to do its thing whatever the CSS is<br>
&lt;gregwhitworth> jarhar: if you're in appearance: auto we're still going to do the cloning regardless<br>
&lt;sarah> eh, I was just going to go on the queue to say essentially what masonf and Steve already said, and also that if a dev is modifying options in-place for e.g. virtualization, they almost certainly are also modifying the value/selection programmatically, otherwise things would probably break anyway (and display the wrong selection styles on the option<br>
&lt;sarah> as well)<br>
&lt;sarah> q-<br>
&lt;masonf> ack jarhar<br>
&lt;gregwhitworth> jarhar: we have a method in the element and if we recalculate the styles that's where I would put this it may result in odd re-flow issues and be complicated to spec<br>
&lt;gregwhitworth> masonf: selectedcontent always gets cloned<br>
&lt;jarhar> q?<br>
&lt;gregwhitworth> sorvell: It's confusing to me but to your point that it's always there that means it can be unrelated thank you for the explanation<br>
&lt;masonf> gregwhitworth: steve covered "what updating the option" means. E.g. I have 5 options, I wrote JS to change contents of existing options, what happens?<br>
&lt;masonf> gregwhitworth: Is that going to be an issue? I'll be changing the values, even in the vdom case, it'll work right? Unless they changed the content of the options, with the same values.<br>
&lt;bkardell_> q+<br>
&lt;jarhar> q?<br>
&lt;jarhar> q+<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;sorvell> q+<br>
&lt;jarhar> q-<br>
&lt;gregwhitworth> masonf: modification of the select vs the option and you choose a different option will result in a re-clone but monkeying with the options will not re-clone and in order to do that you should remove the option and put a whole new option element in its place<br>
&lt;gregwhitworth> ack bkardell_<br>
&lt;masonf> ack bkardell<br>
&lt;gregwhitworth> bkardell_: I'm also confused, I don't think that Loading.... thing is comperable<br>
&lt;gregwhitworth> bkardell_: you wouldn't replace that one and produce the new one. You would loop through them and produce new options<br>
&lt;gregwhitworth> steve: the point of it is that in the VDOM it will change the diff between the work<br>
&lt;gregwhitworth> bkardell_: people could _do_ a lot of things but I don't see anyone doing that<br>
&lt;gregwhitworth> sorvell: we'll make sure to have them key their elements to make sure and we'll need to contend with the fact that this isn't a complete corner case<br>
&lt;gregwhitworth> sarah: they all kind of work that and the laziest devs default will be to update in place<br>
&lt;gregwhitworth> sarah: yeah, kind of<br>
&lt;gregwhitworth> masonf: it will be visually obvious and they'll add the keys on there like they should to<br>
&lt;gregwhitworth> q?<br>
&lt;masonf> ack sorvell<br>
&lt;gregwhitworth> ack sorvell<br>
&lt;gregwhitworth> sorvell: I'm proxying Jake and I think in a poor manner we're making an intentional tradeoff<br>
&lt;gregwhitworth> sorvell: things can get out of whack but practically speaking it's a valid tradeoff and informing devs what to do<br>
&lt;masonf> Proposed resolution: adopt Jake's suggestions: Clone when re-assigning to select.value, don't clone when user selects the same option again<br>
&lt;jarhar> Proposed resolution: adopt Jake's suggestions: Clone when re-assigning to select.value, don't clone when user selects the same option again<br>
&lt;gregwhitworth> masonf: I think that was great in that there are downsides on either side but it's the other side that has more dragons and this is the safer side<br>
&lt;sarah> +1<br>
&lt;masonf> +1<br>
&lt;jarhar> RESOLVED: adopt Jake's suggestions: Clone when re-assigning to select.value, don't clone when user selects the same option again<br>
&lt;sorvell> +1<br>
</details>

<!-- comment id=2495730786 author=gregwhitworth created=2024-11-24T01:12:13Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2495730786 -->
## @gregwhitworth — 2024-11-24

@josepharhar has the edits for this change landed, since there is the upstream WHATWG PR I'm curious if we need this issue still open.

<!-- comment id=2536721316 author=josepharhar created=2024-12-11T18:02:51Z url=https://github.com/openui/open-ui/issues/1119#issuecomment-2536721316 -->
## @josepharhar — 2024-12-11

Thanks for the reminder, updates here:
- https://chromium-review.googlesource.com/c/chromium/src/+/6086797
- https://github.com/whatwg/html/pull/10633#issuecomment-2536720523
