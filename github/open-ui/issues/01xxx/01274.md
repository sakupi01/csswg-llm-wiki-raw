---
number: 1274
title: "[menu] Soft disabling menuitem elements"
state: "open"
labels: ["menu"]
author: "domfarolino"
created_at: "2025-09-03T20:45:12Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1274"
comments: 3
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

Some of us working on the menu elements proposal recently learned that the [APG recommends soft disabling menuitem elements](https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#focusabilityofdisabledcontrols) when they have the `disabled`. My understanding of soft disabling is that it essentially makes `disabled=true` elements non-interactive/activatable, while still keyboard reachable and focusable (maybe these last two are the same?).

Given this, we should probably specify that this soft-disabled behavior applies to `<menuitem disabled>` elements, as opposed to the default "hard disabled" behavior that applies to things like `<button disabled>` (making them unfocusable, unclickable, and keyboard unreachable). Here are some follow-up questions regarding the exact behavior of soft disabling:

 - **Should `:hover` styles apply?**
    - My recommendation: Yes. Hover styles still apply to hard disabled elements (e.g., `<button disabled>`), so we should not deviate

 - **Should hover focus/activate a disabled menuitem?**
    - The APG allows for focus and activation to follow hover for menuitems (see https://www.w3.org/WAI/ARIA/apg/patterns/menubar/examples/menubar-editor/#:~:text=In%20general%2C%20moving,focus%20follows%20hover); basically, if the user activates a `<menuitem>` in a `<menubar>` (for example, by clicking it), and proceeds to hover over a sibling `<menuitem>`, focus and activation will move to the new `<menuitem>`
    - What happens if the user proceeds to hover over a `<menuitem disabled>` sibling? Should focus follow? Should activation follow?
    - My recommendation is that focus/hover should follow, but the `<menuitem disabled>` should not be activated, i.e., it will not invoke its corresponding sub-`<menulist>` popover

 - **Should we focus `<menuitem disabled>` elements on click?**
    - When you click a hard disabled element, the platform neither fires a `click` event at the element, nor focuses it
    - If we want to `<menuitem disabled>` to be focusable at the very least by keyboard navigation, should we let clicking draw in focus too? Should that fire `click` events? (Probably not)
    - My recommendation: clicking/tapping the `<menuitem disabled>` element will focus it, but not activate it. Firing `click` or other activation events could confuse developer JS. The `focus` event will still fire

In short, it seems that a `<menuitem disabled>` should never be able to be activated, although focus and hover should generally behave as if it were. This seems like the best tradeoff for users of accessibility technology, but I'm new to this whole "soft disabling" concept, so please tell me where I'm wrong.

<!-- comment id=3250772064 author=lukewarlow created=2025-09-03T20:56:58Z url=https://github.com/openui/open-ui/issues/1274#issuecomment-3250772064 -->
## @lukewarlow — 2025-09-03

I think this effectively means the element should be aria-disabled=true with an early return in the elements activation behaviour. What that means for click events or .click() idk.

It's worth checking what the actual aria spec says here not just APG.

>  Hover styles still apply to hard disabled elements (e.g., <button
disabled> ), so we should not deviate

Is that actually true? If hard disabled elements still match :hover then we should definitely follow suit with this. Even if they don't I think hover should probably still match given :focus can.

We should of course ensure we have some sort of default styles to convey this state though.

<!-- comment id=4089273588 author=lukewarlow created=2026-03-19T10:50:05Z url=https://github.com/openui/open-ui/issues/1274#issuecomment-4089273588 -->
## @lukewarlow — 2026-03-19

> i'd personally rather there be a new feature for "soft disabling" controls, rather than have the disabled attribute work differently depending on what context its used in - and thus also making it more work for someone to achieve standard disabling behavior, if they wanted to do that.
> 
> The guidance to provide soft disabling of controls, as mentioned by the apg, also talks about how its important to provide consistent behavior for disabled states. hence, imo why it should be the choice of the author whether or not controls can be disabled, but focusable, and also it shouldn't necessarily be limited to just elements within a menu or toolbar. E.g., why not within a select/listbox?

cc @scottaohara  in https://github.com/openui/open-ui/issues/1419#issuecomment-4054782968

<!-- comment id=4433923848 author=dbaron created=2026-05-12T19:18:54Z url=https://github.com/openui/open-ui/issues/1274#issuecomment-4433923848 -->
## @dbaron — 2026-05-12

One note about native menus:  it looks to me based on testing of keyboard navigation across disabled menuitems that (assuming my testing is reaching actual native menus rather than a simulation of them within a native app):
* Windows does soft-disabling of disabled menuitems
* Mac and GTK do hard-disabling

Looking at some existing web menus:
* Google Docs and Figma do hard-disabling

So I think I tend to agree with @scottaohara in https://github.com/openui/open-ui/issues/1419#issuecomment-4054782968 (which I admit was about toolbars rather than menus!).  I think it's not clear that we want to make the existing disabling feature have a different behavior for menuitems when both (1) that behavior difference is far from universal (although certainly many users are exposed to it via the Windows behavior) and (2) it's not clear that the desire for that feature is specific to menus (relative to, say, other things that have focusgroup-like behavior).
