---
number: 1063
title: "[select] `<selectedoption for=id>` as an alternative to split buttons and `<button type=popover>`"
state: "open"
labels: ["needs-triage", "select"]
author: "josepharhar"
created_at: "2024-06-18T16:25:22Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1063"
comments: 7
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

Our latest idea is that in order to support "split buttons" where one button has the `<selectedoption>` and does something author defined and another button actually opens the popover, we support slotting multiple `<button>`s inside a `<select>` into the in-page portion and signify which button opens the popover with `<button type=popover>`.

Instead of doing this, we could make `<selectedoption>` capable of living outside of the `<select>` and link them together by supporting the existing `for` attribute on `<selectedoption>`. This has some benefits:
1. Removes the restriction of only using `<selectedoption>` inside a `<button>`.
2. Removes the `<button type=popover>` attribute and complicated algorithms to figure out which button(s) open the popover, which I am [still trying to figure out how to implement](https://chromium-review.googlesource.com/c/chromium/src/+/5495954).
3. Might make accessibility mapping and other things easier since only one button is allowed inside the select @scottaohara 
4. Might make select changes easier to standardize since I don't have to add a `type=popover` attribute.

Current way of making a split button:
```html
<select>
  <button>i submit a form or something <selectedoption></selectedoption></button>
  <button type=popover>open dropdown</button>
  ...
</select>
```

What I am proposing:
```html
<button>i submit a form or something <selectedoption for=myselect></selectedoption></button>
<select id=myselect>
  <button>open dropdown</button>
</select>
```

In cases where the authors want the layout boxes of the two buttons to be siblings, they can put display:contents on the `<select>`. I think this also might be worth doing by default in the UA stylesheet.

<!-- comment id=2194840033 author=scottaohara created=2024-06-27T14:22:48Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2194840033 -->
## @scottaohara — 2024-06-27

thanks for raising this joey as this is exactly the direction i was hoping for when i proposed the idea in the discussions we've been having about select.

if selectedoption can be associated with a `select` declaratively, then authors can get that value pulled into their own button that they can script to do whatever it needs to do - without having to put it inside the `select` itself.

it also makes it a lot easier for browsers to implement, as the select's mappings don't need to potentially be variable to account for extra content presented in the page along with the select's button part for invoking the popup - it also then means that a browser is off the hook for having to determine "is this a split button, and if so, do keyboard behavior as a single tab stop - or is this not a split button and just happens to be two buttons in this select container, and thus two separate tab stops would be expected... etc."

this direction simplifies a lot, and is not an unreasonable expectation to put on developers - particularly since developers that were making well supported accessible split buttons (and other composite custom controls) would likely be already following this sort of practice of having the controls be siblings, rather than nested, anyway.

<!-- comment id=2194897220 author=josepharhar created=2024-06-27T14:33:58Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2194897220 -->
## @josepharhar — 2024-06-27

One more thing I forgot to clarify - if you put a `<selectedoption>` inside a `<select>`, then the association will be set up automatically without the need for the `for` attribute, so this will still work:
```html
<select>
  <button>
    <selectedoption></selectedoption>
  </button>
  ...
</select>
```

<!-- comment id=2195407344 author=css-meeting-bot created=2024-06-27T18:21:05Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2195407344 -->
## @css-meeting-bot — 2024-06-27

The Open UI Community Group just discussed ``[select] `<selectedoption for=id>` as an alternative to split buttons and `<button type=popover>` ``, and agreed to the following:

* ``RESOLVED: add an attribute called `selectedoptionelement=foo` to select, which can point to a single <selectedoption> element to update. The first <selectedoption> contained within the <select> would also be updated.``

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> jarhar: I mentioned this idea that I wrote about some a11y stuff<br>
&lt;gregwhitworth> jarhar: the idea is that we have support for split button and it will slot in numerous items and if it has type=popover is that it will denote which one toggles the popover<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> jarhar: the idea that I see is where you have a button that opens the select within another button<br>
&lt;gregwhitworth> jarhar: the selectedoption element just looks up its ancestor tree to find out where to get its selected option from<br>
&lt;gregwhitworth> jarhar: if we have a for attribute we can have the selectedoption element get its content from somewhere else<br>
&lt;gregwhitworth> jarhar: you can then have the two buttons and it provides the improvements in the implementation and we don't have a new button type attribute and when you want to put the selectedoption elsewhere<br>
&lt;gregwhitworth> masonf: I love the idea<br>
&lt;gregwhitworth> masonf: split buttons are a real thing that we need to support<br>
&lt;gregwhitworth> masonf: I like not having two buttons that are magical<br>
&lt;bkardell_> yeah that was my immediate thought too<br>
&lt;gregwhitworth> masonf: one comment I would make, actually from Anne, one tweak is rather than having an attribute that is on select that gets the selectedoption element<br>
&lt;scotto6> q+<br>
&lt;gregwhitworth> masonf: it avoids having numerous elements around the document<br>
&lt;gregwhitworth> ack masonf<br>
&lt;jarhar> q?<br>
&lt;gregwhitworth> masonf: it gets it so that implementation is easier to look it up due to referring in the other direction<br>
&lt;gregwhitworth> scotto: I was going to thank jarhar for writing this up and the reversal makes a lot of sense so thank you for the idea Anne<br>
&lt;gregwhitworth> ack scotto<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> jarhar: I still kind of like it being on selectedoption having for<br>
&lt;scotto6> q+<br>
&lt;gregwhitworth> jarhar: I think that updating multiple other selectedoption elements wouldn't be too complicated versus one<br>
&lt;gregwhitworth> jarhar: I like the idea being able to quickly looking it up on the select but in general I'm happy with either of these verses our current approach<br>
&lt;gregwhitworth> jarhar: do you have any idea of what the attribute on the select would be called?<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> masonf: naming wise, I think we should pick a bad name and people will change it to something good :)<br>
&lt;gregwhitworth> masonf: one question I was on the queue to ask is about usecases<br>
&lt;gregwhitworth> masonf: in the inverted case you only have one selectedoption you can refer to but do you only allow just those two?<br>
&lt;jarhar> q?<br>
&lt;gregwhitworth> jarhar: if we do an inverted style where you can only specify one then you shouldn't support any selectedoption that are inside of the select or else we'll end up with a list of them that we'll have to clone stuff into<br>
&lt;gregwhitworth> masonf: to make it easier to comment on, my proposal would be to support both but it would just be two<br>
&lt;gregwhitworth> scotto: the only thing I was going to say, for the labele element on a form control... could it not be that there are multiple selected options in the DOM that the browser can be "no more after that"<br>
&lt;gregwhitworth> scotto: the browser could treat it that way but it won't render it all over the place and stops cloning after that except for inside of the select element<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> ack scotto<br>
&lt;gregwhitworth> scotto: for the label for it can only have one id it won't label every single one that has that ID it will only label the first one<br>
&lt;gregwhitworth> mfreed: if you have two labels with one control does that still work?<br>
&lt;gregwhitworth> scotto: you have to concat them together to make the acc name<br>
&lt;gregwhitworth> scotto: it wouldn't be cloning them into separate options.<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;gregwhitworth> masonf: my only comment was to scotto thing. We're moving towards agreement of at max two, one outside and one inside<br>
&lt;jarhar> q+<br>
&lt;gregwhitworth> masonf: it seems a bit funny to support &lt;n> selectedoptions but the first one works where the other way around there is only one and I think it feels cleaner<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> jarhar:<br>
&lt;gregwhitworth> jarhar: I thought of another reason that the inversion thing is good is that we can incrementally introduce this on other elements<br>
&lt;jarhar> proposed resolution: add the selectedoption attribute to the select element which is an idref to a selectedoption element which lives outside of the select element as a replacement for multiple buttons in select and type=popover<br>
&lt;gregwhitworth> jarhar: if we want to have an input with a selected option and could be feature detected on IDL<br>
&lt;masonf> Proposed resolution: add an attribute called `myselectedoptionelementis=foo` to select, which can point to a single &lt;selectedoption> element to update. The first contained &lt;selectedoption> would also be updated.<br>
&lt;gregwhitworth> masonf: it differs only in that it has a named attribute and outlines that the first child of the selectedoption will be updated<br>
&lt;masonf> Proposed resolution: add an attribute called `selectedoptionelement=foo` to select, which can point to a single &lt;selectedoption> element to update. The first contained &lt;selectedoption> would also be updated.<br>
&lt;jarhar> +1<br>
&lt;bkardell_> lgtm<br>
&lt;dandclark> +1<br>
&lt;keithamus> +1<br>
&lt;scotto6> +1<br>
&lt;dbaron> (I was the code reviewer who told Joey that it was important to explicitly add a test that we *don't* ever ship 'appearance: bikeshed'.)<br>
&lt;masonf> RESOLVED: add an attribute called `selectedoptionelement=foo` to select, which can point to a single &lt;selectedoption> element to update. The first &lt;selectedoption> contained within the &lt;select> would also be updated.<br>
</details>

<!-- comment id=2229298578 author=josepharhar created=2024-07-15T20:06:44Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2229298578 -->
## @josepharhar — 2024-07-15

I created an explainer PR for this here: https://github.com/openui/open-ui/pull/1071

<!-- comment id=2235829622 author=yisibl created=2024-07-18T07:36:07Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2235829622 -->
## @yisibl — 2024-07-18

Maybe the name would be better changed to `selectedoptionfor`?

<!-- comment id=2336544575 author=yisibl created=2024-09-08T04:42:11Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2336544575 -->
## @yisibl — 2024-09-08

Considering the future implementation of `combobox`, how does the current proposal work with `<input>` element?

<!-- comment id=2707790093 author=github-actions[bot] created=2025-03-08T00:37:56Z url=https://github.com/openui/open-ui/issues/1063#issuecomment-2707790093 -->
## @github-actions[bot] — 2025-03-08

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.
