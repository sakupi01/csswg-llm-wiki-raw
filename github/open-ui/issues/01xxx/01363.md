---
number: 1363
title: "[menu] support :open pseudo-class on <menuitem>s that open submenus"
state: "open"
labels: ["menu", "needs-triage"]
author: "dbaron"
created_at: "2026-01-21T16:31:37Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1363"
comments: 7
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

This is sort of a side-issue for #1190, but I'd like to spin it into a separate issue because I think it's a separate side discussion.

I think it would be useful for `menuitem` elements to support the [`:open` pseudo-class](https://drafts.csswg.org/selectors-4/#open-state).  CSS leaves the per-element definitions to be [defined by HTML](https://html.spec.whatwg.org/multipage/semantics-other.html#selector-open), so I think is something that would end up purely in the HTML spec.

When thinking about :hover-activation behavior for opening submenus in #1190, I noticed that the styling of menuitems on many existing menus is a little bit interesting, but is representable in CSS if menuitems that open submenus support the `:open` pseudo-class.  In particular, typical styling seems to be:
* if a menulist (or menubar??) has a menuitem whose submenu is open, the menuitem for the open submenu is typically highlighted even if hover has moved elsewhere (for example, during the delay or safe triangle that keeps the submenu open while the mouse has moved elsewhere)
* when there is no open submenu, the highlight style follows `:hover`

Additionally, an open submenu typically still retains some sort of highlight styling when the `:hover` state has moved into a submenu (although at that point the styling is sometimes different from when the pointer is still in the menu).

I think all of these styles are reasonably representable in CSS (sometimes using selectors like `menulist:has(> menuitem:open)`) if `menuitem` elements support `:open` when they trigger submenus.  I think `:open` is a good name for representing this concept and it already exists in CSS, so I think we should use the existing `:open` pseudo-class here rather than trying to make a new pseudo-class.

<!-- comment id=3779959949 author=lukewarlow created=2026-01-21T17:33:54Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3779959949 -->
## @lukewarlow — 2026-01-21

I think menuitem is perhaps the wrong location?

We have it for dialogs and details which are themselves the things that open. But then we have it for select and I think inputs where they control a 'popover' that opens.

To me it feels like menulist would be the thing with :open. (Though as they're a popover we'd maybe get :popover-open on that for free).

If menuitem gets :open should we add it to buttons that control popovers, and/or dialogs? I'm not sure where to draw the line?

<!-- comment id=3780067099 author=dbaron created=2026-01-21T17:47:19Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3780067099 -->
## @dbaron — 2026-01-21

Yeah, I think we already get `:popover-open` on the menulist.  And I think for the reasons above we do need *something* on the `menuitem` in order to style it correctly.  (It doesn't feel *that* different from a `<select>` being open to me, except for the DOM tree structure being different (see https://github.com/openui/open-ui/issues/1297#issuecomment-3377408751 about non-nested structure.)

<!-- comment id=3781425530 author=scottaohara created=2026-01-21T22:23:22Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3781425530 -->
## @scottaohara — 2026-01-21

this would be useful for any item element that reveals another element, matching the 'expanded' state of the invoking element.

i know i definitely mentioned how this would have been good to have for toggling a popover from a button.

<!-- comment id=3781547267 author=lukewarlow created=2026-01-21T23:08:07Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3781547267 -->
## @lukewarlow — 2026-01-21

So there's this idea we discussed previously. in https://github.com/openui/open-ui/issues/622 should we propose exposing `:open` on command buttons that are in the right states? Essentially matching up with the aria-expanded="true" state?

It'd be hard/impossible to feature detect unfortunately but at least you could use it in future.

Perhaps both of these could be discussed at the joint taskforce meetings to guage how people feel about it.

<!-- comment id=3781612988 author=scottaohara created=2026-01-21T23:29:47Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3781612988 -->
## @scottaohara — 2026-01-21

The odd bit there is that :open has been applied to the element that was revealed - so wouldn’t it be weird to reuse it now on the control that revealed the thing now the :open state?

<!-- comment id=3786474196 author=css-meeting-bot created=2026-01-22T20:12:05Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3786474196 resolution=true -->
## @css-meeting-bot — 2026-01-22

The Open UI Community Group just discussed `[menu] support :open pseudo-class on <menuitem>s that open submenus`, and agreed to the following:

* `RESOLVED:  we need a pseudo class that indicates that a menuitem triggers a submenu, and that submenu is currently open. TBD on the name.`

<details><summary>The full IRC log of that discussion</summary>
&lt;keithamus> dbaron: This is closely related to the previous. Menu triggering, submenu opening, one question becomes how to style them.<br>
&lt;keithamus> dbaron: one thing that happens with hover triggering is the safe triangle, where the submenu opens and you can move the mouse so it's not over the sub-menu, but you won't see hover effects for the sub-menu items until you're past the delay or beyond the triangle<br>
&lt;keithamus> dbaron: in terms of styling, having an open submenu tends to behave like forcing a hover state<br>
&lt;keithamus> dbaron: when menus have hover style, typically if the submenu is open the sumenu being open  - the item that opened the submenu acts like it's hovered, even if the mouse has moved away<br>
&lt;keithamus> dbaron: so we need a way of styling menu items that are currently opening a submenu<br>
&lt;keithamus> dbaron: if we have that css selectors can take care of the rest<br>
&lt;keithamus> dbaron: but we need to be able to match "this is a menu item that's opened a submenu"<br>
&lt;keithamus> dbaron: we have this class ":open" so html can use that for any open elements<br>
&lt;keithamus> dbaron: there's been some discussion in the issue as to whether that makes sense. Does :open apply to the thing that does the opening or the thing that is open<br>
&lt;lwarlow> q+<br>
&lt;keithamus> dbaron: with select the distinction isn't clear, it's select:open, but for menus these are potentially unrelated things<br>
&lt;keithamus> dbaron: so the question is: do people think it makes sense for :open on the menu item, then you can use :popover-open on the submenu<br>
&lt;masonf> q+<br>
&lt;scott> q+<br>
&lt;keithamus> dbaron: or does :open mean the other thing and we need something different<br>
&lt;keithamus> q+<br>
&lt;keithamus> lwarlow: hesitant, but I've realised :open is slightly inconsistent, reason for that is in part because &lt;select> is composite UI control, didn't expose the picker separately, &lt;input> is the thing that triggers and that gets the pseudo. In that sense &lt;input> is like menu-tiem<br>
&lt;keithamus> lwarlow: on the flip side &lt;dialog>, &lt;details>, :open applies to the thing that's open<br>
&lt;keithamus> lwarlow: for inputs ... other than datalist<br>
&lt;keithamus> lwarlow: I don't think :open applies for datalist right now?<br>
&lt;keithamus> lwarlow: I'm slightly torn on this<br>
&lt;sorvell> q+<br>
&lt;keithamus> lwarlow: if we think open applies to a menu item, then the same is true for a button that has opened a dialog or popover, e.g. command invoker that has expanded=true should have :open<br>
&lt;keithamus> lwarlow: that might be confusing if both are open though<br>
&lt;keithamus> ack lwarlow<br>
&lt;keithamus> dbaron: html spec does in a note say :open should work on inputs with an open datalist.<br>
&lt;keithamus> dbaron: not normative though<br>
&lt;keithamus> dbaron: the other thought is that one thing we've thought about pretty heavily, motivated :popover-open, what constitutes semantics of element vs attributes<br>
&lt;keithamus> dbaron: :open is element semantics, but this is sort-of-element-sort-of-attribute semantics<br>
&lt;keithamus> ack masonf<br>
&lt;keithamus> masonf: wanted to say a lot of what lwarlow says. :open use cases are mixed.<br>
&lt;keithamus> masonf: the only problem I see with that is where you can't disambiguate.<br>
&lt;keithamus> masonf: this is one where it's not ambiguous. The menu item itself can't be a popover, dialog, etc, so it can't be open itself, therefore there's no ambiguity.<br>
&lt;keithamus> masonf: the question is: do I as a developer know what menu-item:open is? It seems very clear<br>
&lt;keithamus> masonf: the alternative is to invent a new special pseudo class just for these case, e.g. :menu-item-with-submenu-open<br>
&lt;lwarlow> Hmm that makes sense, so we have :popover-open because a dialog or details can be open separately to the popover thingy. Whereas that's not true of menuitem<br>
&lt;keithamus> masonf: in which case let's just use this<br>
&lt;keithamus> ack scott<br>
&lt;masonf> qq+<br>
&lt;keithamus> scott: I just lean a bit more towards different. This isn't really just for menu items, we don't have a way to determine a button is in the expanded state, we have a11y states but no way to style based on that.<br>
&lt;keithamus> scott: if we can apply :open to the button as well, sure. I personally wouldn't have done that.<br>
&lt;keithamus> scott: maybe :expanded would have been the way to do it<br>
&lt;keithamus> scott: matches my a11y intuition<br>
&lt;lwarlow> q+<br>
&lt;keithamus> scott: as long as it's not confusing to other developers<br>
&lt;keithamus> scott: not just menu items, but like popover, etc.<br>
&lt;keithamus> masonf: I agree this should probably also apply to command but we should keep those separate issues<br>
&lt;keithamus> masonf: e.g. :open is something in css which we can link to in html<br>
&lt;keithamus> masonf: I do think :expanded makes sense but unfortunately here we are<br>
&lt;keithamus> q-<br>
&lt;masonf> ack mason<br>
&lt;Zakim> masonf, you wanted to react to scott<br>
&lt;keithamus> ack sorvell<br>
&lt;keithamus> sorvell: I am slightly ambivalent about menu-item having :open but when I consider some random dialog that opens a dialog some other place being :open, then that's just weird, which leans me toward it being different.<br>
&lt;keithamus> sorvell: we have a thing thats invoking a command, that think is done, everything's good. But the command implies some state we're not capturing. The thing we're not capturing is the state that's relevant to the invokee<br>
&lt;keithamus> sorvell: somehow, via css, to expose that state to the command - the state of the commanded element to the commandor, so the button can say "hey, based on the state of the target"<br>
&lt;keithamus> sorvell: right now I can make a custom command, with a custom state, i might want that reflected in the thing that can generate that.<br>
&lt;masonf> Like ::target-matches(selector)<br>
&lt;masonf> I guess one : for that, not ::<br>
&lt;keithamus> lwarlow: I don't think adding :expanded now is that weird. Even with :open<br>
&lt;keithamus> lwarlow: I think a dialog is a thing that opens, so it gets :open, details, input (ish)...<br>
&lt;sorvell> q+<br>
&lt;keithamus> lwarlow: but the triggering thing is the command invoker, or the menu-item, they don't match open, if we wanted to introduce expanded then that would be okay<br>
&lt;keithamus> lwarlow: the thing that's open is :open and the thing that expanded it is :expanded<br>
&lt;keithamus> lwarlow: having said that it doesn't solve all the use cases, if you have a custom command with custom state, you probably want to style the button with custom state<br>
&lt;keithamus> lwarlow: play/pause on a media element for example<br>
&lt;keithamus> lwarlow: currently there isn't a nice way to do that, only with all knowledge, no generic selector<br>
&lt;lwarlow> button::commandfor(:open)<br>
&lt;keithamus> lwarlow: if I want to style the button based on the thing that is the commandfor target. We'd get this for free.<br>
&lt;masonf> There's this issue: https://github.com/w3c/csswg-drafts/issues/10970<br>
&lt;keithamus> lwarlow: if we could have that, that would be nice.<br>
&lt;keithamus> lwarlow: probably a can of worms for invalidation. Can't get much worse than has?<br>
&lt;keithamus> lwarlow: you can't trigger the open state based on styling the button<br>
&lt;keithamus> lwarlow: that solves the menu item case. It solves the commandfor generic case.<br>
&lt;keithamus> lwarlow: that's nicer than just the pseudo class<br>
&lt;keithamus> lwarlow: if we do the pseudo class and don't think about the rest later, we'll either not do the rest later or we'll do it and the pseudo class will feel weird.<br>
&lt;masonf> q?<br>
&lt;flackr> q+<br>
&lt;keithamus> sorvell: my preferences in order: 1 explore pseudo element / combinator to connect state. 2. a new name: the reason I don't want the same name - I don't think it should be applied to a button that opens a dialog, the reason is i think you can argue the case of the ambiguity, the select e.g. is a composite control, but menu could be argued it's a<br>
&lt;keithamus> composite control too, but the same cannot be argued for dialog.<br>
&lt;keithamus> sorvell: I think its a problem to treat them the same<br>
&lt;keithamus> sorvell: it's not a case where it's the same thing. I think we have a case where there's a composite control that's doing an action which is clearly part of it<br>
&lt;keithamus> sorvell: sometimes the case with command and sometimes not the case.<br>
&lt;keithamus> ack sorvell<br>
&lt;keithamus> ack dbaron<br>
&lt;Zakim> dbaron, you wanted to talk about commands and menus and magic<br>
&lt;masonf> That argument kind of convinced me. menuitem and command invokers should be two separate issues.<br>
&lt;lwarlow> Actually my earlier point about aria-expanded symetry doesn't always work either because we don't set aria-expanded for buttons controlling modal dialogs (it's not needed). So I'm even more in favour of the combinator.<br>
&lt;keithamus> dbaron: can of worms that might change some opinions: one problem is the way menus are structured, you can reference a menulist as a submenu of more than one menu<br>
&lt;flackr> q-<br>
&lt;keithamus> dbaron: a menulist can be a submenu of your file menu and also your help menu, or something like that.<br>
&lt;keithamus> dbaron: either one of those submenus can be its invoker, and can open it up<br>
&lt;lwarlow> q+<br>
&lt;keithamus> dbaron: if we're talking about some kind of combinator relationship, the relationship has to care about what the invoker is, it cannot purely be a reference to the commandfor<br>
&lt;keithamus> dbaron: it's the commandfor _and_ the invoker<br>
&lt;keithamus> qq+<br>
&lt;keithamus> dbaron: I'm still trying to work out exactly how much of this menu stuff is fully representable as commands, and how much is magical when you call togglemenu showmenu or hidemenu<br>
&lt;keithamus> dbaron: it's unlikely the second set is the empty set.<br>
&lt;keithamus> dbaron: some of these items will have to say "some of these which call showmenu or hidemenu etc then we do these things that make this behave like a submenu"<br>
&lt;keithamus> dbaron: maybe we can somehow describe these as properties of the submenu elements<br>
&lt;keithamus> dbaron: .... that's kind of the point of the submenus is to do something else.<br>
&lt;keithamus> dbaron: some of this stuff makes me a little nervous<br>
&lt;keithamus> dbaron: I dont think we can fully describe using commands<br>
&lt;masonf> q+<br>
&lt;keithamus> dbaron: the combinator needs to care about who the invoker is<br>
&lt;keithamus> dbaron: theres a state about this element invoked the other one<br>
&lt;keithamus> ack keithamus<br>
&lt;Zakim> keithamus, you wanted to react to dbaron<br>
&lt;jarhar> keithamus: anchors are a relationship that is in css that defines these two things<br>
&lt;jarhar> keithamus: i think maybe theres something there<br>
&lt;jarhar> keithamus: anchors do the thing you described where its the commandfor and its also the invoker<br>
&lt;jarhar> q+<br>
&lt;keithamus> lwarlow: I think that probably in most cases if we're styling a menu item or a button, the thing it commands ... you probably want to style it based on the targets state<br>
&lt;keithamus> lwarlow: if you take video, you dont want play pause button to be in the paused state if it wasnt the button that paused it<br>
&lt;keithamus> lwarlow: like the button should represent the targets state. popover-open when open<br>
&lt;keithamus> lwarlow: If command and commandfor aren't the right fit for this, we don't have to use them<br>
&lt;keithamus> lwarlow: if they're more work than necessary<br>
&lt;keithamus> lwarlow: The scope of those is "I do something to that element", the list doesn't need to be small, but we can also just have togglemenu if showmenu/hidemenu are problematic<br>
&lt;keithamus> lwarlow: I see what you mean about the styling as to whether you're the acting invoker or not<br>
&lt;keithamus> lwarlow: if you programatically open the menu list, then associate that, it would use the source as the active thing<br>
&lt;keithamus> lwarlow: that could be something other than a menu item<br>
&lt;keithamus> dbaron: then it's a top level menu rather than a submenu<br>
&lt;sorvell> q+<br>
&lt;keithamus> ack lwarlow<br>
&lt;keithamus> lwarlow: potentially<br>
&lt;jarhar> q-<br>
&lt;keithamus> lwarlow: if it's weird to open a menu from a menu item is it weird to open a .... ?<br>
&lt;keithamus> lwarlow: can it just be a one off button?<br>
&lt;keithamus> lwarlow: if it's weird for menu item it might be weird for button, is that accounted for?<br>
&lt;keithamus> masonf: a hamburger is a single button that opens a menu, and I can see that looking different when the menu is open, so there's a use case<br>
&lt;keithamus> masonf: a much more complicated question about command invokers and combinators in between, those are great questions, but even in a world where that exists there's still a common use case here<br>
&lt;lwarlow> q+<br>
&lt;keithamus> masonf: in that world it's okay to have two ways to do something: easy way with :expanded and the more complicated way with combinators e.g.<br>
&lt;keithamus> masonf: command invokers also do stateless things like step up and step down, how do they work?<br>
&lt;keithamus> masonf: I'd love to separate that<br>
&lt;keithamus> ack sorvell<br>
&lt;keithamus> sorvell: the connection combinator seems worthwhile but complicated, maybe we could punt that... but can we take advantage of the fact that this is always an anchor?<br>
&lt;keithamus> sorvell: is there a way with css anchorpos to say "I am the element that has been anchored to"<br>
&lt;keithamus> sorvell: if there isn't, maybe we add that?<br>
&lt;keithamus> lwarlow: I think :open is probably okay but we need to discuss this in the joint task force to see what others think<br>
&lt;keithamus> lwarlow: I will be there to point out the button<br>
&lt;keithamus> lwarlow: I think for what this is doing :open is probably okay.<br>
&lt;masonf> Proposed resolution: we need a pseudo class that indicates that a menuitem triggers a submenu, and that submenu is currently open. TBD on the name.<br>
&lt;keithamus> dbaron: it sounds like we dont have a firm conclusion, and we need to discuss more in the task force.<br>
&lt;lwarlow> +1<br>
&lt;keithamus> +1<br>
&lt;jarhar> +1<br>
&lt;dbaron> +1<br>
&lt;sorvell> +1<br>
&lt;masonf> RESOLVED:  we need a pseudo class that indicates that a menuitem triggers a submenu, and that submenu is currently open. TBD on the name.<br>
&lt;keithamus> Zakim, please leave<br>
&lt;Zakim> leaving.  As of this point the attendees have been masonf, flackr, bkardell, lwarlow<br>
&lt;keithamus> RRSAgent, make minutes<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2026/01/22-openui-minutes.html keithamus<br>
</details>

<!-- comment id=3854729244 author=css-meeting-bot created=2026-02-05T16:20:46Z url=https://github.com/openui/open-ui/issues/1363#issuecomment-3854729244 resolution=true -->
## @css-meeting-bot — 2026-02-05

The CSS Working Group just discussed `[menu] support :open pseudo-class on <menuitem>s that open submenus`, and agreed to the following:

* `RESOLVED:  :open matches a menuitem that triggers a submenu, and that submenu is currently open (confirm with all vendors)`

<details><summary>The full IRC log of that discussion</summary>
&lt;jarhar> dbaron: whatever we decide here should apply to application menus and navigation menus<br>
&lt;jarhar> dbaron: there is a need to style a menu item depending on whether its submenu is open, and we need to decide a pseudo class for this<br>
&lt;jarhar> dbaron: we could reuse :open<br>
&lt;jarhar> dbaron: it isnt clear whether :open currently applies to things that open stuff or the thing itself that is opened<br>
&lt;jarhar> q+<br>
&lt;jarhar> dbaron: in openui, a bunch of us thought that :open was fine, and others thought that reusing :open would be weird on the thing that does the opening<br>
&lt;lwarlow> q+<br>
&lt;jarhar> jarhar: some might think that the menulist should match :open instead of :popover-open<br>
&lt;cwilso> ack next<br>
&lt;cwilso> ack next<br>
&lt;masonf> q+<br>
&lt;jarhar> lwarlow: im not one way or the other right now. currently :open applies in both situations. in details and dialog its the thing that opens. in select its both. for input its kind of the thing that does the opening and kind of the picker<br>
&lt;jarhar> lwarlow: not sure if it applies for datalist<br>
&lt;jarhar> lwarlow: i think menulist matching :popover-open is a consequence of it being a popover<br>
&lt;jarhar> lwarlow: we dont have to add the popover attribute, but its a popover<br>
&lt;jarhar> lwarlow: i dont think menulist will be the only element that will be a popover<br>
&lt;dbaron> s/be a popover/be an implicit popover/<br>
&lt;jarhar> lwarlow: we could add the automatic popoverness to elementinternals<br>
&lt;jarhar> lwarlow: menulist matching :popover-open is fine<br>
&lt;jarhar> lwarlow: my main holdup is if we say that menutiem should match :open then that implies that command invokers should match :open when they have a popover that is open<br>
&lt;jarhar> lwarlow: the relationship there is very similar<br>
&lt;jarhar> lwarlow: if we dont like that, then some sort of :expanded pseudo class<br>
&lt;masonf> q-<br>
&lt;jarhar> lwarlow: we already have :open for many cases that match aria-expanded<br>
&lt;cwilso> ack next<br>
&lt;jarhar> masonf: im afraid of the command invokers because thats a whole can of worms<br>
&lt;jarhar> masonf: :open makes sense because theres no ambiguity<br>
&lt;jarhar> masonf: menuitem is not a thing itself that can be opened<br>
&lt;jarhar> masonf: if you put :open on a menuitem its easy to understand<br>
&lt;jarhar> masonf: im in favor of that resolution<br>
&lt;jarhar> smaug: im looking at xul implementation of menu. david do you remember whats done there? maybe we dont have anything specific for this, could have been done on js side<br>
&lt;jarhar> smaug: the behavior is there<br>
&lt;jarhar> dbaron: i dont remember. my intuition is that xul menus have a different tree structure<br>
&lt;jarhar> dbaron: might not be remembering that correctly<br>
&lt;jarhar> smaug: openui proposal was very similar to xul, but i havent looked at latest<br>
&lt;jarhar> masonf: by tree structure, it has nesting?<br>
&lt;jarhar> dbaron: yes<br>
&lt;jarhar> smaug: in some examples they were doing that<br>
&lt;lwarlow> I could +1 a resolution to use :open but I do think it's worth checking with Apple too.<br>
&lt;dbaron> https://searchfox.org/firefox-main/source/browser/base/content/browser-menubar.inc<br>
&lt;lwarlow> PROPOSED RESOLUTION: menuitems use :open (Confirm with all vendors)<br>
&lt;masonf> Proposed resolution: :open matches a menuitem that triggers a submenu, and that submenu is currently open<br>
&lt;jarhar> proposed resolution: use :open on menuitems while their submenu is open<br>
&lt;masonf> ahh<br>
&lt;masonf> Proposed resolution: :open matches a menuitem that triggers a submenu, and that submenu is currently open (confirm with all vendors)<br>
&lt;cwilso> +1<br>
&lt;masonf> +1<br>
&lt;lwarlow> +1<br>
&lt;jarhar> +1<br>
&lt;dbaron> +1<br>
&lt;cwilso> RESOLVED:  :open matches a menuitem that triggers a submenu, and that submenu is currently open (confirm with all vendors)<br>
</details>
