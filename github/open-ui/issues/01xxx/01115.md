---
number: 1115
title: "select: How should we render `<option label=label>text`?"
state: "closed"
labels: ["needs-triage", "select"]
author: "josepharhar"
created_at: "2024-10-23T21:13:42Z"
closed_at: "2025-05-06T20:30:43Z"
url: "https://github.com/openui/open-ui/issues/1115"
comments: 12
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

In the pre-customizable-select chromium implementation, option elements only render text content by manually collecting it from their descendants. If there is a label attribute, then the text cotent of descendant nodes is ignored and the text in the label attribute is rendered instead.

There is currently some code in chromium which tries to preserve this rendering behavior when not inside a customizable select, but doesn't do this label overwriting behavior and allows all child nodes to be rendered normally when inside a customizable select. It's not very performant right now and I'm currently trying to find ways to work around it.

It occurred to me that this has never been discussed before and could affect how I implement this - how should we render `<option label=label>text</option>`? Should "label" or "text" be rendered? 

This would also affect cases like `<option label=label><img src=...><span>text</span> more text</option>` - if we choose to render "label", then the img and other child content would not be rendered.

If we don't render the label attribute, then should that label attribute be used in the accessibility tree instead of the actual child nodes? Is there a use case for that? @scottaohara 

Related: https://github.com/openui/open-ui/issues/558

<!-- comment id=2444733564 author=josepharhar created=2024-10-29T16:10:42Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2444733564 -->
## @josepharhar — 2024-10-29

This issue provides a use case for not using the label attribute when rendering: https://github.com/openui/open-ui/issues/1118

<!-- comment id=2444738219 author=josepharhar created=2024-10-29T16:12:25Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2444738219 -->
## @josepharhar — 2024-10-29

Right now the label attribute is not used for rendering, but does get copied into the default/fallback button, which is probably not good and should be fixed after we decide what to do here.

```html
<select>
  <option label=label>text</option>
</select>

```

![_usr_local_google_home_jarhar_Downloads_label html](https://github.com/user-attachments/assets/cee07400-463f-4041-ae9a-cdd67567c6c6)

<!-- comment id=2444741757 author=josepharhar created=2024-10-29T16:13:50Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2444741757 -->
## @josepharhar — 2024-10-29

In the spec PR, I'm already saying that options don't use the label attribute to render: https://whatpr.org/html/10629/rendering.html

> An [option](https://whatpr.org/html/10629/form-elements.html#the-option-element) element is expected to be rendered by displaying the element's [label](https://whatpr.org/html/10629/form-elements.html#concept-option-label), indented under its [optgroup](https://whatpr.org/html/10629/form-elements.html#the-optgroup-element) element if it has one. If the [option](https://whatpr.org/html/10629/form-elements.html#the-option-element) is being rendered in a [select](https://whatpr.org/html/10629/form-elements.html#the-select-element) which is being rendered as a [drop-down box](https://whatpr.org/html/10629/rendering.html#drop-down-box) with [base appearance](https://drafts.csswg.org/css-ui/#base-appearance), then the [option](https://whatpr.org/html/10629/form-elements.html#the-option-element) is expected to render all of its children rather than by displaying its [label](https://whatpr.org/html/10629/form-elements.html#concept-option-label).

<!-- comment id=2444743853 author=josepharhar created=2024-10-29T16:14:35Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2444743853 -->
## @josepharhar — 2024-10-29

Proposed resolution: Don't use the option's label attribute to render anything anywhere in the new select.

<!-- comment id=2450138279 author=sorvell created=2024-10-31T15:14:10Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2450138279 -->
## @sorvell — 2024-10-31

It seems unfortunate to change this behavior from how non-custom select works.

Is there another case besides #1118 that needs a label distinct from the children? 

Is `label` relevant to a11y or would that be `aria-label` and if it's the latter, then is the keyboard case in #1118 an a11y concern and could it therefore use `aria-label`? (Unfortunately, it looks like `aria-label` isn't used for keyboard navigation in the normal select)

<!-- comment id=2450348197 author=scottaohara created=2024-10-31T16:45:46Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2450348197 -->
## @scottaohara — 2024-10-31

label is not an a11y-specific attribute, in that it was a method to provide a visible label, rather than using nested text, for an option.

aria-label aside, the accName of the option either comes from the label - which produced visible text 'in' the option, or the visible text an author provides within the option.  so that's why it's not an a11y-feature, because they were two methods to render a visible label.   previously, the label attr took precedent over the visible text, so the visible text an author provided would be ignored, and the label text would be present within the option.  but for calculating the accname, that visible text was all that mattered (again, aria-label aside and the fact it wasn't even consistently used by all browsers/AT to modify an accName - since the UA provided options weren't necessarily 1:1 matches with the markup that someone wrote)  but in the context of a datalist, an option can have visible text AND a label attribute's text both made visually available in an option.  I've mentioned in the current select PR that this distinction is lost with the updates to the content model for options.  arguably, this similar behavior could be useful for the new customizable select too...

<!-- comment id=2450554443 author=scottaohara created=2024-10-31T18:24:05Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2450554443 -->
## @scottaohara — 2024-10-31

linking this issue to my HTML AAM PR for option element mapping updates, - https://github.com/w3c/aria/pull/2365

<!-- comment id=2450588524 author=css-meeting-bot created=2024-10-31T18:43:12Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2450588524 resolution=true -->
## @css-meeting-bot — 2024-10-31

The Open UI Community Group just discussed ``select: How should we render `<option label=label>text`?``, and agreed to the following:

* `RESOLVED: render the label attribute for options when it is present, regardless of the select's appearance value, just like it already does. also the label attribute is bad.`

<details><summary>The full IRC log of that discussion</summary>
&lt;gregwhitworth> jarhar: the option label has the label attribute and in the customizable select world this attribute where you have &lt;option label=label>text and it will render label and not text<br>
&lt;gregwhitworth> jarhar: in the new customizable select world I implemented a thing that ignores the label attribute as I find it weird<br>
&lt;gregwhitworth> jarhar: how should this attribute work, scott says that it should still be used<br>
&lt;scott> q+<br>
&lt;gregwhitworth> jarhar: given this is what it already does it says "render the label attribute unless your the child of a customizable select"<br>
&lt;gregwhitworth> jarhar: also sarah left a comment about typeahead approach and the more I think about this to change the behavior of label is to keep the old behavior<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack scott<br>
&lt;gregwhitworth> scott: thank you for the summary<br>
&lt;sorvell> q+<br>
&lt;masonf> q+<br>
&lt;gregwhitworth> scott: the latest comment I posted is in reviewing the PR yesterday I was getting tripped up with some of the simplication as it does something different if it is in a datalist and it will show it as additional text and in Chrome that actual additional text can be used as a hint and in other browsers it doesn't show<br>
&lt;sarah> q+<br>
&lt;gregwhitworth> scott: I would love if we don't do current behavior I would love to follow suit with datalist and possibly fix this across them<br>
&lt;gregwhitworth> scott: those are my thoughts on it<br>
&lt;gregwhitworth> ack dbaron<br>
&lt;gregwhitworth> dbaron: I wanted to mention the history of this is that it may have gotten lost<br>
&lt;gregwhitworth> dbaron: the reason label exists is that label and optgroup were introduced at the same time and it exists for browsers that don't support optgroup<br>
&lt;gregwhitworth> dbaron: the options of the label attribute if you don't support optgroup but if you do then you show optgroup<br>
&lt;gregwhitworth> dbaron: given that we don't have to worry about compat with optgroup that doesn't say a lot of what we should do<br>
&lt;gregwhitworth> dbaron: the whole plan got messed up by not implementing the same things at the same time<br>
&lt;gregwhitworth> dbaron: since it was the idea and browsers would implement each feature 5 years apart<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack sorvell<br>
&lt;gregwhitworth> sorvell: there as some stuff for closing tags for option and you could use the label; this all seems legacy<br>
&lt;gregwhitworth> sorvell: I think it will be hard to spec how the label will work and the custom select is meant to control how things are rendered<br>
&lt;gregwhitworth> sorvell: I don't think it's 100% orthogonal to that as it does seem pretty necessary to cover in MVP<br>
&lt;gregwhitworth> ack masonf<br>
&lt;gregwhitworth> masonf: I was there primarily to discuss the typeahead of that. We say that we should render the label and people will stop using it as it will not render the way they expect it to<br>
&lt;gregwhitworth> masonf: it's related but orthogonal to typeahead and I would want to ignore what's there and what you search for<br>
&lt;gregwhitworth> q+<br>
&lt;gregwhitworth> masonf: do what the old thing is supported<br>
&lt;gregwhitworth> ack sarah<br>
&lt;gregwhitworth> sarah: When I was writing the typeahead issue we had discussed this but knowing the issue is label should stay odd due to history<br>
&lt;gregwhitworth> sarah: but if it stays there then people will need to know that when it appears odd they'll adjust<br>
&lt;dbaron> s/the reason label exists/I *think* the reason label exists/<br>
&lt;gregwhitworth> sarah: I also proposed the text attribute as the label has meaning around that and this wouldn't contribute to the accessible name and it may contribute to the value but not accname<br>
&lt;gregwhitworth> q?<br>
&lt;masonf> gregwhitworth: I worked with Sunir on the combobox, it'd be cool if we could fork into a new issue for the `text` attribute. We're all agreeing otherwise.<br>
&lt;jarhar> proposed resolution: render the label attribute for options when it is present, regardless of the select's appearance value, just like it already does. also the label attribute is bad.<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;sorvell> +1<br>
&lt;masonf> +1<br>
&lt;gregwhitworth> +1<br>
&lt;scott> +1<br>
&lt;dbaron> +1<br>
&lt;sarah> +1<br>
&lt;jarhar> RESOLVED: render the label attribute for options when it is present, regardless of the select's appearance value, just like it already does. also the label attribute is bad.<br>
&lt;sarah> this is the other issue about the typeahead / maybe value-related question: https://github.com/openui/open-ui/issues/1118<br>
</details>

<!-- comment id=2852867494 author=github-actions[bot] created=2025-05-06T00:53:45Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-2852867494 -->
## @github-actions[bot] — 2025-05-06

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=3899600668 author=sb3nder created=2026-02-13T21:31:45Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-3899600668 -->
## @sb3nder — 2026-02-13

I'm sorry to comment on a closed issue, but I came across a use case that may be relevant here. I'm not sure if there’s another way to handle it, and it might not be a common scenario.

**The use case:** I want to display a select picker with `appearance="base"` on desktop and `appearance="auto"` on mobile.

**The problem:** When using the system picker on mobile, all the rich content inside the `<option>` is displayed. Using the `label` attribute fixes this for the system picker, but it overrides the content of the base picker.

<!-- comment id=3899674744 author=josepharhar created=2026-02-13T21:47:31Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-3899674744 -->
## @josepharhar — 2026-02-13

So you want to show different text in the base appearance vs auto appearance pickers? Not considering whether that's a good idea, you could try something like this:

```html
<option>
  <span style="display:none">appearance:auto</span>
  <span style="::before { content: 'base appearance'; }"></span>
</option>
```

<!-- comment id=3899858975 author=sb3nder created=2026-02-13T22:22:06Z url=https://github.com/openui/open-ui/issues/1115#issuecomment-3899858975 -->
## @sb3nder — 2026-02-13

That should work, thanks!

I didn’t necessarily mean showing completely different text, just a slight variation.\
For example, changing the formatting, hiding an icon that comes from something like `<span>icon_1</span>`, omitting or rephrasing information that wouldn’t make sense without the rich content.
