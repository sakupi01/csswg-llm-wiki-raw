---
number: 1464
title: "[range] Support non-linear (e.g. logarithmic) value scales"
state: "open"
labels: ["Enhanced range slider"]
author: "brechtDR"
created_at: "2026-06-16T13:41:43Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1464"
comments: 1
has_resolution: true
synced_at: "2026-09-25T09:10:09Z"
---

This is about more than range group but pops up a lot in feedback sessions I have on range sliders.

Authors frequently need a range control where the thumb's position along the track maps non-linearly to its value, most commonly a logarithmic or exponential scale. Today, this is impossible with `<input type="range">` and is not covered by the Enhanced Range Input / `<rangegroup>` proposal. I'd like to discuss whether (and how) the enhanced range proposal should support custom value scales.

Problem
Native `<input type="range">` is strictly linear: the value is always min + position × (max − min).

Classic example: a price filter from 1 to 1,000,000. With a linear scale, the entire 1–500,000 region (where most users actually shop) lives in the left half, and a 1px drag near the middle jumps by thousands, so fine-tuning a value like 20 or 40 is impossible. The same problem shows up for:

- Audio / signal controls (frequency, gain in dB)
- Zoom levels and map scale
- File / data sizes (KB → TB)
- Scientific inputs spanning many orders of magnitude

The long-standing workaround is a "log-linear" hack: drive a linear 0–100 slider and transform it in JS with Math.pow/Math.log, writing the real value to a hidden input. This loses the declarative model, complicates accessibility (aria-valuetext, announcements), and has to be re-implemented by every author.

I also found these examples:

https://stackoverflow.com/questions/37814574/exponential-growth-of-input-range-values
https://stackoverflow.com/questions/49814503/change-html-slider-step-exponentially

Proposed directions (for discussion)
Declarative keyword e.g. scale="logarithmic" (default linear) on the range input / <rangegroup>. Covers the overwhelmingly common case with zero JS and degrades to a linear native control. The question is then how to adjust the scaling itself.

JS escape hatch, a bidirectional function pair (valueToPosition / positionToValue) for arbitrary scales (exponential, custom easing, piecewise). The declarative keyword would just be a built-in instance of this.

## Conformance / progressive enhancement
In a non-supporting browser, scale would be ignored, and the control falls back to a linear native range. functional in some way, just without the non-linear feel. That keeps the progressive-enhancement story consistent with the rest of the <rangegroup> proposal.

<!-- comment id=5623820813 author=css-meeting-bot created=2026-09-10T18:51:40Z url=https://github.com/openui/open-ui/issues/1464#issuecomment-5623820813 resolution=true -->
## @css-meeting-bot — 2026-09-10

The Open UI Community Group just discussed `[range] Support non-linear (e.g. logarithmic) value scales`, and agreed to the following:

* `RESOLUTION: Prototype the non-linear scale with datalist as is possible with the current input type=range for enhanced range sliders and update the explainer with that information`

<details><summary>The full IRC log of that discussion</summary>
&lt;jarhar> Brecht: *introduces issue*<br>
&lt;jarhar> q+<br>
&lt;keithamus> q+<br>
&lt;keithamus> jarhar: I've seen sliders like this a lot; just found one on Zillow for example<br>
&lt;keithamus> jarhar: I think you can provide some kind of "math thing" - as in "here's how I want it to scale", or a &lt;datalist>, where it can increase from one tick to another.<br>
&lt;keithamus> jarhar: I think this can be a separate thing from multiple ranges and segments<br>
&lt;keithamus> ack jarhar<br>
&lt;jarhar> keithamus: i believe this already works today with datalist<br>
&lt;jarhar> keithamus: if not i think it should<br>
&lt;jarhar> keithamus: with the element and the nested children in rangegroup, we could have arbitrary options<br>
&lt;jarhar> keithamus: if there is an option, then that defines the set of values in there<br>
&lt;Brecht> q+<br>
&lt;keithamus> ack keithamus<br>
&lt;jarhar> keithamus: then you can have any number of things - you dont have to define logarithms, its an arbitrary thing of values that can be selected. not sure what the interplay is there with multiple ranges, but it seems massagable with datalist<br>
&lt;jarhar> Brecht: one question i have about that is that i see this in two different ways - every item is equally spaced, every stop is linterally spaced, same space between 2 and 4 and 4 and 8<br>
&lt;jarhar> Brecht: see same space on the track even though you have the same stops. if you want the distance between 4 and 8 to be bigger than 2 to 4<br>
&lt;keithamus> https://demo.agektmr.com/datalist/<br>
&lt;jarhar> Brecht: there might still be something or an opportunity that we need to define that<br>
&lt;jarhar> Brecht: should it be equally spaced or not<br>
&lt;jarhar> Brecht: i could prototype this to give an idea<br>
&lt;jarhar> Brecht: im also struggling for a lot of use cases, how big is this use case? and also do we want it always to be in stops in that case? if you have a datalist, do you want it to be a stop or do you want it to be fluid, any point between 4 and 8 to be the same as 2 and 4? how far we want to take this is a really foundational question. that kind of<br>
&lt;jarhar> scares me<br>
&lt;jarhar> keithamus: i do think its orthogonal to the multiples thing which is the core feature here<br>
&lt;jarhar> keithamus: i shared a link on irc - datalist creates snap points but you can choose values in between<br>
&lt;jarhar> keithamus: so i think theres a predefined semantic and we could extend that to say it should only allow one of those values<br>
&lt;jarhar> keithamus: we could always do a rangegroup v2<br>
&lt;keithamus> r?<br>
&lt;keithamus> q?<br>
&lt;keithamus> ack Brecht<br>
&lt;jarhar> Brecht: i think at least this one is worth prototyping<br>
&lt;jarhar> Brecht: this specific use case<br>
&lt;jarhar> keithamus: do you want to resolve on prototyping?<br>
&lt;jarhar> Brecht: maybe i should try to start with this as a prototype and people will say this is enough for our use case. maybe it wont be<br>
&lt;jarhar> Brecht: at least we have an incentive to say you can do it already<br>
&lt;jarhar> Brecht: hopefully we will learn about shortcomings<br>
&lt;Brecht> PROPOSED RESOLUTION: Prototype the non-linear scale with datatypes as is possible with the current input type=range for enhanced range sliders<br>
&lt;jarhar> +1<br>
&lt;keithamus> +1<br>
&lt;Brecht> PROPOSED RESOLUTION: Prototype the non-linear scale with datatypes as is possible with the current input type=range for enhanced range sliders and update the explainer with that information<br>
&lt;keithamus> +1<br>
&lt;keithamus> jarhar: I find the way it snaps to datalist points, and allowing in-between... I haven't really seen this. I see some where it forces you to the points, or it is logarithmic, like values get bigger as you slide to the right. I think it should be a setting, either it snaps or doesn't snap at all.<br>
&lt;flackr> q+<br>
&lt;keithamus> Brecht: attribute on &lt;rangegroup> or &lt;datalist><br>
&lt;keithamus> jarhar: good question<br>
&lt;jarhar> Brecht: it almost feels like we should have a snaplist attribute<br>
&lt;jarhar> Brecht: on a rangegroup<br>
&lt;jarhar> Brecht: but im always scared with attributes that are just on there<br>
&lt;jarhar> flackr: im a bit worried if you have a mode that doesnt snap to the list items that dragging between them then needs to do the proper exponential thing<br>
&lt;jarhar> flackr: maybe you just need the true general thing which is what is the exponential growth of your range<br>
&lt;jarhar> flackr: you need to have whatever you specify be the point<br>
&lt;jarhar> flackr: im also wondering what happens when you press arrow keys to move the range, presumably it should move by a larger amount to the right of the range than to the left<br>
&lt;keithamus> q+<br>
&lt;keithamus> ack flackr<br>
&lt;jarhar> flackr: we have a step property, right? i assume that might need to behave differently on one end to the other<br>
&lt;jarhar> flackr: if were just forcing selecting items from the list then it doesnt matter<br>
&lt;jarhar> keithamus: on firefox, the arrow key ignores the datalist. the mouse snaps to the datalist points, but the arrow keys you can freely choose<br>
&lt;Brecht> q+<br>
&lt;keithamus> ack keithamus<br>
&lt;jarhar> keithamus: i agree with what youre saying otherwise. i think on reflection, the datalist is a interesting but seemingly currently half baked idea that needs a lot of refinement based on what we just discussed<br>
&lt;keithamus> ack Brecht<br>
&lt;keithamus> q+<br>
&lt;jarhar> Brecht: could there be a value step on rangegroup with a keyword for following the datalist specifically? by default you would have a number, you could have an increment of 10 or 5, but if you have a keyword that says datalist point, in that case it would follow the points of a datalist but never in between<br>
&lt;jarhar> Brecht: then youre combining a number with a string<br>
&lt;jarhar> keithamus: the other thing is bewcause of the step value we can always hold to that with script based on change, im guessing that just works<br>
&lt;jarhar> keithamus: but that should always be a potential recourse. for these more niche cases, script is still a valuable primitive and its infinitely expressive compared to attributes<br>
&lt;keithamus> ack keithamus<br>
&lt;jarhar> flackr: for a generic exponential slider, your step is a factor of the current value<br>
&lt;jarhar> flackr: step right is either doubling or halving to the left if 2 is your factor<br>
&lt;jarhar> flackr: and that would provide a consistent distance jump for the step<br>
&lt;jarhar> keithamus: so we had a resolution, which was to prototype this out. i think what weve expolored by further discussing this is that there is a lot to think about, which makes me think that the prototype is the right step forward<br>
&lt;jarhar> keithamus: what weve said here hasnt negated that proposed resolution<br>
&lt;jarhar> keithamus: should we spend more time talking about this?<br>
&lt;jarhar> Brecht: the bigger discussion can be held later<br>
&lt;jarhar> Brecht: we could open a separate issues on the things that i find in there or shortcomings<br>
&lt;jarhar> Brecht: this could be a step of the resolution is to create more issues<br>
&lt;jarhar> keithamus: id also encourage you to look at how to minimize the scope here, theres a lot going on here<br>
&lt;jarhar> keithamus: this is kind of orthogonal<br>
&lt;jarhar> keithamus: presumably we would want to ship this. is this another 2 year project on top of the existing work?<br>
&lt;Brecht> PROPOSED RESOLUTION: Prototype the non-linear scale with datatypes as is possible with the current input type=range for enhanced range sliders and update the explainer with that information. Create separate issues for the short comings such as step increases, snaps, etc....<br>
&lt;keithamus> jarhar: I'd also be curious to see how libraries do this. A lot of range libs exist - do they do more with datalist, or math expressions>?<br>
&lt;jarhar> +1<br>
&lt;Brecht> RESOLUTION: Prototype the non-linear scale with datalist as is possible with the current input type=range for enhanced range sliders and update the explainer with that information<br>
</details>
