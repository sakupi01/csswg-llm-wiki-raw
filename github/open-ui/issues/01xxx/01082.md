---
number: 1082
title: "[select] Removing the capability for the author to provide a datalist element"
state: "closed"
labels: ["select"]
author: "josepharhar"
created_at: "2024-08-12T19:49:38Z"
closed_at: "2025-01-06T21:19:20Z"
url: "https://github.com/openui/open-ui/issues/1082"
comments: 4
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

Now that we are planning to change the parser to basically allow any tag within `<select>` (perhaps except `<input>`), it's worth considering removing the ability for the author to provide their own `<datalist>` element since `<select>` already has the fallback one.

Allowing the author to provide their own `<datalist>` allows them to listen to popover related events and call showPopover/hidePopover on it directly. I'm having a hard time building something that really needs these. For example, I tried making view transitions work but I found that in general making view transitions work on a popover is way too hard.

The author can still target the fallback UA datalist with a pseudo-element, and it will be fully capable and support popover animations for example.

Can anyone think of a use case or capability that we need the author to be able to supply their own `<datalist>` for? If not, I'd like to remove this capability.

<!-- comment id=2284952131 author=josepharhar created=2024-08-12T21:39:48Z url=https://github.com/openui/open-ui/issues/1082#issuecomment-2284952131 -->
## @josepharhar — 2024-08-12

One other thing on this topic - from what I'm seeing so far, I don't think that changing the HTML parser to allow `<input>` inside `<select>` is going to be web compatible enough to ship.

I'm not proposing that `<input>` is allowed in `<select>` in the initial version of the new content model I'm proposing in WHATWG, so from that point of view this is no problem. However, for people who want to build things which we aren't sanctioning as accessible, they will have to use script to manually insert input elements into their selects. Maybe this is OK because it will nudge people towards building things which are accessible, and because those cases will probably need script anyway. If we find a way to make input elements accessible though in the future, we might have to re-introduce `<datalist>` or something though.

<!-- comment id=2291596581 author=mfreed7 created=2024-08-15T15:57:53Z url=https://github.com/openui/open-ui/issues/1082#issuecomment-2291596581 -->
## @mfreed7 — 2024-08-15

Just to clarify, if we resolve this issue the way you're requesting, this will still be allowed, right?

```html
<select>
  <div class=container_for_styling_the_popover>
    <option>...
    <option>...
  </div>
</select>
```

<!-- comment id=2291867706 author=josepharhar created=2024-08-15T18:01:28Z url=https://github.com/openui/open-ui/issues/1082#issuecomment-2291867706 -->
## @josepharhar — 2024-08-15

Yes, that will still be allowed

<!-- comment id=2291908722 author=css-meeting-bot created=2024-08-15T18:25:17Z url=https://github.com/openui/open-ui/issues/1082#issuecomment-2291908722 resolution=true -->
## @css-meeting-bot — 2024-08-15

The Open UI Community Group just discussed `[select] Removing the capability for the author to provide a datalist element`, and agreed to the following:

* `RESOLVED: remove <datalist> from the content model as an element that the author can provide in a <select>, because it doesn't provide any useful capabilities yet`
* `RESOLVED: remove <datalist> from the content model as an element that the author can provide in a <select> because it doesn't provide any useful capabilities yet`

<details><summary>The full IRC log of that discussion</summary>
&lt;dbaron> ScribeNick: dbaron<br>
&lt;dbaron> jarhar: This started when I was updating the explainer to remove &lt;datalist> from some examples.<br>
&lt;dbaron> jarhar: with new parser changes, &lt;datalist> not needed for most rich content<br>
&lt;dbaron> jarhar: it seems we don't need the &lt;datalist> for anything at all if we have a pseudo-element the author can target for the fallback UA datalist<br>
&lt;dbaron> jarhar: further, allowing author to provide their own &lt;datalist> *or* use the fallback one adds a lot of complications and machinery, such as making the datalist a popover without the popover attribute.<br>
&lt;dbaron> jarhar: if we were only using the fallback one we wouldn't need that<br>
&lt;dbaron> jarhar: in terms of capabilitiies, if we're making the parser allow anything, we don't need the datalist to opt in<br>
&lt;dbaron> jarhar: I tried an animation demo with the fallback datalist<br>
&lt;masonf> q+<br>
&lt;dbaron> jarhar: we need the :popover-open pseudo-class to work on the fallback datalist<br>
&lt;una> q+<br>
&lt;dbaron> jarhar: trivial to implement, not sure about spec'ing<br>
&lt;dbaron> masonf: mostly questions:<br>
&lt;dbaron> masonf: I already clarified that you can add a wrapper div or element to add a wrapper for your datalist -- that could, I guess, *be* a datalist element.  You'd need to explicitly make it display:block<br>
&lt;dbaron> jarhar: yeah, that would still be slotted into the fallback datalist.  Not sure if it's even a datalist -- the fallback popover.<br>
&lt;dbaron> masonf: there'd be a fallback popover in the UA shadow dom, containing a slot<br>
&lt;dbaron> jarhar: yes.  That's what I've implemented.  Anything that's not a button or option will be slotted there.  options are a little more complicated...<br>
&lt;masonf> ack mason<br>
&lt;dbaron> masonf: so +1  from me<br>
&lt;masonf> ack una<br>
&lt;dbaron> una: generally not requiring datalist and letting the author decide on the containing element -- sounds great.<br>
&lt;dbaron> una: so in this example you'd have a div with a class that wraps options -- how does the parser know that's the popover?<br>
&lt;dbaron> una: so if you have 2 elements that are siblings, how does it know which are which?<br>
&lt;dbaron> jarhar: with what I'm proposing, the element inside the UA shadow root will be the popover.  The author can't provide the popover anymore.  You can target it with CSS, but can't access from script or provide it yourself.<br>
&lt;dbaron> jarhar: so if you have 2 divs they'll both be slotted inside the UA popover element.<br>
&lt;dbaron> una: if I have 2 divs, sibling div (too hard to scribe full example)<br>
&lt;bkardell_> q+<br>
&lt;dbaron> una: the button is not in the popover<br>
&lt;dbaron> una: can you have wrappers around the button?<br>
&lt;dbaron> jarhar: you can't have wrappers around the button<br>
&lt;masonf> q?<br>
&lt;dbaron> una: the button triggers it, and anything else goes in the popover?<br>
&lt;dbaron> jarhar: yes<br>
&lt;dbaron> jarhar: I couldn't come up with a compelling example where you need the author to have access to their own element that becomes the popover.  The pseudo-element seems good enough.<br>
&lt;dbaron> jarhar: I looked into view transitions and it doesn't seem to help much.<br>
&lt;dbaron> una: one thing was a case of multiple popovers -- sub-popovers -- but I think it seems out of scope<br>
&lt;dbaron> jarhar: but I also don't see how this would limit you -- if you put another popover inside the select it will go inside the UA popover<br>
&lt;dbaron> masonf: and it will be in the top layer no matter what<br>
&lt;scott> so the following<br>
&lt;scott> &lt;select><br>
&lt;scott>   blerp &lt;img src=# alt=foo><br>
&lt;scott>   &lt;selectedoption /><br>
&lt;scott>   &lt;button> [down arrow]&lt;/button><br>
&lt;scott>   &lt;option>nerp&lt;/option><br>
&lt;scott> &lt;/select><br>
&lt;scott> would render as everything but the button inside the popup?<br>
&lt;dbaron> jarhar: if needed we could make the popover hierarchy code aware of this case<br>
&lt;masonf> scott: yes<br>
&lt;dbaron> una: my demos all still use datalist and they still work<br>
&lt;dbaron> jarhar: I haven't implemented this change yet, which is why the demos still work.<br>
&lt;dbaron> masonf: but the only reason it breaks is because datalist is default display:none<br>
&lt;dbaron> masonf: but it might also be ok to put a select > datalist { display: block} in the UA style sheet<br>
&lt;dbaron> jarhar: if we go forward this I'll simplify the content model to drop the concept of having a datalist<br>
&lt;scott> q+<br>
&lt;dbaron> jarhar: so I think replace your datalist with a wrapper div if you need it for styling<br>
&lt;dbaron> una: I think it simplifies the content model.<br>
&lt;bkardell_> q-<br>
&lt;masonf> ack dbaron<br>
&lt;masonf> dbaron: I think speccing the thing with the selectors shouldn't be hard, because it just depends on part-like pseudo elements.<br>
&lt;masonf> dbaron: once you hook into that concept, it's basically done.<br>
&lt;masonf> q?<br>
&lt;masonf> ack scott<br>
&lt;jarhar> q?<br>
&lt;dbaron> scott: I'm not opposed to this, just want to understand:  if allowing people to use &lt;datalist> is just removed, does that potentially roadblock the chance of in the future making comboboxes that open explicit dialogs or explicit grids?  That was one of the reasons I wanted this.  I understand the default would be a listbox of options because that's what it's been for 25+ years.<br>
&lt;dbaron> scott: The fallback is that that just goes into a popup is that browsers repair that, so we expose it as a dialog so people can search around for stuff.<br>
&lt;masonf> q+<br>
&lt;dbaron> scott: my hope there would be that in the future if you wanted a combobox that exposes a dialog you could declare that rather than making the browser guess.<br>
&lt;dbaron> scott: by taking this out now I'm not sure how it woud be put back in<br>
&lt;dbaron> jarhar: My understanding you previously talked about having the author put a dialog rather than a datalist -- we could still upgrade to a world where you do have a datalist or a dialog and you slot that in.  But in this initial version I don't think it makes sense to propose extra machinery for something that's not used yet.<br>
&lt;dbaron> scott: my fear is that by not alluding to it we will close the door on it.<br>
&lt;dbaron> scott: but if you're not concerned about that then I won't be.<br>
&lt;dbaron> jarhar: If I can't make an example of how allowing the &lt;datalist> introduces new capabilities, it's not clear why we have it.<br>
&lt;dbaron> jarhar: But if we make a new listbox primitive, or other explicit choice, we could still upgrade to that.<br>
&lt;dbaron> masonf: just a clarifying question to check that it's future us and not current us:  if the developer puts things in the select that cause it to have to be exposed as a dialog rather than a menu -- the developer would say something in devtools that says we've changed it to dialog.  And we say that to get rid of the warning you should put a dialog in.<br>
&lt;dbaron> scott: how can you warn people if [???] ?<br>
&lt;dbaron> masonf: I think either way the warning shows up.<br>
&lt;dbaron> masonf: I think if the browser has to switch it to dialog role it should have a warning.<br>
&lt;dbaron> masonf: But I think the warning just has to say not to use &lt;select><br>
&lt;dbaron> scott: I think it's ok for the initial release... but I'm worried that by taking this off the table it becomes harder to add in the future.<br>
&lt;dbaron> masonf: 2 ways to build that thing with extra content:  one is to put a dialog inside the select and keep using select, or other is later on we introduce another new element for that purpose.  Do we know if one is better than the other?<br>
&lt;dbaron> scott: I think many use cases are arguably other elements anyway.<br>
&lt;masonf> q?<br>
&lt;dbaron> masonf: combobox (free form text + suggestions) has been suggested, but I think it's a new element or new use of &lt;input type=text><br>
&lt;masonf> ack mason<br>
&lt;jarhar> proposed resolution: remove &lt;datalist> from the content model as an element that the author can provide in a &lt;select> because it doesn't provide any useful capabilities yet<br>
&lt;una> LGTM<br>
&lt;scott> +1<br>
&lt;keithamus> +1<br>
&lt;dbaron> RESOLVED: remove &lt;datalist> from the content model as an element that the author can provide in a &lt;select>, because it doesn't provide any useful capabilities yet<br>
&lt;jarhar> RESOLVED: remove &lt;datalist> from the content model as an element that the author can provide in a &lt;select> because it doesn't provide any useful capabilities yet<br>
</details>
