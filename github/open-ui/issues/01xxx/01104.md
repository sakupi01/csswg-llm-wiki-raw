---
number: 1104
title: "Link delegation to descendant"
state: "open"
labels: ["a11y-tracker"]
author: "noamr"
created_at: "2024-09-30T18:45:15Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1104"
comments: 35
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

As per spec, the `a` element cannot nest, you cannot put links inside links.
This is important from an a11y perspective and for other reasons.
However, many websites today (e.g. Reddit, Rotten Tomatoes) implement this kind of link "nesting" on their own with somewhat verbose javascript.

Proposing to discuss finding a web platform solution for this that would take accessibility and ergonomics into account.
This solution might look like link delegation
```html
<section link="item1-link">
   <link id="item1-link" href="details?item=1">
</section>
```

In the above example, the `link` attribute of the `section` element "delegates" the link to an `a` (or `area`) descendant with the given ID. UAs should implement this by making the section's default click action delegate the click to the `item1-link` element, and they may support it in other UI such as right-clicking the `section`.

<!-- comment id=2387069901 author=SzymonNadbrzezny created=2024-10-01T21:02:19Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387069901 -->
## @SzymonNadbrzezny — 2024-10-01

Could someone explain to me what link nesting/link delegation would be used for? I'm looking currently at Reddit and I'm wondering whether the example of post applies, since they link to post and link from post's description are overlaping?

I personally understand the neatness of this behaviour from user perspective, but it's also a bit annoying for me when I'm trying to enter post and I'm redirected or just have image opened up.

EDIT:
I'm your average user as far as any accesibility is concerned, but might be also biased since I'm not used to that design choice.

<!-- comment id=2387107690 author=noamr created=2024-10-01T21:27:59Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387107690 -->
## @noamr — 2024-10-01

> Could someone explain to me what link nesting/link delegation would be used for? I'm looking currently at Reddit and I'm wondering whether the example of post applies, since they link to post and link from post's description are overlaping?
> 
> I personally understand the neatness of this behaviour from user perspective, but it's also a bit annoying for me when I'm trying to enter post and I'm redirected or just have image opened up.
> 
> EDIT: I'm your average user as far as any accesibility is concerned, but might be also biased since I'm not used to that design choice.

Check for example [walmart](https://www.walmart.ca/en/shop/summer-clearance/5396675859953?catId=10017&icid=deals_page_other_upto_50_off_clothing_small_round_hsk_95680_73W9TSAALR
). You can click on the whole item to go into the item, or click on a particular color to go to the item with that color already selected (when color is applicable). They achieve this with an `<a>` element on top of the regular content (using `z-index`).
This is not an uncommon pattern on the web, and using `z-index` tricks for this seems suboptimal...

<!-- comment id=2387111278 author=gfellerph created=2024-10-01T21:30:31Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387111278 -->
## @gfellerph — 2024-10-01

@noamr beat me to it, but I think card components (wich can mean a lot of things) would generally be a use case for this. A lot of times you have markup like this:

```html
<div class="card">
  <img src="..." />
  <h3>Title</h3>
  <p>Description</p>
  <a href="">Read more</a>
</div>
```

and the requirement is to make the whole card clickable. While you can wrap the `<a>` tag around the whole card, or make the `<a>` tag the card itself, the content of the card becomes the accessible name of the card and that's only the start of the problems with that approach. If you overlay the whole card with the `<a>` tag, all other items become unselectable for pointers (download image, select text for translation or lookup, ...). With the possibility to delegate clicks on `<div class="card">` to the link, it would be possible to keep the nice structure and declaratively make the whole card clickable.

@lukewarlow or @keithamus would this be something that [invokers](https://open-ui.org/components/invokers.explainer/) could do?

<!-- comment id=2387130777 author=SzymonNadbrzezny created=2024-10-01T21:44:16Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387130777 -->
## @SzymonNadbrzezny — 2024-10-01

> > Could someone explain to me what link nesting/link delegation would be used for? I'm looking currently at Reddit and I'm wondering whether the example of post applies, since they link to post and link from post's description are overlaping?
> > I personally understand the neatness of this behaviour from user perspective, but it's also a bit annoying for me when I'm trying to enter post and I'm redirected or just have image opened up.
> > EDIT: I'm your average user as far as any accesibility is concerned, but might be also biased since I'm not used to that design choice.
> 
> Check for example [walmart](https://www.walmart.ca/en/shop/summer-clearance/5396675859953?catId=10017&icid=deals_page_other_upto_50_off_clothing_small_round_hsk_95680_73W9TSAALR). You can click on the whole item to go into the item, or click on a particular color to go to the item with that color already selected (when color is applicable). They achieve this with an `<a>` element on top of the regular content (using `z-index`). This is not an uncommon pattern on the web, and using `z-index` tricks for this seems suboptimal...

So they achieve it similarly to how Reddit does it. I'm honestly 50/50 on it because it feels like the result could be very confusing for the end-user especially if the developer didn't take proper measures to make user know they are hovering over something that is also interactable inside card like this. 

<!-- comment id=2387141425 author=noamr created=2024-10-01T21:51:53Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387141425 -->
## @noamr — 2024-10-01

> > Check for example [walmart](https://www.walmart.ca/en/shop/summer-clearance/5396675859953?catId=10017&icid=deals_page_other_upto_50_off_clothing_small_round_hsk_95680_73W9TSAALR). You can click on the whole item to go into the item, or click on a particular color to go to the item with that color already selected (when color is applicable). They achieve this with an `<a>` element on top of the regular content (using `z-index`). This is not an uncommon pattern on the web, and using `z-index` tricks for this seems suboptimal...
> 
> So they achieve it similarly to how Reddit does it. I'm honestly 50/50 on it because it feels like the result could be very confusing for the end-user especially if the developer didn't take proper measures to make user know they are hovering over something that is also interactable inside card like this.

In the Walmart case I don't see much "harm" in it. The whole card is clickable, and clicking the color refines the link to something more specific. 
At Wix we had something similar where a phone number inside a link/card would be automatically clickable as a `tel:` link. This was really useful for small businesses.

<!-- comment id=2387155156 author=lukewarlow created=2024-10-01T22:03:08Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387155156 -->
## @lukewarlow — 2024-10-01

Currently command invokers require a custom element or a button trigger so this sort of thing would work using that API. It also feels a specific and nuanced enough problem to warrant it's own functionality.

I'm wondering if this could be something more generic however. Labels can already extend the click target of a nested/associated form control. Are there other situations like this where expanding the click area without applying other semantics is desired?

Whether this is a good pattern I'm genuinely not sure. But I would anecdotally say it's a common one. Certainly common enough to warrant a discussion even if the outcome is 'we're aware of this pattern here's the reasons it's bad'.

<!-- comment id=2387781087 author=noamr created=2024-10-02T07:14:29Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2387781087 -->
## @noamr — 2024-10-02

> Currently command invokers require a custom element or a button trigger so this sort of thing would work using that API. It also feels a specific and nuanced enough problem to warrant it's own functionality.
> 
> I'm wondering if this could be something more generic however. Labels can already extend the click target of a nested/associated form control. Are there other situations like this where expanding the click area without applying other semantics is desired?

The problem with labels specifically is that they require an additional element, which means this can't be used on `<tr>` or other situations where adding an element would affect parsing. That's why the proposal is going towards an attribute-based approach, but of course everything is open.

<!-- comment id=2389422292 author=scottaohara created=2024-10-02T18:29:48Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389422292 -->
## @scottaohara — 2024-10-02

i'm of two minds about something like this.

First, I have an initial positive reaction to this because wrapping structured content similar to the example [card markup in this comment](https://github.com/openui/open-ui/issues/1104#issuecomment-2387111278) in a hyperlink (`<a href>`) is a rubbish practice.  and the hack of using a positioned hyperlink or hyperlink's before or after pseudo content can be problematic as well.

I also find the use case for `tr` elements being able to reference a hyperlink within one of their cells as being the intended result of clicking 'anywhere' on the row as being a very common use case that would be nice to account for.  It'd help drive home the idea that if one wants to make the row clickable, there MUST be a hyperlink to reference within the row so that it can be consistently accessible.  (not all screen readers consistently allow for navigating tables by rows, and rows in and of themselves are not generally expected to be interactive - so the existence of the hyperlink is often essential)


The concern I have for something like this is no different than the concern i have for the current hacks people have to resort to for this 'desired' UX - in that this practice can often result in accidental activations.  Speaking for myself, I have often accidentally activated many 'clickable cards' like this as i'm simply trying to swipe/scroll through a UI on my phone.  I also have a tendency to accidently click on UI when using my mouse, and generally this is "fine"... except for in these cases where large sections of content that don't visually appear like they should be interactive, are.

Additionally, per the new WCAG 2.2 Target Size minimum SC, i'd be concerned with the potential for undersized targets to exist within the larger clickable target... I could see instances of what the SC currently has exceptions for, not taking something like this into account, since some of those exceptions were created under the present understanding that nested interactive elements aren't valid.  
For example, a secondary link within a string of text would often fall short of meeting _both_ the minimum 24 width AND height requirements of the SC (ltr / rtl words commonly meeting the width, but not necessarily the height requirement).   Smaller target sizes in that case being more of a potential usability issue. But if the entire section of content is clickable, then the potential for accidental activation for someone trying to click the 'nested' link, but instead hitting the larger 'target' becomes far more likely.  

I'm not mentioning the above in any sort of "i think this is a bad idea" sort of way... it's a very common pattern and being able to do this without hyperlink hacks or JS would be nice.  But, we should also acknowledge that the 'large clickable pattern' has at least 'minor' usability issues related to accidental activation in some contexts - and absolutely has increased usability issues for those with motor disabilities when there are nested interactive elements.  

<!-- comment id=2389574598 author=noamr created=2024-10-02T20:02:31Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389574598 -->
## @noamr — 2024-10-02

> i'm of two minds about something like this.
> 
> First, I have an initial positive reaction to this because wrapping structured content similar to the example [card markup in this comment](https://github.com/openui/open-ui/issues/1104#issuecomment-2387111278) in a hyperlink (`<a href>`) is a rubbish practice. and the hack of using a positioned hyperlink or hyperlink's before or after pseudo content can be problematic as well.
> 
> I also find the use case for `tr` elements being able to reference a hyperlink within one of their cells as being the intended result of clicking 'anywhere' on the row as being a very common use case that would be nice to account for. It'd help drive home the idea that if one wants to make the row clickable, there MUST be a hyperlink to reference within the row so that it can be consistently accessible. (not all screen readers consistently allow for navigating tables by rows, and rows in and of themselves are not generally expected to be interactive - so the existence of the hyperlink is often essential)
> 
> The concern I have for something like this is no different than the concern i have for the current hacks people have to resort to for this 'desired' UX - in that this practice can often result in accidental activations. Speaking for myself, I have often accidentally activated many 'clickable cards' like this as i'm simply trying to swipe/scroll through a UI on my phone. I also have a tendency to accidently click on UI when using my mouse, and generally this is "fine"... except for in these cases where large sections of content that don't visually appear like they should be interactive, are.
> 
> Additionally, per the new WCAG 2.2 Target Size minimum SC, i'd be concerned with the potential for undersized targets to exist within the larger clickable target... I could see instances of what the SC currently has exceptions for, not taking something like this into account, since some of those exceptions were created under the present understanding that nested interactive elements aren't valid. For example, a secondary link within a string of text would often fall short of meeting _both_ the minimum 24 width AND height requirements of the SC (ltr / rtl words commonly meeting the width, but not necessarily the height requirement). Smaller target sizes in that case being more of a potential usability issue. But if the entire section of content is clickable, then the potential for accidental activation for someone trying to click the 'nested' link, but instead hitting the larger 'target' becomes far more likely.
> 
> I'm not mentioning the above in any sort of "i think this is a bad idea" sort of way... it's a very common pattern and being able to do this without hyperlink hacks or JS would be nice. But, we should also acknowledge that the 'large clickable pattern' has at least 'minor' usability issues related to accidental activation in some contexts - and absolutely has increased usability issues for those with motor disabilities when there are nested interactive elements.

I resonate with all of your concerns, they are all valid!
To respond to some of them, I tend to look at the inner click as a progressive enhancement. The whole {card|row|item} is clickable and would lead you to the right place, and the inner click is potentially a refinement of that link, like the Walmart example where the inner link already chooses a color for a shirt that the outer link would lead to.

If the inner link is a progressive enhancement, sometimes missing it and clicking on the outer link by mistake is perhaps acceptable?

<!-- comment id=2389714541 author=scottaohara created=2024-10-02T21:19:20Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389714541 -->
## @scottaohara — 2024-10-02

i think you are thinking of this in the entirely opposite way i am.  since the inner element, the actual hyperlink for the item, is the thing that would need to be actionable / keyboard accessible / identified as a hyperlink.

the outer 'card' or row, for instance, is not an interactive element. it should not generally be keyboard accessible, and in many cases wouldn't be expected to be.  This is one reason why people 'hack' this now with hyperlinks that overlay the larger clickable area.  because it's a hyperlink that people using keyboard or assistive technology would expect to interact with.

additionally, my comments about the potential for nested interactives extends to the fact that often these sorts of components consist of a couple if not a handful of other links / buttons that are secondary actions and not related to the outside click (some might take you to the same page... some don't - or in the case of favoriting, deleting, etc. for such items, those controls might be on the subsequently linked page, but falling back to that is not a reasonably equitable UX if someone is trying to favorite/delete a bunch of items on one page, rather than having to navigate to a page, perform action, navigate back, find new item, perform same action sequence again...).  i realize you were not commenting on all of my points, but these would still be usability issues regardless.

so no.  i'm afraid we're not aligned in what we'd consider acceptable here.  i'm wondering now if i imposed my own assumptions on how this could be helpful and accessible onto something that hasn't actually yet accounted for the latter.

<!-- comment id=2389727867 author=dbaron created=2024-10-02T21:29:22Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389727867 -->
## @dbaron — 2024-10-02

Yeah, I was also going to ask about the "nested interactive elements" aspect here, but I think @scottaohara beat me to it.  I agree it's worth clarifying what your expectations for this sort of thing are regarding tab order and keyboard navigation, exposure to assistive technology (where my understanding is that nested interactive elements are often problematic), exposure to any other UA features that enumerate/navigate links, etc.

<!-- comment id=2389754976 author=noamr created=2024-10-02T21:51:27Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389754976 -->
## @noamr — 2024-10-02

> Yeah, I was also going to ask about the "nested interactive elements" aspect here, but I think @scottaohara beat me to it. I agree it's worth clarifying what your expectations for this sort of thing are regarding tab order and keyboard navigation, exposure to assistive technology (where my understanding is that nested interactive elements are often problematic), exposure to any other UA features that enumerate/navigate links, etc.

Currently the idea is that the external element would not be part of the tab order / default keyboard navigation, and would only extend the clickable area and potentially additional items in the context menu like "open in a new tab".

<!-- comment id=2389761938 author=noamr created=2024-10-02T21:57:22Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2389761938 -->
## @noamr — 2024-10-02

> i think you are thinking of this in the entirely opposite way i am. since the inner element, the actual hyperlink for the item, is the thing that would need to be actionable / keyboard accessible / identified as a hyperlink.

I think I misrepresented the example. I was thinking of a situation where there is more than one link - one being the "default" link which the enclosing element delegates to, and additional ones that go "deeper", like pre-choosing a color. This would enhance the click area of the default link but won't do much for the other ones.

> 
> the outer 'card' or row, for instance, is not an interactive element. it should not generally be keyboard accessible, and in many cases wouldn't be expected to be. This is one reason why people 'hack' this now with hyperlinks that overlay the larger clickable area. because it's a hyperlink that people using keyboard or assistive technology would expect to interact with.

Yup, there are no plans to make these outer elements keyboard interactive. All they do is delegate the click area (and potentially some context menu items) to the link.

> 
> additionally, my comments about the potential for nested interactives extends to the fact that often these sorts of components consist of a couple if not a handful of other links / buttons that are secondary actions and not related to the outside click (some might take you to the same page... some don't - or in the case of favoriting, deleting, etc. for such items, those controls might be on the subsequently linked page, but falling back to that is not a reasonably equitable UX if someone is trying to favorite/delete a bunch of items on one page, rather than having to navigate to a page, perform action, navigate back, find new item, perform same action sequence again...). i realize you were not commenting on all of my points, but these would still be usability issues regardless.

Absolutely. Having nested links that act in a way that conflicts with the default link is a usability issue here we have to address. I wonder if having something like this in the web platform gives us more opportunities to address this than letting people hack around it with `z-index`. If we arrive at the conclusion that it just adds more footguns, we won't do it.

<!-- comment id=2392218165 author=nmn created=2024-10-03T19:56:08Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2392218165 -->
## @nmn — 2024-10-03

I'm seeing a bunch of concerns about a11y for this pattern. I want to clear the air and explain that we at Meta (while working on facebook.com) arrived at this exact solution to *solve* our a11y problems.

---

The use-case has been described well. Imagine a card, say to a profile. This card contains a big profile photo followed by the username in large font-size which is also a link to the profile page of this user.

Further down, there are a few action buttons to say, message the user, follow them, etc.

---

The card, as-is, is already accessible. There are no nested interactive elements and everything has the correct labels for screenreader users. Now, if we want to make the *whole* card clickable and to act as a link to the user's profile what are our options?

1. The best solution so far is to use `absolute` positioning on the `<a>` around the username and make it as big as the card itself and then use `z-index` to ensure the other buttons in the card are on top of this link.
  - If we need non-static positioning on anything between the card itself and the link, this system breaks down.
  - For sighted keyboard users, this messes up the focus ring as it will go around the whole card by default.
  - z-index solutions are risky in general.
2. Another solution is to render a link as a sibling of the card and use absolute position over the whole card.
  - This creates duplicate links to the same content
  - The label for the container needs to be manually set as `aria-describedby` cannot be used safely. Pointing it to the whole card creates nested links in the screenreader text.

---

So, link delegation ends up being a great and simple solution. After going through many other solutions which were all very challenging I proposed this solution to our a11y team internally and it was our best solution to this problem.

- The outer card container element remains a normal `div`.
- It does not have any special role.
- It is not focusable
- It does not behave like a link in anyway

It just has a JS click handler which forwards the click to the "target link" within. That's it. It's *only* a mouse click (and touch) affordance. It automatically ignores any clicks nested interactive elements.

---

So, overall, this proposal doesn't create any new a11y issues. The links within the card should still be big enough and accessible in all the ways links and buttons should be. The card forwarding its clicks to one of them doesn't change the calculus.

Finally, the concern about large clickable targets making things harder to scroll etc, that's a design issue and not directly relevant to this proposal IMO. There are plenty of large buttons and links that are not cards and do not need nested interactive elements.

<!-- comment id=2393731617 author=scottaohara created=2024-10-04T13:38:48Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2393731617 -->
## @scottaohara — 2024-10-04

>I think I misrepresented the example. I was thinking of a situation where there is more than one link - one being the "default" link which the enclosing element delegates to, and additional ones that go "deeper", like pre-choosing a color. This would enhance the click area of the default link but won't do much for the other ones.

thanks for clarifying @noamr - because yes, your prior message made it seem like someone could progressively enhance this proposal by putting an actual link into the container, but could otherwise define this without having a link in there at all.  Your follow up alleviates my concern with this as that just seems a misunderstanding now.  I appreciate you acknowledging the increased click area for the 'default' link, but that any other 'nested' targets would be whatever size the author made them - which is where my primary concern is, and will likely be something to address via author/design awareness.

re: @nmn's response...
your comment appears to be trying to squash the concerns that came about due to noam's [follow up](https://github.com/openui/open-ui/issues/1104#issuecomment-2389574598) to my original comment.  as just mentioned, that seemed to be largely a misunderstanding of what he meant when he said "If the inner link is a progressive enhancement, sometimes missing it and clicking on the outer link by mistake is perhaps acceptable?"

your response seems to be very focused on screen reader accessibility.  your ending paragraphs seem a bit dismissive about potential issues for target sizes though... Facebook generally does a good job with ensuring targets within larger interactive targets meet the new wcag 2.2 minimum target size.  though instagram is an app i have some issues with accidental clicks (or in my case, taps).  The most egregious instances though being websites that have sticky videos with dismiss/minimize buttons overlaid on top.  The target of the button is often too small and it's hard to click/tap it and not the video (which may then pause the video one was trying to watch, or if the video also has JS click event / overlaid invisible hyperlink - accidental navigation to a new web page can occur).

I do agree with you though, that my concerns here are not "new" a11y issues... nor as I already mentioned were they meant as opposition to the idea (again - where concern arose was due to the now cleared-up misunderstanding). But my point in bringing up target size issues is so that they can be acknowledged.  Raising the potential for someone to have an idea of how such instances could potentially be accommodated through a potential standardization of such a feature.  Or at the very least, author guidance be provided to help ensure best practices for sizing of nested targets to at least meet wcag minimums.  When these sorts of concerns don't get raised, then it becomes less likely they get addressed in any potential work/explainer/spec text or author guidance.

i hope this helps clarify where i'm coming from here, and that i simply want to make sure we think about more than just the screen reader experience.  addressing those concerns might have zero actual impact on how this feature is developed - and may end up simply being a unified understanding a messaging on how authors can use this feature and ensure the best UX they can for any nested targets beyond the primary cta/hyperlink

<!-- comment id=2394531237 author=nmn created=2024-10-04T20:37:19Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2394531237 -->
## @nmn — 2024-10-04

I understand and appreciate your clarification @scottaohara. Sorry for any miscomprehension on my part.

> Raising the potential for someone to have an idea of how such instances could potentially be accommodated through a potential standardization of such a feature.

I have an idea here, but they don't *feel* actually doable.

- Essentially if the "container element" (what do we call this container that delegates its click? The "delegator container"? We call the component "ContainerPressable") could somehow *detect* all the clickable child elements and leave larger gaps in its *own* hit area, that would be pretty awesome.

<!-- comment id=2402306379 author=jakearchibald created=2024-10-09T13:10:40Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2402306379 -->
## @jakearchibald — 2024-10-09

@nmn I'm struggling to understand:

> 1. The best solution so far is to use `absolute` positioning on the `<a>` around the username and make it as big as the card itself and then use `z-index` to ensure the other buttons in the card are on top of this link.

…leading to:

> So, link delegation ends up being a great and simple solution. After going through many other solutions which were all very challenging I proposed this solution to our a11y team internally and it was our best solution to this problem.
> 
> * The outer card container element remains a normal `div`.
> * It does not have any special role.
> * It is not focusable
> * It does not behave like a link in anyway
> 
> It just has a JS click handler which forwards the click to the "target link" within. That's it. It's _only_ a mouse click (and touch) affordance. It automatically ignores any clicks nested interactive elements.

Is the `absolute` positioning solution still the "best solution so far", or are you saying a simple JS click handler is better? If the `absolute` solution is still better, why?

<!-- comment id=2402886165 author=nmn created=2024-10-09T17:21:27Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2402886165 -->
## @nmn — 2024-10-09

@jakearchibald Fair question!

The `absolute` positioning solution is the *commonly used* best solution, and the best solution that doesn't depend on JS.

I believe link delegation is actually a *better* solution.

<!-- comment id=2403110008 author=scottaohara created=2024-10-09T18:52:43Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2403110008 -->
## @scottaohara — 2024-10-09

i think that's an important qualifier.   absolute positioning is the current "best" solution that doesn't involve JS.  I'd argue that using JS is really a better solution for tackling this now... but for the fact it necessitates JS which i know people want to avoid.

<!-- comment id=2405777834 author=css-meeting-bot created=2024-10-10T18:27:41Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2405777834 resolution=true -->
## @css-meeting-bot — 2024-10-10

The Open UI Community Group just discussed `Link delegation to descendant`, and agreed to the following:

* `RESOLVED: Incubate an explainer for a new solution for declarative link delegation`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> masonf: ok I can try and do it justice<br>
&lt;hdv> masonf: thing is, you can't nest anchors<br>
&lt;hdv> masonf: but there are times where you want to act something is clickable<br>
&lt;hdv> masonf: but nesting anchors is bad for all kinds of reasons<br>
&lt;hdv> masonf: so an attribute, something like 'link' can be added to a container that you refer to by idref, so that the container behaves / works like you're clicking on the link<br>
&lt;hdv> masonf: so the link attr would do nothing to the accessibility tree, only the existing link would be in there. Proposal would also be that the keyboard behaviour was @@@<br>
&lt;hdv> masonf: it would need to point inward, as otherwise would be risky<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> q+<br>
&lt;hdv> namangoel: that covers everything, it is what we ended up doing for Facebook<br>
&lt;hdv> namangoel: outer div is just a div with a click handler, intentionally with no specific semantics<br>
&lt;hdv> masonf: no interactive elements would allow for this<br>
&lt;hdv> namangoel: the use case on Facebook is all cards… eg each cards has a profile and some buttons like follow and message. We want you to be able to click the whole card, and also the buttons in the card<br>
&lt;hdv> namangoel: before we made the whole container a link and it was horrible for accessibility so we reverted that<br>
&lt;hdv> namangoel: one solution that exists elsewhere is an abspos hack<br>
&lt;hdv> namangoel: the problem with that one is that it messes with the focus rings and makes that misleading… and difficult to use in all scenarios eg if you need relative positioned elements inside<br>
&lt;hdv> namangoel: our current solution avoids that<br>
&lt;hdv> namangoel: by making the outer div just have the one click handler and nothing else<br>
&lt;hdv> namangoel: we just forward the click event to the inner element. I don't think we handle right clicks, that's probably worth discussing<br>
&lt;hdv> ack greg<br>
&lt;hdv> gregwhitworth: can I ask why this was deferred?<br>
&lt;frehner> q+<br>
&lt;hdv> masonf: because noam wanted to introduce it<br>
&lt;hdv> gregwhitworth: making this declarative seems to have various benefits<br>
&lt;hdv> gregwhitworth: wrapping the whole thing in an anchor was another way I saw, which was problematic<br>
&lt;hdv> gregwhitworth: I am not opposed to this… agree with masonf we need exclusions, for instance for interactive elements as masonf mentioned<br>
&lt;hdv> masonf: can see pushback go further<br>
&lt;hdv> masonf: what if it would only work on a div?<br>
&lt;hdv> keithamus: is there a reason to do that? or are you talking spiritually?<br>
&lt;hdv> masonf: no literally<br>
&lt;gregwhitworth> q?<br>
&lt;hdv> masonf: because divs (and spans) are non semantic elements which would avoid the messing with existing elements<br>
&lt;gregwhitworth> ack frehner<br>
&lt;hdv> keithamus: does section have semantics?<br>
&lt;hdv> masonf: it's a grouping el<br>
&lt;hdv> masonf: probably makes it easier to get to WHATWG if it applied to a specific list of elements<br>
&lt;hdv> q?<br>
&lt;hdv> frehner: was looking at implementing this pattern but for tables, eg table rows<br>
&lt;keithamus> q+<br>
&lt;hdv> +1 to the table use case! I've had to built this in the past…<br>
&lt;hdv> gregwhitworth: the goal would be, I think, to land an explainer for this feature?<br>
&lt;hdv> masonf: yes and for now to see if there is an interst<br>
&lt;hdv> s/interst/interest<br>
&lt;hdv> masonf: and also, do we want to incubate it here in Open UI<br>
&lt;hdv> gregwhitworth: I kind of want to avoid restricting it prematurely<br>
&lt;hdv> gregwhitworth: I like the premise of frehner's use case<br>
&lt;hdv> gregwhitworth: we should probably look at a few grouping elements… but if there are concrete other use cases that we can justify that's great<br>
&lt;nmn> Let's just restrict it to *not* work on any interactive element at first?<br>
&lt;hdv> gregwhitworth: though with tables it adds a lot of complexities, eg how AT handle tables, could think of v1 vs v2<br>
&lt;masonf> q?<br>
&lt;gregwhitworth> q?<br>
&lt;hdv> masonf: but this attr probably wouldnt do anything with AT, just a click handler<br>
&lt;hdv> namangoel: think we should explicitly exclude it from buttons, links and interactive element… and just add it to generic containers<br>
&lt;hdv> ack keith<br>
&lt;hdv> keithamus: what's bugging me… where does it go…going back to masonf's comment, should it just go on a div? I know it's not an assistive feature, but it kind of is. This is an affordance to mouse users, it is semantically relevant. Seems disjoint to me to only allow on elements that have no semantics, because now suddenly we give divs semantic value within the page<br>
&lt;hdv> keithamus: so kind of wonder if it should be a new element that has this semantic<br>
&lt;gregwhitworth> q+<br>
&lt;masonf> q+<br>
&lt;hdv> keithamus: and then we can look at specifically allowing it in certain positions like table rows<br>
&lt;hdv> keithamus: as opposed to an attribute that goes everywhere<br>
&lt;hdv> masonf: making it an element would make this proposal a lot harder it seems to me?<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> masonf: but agree this may have semantics of some kind… though it seems from the initial proposal that it doesn't have semantics<br>
&lt;masonf> q+ nmn<br>
&lt;hdv> gregwhitworth: I'm not hearing objections to the potential exploration of this<br>
&lt;hdv> gregwhitworth: we'll need to find someone to work on the explainer<br>
&lt;gregwhitworth> ack gregwhitworth<br>
&lt;hdv> masonf: we may have docs already internally<br>
&lt;gregwhitworth> ack nmn<br>
&lt;hdv> nmn: I am also scared of a new element, would complicate things, might need `display:contents` by default or somerthing<br>
&lt;hdv> nmn: so restricting to `div`s and `span`s probably be better. Don't think this would change semantics, just provide convenience for click handling<br>
&lt;keithamus> q+<br>
&lt;hdv>  keithamus: I think it depends on your definition of semantics… it changes the behaviour of the element, because the interaction element changes for the element<br>
&lt;gregwhitworth> ack keithamus<br>
&lt;hdv> keithamus: right now, if you click inside of a div, nothing will happen, but with this attr, something will happen… my concern is, if we limit to div, the concept of a div people have kind of changes… from being just a container, to being an element that can house a delegation behaviour<br>
&lt;masonf> q+<br>
&lt;hdv> keithamus: restricting to “non semantic elements” would kind of turn those into “semantic elements”, would almost prefer, not that I like that a lot, see it to be only allowed on, say, a `section`<br>
&lt;gregwhitworth> ack masonf<br>
&lt;hdv> masonf: seems clear to me the explainer needs a section on semantics<br>
&lt;hdv> masonf: I kind of think of this of purely a behaviour… the parallel here is the popover attribute, that you can stick on a div, and makes the behaviour of the div different, not the semantics<br>
&lt;hdv> q+<br>
&lt;hdv> keithamus: if we say semantics = roles, that's one definition of semantics, but there are wider definitions of semantics<br>
&lt;hdv> keithamus: in my opinion popover is semantic<br>
&lt;hdv> masonf: there was a long discussion on this in popover… I hate the word semantic, has too many definitions<br>
&lt;gregwhitworth> hdv: as a philosophy major I love the term semantics, it's ridiculous<br>
&lt;gregwhitworth> hdv: the other definition of semantic that is used in ARIA specs is anything that happens in the accessibility tree so that can apply to popover and I kind of agree with Keith that it does apply semantics but more than being a div<br>
&lt;masonf> q?<br>
&lt;masonf> ack hdv<br>
&lt;gregwhitworth> ack hdv<br>
&lt;gregwhitworth> Proposed Resolution: Incubate an explainer for a new solution for declarative link delegation<br>
&lt;hdv> gregwhitworth: I think we need an explainer<br>
&lt;masonf> +1<br>
&lt;hdv> +1<br>
&lt;gregwhitworth> RESOLVED: Incubate an explainer for a new solution for declarative link delegation<br>
&lt;masonf> My whole VC unit just rebooted, sorry<br>
</details>

<!-- comment id=2413430737 author=noamr created=2024-10-15T09:54:20Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2413430737 -->
## @noamr — 2024-10-15

Explainer PR: #1109 

<!-- comment id=2800232387 author=github-actions[bot] created=2025-04-14T00:54:31Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-2800232387 -->
## @github-actions[bot] — 2025-04-14

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.

<!-- comment id=3151387080 author=frehner created=2025-08-04T16:08:08Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3151387080 -->
## @frehner — 2025-08-04

I've prototyped this in a table scenario with [buttons](https://codepen.io/afrehner/pen/raOyvrv) and [links](https://codepen.io/afrehner/pen/empvrjM) and [checkboxes](https://codepen.io/afrehner/pen/jEbmebw), and here are some takeaways from playing:

- The explainer mentions "Approach 2: a new element type". Given the table use-case, I think it's best to rule out this approach as inserting a new element into table structure would likely be painful and not backwards compatible. An attribute makes this much easier to adopt for tables.
- Name bikeshedding #1253
- Modifier key passthrough #1254
- Target styling #1255
- buttons and checkbox support #1256

I also wondered about security, and how this makes it harder to tell what action is actually being taken. But upon reflection, it seems like this is no more insecure than allowing a `div` to have a click handler anywhere else, so it didn't seem like it was opening the door to new dark patterns. 

<!-- comment id=3714534693 author=eirikbacker created=2026-01-06T12:31:30Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3714534693 -->
## @eirikbacker — 2026-01-06

Hi! Regarding ` :link-delegate-active`  and `:link-delegate-hover`, is it correctly understood that these states are intended for the target element, and not the container?

**Approach 1:**
```
<tr clickdelegation="button-1">
  <td><button type="button" id="button-1">Row action</button></td> ⬅️ The target element gets :click-delegate-hover
</tr>
```
or
**Approach 2:**
```
<tr clickdelegation="button-1"> ⬅️ The container element gets :click-delegate-hover
  <td><button type="button" id="button-1">Row action</button></td>
</tr>
```

In my opinion, approach 1 would be best, as this allows me to choose wether the button should visually indicate hover, or the container (using `:has(:click-delegate-hover)`) ☺️ 

<!-- comment id=3714957126 author=eirikbacker created=2026-01-06T14:40:04Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3714957126 -->
## @eirikbacker — 2026-01-06

PS: I see your codepen examples requires `id` for each target element @frehner ☺️ 
Is it still possible to look into the direction you proposed with something like the `defaultlink` attribute to avoid having too many generated id's? 🤔 

With `id`:
````
<tr clickdelegate="my-button">
  <td><button id="my-button"></button></td>
<tr>
````

With `clicktarget`:
```
<tr clickdelegate>
  <td><button clicktarget></button></td>
<tr>
```

<!-- comment id=3723005575 author=nmn created=2026-01-08T09:33:32Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3723005575 -->
## @nmn — 2026-01-08

On the discussion about `:click-delegate-hover` I feel pretty strongly that the most useful thing would be for the `button` within the card to get the `:click-delegate-hover` pseudo class when the card is hovered.

Let's consider the various cases:

### Case 1. The button is hovered.
- The button can use the `:hover` pseudo class
- The card can *also* use the `:hover` pseudo class

### Case 2: The card is hovered
- The card itself can use `:hover`
- The button can use `:click-delegate-hover`

---

This is also the most common kind of use-case. The button within the card is the "canonical" interactive element, so we may want highlight it in some way, when it's delegate, the card, is hovered.

However, since the button itself remains clickable, there is little reason for the reverse as far as I can think of.

---

And, while going against the "don't discuss naming" rule, I think the card's attribute should be called `clickdelegatefor`, which matches the `id` on the target button more clearly.

<!-- comment id=3725073840 author=frehner created=2026-01-08T18:14:38Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3725073840 -->
## @frehner — 2026-01-08

> PS: I see your codepen examples requires `id` for each target element [@frehner](https://github.com/frehner) ☺️ Is it still possible to look into the direction you proposed with something like the `defaultlink` attribute to avoid having too many generated id's? 🤔
> 
> With `id`:
> 
> ```
> <tr clickdelegate="my-button">
>   <td><button id="my-button"></button></td>
> <tr>
> ```
> 
> With `clicktarget`:
> 
> ```
> <tr clickdelegate>
>   <td><button clicktarget></button></td>
> <tr>
> ```

For now, that's how many of the things in HTML work. However, there's been some discussions in https://github.com/wicg/idrefs about improving the situation. 

<!-- comment id=3725080863 author=frehner created=2026-01-08T18:16:34Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3725080863 -->
## @frehner — 2026-01-08

>I think the card's attribute should be called clickdelegatefor

Good point; matches commandfor and interestfor as well. 

<!-- comment id=3728661516 author=eirikbacker created=2026-01-09T12:12:04Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3728661516 -->
## @eirikbacker — 2026-01-09

good points! thanks for quick reply @frehner and @nmn ☺️
I was not aware of the https://github.com/wicg/idrefs - thanks for tips!
And if where already going against the "don't discuss naming" rule:
- If `id` is required, should we just use `command="click"` + `commandfor="id"` to align with this spec, as the consumer should not mix `command` and `clickdelegatefor` anyway? 🤔 

<!-- comment id=3728712552 author=lukewarlow created=2026-01-09T12:27:59Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3728712552 -->
## @lukewarlow — 2026-01-09

I think using command and commandfor is the wrong direction because they currently only apply to buttons and menuitems. We don't want to expose them generally, so I think keeping this click delegation separate is the better way to go.

<!-- comment id=3728847153 author=eirikbacker created=2026-01-09T13:07:30Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-3728847153 -->
## @eirikbacker — 2026-01-09

Good point and much supported @lukewarlow ! 👍 💯 

<!-- comment id=4498654820 author=sb3nder created=2026-05-20T13:01:38Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-4498654820 -->
## @sb3nder — 2026-05-20

One thing to note is that currently CSS solutions handle [pointer cancellation](https://www.w3.org/WAI/WCAG21/Understanding/pointer-cancellation.html) far better than JS solutions.

This is because, in JS it's easier to look only at the `click` event target than to handle three or more event targets (touch is special…): `pointerdown`, `pointerup`, and `click`, in order to implement the correct behavior.

<!-- comment id=4499083492 author=lukewarlow created=2026-05-20T13:52:00Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-4499083492 -->
## @lukewarlow — 2026-05-20

I'll preface this by saying I haven't read through this entire thread in much detail but I do remember thinking recently that this should perhaps just be a generic click delegation behaviour rather than links?

Labels are effectively this, so explaining the label in terms of a primitive seems nice?

With that aside I do think there's some things that need thinking through. We probably need a security and privacy considerations section.

- does this work with speculation rules? If I hover this can it prefetch the target?
- does the browser display UI when hovering this the same way they do when hovering the anchor? So users know they'll navigate when they click this thing?
- do we need to provide a way to tell that the navigation came from the a delegate rather than the link itself? Presumably the navigate events source would be the the card not the link?
- does this fire a click event on the link or does it bypass to the actual activation handler? If it bypasses click but then people are using that to observe things that could be an issue?

<!-- comment id=4499944807 author=frehner created=2026-05-20T15:28:29Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-4499944807 -->
## @frehner — 2026-05-20

@lukewarlow I agree this should eventually cover more than just links. A common use-case is one of “selectable” table rows with checkboxes. 

However it was discussed here https://github.com/openui/open-ui/issues/1256 and tabled for later. 

<!-- comment id=4500114842 author=sb3nder created=2026-05-20T15:47:05Z url=https://github.com/openui/open-ui/issues/1104#issuecomment-4500114842 -->
## @sb3nder — 2026-05-20

Do we have a general idea of the algorithm to handle nested interactive?
I immagine that it starts by ignoring interaction with focusable element, and maybe element nested in focusable.
