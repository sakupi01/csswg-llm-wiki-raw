---
number: 1024
title: "Imperative invoker relationships"
state: "closed"
labels: ["agenda+", "command-invokers", "popover"]
author: "mfreed7"
created_at: "2024-03-31T15:10:27Z"
closed_at: "2024-04-09T22:35:33Z"
url: "https://github.com/openui/open-ui/issues/1024"
comments: 7
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

This is two issues in one:

1. Should it be possible to do `popover.showPopover({invoker: anotherElement})` and have that set the invoker relationship? Reminder: an invoker relationship allows `popover` to be "nested" inside `anotherElement`'s containing popover, and it also fixes up the keyboard tab order so `popover` comes just after `anotherElement`. Both of these happen automatically for `<button popovertarget=popover>`.
2. If the answer to the above is "yes", then what should be done about the [invokers proposal](https://open-ui.org/components/invokers.explainer/)? First, it doesn't connect popovers in the same way that `popovertarget` does (nesting plus keyboard behavior). Second, it doesn't have a proposed imperative API at all, that I know of.

<!-- comment id=2028797216 author=lukewarlow created=2024-03-31T15:25:56Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2028797216 -->
## @lukewarlow — 2024-03-31

> First, it doesn't connect popovers in the same way that popovertarget does (nesting plus keyboard behavior).

I don't fully understand what you're referring to but just wanted to say anything popovertarget does invoketarget should absolutely be doing and it's a spec bug if there's something missing.

<!-- comment id=2029512147 author=keithamus created=2024-04-01T10:00:11Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2029512147 -->
## @keithamus — 2024-04-01

1. Passing the invoker is useful, so yes I think it should be possible. 
2. a. As Luke says, if we're aiming to deprecate popovertarget then invoketarget should do the same thing. Arguably it makes sense for invoketarget to fixup tab order in all scenarios and not just for popovers (for dialog it's irrelevant but details/video/input etc?).
  b. I think the imperative API remains as `showPopover`. We could also extend `showModal()` to take `{invoker}`... [TAG did mention a desire to add `invoke(action, invoker)`](https://github.com/w3ctag/design-reviews/issues/920#issuecomment-1989499571) so maybe that should be the direction of travel?

<!-- comment id=2037882914 author=css-meeting-bot created=2024-04-04T18:16:10Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2037882914 resolution=true -->
## @css-meeting-bot — 2024-04-04

The Open UI Community Group just discussed `Imperative invoker relationships`, and agreed to the following:

* `RESOLVED: add an option to showPopover() to declare the invoking element.`
* `RESOLVED: invoketarget=popover should create the same relationships (e.g. nesting and keyboard behavior) that popovertarget=popover does. Behavior TBD for interesttarget attribute.`

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> masonf: I raised this one recently<br>
&lt;hdv> masonf: they're kind of two interrelated issues, I packed them into one<br>
&lt;hdv> masonf: when you use popovertarget in a button, various things happen for you<br>
&lt;hdv> masonf: more than I even mentioned in the issue<br>
&lt;hdv> masonf: eg for nesting popovers more easily<br>
&lt;hdv> masonf: another is that keyboard navigation is fixed up, so hitting tab takes you to the popover when it is open after you've opened it. That doesn't happen when you open it through script<br>
&lt;hdv> q+<br>
&lt;masonf> hdv: +1 basically, that makes sense. Let's do it.<br>
&lt;masonf> q?<br>
&lt;flackr> +1<br>
&lt;luke> +1<br>
&lt;luke> q+<br>
&lt;hdv> ack me<br>
&lt;masonf> ack luke<br>
&lt;hdv> luke: I think it makes sense… I do wonder if this should apply to other things too as Keith mentions? like &lt;dialog>?<br>
&lt;keithamus> q+<br>
&lt;masonf> ack keith<br>
&lt;hdv> masonf: what's unique about popover is that a connection is formed in a very specific way, I don't think that's the case for dialog, they're more independent of their invoker<br>
&lt;hdv> keithamus: for a dialog that is modal we don't need to have that connection<br>
&lt;hdv> keithamus: another one could be &lt;details>, maybe a similar relationship<br>
&lt;hdv> keithamus: not sure if good for stuff like &lt;video><br>
&lt;luke> q+<br>
&lt;hdv> keithamus: I feel anything but popover would be beyond v1 of invokers<br>
&lt;masonf> ack luke<br>
&lt;hdv> luke: I agree … only other thing would be input elements… I guess especially selects… effectively a popover but not exposed as a popover<br>
&lt;masonf> q+<br>
&lt;hdv> luke: but could come up with that in the future<br>
&lt;hdv> masonf: part two is should hte same apply to invokers?<br>
&lt;scotto> q+<br>
&lt;masonf> ack mason<br>
&lt;hdv> luke: if there's a difference between popovertarget and invokertarget it should be a deliberate choice<br>
&lt;brecht_dr> q+<br>
&lt;hdv> keithamus: you might be referring to case where there is no point for the aria-details mapping?<br>
&lt;hdv> [ relevant post that Scott and I wrote: https://hidde.blog/popover-accessibility/ ]<br>
&lt;hdv> masonf: so is it safe to assume they're the same?<br>
&lt;hdv> luke: seems to me yes?<br>
&lt;hdv> luke: another thing I wanted to ask would be is all of this different if a popover is invoked by hover?<br>
&lt;masonf> ack scott<br>
&lt;brecht_dr> q-<br>
&lt;hdv> scotto: for invoketarget it would matter what the invoked element was… there is no reason to give a details relationship to a modal dialog as nobody would ever experience that relationship as no user could ever experience it as it was on a button that isn't in the modal and the modal is the only thing that's interactable at that time<br>
&lt;hdv> scotto: for video… probably don't need to have the relationship on video, play is more of a state<br>
&lt;masonf> Proposed resolution: add an option to showPopover() to declare the invoking element.<br>
&lt;masonf> Proposed resolution: invoketarget=popover should create the same relationships (e.g. nesting and keyboard behavior) that popovertarget=popover does. Behavior TBD for interesttarget attribute.<br>
&lt;brecht_dr> +1<br>
&lt;keithamus> +1<br>
&lt;luke> +1 to both of those<br>
&lt;hdv> +1 to all of the above<br>
&lt;hdv> keithamus: we could look at adding this before the toggleevent<br>
&lt;hdv> keithamus: I think there was already a PR for that in the spec?<br>
&lt;hdv> masonf: there are two things I heard about beforetoggle… one was about what was the invoker, the other what was the trigger<br>
&lt;hdv> s/things/requests<br>
&lt;hdv> s/heard/heard at WHATWG<br>
&lt;masonf> RESOLVED: add an option to showPopover() to declare the invoking element.<br>
&lt;masonf> RESOLVED: invoketarget=popover should create the same relationships (e.g. nesting and keyboard behavior) that popovertarget=popover does. Behavior TBD for interesttarget attribute.<br>
</details>

<!-- comment id=2040512861 author=tabatkins created=2024-04-05T19:37:18Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2040512861 -->
## @tabatkins — 2024-04-05

Oooh, thanks y'all.

For context: I raised this with Mason when exploring switching Bikeshed's info panels (showing information on `dfn` and `a` elements) to using popovers, so I could stop using the roving tabindex hacks to get good tabbing behavior. Unfortunately, you currently only get that focus behavior from a `<button popovertarget>`, which meant I was SOL.

Being able to specify the invoker in the JS call will solve my problems, thanks!

<!-- comment id=2105680277 author=lukewarlow created=2024-05-11T11:11:10Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2105680277 -->
## @lukewarlow — 2024-05-11

@mfreed7 would `popover.showPopover({invoker: anotherElement})` work if the popover is in one shadow root, and `anotherElement` is in another? That would solve part of the issues with popoverTargetElement *not* working that way.

<!-- comment id=2108944530 author=mfreed7 created=2024-05-13T23:00:34Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2108944530 -->
## @mfreed7 — 2024-05-13

> @mfreed7 would `popover.showPopover({invoker: anotherElement})` work if the popover is in one shadow root, and `anotherElement` is in another? That would solve part of the issues with popoverTargetElement _not_ working that way.

It certainly *could*, assuming we did **not** create an API to retrieve the invoker. Otherwise, we risk opening access to a shadow root via this mechanism.

<!-- comment id=2539868087 author=css-meeting-bot created=2024-12-12T19:42:34Z url=https://github.com/openui/open-ui/issues/1024#issuecomment-2539868087 -->
## @css-meeting-bot — 2024-12-12

The Open UI Community Group just discussed `Imperative invoker relationships`.

<details><summary>The full IRC log of that discussion</summary>
&lt;hdv> lwarlow: was this intentional or a bug?<br>
&lt;hdv> masonf: sounds like a bug?<br>
&lt;hdv> lwarlow: when you toggle popover you can link a popover with its invoker element, the only restriction is the element has to be an HTML element<br>
&lt;hdv> lwarlow: works with nested popover, does implicit anchoring, in theory also fixes focus order (but doesn't actually happen in Chrome, or in my Webkit patch or Firefox patch)<br>
&lt;hdv> lwarlow: looks like they check if there's a popovertarget attr… i.e. browsers don't match the spec<br>
&lt;hdv> masonf: will have to check, I think sequential focus spec might do this, not sure<br>
&lt;hdv> masonf: when you use the source param, a bit of state needs to be saved on that element, that the focus state can look at<br>
&lt;hdv> masonf: we'll figure that out. Seems relatively straightforward<br>
&lt;hdv> masonf: glad you caught it<br>
&lt;hdv> lwarlow: I'm happy to fix the webkit implementation. Maybe with a weak reference<br>
&lt;hdv> lwarlow: either way can jump from one to another<br>
&lt;hdv> masonf: right now there's a reference to the invoker, as in spec<br>
&lt;hdv> lwarlow: I think @@@ is where browser is different from spec<br>
&lt;hdv> masonf: ok. we can discuss more offline<br>
&lt;masonf> q?<br>
&lt;hdv> Zakim, end meeting please<br>
</details>
