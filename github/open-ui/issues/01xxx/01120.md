---
number: 1120
title: "select: define corner case behavior for <selectedoption> "
state: "closed"
labels: ["needs-triage", "select"]
author: "sorvell"
created_at: "2024-10-25T15:06:09Z"
closed_at: "2025-08-18T18:05:37Z"
url: "https://github.com/openui/open-ui/issues/1120"
comments: 4
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

There are a lot of weird things you can do with the `<selectedoption>` element. These should at least all have well-defined behavior and not produce unexpected errors or hang the browser. 

Some cases to consider, definitely not an exhaustive list:

1. Child content, especially when there is no `select.selectedOption`

```html
<selectedoption>some stuff</selectedoption>
```

2. As descendant of itself

```html
<selectedoption>
  <selectedoption></selectedoption>
</selectedoption>
```

3. As descendant of `<option>`

```html
<option>
  1 <selectedoption></selectedoption>
</option>
```

4. When targeted by multiple select's
```html
<selectedoption id="shared"></selectedoption>
<select selectedoptionelement="shared">...</select>
<select selectedoptionelement="shared">...</select>
```

or 

```html

<select selectedoptionelement="shared">...</select>
<select>
  <selectedoption id="shared"></selectedoption>
</select>
```

<!-- comment id=2444773995 author=josepharhar created=2024-10-29T16:26:57Z url=https://github.com/openui/open-ui/issues/1120#issuecomment-2444773995 -->
## @josepharhar — 2024-10-29

> 1. Child content, especially when there is no select.selectedOption

You mean if the author puts stuff in there? It's well defined - when the next clone happens due to select.value being assigned to or the user chooses a different option, then the contents will be replaced by the browser.

> 2. As descendant of itself

If a selectedoption has an ancestor selectedoption, then it will become disabled. It will also be removed by the browser whenever cloning occurs in the ancestor selectedoption.

> 3. As descendant of option

If a selectedoption has an ancestor option, then it will become disabled - I haven't implemented this yet, it currently hangs the browser :)

> 4. When targeted by multiple select's

Each select will clone into that selectedoption whenever either one of them decides to do a clone. This seems intuitive to me in the odd case that you decide to do this.

<!-- comment id=2660985666 author=lukewarlow created=2025-02-15T16:08:15Z url=https://github.com/openui/open-ui/issues/1120#issuecomment-2660985666 -->
## @lukewarlow — 2025-02-15

Is this issue closable or are there left over questions or queries related to these edge cases?

<!-- comment id=2666739881 author=josepharhar created=2025-02-18T19:26:33Z url=https://github.com/openui/open-ui/issues/1120#issuecomment-2666739881 -->
## @josepharhar — 2025-02-18

I think that its pretty well defined in the spec PR and implementation

<!-- comment id=3194799920 author=github-actions[bot] created=2025-08-18T01:00:37Z url=https://github.com/openui/open-ui/issues/1120#issuecomment-3194799920 -->
## @github-actions[bot] — 2025-08-18

There hasn't been any discussion on this issue for a while, so we're marking it as stale. If you choose to kick off the discussion again, we'll remove the 'stale' label.
