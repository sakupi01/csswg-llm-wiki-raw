---
number: 1338
title: "[Range] Clarifying the propagation of the disabled attribute"
state: "open"
labels: ["Enhanced range slider", "needs-triage"]
author: "brechtDR"
created_at: "2025-11-26T09:38:01Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1338"
comments: 1
has_resolution: true
synced_at: "2026-08-27T14:25:57Z"
---

The `<rangegroup>` is modeled after the grouping behavior of `<fieldset>` and `<legend>` for accessibility and structure. This parallel makes the behavior of the disabled attribute critical, especially for form submission and progressive enhancement.

## Specific Questions Raised by Feedback:

**Group Disabling**: If the `disabled` attribute is applied to the `<rangegroup>` (e.g., `<rangegroup disabled>`), does it cascade to and disable all contained `<input type="range">` handles? Should the `<rangegroup?` allow a `disabled` attribute to begin with?

If yes on group disabling: 
1. **Granular Control**: Can individual handles maintain a disabled state while the parent <rangegroup> remains active?
2. **State Persistence:** If the entire group is disabled and later re-enabled, should individual handles that were previously disabled retain their initial disabled state, as is standard behavior for other HTML form elements?


We need some clear specifications for the behavior of the disabled attribute on both the parent `<rangegroup>`(optional) and its child elements, ensuring that the defined propagation rules support the intended progressive enhancement fallback and consistent state management.

<!-- comment id=3643489654 author=css-meeting-bot created=2025-12-11T19:34:13Z url=https://github.com/openui/open-ui/issues/1338#issuecomment-3643489654 resolution=true -->
## @css-meeting-bot — 2025-12-11

The Open UI Community Group just discussed `[Range] Clarifying the propagation of the disabled attribute`, and agreed to the following:

* `RESOLVED: A disabled state on the rangegroup should make it completely disabled, a thumb can be individually disabled, but when all thumbs are disabled, it should behave like a fully disabled group`

<details><summary>The full IRC log of that discussion</summary>
&lt;keithamus> q+<br>
&lt;keithamus> ack brecht_dr<br>
&lt;jarhar> keithamus: it seems straightforward to me that each thumb if disabled would not do the default activation and skip focus<br>
&lt;jarhar> keithamus: you can programatically change the value<br>
&lt;masonf> q+<br>
&lt;jarhar> keithamus: seems useful to have disabled on the range group, and that would propagate to all handles<br>
&lt;jarhar> masonf: i agree<br>
&lt;keithamus> ack keithamus<br>
&lt;keithamus> ack masonf<br>
&lt;jarhar> masonf: if an individual handle is disabled it should stay locked in place at that value and not have nudging behavior<br>
&lt;brecht_dr> q+<br>
&lt;jarhar> brecht_dr: if you had some form controls through js that depend on selection of something else<br>
&lt;jarhar> brecht_dr: the progressive enhancement, what if you want to set both thumbs to disabled<br>
&lt;jarhar> brecht_dr: both range sliders would be disabled<br>
&lt;jarhar> brecht_dr: but then you dont have disabling as a group in that case<br>
&lt;jarhar> brecht_dr: does two thumbs being disabled mean the same thing as the group being disabled?<br>
&lt;jarhar> masonf: you mentioned progressive enhancement. if you dont have the rangegroup, then you have sliders painting on top of each other?<br>
&lt;jarhar> masonf: a range group that only has disabled sliders could become disabled. i wonder if that announced any differently<br>
&lt;jarhar> masonf: a fully disabled full range slider gets its track painted as disabled in addition to the thumbs<br>
&lt;jarhar> keithamus: that seems easy enough to do<br>
&lt;jarhar> keithamus: you can probably do that with css today as well<br>
&lt;jarhar> keithamus: other than styling, presumably each of the thumbs is a tab stop, so if you disabled them all then theres no tab stop<br>
&lt;keithamus> q?<br>
&lt;masonf> q?<br>
&lt;keithamus> ack brecht_dr<br>
&lt;brecht_dr> PROPOSED RESOLUTION: A disabled state on the rangegroup should make it completely disabled, a thumb can be individually disabled, but when all thumbs are disabled, it should behave like a fully disabled group<br>
&lt;masonf> +1<br>
&lt;keithamus> +1<br>
&lt;brecht_dr> RESOLVED: A disabled state on the rangegroup should make it completely disabled, a thumb can be individually disabled, but when all thumbs are disabled, it should behave like a fully disabled group<br>
</details>
