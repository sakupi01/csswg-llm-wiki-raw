---
number: 1312
title: "[menu] Should an event be fired when a <menuitem> is selected? Which event?"
state: "open"
labels: ["menu", "select"]
author: "mfreed7"
created_at: "2025-10-28T20:12:17Z"
closed_at: null
url: "https://github.com/openui/open-ui/issues/1312"
comments: 12
has_resolution: false
synced_at: "2026-08-27T14:25:57Z"
---

Since the main purpose of a menu of `<menuitem>`s is to offer items that **do** something when you select them, developers need a reliable event to attach behaviors to. This might sound obvious, but upon further thought, the obvious candidate, `click`, has a problem: 

For a traditional "click" on the menu item with a mouse or touch, or in most cases with a keyboard activation via spacebar or Enter (platform dependent), a `click` will already be fired on the `<menuitem>` (or a descendant) by the platform. However, it's desirable to follow the implemented behavior of customizable-`<select>`, which *also* allows the user to `mousedown` on a button that invokes a menu, drag the mouse over to the newly-opened menu, and release the mouse (`mouseup`) over the submenu item they'd like to select. (E.g. this is a standard pattern for all menus on MacOS.) In that case, the [pointerevents spec says](https://www.w3.org/TR/pointerevents/#:~:text=let%20target%20be%20the%20nearest%20common%20inclusive%20ancestor) that the `click` event gets fired on the nearest common inclusive ancestor of the `mousedown` and `mouseup` elements, and that's **never** going to be the `<menuitem>` that the user selected.

There seem to be two options:

1. Modify the [spec'd behavior](https://www.w3.org/TR/pointerevents/#:~:text=let%20target%20be%20the%20nearest%20common%20inclusive%20ancestor) for the `click` event for this specific case, and re-target it to the selected `<menuitem>`.
2. Create a new event, like `activated` (needs bikeshedding).

<span>#</span>1 would nicely solve the problem, and would meet most developers where they are, which is to expect `click` to work. The downside is that it would require some "weird" changes to the pointerevents spec, which might be counterintuitive in some cases.

<span>#</span>2 would avoid the spec weirdness, and it'd be fairly easy to understand `menuitem.addEventListener('activate',()=>())`. However, it introduces a footgun, whereby developers do `menuitem.addEventListener('click',...` instead, and do not test the mousedown-drag-mouseup behavior, so they don't notice that it doesn't work.

Given the footgun, I think my preference is <span>#</span>1: change the behavior of `click`.

As a side-note, this issue also applies to customizable-`<select>`: if you mousedown-drag-mouseup, you won't get a `click` on the selected `<option>`. This could be a source of bugs there also.

<!-- comment id=3458656992 author=scottaohara created=2025-10-28T21:51:16Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3458656992 -->
## @scottaohara — 2025-10-28

fwiw, based on what you've outlined i'd also lean towards option 1.    

i've also pinged Patrick to take a look at this issue.  I imagine you may have already pinged Rob as well?

<!-- comment id=3459136754 author=lukewarlow created=2025-10-29T00:11:27Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3459136754 -->
## @lukewarlow — 2025-10-29

Btw slightly tangential but is it mouse down and mouse up or pointer down and pointerup? Reason I ask is because I think chromium currently uses pointer for its tracking here.

<!-- comment id=3460538944 author=patrickhlauke created=2025-10-29T09:22:52Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3460538944 -->
## @patrickhlauke — 2025-10-29

suggest opening a mirror issue for this on https://github.com/w3c/pointerevents/issues - incidentally, also intrigued by the mention of

> follow the implemented behavior of customizable-`<select>`

and whether that is also already covered by current PE behaviour (and if not, making an issue about that too)

<!-- comment id=3460547237 author=patrickhlauke created=2025-10-29T09:25:02Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3460547237 -->
## @patrickhlauke — 2025-10-29

> Btw slightly tangential but is it mouse down and mouse up or pointer down and pointerup? Reason I ask is because I think chromium currently uses pointer for its tracking here.

just on that, I believe it's best to talk about/mention pointerdown/pointerup even in prose just to be more inclusive of non-mouse pointers where this may still be relevant (such as stylus)

<!-- comment id=3462721733 author=mfreed7 created=2025-10-29T17:06:59Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3462721733 -->
## @mfreed7 — 2025-10-29

Thanks for all the comments here!

> fwiw, based on what you've outlined i'd also lean towards option 1.

Great, thanks.

> i've also pinged Patrick to take a look at this issue. I imagine you may have already pinged Rob as well?

No, I didn't yet, but good idea: @flackr 

> suggest opening a mirror issue for this on https://github.com/w3c/pointerevents/issues - incidentally, also intrigued by the mention of

Great idea! Done: https://github.com/w3c/pointerevents/issues/560

> and whether that is also already covered by current PE behaviour (and if not, making an issue about that too)

I think we should discuss both here and there, since the concerns are the same I think.

> Btw slightly tangential but is it mouse down and mouse up or pointer down and pointerup? Reason I ask is because I think chromium currently uses pointer for its tracking here.

> just on that, I believe it's best to talk about/mention pointerdown/pointerup even in prose just to be more inclusive of non-mouse pointers where this may still be relevant (such as stylus)

I meant specifically mouse here, because (AFAIK) this explicitly **isn't** a pattern on touchscreen. Touchdown-drag-touchup is usually a scroll gesture. In most native mobile UI that we've seen, menus and selects don't allow this pattern for selecting items with a touchscreen.

<!-- comment id=3462724923 author=mfreed7 created=2025-10-29T17:07:55Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3462724923 -->
## @mfreed7 — 2025-10-29

> non-mouse pointers where this may still be relevant (such as stylus)

Ahh I just got this. What's the correct term for {something}events that means precision pointer devices only, i.e. not fingers on a touchscreen.

<!-- comment id=3462800042 author=patrickhlauke created=2025-10-29T17:29:29Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3462800042 -->
## @patrickhlauke — 2025-10-29

you could write it more generally, say pointer, but then add something like "unless the interaction interferes with platform conventions" or similar. in PE, we've tried to make a distinction between direct and indirect manipulation (though that doesn't necessarily just mean mouse vs touchscreen) https://w3c.github.io/pointerevents/#dfn-direct-manipulation

<!-- comment id=3469293580 author=josepharhar created=2025-10-30T17:47:35Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3469293580 -->
## @josepharhar — 2025-10-30

For customizable select, there isn't any special behavior implemented in pointer events, at least not yet. This issue does not apply as much for customizable select because it's not a typical pattern for authors to hook up their own activation code for the user selecting an option directly to the option element itself - you'd typically add a change event listener to the select element rather than a click event listener on the option element.

Customizable select does support the click and drag behavior to choose an option, but the click is fired on the select element rather than the option element because UIEvents currently says to fire click events on the nearest common ancestor of the pointerdown/mousedown and pointerup/mouseup elements: https://www.w3.org/TR/uievents/#:~:text=SHOULD%20fire%20click%20and%20dblclick%20events%20on%20the%20nearest%20common%20inclusive%20ancestor%20when%20the%20associated%20mousedown%20and%20mouseup%20event%20targets%20are%20different

<!-- comment id=3469585173 author=css-meeting-bot created=2025-10-30T18:51:24Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3469585173 -->
## @css-meeting-bot — 2025-10-30

The Open UI Community Group just discussed `[menu] Should an event be fired when a <menuitem> is selected? Which event?`, and agreed to the following:

* ``RESOLVED: just make `click` work in this case, and for customizable-<select> also``

<details><summary>The full IRC log of that discussion</summary>
&lt;frehner> masonf: on Menus: you build a menu, every item in the menu is an action you take,<br>
&lt;lwarlow> q+<br>
&lt;frehner> ... on Mac and custom select, you can mousedown, hold it down and move to a submenu, and let go on sub-item, and that sub-item was selected.<br>
&lt;frehner> ... but that doesn't work for pointer events because it will only add a click event on the common ancestor<br>
&lt;sarah> q+<br>
&lt;frehner> options: 1) new event<br>
&lt;frehner> ... 2) redefine how click works in this case and custom select<br>
&lt;frehner> ... preference is 2<br>
&lt;jarhar> q?<br>
&lt;lwarlow> https://developer.mozilla.org/en-US/docs/Web/API/Element/DOMActivate_event ;)<br>
&lt;frehner> ... if you're used to using "click", but we do 1), then there's confusion<br>
&lt;gregwhitworth> ack lwarlow<br>
&lt;frehner> lwarlow: bring back dom activate event :)<br>
&lt;frehner> jarhar: in the case of options over the picker<br>
&lt;frehner> masonf: you have to move the mouse in that case<br>
&lt;frehner> lwarlow: re: behavior, make click work<br>
&lt;jarhar> q+<br>
&lt;frehner> ... new event unlikely to be supported by whatwg here<br>
&lt;frehner> ... making click work here seems fine<br>
&lt;frehner> ... is menu item activation different than click event?<br>
&lt;frehner> masonf: command stuff works, but dev click-related stuff wouldn't.<br>
&lt;frehner> lwarlow: special case it then<br>
&lt;gregwhitworth> q?<br>
&lt;gregwhitworth> ack sarah<br>
&lt;frehner> sarah: +1 having click work<br>
&lt;frehner> ... preventDefault not working would be weird<br>
&lt;frehner> ... is this only for mousedown -> submenu -> mouseup? Or menu items in the same menu?<br>
&lt;frehner> masonf: correct<br>
&lt;frehner> sarah: what about drag and drop?<br>
&lt;frehner> jarhar: a draggable menu item that creates a submenu.<br>
&lt;gregwhitworth> ack jarhar<br>
&lt;frehner> ... +1 for click<br>
&lt;frehner> ... I hope it's not too hard, but good luck ;)<br>
&lt;frehner> ... and hopefully doesn't break custom select<br>
&lt;frehner> masonf: +1, I want to make this work for custom select too<br>
&lt;domfarolino> q+<br>
&lt;frehner> lwarlow: for select, that seems weird to have click on menuitems<br>
&lt;gregwhitworth> ack domfarolino<br>
&lt;frehner> domfarolino: question about internal workings of mousedown/mouseup<br>
&lt;frehner> lwarlow: would click event fire before that stuff, and it's cancellable?<br>
&lt;frehner> masonf: no click happens after<br>
&lt;frehner> lwarlow: activation behavior on new click or mouse?<br>
&lt;frehner> masonf: if you cancel click event, it's already happened<br>
&lt;frehner> ... I may end up rewiring how it works to use the synthetic event<br>
&lt;gregwhitworth> q?<br>
&lt;lwarlow> +1<br>
&lt;masonf> Proposed resolution: just make `click` work in this case, and for customizable-&lt;select> also<br>
&lt;frehner> domfarolino: do we want to clarify timing here?<br>
&lt;frehner> masonf: I want to come back after writing tests<br>
&lt;domfarolino> +1<br>
&lt;masonf> RESOLVED: just make `click` work in this case, and for customizable-&lt;select> also<br>
&lt;sarah> +1<br>
</details>

<!-- comment id=3642669160 author=sb3nder created=2025-12-11T16:12:28Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-3642669160 -->
## @sb3nder — 2025-12-11

Taking inspiration from base-select behavior,
I tried to write some ideas, borrowing the current spec as a base.

Drag selection applies when `pointerdown` target is outside `<option>` and `pointerup` is inside.\
(it does not seem to apply any other restriction on `pointerdown` target, for example being inside `<select>`)

spec: https://w3c.github.io/pointerevents/#the-click-auxclick-and-contextmenu-events:~:text=Define%20target%20as%20follows%3A

---
### try 1

Otherwise (*event* is a `click` or `auxclick` event for which *userEvent* is a `pointerup` event that was dispatched uncaptured)\
let *commonInclusiveAncestor* be the nearest common inclusive ancestor of the corresponding pointerdown and pointerup targets in the DOM at the moment *event* is being dispatched.

If *event* is not a `click` event, or *event* pointerType is not `mouse`, let *target* be *commonInclusiveAncestor*.

Otherwise let *newTarget* be the nearest inclusive ancestor of `pointerup` target,\
where *newTarget* tagName is OPTION or MENUITEM,\
and *newTarget* is not an inclusive ancestor of `pointerdown` target,\
at the moment *event* is being dispatched.

If *newTarget* exists, let *target* be *newTarget*,\
otherwise let *target* be *commonInclusiveAncestor*.

---

### try 2

Otherwise let *newTarget* be the nearest inclusive ancestor of `pointerup` target,\
where `drag-click-target` CSS property evaluates to true,\
at the moment *event* is being dispatched.

If *newTarget* exists, let *target* be *newTarget*,\
otherwise let *target* be *commonInclusiveAncestor*.

`drag-click-target: auto | up;`
- `auto` evaluates to false.
- `up` evaluates to true if the element is an inclusive ancestor of `pointerup` target,\
and is not an inclusive ancestor of `pointerdown` target.

---

Should this also apply to `auxclick` events?

<!-- comment id=4653090681 author=dbaron created=2026-06-08T20:14:29Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-4653090681 -->
## @dbaron — 2026-06-08

There's been some discussion about how to do this in https://github.com/w3c/pointerevents/pull/637 .  The Pointer Events working group didn't like adding an exception to the `click` targeting rules, and suggested pointer capture instead.  Capturing on the pointerup doesn't appear to work at first glance, although it's not clear whether it's a shorter path to try to make it work or whether it would be simpler to go with the more complex solution of capturing on pointer down and repeatedly changing the capture as the mouse moves.

<!-- comment id=5147497496 author=dbaron created=2026-07-31T21:10:48Z url=https://github.com/openui/open-ui/issues/1312#issuecomment-5147497496 -->
## @dbaron — 2026-07-31

I ended up taking the approach suggested in https://github.com/w3c/pointerevents/pull/637#issuecomment-4684760540 , in https://github.com/whatwg/html/commit/89a843fb8716098bf3c772062b5a8cf1489b0ca3 and https://crrev.com/c/8133028 and https://github.com/web-platform-tests/wpt/pull/61506 .

I think that means this is done... except that maybe it deserves a mention in the explainer too.
