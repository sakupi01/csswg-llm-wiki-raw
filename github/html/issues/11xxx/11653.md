---
number: 11653
title: "Popping node vs. end tag behavior in <option> and <selectedcontent>"
state: "closed"
labels: ["topic: parser", "topic: select"]
author: "dmsnell"
created_at: "2025-09-11T17:53:49Z"
closed_at: "2025-10-13T17:15:39Z"
url: "https://github.com/whatwg/html/issues/11653"
comments: 9
has_resolution: false
synced_at: "2026-09-25T16:50:25Z"
---

### What is the issue with the HTML Standard?

The recent changes in #10548 modify the parsing behavior so that a selected OPTION element will be appropriately cloned into the appropriate SELECTEDCONTENT. However, there seems to be a discrepancy between how the rules are written and how Chrome currently implements this.

(Given that this is currently a Chrome-only feature in production it’s unclear to me how other standards-compliant browsers and parsers will handle it).

The only reference to run the `maybe clone an option into selectedcontent` algorithm is found in the section for an end tag whose name is `OPTION`. However:

 - Chrome clones the appropriate OPTION into a SELECTEDCONTENT even when no end tag whose name is `OPTION` appears.
 - It is reasonable and preferable that the cloning operation runs regardless of the particular tokens in the markup.

Since the cloning probably needs to occur whenever popping any `OPTION` element off of the stack of open elements, it needs to run both during `generate implied end tags` and also when performing any pop operation on the stack of open elements.

<!-- comment id=3283001446 author=josepharhar created=2025-09-11T23:51:26Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3283001446 -->
## @josepharhar — 2025-09-11

I agree that this is a missing part of the spec, we should do that in generate implied end tags.

It also makes me wonder if any other end tag rules for other elements are missing in generate implied end tags, and if the fix should be more generic to run end tag rules for any tag that gets closed in generate implied end tags.

<!-- comment id=3283872329 author=annevk created=2025-09-12T06:21:38Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3283872329 -->
## @annevk — 2025-09-12

cc @whatwg/html-parser 

<!-- comment id=3285810179 author=dmsnell created=2025-09-12T15:50:05Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3285810179 -->
## @dmsnell — 2025-09-12

@josepharhar note that it’s not only in `generate implied end tags` where we might expect the algorithm to run. If I’m not misreading the spec, when:

 - IN BODY we encounter a star tag whose name is `SELECT`.
 - The parser was not created as part of the fragment case with `SELECT` as context element.
 - The stack of elements has a `SELECT` element in scope.

Then we:

> Pop elements from the stack of open elements until a `select` element has been popped from the stack.

So I think that this step skips generating implied end tags but might implicitly close any open `OPTION` elements, none of which contain the expected end tag, and one of which might be the selected content.

```html
<select id="real">
<option>a
<option selected>b
<select id="closes-real-but-is-ignored">
```

<!-- comment id=3298930360 author=zcorpan created=2025-09-16T14:04:45Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3298930360 -->
## @zcorpan — 2025-09-16

Handling in generate implied end tags is still not sufficient, because, at least, the end-of-file token doesn't run those steps.

There are a couple of "When a foo element is popped off the stack of open elements of an HTML parser or XML parser," phrasings:

https://html.spec.whatwg.org/#:~:text=The%20element%20is%20popped%20off%20the%20stack%20of%20open%20elements%20of%20an%20HTML%20parser%20or%20XML%20parser%2E

https://html.spec.whatwg.org/#:~:text=the%20element%20is%20popped%20off%20the%20stack%20of%20open%20elements%20of%20an%20HTML%20parser%20or%20XML%20parser%2C

https://html.spec.whatwg.org/#:~:text=true%2E-,When%20a%20media,false

https://html.spec.whatwg.org/#:~:text=When%20a%20textarea%20element%20is%20popped%20off%20the%20stack%20of%20open%20elements%20of%20an%20HTML%20parser%20or%20XML%20parser%2C%20then%20the%20user%20agent%20must%20invoke%20the%20element%27s%20reset%20algorithm%2E

I think that ought to work for `option` as well.

<!-- comment id=3299094060 author=annevk created=2025-09-16T14:40:36Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3299094060 -->
## @annevk — 2025-09-16

We should have a follow-up issue on formalizing all those better though, I think. We don't want COMEFROM.

<!-- comment id=3378724431 author=josepharhar created=2025-10-07T21:07:01Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3378724431 -->
## @josepharhar — 2025-10-07

Thanks @zcorpan! I will make a paragraph like that for option elements for now.

> We should have a follow-up issue on formalizing all those better though, I think. We don't want COMEFROM.

I agree. Would it make sense to move them all into one algorithm that runs any time any element is popped off the stack of open elements? I suppose that it's not perfect unless we also instrument every place that pops elements off the stack of open elements.

> Handling in generate implied end tags is still not sufficient, because, at least, the end-of-file token doesn't run those steps.

Does this have an explicit step which pops elements off the stack which we could instrument?

<!-- comment id=3378775673 author=josepharhar created=2025-10-07T21:26:09Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3378775673 -->
## @josepharhar — 2025-10-07

I made a PR here: https://github.com/whatwg/html/pull/11764

<!-- comment id=3380120965 author=annevk created=2025-10-08T07:30:11Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3380120965 -->
## @annevk — 2025-10-08

> Would it make sense to move them all into one algorithm that runs any time any element is popped off the stack of open elements?

Presumably this is what WebKit (and I guess Chromium) call `finishParsingChildren()`? It seems reasonable to let elements define such steps and then the parser pops we run those steps for the relevant element, if it has any defined. (Or they have them always defined but by default no-op.)

Generally the existence of this mechanism is a bit of a design smell and cannot be replicated by custom elements as it would make it very easy to design elements that work declaratively, but not imperatively. I'm not sure how to convey that properly however.

> I suppose that it's not perfect unless we also instrument every place that pops elements off the stack of open elements.

Right, I think we want to make "popping" an algorithm.

> Does this have an explicit step which pops elements off the stack which we could instrument?

Yeah I think so. "Stops parsing" and "abort a parser" both pop.

<!-- comment id=3400498394 author=annevk created=2025-10-14T07:38:20Z url=https://github.com/whatwg/html/issues/11653#issuecomment-3400498394 -->
## @annevk — 2025-10-14

Follow-up issue: #11781.
