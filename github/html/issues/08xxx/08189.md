---
number: 8189
title: "Rendering <select> as a listbox is a one-line widget that opens a popup on iOS and Android"
state: "closed"
labels: ["topic: forms", "topic: rendering", "topic: select"]
author: "zcorpan"
created_at: "2022-08-12T08:57:13Z"
closed_at: "2026-03-11T06:01:49Z"
url: "https://github.com/whatwg/html/issues/8189"
comments: 12
has_resolution: false
synced_at: "2026-09-16T23:44:56Z"
---

See https://github.com/whatwg/html/issues/3410#issuecomment-1212874627

I suppose there's a good reason for this in terms of usability -- it may be hard to select options on a touchscreen without sufficient spacing, and taking up more space for an inline widget might be less web-compatible compared to showing a one-line widget.

On even smaller screens like a watch this approach also makes sense.

HTML doesn't currently allow it in the rendering section, however. What should the spec say? Allow either inline listbox or a one-line widget that opens a popup?

cc @whatwg/forms @mfreed7 

<!-- comment id=2501756154 author=josepharhar created=2024-11-26T19:27:33Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2501756154 -->
## @josepharhar — 2024-11-26

Yeah it's unfortunate that there is a platform difference like this with the same html, and I'm worried that it's something we won't be able to change. It also poses challenges for making appearance:base work for listbox/multiple selects: https://issues.chromium.org/issues/357649033#comment4

In order to make the spec reflect the current reality, we could add some text saying that the UA may force popup rendering instead of listbox rendering if it chooses.

In order to actually allow developers to have an in-page listbox rendering that is respected across platforms and that we could have appearance:base multi-select for, I wonder if we could add another attribute?

<!-- comment id=2503177401 author=annevk created=2024-11-27T08:03:51Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2503177401 -->
## @annevk — 2024-11-27

I think we first need to properly understand how we got here in the first place. Adding new features to fix an existing problem generally does not solve the problem.

cc @pxlcoder @nt1m 

<!-- comment id=2518691459 author=josepharhar created=2024-12-04T22:30:13Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2518691459 -->
## @josepharhar — 2024-12-04

This behavior for chromium and webkit was implmented here:
- https://chromium.googlesource.com/chromium/src/+/2477d2d429b537532afead1cb60e6a90f5581ff4%5E%21/
- https://bugs.webkit.org/show_bug.cgi?id=36006

Before this patch, `<select multiple>` and `<select size=2>` were always rendered in-page for all platforms. I'm guessing that there was a desire to add native support for some platforms which had multi-select popups, and it was decided to do it this way, and the platforms which didn't have native support for multi-select popups continued to use the existing in-page listbox.

<!-- comment id=2877242732 author=josepharhar created=2025-05-13T16:37:27Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2877242732 -->
## @josepharhar — 2025-05-13

In order to reuse the size attribute moving forward instead of creating a new attribute, address this issue, and have feature parity across mobile and desktop, here is my proposal:

- Make `<select multiple>` always render an in-page listbox across platforms.
- Make `<select multiple size=1>` always render as a button with a popup across platforms.

This may be a "breaking" change for mobile which requires authors to add the size=1 attribute if they want it, but I am working on collecting data to see how prevalent this is. Based on bug reports I've worked on over the years, I believe that `<select multiple>` as an in-page listbox on desktop is used significantly more than the popup on mobile.

<!-- comment id=2880172875 author=lukewarlow created=2025-05-14T13:03:41Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2880172875 -->
## @lukewarlow — 2025-05-14

> Make `<select multiple>` always render an in-page listbox across platforms.

I'm not sure if this will be web compatible but seems acceptable if it is (though making it always a drop-down box would probably also be okay).

We should also make `<select size="5">` always render as a listbox if possible too.

<!-- comment id=2880182441 author=josepharhar created=2025-05-14T13:06:38Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2880182441 -->
## @josepharhar — 2025-05-14

> I'm not sure if this will be web compatible but seems okay if it is.

I made a UseCounter for how often that mobile picker is actually used here:
- https://chromiumdash.appspot.com/commit/4b9389daefc020b1ba340e14358d3f4fd994feb4
- https://chromestatus.com/metrics/feature/timeline/popularity/5549

It should reach stable by the end of may or so.

> We should also make `<select size="5">` always render as a listbox if possible too.

Yes, that is my plan. Explicit size attributes can always be used to opt in to listbox (size > 1) or menulist/popup (size == 1)

<!-- comment id=2884312794 author=css-meeting-bot created=2025-05-15T15:49:43Z url=https://github.com/whatwg/html/issues/8189#issuecomment-2884312794 -->
## @css-meeting-bot — 2025-05-15

The CSS Working Group just discussed `Rendering <select> as a listbox is a one-line widget that opens a popup on iOS and Android`.

<details><summary>The full IRC log of that discussion</summary>
&lt;dandclark> jarhar: This is about how select element with multiple is rendered as button with popup on mobile, and in-page listbox on desktop<br>
&lt;dandclark> ...can't opt into one oth or the other, even with `size` attr<br>
&lt;dandclark> ...HTML spec doesn't explain this<br>
&lt;dandclark> ...I want to improve this<br>
&lt;dandclark> ...Have goals mentioned in comment. Previously talked about adding new attr to do this.<br>
&lt;dandclark> ...annevk recommended against, so want to use the size attr<br>
&lt;dandclark> ...Have goal of feature parity across platforms<br>
&lt;dandclark> ...Same HTML create same UI across platforms<br>
&lt;dandclark> ...Believe in-page rendering on desktop is more highly used, have use counter to verity<br>
&lt;dandclark> ...Setting size=1 to opt into button with popup rendering, size > 1 opts into listbox rendering<br>
&lt;dandclark> ...Thoughts?<br>
&lt;dandclark> astearns: Concern is web compat<br>
&lt;emilio> q+<br>
&lt;dandclark> ...Seems risky. Glad you have use counter<br>
&lt;dandclark> jarhar: Yep don't want to break the web, can change course if needed.<br>
&lt;astearns> ack emilio<br>
&lt;dandclark> masonf: Nice thing here is that existing thing for size>1 is bad, so hopefully low usage<br>
&lt;dandclark> emilio: Does this change the dfault for multiple on mobile?<br>
&lt;dandclark> jarhar: That's what i'm proposing<br>
&lt;dandclark> emilio: That's the scary part.<br>
&lt;dandclark> ...Does this involve impementing size > 1 with multiple on desktop?<br>
&lt;dandclark> jarhar: That's one of my goals<br>
&lt;dandclark> emilio: I think end goal is nice. Scared about webcompat. But worth a shot<br>
&lt;masonf> s/size > 1/size = 1<br>
&lt;jarhar> q?<br>
&lt;dandclark> ...Other interesting thing is how will that work on desktop<br>
&lt;dandclark> ...Not a lot of examples for muti-select<br>
&lt;dandclark> ...Needs specifying<br>
&lt;dandclark> ...whether you expect it to close<br>
&lt;dandclark> ...Do you expect seleding a new option to require reopening the popup<br>
&lt;jarhar> https://github.com/openui/open-ui/issues/1217<br>
&lt;dandclark> ...Could be either useful or annoying<br>
&lt;dandclark> jarhar: Linked issue is about this<br>
&lt;dandclark> ...seen sites where there's no extra button to commit or cancel<br>
&lt;dandclark> ...iOS one has close button but not an OK or cancel thing, so as soon as you select the options in native picker you can see the in-page button change<br>
&lt;dandclark> ...pressing option doesn't close picker<br>
&lt;dandclark> ...On android, there's both OK and Cancel buttons, doesn't commit selection until you press button<br>
&lt;dandclark> ...Hoping to at least make base:appearance one consistent.<br>
&lt;dandclark> ...Don't necessarily have to change native<br>
&lt;dandclark> emilio: Tricky part on desktop is that both use cases make sense<br>
&lt;dandclark> ...say you have list of langs, want to select 2 or 3, don't want to have to reopen popup<br>
&lt;dandclark> ...But for sorting option, most of the time you only want one but can select multiple. Good you are thinking about this. End goal makes sense, compat wise might be annoying but we'll see<br>
&lt;RobAtADP> what would the in page widget show after closing with multiple options selected?<br>
&lt;dandclark> jarhar: If compat doesn't work out, might be different without size attr, will  have to give guidance that that be used<br>
&lt;dandclark> astearns: Maybe this is something that gets gated on appearance: base if we have compat concerns?<br>
&lt;dandclark> jarhar: Using CSS to switch modes would be challenging<br>
&lt;dandclark> ...Thought we couldn't do it with in-page CSS. But the way it works now with button vs popup mode is it changes UA shadow root and slotting behavior<br>
&lt;dandclark> ...chaning that on computed val of CSS prop would be not good<br>
&lt;dandclark> ...Did a lot of work to have custom select slotting not to depend on this, would be more work for this new case<br>
&lt;dandclark> ntim: More fundamentally, making behavior changes based on appearance:base is something to avoid<br>
&lt;dandclark> ...Appearance should be for stylistic rather than behavior changes IMO. it's confusing for devs otherwise<br>
&lt;dandclark> annevk: Tend to agree, but this is edge case.<br>
&lt;dandclark> ...Isn't shadow different between auto and base already?<br>
&lt;dandclark> jarhar: Carefully designed so it doesn't change<br>
&lt;dandclark> astearns: Thought there were cases where appearance:base defined that shadow composition of a control, where it would not have been defined before<br>
&lt;dandclark> annevk: But that doesn't make it depend on computed style, could have same shadow for both<br>
&lt;dandclark> ntim: The current cases in css forms spec where we say elements work with appearance:base, it doesn't change behavior. Sliders are still sliders.<br>
&lt;dandclark> ...It's just what's exposed to styling that changes.<br>
&lt;dandclark> ...Anyway seems there's more consensus around making HTML attribute the opt-in. So not sure we have to consider appearance.<br>
&lt;dandclark> astearns: Other comments/questions?<br>
&lt;dandclark> ...Do you want resolution today?<br>
&lt;dandclark> jarhar: No. Discussion more than enough<br>
&lt;dandclark> annevk: Don't think I ever said I don't want a new attribute. Just that we should try to understand existing behavior.<br>
&lt;dandclark> ...Not sure we do, but maybe still OK with changing if it's compatible<br>
&lt;dandclark> astearns: Don't object to new attr?<br>
&lt;dandclark> annevk: Not necessarily, it depends<br>
&lt;dandclark> astearns: Also makes sense even if web compatible to consider this switch on the attr vs a design that has a new attr that was more specifically targeted to this problem<br>
&lt;dandclark> ...What would make more sense for devs?<br>
&lt;dandclark> ntim: If new attr is added, shouldn't just be for the sake of compat. Shouldn't be the only reason to add new attr.<br>
&lt;dandclark> ...Would make more sense to add new attribute if it's well tailored for a certain use case<br>
&lt;dandclark> masonf: I think if this were redesigned today the attribute would be popup vs in page.<br>
&lt;dandclark> ...That would be more semantic<br>
&lt;dandclark> annevk: makes sense, but you still have to figure out CSS, probably in CSS<br>
&lt;dandclark> jarhar: Promising how tall the thing will be in appearance:base world might be hard or impossible<br>
&lt;dandclark> annevk: Yeah probably wont' work<br>
&lt;dandclark> ...but in apperance:base world, size thing is exposed to CSS<br>
&lt;dandclark> ...Can do `calc` magic and make it work<br>
&lt;dandclark> ...Sounds reasonable that it can work<br>
&lt;dandclark> astearns: Seems like a non goal to preserve the existing behavior and then add extra way of getting consistent rendering<br>
&lt;dandclark> ...Like the idea of having consistent rendering to fix this<br>
&lt;dandclark> annevk: yes<br>
&lt;dandclark> astearns: Yes, too many modes.<br>
&lt;dandclark> ...Will wait for use counter results then come back to discuss<br>
</details>

<!-- comment id=3079939559 author=josepharhar created=2025-07-16T19:05:54Z url=https://github.com/whatwg/html/issues/8189#issuecomment-3079939559 -->
## @josepharhar — 2025-07-16

I created a PR to make `<select multiple size=1>` have a popup which would address this issue by getting cross-platform parity: https://github.com/whatwg/html/pull/11460

I'd like to get feedback on this from Apple and Mozilla before I go further with launching this in chrome.

<!-- comment id=3163068769 author=annevk created=2025-08-07T08:27:21Z url=https://github.com/whatwg/html/issues/8189#issuecomment-3163068769 -->
## @annevk — 2025-08-07

@cwilso why did you add stage: 4 here? Does this still need agenda+?

<!-- comment id=3164697238 author=css-meeting-bot created=2025-08-07T15:27:22Z url=https://github.com/whatwg/html/issues/8189#issuecomment-3164697238 -->
## @css-meeting-bot — 2025-08-07

The CSS Working Group just discussed `Rendering <select> as a listbox is a one-line widget that opens a popup on iOS and Android`.

<details><summary>The full IRC log of that discussion</summary>
&lt;zcorpan> jarhar: I want to have parity for mobile and desktop<br>
&lt;zcorpan> jarhar: Ability to get either UX on both mobile and desktop<br>
&lt;zcorpan> annevk: There's no native equivalent. Is a challenge<br>
&lt;zcorpan> jarhar: I can't speak to the macOS native stuff... Current prototype in chrome uses popover to render the picker. Similar to appearance: base. But with more opinionated styles<br>
&lt;zcorpan> jarhar: Maybe could implement as popover with !important styles?<br>
&lt;zcorpan> jarhar: Can switch to native in the future if it becomes available<br>
&lt;zcorpan> annevk: Possibly. Not sure if acceptable<br>
&lt;zcorpan> jarhar: Would it be helpful if I create a demo?<br>
&lt;zcorpan> annevk: Not needed, but thx<br>
&lt;zcorpan> emilio: Agree with wanting to be able to make a listbox on mobile. No strong opinion on the design or specifics<br>
</details>

<!-- comment id=3164739523 author=josepharhar created=2025-08-07T15:35:45Z url=https://github.com/whatwg/html/issues/8189#issuecomment-3164739523 -->
## @josepharhar — 2025-08-07

I'm not sure why stage:4 was added, I just removed it.

<!-- comment id=3252571499 author=foolip created=2025-09-04T08:52:38Z url=https://github.com/whatwg/html/issues/8189#issuecomment-3252571499 -->
## @foolip — 2025-09-04

This was briefly revisited in https://github.com/whatwg/html/issues/11622. We think nothing further is needed here since @josepharhar requested and got feedback. @josepharhar please add back the agenda+ label if there is a need for further WHATNOT discussion.
