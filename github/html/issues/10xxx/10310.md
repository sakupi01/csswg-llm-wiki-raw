---
number: 10310
title: "HTML parser changes for customizable `<select>`"
state: "closed"
labels: ["addition/proposal", "topic: forms", "topic: parser", "topic: select"]
author: "josepharhar"
created_at: "2024-04-30T21:27:01Z"
closed_at: "2025-07-22T03:42:00Z"
url: "https://github.com/whatwg/html/issues/10310"
comments: 48
has_resolution: false
synced_at: "2026-09-16T23:45:10Z"
---

### What is the issue with the HTML Standard?

In order to support the [stylable select aka appearance:base-select proposal](https://github.com/whatwg/html/issues/9799), we need to make changes to the HTML parser to allow more tags inside `<select>`, because the current HTML parser basically throws away all tags besides `<option>`, `<optgroup>`, and `<hr>`.

Here are options for how we can extend the HTML parser with varying levels of web compatibility:
1. Allow any tags within `<select>`
2. Allow `<button>` and `<datalist>` tags in `<select>`, and allow any tags within `<button>` and `<datalist>`
3. Allow any tags within a `<select>`’s `<option>`, `<button>`, or `<datalist>`

## 1. Allow any tags within `<select>`
Allowing any tags within `<select>` would be good because it is more flexible for developers since they won’t necessarily have to add a `<datalist>`, but it is the most breaking change of the options I listed. I added [a use counter](https://chromestatus.com/metrics/feature/timeline/popularity/4844) for tags which get dropped [in select mode](https://html.spec.whatwg.org/#parsing-main-inselect), and it is at 0.3%, which is quite high. I also looked at [dozens of the websites](https://docs.google.com/document/d/1tYi-AEpOvcLEJLGuew_s92HRV1JIjWtT_33oD-q2KeI/edit?usp=sharing) with an experimental patch to allow any tags, and while most of them seemed unaffected by allowing anything, some of them were broken:
- http://tx.7ma.cn/
This website has a `<select>` tag without a closing `</select>` tag, and it causes a bunch of the HTML to get put inside the `<select>` instead of being rendered/parsed after the `<select>`.
- http://top.ge/
This website has a `<select>` with additional tags inside the `<option>`s, but it doesn’t actually use the `<select>` element to render and instead has some other custom thing which appears to be reading out the DOM contents of the `<select>`. The dropdown in the website now has a bunch of newlines and odd content in the options.

There’s good reason to believe other sites will be affected in a similar way.

## 2. Allow `<button>` and `<datalist>` tags in `<select> `
A more web-compatible option would be to make the parser allow `<button>` and `<datalist>` in `<select>`, and then make the parser allow anything inside `<button>`/`<datalist>`. These tags correspond to the two visual parts of the `<select>` as per [the explainer](https://open-ui.org/components/selectlist/). I have use counters for `<button>` and `<datalist>` tags inside `<select>` here, and they have very low usage (0.001% and 0.0001% respectively):
https://chromestatus.com/metrics/feature/timeline/popularity/4771
https://chromestatus.com/metrics/feature/timeline/popularity/4772

These usage numbers are low enough that we would be willing to ship in chrome.

## 3. Allow any tags within a `<select>`’s `<option>`, `<button>`, or `<datalist>`
This is like the first option, but doesn’t allow other tags in between `<option>`s. Based on the sites which broke, I don’t think this would be significantly more compatible than just allowing all tags. I also think this might be confusing to developers when arbitrary content can be added inside options but not in between them unless they are all wrapped by a datalist tag.

I also looked through the commit logs of both the webkit/chromium implementation and spec (and [here](https://htmlparser.info/parser/#the-select-element)) in order to find out what the justification was for dropping tags inside `<select>`, and I didn’t find out anything useful. The implementation had [minimal context here](https://bugs.webkit.org/show_bug.cgi?id=41627). When going through the commit log of the HTML spec, I got back to the initial commit of the git repo, which didn’t explain either.

-----

My preference is option 2 because it has the lowest risk of breaking websites, but I have gotten feedback that requiring developers to write `<datalist>` is not great. Nevertheless, given this compat data I think that’s our best option for moving forward.

<!-- comment id=2088612460 author=aardrian created=2024-05-01T15:12:54Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2088612460 -->
## @aardrian — 2024-05-01

When you say "any tags" do you genuinely believe _any_ element can and should be allowed in an `<option>` and/or `<select>`? As in, would this construct be legal?
```
<select>
 <main>
  <h1>sandwiches</h1>
  <ul>
   <li><option>Meat</option></li>
   <li><option>Not Meat</option></li>
  </ul>
 </main>
</select>
```
I know that only addresses your option 1, but pretend I am doing similar for options 2 and 3.

And if you do mean that, how do you propose exposing that through accessibility APIs? Using my example, if I want to navigate by landmarks, is the main region hidden until I expand the `<select>`? What happens when the `<select>` has focus and I just want to arrow through options using only my keyboard? 

The scope of this suggestion feels more like using a howitzer to swat a fly (that fly being devs who fail to close their select menus). Or at least I am not understanding the benefit for users as outlined above.

<!-- comment id=2088621307 author=josepharhar created=2024-05-01T15:19:14Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2088621307 -->
## @josepharhar — 2024-05-01

> When you say "any tags" do you genuinely believe any element can and should be allowed in an `<option>` and/or `<select>`? As in, would this construct be legal?

We discussed this in OpenUI here, and the conclusion as I understand it was to allow anything to be parsed/rendered like normal content, but emit console messages showing why it is not correct for accessibility: https://github.com/openui/open-ui/issues/540

@scottaohara do you have any additional thoughts on this topic?

<!-- comment id=2088653805 author=aardrian created=2024-05-01T15:41:54Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2088653805 -->
## @aardrian — 2024-05-01

That's a lot of reading, but this appears to be [the resolution](https://github.com/openui/open-ui/issues/540#issuecomment-1212330487):

> RESOLVED: Allow interactive content outside of `<options>`, but issue strong console errors or warnings in this case.

Which, ok, the console will tell devs to maybe not do that. However, you are making an issue to _allow_ it. And your take on that resolution is:

>  the conclusion as I understand it was to allow anything to be parsed/rendered like normal content, but emit console messages showing why it is not correct for accessibility

Given that...

* In your words, what is "normal content"?
* Using my example above, how do you envision that working for _users_?

These aren't questions for Scott as he didn't file this issue. I would like to know your take.

<!-- comment id=2089747169 author=annevk created=2024-05-02T06:56:05Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2089747169 -->
## @annevk — 2024-05-02

The parser changes themselves don't have a11y impact. Web developers can already construct arbitrary node trees using API calls and browsers already have to handle those. This is purely about what trees can be constructed using syntax instead of API calls.

Now there is a separate question as to what the new content model of the `select` element is going to be and there we should discuss any new kind of a11y mapping that goes with that and how to determine the fallback for platforms that won't enable the richer rendering, etc.

Let's not conflate them however.

<!-- comment id=2090256411 author=zcorpan created=2024-05-02T11:23:07Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2090256411 -->
## @zcorpan — 2024-05-02

cc @whatwg/html-parser 

<!-- comment id=2090312514 author=zcorpan created=2024-05-02T11:53:37Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2090312514 -->
## @zcorpan — 2024-05-02

I agree that option 2 seems most workable and easier to understand than option 3.

I'm guessing that the content model (i.e. what is allowed for authors to use, not what does the parser do) for `button` can stay as-is? The content model around `datalist`, `option` and `optgroup` probably needs changing to allow other elements in between.

<!-- comment id=2090456466 author=aardrian created=2024-05-02T13:06:13Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2090456466 -->
## @aardrian — 2024-05-02

> The parser changes themselves don't have a11y impact. 

I think you are conflating the accessibility tree with tangible accessibility impact on users. For example, a keyboard user is not impacted by how my construct is represented in the accessibility tree -- they still need to open the select menu to get to the content. Users of Windows High Contrast Mode / Contrast Themes could be impacted based on how this content is mapped to native controls when it assigns user-chosen colors.

Authors will absolutely do things like nest interactive controls, conflicting roles, and more. So whether the changes impact the accessibility tree or the content model feels dismissive of my broader question.

So forgetting the accessibility tree, what if my construct is:

```
<select>
 <main>
  <h1>sandwiches</h1>
  <ul>
   <li><option>Meat</option></li>
   <li><option>Not Meat</option></li>
  </ul>
   <select>
    <button>Do</button>
    <option>This</option>
   </select>
 </main>
</select>
```

Is the idea that in that scenario a keyboard user has to expand the `<select>` to get to the other controls? How do arrow keys work in that case? What about when the user needs to scroll to see all the content? Etc.

Yeah, that's a very specific example, but this proposal is making a _very_ broad suggestion without any equally broad statements how things like keyboard navigation would work.

Also:
> My preference is option 2 because it has the lowest risk of breaking websites, but I have gotten feedback that requiring developers to write <datalist> is not great. 

Is this feedback a function of lack of existing support for `<datalist>` (because browser makers can fix that)? Is this feedback anchored on anything specific?

<!-- comment id=2090640280 author=annevk created=2024-05-02T14:31:08Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2090640280 -->
## @annevk — 2024-05-02

All I'm saying is that this is not the best issue to discuss that broader question. I also wasn't talking about the accessibility tree.

I'm saying that you can create such a construct today. Example: https://software.hixie.ch/utilities/js/live-dom-viewer/saved/12696. The result is a `select` with no options. This issue doesn't propose changing any of that. That requires bigger changes we'll indeed need to discuss in depth.

<!-- comment id=2091292956 author=aardrian created=2024-05-02T19:01:59Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2091292956 -->
## @aardrian — 2024-05-02

> All I'm saying is that this is not the best issue to discuss that broader question.

Fair enough. I don't see an issue for discussing the accessibility impacts I raised, though. @josepharhar have you filed that and I can't find it?

Also, still curious on the `<datalist>` justification you cited.

<!-- comment id=2091865609 author=josepharhar created=2024-05-02T22:41:26Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2091865609 -->
## @josepharhar — 2024-05-02

> I don't see an issue for discussing the accessibility impacts I raised, though. @josepharhar have you filed that and I can't find it?

Looks like scott filed one here: https://github.com/whatwg/html/issues/10317

> Also, still curious on the datalist justification you cited.

I wrote that "I have gotten feedback that requiring developers to write datalist is not great" because developers said that it would be better if they could use the new parser mode without needing to also write `<datalist>`. It's not great to require `<datalist>` to opt in to new parsing behavior, but not a huge deal to have to write it.

<!-- comment id=2091913335 author=aardrian created=2024-05-02T23:42:30Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2091913335 -->
## @aardrian — 2024-05-02

> Looks like scott filed one here: https://github.com/whatwg/html/issues/10317

Indeed he did. And I very much appreciate that Scott did so. But I note it was not filed alongside this issue and he was the one who stepped in to file it after I asked.

> I wrote that "I have gotten feedback that requiring developers to write datalist is not great" because developers said…

Again, what developers? If it was a couple friends of yours, that's fine because it's still feedback but it also doesn't carry a lot of weight. But if it was hundreds of devs as a function of a survey as part of discovery for filing this issue, that feedback carries a lot more weight IMO.

<!-- comment id=2092320546 author=annevk created=2024-05-03T05:29:18Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2092320546 -->
## @annevk — 2024-05-03

It seems somewhat self-evident that requiring `<datalist>` is not great? If you could leave it out and get the same results how would that not be better?

<!-- comment id=2092747196 author=zcorpan created=2024-05-03T10:39:49Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2092747196 -->
## @zcorpan — 2024-05-03

Existing sites might have other tags in `option` and expect the current behavior. The parser currently drops most tags but has special handling for `<select>`, `<input>`, `<keygen>`, `<textarea>`, and for ["in select in table"](https://html.spec.whatwg.org/#parsing-main-inselectintable) also special handling of table-related tags.

Maybe we could keep those special-cases and accept *other* tags in `option`? Maybe even in `select`? Keeping the "break out of `select`" tags as-is seems especially relevant for web compat.

<!-- comment id=2092819690 author=zcorpan created=2024-05-03T11:30:57Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2092819690 -->
## @zcorpan — 2024-05-03

https://ablefast.com/ has `<div><select></div><option>`. If we go with a variant of option 1, this should continue to work.

```
<div class="input-field">
<label for>Past Pools Results:</label>
<select class name="date" onChange="if (!window.__cfRLUnblockHandlers) return false; window.location.href=this.value" data-cf-modified-5aeb4253f0e4e65f25b139af->
<option disabled selected>Select Date</option>
</div>
<div class="table-wrapper table-responsive">
<option value="https://ablefast.com/results/2024-05-04">04-May-2024</option>
```

Also, https://html.spec.whatwg.org/#parsing-main-inselect:has-an-element-in-select-scope would need to be changed to correctly handle `<select>` and `</select>`, since it currently assumes there won't be other elements on the stack (except in the fragment case). Probably it should be more like table scope (with with `select` instead of `table`).

<!-- comment id=2093324622 author=josepharhar created=2024-05-03T16:17:31Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2093324622 -->
## @josepharhar — 2024-05-03

> Maybe we could keep those special-cases and accept other tags in option? Maybe even in select? Keeping the "break out of select" tags as-is seems especially relevant for web compat.

Keeping some special cases sound reasonable, but allowing other tags within option without the datalist opt-in would break top.ge (mentioned in the issue description) because it has `<a>` and `<b>` tags inside `<option>`s, then pulls the DOM out and renders it outside of the `<select>`.

<!-- comment id=2096087807 author=zcorpan created=2024-05-06T14:00:12Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2096087807 -->
## @zcorpan — 2024-05-06

top.ge is affected by both option 1 and option 3. But it would still be usable, right? I tested inserting the `a` and `b` elements in devtools and the widget still worked.

<!-- comment id=2096598486 author=josepharhar created=2024-05-06T17:53:04Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2096598486 -->
## @josepharhar — 2024-05-06

> top.ge is affected by both option 1 and option 3. But it would still be usable, right? I tested inserting the a and b elements in devtools and the widget still worked.

Thanks for taking a look, I really hope I'm wrong about this!

I'm really not sure exactly what the site is doing, but with my [experimental patch](https://chromium-review.googlesource.com/c/chromium/src/+/5518837) to make the parser allow anything, this is what the site looks like:
![Screenshot 2024-05-06 at 10 43 54 AM](https://github.com/whatwg/html/assets/7681467/b054e4a5-4d29-46c6-b142-80d1769f404d)

The HTML of each item in that dropdown list before my patch looks like this:
```html
<li data-original-index="0" class="selected">
  <a tabindex="0" class="" data-tokens="null" role="option" aria-disabled="false" aria-selected="true">
    <span class="text">  ყველა  (19576) </span>
    <span class="glyphicon glyphicon-ok check-mark"></span>
  </a>
</li>
```
And with the patch, it looks like this (I manually edited the whitespace/newlines to make it readable):
```html
<li data-original-index="0" class="selected">
  <a tabindex="0" class="" data-tokens="null" role="option" aria-disabled="false" aria-selected="true">
    <span class="text"> </span>
  </a>
  <a tabindex="0" aria-disabled="false" aria-selected="true"> ყველა </a>
  (<b>19576</b>)
  <span class="glyphicon glyphicon-ok check-mark"></span>
</li>
```

<!-- comment id=2097041513 author=zcorpan created=2024-05-06T22:34:38Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2097041513 -->
## @zcorpan — 2024-05-06

OK, yeah that looks pretty broken, though still _functional_.

Regarding "break out of `select`" tags, is it a requirement to be able to use `input` in `select`? Is it a requirement to be able to use `table` in `select` in `table` (maybe a flight seat picker or so)? I don't see such examples in https://open-ui.org/components/selectlist/#use-cases or https://microsoftedge.github.io/Demos/selectlist/index.html

<!-- comment id=2100966788 author=LeaVerou created=2024-05-08T16:33:40Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2100966788 -->
## @LeaVerou — 2024-05-08

_Writing this with my TAG hat on, and is the same feedback I would give during a design review._

Requiring an opt-in so that HTML content can work in the predictable, obvious way is author-hostile. Elements being dropped within `<select>` and `<option>` has historically been the source of a lot of author pain, even outside styling use cases (e.g. in scripting). Fixing it by introducing more syntax means the default behavior continues to be unintuitive, while authors need to learn yet another trick so their HTML works in the expected way. Opting to hold up such a confusing behavior to avoid changing how unclosed `<select>`s render in a tiny fraction of obscure websites is …the wrong weighing of priorities IMO. 

Not to mention it makes it impossible to use this feature via progressive enhancement, since it completely breaks `<select>` in browsers that don’t support this. This is not necessarily a showstopper by itself, but it will slow down adoption considerably which also means that by the time we have enough author feedback about using it on real-world scenarios, it will be too late to change it in any way.

It might have been a reasonable weighing of tradeoffs if the use counter showed a much higher percentage, but 0.3% is actually _encouraging_ as an upper bound for breakage. I suspect for many, if not most, cases within that 0.3%, the change will actually _fix_ content that is currently broken. E.g. in the example given of an unclosed `<select>` that is eating up the rest of the page, I’d argue the change actually makes the content _more_ accessible. And as it was pointed out, in many other cases, the content is still _functional_, even if it looks broken.

----

Looking at the [compat analysis document](https://docs.google.com/document/d/1tYi-AEpOvcLEJLGuew_s92HRV1JIjWtT_33oD-q2KeI/edit) (thanks @josepharhar for such a detailed report!), there are 19 websites in there of which 13 are actually unaffected by the change (green). Of the 6 that are marked yellow or red:
- 3 are hidden with `display: none`, so not sure why they are marked yellow. If they are hidden, then the change does not negatively affect them and thus they should be green. What am I missing?
- And in the remaining 3, it appears the select is still functional, just would not look pretty, which is probably appropriate since it actually *does* have this HTML content inside it.

Looking at [chromestatus](https://chromestatus.com/metrics/feature/timeline/popularity/4844) it appears there are 117 websites total that do this. How were the 19 in the doc selected? Is it the 19 most popular ones? The 19 first ones?

<!-- comment id=2101515668 author=josepharhar created=2024-05-08T21:32:03Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2101515668 -->
## @josepharhar — 2024-05-08

> Is it a requirement to be able to use table in select in table (maybe a flight seat picker or so)?

The only reason I can think of to use a table in a select would be for laying out the options in a table.

> Requiring an opt-in so that HTML content can work in the predictable, obvious way is author-hostile

I agree. I am becoming increasingly convinced that we should do option 1 and deal with the compat fallout.

> 3 are hidden with display: none, so not sure why they are marked yellow. If they are hidden, then the change does not negatively affect them and thus they should be green. What am I missing?

I agree, you're not missing anything. I was just taking some notes without thinking that I'd publish them and decided to use yellow for these websites since I couldn't figure out if my changes would make a difference or not.

> And in the remaining 3, it appears the select is still functional, just would not look pretty, which is probably appropriate since it actually does have this HTML content inside it.

Yeah the only one which seemed more impacted was http://tx.7ma.cn/ since there is a checkbox and submit button which no longer get rendered.

> Looking at [chromestatus](https://chromestatus.com/metrics/feature/timeline/popularity/4844) it appears there are 117 websites total that do this. How were the 19 in the doc selected? Is it the 19 most popular ones? The 19 first ones?

If I remember correctly it was just the first 19 ones. I also skimmed the list for websites I could recognize, which there were not many of.

<!-- comment id=2101600160 author=lukewarlow created=2024-05-08T22:24:13Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2101600160 -->
## @lukewarlow — 2024-05-08

> is it a requirement to be able to use input in select?

I can see this being useful for building searchable selects. Like a bounded version of input + datalist?

<!-- comment id=2102741168 author=LeaVerou created=2024-05-09T14:11:29Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2102741168 -->
## @LeaVerou — 2024-05-09

> > is it a requirement to be able to use input in select?
> 
> I can see this being useful for building searchable selects. Like a bounded version of input + datalist?

That would require far more extensive changes than the parser changes described here to become possible. Also, it's such a common need that it should be possible without having to roll your own UX (in fact, I'd argue browsers should implement it by default for selects with more than say 20 options).

<!-- comment id=2102754501 author=lukewarlow created=2024-05-09T14:18:11Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2102754501 -->
## @lukewarlow — 2024-05-09

> That would require far more extensive changes than the parser changes described here to become possible.

Option 1 would allow it afaik? (Depending on the specifics of what special cases we keep). It would obviously require JavaScript to implement but it's an example of where that parser change could be useful.

<!-- comment id=2102892017 author=LeaVerou created=2024-05-09T15:30:15Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2102892017 -->
## @LeaVerou — 2024-05-09

> > That would require far more extensive changes than the parser changes described here to become possible.
> 
> Option 1 would allow it afaik? (Depending on the specifics of what special cases we keep). It would obviously require JavaScript to implement but it's an example of where that parser change could be useful.

I could be wrong, but I believe the way the accessibility tree works rn, it doesn't work well with labels and controls nested within other interactive controls. 

<!-- comment id=2102943594 author=lukewarlow created=2024-05-09T15:55:08Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2102943594 -->
## @lukewarlow — 2024-05-09

So I think it would be okay because the input would actually be inside the popover rather than the select button trigger. Having said that the changes to the parser would need changes to the content model and accessibility mappings to make full use of, regardless of the input.

Just wanted to say if we could allow that to work at the parser level it would be good.

<!-- comment id=2103129265 author=mfreed7 created=2024-05-09T17:40:57Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2103129265 -->
## @mfreed7 — 2024-05-09

> Looking at the [compat analysis document](https://docs.google.com/document/d/1tYi-AEpOvcLEJLGuew_s92HRV1JIjWtT_33oD-q2KeI/edit) (thanks @josepharhar for such a detailed report!), there are 19 websites in there of which 13 are actually unaffected by the change (green). Of the 6 that are marked yellow or red:

So we're supportive of trying to ship option <span>#</span>1, assuming it's actually web compatible. (For reference, Chromium's "safe" threshold for deprecations and changes is typically 0.001%, so the current 0.3% use counter is considered extremely high.) I spoke to @josepharhar about this, and we'll do an experiment to see what breakage we might see if we go the "option 1" route. If that experiment produces good results, we'll be supportive of trying to spec option <span>#</span>1.

> > > That would require far more extensive changes than the parser changes described here to become possible.
> > 
> > 
> > Option 1 would allow it afaik? (Depending on the specifics of what special cases we keep). It would obviously require JavaScript to implement but it's an example of where that parser change could be useful.
> 
> I could be wrong, but I believe the way the accessibility tree works rn, it doesn't work well with labels and controls nested within other interactive controls.

I think it's really important to emphasize that this issue ("HTML Parser changes for...") is about just the **parser changes**. Allowing all content (a.k.a. "option 1") does **not** necessarily mean all content is fully supported. It means all content is **parsed**. We still need to work through what content is "allowed", and exactly what it means to be "not allowed". That's https://github.com/whatwg/html/issues/10317.

An important point to make, though, is that similar to the web platform, accessibility is not static. There are great folks working on making AT able to understand more types of things, and represent more complex content in a way that works for AT users. Right now, my understanding is that having something like a button inside an `<option>` is an a11y no-no. However, there are proposals to expand what ARIA/etc can represent. For example, see this proposal: https://github.com/w3c/aria/issues/1440. If adopted, that would make it possible to have accessible buttons within options. Given that we're potentially stuck on web compat changing the parser now, let's not immediately lock ourselves into another such situation later by trying to proactively restrict content types within the parser. The right course of action here would seem to be allowing as much content as possible **in the parser**, and then figuring out how to make it accessible now, or keeping folks from using it until it can be made accessible later.

<!-- comment id=2103596858 author=aardrian created=2024-05-09T23:26:54Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2103596858 -->
## @aardrian — 2024-05-09

> There are great folks working on making AT able to understand more types of things, and represent more complex content in a way that works for AT users.

FWIW, this goes beyond AT (though keyboards _are_ AT, most non-practitioners think "AT" means screen readers). But what I mean is that this comes down to platform support. As, in the browser _and_ the operating system need to support these things and expose them properly for screen readers (and sure, other AT) to be able to use them. For example, Windows UIA does not support heading levels past 9, so Chrome caps it there _despite_ ARIA imposing no such limit.

If you get parsers to allow literally anything in selects, you can't expect the Orca (random example) developers to just make it work for users (at least not with platform support from all the relevant *nix flavors). Nor the ARIA authors to make it just work (the linked ARIA example has been open for 3 years). While this issue is expressly _not_ about accessibility, if you aren't accounting for it in this discussion then please don't suggest can make sure it "works for AT users".

Probably contribute to https://github.com/whatwg/html/issues/10317.

And this response is not a shot at Mason. His was just the easiest to quote from the speculative discussion of accessibility implications.

<!-- comment id=2103618584 author=mfreed7 created=2024-05-09T23:55:00Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2103618584 -->
## @mfreed7 — 2024-05-09

> FWIW, this goes beyond AT (though keyboards _are_ AT, most non-practitioners think "AT" means screen readers). 

Fair. I'm not a practitioner, and I likely am using "AT" incorrectly. Is there a better term for all of the affected tech here?

> If you get parsers to allow literally anything in selects,

My only point here is that the parser is not a gatekeeper. Any site written in React roughly-speaking doesn't use the HTML parser at all. You can very happily do this today:

```
render() {
  return (
    <button>
      <button>Nested button!</button>
    </button>
  );
}
```

So I'd like to take care of accessibility at a different layer than the parser, so React sites and others get the benefit also.

> Probably contribute to #10317.

Yes, please. Let's have this conversation there.

> And this response is not a shot at Mason. His was just the easiest to quote from the speculative discussion of accessibility implications.

No problem!

<!-- comment id=2104508424 author=aardrian created=2024-05-10T12:17:24Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2104508424 -->
## @aardrian — 2024-05-10

> Fair. I'm not a practitioner, and I likely am using "AT" incorrectly. Is there a better term for all of the affected tech here?

No, that is the [correct term](https://www.w3.org/TR/WCAG22/#dfn-assistive-technologies). My point is that "AT" encompasses more than screen readers. The vast majority of AT does not use the accessibility tree.

<!-- comment id=2127184994 author=zcorpan created=2024-05-23T13:55:50Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2127184994 -->
## @zcorpan — 2024-05-23

> Looking at [chromestatus](https://chromestatus.com/metrics/feature/timeline/popularity/4844) it appears there are 117 websites total that do this.

No, I believe it just shows a sample of roughly 100 sites. The data is from httparchive, which contains 12,900,240 sites for desktop and 16,193,055 sites for mobile. 0.17% of the mobile dataset is ~27,500 sites.

If the "3 out of 19 sites are broken" is representative, it would mean breaking ~4,300 sites in the httparchive dataset.

Again, I think the "break out of select" tags are most likely to break sites if they were to no longer break out of select.

<!-- comment id=2128157551 author=mfreed7 created=2024-05-23T22:48:15Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2128157551 -->
## @mfreed7 — 2024-05-23

> > Looking at [chromestatus](https://chromestatus.com/metrics/feature/timeline/popularity/4844) it appears there are 117 websites total that do this.
> 
> No, I believe it just shows a sample of roughly 100 sites.

That's correct - that list is just a sample set of the "top" URLs that hit the use counter. Where "top" is defined by [this methodology](https://developer.chrome.com/blog/crux-rank-magnitude). There are many more than 117 sites hitting this use counter.

> The data is from httparchive, which contains 12,900,240 sites for desktop and 16,193,055 sites for mobile. 0.17% of the mobile dataset is ~27,500 sites.

I'm not sure where the "0.17%" in this sentence comes from, so I'm not sure about its correctness. 

> Again, I think the "break out of select" tags are most likely to break sites if they were to no longer break out of select.

I think I agree with this, with the caveat that the location matters. I.e. I believe most of the broken sites were `<select><option>something<input>` where the `<input>` terminated the `<select>`. But I think our numbers show that the use counters would be much much lower for `<select><datalist><option>something<input>`. I.e. we might be able to make an `<input>` that isn't nested within another element continue to terminate the `<select>` and have that be web compatible.

<!-- comment id=2130104871 author=LeaVerou created=2024-05-24T18:06:34Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2130104871 -->
## @LeaVerou — 2024-05-24

> I think I agree with this, with the caveat that the location matters. I.e. I believe most of the broken sites were `<select><option>something<input>` where the `<input>` terminated the `<select>`. But I think our numbers show that the use counters would be much much lower for `<select><datalist><option>something<input>`. I.e. we might be able to make an `<input>` that isn't nested within another element continue to terminate the `<select>` and have that be web compatible.

Do you mean that `<datalist>` would only be required if form elements are included inside `<option>` and not otherwise?

<!-- comment id=2130111075 author=mfreed7 created=2024-05-24T18:10:28Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2130111075 -->
## @mfreed7 — 2024-05-24

> > I think I agree with this, with the caveat that the location matters. I.e. I believe most of the broken sites were `<select><option>something<input>` where the `<input>` terminated the `<select>`. But I think our numbers show that the use counters would be much much lower for `<select><datalist><option>something<input>`. I.e. we might be able to make an `<input>` that isn't nested within another element continue to terminate the `<select>` and have that be web compatible.
> 
> Do you mean that `<datalist>` would only be required if form elements are included inside `<option>` and not otherwise?

Right. That would be one way to get around specifically the compat issue, if we wanted to permit form elements to be parsed within `<select>`. Again, we're just talking about parser rules here, not whether that should be "allowed". That's https://github.com/whatwg/html/issues/10317.

<!-- comment id=2135279331 author=zcorpan created=2024-05-28T13:54:36Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2135279331 -->
## @zcorpan — 2024-05-28

> > The data is from httparchive, which contains 12,900,240 sites for desktop and 16,193,055 sites for mobile. 0.17% of the mobile dataset is ~27,500 sites.
> 
> I'm not sure where the "0.17%" in this sentence comes from, so I'm not sure about its correctness.

The graph under "Adoption of the feature on top sites" says 0.17% for Apr 1, 2024, both mobile and desktop.

> > Again, I think the "break out of select" tags are most likely to break sites if they were to no longer break out of select.
> 
> I think I agree with this, with the caveat that the location matters. I.e. I believe most of the broken sites were `<select><option>something<input>` where the `<input>` terminated the `<select>`. But I think our numbers show that the use counters would be much much lower for `<select><datalist><option>something<input>`. I.e. we might be able to make an `<input>` that isn't nested within another element continue to terminate the `<select>` and have that be web compatible.

OK, yes.

<!-- comment id=2135724122 author=mfreed7 created=2024-05-28T16:59:12Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2135724122 -->
## @mfreed7 — 2024-05-28

> The graph under "Adoption of the feature on top sites" says 0.17% for Apr 1, 2024, both mobile and desktop.

Ahh! Ok, thanks. I actually never look at that particular plot, since the definition of "top sites" is a bit rough. But anyway, thanks, makes sense.

<!-- comment id=2138986436 author=hsivonen created=2024-05-30T08:22:14Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2138986436 -->
## @hsivonen — 2024-05-30

Considering how long the trickle of bugs from SVG integration and `template` integration (and their combination!) has been, I feel uneasy about poking `select` parsing at all. However, I realize that there's enough demand for better styling of `select` that not changing the parsing at all is probably not a practical approach.

Therefore, my preference is explicit opt-in with `button` and `datalist` and particular spec writing care taken to consider how `svg`, `math`, and `template` as descendants of `select` affect coming back through the "in select" and the "in select in table" insertion modes when coming out of SVG and `template` for any reason (including breakout from SVG on particular special names).

<!-- comment id=2140864955 author=josepharhar created=2024-05-30T21:06:33Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2140864955 -->
## @josepharhar — 2024-05-30

> Therefore, my preference is explicit opt-in with button and datalist and particular spec writing care taken to consider how svg, math, and template as descendants of select affect coming back through the "in select" and the "in select in table" insertion modes when coming out of SVG and template for any reason (including breakout from SVG on particular special names).

Thanks for the feedback! This was my preference as well, but most of the feedback I've gotten so far is that we shouldn't require datalist to opt into parser changes, so I'm going to try to see how far I can get doing that in chromium.

What if we got rid of the "in select" and "in select in table" modes entirely? I think that would make things a lot simpler to spec and implement. We could also make sure that `<select><select>` turns into `<select></select><select></select>` with whatever the same mechanism for `<button>` is, which apparently doesn't require a parser mode.

> Maybe we could keep those special-cases and accept other tags in option? Maybe even in select? Keeping the "break out of select" tags as-is seems especially relevant for web compat.

Of the two websites we've seen that are affected so far, one of them wouldn't be helped by keeping the "break out of select" tags, and hopefully the other can add its missing `</select>` tag perhaps with some outreach.

I also wonder if we still need to have the "in select" mode in order to break out of select since nested `<button>`s do this somehow without the need for a special mode?

<!-- comment id=2145941673 author=zcorpan created=2024-06-03T19:16:29Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2145941673 -->
## @zcorpan — 2024-06-03

> What if we got rid of the "in select" and "in select in table" modes entirely? I think that would make things a lot simpler to spec and implement. We could also make sure that `<select><select>` turns into `<select></select><select></select>` with whatever the same mechanism for `<button>` is, which apparently doesn't require a parser mode.

Note that `<select><select>` is parsed as `<select></select>`.

Other cases to test are `<x><select></x><option>` for call-sites of https://html.spec.whatwg.org/#has-an-element-in-scope (html, body, button, "address", "article", "aside", "blockquote", "button", "center", "details", "dialog", "dir", "div", "dl", "fieldset", "figcaption", "figure", "footer", "header", "hgroup", "listing", "main", "menu", "nav", "ol", "pre", "search", "section", "summary", "ul", form, "dd", "dt", h1-h6, nobr, "applet", "marquee", "object", "rb", "rtc", "rp", "rt", and [AAA](https://html.spec.whatwg.org/#parsing-main-inbody:has-an-element-in-scope-13)).

> > Maybe we could keep those special-cases and accept other tags in option? Maybe even in select? Keeping the "break out of select" tags as-is seems especially relevant for web compat.
> 
> Of the two websites we've seen that are affected so far, one of them wouldn't be helped by keeping the "break out of select" tags, and hopefully the other can add its missing `</select>` tag perhaps with some outreach.

With a search for sites that have one of the breakout tags in select, I expect we would find more than one such broken site. Since the rest of the page could end up inside the `select`, the breakage could be pretty severe. Should we break them anyway? Maybe if it's extremely rare and the change is good for the long term...

> I also wonder if we still need to have the "in select" mode in order to break out of select since nested `<button>`s do this somehow without the need for a special mode?

`<button>` uses "has a button element in scope". It might be possible for `select` under option 1...

<!-- comment id=2149514218 author=josepharhar created=2024-06-05T11:01:11Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2149514218 -->
## @josepharhar — 2024-06-05

Thanks for the feedback!

> Note that `<select><select>` is parsed as `<select></select>`.

Ah I see, perhaps this is something we should continue to do differently from `<button>` and is hopefully implementable without a special select parser mode?

> Other cases to test are `<x><select></x><option>` for call-sites of https://html.spec.whatwg.org/#has-an-element-in-scope

Thanks, I will have to take a closer look at this part of the spec. It looks like `<div><select></div><option>` parses into `<div><select><option>`. This behavior seems fine and could be preserved, do you think that will be hard to do?

> With a search for sites that have one of the breakout tags in select, I expect we would find more than one such broken site. Since the rest of the page could end up inside the select, the breakage could be pretty severe. Should we break them anyway? Maybe if it's extremely rare and the change is good for the long term...

Yeah I'm not sure we will know for sure unless we try shipping the changes, which I might try doing in chrome in pre-stable channels.

> `<button>` uses "has a button element in scope". It might be possible for select under option 1...

Great, perhaps we could create a "has a select element in scope" algorithm

<!-- comment id=2189178702 author=zcorpan created=2024-06-25T14:53:01Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2189178702 -->
## @zcorpan — 2024-06-25

I discussed this with @hsivonen today.

- We're ok with Chromium experimenting with option 1 to find out if it's acceptable in terms of web compat. With the use counter being 0.3% we expect there will be some broken sites.
- I think option 1 is best for developer experience over the longer term.
- One of @hsivonen 's concerns is that the parser has various assumptions and a change like this can break assumptions in subtle ways that might be discovered later. I suppose tests [for all interesting cases](https://web-platform-tests.org/writing-tests/making-a-testing-plan.html) could help.

<!-- comment id=2287286193 author=josepharhar created=2024-08-13T22:39:11Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2287286193 -->
## @josepharhar — 2024-08-13

I created a spec PR here, review would be appreciated: https://github.com/whatwg/html/pull/10557

<!-- comment id=2304377029 author=zcorpan created=2024-08-22T10:57:36Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2304377029 -->
## @zcorpan — 2024-08-22

Analysis of the [mXSS](https://cure53.de/fp170.pdf) risk with this change:

Current parsers will ignore most tags in `select`, and the new parser will not. This allows for getting different parser state between new and legacy parsers, which can be used for mXSS. For example, see https://software.hixie.ch/utilities/js/live-dom-viewer/saved/13018 (targeting legacy browsers where the sanitizer uses the new parsing, but the other way around is probably also possible).

However, as far as I can tell an mXSS attack still needs something else like a RAWTEXT element to be unfiltered, which is more general and e.g. DOMPurify checks for ([demo showing DOMPurify mitigating this attack](https://livedom.bentkowski.info/#%7B%22input%22%3A%22%3Cnew-select%3E%3Cstyle%3E%3C%2Fnew-select%3E%3Cimg%20src%20onerror%3Dxss()%3E%3C%2Fstyle%3E%22%2C%22parser%22%3A%22%2F%2F%20name%3A%20DOMPurify%5Cnclass%20LiveDomParser%20%7B%5Cn%20%20init()%20%7B%5Cn%20%20%20%20const%20s%20%3D%20document.createElement(%5C%22script%5C%22)%3B%5Cn%20%20%20%20s.src%20%3D%20%5C%22https%3A%2F%2Fcure53.de%2Fpurify.js%5C%22%3B%5Cn%20%20%20%20%2F%2F%20s.src%20%3D%20'https%3A%2F%2Fcdnjs.cloudflare.com%2Fajax%2Flibs%2Fdompurify%2F2.1.0%2Fpurify.min.js'%3B%5Cn%20%20%20%20document.head.append(s)%3B%5Cn%20%20%7D%5Cn%20%20parse(s)%20%7B%5Cn%20%20%20%20const%20sanitized%20%3D%20DOMPurify.sanitize(s%2C%20%7B%5Cn%20%20%20%20%20%20%20%20CUSTOM_ELEMENT_HANDLING%3A%20%7B%5Cn%20%20%20%20%20%20%20%20%20%20%20%20tagNameCheck%3A%20%2F%5Enew-%2F%2C%20%2F%2F%20allow%20all%20tags%20starting%20with%20%5C%22new-%5C%22%5Cn%20%20%20%20%20%20%20%20%20%20%20%20attributeNameCheck%3A%20null%2C%20%2F%2F%20default%20%2F%20standard%20attribute%20allow-list%20is%20used%5Cn%20%20%20%20%20%20%20%20%20%20%20%20allowCustomizedBuiltInElements%3A%20false%2C%20%2F%2F%20no%20customized%20built-ins%20allowed%5Cn%20%20%20%20%20%20%20%20%7D%2C%5Cn%20%20%20%20%7D)%3B%5Cn%20%20%20%20display(sanitized%2C%20%5C%22Sanitized%20string%5C%22)%3B%5Cn%20%20%20%20display(%5Cn%20%20%20%20%20%20new%20DOMParser().parseFromString(sanitized%2C%20%5C%22text%2Fhtml%5C%22)%2C%5Cn%20%20%20%20%20%20%5C%22Sanitized%20string%20as%20DOM%20(new%20select%20parsing%20simulated)%5C%22%5Cn%20%20%20%20)%3B%5Cn%20%20%20%20%2F%2F%20Change%20%5C%22newselect%5C%22%20to%20%5C%22select%5C%22%5Cn%20%20%20%20const%20sanitizedSelect%20%3D%20sanitized.replace(%2Fnew-select%2Fg%2C%20%5C%22select%5C%22)%3B%5Cn%20%20%20%20display(%5Cn%20%20%20%20%20%20new%20DOMParser().parseFromString(sanitizedSelect%2C%20%5C%22text%2Fhtml%5C%22)%2C%5Cn%20%20%20%20%20%20%5C%22Sanitized%20string%20as%20DOM%20(legacy%20browser)%5C%22%5Cn%20%20%20%20)%3B%5Cn%20%20%7D%5Cn%7D%5Cn%22%7D)).

Conclusion: sanitizers need to have general protection against mXSS, and so this change doesn't introduce new mXSS vectors, even though the parsed tree can be very different between new and legacy parsers.

<!-- comment id=2506360351 author=annevk created=2024-11-28T15:24:13Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2506360351 -->
## @annevk — 2024-11-28

@josepharhar would it be possible for you to share an update here on Chrome shipping this particular HTML parser change? I remember I asked during a meeting as well, but I just looked at all the minutes going back to TPAC and couldn't find anything.

I think it would be nice if we could proof out these parser changes in other implementations as well as they are so fundamental and I'm wondering what the risk would be.

<!-- comment id=2506367040 author=annevk created=2024-11-28T15:27:56Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2506367040 -->
## @annevk — 2024-11-28

Oh, via the incoming issue link above I stumbled upon https://issues.chromium.org/issues/379034733 which I guess means bad news?

<!-- comment id=2549435591 author=josepharhar created=2024-12-17T19:32:23Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2549435591 -->
## @josepharhar — 2024-12-17

For posterity, I posted an update here: https://github.com/whatwg/html/pull/10557#issuecomment-2515300782

<!-- comment id=2640519900 author=josepharhar created=2025-02-06T17:19:33Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2640519900 -->
## @josepharhar — 2025-02-06

We [encountered an issue](https://issues.chromium.org/issues/394337117) while rolling out parser changes because a site was doing this:
```html
<select id=select1></select>
<script>
select1.innerHTML = '<select id=select2><option>option</option></select>';
</script>
```
Before the parser changes, the HTML fragment parsing algorithm would somehow set the parser mode to the "in select" mode, which would make `<select id=select2>` get dropped. With the new parser changes, we ended up with nested `<select>`s getting into the DOM tree which broke the site.

In this case, `<select id=select1>` is the fragment parsing [context element](https://html.spec.whatwg.org/multipage/parsing.html#concept-frag-parse-context). It seems that looking up through the stack of open elements (which is how the spec PR tries to prevent nested `<select>`s) does not find the context element, and I also saw the [fragment case](https://html.spec.whatwg.org/multipage/parsing.html#fragment-case) note which if I'm reading correctly says that we aren't allowed to change the actual tree building algorithm based on whether we are parsing a fragment.

Can anyone recommend how to change the spec PR to un-break this case?

<!-- comment id=2660181590 author=josepharhar created=2025-02-14T20:13:48Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2660181590 -->
## @josepharhar — 2025-02-14

To follow up on my previous comment:

Here is the line of the existing spec which supports the innerHTML behavior in [reset-the-insertion-mode-appropriately](https://html.spec.whatwg.org/multipage/parsing.html#reset-the-insertion-mode-appropriately)

> Loop: If node is the first node in the stack of open elements, then set last to true, and, if the parser was created as part of the [HTML fragment parsing algorithm](https://html.spec.whatwg.org/multipage/parsing.html#html-fragment-parsing-algorithm) ([fragment case](https://html.spec.whatwg.org/multipage/parsing.html#fragment-case)), set node to the [context](https://html.spec.whatwg.org/multipage/parsing.html#concept-frag-parse-context) element passed to that algorithm.

This sets the parser mode to the "in select mode", which is what prevents the nested select element from being parsed.

I added prose like this to the select element case in the parser to fix the regression: https://github.com/whatwg/html/pull/10557/commits/9d860113c21b35a2db45be3245784cfa1741260a

<!-- comment id=2962478935 author=Shagufta-Tabreen created=2025-06-11T12:22:02Z url=https://github.com/whatwg/html/issues/10310#issuecomment-2962478935 -->
## @Shagufta-Tabreen — 2025-06-11

Allow any tags inside <select>
(Most flexible, but might break old websites)

Allow only <button> and <datalist> inside <select>
(Safer, but still adds flexibility)

Allow any tags inside <option>, <button>, or <datalist>
(Even more controlled)
@josepharhar - Adding elements out of select is much better as the solution built can be controlled.
