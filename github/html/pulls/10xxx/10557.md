---
number: 10557
title: "Relax `<select>` parser"
state: "closed"
labels: ["do not merge yet", "normative change", "topic: parser", "topic: select"]
author: "josepharhar"
created_at: "2024-08-13T22:37:01Z"
closed_at: "2025-07-22T03:41:59Z"
url: "https://github.com/whatwg/html/pull/10557"
comments: 50
has_resolution: false
synced_at: "2026-09-17T04:50:53Z"
---

This patch makes the parser allow additional tags in `<select>` besides `<option>`, `<optgroup>`, and `<hr>`, mostly by removing the "in select" and "in select in table" parser modes.

In order to replicate the behavior where opening a `<select>` tag within another open `<select>` tag inserts a `</select>` close tag, a traversal through the stack of open elements was added which I borrowed from the `<button>` part of the parser.

This patch also changes the processing model to make `<select>` look through all its descendants in the DOM tree for `<option>` elements, rather than just children and optgroup children which conform to the content model. This is needed for compat reasons because there are websites which put other tags in between their `<select>` and `<option>`s which would break with this parser change unless we also update this processing model. More context [here](https://github.com/whatwg/html/pull/10557#issuecomment-2292486522) and [here](https://github.com/whatwg/html/issues/10310).

Fixes https://github.com/whatwg/html/issues/10310

- [x] At least two implementers are interested (and none opposed):
   * Chromium
   * https://github.com/mozilla/standards-positions/issues/1086
   * https://github.com/WebKit/standards-positions/issues/414
- [x] [Tests](https://github.com/web-platform-tests/wpt) are written and can be reviewed and commented upon at:
   * https://github.com/html5lib/html5lib-tests/pull/178
   * https://wpt.fyi/results/html/semantics/forms/the-select-element/customizable-select/select-parsing.tentative.html
- [x] [Implementation bugs](https://github.com/whatwg/meta/blob/main/MAINTAINERS.md#handling-pull-requests) are filed:
   * Chromium: Already implemented
   * Gecko: https://bugzilla.mozilla.org/show_bug.cgi?id=1933591
   * WebKit: https://bugs.webkit.org/show_bug.cgi?id=283732
- [x] [MDN issue](https://github.com/whatwg/meta/blob/main/MAINTAINERS.md#handling-pull-requests) is filed: I read the select, option, and optgroup element pages, and I didn't see any content that needs to be updated.
- [x] The top of this comment includes a [clear commit message](https://github.com/whatwg/meta/blob/main/COMMITTING.md) to use. <!-- If you created this PR from a single commit, Github copied its message. Otherwise, you need to add a commit message yourself. -->

(See [WHATWG Working Mode: Changes](https://whatwg.org/working-mode#changes) for more details.)


<!--
    This comment and the below content is programmatically generated.
    You may add a comma-separated list of anchors you'd like a
    direct link to below (e.g. #idl-serializers, #idl-sequence):

    Don't remove this comment or modify anything below this line.
    If you don't want a preview generated for this pull request,
    just replace the whole of this comment's content by "no preview"
    and remove what's below.
-->
***
<a href="https://whatpr.org/html/10557/form-elements.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">/form-elements.html</a>  ( <a href="https://whatpr.org/html/10557/734afba...e944683/form-elements.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">diff</a> )
<a href="https://whatpr.org/html/10557/index.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">/index.html</a>  ( <a href="https://whatpr.org/html/10557/734afba...e944683/index.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">diff</a> )
<a href="https://whatpr.org/html/10557/infrastructure.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">/infrastructure.html</a>  ( <a href="https://whatpr.org/html/10557/734afba...e944683/infrastructure.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">diff</a> )
<a href="https://whatpr.org/html/10557/parsing.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">/parsing.html</a>  ( <a href="https://whatpr.org/html/10557/734afba...e944683/parsing.html" title="Last updated on Feb 28, 2025, 5:29 PM UTC (e944683)">diff</a> )

<!-- comment id=2288517645 author=annevk created=2024-08-14T11:39:54Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2288517645 -->
## @annevk — 2024-08-14

Why does this also make a bunch of changes to the processing model of `select` elements? If that needs to be part of this change that should really be better motivated in the commit message.

<!-- comment id=2292486522 author=josepharhar created=2024-08-16T00:08:20Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2292486522 -->
## @josepharhar — 2024-08-16

> Why does this also make a bunch of changes to the processing model of select elements? If that needs to be part of this change that should really be better motivated in the commit message.

The changes in the processing model do support the new proposed content model for select, but they also mitigate compat issues for websites which are already putting tags in between `<select>` and `<option>` in their HTML.

For example, without these changes to the processing model, the following `<select>` would not register as having any options at all:
```html
<select>
  <div>
    <option>...</option>
    ...
  </div>
</select>
```
In my compat analysis, I found a lot of websites which are doing this, so in order to ship the parser changes separately from customizable select in chrome, I plan to ship the parser changes and these processing model changes together, because otherwise there would be too much breakage.

I'm happy to put them in a separate PR if you want, or keep them here and update the commit message (sorry for not putting it in there). Which would you prefer?

<!-- comment id=2292882848 author=annevk created=2024-08-16T06:11:37Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2292882848 -->
## @annevk — 2024-08-16

Thanks! Given that rationale I think it's good to couple the changes, but that should be in the commit message as well.

<!-- comment id=2296916927 author=zcorpan created=2024-08-19T15:59:08Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2296916927 -->
## @zcorpan — 2024-08-19

This doesn't define optional tags for `</option>` and `</optgroup>` correctly.

The definition for "have a particular element in select scope" may be needed for that, but should be changed to be similar to "have a particular element in button scope" (but for `select`).

In particular, allow these without a parse error:

```html
<select>
<optgroup>
<option>
<optgroup>
</select>
```
The second `<optgroup>` should pop the `option` and `optgroup`

```html
<select>
<option><p>
<option>
</select>
```
This should generate implied end tags and pop the `option`.

```html
<select>
<optgroup>
<hr>
<option>
<hr>
</select>
```
The `hr`s should pop the `optgroup` and `option` in a `select`.

See how the parser deals with `<ruby><rtc><rt>`, I think that can be used as a model for `select`.

<!-- comment id=2297488729 author=josepharhar created=2024-08-19T21:28:14Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2297488729 -->
## @josepharhar — 2024-08-19

> Thanks! Given that rationale I think it's good to couple the changes, but that should be in the commit message as well.

Done.

> This doesn't define optional tags for `</option>` and `</optgroup>` correctly.

@zcorpan thanks for the feedback! I did some experimenting and added some logic to the start tags for option, optgroup, and hr. What do you think?

<!-- comment id=2304149649 author=zcorpan created=2024-08-22T09:02:46Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2304149649 -->
## @zcorpan — 2024-08-22

- Need to check that a `select` element is in scope so that parsing of option/optgroup tags outside of `select` doesn't change. Example `<option><p>1</option>2<option>3`
- Need to add `select` to the "[in scope](https://html.spec.whatwg.org/#has-an-element-in-scope)" list so that `<div><select></div><option>` doesn't close the `select`.
- Should report parse errors when unexpected elements are popped. Compare with what the spec does for `rt`/`rtc` in `ruby`. Example `<select><option><span>1</option>2<option>3`

<!-- comment id=2305935774 author=josepharhar created=2024-08-22T23:57:54Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2305935774 -->
## @josepharhar — 2024-08-22

> * Need to check that a `select` element is in scope so that parsing of option/optgroup tags outside of `select` doesn't change. Example `<option><p>1</option>2<option>3`

I thought that this is covered by "While the stack of open elements has an option element in select scope". What exactly should I change?

> * Need to add `select` to the "[in scope](https://html.spec.whatwg.org/#has-an-element-in-scope)" list so that `<div><select></div><option>` doesn't close the `select`.

Done

> * Should report parse errors when unexpected elements are popped. Compare with what the spec does for `rt`/`rtc` in `ruby`. Example `<select><option><span>1</option>2<option>3`

I'm guessing this is from "If the current node is not now a rtc element or a ruby element, this is a parse error," right?

Should I add "If the current node is not now an option element, this is a parse error" after "While the stack of open elements has an option element in select scope, pop an element from the stack of open elements"?

<!-- comment id=2306943717 author=zcorpan created=2024-08-23T11:58:59Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2306943717 -->
## @zcorpan — 2024-08-23

The "in select scope" I think should be removed altogether since it assumes the stack will not have other elements when in a `select`, which is no longer the case. Use the normal "in scope" instead.

High-level of what I think should happen: when parsing option or optgroup start tag: check that select is in scope, check that option or optgroup is in scope, generate implied end tags (except for optgroup when handling `<option>`), check the current node or the stack of open elements again, report a parse error if appropriate, then pop elements off the stack until the `option` or `optgroup` has been popped, then insert the new element.

I can look into this more next week and suggest more specific changes.

<!-- comment id=2313810604 author=josepharhar created=2024-08-28T00:26:31Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2313810604 -->
## @josepharhar — 2024-08-28

> High-level of what I think should happen: when parsing option or optgroup start tag: check that select is in scope, check that option or optgroup is in scope, generate implied end tags (except for optgroup when handling `<option>`), check the current node or the stack of open elements again, report a parse error if appropriate, then pop elements off the stack until the `option` or `optgroup` has been popped, then insert the new element.

Thanks! I gave this a try

<!-- comment id=2329921158 author=josepharhar created=2024-09-04T20:34:56Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2329921158 -->
## @josepharhar — 2024-09-04

@zcorpan how does the latest text look?

<!-- comment id=2342256937 author=josepharhar created=2024-09-10T22:34:51Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2342256937 -->
## @josepharhar — 2024-09-10

> This doesn't define optional tags for `</option>` and `</optgroup>` correctly.

I was talking to @mfreed7 about the changes we've made in the PR so far, and I feel like I couldn't provide a good explanation for why we are making the parser not support cases like these:
- `<hr>` inside an `<option>`
- nested `<optgroup>`s

Is it just compat reasons? Is there a good justification?

<!-- comment id=2343045964 author=zcorpan created=2024-09-11T08:53:59Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2343045964 -->
## @zcorpan — 2024-09-11

It would be a breaking change from what is conforming HTML today, and break compat for sites that omit `</option>` and `</optgroup>` tags. It's the same as why we can't allow certain elements in `p`.

<!-- comment id=2344721374 author=mfreed7 created=2024-09-11T21:19:37Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2344721374 -->
## @mfreed7 — 2024-09-11

> It would be a breaking change from what is conforming HTML today

But that's true of the rest of this PR also, correct? E.g. making `<input>` no longer close the `<select>`. We are just thinking that the "new" behavior we're headed towards with `appearance:base` is that roughly "all" content is now valid within `<select>`. It feels very odd to instead say "all content is valid, except for nested `<optgroup>` and `<hr>` within an `<option>`". Neither of those sound like they come from anything other than a place of web compat grandfathering. So I guess our hope would be that we can try to remove those two restrictions along with the rest, and see what actually falls out as a real web compat issue? Unless there's some deeper semantic problem with nesting optgroups? Or including horizontal lines within an option?

<!-- comment id=2345467836 author=zcorpan created=2024-09-12T07:24:41Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2345467836 -->
## @zcorpan — 2024-09-12

`<input>` is not allowed in `select` today, so it's not a change in behavior for conforming HTML. I think there's an implied contract of not breaking content that follows HTML's authoring requirements.

`hr` in `select` is pretty new, so we could probably make it not imply `</option>` without affecting web compat significantly. A tradeoff is for authors who want to omit optional tags can't omit `</option>` only when the next child is `hr`. Is that more annoying than not being able to use `hr` in `option`? I suspect no.

I expect we can't allow nesting of `optgroup`, but I haven't researched it. If that is allowed, it would be reasonable to expect that the native dropdown can support nested `optgroup`s. I don't know if that is possible on the relevant platforms.

<!-- comment id=2345476670 author=zcorpan created=2024-09-12T07:29:33Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2345476670 -->
## @zcorpan — 2024-09-12

> It would be a breaking change from what is conforming HTML today

To clarify, I meant: It would be a breaking change for existing conforming HTML content.

<!-- comment id=2346762592 author=scottaohara created=2024-09-12T16:34:51Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2346762592 -->
## @scottaohara — 2024-09-12

fwiw, i can't think of a good reason for an author to even need an `hr` inside of an `option`.  

if someone wants to create a visual separation between the text within an option - then that sounds like something that could/should be accomplished with CSS.  The semantics/a11y mappings of a separator dont' really make sense in the context of an option

<!-- comment id=2346901112 author=josepharhar created=2024-09-12T17:51:13Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2346901112 -->
## @josepharhar — 2024-09-12

> I expect we can't allow nesting of `optgroup`, but I haven't researched it. If that is allowed, it would be reasonable to expect that the native dropdown can support nested `optgroup`s. I don't know if that is possible on the relevant platforms.

Yeah that's fair, UIs won't support nested optgroups, at least not yet. There is a separate issue to track this here: https://github.com/whatwg/html/issues/5789

I suppose that if we want to support nested optgroups then we could revisit this, and disallow nested optgroups in the parser for now.

> `<input>` is not allowed in `select` today, so it's not a change in behavior for conforming HTML. I think there's an implied contract of not breaking content that follows HTML's authoring requirements.
> 
> `hr` in `select` is pretty new, so we could probably make it not imply `</option>` without affecting web compat significantly. A tradeoff is for authors who want to omit optional tags can't omit `</option>` only when the next child is `hr`. Is that more annoying than not being able to use `hr` in `option`? I suspect no.



> fwiw, i can't think of a good reason for an author to even need an `hr` inside of an `option`.
> 
> if someone wants to create a visual separation between the text within an option - then that sounds like something that could/should be accomplished with CSS. The semantics/a11y mappings of a separator dont' really make sense in the context of an option

We can forbid hr in select in the content model and still allow it to be parsed, just like we are doing with many other tags in this change. I suspect that hr in option would be a significantly less breaking change than other things.

I see the conforming HTML argument though and I don't feel that strongly about this. Mason's point is that we should only create special cases like this if we have a really good reason. If not changing conforming HTML is that important, then I think its probably fine since hr in select is not an important use case.

<!-- comment id=2353232886 author=zcorpan created=2024-09-16T15:24:08Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2353232886 -->
## @zcorpan — 2024-09-16

`hr` in `select` was added just last year, so we shouldn't flip-flop on that. But it could be allowed only as a sibling to an `option` element in the content model, if we're ok with `hr` no longer implicitly closing `option` elements.

<!-- comment id=2353266624 author=zcorpan created=2024-09-16T15:38:18Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2353266624 -->
## @zcorpan — 2024-09-16

Since the content model in https://github.com/whatwg/html/pull/10586 only allows `div` and `span`, maybe there's less need to "aggressively" close elements when seeing `option` or `optgroup`. Is only `div` and `span` the end goal, or do we eventually want to allow other HTML elements?

If, for web compat, it's ok to only close `option` if it's the current node when seeing "option" or "optgroup" (maybe also "hr"), that would make the parser change much simpler. It would also allow for a possible future of allowing nested `optgroup`s (would need a `div` in between).

<!-- comment id=2353430802 author=josepharhar created=2024-09-16T16:50:27Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2353430802 -->
## @josepharhar — 2024-09-16

> hr in select was added just last year, so we shouldn't flip-flop on that. But it could be allowed only as a sibling to an option element in the content model, if we're ok with hr no longer implicitly closing option elements.

The content model PR currently doesn't list hr elements in the content model for options

> Is only div and span the end goal, or do we eventually want to allow other HTML elements?

Eventually allowing other HTML elements is ideal as we add support for additional use cases and find ways to make them accessible.

> If, for web compat, it's ok to only close option if it's the current node when seeing "option" or "optgroup" (maybe also "hr"), that would make the parser change much simpler. It would also allow for a possible future of allowing nested optgroups (would need a div in between).

As Mason said, ideally we try removing restrictions and see what real issues arise. Would I implement this by removing the "generate implied end tags" line?

<!-- comment id=2353877494 author=zcorpan created=2024-09-16T20:18:37Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2353877494 -->
## @zcorpan — 2024-09-16

If we envision allowing more HTML elements later, then we should at least generate implied end tags, so that HTML authoring rules are somewhat consistent. For example, it should be possible to omit `</p>` in `select`.

<!-- comment id=2357171165 author=josepharhar created=2024-09-17T23:21:00Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2357171165 -->
## @josepharhar — 2024-09-17

> If we envision allowing more HTML elements later, then we should at least generate implied end tags, so that HTML authoring rules are somewhat consistent. For example, it should be possible to omit `</p>` in `select`.

Ah I see. So if I leave that in and then remove the "While the stack of open elements has an option element in scope, pop an element" part and the equivalent one for optgroup then we should be good?

<!-- comment id=2358898999 author=zcorpan created=2024-09-18T16:18:48Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2358898999 -->
## @zcorpan — 2024-09-18

No, still need to pop the `option` or `optgroup` if that is the current node. Actually, using the `ruby` model is maybe best, however the parse error condition needs to be different (since we want to allow `<select><div><option><option>`, the parent is not a `select` here).

Something like this:

> - A start tag whose tag name is "optgroup"
>
>    If the stack of open elements has a `select` element in scope, then generate implied end tags. If the stack of open elements now has an `option` or `optgroup` element in scope, then this is a parse error.
>
>   Insert an HTML element for the token.
>
> - A start tag whose tag name is "option"
>
>    If the stack of open elements has a `select` element in scope, then generate implied end tags, except for `optgroup` elements. If the stack of open elements now has an `option` element in scope, then this is a parse error.
>
>   Insert an HTML element for the token.

This also removes "Reconstruct the active formatting elements , if any.", so that that happens inside the `option` element instead when seeing a token that reconstructs active formatting elements.

<!-- comment id=2358985687 author=josepharhar created=2024-09-18T17:00:47Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2358985687 -->
## @josepharhar — 2024-09-18

Thanks! I implemented your suggestion

<!-- comment id=2359162041 author=josepharhar created=2024-09-18T18:38:43Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2359162041 -->
## @josepharhar — 2024-09-18

Generating all implied end tags with no exceptions in the optgroup case disallows nested optgroups, but I guess I'm ok with that since we can make additional changes to allow it later.

I'll apply the changes in here so far to the chromium implementation and start working on html5lib tests so we can get this merged.

<!-- comment id=2359255654 author=zcorpan created=2024-09-18T19:33:56Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2359255654 -->
## @zcorpan — 2024-09-18

Great.

`hr` no longer closing `option` and `optgroup` is a behavior change from what the parser currently does, which I'm somewhat uncomfortable with. It makes currently valid HTML invalid. @hsivonen @annevk WDYT?

<!-- comment id=2360626751 author=annevk created=2024-09-19T10:36:22Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2360626751 -->
## @annevk — 2024-09-19

I'm not a big fan of breaking conforming HTML. Especially as there doesn't appear to be a compelling reason from reading the thread.

I recommend not using "grandfathering" by the way, it has a rather bad history: https://en.wikipedia.org/wiki/Grandfather_clause

<!-- comment id=2364703976 author=josepharhar created=2024-09-20T22:32:52Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2364703976 -->
## @josepharhar — 2024-09-20

Ok, I pushed a change to disallow hr in option. How does it look now?

<!-- comment id=2389399910 author=josepharhar created=2024-10-02T18:23:10Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2389399910 -->
## @josepharhar — 2024-10-02

@zcorpan @hsivonen are there any other changes you think should be made?

<!-- comment id=2389698694 author=josepharhar created=2024-10-02T21:07:31Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2389698694 -->
## @josepharhar — 2024-10-02

I just re-added some cases to option and optgroup parsing to continue supporting auto-closing tags outside of a select tag to support cases like this, which I found in a test case [here](https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/web_tests/fast/forms/select/option-defaultselected.html;l=25;drc=77578ccb4082ae20a9326d9e673225f1189ebb63):
```javascript
const select = document.createElement('select');
select.innerHTML = '<option>one<option>two';
```

<!-- comment id=2408222359 author=josepharhar created=2024-10-11T23:06:23Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2408222359 -->
## @josepharhar — 2024-10-11

I created an html5lib PR to update tests: https://github.com/html5lib/html5lib-tests/pull/178

<!-- comment id=2514427365 author=zcorpan created=2024-12-03T12:27:03Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2514427365 -->
## @zcorpan — 2024-12-03

Per discussion in Matrix ([1](https://matrixlogs.bakkot.com/WHATWG/2024-12-02#L22), [2](https://matrixlogs.bakkot.com/WHATWG/2024-12-03#L6)), the web compat situation is not yet clear, see e.g. `<select><input>` issue: https://issues.chromium.org/issues/379612186

@josepharhar what is the rollout plan in Chromium? I think it makes sense to wait with merging this PR until it has been shipping in Stable for a bit.

<!-- comment id=2515300782 author=josepharhar created=2024-12-03T18:29:44Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2515300782 -->
## @josepharhar — 2024-12-03

I disabled this new parser behavior in chrome 131 due to a bug with one of multiple code paths which collects options to render in the select's popup which doesn't exist in the spec. I am going to re-enable in chrome 133, and yes `<input>` in `<select>` is still an open question. I'd like to support this if possible and I will come back here to report on that after the next shipping attempt.

> I think it makes sense to wait with merging this PR until it has been shipping in Stable for a bit.

That sounds reasonable to me especially since shipping this change in chrome is not blocked by merging this PR.

<!-- comment id=2518578246 author=lukewarlow created=2024-12-04T21:16:28Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2518578246 -->
## @lukewarlow — 2024-12-04

I think this is missing a required spec change: https://html.spec.whatwg.org/multipage/parsing.html#:~:text=An%20end%20tag%20whose%20tag%20name%20is%20one%20of%3A%20%22address%22 - select needs adding to this list of elements?

<!-- comment id=2521399519 author=lukewarlow created=2024-12-05T20:59:21Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2521399519 -->
## @lukewarlow — 2024-12-05

Btw I think [this](https://wpt.live//html/semantics/forms/the-select-element/select-value.html) test needs updating as it's currently failing in Chromium with this change

<!-- comment id=2522542479 author=zcorpan created=2024-12-06T08:51:30Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2522542479 -->
## @zcorpan — 2024-12-06

@lukewarlow good catch! Right now the end tag is handled by "Any other end tag", which will ignore the tag if the current node is not a `select` element. (Chromium seems to implement something else, testing with `<select><div></select>x`.)

<!-- comment id=2523988375 author=josepharhar created=2024-12-06T19:15:11Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2523988375 -->
## @josepharhar — 2024-12-06

> I think this is missing a required spec change: https://html.spec.whatwg.org/multipage/parsing.html#:~:text=An%20end%20tag%20whose%20tag%20name%20is%20one%20of%3A%20%22address%22 - select needs adding to this list of elements?



> @lukewarlow good catch! Right now the end tag is handled by "Any other end tag", which will ignore the tag if the current node is not a `select` element. (Chromium seems to implement something else, testing with `<select><div></select>x`.)

Thanks, I added select there in this PR. It's already included there [in chromium](https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/html/parser/html_tree_builder.cc;l=2069;drc=8ff4f09f8bbc04e69f0f20144a2005eb4c1eed53).



> Btw I think [this](https://wpt.live//html/semantics/forms/the-select-element/select-value.html) test needs updating as it's currently failing in Chromium with this change

Thanks, fixing that [here](https://chromium-review.googlesource.com/c/chromium/src/+/6077768).

<!-- comment id=2525275511 author=flavorjones created=2024-12-07T18:47:13Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2525275511 -->
## @flavorjones — 2024-12-07

@josepharhar Are the test changes in https://github.com/html5lib/html5lib-tests/pull/178 up to date with these proposed spec changes now? It's been hard to follow along across the multiple repositories.

<!-- comment id=2529114096 author=josepharhar created=2024-12-09T18:59:34Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2529114096 -->
## @josepharhar — 2024-12-09

> @josepharhar Are the test changes in [html5lib/html5lib-tests#178](https://github.com/html5lib/html5lib-tests/pull/178) up to date with these proposed spec changes now? It's been hard to follow along across the multiple repositories.

I just updated all tests in the PR

<!-- comment id=2555852087 author=flavorjones created=2024-12-19T22:04:25Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2555852087 -->
## @flavorjones — 2024-12-19

It doesn't look like the pr-preview bot updated the "diff" links after f8d14341 was added to this PR (the links still point to 08bdb25). Would it be possible for someone to nudge the bot to recreate those links? Thank you!

<!-- comment id=2563020359 author=josepharhar created=2024-12-26T18:44:54Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2563020359 -->
## @josepharhar — 2024-12-26

> It doesn't look like the pr-preview bot updated the "diff" links after [f8d1434](https://github.com/whatwg/html/commit/f8d143419f1489e74628179517c25abed770ca4c) was added to this PR (the links still point to [08bdb25](https://github.com/whatwg/html/commit/08bdb255206852c208f17dd1e0a315acc6111bf4)). Would it be possible for someone to nudge the bot to recreate those links? Thank you!

Looks to me like its up to date now

<!-- comment id=2657679197 author=josepharhar created=2025-02-13T20:48:34Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2657679197 -->
## @josepharhar — 2025-02-13

I just pushed a commit to bring back the behavior where `<input>` closes `<select>` for compat reasons. As discussed at WHATNOT today, it would be good to try relaxing this in the future if we allow `<input>` in the content model by perhaps only making `<input>` close `<select>` when the `<input>` is a direct child of `<select>`.

<!-- comment id=2689196145 author=lukewarlow created=2025-02-27T21:55:47Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2689196145 -->
## @lukewarlow — 2025-02-27

Might be a silly question but does the check for HTML fragment parsing algorithm mean that this doesn't apply if you're parsing in an XHTML document?

<!-- comment id=2690171101 author=annevk created=2025-02-28T09:35:08Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2690171101 -->
## @annevk — 2025-02-28

@lukewarlow XHTML documents use the XML parser.

<!-- comment id=2845344812 author=josepharhar created=2025-05-01T17:44:14Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2845344812 -->
## @josepharhar — 2025-05-01

This spec PR currently disallows `<input>` in `<select>` by looking up all of the ancestor nodes when inserting an `<input>`. Is anyone opposed to changing this to only looking at the parent node rather than all of the ancestors?

This would allow this to be parsed without the parser changing things:
```html
<select>
  <div>
    <input>
```

<!-- comment id=2847093540 author=annevk created=2025-05-02T12:29:18Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2847093540 -->
## @annevk — 2025-05-02

That would mean `<select><option><input>` and `<select><optgroup><input>` would also not pop the `option` and `optgroup` elements or the `select` element? In principle that seems reasonable, though it's not entirely clear if it's worth the potential web compatibility fallout as we don't have a plan to make it accessible, right?

<!-- comment id=2847348994 author=josepharhar created=2025-05-02T14:28:32Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2847348994 -->
## @josepharhar — 2025-05-02

Good point, I'd be OK with doing the `<input>` closing `<select>` logic when encountering `<input>`, `<option>`, or `<optgroup>` as the parent node.

<!-- comment id=2847353840 author=annevk created=2025-05-02T14:30:54Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2847353840 -->
## @annevk — 2025-05-02

That doesn't answer the more significant question. (And I don't really have the data to pick between that and what you suggested. I was just pointing out a consequence.)

<!-- comment id=2847387972 author=josepharhar created=2025-05-02T14:47:10Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2847387972 -->
## @josepharhar — 2025-05-02

I have UseCounters for this which haven't reached stable yet, and I am working with accessibility folks on this right now. I can share more once the UseCounters hit stable and when I get more accessibility feedback.

<!-- comment id=2963222382 author=josepharhar created=2025-06-11T15:13:09Z url=https://github.com/whatwg/html/pull/10557#issuecomment-2963222382 -->
## @josepharhar — 2025-06-11

This PR won't be merged and will instead be included in a bigger PR including all customizable select PRs: https://github.com/whatwg/html/pull/10548
