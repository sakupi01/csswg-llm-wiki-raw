---
number: 10605
title: "[css-sizing] Intrinsic size of <img> / <video> with aspect ratio but no definite size"
state: "open"
labels: ["css-sizing-3"]
author: "emilio"
created_at: "2024-07-21T01:54:20Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/10605"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:01Z"
---

Consider [this test-case](https://bug1905511.bmoattachments.org/attachment.cgi?id=9413986) from [Mozilla bug 1905511](https://bugzilla.mozilla.org/show_bug.cgi?id=1905511).

In it, you can see how, while loading an image, Firefox treats it as having no intrinsic size (like the SVG). This is consistent with how we want to treat videos (for the use-case in https://github.com/w3c/csswg-drafts/issues/7524 to work, which works now in Firefox and WebKit).

However that's not what Blink does, and people get a bit confused (because the image might grow too much). It seems WebKit does something weird where it doesn't stretch images with no intrinsic size but a ratio (like the SVG in the test-case).

Blink seems to just give the image a 0x0 size while loading, which is slightly unfortunate, but maybe fine. I could live with that inconsistency between `<img>` and `<video>` I guess. The issue with `<video>` is really the default object size.

Questions:

 * What of the two `<img>` behaviors do we want? Both seem acceptable to me, but let's be interoperable? :)
   * `<img>` has `0x0` intrinsic size (doesn't respect aspect-ratio if no dimension is definite, Blink's behavior)
   * `<img>` has no intrinsic size (might grow too much, Firefox's behavior).
 * Is there some interest in Blink for fixing the `<video>` situation in #7524? ([test here](https://wpt.fyi/results/html/rendering/replaced-elements/attributes-for-embedded-content-and-images/video-aspect-ratio.html)). Chromium filed it so I was hoping they'd get to it sooner rather than later.
 * (Bonus) What is WebKit doing?

Want to run it through the group before going with Blink's behavior, in case people have strong opinions...

cc: @fantasai @jensimmons @bfgeek @chrishtr

<!-- comment id=2241386884 author=jensimmons created=2024-07-21T02:10:09Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2241386884 -->
## @jensimmons — 2024-07-21

I don't understand the question from this level of explanation. Can you provide more context?

<!-- comment id=2241391450 author=emilio created=2024-07-21T02:27:22Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2241391450 -->
## @emilio — 2024-07-21

So, consider a `<video>`. If you just set `<video width=100 height=100>` that maps to `width: 100px; height: 100px; aspect-ratio: auto 100 / 100;`, so far so good. But if you override the width / height with CSS (setting them to e.g. auto, because let's say you're using it in a flex container or something else), that'd fall back to 300x150 (rather than what you'd expect which is stretching or what not). So in #7524 I made `<video>` not report an intrinsic size if it had an aspect-ratio, effectively.

For `<img>`, you have the same issue, it's just that the fallback size is 0x0. Firefox treats it the same way as `<video>`, but Blink doesn't, and WebKit is doing something very weird too, which means that we get bug reports :)

Falling back to 0x0 is not so terrible as 300x150, so I _think_ I'd be ok with such thing (even if a bit unfortunate / inconsistent). The main question is whether we go with the 0x0 (and I change Firefox and add a test), or with the no size behavior (and I close the bug as invalid and add a test).

cc @jakearchibald too (since he filed #7524), and @Loirooriol since he was involved on some of those discussions.

<!-- comment id=2246259046 author=bfgeek created=2024-07-23T20:32:10Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2246259046 -->
## @bfgeek — 2024-07-23

I think I could get behind a `0x0` while loading in both cases. No intrinsic size (SVG-like) is dangerous as can be quite large in the degenerate case (similar argument against the default `300x150`).

<!-- comment id=2246333324 author=emilio created=2024-07-23T21:16:36Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2246333324 -->
## @emilio — 2024-07-23

@bfgeek 0x0 probably doesn't quite work for `<video>` tho, because of the common case described in the original issue of having an un-loaded video that you load on interaction or so.

<!-- comment id=2471660887 author=Loirooriol created=2024-11-12T21:53:52Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2471660887 -->
## @Loirooriol — 2024-11-12

So if I get it right, this is about

```html
<!DOCTYPE html>
<style>video, img { width: auto; height: auto; aspect-ratio: 1; border: solid; background: cyan }</style>
<div style="width: 200px">
  <video width="100" height="100"></video>
  <img width="100" height=100 src="https://software.hixie.ch/utilities/cgi/test-tools/delayed-file?pause=60">
</div>
```

In Servo we currently match Blink: the video is 300x150, and the image is 0x0 (but unlike Blink, we pass [the video test](http://wpt.live/html/rendering/replaced-elements/attributes-for-embedded-content-and-images/video-aspect-ratio.html)).

The difference is because we let image elements with no resource fall back to a 0x0 natural size. The video gets no natural size, so it uses a [default object size](https://drafts.csswg.org/css-images-3/#default-object-size) of 300x150.

<!-- comment id=2518152465 author=css-meeting-bot created=2024-12-04T17:55:42Z url=https://github.com/w3c/csswg-drafts/issues/10605#issuecomment-2518152465 -->
## @css-meeting-bot — 2024-12-04

The CSS Working Group just discussed `[css-sizing] Intrinsic size of <img> / <video> with aspect ratio but no definite size`.

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> emilio: This is about what happens when a video isn't loaded, and you specify width/height but override it with 'auto'<br>
&lt;fantasai> emilio: There's inconsistency here with mage.<br>
&lt;fantasai> emilio: very common to have an unloaded video<br>
&lt;fantasai> emilio: test in WPT that Blink still failing, but WK and GK pass<br>
&lt;fantasai> emilio: Not super well-defined. Should we improve it?<br>
&lt;joshtumath> s/mage/image<br>
&lt;emilio> https://wpt.fyi/results/html/rendering/replaced-elements/attributes-for-embedded-content-and-images/video-aspect-ratio.html?label=experimental&amp;label=master&amp;aligned is the wpt fwiw<br>
&lt;TabAtkins> Urgh, even reading over this a few times, I'm still not super sure what the behaviors are and what we want out of it. :/<br>
&lt;fantasai> astearns: Preference for what to define?<br>
&lt;fantasai> emilio: When I implemented this, I very explicitly thought about it, so would prefer to align on Gecko/WebKit behavior<br>
&lt;fantasai> emilio: It is how images behave, so makes sense<br>
&lt;fantasai> astearns: what is that behavior?<br>
&lt;oriol> q+<br>
&lt;fantasai> oriol: I think the test emilio points out that Blink is failing is not completely related<br>
&lt;astearns> ack oriol<br>
&lt;fantasai> oriol: in Servo, we match Blink in a bunch of cases<br>
&lt;fantasai> oriol: Problem emilio points out is you have a video and image element, both of which have width/ehight set ot auto<br>
&lt;fantasai> oriol: they behave different in Blink<br>
&lt;fantasai> oriol: we behave like Blink, but pass the Test<br>
&lt;fantasai> oriol: so there's room to do both things<br>
&lt;fantasai> oriol: in particular, difference in Servo between imgaes and video, if an img has no resource, we fall back to 0x0 natural size (not undefined), but video is undefined (no natural size) so get default object size (300x150)<br>
&lt;fantasai> emilio: given you have aspect ratio, you end up with a sensible behavior<br>
&lt;fantasai> emilio: given how common it has to have an unloaded video...<br>
&lt;fantasai> emilio: I think I need to think through this again, been a long time<br>
&lt;fantasai> emilio: I'll take it back to GH<br>
</details>
