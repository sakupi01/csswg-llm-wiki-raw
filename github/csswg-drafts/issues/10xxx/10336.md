---
number: 10336
title: "[css-cascade-6] reusable named scopes"
state: "open"
labels: ["css-cascade-6"]
author: "mayank99"
created_at: "2024-05-15T04:30:20Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/10336"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:06Z"
---

_(This was originally posted in https://github.com/w3c/csswg-drafts/issues/9742#issuecomment-2002084564 but moved to its own issue later.)_

Proposal:

A scope _statement_, similar to a layer statement, could be used to create a reusable named scope. For example:

```css
@scope foo (.start) to (.end);
```

A reusable named scope can then be used without specifying `<scope-start>`/`<scope-end>`:

```css
@scope foo {
  p { color: red; }
}
```

The main thing this enables is avoiding the need to repeat selectors for every use.

---

Open questions:
- how do we handle duplicate scope names?
- how do we differentiate between [named scopes](https://github.com/w3c/csswg-drafts/issues/9742) and reusable named scopes? (different syntax?)

<!-- comment id=2111676782 author=romainmenke created=2024-05-15T06:25:32Z url=https://github.com/w3c/csswg-drafts/issues/10336#issuecomment-2111676782 -->
## @romainmenke — 2024-05-15

For `@media` this same feature is currently specified as `@custom-media` : https://drafts.csswg.org/mediaqueries-5/#custom-mq

```css
@custom-media --modern (color), (hover);

@media (--modern) and (width > 1024px) {
  .a { color: green; }
}
```

This separates declaring a scope and using a scope.

<!-- comment id=2112409826 author=mayank99 created=2024-05-15T12:36:40Z url=https://github.com/w3c/csswg-drafts/issues/10336#issuecomment-2112409826 -->
## @mayank99 — 2024-05-15

@romainmenke good point, I wonder if there should be a common primitive for "defining" things. 

Example syntax (bikesheddable):

```css
@define(scope) --foo (.start) to (.end);
@define(media) --modern (color), (hover);
```

<!-- comment id=2112446181 author=romainmenke created=2024-05-15T12:55:27Z url=https://github.com/w3c/csswg-drafts/issues/10336#issuecomment-2112446181 -->
## @romainmenke — 2024-05-15

`@custom-*` can be the common primitive :)

A benefit of a dedicated at-rule is that things can be different without causing conflicts.

<!-- comment id=2669658318 author=mirisuzanne created=2025-02-19T20:11:53Z url=https://github.com/w3c/csswg-drafts/issues/10336#issuecomment-2669658318 -->
## @mirisuzanne — 2025-02-19

So the rough proposal would be something like: 

```css
@custom-scope --foo (.start) to (.end);

@scope --foo {
  /* styles scoped start to end */
}
```

What if I write a scope name and boundaries?

```css
@scope --foo (.start) to (.end) {
  /* styles scoped start to end */
}
```

I could imagine that as… 

- defining custom scopes on the fly
- concatenating parts of a scope definition (e.g. `@custom-scope --foo (.start); @scope --foo to (.end)`)
- getting the union of two scopes (though we would probably want a more defined and/or syntax for that)
- invalid
