---
number: 10070
title: "[css-view-transitions] 'type' argument that takes an array of types is very weird naming"
state: "closed"
labels: ["Needs Edits", "css-view-transitions-2"]
author: "fantasai"
created_at: "2024-03-13T16:13:34Z"
closed_at: "2024-03-25T13:23:00Z"
url: "https://github.com/w3c/csswg-drafts/issues/10070"
comments: 21
has_resolution: true
synced_at: "2026-07-04T04:44:47Z"
---

See https://www.w3.org/2024/03/13-css-minutes.html#x269

It's very weird to have a singular noun representing an array of such nouns.

<!-- comment id=1994920492 author=vmpstr created=2024-03-13T16:37:44Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1994920492 -->
## @vmpstr — 2024-03-13

FWIW this comes from https://github.com/w3c/csswg-drafts/commit/d6c50e9e8fff5636fde60438370e69dbb62d9475 I can't quite find the exact spot where we discussed this. The origin of a singular word here is that in CSS a list of idents is typically singular because of syntax (e.g. the only thing I can think of right now is `animation-name`), so we just matched javascript to that CSS name.

Should we have CSS `type: <custom-ident>+` and JS `sequence<DOMString>? types`? to be consistent with each domain

<!-- comment id=1995165143 author=noamr created=2024-03-13T17:49:48Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1995165143 -->
## @noamr — 2024-03-13

The discussion about this was in TPAC 2023.
One solution to this is to not have a special `startViewTransition` signature at all, and instead:
```js
const vt = document.startViewTransition(updateCallback);
vt.typeList.add(...theTypesIWantedToAdd);
```

<!-- comment id=1997005508 author=bramus created=2024-03-14T09:25:20Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997005508 -->
## @bramus — 2024-03-14

> (e.g. the only thing I can think of right now is `animation-name`)

A counterexample is `font-family` which you set to a list of font names. The property could as well be named `font-name` but instead it use the `family` suffix.

Rewinding a little bit back, I raised this concern because there were a bunch of different names for it, which seems a bit weird: `types`, `type`, and `typeList` (the `types` one turning out to be a typo in the spec).

I could see Noam’s suggestion above work, but then that would not solve the descriptor in the `@view-transition` rule, which still is `type` _(at this moment)_.

Moving forward, I think the most consistent way to talk about this would be to use the term _“Type List”_ across the board. In CSS that would become `type-list` (in the `@view-transition` rule) and in JS `typeList` (when passing it into `startViewTransition()`, when reading it from a `ViewTransition` object, and when reading it from `CSSViewTransitionRule`).

So, proposal: Use the term “Type List” across the board. That is, `type-list` in a CSS context and `typeList` in a JS context.

<!-- comment id=1997011866 author=noamr created=2024-03-14T09:28:49Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997011866 -->
## @noamr — 2024-03-14

> > (e.g. the only thing I can think of right now is `animation-name`)
> 
> A counterexample is `font-family` which you set to a list of font names. The property could as well be named `font-name` but instead it use the `family` suffix.
> 
> Rewinding a little bit back, I raised this concern because there were a bunch of different names for it, which seems a bit weird: `types`, `type`, and `typeList` (the `types` one turning out to be a typo in the spec).
> 
> I could see Noam’s suggestion above work, but then that would not solve the descriptor in the `@view-transition` rule, which still is `type` _(at this moment)_.

That one is a list of idents though. It's equivalent to `class`, it's not a JS array. So I think `type` is fine for the descriptor.

> 
> Moving forward, I think the most consistent way to talk about this would be to use the term _“Type List”_ across the board. In CSS that would become `type-list` (in the `@view-transition` rule) and in JS `typeList` (when passing it into `startViewTransition()`, when reading it from a `ViewTransition` object, and when reading it from `CSSViewTransitionRule`).

Is this consistent with anything existing in CSS atm? I was using the `*List` suffix only for a `DOMTokenList` which is consistent with `classList`.

<!-- comment id=1997376871 author=noamr created=2024-03-14T12:45:28Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997376871 -->
## @noamr — 2024-03-14

@bramus also the new `view-transition-class` takes a list of idents and doesn't use the word "list".

<!-- comment id=1997836510 author=khushalsagar created=2024-03-14T16:20:05Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997836510 -->
## @khushalsagar — 2024-03-14

> That one is a list of idents though. It's equivalent to class, it's not a JS array. So I think type is fine for the descriptor.

The `class` vs `classList` is a good counter for why `type` in CSS is reasonable. But it feels like authors will have an easier time if its the same name everywhere:

1. [at-rule](https://drafts.csswg.org/css-view-transitions-2/#view-transition-type-descriptor)
2. [CSSViewTransitionRule](https://drafts.csswg.org/css-view-transitions-2/#navgation-behavior-rule-interface)
3. [startViewTransition](https://drafts.csswg.org/css-view-transitions-2/#dictdef-startviewtransitionoptions)
4. ViewTransition object

2 and 4 are already using `typeList`. So could startViewTransition and the at-rule use `typeList` as well. We have to do `typeList` for CSSViewTransitionRule because of https://github.com/w3c/csswg-drafts/issues/9905.

<!-- comment id=1998251585 author=noamr created=2024-03-14T19:23:41Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1998251585 -->
## @noamr — 2024-03-14

> > That one is a list of idents though. It's equivalent to class, it's not a JS array. So I think type is fine for the descriptor.
> 
> The `class` vs `classList` is a good counter for why `type` in CSS is reasonable. But it feels like authors will have an easier time if its the same name everywhere:
> 
> 1. [at-rule](https://drafts.csswg.org/css-view-transitions-2/#view-transition-type-descriptor)
> 2. [CSSViewTransitionRule](https://drafts.csswg.org/css-view-transitions-2/#navgation-behavior-rule-interface)
> 3. [startViewTransition](https://drafts.csswg.org/css-view-transitions-2/#dictdef-startviewtransitionoptions)
> 4. ViewTransition object
> 
> 2. and 4) are already using `typeList`. So could startViewTransition and the at-rule use `typeList` as well. We have to do `typeList` for CSSViewTransitionRule because of [[css-view-transitions-2] `CSSViewTransitionRule.type` overrides deprecated `CSSRule.type` #9905](https://github.com/w3c/csswg-drafts/issues/9905).

I think if we do away with (3) (https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1995165143), having it `type` where we can is equivalent with existing things (class, view-transition-class, font-family, part...). We use `typeList` only because the word `type` is overused and sometimes conflicting (e.g. in `CSSViewTransitionRule`).

So I think using `type` in CSS but `typeList` in JS when it's a `DOMTokenList` is consistent enough with current things. But I'm happy to resolve on whatever we reach consensus around.

<!-- comment id=1998519796 author=khushalsagar created=2024-03-14T21:33:46Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1998519796 -->
## @khushalsagar — 2024-03-14

Thanks for clarifying Noam. So sounds like we at least have consensus on renaming the `type` param in `startViewTransition` to `typeList` for consistency across all javascript APIs. Let me Agenda+ for that.

I have a stronger preference for `type-list` for the at-rule than `type` for the same arguments that were made on https://github.com/w3c/csswg-drafts/issues/9972. Makes it more obvious that `typeList` on the ViewTransition object is initialized using the descriptor in the CSS at-rule.

@nt1m @fantasai what's your preference?

<!-- comment id=1998537578 author=nt1m created=2024-03-14T21:48:10Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1998537578 -->
## @nt1m — 2024-03-14

I tend towards `types`, there is precedent for using plural in descriptors (e.g. @counter-style has a few)

<!-- comment id=1998556711 author=khushalsagar created=2024-03-14T22:03:52Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1998556711 -->
## @khushalsagar — 2024-03-14

^ Is there a reason to favour `types` over `type-list`, even though the latter maps directly to the script references to types.

<!-- comment id=1999138223 author=bramus created=2024-03-15T08:13:59Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999138223 -->
## @bramus — 2024-03-15

> I tend towards `types`, there is precedent for using plural in descriptors (e.g. @counter-style has a few)

As this point I was gonna suggest `types` as well. It describes what you are defining _(hinting at possibility to add more than one)_, it’s vague enough to be correct in both CSS and JS land, and it makes it easy for the author to use: the thing is called `types` _(independent of the context)_.

As you mention `@counter-style` has precedent for having a descriptor that is plural. Another one is [`@font-palette-values`](https://drafts.csswg.org/css-fonts/#om-fontpalettevalues)’s [`override-colors`](https://drafts.csswg.org/css-fonts/#override-color)

<!-- comment id=1999158754 author=noamr created=2024-03-15T08:28:30Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999158754 -->
## @noamr — 2024-03-15

> > I tend towards `types`, there is precedent for using plural in descriptors (e.g. @counter-style has a few)
> 
> As this point I was gonna suggest `types` as well. It describes what you are defining _(hinting at possibility to add more than one)_, it’s vague enough to be correct in both CSS and JS land, and it makes it easy for the author to use: the thing is called `types` _(independent of the context)_.
> 
> As you mention `@counter-style` has precedent for having a descriptor that is plural. Another one is [`@font-palette-values`](https://drafts.csswg.org/css-fonts/#om-fontpalettevalues)’s [`override-colors`](https://drafts.csswg.org/css-fonts/#override-color)

I'm ok with this, we went with `type` because of a notion in the TPAC conversation that CSS doesn't use plurals. (I believe @tabatkins & @astearns raised this?)

<!-- comment id=2000221035 author=khushalsagar created=2024-03-15T18:24:46Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2000221035 -->
## @khushalsagar — 2024-03-15

So *proposed resolutions* based on the comments so far is:

- Rename the `type` param in `startViewTransition` to `typeList`.
- Rename `type` descriptor in the css at-rule to `types`.

<!-- comment id=2000358181 author=vmpstr created=2024-03-15T19:52:53Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2000358181 -->
## @vmpstr — 2024-03-15

Replying to different people:

> A counterexample is `font-family` which you set to a list of font names. The property could as well be named `font-name` but instead it use the `family` suffix.

I don't believe this is a counterexample, since this is a list of font families (each item is a font family and there is potentially more than one). 

> So I think using `type` in CSS but `typeList` in JS when it's a `DOMTokenList` is consistent enough with current things. But I'm happy to resolve on whatever we reach consensus around.

That's my preference as well, since I believe the majority of the use cases are likely to specify a single type. Although there is precedence in plurality, it feels it would be more consistent with singular names. However, I'm happy with whatever CSSWG agrees on at this point

<!-- comment id=2000391201 author=nt1m created=2024-03-15T20:23:11Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2000391201 -->
## @nt1m — 2024-03-15

> So _proposed resolutions_ based on the comments so far is:
> 
>     * Rename the `type` param in `startViewTransition` to `typeList`.
> 
>     * Rename `type` descriptor in the css at-rule to `types`.

Why not use `types` for both the JS & CSS APIs?

<!-- comment id=2000399726 author=khushalsagar created=2024-03-15T20:30:11Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2000399726 -->
## @khushalsagar — 2024-03-15

^ That was resolved here: https://github.com/w3c/csswg-drafts/issues/9905#issuecomment-1969544713.

<!-- comment id=2003441304 author=bramus created=2024-03-18T10:08:41Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2003441304 -->
## @bramus — 2024-03-18

That resolution was to rename `type` to `typeList` for the CSSOM, as [`type` already exists there on `CSSRule` (and is deprecated by now)](https://drafts.csswg.org/cssom/#legacy-css-style-sheet-members:~:text=The%20type%20attribute%20is%20deprecated).

Tim’s suggesting to rename the descriptor and parameter to simply `types`, something [I’m also OK with](https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999138223).

As noam [mentioned](https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999158754), `type` was chosen at TPAC in Sept 2023 but I currently can’t seem to find back the minutes regarding this.

<!-- comment id=2003466979 author=noamr created=2024-03-18T10:16:37Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2003466979 -->
## @noamr — 2024-03-18

> That resolution was to rename `type` to `typeList` for the CSSOM, as [`type` already exists there on `CSSRule` (and is deprecated by now)](https://drafts.csswg.org/cssom/#legacy-css-style-sheet-members:~:text=The%20type%20attribute%20is%20deprecated).
> 
> Tim’s suggesting to rename the descriptor and parameter to simply `types`, something [I’m also OK with](https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999138223).
> 
> As noam [mentioned](https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1999158754), `type` was chosen at TPAC in Sept 2023 but I currently can’t seem to find back the minutes regarding this.

OK, actually in TPAC the resolution was that this argument name was TBD: https://github.com/w3c/csswg-drafts/issues/8960#issuecomment-1719695337

The relevant resolution is this:
https://github.com/w3c/csswg-drafts/issues/9534#issuecomment-1802364085

<!-- comment id=2009980134 author=css-meeting-bot created=2024-03-20T16:22:27Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2009980134 resolution=true -->
## @css-meeting-bot — 2024-03-20

The CSS Working Group just discussed `[css-view-transitions] 'type' argument that takes an array of types is very weird naming`, and agreed to the following:

* `RESOLVED: Switch to using "types" in all the locations.`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> vmpstr: We have an attribute called 'type' in the opt-in block<br>
&lt;fantasai> vmpstr: the corresponding argument is 'type', which takes an array<br>
&lt;fantasai> vmpstr: issue raised that 'type' refers to an array of items<br>
&lt;fantasai> vmpstr: so bikeshedding issue to figure out what to call these<br>
&lt;fantasai> vmpstr: proposals include 'types' everywhere or using 'typeList'<br>
&lt;fantasai> vmpstr: interested in opinions<br>
&lt;khush_> q+<br>
&lt;ntim> q-<br>
&lt;astearns> ack khush_<br>
&lt;fantasai> khush_: I was suggesting to just use 'type-list'<br>
&lt;fantasai> khush_: in a different place we resolved on this for CSS transition rule<br>
&lt;bramus> q+<br>
&lt;fantasai> khush_: several places where this is provided, should be consistent<br>
&lt;astearns> ack bramus<br>
&lt;fantasai> khush_: since we said 'typeList' for CSSViewTransitionRule, let's use 'typeList' everywhere<br>
&lt;fantasai> 'type-list'/'typeList'<br>
&lt;fantasai> bramus: it was inspired by being a DOMTokenList, but no longer a DOMTokenList<br>
&lt;ntim> +1 to bramus<br>
&lt;fantasai> bramus: I'm a fan of using 'types', it works in both CSS and JS<br>
&lt;vmpstr> q+<br>
&lt;astearns> ack vmpstr<br>
&lt;fantasai> vmpstr: Wrt DOMTokenList, it wouldn't be in CSS object but in DOM it might be<br>
&lt;fantasai> vmpstr: OK with renaming, but wanted to clarify<br>
&lt;bramus> scribe+<br>
&lt;astearns> ack fantasai<br>
&lt;TabAtkins> fantasai: I'm also a fan of consistency<br>
&lt;TabAtkins> fantasai: I unfortuantely don't have much familiarity with the OM in general<br>
&lt;TabAtkins> fantasai: But I remember going with typeList for consistency with classList, but it's not a list in HTML, it's a space-separated list of classes<br>
&lt;TabAtkins> fantasai: I think consistnecy acorss objects is important, but it shoudl be clear that if something is an Array it's an Array, not a string<br>
&lt;TabAtkins> fantasai: We don't use much plurals in CSS syntax, it's unusual<br>
&lt;TabAtkins> bramus: There's some precedent in descriptors<br>
&lt;TabAtkins> bramus: override-colors, and symbols<br>
&lt;TabAtkins> fantasai: For those you're expected to give multiple values, you really wouldn't give just one<br>
&lt;TabAtkins> fantasai: While for this, in many cases it's a single value<br>
&lt;khush_> https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997836510<br>
&lt;TabAtkins> fantasai: So what are the options?<br>
&lt;TabAtkins> fantasai: CSSViewTransition at-rule descriptor<br>
&lt;TabAtkins> fantasai: and the corresponding CSSViewTransitionRule object<br>
&lt;astearns> (this is in the comment khush_ linked above)<br>
&lt;khush_> There is a summary here: https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-1997836510<br>
&lt;TabAtkins> vmpstr: startViewTransition() parameter<br>
&lt;TabAtkins> vmpstr: And last is ViewTransition object returned by that<br>
&lt;TabAtkins> fantasai: I have a question, for CSSViewTransitionRule<br>
&lt;TabAtkins> fantasai: We have something that represetns the types as an array<br>
&lt;TabAtkins> fantasai: Do we also have that as a string? Or just as an array<br>
&lt;TabAtkins> vmpstr: We only ahve an array<br>
&lt;TabAtkins> (we don't have a consistent pattern in the OM for this)<br>
&lt;TabAtkins> fantasai: Is that normal for OM representations of a rule?<br>
&lt;TabAtkins> TabAtkins: It's not unusual.<br>
&lt;TabAtkins> khush_: the other question is, for CSSVTRule, what type should that have? Currently DOMTokenList, but could be a string or an array of strings<br>
&lt;TabAtkins> vmpstr: And one limitation, the OM one can't be called "type", becuase that already exists. And I think that's all the restrictions.<br>
&lt;TabAtkins> vmpstr: I propose just "types" everywhere, we can revisit if someone doesn't like it<br>
&lt;TabAtkins> +1 to this, personally<br>
&lt;bramus> +1<br>
&lt;ntim> I like types everywhere<br>
&lt;khush_> +1<br>
&lt;ntim> There's className too if you look at class :D<br>
&lt;TabAtkins> astearns: proposed to use "types" everywhere. Concerns?<br>
&lt;TabAtkins> RESOLVED: Switch to using "types" in all the locations.<br>
&lt;astearns> https://github.com/w3c/csswg-drafts/issues/10101<br>
</details>

<!-- comment id=2016587375 author=cdoublev created=2024-03-23T19:52:21Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2016587375 -->
## @cdoublev — 2024-03-23

The change is missing here. 😄

https://github.com/w3c/csswg-drafts/blob/dbdd87d907d9e7b6334aa52b6b589f6ce3b28771/css-view-transitions-2/Overview.bs#L531

<!-- comment id=2017524986 author=noamr created=2024-03-25T09:10:32Z url=https://github.com/w3c/csswg-drafts/issues/10070#issuecomment-2017524986 -->
## @noamr — 2024-03-25

> The change is missing here. 😄
> 
> https://github.com/w3c/csswg-drafts/blob/dbdd87d907d9e7b6334aa52b6b589f6ce3b28771/css-view-transitions-2/Overview.bs#L531

Oops, thanks for catching that, will follow up.
