---
number: 10989
title: "[css-values-5] Resolve `first-valid()` at parse time"
state: "open"
labels: ["css-values-5"]
author: "cdoublev"
created_at: "2024-10-02T07:59:48Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/10989"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:04Z"
---

The current [definition](https://drafts.csswg.org/css-values-5/#first-valid) says:

  > If none of the arguments represent a valid value for the property, the property is invalid at computed-value time.

But in the discussion before the [resolution](https://github.com/w3c/csswg-drafts/issues/5055#issuecomment-1022425917) to add `first-valid()`:

  > > This would go away at parse time?
  >
  > Correct.

And in a comment before:

  > > When does it resolve?
  >
  > Parse time. [...]
  >
  > > What if all values are invalid?
  > >
  > > [...]
  > >
  > > `.foo { display: grid; display: first-supported(foo; bar); }`
  > >
  > > If it's resolved at parse time in standard properties, and all specified values are invalid, is the declaration dropped, producing `display: grid`? Or `display` becomes invalid at computed-value time?
  >
  > Parse failure.

This discussion/comment happened before the commit adding `first-valid()`, so forgive me if I am missing other discussions and if the current text is intentional.

If not, does "go away at parse time" mean it should serialize with the first valid value?

When validating at parse time, the value for a "manual" fallback, for UAs that do not support `first-valid()`, is not required as an argument of `first-valid()`:

```css
div {
  /* validation at computed value time */
  color: red; /* for UAs that do not support first-valid() */
  color: first-valid(unsupported-color-name, red);
  /* validation at parse time */
  color: red; /* for UAs that do not support first-valid() */
  color: first-valid(unsupported-color-name);
}
```

Also, I notice that when validated at parse time, it can still be evaluated at computed value time using a custom property, assuming `<whole-value>`s are parsed after the (custom) property value range:

```css
div {
  --color: first-valid(unsupported-color-name, red);
  color: var(--color);
}
```

<sup>edit: added the note about using custom property.</sup>

<!-- comment id=2628784463 author=cdoublev created=2025-02-01T05:25:28Z url=https://github.com/w3c/csswg-drafts/issues/10989#issuecomment-2628784463 -->
## @cdoublev — 2025-02-01

Comments in #11136 clarified that `first-valid()` is validated at parse time when it is not provided a substitution function that resolve after parsing.

I leave this issue open, assuming the spec needs to be clarified a bit. 

(**edit:** never mind, the example can only be invalid) ~~Meanwhile, should `first-valid()` be parsed before trying to parse the property value range? For example, should `--custom: first-valid(url(bad .url))` be valid? If so, what should be its specified value when serialized? That is, when it is not provided a substitution function resolved after parsing, should `first-valid()` be substituted with its first valid argument?~~
