---
number: 13853
title: "[web-animations] How/when do the \"rangeStart/rangeEnd\" attributes the Animation interface get evaluated"
state: "open"
labels: ["Needs Design / Proposal", "Needs Edits", "Needs Testcase (WPT)", "web-animations-2"]
author: "weinig"
created_at: "2026-04-28T19:15:03Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13853"
comments: 18
has_resolution: true
synced_at: "2026-08-06T00:47:06Z"
---

How/when do the `<length-percentage>` values that can be passed to the [rangeStart](https://drafts.csswg.org/web-animations-2/#dom-animation-rangestart)/[rangeEnd](https://drafts.csswg.org/web-animations-2/#dom-animation-rangeend) attributes on the Animation interface (or via [KeyframeAnimationOption](https://drafts.csswg.org/web-animations-2/#dom-keyframeanimationoptions-rangestart)) when assigning a DOMString, get resolved.

Specifically, the KeyframeAnimationOption section for `rangeEnd` says "A [DOMString](https://webidl.spec.whatwg.org/#idl-DOMString) value is parsed as an [animation-range-end](https://www.w3.org/TR/scroll-animations-1/#propdef-animation-range-end) value to yield a [TimelineRangeOffset](https://drafts.csswg.org/web-animations-2/#dictdef-timelinerangeoffset)". `animation-range-end` is defined as `[ normal | <length-percentage> | <timeline-range-name> <length-percentage>? ]#`, so, `<length-percentage>` values will have to resolved.

Given all that, how/when should a string like `1em` be resolved? 

<!-- comment id=4338915422 author=ydaniv created=2026-04-28T20:43:58Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4338915422 -->
## @ydaniv — 2026-04-28

I suppose you have to have an active timeline in order to resolve percentages.
So I assume whenever _both_ of the following two apply:

1. The property `range*` is set.
2. The `Animation.timeline` becomes _active_

Meaning, when a timeline becomes active, any set `range*` property is resolved.
Or, when a `range*` property is set, if the timeline of the associated animation is active, that property is resolved.

<!-- comment id=4339711739 author=weinig created=2026-04-28T23:03:30Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4339711739 -->
## @weinig — 2026-04-28

This is not meant to be an issue about percentages, which seem to be well defined to happen at use time like animation-range-start/animation-range-end, but rather about things that get resolved normally for computed style, like font relative units (the em example) or sibling-index(). 

<!-- comment id=4351571423 author=ydaniv created=2026-04-30T10:11:31Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4351571423 -->
## @ydaniv — 2026-04-30

OK, but as I understood it you're specifically asking about the imperative interface, right? So can there be a different time to resolve these, other then what I suggested?

<!-- comment id=4356939222 author=weinig created=2026-04-30T23:52:49Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4356939222 -->
## @weinig — 2026-04-30

I don’t understand exactly what you are proposing/stating?

Are you saying the spec already defines this clearly and I just missed it? If so, can you point me to the spec text that states it?

Or are you proposing how it should be specified? If so, it seems to be missing important details. Going back to my example, how are the font relative units resolved? 

The only other place I can think of that tries to use parsed CSS property values outside of normal CSS usage is DOMMatrix (https://drafts.csswg.org/geometry/#dommatrix-parse) which clarifies that only absolute lengths are supported. 

I think at least that level of detail is needed here. 

<!-- comment id=4363828575 author=ydaniv created=2026-05-02T12:58:36Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4363828575 -->
## @ydaniv — 2026-05-02

> Are you saying the spec already defines this clearly and I just missed it? If so, can you point me to the spec text that states it?
>
> Or are you proposing how it should be specified? If so, it seems to be missing important details. Going back to my example, how are the font relative units resolved?

Since you opened the issue I'm assuming something is probably missing. I'm simply trying to understand what are the options for the resolution time. But I see now we're also concerned with "how" the resolution works.

Well, the `range*` properties belong to an animation, which has an associated effect, and effect has a `target` Element. It's not abstract like a `DOMMatrix`. Shouldn't that solve the "how"?

<!-- comment id=4413741580 author=weinig created=2026-05-09T21:45:18Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4413741580 -->
## @weinig — 2026-05-09

> Well, the range* properties belong to an animation, which has an associated effect, and effect has a target Element. It's not abstract like a DOMMatrix. Shouldn't that solve the "how"?

I don't think an instance of `Animation` always has an `effect`, (the `effect` is optional in the constructor) and similarly, an `effect` doesn't always have a `target` element (the constructor for `KeyframeEffect` allows passing in a null `target`).

It's plausible that `rangeStart` and `rangeEnd` are never utilized if there is no `effect` and/or `target`, but that is not totally clear to me.

But even if we assume that effect's target is available at times where resolving `rangeStart` and `rangeEnd` happens, it's not clear how that should work. 

I think new hooks are needed in one of the base CSS specs (CSS Syntax or CSS Cascade maybe) to specify what it means to parse and resolve a CSS property outside the normal cascade.

<!-- comment id=4419826902 author=ydaniv created=2026-05-11T10:35:52Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4419826902 -->
## @ydaniv — 2026-05-11

> But even if we assume that effect's target is available at times where resolving rangeStart and rangeEnd happens, it's not clear how that should work.

Sorry, I may be out of my element here (none intended), can you explain why when you have a target it's still not clear how to resolve values?

> I think new hooks are needed in one of the base CSS specs (CSS Syntax or CSS Cascade maybe) to specify what it means to parse and resolve a CSS property outside the normal cascade.

Does "outside of the normal cascade" come from setting the `range*` directly on the animation and not coming from style? So the issue is it's not clear how to these values cascade?

<!-- comment id=4426751913 author=weinig created=2026-05-12T02:05:46Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4426751913 -->
## @weinig — 2026-05-12

> > But even if we assume that effect's target is available at times where resolving rangeStart and rangeEnd happens, it's not clear how that should work.
> 
> Sorry, I may be out of my element here (none intended), can you explain why when you have a target it's still not clear how to resolve values?

Heh.

What I mean is that there is no specification text that specifies how to take a string, the grammar for a CSS property, and an Element, and produce a TimelineRangeOffset. 

> 
> > I think new hooks are needed in one of the base CSS specs (CSS Syntax or CSS Cascade maybe) to specify what it means to parse and resolve a CSS property outside the normal cascade.
> 
> Does "outside of the normal cascade" come from setting the `range*` directly on the animation and not coming from style? 

Yes.

> So the issue is it's not clear how to these values cascade?

No. I only mention "outside of the normal cascade", because that is the specified way we know how to resolve things using CSS property grammars. That is, it is well defined how a CSS property in a style rule gets resolved. I can point to spec text that explains how values are resolved, e.g https://www.w3.org/TR/css-cascade-4/#value-stages.

<!-- comment id=4614353699 author=css-meeting-bot created=2026-06-03T16:02:51Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4614353699 -->
## @css-meeting-bot — 2026-06-03

The CSS Working Group just discussed `[web-animations] How/when do the "rangeStart/rangeEnd" attributes the Animation interface get evaluated`.

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> weinig: there's an ability to set the range start and end attributes thru the IDL, but it's unclear how, especially in the case you pass a string and it's parsed to CSS, how and when that's resolved to a px value<br>
&lt;TabAtkins> weinig: just no text explaining it<br>
&lt;TabAtkins> weinig: so qs about relative units, calc(), etc. and how does one actually construct the timeline-range offset, given an arbitrary css numeric value?<br>
&lt;emilio> q+<br>
&lt;TabAtkins> astearns: suggestions?<br>
&lt;TabAtkins> weinig: i don't really understand the use-case for supporting arbitrary css here<br>
&lt;TabAtkins> weinig: so my proposal would be to remove that, and make it constrained to a simpler data type<br>
&lt;ydaniv> q+<br>
&lt;TabAtkins> weinig: the processing model is a little opaque to me, unclear there is always a subject ot resolve these values against<br>
&lt;TabAtkins> flackr: i can answer intent, tho there's a q<br>
&lt;TabAtkins> flackr: intent was simplificity of letting authors say the range start is Xpx, or Xvh<br>
&lt;TabAtkins> flackr: to parse but not necessarily resolve a specific px value. if you read it back we can return an object that represents those lengths, even calcs<br>
&lt;astearns> ack emilio<br>
&lt;TabAtkins> emilio: tend to agree with Sam, there's other weirdness<br>
&lt;TabAtkins> emilio: you want to keep as specified, but that implies... what about sibling-index()? now the range start/end changes with dom shape<br>
&lt;TabAtkins> emilio: argument for matching IO margins, constrained to px and %<br>
&lt;TabAtkins> emilio: it simplifies a lot of this stuff, but still covers most use-cases<br>
&lt;kizu> q+<br>
&lt;TabAtkins> weinig: other option is MQ route, uses the global context always<br>
&lt;TabAtkins> fantasai: i think that would be confusing<br>
&lt;TabAtkins> flackr: intent is things like sibling-index() should be an error...<br>
&lt;TabAtkins> emilio: that conflicts with leaving the specified style...<br>
&lt;TabAtkins> emilio: instead of inventing something new, can we say we match IO?<br>
&lt;TabAtkins> flackr: so if you want a clac() you ahve to do it yourself?<br>
&lt;TabAtkins> weinig: i think calc() is fine if you use the right units...<br>
&lt;TabAtkins> emilio: i think IO doesn't allow calc() right now<br>
&lt;TabAtkins> weinig: i'm more familiar with DOMMatrix<br>
&lt;TabAtkins> emilio: oh a third pattern, fair<br>
&lt;TabAtkins> weinig: ideally a spec like webanim shouldn't have to know much about this, we shoudl ahve a hook lower in the stack for getting a length value<br>
&lt;TabAtkins> weinig: seems to be coming up more and more<br>
&lt;TabAtkins> emilio: yeah, as we add more fancy features - what does random() mean, etc<br>
&lt;astearns> ack ydaniv<br>
&lt;TabAtkins> emilio: so yes, think speciyfing rangeStart/End same as DOMMatrix seems fine. hopefully it's sane, and we can extract that definition to CSSOM or something<br>
&lt;TabAtkins> ydaniv: a bit confused<br>
&lt;TabAtkins> ydaniv: as an author i'd expect rangeStart/End to behave same as range-start in CSS<br>
&lt;TabAtkins> ydaniv: are we saying we should limit those values?<br>
&lt;TabAtkins> weinig: in CSS there's a lot fo context - it has a subject element, a parent, a root. none of those have here<br>
&lt;dshin-moz> Maybe the value should be computationally independent (Definition from registered custom properties)?<br>
&lt;TabAtkins> emilio: yeah you could obtain that context, but you have to define exactly when it happens<br>
&lt;TabAtkins> weinig: we have a keyframeeffect and a target, that shoudl then be well-defined, yeah?<br>
&lt;TabAtkins> weinig: if someone writes out all those definitions, yeah<br>
&lt;TabAtkins> ydaniv: there are use-cases for arbitrary values in ranges, like calc()/etc. we don't want to lose it<br>
&lt;TabAtkins> emilio: calc() would be fine...<br>
&lt;TabAtkins> ydaniv: and variables<br>
&lt;TabAtkins> kizu: mostly noting same as ydaniv, calc() is useful, using ems and vw etc are useful. this might just be when talkinga bout JS apis, this is a common thing where we ahve APIs that accept some CSS but they're not evaluated in the context of anything<br>
&lt;TabAtkins> kizu: would be great if all these APIs had a way to give that context<br>
&lt;TabAtkins> kizu: would solve it for all these cases at once, could have a fallback of some sort when context isn't there<br>
&lt;TabAtkins> kizu: i think i'd expect if there was no context, em would evaluate at the root level<br>
&lt;TabAtkins> astearns: would it make sense to put more strict guideliens on the api for now, and work on a more general context-resolution mechanism for all of these things?<br>
&lt;TabAtkins> astearns: i wonder if it makes any sense to come up with a definition for evaluating these just for rangeStart/End when other bits of the JS API also don't work<br>
&lt;TabAtkins> flackr: part of the problem is you can immediately read the rangeStart/End as a CSSNumericValue, so it's a bit of a lie to say we can take arbitrary CSS without a context<br>
&lt;TabAtkins> flackr: so i'm in favor of saying for now it's restricted - it does support calc(), to point out, that's a CSSNumericValue<br>
&lt;TabAtkins> flackr: but beyond that woudl be an erroneous value<br>
&lt;TabAtkins> flackr: can look at adding some API for resolving this generically<br>
&lt;TabAtkins> (I always intended to have a way to resolve an arbitrary value against an element, to get a simplified-down CSSNumericValue, yes)<br>
&lt;TabAtkins> kizu: so just verifying this only affects the JS api, yeah? not css?<br>
&lt;TabAtkins> flackr: yes<br>
&lt;TabAtkins> ydaniv: yeah, confirmed that vars are already borken in chrome. would like it be fixed in the future.<br>
&lt;TabAtkins> astearns: proposed resolution, rangeStart/End will follow the conventions in DOMMatrix to only allow px/%/calc/what-have-you<br>
&lt;TabAtkins> weinig: both to the domstring input and the cssnumericvalue input, the latter can also have relative units<br>
&lt;ydaniv> s/it be fixed/to see WAAPI merged with CSS behavior/<br>
&lt;TabAtkins> weinig: the Typed OM value<br>
&lt;TabAtkins> (aka CSS.em(5))<br>
&lt;TabAtkins> flackr: can cssnumericvalue take sibling-index?<br>
&lt;TabAtkins> emilio: no, but can take em or vh<br>
&lt;TabAtkins> flackr: but those don't need to reoslve to a px value<br>
&lt;TabAtkins> emilio: at some point it does, es<br>
&lt;ydaniv> What about CQ units?<br>
&lt;TabAtkins> flackr: but the JS API returns a numeric value, not a resolved px<br>
&lt;TabAtkins> emilio: but then you still need to define when it's resolved.<br>
&lt;TabAtkins> yeah it's setting "5em" or `CSS.em(5)`<br>
&lt;TabAtkins> weinig: sounds like we need to bring this back, we're missing some clarity. wasn't clear it also applied to relative units<br>
&lt;TabAtkins> flackr: yeah, i thought it was just things that CSSNumericValue couldn't represent. I dont' think we need to go further<br>
&lt;TabAtkins> astearns: okay back to the issue, let's handle this Soon<br>
</details>

<!-- comment id=4820632977 author=weinig created=2026-06-27T19:20:49Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4820632977 -->
## @weinig — 2026-06-27

My proposal is to restrict allowed values from the `TimelineRangeOffset`, `CSSNumericValue` or `DOMString` input to absolute units as was done with DOMMatrix and IntersectionObservers.

<!-- comment id=4820636728 author=weinig created=2026-06-27T19:21:30Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4820636728 -->
## @weinig — 2026-06-27

@astearns now that I have a concrete proposal, what is the next step?

<!-- comment id=4847264556 author=ydaniv created=2026-06-30T19:34:20Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4847264556 -->
## @ydaniv — 2026-06-30

What does absolute units mean in this context? Only `px`? Are these excluding `%`s? This practically renders the feature quite unusable.
Anything we can do to specify the evaluation better? (which is also the original question in this issue)

<!-- comment id=4847863920 author=weinig created=2026-06-30T20:59:02Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4847863920 -->
## @weinig — 2026-06-30

It would be this definition of absolute length units, https://drafts.csswg.org/css-values-4/#absolute-lengths. 

Percentages are unaffected and therefore allowed. 

<!-- comment id=4847921983 author=weinig created=2026-06-30T21:07:25Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4847921983 -->
## @weinig — 2026-06-30

As to what could be done to specify evaluation, a processing model would need to be defined, specifying things like 

- what element to use for evaluation
- when that evaluation happens (doing it immediately has downsides like requiring a synchronous style resolution)
- what invalidation, if any, happens when the element changes

This could always be added later, specing the more conservative option does not rule it out for the future. 

<!-- comment id=4851685932 author=emilio created=2026-07-01T07:51:04Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4851685932 -->
## @emilio — 2026-07-01

Strong +1 to Sam's proposal. For now it seems like the most obvious thing to do. Someone needs to write a coherent processing model for how things should behave in order for them to support element-dependent things.

<!-- comment id=4857950246 author=css-meeting-bot created=2026-07-01T16:52:53Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-4857950246 resolution=true -->
## @css-meeting-bot — 2026-07-01

The CSS Working Group just discussed `[web-animations] How/when do the "rangeStart/rangeEnd" attributes the Animation interface get evaluated`, and agreed to the following:

* `RESOLVED: make it a parse error to set string values or typed CSSOM values that require an element to resolve`

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> weinig: issue is that in the animations API in the JS API, there are places where you can set values<br>
&lt;JoshT> ...the setters have been defined to take strings<br>
&lt;JoshT> ... but the spec says 'parse this as...' and then a CSS property<br>
&lt;JoshT> ... in this case, the issue with range-start is that it takes what it should parse, and animation-range-end allows a length-percentage with any number<br>
&lt;JoshT> ... you can have length relative units<br>
&lt;JoshT> ... there's no spec on how to resolve those units<br>
&lt;JoshT> ... it's not happening during the style resolution<br>
&lt;JoshT> ... in other places, there's an API that takes a string and parses it as a CSS construct<br>
&lt;JoshT> ... normally we limit it to absolute units because we don't have a way to resolve relative units<br>
&lt;JoshT> ... in this bug it's about range-end, but we can take the same idea to other cases and limit to absolute length units and %. % are ok because they resolve later<br>
&lt;TabAtkins> I'm fine with this<br>
&lt;fantasai> sgtm<br>
&lt;JoshT> astearns: is it possible to define some way to resolve %?<br>
&lt;JoshT> weinig: % are not an issue. it's just relative units<br>
&lt;JoshT> astearns: so can we just pick something to resolve against like the root?<br>
&lt;JoshT> weinig: there are other options we could takw<br>
&lt;TabAtkins> q+<br>
&lt;JoshT> ... one is to treat it like media queries<br>
&lt;JoshT> ... say that relative units resolve against the initial style. could be different to the root<br>
&lt;JoshT> ... could say the units resolve against the target element of anim if it exists<br>
&lt;JoshT> ... then need to define what happens if target is null<br>
&lt;JoshT> ... and we need to pay the sync style resolution cost<br>
&lt;JoshT> ... or alt, we could define alternative time that these values get resolved against an element<br>
&lt;JoshT> ... i think they're all complicated<br>
&lt;JoshT> ... someone would need to figure out the tradeoffs<br>
&lt;JoshT> ... right now not implementable<br>
&lt;JoshT> ... want something implementable in a uniform way<br>
&lt;ydaniv> SGTM, until we define this properly<br>
&lt;JoshT> ... or maybe in future we'll have a generic way in JS to say 'given this element and unit, give me the computed px values'<br>
&lt;JoshT> ... someone in bug says this makes it useless<br>
&lt;JoshT> ... don't know if they were thinking if % weren't allowed<br>
&lt;JoshT> ydaniv: [that was me]. it is limiting, but it's OK to patch this now and spec it later<br>
&lt;JoshT> astearns: I agree and I'm fine, but if there is something that would be better for authors where we can arbitrarily pick an element without author choice, it might be better to do that than only have absolute units<br>
&lt;JoshT> ... if we do only absolute units now, we'll need an opt in to get the better behaviour<br>
&lt;JoshT> weinig: we can just do a parse error for relative units for now, and then not have a parse error in the future<br>
&lt;astearns> ack TabAtkins<br>
&lt;JoshT> TabAtkins: I agree with sam. don't want to resolve on root element<br>
&lt;JoshT> ... would rather make it parse-time invalid now<br>
&lt;fantasai> +1 to TabAtkins and weinig<br>
&lt;JoshT> weinig: my proposal is for both the string form and type CSS form<br>
&lt;TabAtkins> yeah there shoudln't be any difference between those two<br>
&lt;JoshT> ... you can do a typed value that is an em value, so we have to treat them the same here<br>
&lt;JoshT> PROPOSED: make it a parse error to set string values or typed CSSOM values that don't use absolute length units or percentages<br>
&lt;JoshT> weinig: one other clarificatio.n also precludes things like var, sibling-index() and anything that requires an element to resolve<br>
&lt;fantasai> sgtm<br>
&lt;fantasai> We can always relax restrictions once we figure out what's the best thing to do<br>
&lt;JoshT> ... but the implication is we're excluding anything that requires an element context<br>
&lt;JoshT> PROPOSED: make it a parse error to set string values or typed CSSOM values that require an element to resolve<br>
&lt;JoshT> RESOLVED: make it a parse error to set string values or typed CSSOM values that require an element to resolve<br>
&lt;JoshT> weinig: I've filed a bunch of issues like this. should I turn them all into a single issue that we can resolve on later?<br>
&lt;JoshT> ... or should we go through them one by one?<br>
&lt;JoshT> astearns: I'm fine with that if they're all basically the same decision<br>
&lt;JoshT> ... we can even make it an async proposal<br>
</details>

<!-- comment id=5134243082 author=flackr created=2026-07-30T17:38:39Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-5134243082 -->
## @flackr — 2026-07-30

FWIW, the intention is that view timeline stores a CSSNumericValue (which as I understand can store things like 3em in an unresolved state), and then resolve it against the target when needed. Of course there are things it can't store AFAICT like sibling-index which should still throw.

<!-- comment id=5134679590 author=weinig created=2026-07-30T18:21:18Z url=https://github.com/w3c/csswg-drafts/issues/13853#issuecomment-5134679590 -->
## @weinig — 2026-07-30

That’s still an option for the future, it just needs to be fully spec’d out. For now the resolution the WG agreed on is that things like em will throw an exception. 
