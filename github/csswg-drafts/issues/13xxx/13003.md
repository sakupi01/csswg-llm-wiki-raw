---
number: 13003
title: "[css-syntax-3][css-cascade][css-conditional] Missing definition of `<declaration>`"
state: "open"
labels: ["css-cascade-4", "css-cascade-5", "css-conditional-3", "css-conditional-5", "css-syntax-3", "editorial"]
author: "SebastianZ"
created_at: "2025-10-22T13:01:11Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13003"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:38Z"
---

CSS Cascade 4 and 5 as well as CSS Conditional 3 and 5 refer to a `<declaration>` type, which is currently not defined.

That should probably be added to CSS Syntax 3, where the [term "declaration"](https://drafts.csswg.org/css-syntax-3/#declaration) is already defined.

@tabatkins @SimonSapin 

Sebastian

<!-- comment id=3434665669 author=svgeesus created=2025-10-23T00:34:28Z url=https://github.com/w3c/csswg-drafts/issues/13003#issuecomment-3434665669 -->
## @svgeesus — 2025-10-23

Syntax 3 defines

- [`declaration`](https://drafts.csswg.org/css-syntax-3/#declaration), in § 5.2
- [`<declaration-list>`](https://drafts.csswg.org/css-syntax-3/#typedef-declaration-list), in § 7.1
- [`<declaration-rule-list>`](https://drafts.csswg.org/css-syntax-3/#typedef-declaration-rule-list), in § 7.1
- [`<declaration-value>`](https://drafts.csswg.org/css-syntax-3/#typedef-declaration-value), in § 7.2

but not `<declaration>`

<!-- comment id=4147833413 author=cdoublev created=2026-03-28T10:45:51Z url=https://github.com/w3c/csswg-drafts/issues/13003#issuecomment-4147833413 -->
## @cdoublev — 2026-03-28

Fixed by 140f4d1?

<!-- comment id=4175281179 author=cdoublev created=2026-04-02T07:35:11Z url=https://github.com/w3c/csswg-drafts/issues/13003#issuecomment-4175281179 -->
## @cdoublev — 2026-04-02

  > Fixed by 140f4d1?

Its [definition](https://drafts.csswg.org/css-conditional-5/#typedef-declaration) does not match [this test from WPT](https://github.com/web-platform-tests/wpt/blob/a0922f6ecb98873239584c0e6b618c6384bc4736/css/css-cascade/parsing/supports-import-parsing.html#L62C3-L62C83), which all main browsers [pass](https://wpt.fyi/results/css/css-cascade/parsing/supports-import-parsing.html?label=stable&label=master&aligned) it in their stable version:

  > ```js
  > test_invalid_supports_import('@import url("nonexist.css") supports(foo: bar);');
  > ```

  > `<declaration>` here matches anything that would be successfully parsed by [consume a declaration](https://www.w3.org/TR/css-syntax-3/#consume-a-declaration), ignoring the context-validation check at the end of that algorithm.

<details>
<summary><del>However, CSS Cascade also requires:</del></summary>
<br>

  > If a `<declaration>` is given in place of a `<supports-condition>`, it must be interpreted as a `<supports-decl>` (i.e. the extra set of parentheses is implied) and treated as a `<supports-condition>`.

This appears in a [section](https://drafts.csswg.org/css-cascade-6/#import-conditions) defining how to evaluate import conditions, so *interpreted* can be understood as *evaluated* instead of *parsed and evaluated*.

If it is understood as *parsed and evaluated*, `<supports-condition>` would catch an invalid `<declaration>` because it produces `( <any-value>? )`.

</details>

(edit) I misinterpreted this requirement: an extra set of parentheses should not be implied for an *invalid* `<declaration>`.

But ~~even if it is understood as *evaluated*~~, `@import` accepts `<media-query-list>` as its last value, which is parsed forgivingly, and should match an invalid `supports()`.

So this test is certainly incorrect.

Now, if the goal is to forgive any invalid input in `supports()`, I think this should be achieved generally and consistently, by parsing an unwrapped `support()` argument as if it was wrapped in parens, rather than by defining `<declaration>` with a relatively open value range, as well as some feature test functions, inconsistently:

  - `font-format()` and `font-tech()` accept a closed range of keywords
  - `at-rule()`, `env()`, `named-feature()`, accept an open range of `<ident>`
  - `<supports-condition-name>` accept an open range of `<dashed-ident>`

Except custom environment variables and custom condition names, the result from evaluating these features cannot change over time. If it is performed at parse time, and if features evaluating to false are made invalid, it can make the implementation to evaluate these features easier.

<!-- comment id=4185174247 author=SebastianZ created=2026-04-03T20:58:45Z url=https://github.com/w3c/csswg-drafts/issues/13003#issuecomment-4185174247 -->
## @SebastianZ — 2026-04-03

@cdoublev Thank you for digging deeper into that! Though to me that sounds like it goes far beyond what this issue covers and deserves a separate issue.

What I would have expected for this is a general definition of `<declaration>` in CSS Syntax 3 (without any restrictions like added in https://github.com/w3c/csswg-drafts/commit/140f4d183512061b4fe161e117a395e9e20ede99).

As that change was made by @tabatkins, maybe they can provide some feedback.

Sebastian
