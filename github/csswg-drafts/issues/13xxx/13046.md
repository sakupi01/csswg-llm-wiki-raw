---
number: 13046
title: "[css-mixins-1] @function grammar is missing open-paren"
state: "closed"
labels: []
author: "ferdnyc"
created_at: "2025-10-31T21:33:57Z"
closed_at: "2025-11-01T14:02:32Z"
url: "https://github.com/w3c/csswg-drafts/issues/13046"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:45:24Z"
---

The definition of the `@function` syntax is:

```html
<pre class="prod def" nohighlight>
&lt;@function> = @function <<function-token>> <<function-parameter>>#? )
	[ returns <<css-type>> ]?
{
	<<declaration-rule-list>>
}

<dfn><<function-parameter>></dfn> = <<custom-property-name>> <<css-type>>? [ : <<default-value>> ]?
<dfn><<css-type>></dfn> = <<syntax-component>> | <<@function/type()>>
<dfn><<default-value>></dfn> = <<declaration-value>>
<dfn function lt="type()" for="@function">&lt;type()></dfn> = type( <<syntax>> )
</pre>
```

The literal close-paren (`)`) that follows the `<function-parameter>` list is represented, but the _open_ paren (`(`) that should fall between the `<function-token>` and the (optional) parameter list is not.

The simplest solution is for the grammar to read:

```html
<pre class="prod def" nohighlight>
&lt;@function> = @function <<function-token>> ( <<function-parameter>>#? )
	[ returns <<css-type>> ]?
{
	<<declaration-rule-list>>
}

<dfn><<function-parameter>></dfn> = <<custom-property-name>> <<css-type>>? [ : <<default-value>> ]?
<dfn><<css-type>></dfn> = <<syntax-component>> | <<@function/type()>>
<dfn><<default-value>></dfn> = <<declaration-value>>
<dfn function lt="type()" for="@function">&lt;type()></dfn> = type( <<syntax>> )
</pre>
```

Unless `<function-token>` is meant to include the open-paren, but since it isn't defined that way anywhere (formally or informally), that seems unlikely.

(Also, it would make more sense structurally if `<function-token>` were then defined to include not only the open-paren, but also the argument list and the close-paren. Its definition could then be decomposed further.)

<!-- comment id=3475386654 author=ferdnyc created=2025-11-01T01:35:37Z url=https://github.com/w3c/csswg-drafts/issues/13046#issuecomment-3475386654 -->
## @ferdnyc — 2025-11-01

@tabatkins reports that `<function-token>` _does_ include the open-paren, in which case I guess this becomes a report that `<function-token>` isn't formally defined anywhere.

<!-- comment id=3475846005 author=tabatkins created=2025-11-01T06:25:02Z url=https://github.com/w3c/csswg-drafts/issues/13046#issuecomment-3475846005 -->
## @tabatkins — 2025-11-01

? Sure it is, <https://drafts.csswg.org/css-syntax/#typedef-function-token>

<!-- comment id=3476335338 author=astearns created=2025-11-01T12:20:16Z url=https://github.com/w3c/csswg-drafts/issues/13046#issuecomment-3476335338 -->
## @astearns — 2025-11-01

Well, the open-paren-consuming part is defined in https://drafts.csswg.org/css-syntax/#consume-ident-like-token

<!-- comment id=3476397514 author=ferdnyc created=2025-11-01T14:02:32Z url=https://github.com/w3c/csswg-drafts/issues/13046#issuecomment-3476397514 -->
## @ferdnyc — 2025-11-01

Ah, so it is, thanks @astearns.

It is a bit unfortunate that that aspect isn't part of any of the formal grammar productions anywhere, as (for example) MDN [imports the `@function` grammar](https://developer.mozilla.org/en-US/docs/Web/CSS/@function#formal_syntax) (and only the grammar) directly from the standards documents, and therefore lacks any definition of `<function-token>`.

But something can probably be added to the data on their end, to provide a definition.

Closing this as addressed, though, apologies for the noise.

<!-- comment id=3478457016 author=tabatkins created=2025-11-02T22:59:08Z url=https://github.com/w3c/csswg-drafts/issues/13046#issuecomment-3478457016 -->
## @tabatkins — 2025-11-02

The `<*-token>` definitions are the bottom of the grammar productions; below that there's only parsing algorithms. At *some* point you have to stop and switch to something imperative.
