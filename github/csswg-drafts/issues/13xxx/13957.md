---
number: 13957
title: "[css-multicol] Examples 30 & 31 do not follow from the spec - column balancing is under-specified and/or incorrect"
state: "open"
labels: ["css-multicol-1", "css-multicol-2"]
author: "Psychpsyo"
created_at: "2026-05-23T09:41:11Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13957"
comments: 15
has_resolution: false
synced_at: "2026-08-06T00:47:06Z"
---

[css-multicol](https://www.w3.org/TR/css-multicol-1/) and [css-multicol-2](https://www.w3.org/TR/css-multicol-2/) have this example:
> EXAMPLE 31
> ```css
> article {
>   width: 60em;
>   height: auto;
>   columns: 4;
>   column-fill: balance;
> }
> ```
> In this example, an article starts with an unbreakable figure which sets the column height. Subsequent content is filled sequentially into the remaining columns:
> <img width="665" height="120" alt="Image" src="https://github.com/user-attachments/assets/67d05804-4140-4e69-9966-082ab4ced184" />

Nowhere in the definition of `column-fill: balance` or the explanation of balanced column filling does it say that columns should be filled sequentially up to some hypothetical minimum height determined up-front. The spec for `column-fill: balance` only says to "balance content equally between columns, as far as possible". A bit further up, [§8 Filling Columns](https://www.w3.org/TR/css-multicol-2/#filling-columns) tells us that this means to "minimize variations in column height". Sequential filling is called out specifically as a thing to do when columns are not balanced.
And one can achieve a more balanced distribution here, by filling all three columns that come after the figure with three lines of text each.

All browsers currently produce the example rendering from the spec when faced with a situation that would lead to it, but I cannot find any spec basis for this. How exactly am I supposed to determine 1. the column height of a balanced multicol container and 2. the expected distribution?

I am asking because I've written [an implementation](https://github.com/LadybirdBrowser/ladybird/pull/8785/changes/cb3903290e391ef5021e8bff1492b7fc26ffc0e6) for the Ladybird browser which manages to produce a more balanced layout. As it stands, this is compliant with the normative requirements, but those seem to be at odds with what everyone else is doing.

PS: Example 30 makes a similar assumption that there must be some vaguely implementation-un-defined height-establishing step up-front.

<!-- comment id=5061491298 author=rachelandrew created=2026-07-23T17:37:39Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5061491298 -->
## @rachelandrew — 2026-07-23

@Psychpsyo sorry I missed this previously. The height in the examples is defined by there being something taller in the container or a height being set on the multicol container. Otherwise the container has no defined height (it's defined by the content in the columns) so balancing doesn't apply.

Looping in @mstensho as another implementor. 

<!-- comment id=5061543512 author=Psychpsyo created=2026-07-23T17:43:41Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5061543512 -->
## @Psychpsyo — 2026-07-23

> The height in the examples is defined by there being something taller in the container or a height being set on the multicol container.

Sonething taller than what?

<!-- comment id=5065947243 author=rachelandrew created=2026-07-24T03:48:54Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5065947243 -->
## @rachelandrew — 2026-07-24

Something that's a fixed height so it's taller than the text content (so an image, for example). If you have a few words of text and an image, and no assigned height on the multicol container, then the image is going to define the height of the container.

I just built all of these in CodePen so we can see whether the problem is with the examples, or the text, or implementations. See: https://es-d-30847312820260726-47e00f2802727be1f5018d8f46fe1599.codepen.dev/ I think the examples could be improved to be clearer. Also, we don't seem to have interop on the first one, so there's something confusing here. Would love to hear from @mstensho 

I don't think there's anything assuming an undefined height though, as mentioned in your comment. The height is either author defined, or is caused by the content of the container.

<!-- comment id=5066203892 author=SelenIT created=2026-07-24T04:39:17Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5066203892 -->
## @SelenIT — 2026-07-24

It seems that these examples try to explain the last part of that passage from the [§8 Filling Columns](https://www.w3.org/TR/css-multicol-2/#filling-columns):

> If columns are balanced, user agents should try to minimize variations in column height, while honoring forced breaks, [widows](https://www.w3.org/TR/css-break-4/#propdef-widows) and [orphans](https://www.w3.org/TR/css-break-3/#propdef-orphans), _and other properties that may affect column heights_.

I.e. _generally_ the balancing algorithm tries to minimize variations in column height _as much as possible_ (by choosing the most fitting height for all columns), but there can be things that explicitly or implicitly set the minimum column height, and in such cases the balancing algorithm can't make the height of all columns smaller than that minimum even if there is not enough content to fill the other columns. And after the column height is chosen, no other adjustment can be done, only sequential filling.

That said, I agree that the phrase "If columns are not balanced, they are filled sequentially", implying that balanced columns are filled differently, is misleading. Maybe it should be replaced by something like "After the height of the columns is determined, they are filled sequentially"?

<!-- comment id=5066216714 author=Psychpsyo created=2026-07-24T04:41:56Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5066216714 -->
## @Psychpsyo — 2026-07-24

> Something that's a fixed height so it's taller than the text content (so an image, for example). If you have a few words of text and an image, and no assigned height on the multicol container, then the image is going to define the height of the container.

The final height, yes. The height that the multicol as a whole ends up being at the end is not where my confusion comes from.

> I don't think there's anything assuming an undefined height though, as mentioned in your comment. The height is either author defined, or is caused by the content of the container.

The undefined height I'm talking about is not the total height of the container, but rather the height that I am to fill up with content in any given column.
In example 31, concretely, this would be asking: Why does every engine fill the second column all the way, up to the height of the purple box?
By the time we are filling the second column, layout isn't done yet, so the multicol container has no height yet. And even if it did, we'd be under no obligation to stack content all the way there. `column-fill: balance` suggests that we should still be balancing across all columns.
Here's a more extreme version of example 31 to demonstrate:

<img width="978" height="818" alt="Image" src="https://github.com/user-attachments/assets/0126647d-e63d-41d9-9afa-2fe6cd869a54" />

It is filled like this in every browser, when really, (I think) this would be the more correct way to fill it:

<img width="978" height="818" alt="Image" src="https://github.com/user-attachments/assets/c920936f-6761-4f41-a2fe-8cc284a3a010" />

Image 1 here leads to a total height variation of 3.5 column heights. (take every set of 2 columns and sum their difference)
Image 2 leads to a total height variation of just 1.5 column heights. (the three text columns are the same height, but each half a column shorter than the purple one)

<!-- comment id=5066238373 author=Psychpsyo created=2026-07-24T04:46:02Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5066238373 -->
## @Psychpsyo — 2026-07-24

> It seems that these examples try to explain the last part of that passage from the [§8 Filling Columns](https://www.w3.org/TR/css-multicol-2/#filling-columns):
> 
> > If columns are balanced, user agents should try to minimize variations in column height, while honoring forced breaks, [widows](https://www.w3.org/TR/css-break-4/#propdef-widows) and [orphans](https://www.w3.org/TR/css-break-3/#propdef-orphans), _and other properties that may affect column heights_.
> 
> I.e. _generally_ the balancing algorithm tries to minimize variations in column height _as much as possible_ (by choosing the most fitting height for all columns)

Nowhere does the spec say that the balancing algorithm is meant to do this balancing by setting the column height to something that'll lead to a nice balance. Maybe that's where my confusion comes from, as my initial assumption was that the balancing could also be done while filling the columns, without knowing the height yet, because that can give more balanced results. (See the second image from my previous post)

> That said, I agree that the phrase "If columns are not balanced, they are filled sequentially", implying that balanced columns are filled differently, is misleading. Maybe it should be replaced by something like "After the height of the columns is determined, they are filled sequentially"?

There should probably also be something that tells the reader explicitly that column balancing is not a special way of filling the columns, but rather a special way of determining their overall height.

<!-- comment id=5066320821 author=Psychpsyo created=2026-07-24T05:01:46Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5066320821 -->
## @Psychpsyo — 2026-07-24

> I just built all of these in CodePen so we can see whether the problem is with the examples, or the text, or implementations. See: https://es-d-30847312820260726-47e00f2802727be1f5018d8f46fe1599.codepen.dev/ I think the examples could be improved to be clearer. Also, we don't seem to have interop on the first one, so there's something confusing here. Would love to hear from [@mstensho](https://github.com/mstensho)

The first one is probably because `widows` and `orphans` default to `2`. Firefox doesn't have those yet and I guess Safari fails to honor them in multicol.

<!-- comment id=5068255729 author=mstensho created=2026-07-24T09:22:19Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5068255729 -->
## @mstensho — 2026-07-24

> Nowhere does the spec say that the balancing algorithm is meant to do this balancing by setting the column height to something that'll lead to a nice balance. Maybe that's where my confusion comes from, as my initial assumption was that the balancing could also be done while filling the columns, without knowing the height yet, because that can give more balanced results. (See the second image from my previous post)

The way it's implemented, `column-fill` indeed doesn't affect how columns are filled, which sure can be a source of confusion. `column-fill` doesn't affect how columns are filled. Huh? :) `column-fill` only affects the height of the columns (and the containing line). I don't think this is what the spec says.

From the spec, we know that all columns within a line have the same height.

https://drafts.csswg.org/css-multicol-2/#the-multi-column-model
> and all column boxes in a line have the same column height.

In the implementations, `column-fill:balance` will make sure that the column height is large enough (but no larger than that) to fit all fragmented content - instead of setting the height to that of the content box of the multicol container. So we're possibly violating the spec in some cases:

> In the simplest case a multicol container will contain only one line of columns, inside one multicol row, and the height of each column will be equivalent to the used height of the multi-column container’s content box.

I doesn't mention anything about balancing affecting this, meaning that:

```html
<div style="columns:2; column-fill:balance; height:1000px; line-height:42px; orphans:1; widows:1;">
  text<br>
  text<br>
</div>
```

should result in 1000px tall columns. Blink doesn't do that. (Similarly for the other engines, I guess.) Instead the column height becomes 42px - just enough for one line of text.

It gets more interesting. If we add a column rule, it *does* become 1000px tall, in all engines.

https://drafts.csswg.org/css-multicol-2/#column-gaps-and-rules
> The length of the column gaps and column rules is equal to the column height.

And that's in spite of the column height being 42px for layout purposes. In Blink we have special code that makes sure that the rules extend to the end of the multicol container (1000px), rather than just the column height (42px). Maybe the other engines do something similar.

Let's scroll to chapter 8. https://drafts.csswg.org/css-multicol-2/#filling-columns
> If columns are balanced, user agents should try to minimize variations in column height

The spec has already said that "all column boxes in a line have the same column height". There may be zero of such variation!

Maybe the rest of the spec evolved (introducing column lines and rows), whereas this part of the spec wasn't updated. It should say something about calculating column heights for balancing.

It is clear that balancing sometimes needs to affect the column height. While that necessarily wasn't the case in the previous example, where there was a fixed container height, if the container height is auto, column balancing should result in a neat column height. E.g.

```html
<!DOCTYPE html>
<div style="columns:3; orphans:1; widows:1; line-height:20px;">
  <div style="contain:size; height:100px;"></div>
  text<br>
  text<br>
  text<br>
  text<br>
</div>
```

The total height of the content (if not fragmented) is 180px (100px + 4 * 20px). We want to balance it into three columns, so 180px / 3 = 60px. However, the inner DIV is unbreakable and it is 100px, so the column height will become 100px. The height of the multicol container also becomes 100px. All engines agree on that. They all successfully calculate a suitable column height and multicol container height. What they don't do is the "minimize variations" part, apart from what we get for free by simply setting a good column height. All existing engines place all four lines in the second column, because they fit. The third column remains empty.

In summary, maybe column balancing should consist of two steps.

1. If available height is unconstrained, calculate a good column height. Blink / Gecko / WebKit do this. Otherwise column height should follow from available height (e.g. the content box of the multicol container). Blink / Gecko / WebKit all seem to calculate a "good column height" even in constrained cases, so that if there's not enough content to fill a fixed-size multicol container, the columns will be shorter than the content-box of the multicol container. This seems wrong, and we need special code to get the column rules right because of this.
2. When filling the columns, attempt to minimize variations. Blink / Gecko / WebKit don't do this.

<!-- comment id=5114941888 author=rachelandrew created=2026-07-29T08:16:04Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5114941888 -->
## @rachelandrew — 2026-07-29

@mstensho do you feel there are changes we need to make in the level 1 specification? The reason I spotted this issue is I'm trying to sort out the tests so we can get this to REC, so I'd like to make any final changes and republish. 

There's definitely places I need to update in the L2 draft, as you point out. I can do another pass through that, but I'd love to get the L1 draft finalized.

<!-- comment id=5116742272 author=mstensho created=2026-07-29T10:50:24Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5116742272 -->
## @mstensho — 2026-07-29

I realize now that both L1 and L2 both sort of suggest that what @Psychpsyo implemented in Ladybird is actually wrong. But maybe that's just a spec issue.

https://drafts.csswg.org/css-multicol-1/#cf

(regarding the `column-fill` property)

> In continuous contexts, this property does not have any effect when there are overflow columns.

In other words:

```html
<!DOCTYPE html>
<div style="columns:3; width:60%; height:60px; line-height:20px; orphans:1; widows:1; background:yellow;">
  line<br>
  line<br>
  line<br>
  line<br>
  <div style="contain:size; border:solid; height:100px;"></div>
  overflow<br>
</div>
```

There's an overflowing column here, and if `column-fill` shouldn't affect behavior in such cases, the four lines preceding the tall unbreakable `div` cannot be neatly balanced. One would think that if balancing is enabled, there might be two lines in the first column, and two lines in the second, but with sequential filling there will be three in the first and one in the second.

This is what it looks like in Chromium:

<img width="580" height="124" alt="Image" src="https://github.com/user-attachments/assets/1ba7194f-c3b2-4af4-8a21-5a5028bdfa07" />

<!-- comment id=5116905817 author=mstensho created=2026-07-29T11:06:16Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5116905817 -->
## @mstensho — 2026-07-29

L1 also has this:

> All column boxes in a line have the same column width, and all column boxes in a line have the same column height.

And this:

> If columns are balanced, user agents should try to minimize variations in column height

There can be zero variation in column height if the first piece of spec is to be honored. So maybe this latter part should rather say something about the amount of filling, rather than the column height.

Of course, as mentioned earlier, column balancing may also affect the column height (but columns on the same line should still have the exact same height), if the available space is unconstrained.

<!-- comment id=5165831223 author=rachelandrew created=2026-08-03T11:39:21Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5165831223 -->
## @rachelandrew — 2026-08-03

I think saying they all have the same height is correct, it's the amount of filling (which is the only visible height given that we don't have the ability to put background colors on individual columns). So something like:

`user agents should try to minimize variations in the amount of content in each column`

<!-- comment id=5165985737 author=Psychpsyo created=2026-08-03T11:55:58Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5165985737 -->
## @Psychpsyo — 2026-08-03

> (which is the only visible height given that we don't have the ability to put background colors on individual columns)

I think the column rules paint to the very bottom so their height is visible from those.
Also, a fragmented box should (according to [css-break](https://www.w3.org/TR/css-break-3/#box-splitting)) get extended all the way to the fragmentainer (=column) end, which would also make the gap between the last bit of content and the bottom of the column observable.
Not sure if that would be desirable for balanced columns and I know that at least Firefox has some grid-fragmentation related edge cases where this doesn't currently happen. (see https://github.com/w3c/csswg-drafts/issues/12801#issuecomment-4836629840)

> So something like:
> 
> `user agents should try to minimize variations in the amount of content in each column`

Sounds about right, although we might want to acknowledge whether or not an extended box background counts as 'content' for this. (probably not since it would have to care about whether or not the box has any visible decorations and then, the moment that it does, it would make the balancing useless)

<!-- comment id=5189482978 author=rachelandrew created=2026-08-05T08:33:39Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5189482978 -->
## @rachelandrew — 2026-08-05

I've been discussing with Florian at the CSSWG, I think we essentially have a small editorial change to clarify the L1 spec (content rather than height).

I think the bigger problem for a new implementor is the fact we've added rows to the spec in L2 and there's still a few things falling out of that to tidy up in the level 2 spec, so I'll leave this open to make sure I go through that.

@Psychpsyo very happy to hear anything else you think is unclear in level 2. Multicol was an early "CSS3" spec and was mostly just a bunch of examples, to some extent we've specced what browsers were doing, so a new implementation is likely to find things. Adding rows has highlighted some of this as we've needed to be more precise.

<!-- comment id=5190606038 author=Psychpsyo created=2026-08-05T10:28:04Z url=https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5190606038 -->
## @Psychpsyo — 2026-08-05

> [@Psychpsyo](https://github.com/Psychpsyo) very happy to hear anything else you think is unclear in level 2. Multicol was an early "CSS3" spec and was mostly just a bunch of examples, to some extent we've specced what browsers were doing, so a new implementation is likely to find things. Adding rows has highlighted some of this as we've needed to be more precise.

I'm not sure I have that much to say right now as it's been a while since I worked on this. Ladybird (where I was doing this) has also since stopped accepting contributions from non-maintainers (that includes me). I did file https://github.com/w3c/csswg-drafts/issues/12688 back then, which needs some clarification on whether or not `column-height` on its own can enable multicol layout.
As for rows: They didn't seem all that problematic, and analogous stuff has existed before with spanners, paginated flows and nested multicols. Then again, that's not a bit of the spec I really got to in my implementation.
(And any confusion I ran into there either got cleared up by the spec or I managed to find existing Github issues. If that hadn't been the case, I would've opened those issues myself.)

One thing that I do worry about with current engine behavior for balanced columns is that they all seem to manage picking a height and then flowing into that. If the spec wording for `column-fill: balance` gets updated to talk about content rather than height, it would mean that current engine behavior is wrong. I am in favour of that because I dislike the current behavior on account of [this comment](https://github.com/w3c/csswg-drafts/issues/13957#issuecomment-5068255729):
> The way it's implemented, `column-fill` indeed doesn't affect how columns are filled, which sure can be a source of confusion. `column-fill` doesn't affect how columns are filled. Huh? :)

But I don't think anyone has established whether or not the current behavior might be necessary for web compat. Although I doubt it is. In either case, the spec currently says nothing at all about the algorithm of choice that one might use to implement this. This then left me wondering if I'm just meant to figure something out and run with it, or if there's some secret algorithm that all current browsers happen to do, which I'd need to potentially reverse-engineer and copy to remain web-compatible. (I chose to do my own thing, purely because it seemed more interesting.)

It would be nice if there was at least a note about that in the spec, and even nicer if the spec had an algorithm or at least some pointers for a general balancing strategy to recommend. Because, given `widows`, `orphans`, all the breaking properties, [parallel flows](https://www.w3.org/TR/css-break-3/#parallel-flows), etc... this problem seems far from trivial.
