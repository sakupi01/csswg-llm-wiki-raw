---
number: 13719
title: "[css-color-4] Clarify how to resolve `<system-color>` and `<deprecated-color>`"
state: "open"
labels: ["Needs Edits", "css-color-4"]
author: "cdoublev"
created_at: "2026-03-25T06:53:17Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13719"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:37Z"
---

  > To maintain legibility, the `<system-color>` keywords also respond to light mode or dark mode changes.

This [statement](https://drafts.csswg.org/css-color-4/#css-system-colors) is not backed by any defined requirement. I think it should rather be a recommendation for the UAs:

  > To maintain legicility, the UA *should* adapt the resolved value of `<system-color>` keywords to light mode or dark mode changes.

But *light mode or dark mode changes* seems restrictive. I think UAs should adapt to the [*used color scheme*](https://drafts.csswg.org/css-color-adjust-1/#determine-the-used-color-scheme) instead.

<!-- comment id=4132415901 author=cdoublev created=2026-03-26T07:47:42Z url=https://github.com/w3c/csswg-drafts/issues/13719#issuecomment-4132415901 -->
## @cdoublev — 2026-03-26

To elaborate on my previous comment (after a more in-depth research).

  > > To maintain legibility, the `<system-color>` keywords also respond to light mode or dark mode changes.
  >
  > This statement is not backed by any defined requirement.

This [comment](https://github.com/w3c/csswg-drafts/issues/6773#issuecomment-1098285102) confirms that this requirement needs to be defined (emphasize added):

  > There may be some updates needed around `color-scheme`, as well. (i.e. to **clarify that it affects the computed value of system colors**)

It was written before the resolution to use the corresponding `<system-color>` value in its colorspace as the computed value, instead of the specified keyword.

However, I would expect it to be clarified in [§ 14.5. Resolving other colors (CSS Color)](https://drafts.csswg.org/css-color-4/#resolving-other-colors), which currently only says this:

  > The declared value for each `<system-color>` keyword and `<deprecated-color>` keyword is itself. The computed value is the corresponding color in its color space.

Similarly, I was not expecting to find the [`<system-color>` mappings](https://drafts.csswg.org/css-color-adjust-1/#forced-color-palettes) in CSS Color Adjustment, fwiw.

---

[These comments](https://github.com/w3c/csswg-drafts/issues/3873#issuecomment-548001301) also suggest that how to compute `<deprecated-color>` is also currently underspecified:

  > Dropping a note here to say: don't forget to say how these values compute, if they compute to other keyword values or to a fixed color or to themselves. (I'm guessing fewer keywords that compute to themselves here is better for implementations.)

  > Given that these are legacy values for legacy compat & we don't want authors using them in new content, we can probably safely define them all as light mode values.

And #13459 asked if `<deprecated-color>`s should be (parse time) aliases of `<system-color>`s.

---

  > But light mode or dark mode changes seems restrictive.

I thought other modes than light/dark modes exist tbh. But my point was also that the linked algorithm does not just consider the user preferred color scheme.

<!-- comment id=4138011658 author=svgeesus created=2026-03-26T20:29:44Z url=https://github.com/w3c/csswg-drafts/issues/13719#issuecomment-4138011658 -->
## @svgeesus — 2026-03-26

[Better?](https://www.w3.org/TR/css-color-4/#css-system-colors)

<!-- comment id=4140241870 author=cdoublev created=2026-03-27T05:07:53Z url=https://github.com/w3c/csswg-drafts/issues/13719#issuecomment-4140241870 -->
## @cdoublev — 2026-03-27

I think so. Thank you.

Regarding `<deprecated-color>`. I do not think its computed value is tested on WPT. ~~If it was defined as a parse time alias of `<system-color>`, as suggested in #13459, this would change the declared/resolved value that can be observed in Chrome and Firefox today:~~ (edit) No, sorry. There was an oversight in my test code. I can observe that the computed value of `<deprecated-color>` is the same value as the corresponding `<system-color>`, so defining `<deprecated-color>` as an alias would only change the declared value :

```js
const { style: declared } = target
const resolved = getComputedStyle(target)

// ButtonBorder (<deprecated-color>) maps to ActiveBorder (<system-color>)

declared.colorScheme = 'light'
declared.color = 'buttonborder'
resolved.color // "rgb(0, 0, 0)"
declared.colorScheme = 'dark'
resolved.color // "rgb(255, 255, 255)"

declared.colorScheme = 'light'
declared.color = 'activeborder'
resolved.color // "rgb(0, 0, 0)"
declared.colorScheme = 'dark'
resolved.color // "rgb(255, 255, 255)"
```

~~This is the behavior suggested in the comment I already quoted in my previous comment:~~

  > ~~Given that these are legacy values for legacy compat & we don't want authors using them in new content, we can probably safely define them all as light mode values~~

~~So do you want to postpone this question until #13459 is answered?~~ Do you want me to open a separate issue? Or do you want to clarify it right away?

<!-- comment id=4157689875 author=svgeesus created=2026-03-30T19:42:36Z url=https://github.com/w3c/csswg-drafts/issues/13719#issuecomment-4157689875 -->
## @svgeesus — 2026-03-30

I agree this needs to be tested on WPT.

I prefer to use an explicit alias, and just discussed this with @Loirooriol but other implementers have yet to chime in.

Not sure this needs a separate issue.
