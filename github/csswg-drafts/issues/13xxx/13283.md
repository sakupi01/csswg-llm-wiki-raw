---
number: 13283
title: "[css-selectors-4] `::is()` functional pseudo element selector"
state: "open"
labels: ["selectors-5"]
author: "romainmenke"
created_at: "2025-12-31T06:57:25Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13283"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:45:42Z"
---

See: https://github.com/w3c/csswg-drafts/issues/9702

I've been wondering if a functional pseudo element selector might create a way forwards.
Unsure about naming, but we can always bikeshed later if this idea is worthwhile.

This `::is()` selector would work as other pseudo elements in that order in a compound matters, e.g. `:hover::is(...)` != `::is(...):hover`.

It accepts a complex selector list as its argument and is not limited to only pseudo elements.
It can select pseudo elements but isn't limited to only pseudo elements, it can also select regular elements.

So `::is(.foo)` works and it works exactly as `:is(.foo)`.

On its left side it matches either the originating elements or the element itself.
On its right side it matches either the pseudo element or the element itself.

For all other aspects (specificity, forgiving, ...) this new selector behaves as the existing `:is()` pseudo class.

Leaning into the special behavior of pseudo selectors (that they are combinators) might give us a way forwards with nesting. Nesting could be specified in terms of this new selector. This would not be a breaking change as any existing CSS would continue to work as it did before.

-----

Some examples:

```css
.foo::is(::before, .bar) {}

/* equivalent to: */
.foo::before {}
.foo.bar {}
```

```css
:hover::is(::before, .foo) {}

/* equivalent to: */
:hover::before {}
:hover.foo {} /* itself equivalent to: `.foo:hover {}` */
```

```css
::is(::before, .foo):hover {}

/* equivalent to: */
::before:hover {}
.foo:hover {} /* itself equivalent to: `:hover.foo {}` */
```


-----

Examples from https://github.com/w3c/csswg-drafts/issues/9702#issuecomment-3573406054


```css
A::before { &:hover {}}

/* equivalent to: */
::is(A::before):hover {}

/* equivalent to: */
A::before:hover {}
```

in contrast to:

```css
A::before { :hover& {}}

/* equivalent to: */
:hover::is(A::before) {}

/* equivalent to: */
A:hover::before {}
```

---

```css
:hover { ::before& {}}

/* equivalent to: */
::before::is(:hover) {}

/* equivalent to: */
::before:hover {}
```

in contrast to:

```css
:hover { &::before {}}

/* equivalent to: */
::is(:hover)::before {}

/* equivalent to: */
:hover::before {}
```

------

## Using `:is()` the functional pseudo **class** with pseudo elements

`:is()` could be specified to accept pseudo elements but without any of the special behavior mentioned above.

This is still useful to filter lists of pseudo elements

```css
::before, ::after {
  &:is(::before) {}
}

/* equivalent to: */
::is(::before, ::after):is(::before) {}

/* equivalent to: */
::before:is(::before) {}
::before:is(::after) {} /* never matches */
```

```css
::before, ::after {
  :is(::before)& {}
}

/* equivalent to: */
:is(::before)::is(::before, ::after) {}

/* equivalent to: */
::before::before {} /* never matches */
::before::after {} /* never matches */
```

<!-- comment id=3701596217 author=Loirooriol created=2025-12-31T07:09:28Z url=https://github.com/w3c/csswg-drafts/issues/13283#issuecomment-3701596217 -->
## @Loirooriol — 2025-12-31

`::before:hover {}` is supposed to work: https://drafts.csswg.org/selectors-4/#pseudo-element-states

<!-- comment id=3701611442 author=romainmenke created=2025-12-31T07:18:30Z url=https://github.com/w3c/csswg-drafts/issues/13283#issuecomment-3701611442 -->
## @romainmenke — 2025-12-31

Thanks, I always forget that it is specified to work even if it isn't implemented (yet).
I've updated the examples.

<!-- comment id=4558694232 author=svgeesus created=2026-05-27T21:10:02Z url=https://github.com/w3c/csswg-drafts/issues/13283#issuecomment-4558694232 -->
## @svgeesus — 2026-05-27

Selectors 4 is being stabilized to move to Recommendation. Proposed new features are being re-tagged for Selectors-5.
