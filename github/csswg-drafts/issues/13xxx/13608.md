---
number: 13608
title: "[animation-triggers-1] Can we add a `timeline-trigger-range` shorthand?"
state: "open"
labels: ["Agenda+", "animation-triggers-1"]
author: "bramus"
created_at: "2026-03-05T15:06:12Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13608"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:47Z"
---

When specifying a range for a timeline-trigger, there are the `timeline-trigger-activation-range` and `timeline-trigger-active-range` properties.

In the `timeline-trigger` shorthand these are fairly easy to specify:

```css
timeline-trigger:
  --t
  view()
  cover / contain;
```

But once you turn to longhands, you have to type:

```css
timeline-trigger-name: --t;
timeline-trigger-source: view();
timeline-trigger-activation-range: cover;
timeline-trigger-active-range: contain;
```

Feedback I have received, is that the person wanted to type these longhands:

```css
timeline-trigger-name: --t;
timeline-trigger-source: view();
timeline-trigger-ranges: cover / contain;
```

I must say this makes sense, especially because I very often also write my activation and active ranges on one line (see first snippet).

<!-- comment id=4007173575 author=ydaniv created=2026-03-05T19:21:18Z url=https://github.com/w3c/csswg-drafts/issues/13608#issuecomment-4007173575 -->
## @ydaniv — 2026-03-05

In general this looks good to me 👍 

I think, though, that `-ranges` is not really according to our naming style. So I think this should probably be `-range`.
And then there's the question, maybe change the longhands to `timeline-trigger-range-activation` and `timeline-trigger-range-active`?
I know it's not quite like `-start`/`-end`, but still might make be more consistent for authors.

<!-- comment id=4007483821 author=bramus created=2026-03-05T20:13:23Z url=https://github.com/w3c/csswg-drafts/issues/13608#issuecomment-4007483821 -->
## @bramus — 2026-03-05

I went with `timeline-trigger-ranges` in the code _(although I see I used `timeline-trigger-range` in the title 😅)_ because:

- They are two ranges _(whereas other properties like `animation-range` are effectively one single range)_
- For Event Triggers, I assume it would also be called something like `event-trigger-events`

> … maybe change the longhands to `timeline-trigger-range-activation` and `timeline-trigger-range-active`?

Not a fan of those names 😬

PS: Congrats on getting `animation-triggers-1` split off from `css-animations-2`! 🎉

<!-- comment id=4021999596 author=ydaniv created=2026-03-09T08:24:33Z url=https://github.com/w3c/csswg-drafts/issues/13608#issuecomment-4021999596 -->
## @ydaniv — 2026-03-09

> They are two ranges (whereas other properties like animation-range are effectively one single range)
> For Event Triggers, I assume it would also be called something like `event-trigger-events`

True that. 🤔 
Though we usually use singular names.

> > … maybe change the longhands to timeline-trigger-range-activation and timeline-trigger-range-active?
>
> Not a fan of those names 😬

yeah, well, I'm reading this more as `timeline-trigger.range.active` and `timeline-trigger.range.activation`, as in a code path, and less as plain English. We do have this scheme in many names.

<!-- comment id=4877667218 author=ydaniv created=2026-07-03T15:28:24Z url=https://github.com/w3c/csswg-drafts/issues/13608#issuecomment-4877667218 -->
## @ydaniv — 2026-07-03

I guess `-ranges` is fine, since it's 2 ranges that are listified.
