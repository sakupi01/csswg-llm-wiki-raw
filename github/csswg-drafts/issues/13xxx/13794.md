---
number: 13794
title: "[css-values-4] `round(line-width, ...)` specification is confusing"
state: "closed"
labels: ["Closed Accepted as Obvious Bugfix", "css-values-4"]
author: "romainmenke"
created_at: "2026-04-12T08:47:06Z"
closed_at: "2026-05-06T23:05:24Z"
url: "https://github.com/w3c/csswg-drafts/issues/13794"
comments: 15
has_resolution: false
synced_at: "2026-07-04T04:45:41Z"
---

see: 
- https://github.com/w3c/csswg-drafts/commit/f63d90ecd8b444eee8a69567e548f8b43241b41a
- https://drafts.csswg.org/css-values-4/#round-func

> Otherwise, identical to nearest, except that if A is positive (non-zero) and one of lower B or upper B is zero, choose the non-zero one, then snap as a border width.

Can be read as:

- `line-width` is almost identical to `nearest`
- has an exception when lower/upper B is zero and ...
- always snaps to border width

This violates the assert in https://drafts.csswg.org/css-values-4/#snap-a-length-as-a-border-width

> Assert: len is non-negative.

or:

- `line-width` is almost identical to `nearest`
- has an exception when lower/upper B is zero and ... only when in this exception -> snap to border width

When upper and lower B are both non-zero and A is positive (non-zero) there still isn't any border width snapping. This seems weird to me.

-----

> If `<rounding-strategy>` is line-width, B may also be omitted and has special behavior (defined above) essentially defaulting it to one device pixel. In all other cases, omitting B is invalid.

Can be read as:

- `line-width` allows `B` to be omitted
- always has special behavior -> snapping to border width

or:

- `line-width` allows `B` to be omitted
- only when `B` is omitted the value is snapped to border width

----

@tabatkins I assume the intention is to always have the border width snapping behavior?
It might help to split into a bit more sentences :)

<!-- comment id=4363976684 author=sb3nder created=2026-05-02T14:04:58Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4363976684 -->
## @sb3nder — 2026-05-02

> Otherwise, identical to nearest, except that if A is positive (non-zero) and one of lower B or upper B is zero, choose the non-zero one, then snap as a border width.

In which cases A>0 and upper B is zero?

~~For A>0 and B=0, lower B and upper B are zero? if so then the "non-zero one" does not exist.~~

<!-- comment id=4392822384 author=tabatkins created=2026-05-06T22:59:08Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4392822384 -->
## @tabatkins — 2026-05-06

Ah, I did write that badly.

Intention was:
* non-zero A and omitted B is just snapped (as written), 
* non-zero A and specified B acts like nearest but always chooses the non-zero B (if relevant), then snaps
* other cases just act like nearest

<!-- comment id=4396050604 author=cdoublev created=2026-05-07T09:50:37Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4396050604 -->
## @cdoublev — 2026-05-07

  > - other cases just act like nearest

But `nearest` requires not omitting B.

I suppose that `round(line-width, -1px) * round(line-width, -1px)` should result to `1px`.

<!-- comment id=4396325494 author=sb3nder created=2026-05-07T10:31:14Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4396325494 -->
## @sb3nder — 2026-05-07

@cdoublev 
I believe it's like this:
```css
round(line-width, -1px); /* invalid(missing B) */
round(line-width, 0px); /* 0px snapped */
round(line-width, 1px); /* 1px snapped */
round(line-width, 10px); /* 10px snapped */

round(line-width, -1px, 0.5px); /* -1px nearest */
round(line-width, 0px, 0.5px); /* 0px nearest snapped */
round(line-width, 1px, 0.5px); /* 1px nearest snapped */
round(line-width, 10px, 0.5px); /* 10px nearest snapped */
```

[EDIT] missed that A must be `<length>`, this below is all invalid:
```css
round(line-width, -1); /* -1 nearest */
round(line-width, 0); /* invalid(not a <length>) */
round(line-width, 1); /* invalid(not a <length>) */
round(line-width, 10); /* invalid(not a <length>) */

round(line-width, -1, 0.5); /* -1 nearest */
round(line-width, 0, 0.5); /* invalid(not a <length>) */
round(line-width, 1, 0.5); /* invalid(not a <length>) */
round(line-width, 10, 0.5); /* invalid(not a <length>) */
```

<!-- comment id=4397010693 author=cdoublev created=2026-05-07T12:16:20Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4397010693 -->
## @cdoublev — 2026-05-07

Why do you consider `round(line-width, -1px)` invalid, but not `round(line-width, -1px, 0.5px)`?

<!-- comment id=4397137123 author=sb3nder created=2026-05-07T12:34:57Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4397137123 -->
## @sb3nder — 2026-05-07

Because omitting B is invalid for `nearest` when A is not a `<number>`,
so `round(nearest, -1px)` is invalid, and `round(nearest, -1px, 0.5px)` is `-1px`.

<!-- comment id=4397496647 author=romainmenke created=2026-05-07T13:23:51Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4397496647 -->
## @romainmenke — 2026-05-07

Thank you for the edits @tabatkins, much clearer now.

I might have missed it but maybe good to call out in the spec why negative `A` doesn't have snapping? Is this a limitation in engines?

<!-- comment id=4401537231 author=tabatkins created=2026-05-07T22:07:12Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4401537231 -->
## @tabatkins — 2026-05-07

> But nearest requires not omitting B.

As @sb3nder said, that just means it's invalid, yeah. 

> I might have missed it but maybe good to call out in the spec why negative A doesn't have snapping? Is this a limitation in engines?

I presume it's just because a negative length doesn't meaningfully correspond to any number of device pixels. (But I suppose it's not terrible to just snap it the same way; the device pixel grid is a linear system and thus extends into the negatives just fine.)

<!-- comment id=4405404123 author=cdoublev created=2026-05-08T09:42:26Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4405404123 -->
## @cdoublev — 2026-05-08

An author might want to translate an element on x device pixels to the left, so with a negative value.

Anyway, if a negative length should be invalid when B is omitted, I suggest defining it more explicitly than with *"indentical to `nearest`"*.

<!-- comment id=4405522937 author=emilio created=2026-05-08T10:01:17Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4405522937 -->
## @emilio — 2026-05-08

Yeah I think negative snapping makes sense too, we resolved to do the same for outline-offset for example: https://github.com/w3c/csswg-drafts/issues/12906

<!-- comment id=4405620244 author=emilio created=2026-05-08T10:16:36Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4405620244 -->
## @emilio — 2026-05-08

@tabatkins the spec says:

> https://drafts.csswg.org/css-values-4/#valdef-rounding-strategy-line-width
> If <rounding-strategy> is [line-width](https://drafts.csswg.org/css-values-4/#valdef-rounding-strategy-line-width), the [type](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue-type) of A must match [<length>](https://drafts.csswg.org/css-values-4/#length-value).

But is that enforceable as a parse-time restriction? E.g. that'd prevent from rounding percentages as a line-width (which might be desirable?).

<!-- comment id=4411719785 author=cdoublev created=2026-05-09T06:27:28Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4411719785 -->
## @cdoublev — 2026-05-09

For reference, c196082 added snapping of a negative length.

*"The type of A must match `<length>`"* means matching as defined in https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue-match, but I guess you already remembered that, and unless I misunderstood your question.

<!-- comment id=4412449451 author=cdoublev created=2026-05-09T11:49:40Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4412449451 -->
## @cdoublev — 2026-05-09

I do not know if the new version of the snapping algo returns a positive value from a negative value:

  > 2. If the absolute value of `len` is greater than zero, but less than 1 device pixel,
  >    round it away from zero to 1 device pixel.
  > 4. If the absolute value of `len` is greater than 1 device pixel,
  >    round it towards zero to the nearest integer number of device pixels.

Was the intention the following?

  > To snap a length as a line width given a `<length>` `len`:
  >
  > 1. If `len` is an integer number of divce pixels, do nothing.
  > 2. If the absolute value of `len` is greater than zero, but less than 1 device pixel,
  >    round `len` down to -1 device pixel if it is negative,
  >    otherwise round `len` up to 1 device pixel.
  > 3. If the absolute value of `len` is greater than 1 device pixel,
  >    round `len` down to the nearest integer number of device pixels if it is negative,
  >    otherwise round `len` up to the nearest integer number of device pixels.

<!-- comment id=4412482835 author=sb3nder created=2026-05-09T12:08:44Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4412482835 -->
## @sb3nder — 2026-05-09

I think so, would not suffice adding `or -1` as clarification?
> 2. If the absolute value of len is greater than zero, but less than 1 device pixel,
round it away from zero to 1 or -1 device pixel.

because you can't change the sign of number by rounding it "away from zero".

<!-- comment id=4435440468 author=tabatkins created=2026-05-12T22:43:53Z url=https://github.com/w3c/csswg-drafts/issues/13794#issuecomment-4435440468 -->
## @tabatkins — 2026-05-12

Yup, just an oversight in that spot. Edited to say "or -1".
