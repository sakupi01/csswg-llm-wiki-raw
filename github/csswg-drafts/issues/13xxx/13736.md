---
number: 13736
title: "[css-values-5] sibling-index()/count() should return 1, not 0, when the tree ref fails"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-values-5"]
author: "tabatkins"
created_at: "2026-03-27T21:28:01Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13736"
comments: 2
has_resolution: true
synced_at: "2026-07-04T04:45:46Z"
---

The [Tree-Counting Functions](https://drafts.csswg.org/css-values-5/#tree-counting) use tree-scoped name/ref machinery to avoid exposing DOM information from the shadow up to the light tree. 

The spec currently says that when this mechanism successfully blocks a tree-counting function, it causes it to resolve to 0. But 0 is not a value that can ever be returned by tree-counting functions normally; the smallest possible value is 1, when the element is by itself, and thus is also index 1 among its siblings.

I don't think there's a good reason to return 0 in particular here, and doing so would screw up code that, does, say, `calc( (sibling-index() - 1) * 20px)` to give a gradually-increasing value; it would get `-20px` with the current spec when the ref failed.

I think we should return 1 for both of these functions.

<!-- comment id=4791604896 author=css-meeting-bot created=2026-06-24T16:44:51Z url=https://github.com/w3c/csswg-drafts/issues/13736#issuecomment-4791604896 resolution=true -->
## @css-meeting-bot — 2026-06-24

The CSS Working Group just discussed `[css-values-5] sibling-index()/count() should return 1, not 0, when the tree ref fails`, and agreed to the following:

* `RESOLVED: if sibling-index() or sibling-count() fails to find a thing to count, it will return 1`

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> +1<br>
&lt;kbabbitt> fantasai: 1 seems fine to me<br>
&lt;flackr> +1<br>
&lt;kizu> +1<br>
&lt;kbabbitt> astearns: Proposed resolution: if sibling-index() or sibling-count() fails to find a thing to count, it will return 1<br>
&lt;lea> +1<br>
&lt;kbabbitt> sgtm<br>
&lt;kbabbitt> RESOLVED: if sibling-index() or sibling-count() fails to find a thing to count, it will return 1<br>
</details>

<!-- comment id=4806617328 author=kbhomes created=2026-06-26T05:25:12Z url=https://github.com/w3c/csswg-drafts/issues/13736#issuecomment-4806617328 -->
## @kbhomes — 2026-06-26

I've updated the existing tree-scoped WPT as part of a Firefox patch: https://github.com/web-platform-tests/wpt/pull/60895
