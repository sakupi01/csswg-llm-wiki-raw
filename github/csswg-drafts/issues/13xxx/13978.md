---
number: 13978
title: "[css-values-5] Are sibling-count() and sibling-index() math functions?"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-values-5"]
author: "kbhomes"
created_at: "2026-05-28T13:07:05Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13978"
comments: 6
has_resolution: true
synced_at: "2026-08-13T02:45:19Z"
---

The [specification section](https://drafts.csswg.org/css-values-5/#tree-counting) for tree counting functions isn't explicit about these being math functions or not, other than by omission of the phrase "math function". Is there a definitive answer to whether they are math functions, and why not if they aren't? The behavior seems like it would be the same, other than one case: the serialization of `calc(sibling-index())`. That would simplify and serialize to `sibling-index()` as a math function, or remain as `calc(sibling-index())` if it was a numeric value / leaf node. I don't believe there are tests disambiguating this currently.

It should be noted that Chrome and Safari both appear to treat these as math functions in their implementations, based on their serialization of `calc(sibling-index())`.

```js
document.body.style.zIndex = 'calc(sibling-index())';
console.log(document.body.style.zIndex);
// "sibling-index()" in both Chrome and Safari
```

I'm working on the Firefox implementation for these functions and we're discussing whether to implement them as math functions or not. Implementing them as math functions appears to be simpler than adding support in calc trees _and_ number/integer parsing, and matches Chrome and Safari, but appears to deviate from specification on that serialization case if it isn't a math function.

<!-- comment id=4583561690 author=weinig created=2026-05-30T17:31:05Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4583561690 -->
## @weinig — 2026-05-30

We have discussed this a bit before in a few places, https://github.com/w3c/csswg-drafts/issues/10919 comes to mind. 

They are not math functions.

It looks like you are right, and WebKit and Chrome serialize `calc(sibling-index())` incorrectly.

My personal opinion is that the serialization WebKit and Chrome have right now is preferable and more consistent, so if it turns out the spec requires maintaining the outer calc(), I think it would make sense to change it to not require that.

cc @tabatkins 

<!-- comment id=4583595261 author=weinig created=2026-05-30T17:46:09Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4583595261 -->
## @weinig — 2026-05-30

There is also this issue about calc serialization which I think plays into this as well https://github.com/w3c/csswg-drafts/issues/12059.

<!-- comment id=4594505604 author=cdoublev created=2026-06-01T16:27:46Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4594505604 -->
## @cdoublev — 2026-06-01

The other difference is that math-functions are clamped and possibly rounded ([spec](https://drafts.csswg.org/css-values-4/#calc-range)):

  > Parse-time range-checking of values is not performed within math functions, and therefore out-of-range values do not cause the declaration to become invalid.

  > Additionally, if a math function that resolves to `<number>` is used somewhere that only accepts `<integer>`, the computed value and used value are rounded to the nearest integer, in the same manner as clamping, above.

So given the property `foo` defined with `<number [-infinity, 0]>`, `calc(1)` would be valid but not `sibling-index()`.

I agree that it would be probably be better to treat math functions and tree-counting functions in the same way, perhaps as *calculations*?

I expressed this concern in #10979 (perhaps not clearly), fwiw.

(edit) There is a [resolution](https://github.com/w3c/csswg-drafts/issues/11040#issuecomment-2770596975) to round non-math functions (there are none atm) to `<integer>` when they resolve to `<number>`.

<!-- comment id=4595495598 author=Loirooriol created=2026-06-01T18:45:53Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4595495598 -->
## @Loirooriol — 2026-06-01

Somewhat related discussion: [#11204 - What is meant by the distinction between "math function" vs. "function that evaluates to a `<number>`"](https://github.com/w3c/csswg-drafts/issues/11204)

<!-- comment id=4606578252 author=tabatkins created=2026-06-02T20:05:35Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4606578252 -->
## @tabatkins — 2026-06-02

They are indeed not math functions, for the reasons explained in #11204, yeah. A function is defined as a math function if it, well, does math on values and needs to inherit the [calculation context](https://drafts.csswg.org/css-values-4/#calc-context) from where it's used. Sibling count/index don't do this, they're just functions that return numbers.

@cdoublev points out the reason why the `calc()` wrapper should be maintained - if the context it's used in has a limited range, a `calc(N)` outside that range gets clamped to the allowed range, and a `calc(sibling-index())` should do the same. Dropping the calc() loses us that ability.

But also as Guillaume points out, the *integer-rounding* behavior of math functions has already been extended in general to number-returning functions, and I think it would be reasonable to also extend the clamping, for the same reason that math functions clamp. (You can't check the range at parse time, and we don't want to have to define special error behavior just for dynamic functions that can't be triggered by hand.)

So yeah, Agenda+ for two simple resolutions:

* Extend the resolution from #11040 (all number-returning functions round to `<integer>` when needed) to also cover range-clamping.
* Modify calculation serialization to drop the calc() wrapper if the top-level node is just a lone number-returning function, now that the previous resolution makes the wrapper unnecessary.

<!-- comment id=5205460222 author=css-meeting-bot created=2026-08-06T13:45:12Z url=https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-5205460222 resolution=true -->
## @css-meeting-bot — 2026-08-06

The CSS Working Group just discussed `[css-values-5] Are sibling-count() and sibling-index() math functions?`, and agreed to the following:

* `RESOLVED: All functions that always return either <integer> or <number> are "numeric functions", and these are range-clamped, rounded to <integer> in integer contexts, and are not wrapped in calc() when serialized (because the relevant behavior is built in).`

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> q+<br>
&lt;bramus> emilio: sajid is firefox contributor … can present<br>
&lt;bramus> … we want to make them an arbitrary substitution function<br>
&lt;bramus> … so it composes well with ident() and stuff<br>
&lt;bramus> keithamus: like a color scale<br>
&lt;bramus> emilio: if you do ident(s-index())<br>
&lt;bramus> … it only works<br>
&lt;astearns> ack emilio<br>
&lt;bramus> … nah, it works<br>
&lt;bramus> TabAtkins: currently, the sibling-* are ordainary functions that resolve to numbers<br>
&lt;bramus> … not math functions<br>
&lt;bramus> … dont need to be arbitrary substitution functions either<br>
&lt;bramus> … functions that accept numbes already know to handle calc()_<br>
&lt;bramus> … calling them math functions might make sense<br>
&lt;bramus> emilio: easier to treat them as that<br>
&lt;bramus> … same way you resolve ems, you can now go to element (calculation context)<br>
&lt;bramus> … can also compute function to number itself<br>
&lt;bramus> … steps you need to take are not different from any other length unit and since you need to stash them in ??? implement once in calc tree versus inside+outside<br>
&lt;bramus> TabAtkins: this appears to be a general arg for any numeric functions<br>
&lt;kbabbitt> q+<br>
&lt;bramus> … any future fns that dont do calc but somehow return a number<br>
&lt;bramus> emilio: unless they can resolve at parse time<br>
&lt;bramus> … spec wise only meaningful diff is serialization<br>
&lt;bramus> TabAtkins: yes<br>
&lt;bramus> … for that, numbers, math fns, even if we resolve them away, we serialize with calc() wrapper<br>
&lt;bramus> emilio: no, a min is not a calc<br>
&lt;bramus> TabAtkins: but once you resolve away<br>
&lt;bramus> emilio: yes<br>
&lt;bramus> TabAtkins: it will retain fact that its a calc tree<br>
&lt;bramus> … for reason of auto clamping<br>
&lt;bramus> … so sibling-index() and cmoputed value might see calc(1)<br>
&lt;bramus> emilio: no, bc you can clamp at sibling index at computed value time<br>
&lt;bramus> astearns: maybe go to queueu?<br>
&lt;astearns> ack kbabbitt<br>
&lt;bramus> kbabbitt: if I follow correct, we talk about a class of tings that are valid in calc tree that ar enot mumbers or operators<br>
&lt;bramus> … wodering if there other things in that class, like color channel names<br>
&lt;bramus> TabAtkins: those are calc keywords<br>
&lt;bramus> … can use pi and e in there<br>
&lt;bramus> kbabbitt: gotcha<br>
&lt;emilio> document.body.style.zIndex // "calc(1)"<br>
&lt;emilio> getComputedStyle(document.body).zIndex // 1<br>
&lt;bramus> TabAtkins: other possibliy: channel extractor functions: fall under general argument<br>
&lt;astearns> ack fantasai<br>
&lt;bramus> fantasai: in the issue, there was a question about them being math functions or a general category of fns that get clamped/rounded but not math fns<br>
&lt;bramus> … and tab you have two resolutions you asked for?<br>
&lt;fantasai> https://github.com/w3c/csswg-drafts/issues/13978#issuecomment-4606578252<br>
&lt;bramus> … one to say that all number returning fns also do range clamping<br>
&lt;bramus> … and to modify calc() serializaton for them so that they dont need the calc() wrapper<br>
&lt;bramus> … what would be distionction between doing that and making them any math function?<br>
&lt;bramus> TabAtkins: no author observable diff<br>
&lt;bramus> … if possible, would like to preserver math function keeping its meaning … carrying calc context with ith – OH, thats potentially important<br>
&lt;emilio> q+<br>
&lt;bramus> … it has a purpose for being of a defined class, and woul dlike to not use that for other things, but not make it observable<br>
&lt;bramus> emilio: can take of particular element as calculation context<br>
&lt;emilio> ack emilio<br>
&lt;bramus> TabAtkins: no, thats, what are the things that a number in that spot needs to know abut itself (e.g percentage resolving)<br>
&lt;bramus> … that is what a math fn brings you (in spec terms)<br>
&lt;astearns> ack fantasai<br>
&lt;bramus> fantasai: proposal to resolve on the two different behavioral differences<br>
&lt;bramus> … and create a term for all those functions that have that<br>
&lt;bramus> … does not need to be math functions<br>
&lt;bramus> TabAtkins: yeah, numeric functions<br>
&lt;bramus> fantasai: create a category for that, include math fns, and they do not need to wrapped in calc as their range is clamped automatically and then make sibling-* be of that type<br>
&lt;bramus> TabAtkins: definition of numeric function would be any function that returns number or int<br>
&lt;bramus> fantasai: yes, and all such functions are range clamped<br>
&lt;bramus> TabAtkins: and will auto round if used where int is expected<br>
&lt;dbaron> (I presume you mean "always returns &lt;number> or &lt;integer>" rather than just "sometimes returns &lt;number> or &lt;integer>"?)<br>
&lt;fantasai> PROPOSED: All functions returning either &lt;integer> or &lt;number> are "numeric functions", and these are range-clamped, rounded to &lt;integer> in integer contexts, and are not wrapped in calc() when serialized.<br>
&lt;bramus> TabAtkins: yes dbarond, because if its sometimes it is a arbitrary substitution function<br>
&lt;fantasai> PROPOSED: All functions that always return either &lt;integer> or &lt;number> are "numeric functions", and these are range-clamped, rounded to &lt;integer> in integer contexts, and are not wrapped in calc() when serialized.<br>
&lt;keithamus> s/dbarond/dbaron/<br>
&lt;fantasai> PROPOSED: All functions that always return either &lt;integer> or &lt;number> are "numeric functions", and these are range-clamped, rounded to &lt;integer> in integer contexts, and are not wrapped in calc() when serialized (because the relevant behavior is built in).<br>
&lt;bramus> astearns: final?<br>
&lt;kbabbitt> lgtm<br>
&lt;TabAtkins> (kizu go to jail)<br>
&lt;bramus> … objections?<br>
&lt;fantasai> RESOLVED: All functions that always return either &lt;integer> or &lt;number> are "numeric functions", and these are range-clamped, rounded to &lt;integer> in integer contexts, and are not wrapped in calc() when serialized (because the relevant behavior is built in).<br>
&lt;bramus> … hearing none, we are resolved<br>
&lt;bramus> astearns: do we need extra resolution for the sibling-* functions?<br>
&lt;bramus> TabAtkins: no, these are covered by that<br>
&lt;bramus> … more fns coming<br>
&lt;TabAtkins> nucsslear physics<br>
</details>
