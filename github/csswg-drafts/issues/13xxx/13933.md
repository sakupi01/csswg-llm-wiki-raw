---
number: 13933
title: "[css-variables] Remove \"animation taint\" when inheriting values"
state: "open"
labels: ["css-variables-1"]
author: "tabatkins"
created_at: "2026-05-15T22:40:06Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13933"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:42Z"
---

[spec link](https://drafts.csswg.org/css-variables/#animation-tainted)

Currently, if a custom property is animated, its value is considered "animation-tainted", and this taint follows it through substitution into other custom properties. An animation-tainted value can't be used in any of the animation properties, as that would create cyclic issues.

In <https://bsky.app/profile/janeori.propjockey.io/post/3migosaiets2y>, @JaneOri points out that this restriction isn't necessary once the custom property value is inherited, as a child can no longer influence how a parent animates. If the custom property only has a discrete set of possible values, you can even use a CQ to manually test and set the values onto a new custom property on the child, *effectively* inheriting it but without the taint. That's a little silly, of course, and doesn't work for continuous-value custom props.

We should probably remove the taint when inheriting. This'll require some rewriting of the concept: as written, the spec today taints *the custom property itself* if it's mentioned in a `@keyframes` rule, not the *value* of a custom property that comes from an active animation. I don't know how browsers actually implement the tainting today, tho.

Thoughts? @lilles @andruud @emilio etc?

<!-- comment id=4496486409 author=andruud created=2026-05-20T09:00:19Z url=https://github.com/w3c/csswg-drafts/issues/13933#issuecomment-4496486409 -->
## @andruud — 2026-05-20

> This'll require some rewriting of the concept: as written, the spec today taints _the custom property itself_ if it's mentioned in a `@keyframes` rule, not the _value_ of a custom property that comes from an active animation. I don't know how browsers actually implement the tainting today, tho.

Blink indeed taints the _value_. (So e.g. `--x: 42 !important` would give a non-tainted computed value for `--x`, even a current animation on `--x` adds some effect value to the cascade.)

> This'll require some rewriting of the concept: as written, the spec today taints _the custom property itself_

That could make sense for separate reasons, but I don't think it's _required_ for this? I think this just means animation-taint information now only lives during the style resolution of a single element, and is never stored on the computed style in the first place (thus no taint to actively remove on inheritance)? Instead, `var()` etc would check the temporary taint info during substitution.
