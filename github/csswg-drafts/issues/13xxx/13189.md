---
number: 13189
title: "[css-values-5] Unit cycles via if()"
state: "open"
labels: ["Closed Accepted as Obvious Bugfix", "Needs Edits", "css-values-5"]
author: "andruud"
created_at: "2025-12-08T14:03:45Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13189"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:37Z"
---

I think we're missing some cycle detection handling for `if()`. In the following example, the `writing-mode` depends on the computed `font-size`, and vice-versa:

```css
:root {
  width: 100px;
  height: 80px;
}

div {
  writing-mode: if(
    style(1em = 10px): vertical-rl;
    else: horizontal-tb);
  font-size: calc(100vi / 10);
}
```

As of https://github.com/w3c/csswg-drafts/issues/6026, we can run into cycles via `overflow`, `scrollbar-gutter`, and `scrollbar-width` as well, e.g.:

```css
:root {
  width: 100px;
  overflow: if(
    style(100px = 100vw): scroll;
    else: hidden;
  );
}
```

We should be able to fix this by [guarding](https://drafts.csswg.org/css-values-5/#guarded) the appropriate substitution context for a given unit seen in the `<style-query>`. For example, `style(1em = ...)` would guard `font-size`; `style(1vw = ...)` would guard `scrollbar-gutter`, and `scrollbar-width` on the root element; `style(1vi = ...)` would guard `writing-mode`.

<!-- comment id=4145213087 author=tabatkins created=2026-03-27T20:56:27Z url=https://github.com/w3c/csswg-drafts/issues/13189#issuecomment-4145213087 -->
## @tabatkins — 2026-03-27

I agree here. The use of property-dependent units should, implicitly, guard the dependent property, same as if you were referencing the property directly. `1em` with `font-size` is no different than `1--em` with `--em`.
