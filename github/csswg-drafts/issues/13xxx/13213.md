---
number: 13213
title: "[css-values] Proposal: a way to revert a property to use presentational hints if any"
state: "open"
labels: []
author: "benface"
created_at: "2025-12-13T04:11:04Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13213"
comments: 28
has_resolution: false
synced_at: "2026-07-04T04:45:32Z"
---

### Problem

Imagine you're building a "drop-in UI" / "widget" kind of library. You don't control the environment (website/app/whatever) that's going to render your widget. How do you ensure that the CSS you write for it won't break due to author styles from the embedding page (e.g. weird/unexpected CSS resets)? Ideally, you would use a custom element and the style encapsulation of shadow DOM. But let's imagine you need it to be in the light DOM for some reason. Correct me if I'm wrong, but I believe that the next best thing, at least once [the resolution of the "Allow authors to explicitly place unlayered styles in the cascade layer order" issue](https://github.com/w3c/csswg-drafts/issues/6323#issuecomment-2654364266) is implemented, is to combine a strong cascade layer with `revert` (specifically **not** `revert-layer`, since we need to neutralize author styles across layers), so that elements inside the widget start from a predictable, consistent baseline:

```css
@layer ^my-library {
  @layer reset {
    .my-library-container {
      &,
      &::before,
      &::after,
      *,
      *::before,
      *::after {
        all: revert;
      }
    }
  }

  .my-library-some-element {
    /* now we should be able to confidently style this! */
  }
}
```

Unfortunately, there is *one* issue with that approach: `revert` also discards presentational hints, which breaks otherwise self-contained markup such as `<img width height>` and SVG presentation attributes. Any `<svg>` with `<path>`s in `.my-library-container` will be broken now that `d` is a presentational hint.

### Solution

~A new `presentational` global value that behaves exactly the same as `revert`, except that it does NOT discard presentational hints. Note: I tried to think of a name that starts with `revert-` for consistency, but `revert-hints` or `revert-presentational` sound like they will revert the hints instead of reverting TO them; `revert-to-hints` or `revert-to-presentational` sound unCSSy to me; and anything else I could think of (e.g. `revert-stylesheets` or `revert-css`) sounded unclear or ambiguous. Granted, `presentational` is not perfect either because only a small subset of properties can be influenced by presentational hints, but since the functional difference with `revert` is limited to properties that do, it seems... fine?~

See https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3848545046 and following comments.

<!-- comment id=3649050016 author=Loirooriol created=2025-12-13T06:46:13Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3649050016 -->
## @Loirooriol — 2025-12-13

The usecase doesn't make sense to me. You are distrusting the CSS provided by the author who uses your library, but you are trusting the presentational hints from the markup that was written... by the same author?

<!-- comment id=3649051230 author=benface created=2025-12-13T06:48:09Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3649051230 -->
## @benface — 2025-12-13

@Loirooriol, no. The markup is rendered by the library.

<!-- comment id=3655189220 author=AtkinsSJ created=2025-12-15T11:44:25Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3655189220 -->
## @AtkinsSJ — 2025-12-15

This problem sounds very hypothetical to me.
- Is this a real situation that you've encountered? 
- Why can't you use shadow dom?
- How vital is it that author styling can't affect your widgets?

(I do think `revert` nullifying html attributes is unfortunate, fwiw.)

<!-- comment id=3656323128 author=benface created=2025-12-15T15:41:05Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3656323128 -->
## @benface — 2025-12-15

Oh, sorry for the way I wrote it, but it is not hypothetical at all. It's a very real problem I've encountered when using [Privy's React library](https://docs.privy.io/basics/react/): their login dialog is fully styled, but my project has some opinionated CSS resets that mess with their styles (notably `position: relative`, `min-width: 0` and `min-height: 0`). So the way I fix it now is with this:
```css
#privy-dialog {
  &,
  &::after,
  &::before,
  &::backdrop,
  &::file-selector-button,
  *,
  *::after,
  *::before,
  *::backdrop,
  *::file-selector-button {
    position: revert;
    min-width: revert;
    min-height: revert;
  }
}
```
But I can do that because I know which properties to reset in my project. If Privy wanted to "fix" their library to prevent stuff like this from happening, they would want to use `all: revert`, which does break all the SVG icons in the dialog.

<!-- comment id=3796177291 author=romainmenke created=2026-01-25T08:13:20Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3796177291 -->
## @romainmenke — 2026-01-25

Is it correct that your setup looks something like this:

```css
@layer privy {
  #privy-dialog {
    position: absolute;
  }
}

@layer reset {
  * {
    position: relative;
  }
}

/* ... more styles and layers for your project */
```

With all the 3rd party (privy) stuff in earlier layer than your reset?

<!-- comment id=3796883704 author=benface created=2026-01-25T16:14:12Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3796883704 -->
## @benface — 2026-01-25

@romainmenke – No. Privy itself doesn't use layers, and I don't control where it injects its CSS (which doesn't matter because **I** use layers, so their styles would win regardless of order). My setup is more like this:

```css
@layer base {
  * {
    position: relative;
    min-width: 0;
    min-height: 0;
  }
  /* more base styles */
}

@layer components {
  /* component styles */
}

@layer utilities {
  /* utility styles */
}

@layer vendor-reset {
  #privy-dialog {
    &,
    &::after,
    &::before,
    &::backdrop,
    &::file-selector-button,
    *,
    *::after,
    *::before,
    *::backdrop,
    *::file-selector-button {
      position: revert;
      min-width: revert;
      min-height: revert;
    }
  }
}
```

<!-- comment id=3796949571 author=romainmenke created=2026-01-25T16:56:14Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3796949571 -->
## @romainmenke — 2026-01-25

> No. Privy itself doesn't use layers

You could import their styles in a layer (e.g. `@import "privy.css" layer(privy)`), that is why I ask.

---- 

So the issue is more that your reset sets a bunch of property values and that privy has subtle dependencies on **default** values for some of those properties.

For example the default value for `position` is `static` and maybe that is what privy expects.
But your reset sets everything to `position: relative;`

Correct?

-----

Isn't this a good use case for `@scope`?

Your resets could have end scopes that align with your 3rd party dependencies.

```css
@scope (html) to (#privy-dialog) {
  * {
    position: relative;
    min-width: 0;
    min-height: 0;
  }
}
```

<!-- comment id=3797003744 author=benface created=2026-01-25T17:42:13Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3797003744 -->
## @benface — 2026-01-25

> You could import their styles in a layer (e.g. `@import "privy.css" layer(privy)`), that is why I ask.

I don't think so. As far as I know, they don't expose their stylesheet directly; I believe they inject it automatically when their dialog component is imported or rendered. But even if it was possible, I'm not sure I understand how it would help?

> So the issue is more that your reset sets a bunch of property values and that privy has subtle dependencies on **default** values for some of those properties.
>
> For example the default value for `position` is `static` and maybe that is what privy expects.
But your reset sets everything to `position: relative;`
>
> Correct?

Exactly. 💯

> Isn't this a good use case for `@scope`?

Yes! The `@scope (html) to (#privy-dialog)` solution you proposed makes sense from the consumer's point of view, but this issue is about what a component library can do to prevent issues like that in the first place, so consumers don't have to do anything. I believe a more complete / foolproof solution from a library's point of view would _also_ use `@scope` though, [as I wrote in #11002](https://github.com/w3c/csswg-drafts/issues/11002#issuecomment-3795639646).

<!-- comment id=3800770747 author=mirisuzanne created=2026-01-26T17:26:05Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3800770747 -->
## @mirisuzanne — 2026-01-26

I do think authors expect `revert` to work without overriding presentational hints. So I can see use-cases for a `revert-as-expected` feature. I think those use-cases a generally the same as `revert`, just with the expected behavior.

---

But given this use-case I have many of the same questions being asked above. 

We've established that scope works as a consumer-side solution. Doesn't it seem like there are also several existing package-side solutions? Is there a reason the package authors can't either use shadow DOM, or clarify the default styles they are currently relying on?

The main reason I would imagine avoiding shadow dom as a package author is if I want things to be stylable -- at which point I'm putting it back on the consumer to handle any conflicts. I can't both protect and expose the package contents at the same time. If they are exposed, the consumer can cause issues and will also have to solve those issues.

The main reason not to clarify required defaults is because there might be a lot of them. That's why there are defaults – so we don't have to be explicit about 500+ properties every time. But again: it seems to me like a package should either let consumers style the widget, in which case the consumer-side fix is the right approach to a consumer-side break – or they have to keep the package from being styled, in which case it should be using shadow DOM?

<!-- comment id=3800794577 author=benface created=2026-01-26T17:31:47Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3800794577 -->
## @benface — 2026-01-26

Totally reasonable, @mirisuzanne.

> I do think authors expect `revert` to work without overriding presentational hints. So I can see use-cases for a `revert-as-expected` feature. I think those use-cases a generally the same as `revert`, just with the expected behavior.

Let's restart this thread from scratch with this in mind. Is it too late to change how `revert` works?

<!-- comment id=3800836080 author=mirisuzanne created=2026-01-26T17:41:50Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3800836080 -->
## @mirisuzanne — 2026-01-26

I don't know if `revert` can be changed, or if we would (likely) need a new value for it - but you could edit the title and problem statement to reflect that. It's a feature I've seen many authors working around in different ways. I'm trying to remember where I've seen more extensive articles – maybe from @mayank99 or @kizu?

<!-- comment id=3800877976 author=mayank99 created=2026-01-26T17:51:32Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3800877976 -->
## @mayank99 — 2026-01-26

> I'm trying to remember where I've seen more extensive articles – maybe from @mayank99 or @kizu?

This is what I've written on the topic: https://mayank.co/blog/revert-layer

The `revert-layer` workaround is fine when there are no layers being used or when you control all the layers, but it doesn't help when the layers are created by third parties.

`revert-as-expected` or `revert-all-layers` would be very useful in such cases.

<!-- comment id=3848545046 author=Loirooriol created=2026-02-04T16:46:33Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3848545046 -->
## @Loirooriol — 2026-02-04

I can't find it but I think there is already some issue about a functional notation, e.g. `revert-layer(2)` could revert 2 layers instead of 1. Then this could be `rever-layer(all)`.

<!-- comment id=3849139866 author=benface created=2026-02-04T18:43:21Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849139866 -->
## @benface — 2026-02-04

That would be amazing @Loirooriol! Much better than `presentational`. I will try to find it.

<!-- comment id=3849143645 author=benface created=2026-02-04T18:44:15Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849143645 -->
## @benface — 2026-02-04

Maybe you were thinking of https://github.com/w3c/csswg-drafts/issues/10416?

<!-- comment id=3849188256 author=mirisuzanne created=2026-02-04T18:55:13Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849188256 -->
## @mirisuzanne — 2026-02-04

That's probably the one. I think we'd potentially want these options… 

- `revert-layer(<integer>)` - number of layers to revert
- `revert-layer(<name>)` - revert all layers above and including the named layer
- `revert-layer(to <name>)` - revert all layers above, so we get the value from the named layer
- `revert-layer(<something-that-gets-us-hints>)` - what this feature is about

If we're ever allowing named layers in this function, we have potential conflict with custom idents – so we might want to use a reserved name like `initial` rather than `all`? Or we need some other way to distinguish the syntax, because `all` is a valid layer name.

<!-- comment id=3849464496 author=benface created=2026-02-04T19:58:51Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849464496 -->
## @benface — 2026-02-04

If `initial` is not a valid layer name, then I think `revert-layer(initial)` could make sense, if we consider unlayered styles to be "the initial layer".

<!-- comment id=3849625243 author=mayank99 created=2026-02-04T20:37:10Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849625243 -->
## @mayank99 — 2026-02-04

`revert-layer(to initial)` would probably be more intuitive than `revert-layer(initial)`. The latter does not sound like it would revert all styles (layered and unlayered).

Related: In https://github.com/w3c/csswg-drafts/issues/10094, I had proposed making `@layer initial` the "first" layer. There were some issues with that proposal, but it could be resurrected if we are saying `initial` == presentational hints.

<!-- comment id=3849675386 author=benface created=2026-02-04T20:49:09Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849675386 -->
## @benface — 2026-02-04

Huh, very interesting @mayank99. Looks like you proposed a different at-rule for the same concept in https://github.com/w3c/csswg-drafts/issues/11751, maybe because `@layer initial` would work differently than any other layer (especially when nested in another layer; I'm guessing it would have to "escape" any layer it's in)?

<!-- comment id=3849714990 author=mayank99 created=2026-02-04T20:59:43Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849714990 -->
## @mayank99 — 2026-02-04

@benface Nesting was indeed one of the concerns. `@layer foo.initial` probably doesn't make sense.

I guess for the current issue, `revert-layer(to initial)` or `revert-layer(initial)` could be special cased to always escape nesting, whereas `revert-layer(<sublayer-name>)` would be scoped to the current layer.

<!-- comment id=3849747023 author=benface created=2026-02-04T21:06:56Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849747023 -->
## @benface — 2026-02-04

Actually, you make me realize that `revert-layer(<sublayer-name>)` could be ambiguous because it is valid to give a layer the same name as a parent layer. Imagine this:
```css
/* unlayered styles */

@layer unimportant {
  /* styles that are overridden by the unlayered styles */

  @layer unimportant {
    /* styles that are even less important, overridden by the above */

    @layer foo {
      something: revert-layer(unimportant);
    }
  }
}
```
Which `unimportant` layer does `revert-layer(unimportant)` revert?  Probably the nearest one, but what if the user wants to revert the outer one?

<!-- comment id=3849997102 author=mirisuzanne created=2026-02-04T22:09:31Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3849997102 -->
## @mirisuzanne — 2026-02-04

> Which unimportant layer does revert-layer(unimportant) revert? Probably the nearest one, but what if the user wants to revert the outer one?

It can't be the outer one, because layers can get wrapped on import - so a file doesn't know what outer layers might exist. Real bad to accidentally revert layers you don't know about. 

It seems to me the main use-cases are:

- I want to revert the current layer (`revert-layer`)
- I know the name of a nearby layer I want to revert _to_, or revert _past_
- I want to revert all the way to presentational hints
- I want to revert to browser defaults (`revert`)

Both the `<integer>` option and 'referencing the same name higher up the stack' are kinda specific use-cases that authors could work around with a more intentional name scheme. Maybe we don't need them at all. But if we do, `revert-layer(to? <name> <integer>)` could handle reverting a given number of layers with the same name? But I'm not sure that kind of complexity is needed?

<!-- comment id=3888000286 author=mayank99 created=2026-02-12T00:37:44Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888000286 -->
## @mayank99 — 2026-02-12

Today I ran into another use-case: I want to **revert only one layer and all its sublayers**. This seems different from the use-cases listed above because I want to preserve all the other layers that come before _and_ after this layer.

To solve this today, I had to inspect all the sublayer names and write this very tedious ruleset:
```css
*:not(#a#b) {
  @layer thirdpartylib {
    all: revert-layer;
    @layer A { all: revert-layer; }
    @layer B { all: revert-layer; }
    @layer C { all: revert-layer; }
    // …
  }
}
```
<details>
<summary>Surrounding setup</summary>

```css
@layer reset, thirdpartylib, page;
@import "thirdpartylib.css" layer(thirdpartylib);
```

</details>

As you can imagine, this is somewhat brittle because the sublayer names (`A`, `B` …) might change drastically if I update `thirdpartylib` to a newer version.

It would be nice if I could just do `all: revert-all-layers` inside `@layer thirdpartylib` to revert all the sublayers in one go.

Tying it back to the original issue, `all: revert-all-layers` when used unlayered could revert all top-level layers (including sublayers), back to presentational hints.

<!-- comment id=3888113902 author=benface created=2026-02-12T01:09:49Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888113902 -->
## @benface — 2026-02-12

@mayank99 – Doesn't that first `all: revert-layer` have precedence over nested layers though?

<!-- comment id=3888190057 author=mayank99 created=2026-02-12T01:40:07Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888190057 -->
## @mayank99 — 2026-02-12

> Doesn't that first `all: revert-layer` have precedence over nested layers though?

@benface Yes, it has "precedence" in the cascade, but it only reverts the styles that are outside the sublayers, i.e. it reverts from `@layer thirdpartylib` to `@layer thirdpartylib.C` (last sublayer). You still need to revert the sublayers individually.

<!-- comment id=3888357764 author=benface created=2026-02-12T02:43:18Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888357764 -->
## @benface — 2026-02-12

Huh, I see. Then I agree that it is very much needed.

> Tying it back to the original issue, `all: revert-all-layers` when used unlayered could revert all top-level layers, back to presentational hints.

That wouldn't quite solve my use case though: I want something that works regardless of which layer we're currently in, just like `revert` (simply without overriding presentational hints).

<!-- comment id=3888386211 author=Loirooriol created=2026-02-12T02:55:17Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888386211 -->
## @Loirooriol — 2026-02-12

@mayank99 That seems reasonable, but it sounds more like `revert-sub-layers` or `revert-to-parent-layer`, rather than `revert-all-layers`

<!-- comment id=3888414150 author=mayank99 created=2026-02-12T03:05:13Z url=https://github.com/w3c/csswg-drafts/issues/13213#issuecomment-3888414150 -->
## @mayank99 — 2026-02-12

Yeah, a different name or syntax would be clearer. 

I can see `revert-layer(to initial)` working, if you imagine `initial` as the "initial" (default) state of the layer you're in. 

- When unlayered, the `initial` state is "presentational hints".
- When inside a layer, the `initial` state is "empty" (nothing in that layer).
