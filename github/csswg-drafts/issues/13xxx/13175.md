---
number: 13175
title: "[web-animations-2] Make TimelineTrigger interface support multiple timelines"
state: "open"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Edits", "Needs Testcase (WPT)", "animation-triggers-1"]
author: "DavMila"
created_at: "2025-12-01T19:57:13Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13175"
comments: 9
has_resolution: false
synced_at: "2026-07-04T04:45:36Z"
---

A bit of background due to the out-of-date `AnimationTrigger` spec: since the `AnimationTrigger` [interface](https://drafts.csswg.org/web-animations-2/#the-animationtrigger-interface) was initially written, we've [resolved](https://github.com/w3c/csswg-drafts/issues/12336#issuecomment-3206545734) to have separate CSS property namespaces for different types of animation triggers, i.e. `event-trigger`, `timeline-trigger`. Accordingly, the web-animations spec will be updated to have different interfaces, i.e. `EventTrigger`, `TimelineTrigger`.  `TimelineTrigger` will likely resemble/replace the current `AnimationTrigger` interface (which was initially conceived for just  timelines).

One of the future extensions to the `timeline-trigger` property will be support for multiple timelines. This will be important for use cases such as [pointer timelines](https://drafts.csswg.org/pointer-animations-1/#pointer-timelines) whose use cases are likely to hinge on 2-D pointer intersections, e.g. an author can declare a trigger range as follows: `timeline-trigger: --pointer-trigger pointer(x) pointer(y)` where pointer(x) and pointer(y) declare [pointer-timelines](https://drafts.csswg.org/pointer-animations-1/#funcdef-pointer).


Given this, I propose the `TimelineTrigger` interface (currently in the outdated spec as `AnimationTrigger`) have its members as lists rather than single items, i.e. the following:

```
interface TimelineTrigger {
   attribute sequence<AnimationTimeline> timelineList;
   attribute sequence<any> rangeStartList;
   attribute sequence<any> rangeEndList;
   attribute sequence<any> exitRangeStartList;
   attribute sequence<any> exitRangeEndList;
}
```

cc/ @ydaniv @tabatkins @flackr 

<!-- comment id=3602276748 author=DavMila created=2025-12-02T14:14:26Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3602276748 -->
## @DavMila — 2025-12-02

Here is a summary of the change:

The [current interface](https://drafts.csswg.org/web-animations-2/#the-animationtrigger-interface) is this:
```
interface TimelineTrigger {
   attribute AnimationTimeline timeline;
   attribute any rangeStart;
   attribute any rangeEnd;
   attribute any exitRangeStart;
   attribute any exitRangeEnd;
}
```

I am proposing we change it to the following:

```
interface TimelineTrigger {
   attribute sequence<AnimationTimeline> timelineList;
   attribute sequence<any> rangeStartList;
   attribute sequence<any> rangeEndList;
   attribute sequence<any> exitRangeStartList;
   attribute sequence<any> exitRangeEndList;
}
```

Concerning the choice of *List as a way to pluralize the fields, we have some precedents in [HTMLElement.relList](https://html.spec.whatwg.org/multipage/semantics.html#dom-link-rellist) and [Element.classList](https://dom.spec.whatwg.org/#ref-for-dom-element-classlist%E2%91%A0).

<!-- comment id=3603411337 author=flackr created=2025-12-02T18:28:59Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3603411337 -->
## @flackr — 2025-12-02

Agree we should support multiple sources since this is a likely extension to support cases like 2d timelines. It feels a bit odd that it is a bunch of coordinated lists. Would it make sense to have a list of trigger conditions or sources in the top level trigger interface? E.g. something like this:

```
interface TimelineTrigger {
  attribute sequence<TimelineTriggerRange> ranges;
}

interface TimelineTriggerRange {
   attribute AnimationTimeline timeline;
   attribute any rangeStart;
   attribute any rangeEnd;
   attribute any exitRangeStart;
   attribute any exitRangeEnd;
}
```

Open to bikeshedding on the term ranges.

<!-- comment id=3603495564 author=ydaniv created=2025-12-02T18:45:46Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3603495564 -->
## @ydaniv — 2025-12-02

Regarding `timeline => timelineList`, LGTM!

Regarding listfying each range edge separately, I'm not sure this is the ideal API we want to allow, but not sure if I have a better alternative...

Maybe what @flackr suggested?

<!-- comment id=3612886009 author=DavMila created=2025-12-04T15:45:59Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3612886009 -->
## @DavMila — 2025-12-04

> Maybe what [@flackr](https://github.com/flackr) suggested?

Yeah Rob's suggestion sounds good to me.

<!-- comment id=3636577599 author=DavMila created=2025-12-10T11:12:30Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3636577599 -->
## @DavMila — 2025-12-10

I think we're agreeing on doing this. I am putting this up for an async resolution.

The **proposed resolution** is to modify the `TimelineTrigger` interface to support multiple timelines.

old interface: 

```
interface TimelineTrigger {
   attribute AnimationTimeline timeline;
   attribute any rangeStart;
   attribute any rangeEnd;
   attribute any exitRangeStart;
   attribute any exitRangeEnd;
}
```

Proposed interface: 
```
interface TimelineTrigger {
  attribute sequence<TimelineTriggerRange> ranges;
}

interface TimelineTriggerRange {
   attribute AnimationTimeline timeline;
   attribute any rangeStart;
   attribute any rangeEnd;
   attribute any exitRangeStart;
   attribute any exitRangeEnd;
}
```

<!-- comment id=3637046046 author=bramus created=2025-12-10T13:16:22Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3637046046 -->
## @bramus — 2025-12-10

In light of the pending resolution for https://github.com/w3c/csswg-drafts/issues/13174#issuecomment-3636596474, that would immediately become the following then, right?

```
interface TimelineTrigger {
  attribute sequence<TimelineTriggerRange> ranges;
}

interface TimelineTriggerRange {
   attribute AnimationTimeline timeline;
   attribute any activationRangeStart;
   attribute any activationRangeEnd;
   attribute any activeRangeStart;
   attribute any activeRangeEnd;
}
```

<!-- comment id=3637223029 author=DavMila created=2025-12-10T13:59:10Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3637223029 -->
## @DavMila — 2025-12-10

> In light of the pending resolution for [#13174 (comment)](https://github.com/w3c/csswg-drafts/issues/13174#issuecomment-3636596474), that would immediately become the following then, right?
> 

Yep, exactly my thinking.

<!-- comment id=3657610382 author=astearns created=2025-12-15T21:08:27Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3657610382 -->
## @astearns — 2025-12-15

The CSSWG will automatically accept this resolution one week from now if no objections are raised here. Anyone can add an emoji to this comment to express support. If you do not support this resolution, please add a new comment.

Proposed Resolution: Change the TimelineTrigger interface to support multiple timelines in a top-level sequence

<!-- comment id=3683974324 author=astearns created=2025-12-22T20:03:37Z url=https://github.com/w3c/csswg-drafts/issues/13175#issuecomment-3683974324 -->
## @astearns — 2025-12-22

RESOLVED: Change the TimelineTrigger interface to support multiple timelines in a top-level sequence
