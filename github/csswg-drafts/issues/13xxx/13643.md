---
number: 13643
title: "[animation-triggers-1] Should play-forwards and play-backwards have no effect if the animation has already reached its end?"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "animation-triggers-1"]
author: "flackr"
created_at: "2026-03-12T16:28:52Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13643"
comments: 13
has_resolution: true
synced_at: "2026-07-04T04:45:44Z"
---

Currently, the table in [animation-triggers-1](https://drafts.csswg.org/animation-triggers/#animation-trigger-shorthand) says the following above play-forwards and play-backwards:

Keyword | Extra Effect | initial | playing | paused | finished
-- | -- | -- | -- | -- | --
play-forwards | set playback rate to positive | play() | — | play() | play()
play-backwards | set playback rate to negative | play() | — | play() | play()

The reason we wanted to have play-forwards and play-backwards was to support reversing effects, however, if according to the table we call play() when the effect is finished it sounds like we would restart the animation even if it has already reached its end. I think this is not at all what developers would expect.

E.g. suppose a developer sets up an animation on a button:
```css
@keyframes press-animation {
  to: { transform: translate2d(-5px, 5px); }
}

button {
  animation: press-animation forwards;
  event-trigger: --press pointerdown, --depress pointerup;
  animation-trigger: --press play-forward --depress play-backward;
}
```

The intention is clearly that the button should animate to its pressed state, and back, however it may be possible to trigger the pointerup action multiple times (e.g. clicking both buttons one at a time and releasing one at a time). If the user does this, I think the developer would not intend the animation to replay the press animation from the start but rather to just stay pressed.

TLDR; I think we should not play the animation if it is finished after setting the playback rate for `play-forwards` and `play-backwards`

@ydaniv @DavMila 

Note: I think it is not easy to create this degenerate situation with timeline triggers because we don't support multiple triggers yet and stateful triggers won't retrigger the exit state if they didn't first enter the entry state.

<!-- comment id=4053695282 author=bramus created=2026-03-13T09:01:53Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4053695282 -->
## @bramus — 2026-03-13

IIUC you are proposing this:

Keyword | Extra Effect | initial | playing | paused | finished
-- | -- | -- | -- | -- | --
play-forwards | set playback rate to positive | play() | — | play() | ~~play()~~ —
play-backwards | set playback rate to negative | play() | — | play() | ~~play()~~ —

Wouldn’t that prevent `--depress` from actually playing the already finished animation?

> The intention is clearly that the button should animate to its pressed state, and back, however it may be possible to trigger the pointerup action multiple times (e.g. clicking both buttons one at a time and releasing one at a time). If the user does this, I think the developer would not intend the animation to replay the press animation from the start but rather to just stay pressed.

In between the clicking on button1 and the click on button2, that would already trigger `--depress` on button1, no? In that case, it would just revert the already playing animation.

<!-- comment id=4053847298 author=ydaniv created=2026-03-13T09:33:39Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4053847298 -->
## @ydaniv — 2026-03-13

@flackr, yes, as I understood it from stateful events:

> If it’s stateful, it has a binary trigger state associated with it, initially "inactive": while "inactive", it only activates when the defining element receives one of its [enter events](https://drafts.csswg.org/animation-triggers-1/#event-trigger-enter-event), performing an associated enter action and switching its [trigger state](https://drafts.csswg.org/animation-triggers-1/#event-trigger-trigger-state) to "active"; while "active", it only deactivates when it receives one of its [exit events](https://drafts.csswg.org/animation-triggers-1/#event-trigger-exit-event), performing an associated exit action and switching its trigger state back to "inactive".

You can't re-trigger any state, you can only switch states, and therefor making this behavior impossible. I thought that was the whole intention of stateful triggers. So basically, stateful (and timeline) triggers only trigger on state change.

And with stateless, as I understood it, you can re-trigger, since the state is ignored.

So, IIUC, we should simply enphasize that when `finished` the trigger still maintains its state, right?

@bramus,

> <p dir="auto" style="box-sizing: border-box; margin-top: 0px !important; margin-bottom: 16px; color: rgb(240, 246, 252); font-family: -apple-system, &quot;system-ui&quot;, &quot;Segoe UI&quot;, &quot;Noto Sans&quot;, Helvetica, Arial, sans-serif, &quot;Apple Color Emoji&quot;, &quot;Segoe UI Emoji&quot;; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; font-weight: 400; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px; white-space: normal; background-color: rgb(13, 17, 23); text-decoration-thickness: initial; text-decoration-style: initial; text-decoration-color: initial;">IIUC you are proposing this:</p><markdown-accessiblity-table data-catalyst="" style="box-sizing: border-box; display: block; color: rgb(240, 246, 252); font-family: -apple-system, &quot;system-ui&quot;, &quot;Segoe UI&quot;, &quot;Noto Sans&quot;, Helvetica, Arial, sans-serif, &quot;Apple Color Emoji&quot;, &quot;Segoe UI Emoji&quot;; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; font-weight: 400; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px; white-space: normal; background-color: rgb(13, 17, 23); text-decoration-thickness: initial; text-decoration-style: initial; text-decoration-color: initial;">
Keyword | Extra Effect | initial | playing | paused | finished
-- | -- | -- | -- | -- | --
play-forwards | set playback rate to positive | play() | — | play() | ~~play()~~ —
play-backwards | set playback rate to negative | play() | — | play() | ~~play()~~ —
>
> </markdown-accessiblity-table><p dir="auto" style="box-sizing: border-box; margin-top: 0px; margin-bottom: 16px; color: rgb(240, 246, 252); font-family: -apple-system, &quot;system-ui&quot;, &quot;Segoe UI&quot;, &quot;Noto Sans&quot;, Helvetica, Arial, sans-serif, &quot;Apple Color Emoji&quot;, &quot;Segoe UI Emoji&quot;; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; font-weight: 400; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px; white-space: normal; background-color: rgb(13, 17, 23); text-decoration-thickness: initial; text-decoration-style: initial; text-decoration-color: initial;">Wouldn’t that prevent<span> </span><code class="notranslate" style="box-sizing: border-box; font-family: &quot;Monaspace Neon&quot;, ui-monospace, SFMono-Regular, &quot;SF Mono&quot;, Menlo, Consolas, &quot;Liberation Mono&quot;, monospace; font-size: 11.9px; tab-size: 4; white-space: break-spaces; background-color: rgba(101, 108, 118, 0.2); border-radius: 6px; margin: 0px; padding: 0.2em 0.4em;">--depress</code><span> </span>from actually playing the already finished animation?</p>

Not exactly, since you do want to allow triggering a switch to the other state on `finished`.

<!-- comment id=4057082986 author=DavMila created=2026-03-13T18:12:55Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4057082986 -->
## @DavMila — 2026-03-13

Making `play-forwards` a noop if the animation is finished seems reasonable. However, with Rob's example it seems like we catered to that use case when we extended the notion of statefulness to event triggers. I.e. if you wanted to bind the pointerdown and pointerup events as the example is trying to do, you would leverage the statefulness of a single trigger as follows:

```
@keyframes press-animation {
  to: { transform: translate2d(-5px, 5px); }
}

button {
  animation: press-animation forwards;
  event-trigger: --press pointerdown pointerup;
  animation-trigger: --press play-forwards play-backwards;
}
```

This would match the intended behavior right, @flackr?

My reading of the keywords was something like this:

- `play` means no qualification of direction or frequency,
- `play-once` qualifies frequency but not direction,
- `play-forwards` qualifies direction but not frequency.

One more thing to consider is whether a developer might indeed intend to create this independence between the pointerdown and pointerup. Making `play-forwards` a noop makes this use case not possible?

Re: @ydaniv

> You can't re-trigger any state, you can only switch states, and therefor making this behavior impossible.

I think your interpretation of statefulness is right but in Rob's example you'd have two separate triggers (`--press` & `--depress`) whose states are independent of each other so you'd be able to trigger again.

<!-- comment id=4058456810 author=ydaniv created=2026-03-13T22:38:38Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4058456810 -->
## @ydaniv — 2026-03-13

> > You can't re-trigger any state, you can only switch states, and therefor making this behavior impossible.
>
> I think your interpretation of statefulness is right but in Rob's example you'd have two separate triggers (--press & --depress) whose states are independent of each other so you'd be able to trigger again.

Right, I completely missed the fact these are 2 separate triggers!

One (tiny, but important) thing about your example, it needs a `/` between the events as `pointerdown / pointerup` to be stateful:

> If a / is used in the value, the [event trigger](https://drafts.csswg.org/animation-triggers-1/#event-trigger) is stateful; the set of events before the / are the event trigger’s [enter events](https://drafts.csswg.org/animation-triggers-1/#event-trigger-enter-event), while those after the / are the [exit events](https://drafts.csswg.org/animation-triggers-1/#event-trigger-exit-event). (The same events can occur in both sets.)

But, your intent is correct.

Regarding the OP, though the example is a bit contrived, you could think of an example with 2 buttons, which is more likely to occur:

```css
.button-1 {
  event-trigger: --c1 click / click;
}

.button-2 {
  event-trigger: --c2 click / click;
}

.target {
  animation: slide 1s both;
  animation-trigger: --c1 play-forwards play-backwards --c2 play-forwards play-backwards;
}
```

So probably the naive interpretation of the above would be:

1. Click on button 1 slides forwards
2. Click on button 2 slides backwards

But since the state is managed in the trigger, and we have 2 separate triggers, what we actually get is:

1. Click on button 1 slides forwards
2. Click on button 2 slides forwards

And, as I understand it, this is what @flackr is trying to prevent, and I agree, otherwise, having multiple triggers per animation becomes kind of useless.

I'm not sure I have the correct solution for this at the moment, but just wanted to see we're on the same page about the problem.

<!-- comment id=4074288780 author=ydaniv created=2026-03-17T11:28:19Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4074288780 -->
## @ydaniv — 2026-03-17

The problem is that we moved the state to the trigger, when before it was possible to keep it synchronized to the animation's state. And having multiple triggers on a single animation becomes problematic.
Even if we create another state on the animation and manage it, we still need to sync it with the rest of the triggers. If we use the example above it means that click on `button-1` will also flip state of the trigger on `button-2`, and I'm not sure we want to go that way.

-----

Another option I can think of is changing the whole model of "name-clashing" for triggers.
We could say trigger names "add" and don't "clash", they add more event registrations to the same trigger instance. And then if you want multiple triggers it simply means reusing the same name, plus we go back to single trigger per animation, and that would look like:

```css
.button-1 {
  event-trigger: --c click / click;
}

.button-2 {
  event-trigger: --c click / click;
}

.target {
  animation: slide 1s both;
  animation-trigger: --c play-forwards play-backwards;
}
```

This has its pros and cons for sure, but it's also exposing all the control to authors, and it's very explicit about what's happening and where the state is.
One possible down-side could be that it may accidentally "merge" triggers, but that can also be mitigated with `trigger-scope`.
There's also the issue of mixing stateful triggers with stateless triggers. I suppose stateless triggers will simply trigger regardless of state, and also won't affect the state.

<!-- comment id=4118043296 author=flackr created=2026-03-24T12:57:30Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4118043296 -->
## @flackr — 2026-03-24

> And, as I understand it, this is what @flackr is trying to prevent, and I agree, otherwise, having multiple triggers per animation becomes kind of useless.

To be clear, what I thought makes more sense is that trying to play-forwards the animation again (e.g. by clicking the second button) does not restart it. You would still have to click the button again to play-backwards since you created it as a stateful button.

However, you can also create buttons that don't have an exit state and are stateless:
```
.button-1 {
  event-trigger: --c1 click;
}

.button-2 {
  event-trigger: --c2 click;
}

.target {
  animation: slide 1s both;
  animation-trigger: --c1 play-forwards --c2 play-backwards;
}
```

What I thought makes more sense is that `.button-1` will only advance the animation forwards, *not* restart it when it is already finished and you click the button instead. Similarly for `.button-2` except in the reverse direction. These would function as play and rewind buttons buttons like on a tape player.

> Another option I can think of is changing the whole model of "name-clashing" for triggers.
We could say trigger names "add" and don't "clash", they add more event registrations to the same trigger instance. And then if you want multiple triggers it simply means reusing the same name, plus we go back to single trigger per animation, and that would look like:

I think I had mentioned that I was in favour of this in a prior call, I opened #13710 to explicitly discuss it. Unlike timelines where you need to use a single time, there's no reason we can't combine events from different sources as activation / deactivation events for a single named trigger. TLDR, I agree we should do this, but this is a separate issue right?

<!-- comment id=4118212859 author=DavMila created=2026-03-24T13:17:41Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4118212859 -->
## @DavMila — 2026-03-24

> One (tiny, but important) thing about your example, it needs a / between the events as pointerdown / pointerup to be stateful:

Right, thanks.

> Regarding the OP, though the example is a bit contrived, you could think of an example with 2 buttons, which is more likely to occur:

Yeah this seems like a valid example to me so I would support the proposal that play-forwards and play-backwards have no effect if the animation has already reached its end.

> Another option I can think of is changing the whole model of "name-clashing" for triggers.

Like Rob says, this does seem like a separate issue. I think we probably have a lot of options to handle multiple matching names, e.g. `animation-trigger: all(--trigger) play` to mean attach  all, `animation-trigger: all --trigger play` alternatively, etc. 

<!-- comment id=4119620267 author=ydaniv created=2026-03-24T16:22:15Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4119620267 -->
## @ydaniv — 2026-03-24

> What I thought makes more sense is that .button-1 will only advance the animation forwards, not restart it when it is already finished and you click the button instead. Similarly for .button-2 except in the reverse direction. These would function as play and rewind buttons buttons like on a tape player.

So IIUC you want the `play-forwards` `play-backwards` actions to not rewind ever? Or is it a special case when you have multiple triggers on same animation?

The way I see it, you specified 2 stateless triggers, so they should always act on every trigger.
So either you use a stateful trigger here, or we could add 2 new actions like `play-forwards-no-rewind` `play-backwards-no-rewind`.

> I think I had mentioned that I was in favour of this in a prior call, I opened https://github.com/w3c/csswg-drafts/issues/13710 to explicitly discuss it.

That's possible you suggested it and I was too thick to follow at the time (: Thanks for opening!

<!-- comment id=4119670710 author=flackr created=2026-03-24T16:29:44Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4119670710 -->
## @flackr — 2026-03-24

> So IIUC you want the play-forwards play-backwards actions to not rewind ever? Or is it a special case when you have multiple triggers on same animation?
>
> The way I see it, you specified 2 stateless triggers, so they should always act on every trigger.
So either you use a stateful trigger here, or we could add 2 new actions like play-forwards-no-rewind play-backwards-no-rewind.

If you want a restarting animation, you can use play. I thought the reason we added play-forwards and play-backwards was for somewhat stateful non-restarting animations. It would be helpful to have real examples where these actions are being used. If we agree that play direction specific restarting play behavior is the common use case then i'm fine with keeping it as is.

<!-- comment id=4121280089 author=ydaniv created=2026-03-24T20:51:00Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4121280089 -->
## @ydaniv — 2026-03-24

> If you want a restarting animation, you can use play

I thought `play` is the same only doesn't sets `playbackRate`. That's what the spec currently says.

> I thought the reason we added play-forwards and play-backwards was for somewhat stateful non-restarting animations.

I think you're right. But it's not clear in the spec now. We should be more specific, like `play(rewind=false)` etc.
And I think we're still using it for the stateful triggers with non-restarting behavior, but it's the state that's imposing the non-restarting behavior, not the action.

> If we agree that play direction specific restarting play behavior is the common use case then i'm fine with keeping it as is.

The fact that we used `play()` kind of implied we use the `play(rewind=true)` behavior of WAAPI. I'm also fine with reversing the behavior of `rewind`, but then, what is the difference between these two:

```css
.once {
  animation-trigger: --a play-once;
}

.forward {
  animation-trigger: --a play-forwards;
}
```

Don't they end up doing the same? Maybe it's not a problem?

> It would be helpful to have real examples where these actions are being used. 

Either we come up with real examples, or we simply try again to map all the options, and see that we have an API surface that covers them, right?

<!-- comment id=4610116546 author=ydaniv created=2026-06-03T07:38:13Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4610116546 -->
## @ydaniv — 2026-06-03

Another suggestion: 

- Scrap the notion of "stateful/stateless" triggers. All triggers have state.
- The `play`/`play-forwards`/`play-backwards` (and naturally `play-once` if needed) don't replay/rewind
- Animation state is controlled by the actions, and only actions can explicitly rewind, like `reset` and `replay`, and of course alternating `-forwards`/`-backwards`
- We could then scrap `play-once`, and `animation-trigger: --t play` will just play once unless explicitly reset/rewind

WDYT?

<!-- comment id=4613347579 author=DavMila created=2026-06-03T14:20:40Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4613347579 -->
## @DavMila — 2026-06-03

> Another suggestion:
> 
> * Scrap the notion of "stateful/stateless" triggers. All triggers have state.
> * The `play`/`play-forwards`/`play-backwards` (and naturally `play-once` if needed) don't replay/rewind
> * Animation state is controlled by the actions, and only actions can explicitly rewind, like `reset` and `replay`, and of course alternating `-forwards`/`-backwards`

These suggestions sound good to me in general.  
  
> * We could then scrap `play-once`, and `animation-trigger: --t play` will just play once unless explicitly reset/rewind

I wonder if we should consider keeping `play`. One case that we would lose if we drop the current `play` behavior is an action that does nothing if already playing, but restarts if finished (so identical to `animation.play()`). I can see an author wanting this behavior, e.g. when tying the animation to a button click.

Anyway, perhaps that  deserves a separate discussion. I think we are aligned on the **main proposal** in this issue: play-forwards/backwards don't rewind if already finished (% playback rate).

<!-- comment id=4613983278 author=css-meeting-bot created=2026-06-03T15:23:14Z url=https://github.com/w3c/csswg-drafts/issues/13643#issuecomment-4613983278 resolution=true -->
## @css-meeting-bot — 2026-06-03

The CSS Working Group just discussed `[animation-triggers-1] Should play-forwards and play-backwards have no effect if the animation has already reached its end?`, and agreed to the following:

* `RESOLVED: play, play-f, play-b do not auto-rewind at the end; we add play-always which does; we remove play-once since that's now 'play' behavior`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> flackr: the intent when we added play-forwards/backwards was to create the effect where you can ahve an animation that, when trigger-state is true, starts moving forwards, then when it becomes false, goes back to the initial state<br>
&lt;TabAtkins> flackr: but when we specced it, we said if the animation is finished it resets to the beginning<br>
&lt;TabAtkins> flackr: afraid it's a footgun, authors will use it as a scrubber but sometimes it'll reset to the initial state<br>
&lt;TabAtkins> flackr: propose that we say a finished animation stays finished and doesn't jump the current animation position<br>
&lt;ydaniv> q+<br>
&lt;astearns> ack ydaniv<br>
&lt;TabAtkins> ydaniv: suggestion from the issue<br>
&lt;TabAtkins> ydaniv: we ahve a notion of stateful and stateless triggers<br>
&lt;TabAtkins> ydaniv: but we recently decided to change how triggers are managed when you specify the same name multiple times, they merge<br>
&lt;TabAtkins> ydaniv: this somehow changes how you thinka bout handling state<br>
&lt;TabAtkins> ydaniv: suggestion is mostly, let's make all the triggers stateful and remove stateless<br>
&lt;TabAtkins> ydaniv: then treat play-forwards/backwards as a single-time playing action<br>
&lt;TabAtkins> ydaniv: and move the state of the animation to the action itself, so if you want to reset, you apply the reset action, or replay, or otherwise alternate<br>
&lt;TabAtkins> ydaniv: but animations dont' change state on their own<br>
&lt;TabAtkins> ydaniv: so triggers always become stateful<br>
&lt;TabAtkins> flackr: i have a couple questions<br>
&lt;TabAtkins> flackr: isn't what you're saying equivalent to mine, about the actions?<br>
&lt;TabAtkins> ydaniv: in line, yes, but goes a bit further<br>
&lt;TabAtkins> flackr: could we separately resolve on thea ctions and consider stateful/stateless independently? there's complications i don't think have been addressed<br>
&lt;TabAtkins> ydaniv: sure. let's also make this apply to 'play', then<br>
&lt;TabAtkins> ydaniv: so none of play/play-forwards/play-backwards cause a replay<br>
&lt;TabAtkins> ydaniv: play feels the same as the other two, just without forcing a direction<br>
&lt;TabAtkins> flackr: ah, like it's just an unpause. i could see a good arg for that, yeah<br>
&lt;TabAtkins> astearns: so, resolve on play/forwards/backwards and then do a separate issue on statefulness?<br>
&lt;TabAtkins> flackr: yeah<br>
&lt;TabAtkins> ydaniv: yeah<br>
&lt;TabAtkins> astearns: summarize the resolution?<br>
&lt;DavidA> q+<br>
&lt;astearns> ack DavidA<br>
&lt;TabAtkins> flackr: so just thinking this thru - if you have a trigger play an animation, without replay, it'll only play once. if you use replay, it always rewinds even if it's in progress<br>
&lt;TabAtkins> DavidA: yeah i was going to bring exactly this up<br>
&lt;TabAtkins> flackr: would you be okay with resolving just on forward/sbackwards here for now and think about play separately? or do they need to be taken togther?<br>
&lt;TabAtkins> astearns: you were suggesting we include 'play' in this resolution, but david/rob have some concerns that would change the existing behavior for 'play'<br>
&lt;TabAtkins> flackr: specifically, that it might result in confusion about triggers not doing anything when you set the action to play. replay has an additional side-effect that might not always be desired.<br>
&lt;TabAtkins> ydaniv: but then i wonder why we have replay and play both...<br>
&lt;TabAtkins> ydaniv: we can resolve on just play-forwards/backwards and discuss the rest in another issue, that woudl be fine<br>
&lt;TabAtkins> flackr: so resolution: play-forwards/backwards do not rewind the animation<br>
&lt;astearns> ack fantasai<br>
&lt;TabAtkins> fantasai: so play-f/b are aligned with play-once, not with play? is that right?<br>
&lt;TabAtkins> DavidA: in terms of whether they restart the animation, yes<br>
&lt;TabAtkins> fantasai: okay so play-f/b are play-f-once, etc<br>
&lt;TabAtkins> flackr: that's an interesting point, play-once is already doing what ydaniv was asking for<br>
&lt;TabAtkins> fantasai: does seem odd that specifying a direction forces a different rewinding behavior<br>
&lt;TabAtkins> DavidA: i think we could do what ydanic was wanting and have play-f/b not rewind, and if we want to have current play behavior (only rewinds when finished) woudl be a new keyword<br>
&lt;TabAtkins> flackr: maybe play-always<br>
&lt;TabAtkins> (i'm still curious what distinguishes play from play-once, then)<br>
&lt;TabAtkins> fantasai: shoudl the play keywords be hyphenated, or a functional notation that takes some behavior keywords like play(forwards once) etc<br>
&lt;TabAtkins> fantasai: seems we have several axises of parameterization here<br>
&lt;TabAtkins> flackr: there are indeed two axises being controlled here. don't think i feel strongly about the directional ones ever needing repeat behavior<br>
&lt;TabAtkins> flackr: if you want an animation to play backwards you can also set animation-direction:backwards<br>
&lt;TabAtkins> flackr: the whole reason to have the direcitonal animations is to have these "scrubbing" track animations, where the trigger state changes the direction you're playing<br>
&lt;TabAtkins> flackr: so updated reoslution: play-f/b dont' auto-rewind, and i think that obviates the need for play-once (because play only plays once), and consider a play-repeat that does auto-rewind<br>
&lt;TabAtkins> astearns: with that concern about axises of parameterization, does it make sense to have -once/-repeat as a thing on the keywords, or a separate thing entirely? function parameter, different property, etc<br>
&lt;TabAtkins> flackr: animation-direction already allows reversing the direction independent of the trigger<br>
&lt;TabAtkins> astearns: but triggers need it on the keyword?<br>
&lt;TabAtkins> flackr: they do, yeah. you need to change these things at the same time.<br>
&lt;TabAtkins> flackr: wish there was a better syntax for orchestrating several things that your trigger want to do, but that would be overcomplicating the syntax<br>
&lt;TabAtkins> fantasai: the functional syntax!<br>
&lt;TabAtkins> fantasai: i think play-repeat is a little confusing, it implies it'll repeat at the end, but this will just *allow* it to start playing again if it triggers after finishing<br>
&lt;TabAtkins> fantasai: i think play-always is a better name, it always plays even if it's reached the end<br>
&lt;TabAtkins> flackr: that sounds reasonable<br>
&lt;TabAtkins> flackr: i think i prefer keeping the keywords rather than a fucntional notation<br>
&lt;TabAtkins> astearns: so should we take a resolution for htis, or is it too intertwined with other things?<br>
&lt;TabAtkins> flackr: I think we should take the narrow resolution (play and play-f/b don't auto-rewind) and play-always has rewinding<br>
&lt;TabAtkins> astearns: that means play-f/b don't auto-rewind?<br>
&lt;TabAtkins> flackr: yes. i don't think there's a good use-case for that<br>
&lt;TabAtkins> DavidA: play-auto-rewind, if that's not too verbose<br>
&lt;TabAtkins> fantasai: i think that brings u pan interesting q. if this stops playing, does that stop or does it rewind immediately?<br>
&lt;TabAtkins> flackr: auto-rewind is *if you finish* it stops, and next play will start from the beginning again<br>
&lt;TabAtkins> astearns: i think i prefer play-always<br>
&lt;TabAtkins> flackr: yeah has a clear sematnic meaning i think<br>
&lt;TabAtkins> ydaniv: works for me<br>
&lt;TabAtkins> astearns: so proposal is play, play-f, play-b do not auto-rewind at the end; we add play-always which does; we remove play-once since that's now 'play' behavior<br>
&lt;TabAtkins> RESOLVED: play, play-f, play-b do not auto-rewind at the end; we add play-always which does; we remove play-once since that's now 'play' behavior<br>
</details>
