---
number: 13490
title: "[css-scroll-snap-1] [interop-2026] clarify how snap positions are calculated in the case where the target element is split into multiple boxes"
state: "open"
labels: ["css-scroll-snap-1"]
author: "hiikezoe"
created_at: "2026-02-13T08:03:01Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13490"
comments: 2
has_resolution: true
synced_at: "2026-07-30T00:49:41Z"
---

While I am investigating why [multicol-001.html](https://wpt.fyi/results/css/css-scroll-snap/multicol-001.html?label=master&label=experimental&aligned&view=interop&q=label%3Ainterop-2026-scroll-snap) fails on Firefox, I realized fragmented box cases such as multi columns are not simple.

For example, in [this testc ase](https://bugzilla.mozilla.org/attachment.cgi?id=9544854) there is a single snap target element and its `scroll-snap-align` is `end`. And the target element, cyan colored one is split into two boxes, one is (100 x 100) size, the other is (100 x 20) size. 

So my question is in the test case the bottom right edges of both boxes should be snap target positions respectively? Moreover what if the `scroll-snap-align` is `center`? Where is the center of those two boxes on Y axis? If the original scroll destination was close to the center of the second box, then the second box would be target?

<!-- comment id=4022509535 author=emilio created=2026-03-09T09:50:32Z url=https://github.com/w3c/csswg-drafts/issues/13490#issuecomment-4022509535 -->
## @emilio — 2026-03-09

cc @flackr @smfr 

<!-- comment id=5120957622 author=css-meeting-bot created=2026-07-29T16:57:06Z url=https://github.com/w3c/csswg-drafts/issues/13490#issuecomment-5120957622 resolution=true -->
## @css-meeting-bot — 2026-07-29

The CSS Working Group just discussed `[css-scroll-snap-1] [interop-2026] clarify how snap positions are calculated in the case where the target element is split into multiple boxes`, and agreed to the following:

* `RESOLVED: Snap the bounding box, but if it is bigger than the viewport, adjust the alignment [TBD] to keep the element in view.`
* `ACTION: flackr to write up a proposal for the adjustment`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> emilio: when a snap target is split across multiple boxes due to fragmentation/etc, it's not super clear in the spec what we snap to, like what's the center<br>
&lt;TabAtkins> emilio: and some related cases, like what if you have a scroll destination between the boxes<br>
&lt;TabAtkins> emilio: i think there's reasonable answers, like maybe we use bounding box of the fragments like anchor does<br>
&lt;TabAtkins> emilio: but it would be good to confirm<br>
&lt;TabAtkins> (i think it's either bounding box, or first fragment; weak preference to match anchoring)<br>
&lt;TabAtkins> fantasai: This is actually pretty tricky<br>
&lt;TabAtkins> fantasai: if you have a fragmented element across columns, and you can see both columns, using the boudning box isn't unreasoanble, you'll always see at least part of the element<br>
&lt;flackr> q+<br>
&lt;Rossen> ack fantasai<br>
&lt;Rossen> ack flackr<br>
&lt;TabAtkins> fantasai: but if you have multiple columns and the viewport is shorter than the columns, and the elemnt is split between bottom of one and top of another, the viewport centering on the boudning box might not let you see anything<br>
&lt;TabAtkins> flackr: We ahve exceptions for elements larger than the scrollport<br>
&lt;TabAtkins> fantasai: We allow you to pan around the whole area, but your initial view (for 'center' alignment) won't show anything<br>
&lt;TabAtkins> flackr: that's fair<br>
&lt;emilio> q+<br>
&lt;flackr> q+<br>
&lt;TabAtkins> fantasai: So I haven't given this a lot of thought yet, but I think we should *try* to do something that ensures the user can see part of the element<br>
&lt;TabAtkins> Rossen: [restating example, tiny viewport + tall columns]<br>
&lt;TabAtkins> fantasai: yes<br>
&lt;TabAtkins> fantasai: and if you ask for start alignment, you'll snap to the top left corner of the bounding box where the element doesn't exist<br>
&lt;TabAtkins> fantasai: so many failure possibilities here<br>
&lt;TabAtkins> fantasai: i think we need to somehow keep at least one of the fragments in the viewport.<br>
&lt;smfr> q+<br>
&lt;TabAtkins> fantasai: so like align *towards* the center, but only to the extent that we can still see some of the fragments we're snapping to<br>
&lt;Rossen> ack emilio<br>
&lt;TabAtkins> emilio: q for Rob, do you know what Blink implements here?<br>
&lt;TabAtkins> flackr: I *think* Blink implements bounding box. would have to double check but I think that's right<br>
&lt;Rossen> ack flackr<br>
&lt;TabAtkins> flackr: possible solution - we already have some special cases for the initial alignment for things larger than the scrollport. maybe this is a special case for that - if the bounding box is larger thant he scrollport, we instead align to the first fragment<br>
&lt;TabAtkins> flackr: haven't thought thru all the use-cases but it seems like a possiblity<br>
&lt;TabAtkins> fantasai: seems like a good option<br>
&lt;TabAtkins> I also think this sounds plausible<br>
&lt;TabAtkins> fantasai: maybe if you say "end" it uses the last fragment<br>
&lt;Rossen> ack smfr<br>
&lt;TabAtkins> flackr: yeah it might be direction-aware<br>
&lt;smfr> let me rejion<br>
&lt;TabAtkins> Rossen: So rob's path forward sounds pretty good. I assume we don't have anything like this in the spec currently?<br>
&lt;TabAtkins> TabAtkins:  yes<br>
&lt;flackr> q+<br>
&lt;TabAtkins> smfr: i'm back. scroll-snap-align:start use first frag, end use last, center decide on what to do... would it be crazy to just generate snap points for each fragment independently?<br>
&lt;TabAtkins> fantasai: could do that, I guess?<br>
&lt;TabAtkins> fantasai: but when you try to bring something into view that won't work as you expect<br>
&lt;flackr> q-<br>
&lt;TabAtkins> fantasai: if you are elsewhere in the page, and the multicol is *smaller* than the viewport, you dont' want to center the one small fragment at the end of the first column. You want to bring the whole thing into view if you can<br>
&lt;TabAtkins> flackr: agree with elika here, there are cases where snapping to fragments is worse than teh bounding box<br>
&lt;TabAtkins> fantasai: so i think this needs a writeup and we can come back to it, because there are some subtleties around relative sizes of things<br>
&lt;TabAtkins> flackr: we already have this carveout for writing modes, i think this is just extending that<br>
&lt;fantasai> PROPOSAL: Snap the bounding box, but if it is bigger than the viewport, adjust the alignment [TBD].<br>
&lt;fantasai> PROPOSAL: Snap the bounding box, but if it is bigger than the viewport, adjust the alignment [TBD] to keep the element in view.<br>
&lt;emilio> +1<br>
&lt;flackr> +1<br>
&lt;fantasai> RESOLVED: Snap the bounding box, but if it is bigger than the viewport, adjust the alignment [TBD] to keep the element in view.<br>
&lt;TabAtkins> fantasai: Rob, do you want to take a stab at the spec text?<br>
&lt;TabAtkins> flackr: sure<br>
&lt;fantasai> ACTION: flackr to write up a proposal for the adjustment<br>
</details>
