---
number: 13212
title: "[css-properties-values-api] Mass property registration by prefix"
state: "open"
labels: ["css-properties-values-api-1"]
author: "LeaVerou"
created_at: "2021-12-06T10:56:15Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13212"
comments: 25
has_resolution: false
synced_at: "2026-08-06T00:47:06Z"
---

_(Note: Edited on Jul 24th, 2026 to focus on wildcard prefixes. Comma-separated syntax shortcut moved to #14225 )_

## The problem

It's common to register a number of properties that share the same prefix with the same descriptors.
When the properties are known in advance, and are relatively few, #14225 covers it.

However, when the set is mutable, large, and/or not known in advance, this gets _really_ messy. 

### Examples 

- [Particularly egregious example](https://github.com/propjockey/css-conways-game-of-life/blob/main/game-of-life.css#L140).
- [Real-world example](https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5069837986).
- More in this [Twitter thread](https://twitter.com/James0x57/status/1467601715067342853)
- Utility registered properties like `--int-*`, `--length-*` etc [see tweet by @propjockey](https://twitter.com/James0x57/status/1467646389148688384).
@tabatkins said [they think it is feasible](https://twitter.com/tabatkins/status/1467686598649073667).

## Proposal

Support an optional single wildcard at the end of `<custom-property-name>`.

It would be mandatory to precede the wildcard by `-`, both to [reduce the size of any Bloom filters used by implementations](https://github.com/w3c/csswg-drafts/issues/10001#issuecomment-5006057731) but also for author-facing reasons, to prevent uninentional matches.

With #14225 the [4000 lines of property registrations here](https://github.com/propjockey/css-conways-game-of-life/blob/main/game-of-life.css#L140) remain 4000 lines, just lighter.
With this, they collapse to 1:

```css
@property --i-* {
	syntax: "<number>";
	initial-value: 0;
	inherits: true;
}
```

### Spec changes

https://drafts.css-houdini.org/css-properties-values-api/#at-property-rule

> The <custom-property-name> production corresponds to this: it’s defined as any [<dashed-ident>](https://drafts.csswg.org/css-values-4/#typedef-dashed-ident) (a valid [identifier](https://drafts.csswg.org/css-values-4/#css-css-identifier) that starts with two dashes), except -- itself, which is reserved for future use by CSS. [Custom properties](https://drafts.csswg.org/css-variables-2/#custom-property) are solely for use by authors and users; CSS will never give them a meaning beyond what is presented here.

to:

> The <custom-property-name> production is defined as:
> ```
> <custom-property-name> = <dashed-ident> [ '-*']?
> ```
>  with the exception that it excludes -- itself, which is reserved for future use by CSS. [Custom properties](https://drafts.csswg.org/css-variables-2/#custom-property) are solely for use by authors and users; CSS will never give them a meaning beyond what is presented here.

Alternatively, since `<dashed-ident>` does not permit `*`, I suppose we'd need to define a new token, e.g. `<dashed-ident-glob>` or `<dashed-ident-pattern>` or `<dashed-ident-set>`.

### CSSOM changes

We could introduce conveniences later, but no CSSOM changes are needed for an MVP:
- `CSS.registerProperty()` could just accept these tokens under `name`
- CSSOM can represent these under a single `CSSPropertyRule`.

## Challenges

### Conflict resolution

In the same spirit as #14225, `@property` rules **do not cascade**. If multiple match, only **one wins**.

However, I think there is value in tweaking the "last one wins" rule:
1. Exact wins over wildcard, regardless of order
2. Between two prefix rules, the **longest** one wins, regardless of order
3. Otherwise, last one wins

This means this works as expected:

```css
@property --icon-* {
	syntax: "<image>";
	inherits: true;
}

@property --icon-start, --icon-end {
	syntax: "<image>";
	inherits: false;
}
```

or this:

```css
@property --size-* {
	syntax: "<length>";
	inherits: true;
}

@property --size-rel-* {
	syntax: "<length-percentage>";
	inherits: true;
}
```

## Other related issues

- #7523 

<!-- comment id=3646870459 author=tabatkins created=2021-12-06T19:18:38Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870459 -->
## @tabatkins — 2021-12-06

The first one (multiple comma-separated names in the registration) we should definitely do, no question. Such an obvious improvement.

The second (wildcard registration) is something I support if it's feasible on implementations. No need to get fancy with the API, it'll just be a new name form; an `<ident-token>` followed by a "*" `<delim-token>`.

Your idea of combining them if multiple apply is interesting, but new to the API; currently each definition is independent, and will override each other if they collide names. I'd assumed we'd use longest-prefix-match semantics, so in your example `--int-foo-1` would use the `@property --int-foo-*` block, but not the `@property --int-*` one.

<!-- comment id=3646870489 author=tabatkins created=2021-12-06T19:21:30Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870489 -->
## @tabatkins — 2021-12-06

Oh, but:

> It should also be pretty easy to do, if it's just a parse time shortcut that is expanded into multiple rules for the CSSOM.

It shouldn't be expanded into multiple rules. We can either make the .name property an array now, hoping the compat is fine, or keep it a string that might be comma-delimited.

<!-- comment id=3646870505 author=SebastianZ created=2021-12-06T19:37:01Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870505 -->
## @SebastianZ — 2021-12-06

> make the .name property an array now, hoping the compat is fine, or keep it a string that might be comma-delimited.

Then it should probably also be renamed to `names` to indicate that it can hold multiple property names, if there are no compat issues.

Sebastian

<!-- comment id=3646870531 author=bramus created=2023-02-20T16:28:50Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870531 -->
## @bramus — 2023-02-20

Got sent a similar request via Twitter: https://twitter.com/LukyVJ/status/1627624292484542467

With `@property` shipping in browsers other than Chrome soon, and more people using it, I predict more similar requests in the near future.

<!-- comment id=3646870561 author=Crissov created=2025-12-03T14:22:56Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870561 -->
## @Crissov — 2025-12-03

For at least 4 years now, everybody has agreed the syntax for [`@property`](https://drafts.csswg.org/css-variables-2/#typedef-custom-property-name) should be :
~~~~ css
@property <custom-property-name># {
  <declaration-list>
}
~~~~
but in the current draft it still lacks that `#`.

<!-- comment id=3646870585 author=LeaVerou created=2025-12-03T15:45:58Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870585 -->
## @LeaVerou — 2025-12-03

> For at least 4 years now, everybody has agreed the syntax for [`@property`](]([drafts.csswg.org/css-variables-2#typedef-custom-property-name](https://drafts.csswg.org/css-variables-2/#typedef-custom-property-name)) should be :
> 
> @property <custom-property-name># {
>   <declaration-list>
> }
> but in the current draft it still lacks that `#`.

If you can point to a resolution for the "everybody has agreed" I will commit the change!

<!-- comment id=3646870595 author=andruud created=2025-12-04T00:19:09Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870595 -->
## @andruud — 2025-12-04

There is another proposal here: https://github.com/w3c/csswg-drafts/issues/7523#issuecomment-1683831209 (based on Tab's post immediately before):

```css
@property {
  syntax: "<length>";
  inherits: false;
  initial-value: 2147483647px;
  --width: initial; /* 2147483647px */
  --height: initial; /* 2147483647px */
  --depth: 0px;
}
```

And/or we could imagine that a hypothetical `initial()` function is allowed here, to be able to express an initial value in terms of other initial values:

```css
@property {
  syntax: "<length>";
  inherits: false;
  --one: 1px;
  --two: 2px;
  --width: initial(--one);
  --height: initial(--two);
  --foo: calc(initial(--one) + initial(--two));
}
```

That would require a novel concept of "computing" an _initial_ value, but that should be possible as long as everything is [computationally independent](https://drafts.css-houdini.org/css-properties-values-api-1/#computationally-independent) otherwise.

---

Mass registration with wildcards: so ... `@property --int-*` is basically something like a "selector" that "matches" custom property names on that form? I'm not sure if it's wise to bake that much (potentially expensive) complexity into the the computed value process. Hopefully we can consider other ways to solve the use-cases this would be needed for. For example, custom functions/`if()` could help for some of them?

<!-- comment id=3646870614 author=LeaVerou created=2025-12-04T01:21:48Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870614 -->
## @LeaVerou — 2025-12-04

> Mass registration with wildcards: so ... `@property --int-*` is basically something like a "selector" that "matches" custom property names on that form? I'm not sure if it's wise to bake that much (potentially expensive) complexity into the the computed value process. Hopefully we can consider other ways to solve the use-cases this would be needed for. For example, custom functions/`if()` could help for some of them?

I do think we should introduce features that eliminate the need for the **hundreds** of custom properties currently needed by design systems, and yes custom functions and `if()` will help tremendously, but I don't think we'd ever get it down to such small numbers where mass-registration won't be desirable. And there is a nice decoupling in being able to write 

```css
@property --foo-color-* {
	syntax: "<color>";
	initial-value: transparent;
	inherits: true;
}
```

rather than having to list every color design token in a centralized place.

<!-- comment id=3646870627 author=Loirooriol created=2025-12-04T02:07:17Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870627 -->
## @Loirooriol — 2025-12-04

https://drafts.csswg.org/cssom/#dom-window-getcomputedstyle

> append to decls all the [custom properties](https://drafts.csswg.org/css-variables-1/#custom-property) whose [computed value](https://drafts.csswg.org/css-cascade-5/#computed-value) for obj is not the [guaranteed-invalid value](https://drafts.csswg.org/css-variables-2/#guaranteed-invalid-value).

How do you conciliate this with mass registration? With `--foo-color-*`, there would be an infinite amount of such properties.

<!-- comment id=3646870648 author=andruud created=2025-12-04T10:12:04Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870648 -->
## @andruud — 2025-12-04

We'd have to say that `@property --foo-color-*` doesn't actually count as those (infinitely many) properties existing, but instead just affects the computed value process for the actually-specified custom properties that match.

<!-- comment id=3646870664 author=Loirooriol created=2025-12-04T10:39:20Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870664 -->
## @Loirooriol — 2025-12-04

I don't get it, what makes one of these infinitely many properties exist for real? Being used in `var()`?

<!-- comment id=3646870677 author=andruud created=2025-12-04T11:57:19Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870677 -->
## @andruud — 2025-12-04

The same thing that makes an unregistered property exist for real: specifying it somewhere. E.g. for `#foo { --thing: 1; }`, `--thing` shows up in the gCS enumeration on `<div id=foo>`, but the infinite amount of unregistered custom properties that _could_ have been specified do not.

<!-- comment id=3646870695 author=Loirooriol created=2025-12-11T17:38:53Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870695 -->
## @Loirooriol — 2025-12-11

> The same thing that makes an unregistered property exist for real

It's not the same thing, because for unregistered, the logic is that the value is not the guaranteed-invalid value. E.g. this is false:

```html
<!DOCTYPE html>
<style>:root { --foo-bar: initial }</style>
<script>
// This is false, the value is the guaranteed-invalid value
document.documentElement.textContent =
  [...getComputedStyle(document.documentElement)].includes("--foo-bar");
</script>
```

For mass registered properties whose initial value is not the guaranteed-invalid value, the above doesn't work. So instead of checking the computed style, we need to check the cascade. And then this would be true?

```html
<!DOCTYPE html>
<style>
@property --foo-* { syntax: "<number>"; initial-value: 1; inherits: true }
:root { --foo-bar: initial }
</style>
<script>
// This is true, the property is specified
document.documentElement.textContent =
  [...getComputedStyle(document.documentElement)].includes("--foo-bar");
</script>
```

And what about inheritance? Does the property count as specified if it inherits from an ancestor where it's actually specified?

And what about `revert` & friends, do they count as specifying, or can they undo the specification of the property?

<!-- comment id=5069837986 author=LeaVerou created=2026-07-24T12:27:32Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5069837986 -->
## @LeaVerou — 2026-07-24

Hit this once more when trying to use CSS for icons. I have an `icons.css` with a bunch of declarations like:

```css
:root {
	--icon-check: url("icons/check.svg");
	--icon-play: url("icons/play.svg");
	--icon-undo: url("icons/undo.svg");
	--icon-redo: url("icons/redo.svg");
	--icon-lock: url("icons/lock.svg");
	--icon-lock-open: url("icons/lock-open.svg");
	--icon-share: url("icons/share.svg");
	/* … dozens more … */
}
```

Without `@property` this is very fragile: used from stylesheets not adjacent to `icons.css`, the icons are broken, since without registration, the `url()`s are resolved relative to the callsite.

However, it would be extremely verbose to add `@property` for each of them, even with a defaulted `inherits`:

```css
@property --icon-check {
  syntax: '<image>';
  initial-value: url("icons/check.svg");
}

@property --icon-play {
  syntax: '<image>';
  initial-value: url("icons/play.svg");
}

@property --icon-undo {
  syntax: '<image>';
  initial-value: url("icons/undo.svg");
}

@property --icon-redo {
  syntax: '<image>';
  initial-value: url("icons/redo.svg");
}

@property --icon-lock {
  syntax: '<image>';
  initial-value: url("icons/lock.svg");
}

@property --icon-lock-open {
  syntax: '<image>';
  initial-value: url("icons/lock-open.svg");
}

@property --icon-share {
  syntax: '<image>';
  initial-value: url("icons/share.svg");
}

/* ... dozens more ... */
```

The `#` syntax would actively *degrade* the code. Yes, it would be shorter, but now adding an icon would require editing two separate places:

```css
:root {
	--icon-check: url("icons/check.svg");
	--icon-play: url("icons/play.svg");
	--icon-undo: url("icons/undo.svg");
	--icon-redo: url("icons/redo.svg");
	--icon-lock: url("icons/lock.svg");
	--icon-lock-open: url("icons/lock-open.svg");
	--icon-share: url("icons/share.svg");
	/* … dozens more … */
}

@property 
	--icon-check,
	--icon-play,
	--icon-undo,
	--icon-redo,
	--icon-lock,
	--icon-lock-open,
	--icon-share,
	/* … dozens more … */ {
  syntax: '<image>';
}
```

The shorter syntax that @tabatkins has been mentioning would help, but is still noisy and error prone (easy to define a new icon and forget it):

```css
:root {
	--icon-check <image>: url("icons/check.svg");
	--icon-play <image>: url("icons/play.svg");
	--icon-undo <image>: url("icons/undo.svg");
	--icon-redo <image>: url("icons/redo.svg");
	--icon-lock <image>: url("icons/lock.svg");
	--icon-lock-open <image>: url("icons/lock-open.svg");
	--icon-share <image>: url("icons/share.svg");
	/* … dozens more … */
}
```

The North star syntax that truly expresses author intent and has the highest signal to noise would be:

```css
:root {
	--icon-check: url("icons/check.svg");
	--icon-play: url("icons/play.svg");
	--icon-undo: url("icons/undo.svg");
	--icon-redo: url("icons/redo.svg");
	--icon-lock: url("icons/lock.svg");
	--icon-lock-open: url("icons/lock-open.svg");
	--icon-share: url("icons/share.svg");
	/* … dozens more … */
}

@property --icon-* {
  syntax: '<image>';
}
```

<!-- comment id=5070514484 author=LeaVerou created=2026-07-24T13:42:57Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5070514484 -->
## @LeaVerou — 2026-07-24

Alright, since most of this discussion had centered around wildcards, and wildcards are significantly harder, I split out the much simpler comma expansion to #14225 and ported all relevant comments there (please post if I missed yours, or post and delete your comment here if there were no reactions to it in this thread) so we can track the two separately. This also allows me to put this issue under #14224.

I also edited the OP with:
- Framing and use cases to be explicitly distinct from #14225 
- Cut rule cascading from the proposal. Only one rule wins (the more specific one) with source order as the tie-breaker.
- Added proposed spec changes
- Fleshed out the proposed CSSOM changes (= no changes needed for an MVP)

<!-- comment id=5075537863 author=Loirooriol created=2026-07-24T23:28:08Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5075537863 -->
## @Loirooriol — 2026-07-24

> The `#` syntax would actively degrade the code. Yes, it would be shorter, but now adding an icon would require editing two separate places

A solution for this was proposed in https://github.com/w3c/csswg-drafts/issues/7523#issuecomment-1683049214:

```css
@property {
	syntax: '<image>';
	--icon-check: url("icons/check.svg");
	--icon-play: url("icons/play.svg");
	--icon-undo: url("icons/undo.svg");
	--icon-redo: url("icons/redo.svg");
	--icon-lock: url("icons/lock.svg");
	--icon-lock-open: url("icons/lock-open.svg");
	--icon-share: url("icons/share.svg");
	/* … dozens more … */
}
```

You ["found this incredibly confusing to understand"](https://github.com/w3c/csswg-drafts/issues/7523#issuecomment-3609532747), but it avoids the problems of mass registration by prefix, so I prefer it.

<!-- comment id=5084107013 author=LeaVerou created=2026-07-26T15:21:12Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5084107013 -->
## @LeaVerou — 2026-07-26

For that in particular, there's a more architecturally coherent syntax, though it would require repeating the type:

```css
@property 
	--icon-check <image>: url("icons/check.svg"),
	--icon-play <image>: url("icons/play.svg"),
	--icon-undo <image>: url("icons/undo.svg"),
	--icon-redo <image>: url("icons/redo.svg"),
	--icon-lock <image>: url("icons/lock.svg"),
	--icon-lock-open <image>: url("icons/lock-open.svg"),
	/* … dozens more … */
	--icon-share <image>: url("icons/share.svg");
```

Though this resolves verbosity and repetition, it does not resolve coupling. Though once we have the shorthand declaration syntax, perhaps that (together) will.

> the problems of mass registration by prefix, so I prefer it.

Which problems specifically are you referring to?

<!-- comment id=5084593507 author=Loirooriol created=2026-07-26T17:28:18Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5084593507 -->
## @Loirooriol — 2026-07-26

> Which problems specifically are you referring to?

Mostly https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870627 and https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-3646870695.

Also, you already added it to the top post, the conflict resolution challenge. Your proposal matches what I would expect, but it's like an extra thing that needs to be decided.

<!-- comment id=5086070273 author=LeaVerou created=2026-07-27T00:11:15Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5086070273 -->
## @LeaVerou — 2026-07-27

> It's not the same thing, because for unregistered, the logic is that the value is not the guaranteed-invalid value.

We resolved in another thread to make `initial-value` optional, and default to the guaranteed-invalid value, wouldn't that create the same issue for regular registered properties? (I'm not 100% sure I see the issue you're seeing, so this may be completely off)

> Also, you already added it to the top post, the conflict resolution challenge. Your proposal matches what I would expect, but it's like an extra thing that needs to be decided.

Having to make design decisions is not a "problem", it's a normal part of language design. It becomes a problem when there is no clear best option, so consensus is hard and everything we pick will surprise a good chunk of users. So far, there is no evidence that this is the case here.

<!-- comment id=5089036399 author=Loirooriol created=2026-07-27T08:27:25Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5089036399 -->
## @Loirooriol — 2026-07-27

> wouldn't that create the same issue for regular registered properties?

No. Let me try to explain the problem again:

```html
<!DOCTYPE html>
<style>
@property --foo-bar { syntax: "<number>"; initial-value: 1; inherits: true }
</style>
<script>
// This is true, because the computed value isn't the guaranteed-invalid value (it's 1).
document.documentElement.textContent =
  [...getComputedStyle(document.documentElement)].includes("--foo-bar");
</script>
```

The `--foo-bar` property gets included in `getComputedStyle()`. Presumably, this means that with ...

```css
@property --foo-* { syntax: "<number>"; initial-value: 1; inherits: true }
```

... `getComputedStyle()` should include all possible properties starting with `--foo-`. But there are infinitely many, so it's not possible.

I think Anders was saying that it could instead check whether the property gets assigned a value, but that raises various questions:
 - Would this imply changing the current behavior (risking web compatibility), or be restricted to properties mass registered with a prefix (inconsistency)?
 - What is the exact meaning of being assigned a value, specially with respect to resetting/inheritance (explicit or implicit) and cascade rollbacks?

> Having to make design decisions is not a "problem", it's a normal part of language design

My point is that is one approach raises lots of non-obvious design questions, and another approach doesn't because it's simpler, then the later is probably preferable.

<!-- comment id=5089492714 author=Crissov created=2026-07-27T09:14:46Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5089492714 -->
## @Crissov — 2026-07-27

If everybody is gonna use this only with simple types in the `syntax` descriptor anyway, it might make more sense to move these into a family of new at-rules instead:

~~~~ css
@image --icon-check: url("icons/check.svg");
/* … dozens more … */
@image --icon-share: url("icons/share.svg");
~~~~

These could be allowed within declaration blocks (i.e. `:root {}` etc.) or only outside of them. 

For images in particular, it seems it would be helpful to be able to refer to the matched part:

~~~~ css
@image --icon-*: src("icons/" match() ".svg");
~~~~

<!-- comment id=5149009791 author=LeaVerou created=2026-08-01T02:06:36Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5149009791 -->
## @LeaVerou — 2026-08-01

@Loirooriol What @andruud was suggesting is the only reasonable way to handle this so I don't see the inconsistency when the alternative is simply not possible (without a crystal ball). 

@Crissov We already have a resolution for `@image` in another thread, which means a completely different thing. This would severely restrict our syntax space for future atrules, for little gain. 

<!-- comment id=5165655184 author=andruud created=2026-08-03T11:19:39Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5165655184 -->
## @andruud — 2026-08-03

Perhaps we can:

 - Make `initial-value` optional for all syntaxes, not just `*`. If omitted, the initial value is `<guaranteed-invalid>`.
 - Make the `initial-value` descriptor invalid for mass registrations.

<!-- comment id=5166082448 author=Loirooriol created=2026-08-03T12:05:41Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5166082448 -->
## @Loirooriol — 2026-08-03

@LeaVerou I'm not sure you understood my point, a crystall ball that sees the future wouldn't solve anything here. And what andruud originally proposed wasn't completely clear nor necessarily the only solution.

@andruud I guess that avoids the problem (and the first part was already resolved in https://github.com/w3c/csswg-drafts/issues/13211#issuecomment-4040028009 !). But it seems an undesirable restriction, so I still prefer https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5075537863

<!-- comment id=5173260094 author=LeaVerou created=2026-08-04T00:40:39Z url=https://github.com/w3c/csswg-drafts/issues/13212#issuecomment-5173260094 -->
## @LeaVerou — 2026-08-04

> Perhaps we can:
> 
> * Make `initial-value` optional for all syntaxes, not just `*`. If omitted, the initial value is `<guaranteed-invalid>`.

We already have a resolution for that, though I don't currently have the bandwidth to look it up.
