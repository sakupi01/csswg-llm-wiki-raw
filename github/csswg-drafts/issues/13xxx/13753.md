---
number: 13753
title: "[css-lists-3] [css-values-5] Potential issue with `toggle()` and `ul` in css-lists-3 Appendix A"
state: "open"
labels: []
author: "sleepy-monax"
created_at: "2026-04-01T13:37:02Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13753"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:38Z"
---

Hi,

While implementing the `toggle()` function in my web engine, I believe I found an error in [**Appendix A: Sample Style Sheet for HTML**](https://www.w3.org/TR/css-lists-3/#ua-stylesheet) of the `css-lists-3` specification.

The sample stylesheet suggests defining unordered lists as follows:
```css
ul { list-style-type: toggle(disc, circle, square); }
```

However, the initial value of `list-style-type` is `disc`. 

According to `css-values-5` regarding how [`toggle()`](https://www.w3.org/TR/css-values-5/#toggle-notation) is evaluated:
> "Then, compare the property’s inherited value with each Cn. For the earliest Cn that matches the inherited value, the computed value of toggle() is Cn+1."

**The Issue:**
Because the default inherited value is already `disc`, the `toggle()` function will match `disc` (C1) and immediately compute to the next value in the list, which is `circle` (C2). 

This results in a top-level `<ul>` incorrectly computing to `circle` instead of the expected `disc`.

<!-- comment id=4188722971 author=Loirooriol created=2026-04-05T11:13:41Z url=https://github.com/w3c/csswg-drafts/issues/13753#issuecomment-4188722971 -->
## @Loirooriol — 2026-04-05

All browsers agree, the spec should just reflect that.

https://searchfox.org/firefox-main/rev/4b851f6b592ecf1112ee47dd25e8de28c892ad67/layout/style/res/html.css#624-636
```css
/* 2 deep unordered lists use a circle */
:is(ol, ul, menu, dir) ul,
:is(ol, ul, menu, dir) menu,
:is(ol, ul, menu, dir) dir {
  list-style-type: circle;
}

/* 3 deep (or more) unordered lists use a square */
:is(ol, ul, menu, dir) :is(ol, ul, menu, dir) ul,
:is(ol, ul, menu, dir) :is(ol, ul, menu, dir) menu,
:is(ol, ul, menu, dir) :is(ol, ul, menu, dir) dir {
  list-style-type: square;
}
```

https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/html/resources/html.css;drc=bad6a015f63e4a8c73edf6cc0cb41d3f5d89dc46;l=380

```css
:is(dir, menu, ol, ul) dir,
:is(dir, menu, ol, ul) menu,
:is(dir, menu, ol, ul) ul {
    list-style-type: circle
}

:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) dir,
:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) menu,
:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) ul {
    list-style-type: square
}
```

https://searchfox.org/wubkat/rev/7b241c061cc32ed491a286cf275140d5be39f641/Source/WebCore/css/html.css#317-327
```css
:is(dir, menu, ol, ul) dir,
:is(dir, menu, ol, ul) menu,
:is(dir, menu, ol, ul) ul {
    list-style-type: circle;
}

:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) dir,
:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) menu,
:is(dir, menu, ol, ul) :is(dir, menu, ol, ul) ul {
    list-style-type: square;
}
```
