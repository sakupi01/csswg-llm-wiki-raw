---
number: 13326
title: "[meta] Drop `Canonical order` property definition field?"
state: "open"
labels: []
author: "cdoublev"
created_at: "2026-01-10T07:49:14Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13326"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:29Z"
---

Based on `w3c/webref` data, the `Canonical order` of a property can be:

  - `per grammar`, for a large majority of properties
  - `n/a`, for some properties
  - `the horizontal component of the <position>, followed by the vertical component` for `object-position`

Properties in SVG are not defined a `Canonical order`.

Given that:

  - the `Canonical order` for `object-position` is equivalent to `per grammar`
  - `n/a` seems used when the property can only be declared with a single component value
  - [serialization principles](https://drafts.csswg.org/cssom-1/#serialize-a-css-value) already defines the canonical order as being based on the grammar (*"a pattern typically represented by a double bar [||](https://drafts.csswg.org/css-values-4/#comb-any) in the value syntax"*)

... the `Canonical order` field seems pretty much... useless?

Some CSS tool authors expressed the need to identify shorthands in `w3c/webref` data (https://github.com/w3c/webref/issues/1766), and the canonical order for expansion. The grammar does not always allow to determine a canonical order. So an alternative could be to:

  1. only use `Canonical order` for shorthands
  2. use `per grammar` when appropriate, otherwise either an ordered list of property names, or some prose to define the canonical order (eg. for `all`)

Cc @tidoust 

<!-- comment id=3732498940 author=Loirooriol created=2026-01-10T11:44:19Z url=https://github.com/w3c/csswg-drafts/issues/13326#issuecomment-3732498940 -->
## @Loirooriol — 2026-01-10

I tend to agree that shorthands should probably use a different table. Other than "Name" and "Value", all the other current entries are completely useless for shorthands.

"Percentages" is also useless for lots of longhands which don't use percentages. Does it really need to be a propdef entry? It should at least be optional, like "Logical property group".
