---
number: 13163
title: "[css-navigation-1] :link-to() pseudo-class for links that are related to the current navigation"
state: "open"
labels: ["Needs Example or Figure", "css-navigation-1"]
author: "dbaron"
created_at: "2025-11-26T18:09:44Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13163"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:45:30Z"
---

When I presented the spec proposal in #12594 that we agreed to adopt as an editor's draft, there was one section of the spec that was blank.  This section was intended to have pseudo-classes for links that are related to the current navigation.

[This section of the spec](https://drafts.csswg.org/css-navigation-1/#link-navigation-pseudo-classes) now has a proposal in it.

I will fully admit that this proposal is a bit complicated -- but I think the thing we want to do here is a bit complicated, but also quite useful.  @noamr, @bramus, and I bounced around a few different ideas to get to this one (and I think the current proposal is mostly theirs rather than mine).  I think we've gotten to something that's good enough that I'm not embarassed to propose it (which isn't a particularly high bar, though in this case I think it took some discussion to reach it).  But it could probably be better.  However, we face competing constraints of:
* making the syntax not too complex to write
* making the syntax such that people who read it are likely to have a reasonable idea about what it does.

The interesting thing that we're trying to add here is that we're adding a selector that is conceptually matching between *three* different things rather than the normal two (something about the element and something in the selector).  In this case we're proposing a selector that is able to match between three things:  the target of the link (which is something about the element, as normal), data in the selector (also as normal), *and* the from or to URL of the currently active navigation (this is the new part).

The current proposal in the spec is a `:link-to()` pseudo-class that matches links based on the target of the link.  The current draft allows this to be used as a normal two-way-match selector.  But then it also offers a `navigation-param()` function that goes in that selector that turns it into this three-way-match concept and connects it to the URLs involved in the currently active navigation.

The reason we think this is useful is that it is intended to allow setting up view transitions between a list of items and a detailed view of one of those items (in either direction), which we've talked about as a list-to-details or details-to-list transition.  Such transitions often animate aspects of the list item into aspects of the detailed view to help the user visually understand, through the movement in the animation, how the user interface has changed.

The larger example in the current spec might help make this a little bit clearer (though the spec also has some additional, more technical, detail):

<blockquote>

A more interesting example of the ::link-to() pseudo-class is this example which creates a view transition between a item in a list that contains a link (in this document) and the details page for that link (in a different document). This transition works even when the navigation is a back/forward navigation and even if the user has used a language selector UI to change the page into a different language (and thus change the URL). The use of the [:link-to()](https://drafts.csswg.org/css-navigation-1/#link-to-pseudo) pseudo-class ensures that the view transition animations from or to the correct item in the list by matching the relevant parts of the navigation URL to the link URL.
```css
@view-transition {
  /* allow cross-document view transitions */
  navigation: auto;
}

@route --movie-details {
  /* match URLs like /en/movie/123 which is the English page
     about a movie with ID 123 */
  pattern: url-pattern("/:lang/movie/:id");
}

/* capture the overall area representing the movie, and a
   sub-area for its poster image */

/* match an element with class movie-container with a child
   link that links to a movie whose id is the same as the
   movie we are currently navigating to or from.  (lang can
   be different, though.)

   Just :link-to(--movie-details) requires that the target
   of the link match the URL pattern defined by the "@route
   --movie-details" rule.

   The navigation-param(id) further requires that either the
   from or the to URL of the current navigation also match
   the URL pattern represented by the "@route
   --movie-details" rule, and that that the 'id' named group
   from that match be the same as the 'id' named group from
   the match with the link’s target.
   */
.movie-container:has(> .movie-title:link-to(
  --movie-details with navigation-param(id))) {

  view-transition-name: movie-container;

  > .movie-poster {
    view-transition-name: movie-poster;
  }

  /* leave the default cross-fade animation for both image
     captures */
}
```

</blockquote>

<!-- comment id=3583037885 author=noamr created=2025-11-26T20:06:20Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3583037885 -->
## @noamr — 2025-11-26

It's worth to mention issue #8209. We found that one of the most common and most effective ways people use view transitions is for this kind of list<->details transition, as it creates a visual mental model of the navigation.

Mapping a link to a parameter from a navigation URL is the "CSS-only" way to do so. 

<!-- comment id=3601768296 author=bramus created=2025-12-02T12:22:11Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3601768296 -->
## @bramus — 2025-12-02

As it is currently proposed, `a:link-to(--movie-details with navigation-param(id))` always matches the `:id` used in the targeted `a` element with the `navigation-param(id)`. 

I don’t think this covers all scenarios:

-  What about doing arbitrary matches, where you want to select the link to `--movie-details` with the `:id` set to `6`  _(unrelated to the current location)_
- This requires the named of the parameter from the route to have the same name as the used inside the `navigation-param()` function. E.g. what if I want to select the link to `--movie-details` with the `:id` set to the  `:search-id` of the current page URL?

👉 My suggestion to have authors specify which named parameter of the route they want to do a comparison against. Something like `a:link-to(--movie-details with (id: navigation-param(id)))` or `a:link-to(--movie-details with (:id = navigation-param(id)))`

_(This also opens up the door to later do range comparisons, e.g. `:page_id > 6`)_

Another thing is that `navigation-param()`, as it is currently proposed, _always_ extracts parameters from the current active URL. In some cases, you might want to access from either the old or new URL. For this I have filed https://github.com/w3c/csswg-drafts/issues/13179

<!-- comment id=3603021631 author=Crissov created=2025-12-02T16:51:52Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3603021631 -->
## @Crissov — 2025-12-02

I shall note that this pseudo class `:link-to()` offers a more elaborate concept than what I suggested as _pseudo attribute selectors_ in #354, where there was no method to specify custom URL patterns, so only simple cases with predefined/magic named components would have been covered, e.g. somewhat like this:

~~~~ css
/* Example 4 */
[href:path="/"] {
  font-weight: bold;
}

/* Example 5 */
@route --movie-details {
  pattern: url-pattern("/:lang/movie/:id");
}
.movie-container:has(
  > .movie-title[href:path$=:navigation-param(id)]
  /* or …:path(3)=… */ {
  view-transition-name: movie-container;
& > .movie-poster {
    view-transition-name: movie-poster;
  }
}
~~~~

<!-- comment id=3781131323 author=dbaron created=2026-01-21T20:57:37Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3781131323 -->
## @dbaron — 2026-01-21

I think it would be useful to have a somewhat open discussion of this at the face-to-face, both because it didn't exist the last time we presented the spec, and because it's a somewhat complicated design space and those of us who have worked on it so far don't really agree (I think largely because we're prioritizing different concerns).  Hopefully when both @noamr and I are around.  (I think @bramus should be there in person so hopefully doesn't have time constraints.)

<!-- comment id=3790860564 author=dbaron created=2026-01-23T15:40:09Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3790860564 -->
## @dbaron — 2026-01-23

Also, one other high-level point I'd like to add about the complexity here:

The core use cases for `@navigation` (see #8209 for a significant piece of them) involve declarative ways to do something that is *possible* using Javascript (that intercepts the [`navigate` event](https://developer.mozilla.org/en-US/docs/Web/API/Navigation/navigate_event)).  Having it in CSS makes it simpler for authors and also gives us the potential for browsers to optimize it better (for example, potentially by avoiding having to go to the main thread to run the JS).  However, it's really only useful to have it in CSS if developers will actually be able to do what they're doing using the CSS features for a large percentage of the time.  Otherwise we'll end up in a situation where:
* a significant percentage of developers who try to use the feature in CSS will run into a problem that they can't fix using CSS and they need to throw away what they've written in CSS and start over with Javascript interception of the navigations, and
* those developers will talk about this and advise other developers to avoid the CSS feature and just do it in Javascript because they're likely to have the same problem (even if only some of them actually will).

This situation means that when we're designing this feature in particular, the tradeoffs that we make between simplicity and covering more use cases are probably a bit different than normal.  In particular, I think this situation means that we should be more willing than normal to accept some complexity here in order to ensure that the feature covers most of the things that people will want to use it for.  (I think it's still the case that the (perhaps somewhat complex or dense) CSS expressions will be much smaller than the amount of Javascript needed to do the same thing.)

<!-- comment id=3790948391 author=noamr created=2026-01-23T15:58:52Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3790948391 -->
## @noamr — 2026-01-23

I am 

> Also, one other high-level point I'd like to add about the complexity here:
> 
> The core use cases for `@navigation` (see [#8209](https://github.com/w3c/csswg-drafts/issues/8209) for a significant piece of them) involve declarative ways to do something that is _possible_ using Javascript (that intercepts the [`navigate` event](https://developer.mozilla.org/en-US/docs/Web/API/Navigation/navigate_event)). Having it in CSS makes it simpler for authors and also gives us the potential for browsers to optimize it better (for example, potentially by avoiding having to go to the main thread to run the JS). However, it's really only useful to have it in CSS if developers will actually be able to do what they're doing using the CSS features for a large percentage of the time. Otherwise we'll end up in a situation where:
> 
> * a significant percentage of developers who try to use the feature in CSS will run into a problem that they can't fix using CSS and they need to throw away what they've written in CSS and start over with Javascript interception of the navigations, and
> * those developers will talk about this and advise other developers to avoid the CSS feature and just do it in Javascript because they're likely to have the same problem (even if only some of them actually will).
> 
> This situation means that when we're designing this feature in particular, the tradeoffs that we make between simplicity and covering more use cases are probably a bit different than normal. In particular, I think this situation means that we should be more willing than normal to accept some complexity here in order to ensure that the feature covers most of the things that people will want to use it for. (I think it's still the case that the (perhaps somewhat complex or dense) CSS expressions will be much smaller than the amount of Javascript needed to do the same thing.)

Thanks, I think this is a very good observation.
I think I am OK with the complexity of the syntax in [this comment](https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3601768296), and also we can provide some simplified versions, like `:link-to(--movie-details exact-match)` or `:link-to(--movie-details with auto)` or something that doesn't force you to name each and every parameter.

<!-- comment id=3791035183 author=astearns created=2026-01-23T16:16:08Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3791035183 -->
## @astearns — 2026-01-23

> it's really only useful to have it in CSS if developers will actually be able to do what they're doing using the CSS features for a large percentage of the time

This makes sense and I mostly agree with your reasons for coming to this conclusion. So I think we can accept some complexity if we find a way to satisfy a large percentage of the cases.

But one possible way to satisfy these concerns is to target a smaller percentage of the highest-priority cases, and find a way (via naming or dev rel) to communicate the limitations so that authors know only to use it for the cases it’s designed for. This assumes we can identify what the highest-priority cases actually are, and that we can actually make the limitations clear. It also makes it difficult to extend the same mechanism to cover additional cases over time.

So I think I prefer that we find a more general mechanism. I just wanted to mention an alternative fallback approach.

<!-- comment id=3812577923 author=css-meeting-bot created=2026-01-28T17:00:44Z url=https://github.com/w3c/csswg-drafts/issues/13163#issuecomment-3812577923 -->
## @css-meeting-bot — 2026-01-28

The CSS Working Group just discussed `[css-navigation-1] :link-to() pseudo-class for links that are related to the current navigation`.

<details><summary>The full IRC log of that discussion</summary>
&lt;ntim> dbaron:<br>
&lt;ntim> dbaron: The last time I talked about the spec in a WG, I said that pseudo-class for links were in the draft, but there was nothing to look at<br>
&lt;ntim> ... I want to talk to you about it, because they're interesting<br>
&lt;ntim> ...one of the things that this spec does, is make it so you can declaritively start VT depending on cases<br>
&lt;ntim> one of details to list and list to details transitions<br>
&lt;ntim> there's an issue in CSSWG drafts that is very old with a lot of these<br>
&lt;ntim> in many cases, you might have an UI that presents a list of things (list of movies)<br>
&lt;ntim> so not just pure text, could be text + thumbnail<br>
&lt;ntim> when you click an item, it navigates to a view with info<br>
&lt;ntim> you might want a VT where the thumbnail becomes a larger image<br>
&lt;ntim> and title becomes a larger title<br>
&lt;ntim> this is a natural use case for VT<br>
&lt;ntim> one of the nice things about setting this up declaratively is picking up the link you navigated with<br>
&lt;ntim> the URL keeps relationships<br>
&lt;ntim> Doing this declaratively needs a feature where you can have selectors where the link points to<br>
&lt;astearns> s/selectors where/selectors for where/<br>
&lt;ntim> The interesting thing with these selectors is the ability to do non-exact matches<br>
&lt;ntim> people can do this with JS<br>
&lt;ntim> If we do this declaratively, people might need to fallback to JS in some cases, but if that percentage is 50%, the API is not useful<br>
&lt;astearns> q+<br>
&lt;ntim> we need an API that covers enough use cases<br>
&lt;ntim> We want an API that matches where the links points to, with where you're navigating from/to<br>
&lt;ntim> you also want a selector that says which part you care about matching<br>
&lt;ntim> there are 3 pieces<br>
&lt;ntim> URL of the link, URL of the page.<br>
&lt;ntim> the selector that says how to match the two and which pieces have to match or don't<br>
&lt;bramus> q+<br>
&lt;ntim> This makes the selector design a bit interesting relative to how we design selectors<br>
&lt;ntim> what I have in the draft right now<br>
&lt;ntim> we came to the conclusion that people don't like it<br>
&lt;ntim> let's talk about the draft<br>
&lt;ntim> @route to declare URL patterns<br>
&lt;ntim> you can also use URL patterns directly, but easier to name it<br>
&lt;ntim> we have `with navigation-param(id)`<br>
&lt;ntim> The part that we don't like is that we need to match the two parts of the pattern<br>
&lt;ntim> It's compact, but packs too much in the current syntax to be readable<br>
&lt;ntim> bramus will talk about an alternative<br>
&lt;astearns> ack bramus<br>
&lt;ntim> bramus: I left a comment on the issue<br>
&lt;bramus> bramus: As it is currently proposed, a:link-to(--movie-details with navigation-param(id)) always matches the :id used in the targeted a element with the navigation-param(id).<br>
&lt;ntim> bramus: I don't think that covers all cases<br>
&lt;ntim> ...what about doing abirtary matches (matching a certain number)<br>
&lt;ntim> it's currently not possible to match with a certain part of the parameters<br>
&lt;bramus> `a:link-to(--movie-details with (:id = navigation-param(id)))`<br>
&lt;bramus> `a:link-to(--movie-details with (:id = navigation-param(favorite_id)))`<br>
&lt;ntim> suggestion is to allow specifying which named parameter of the route they want to compare against<br>
&lt;ntim> one thing to note, if we introduce this equals syntax, we can extend it later to do syntax like (:id >  ..)<br>
&lt;dbaron> ... useful for directional view transitions, e.g., slide to left or right depending on page number<br>
&lt;ntim> astearns: is this declarative way giving more power than script? can we do this declarative stuff in script?<br>
&lt;ntim> astearns: can people rely on script for everything right now?<br>
&lt;astearns> ack astearns<br>
&lt;ntim> dbaron: I think they can<br>
&lt;noamr> q+<br>
&lt;ntim> dbaron: if you're relying on script, you're no longer using CSS for a big chunk. That script has a few disadvantages: hard to get all the cases, because there's a lot of pieces, you can't do this in little pieces<br>
&lt;ntim> astearns: we need to make this feature complete enough that people don't fallback to script, but that means we might take longer. So we might want to check existing libraries. For VT, there's no existing libraries, so maybe we should wait before working on this<br>
&lt;bramus> q+<br>
&lt;astearns> ack fantasai<br>
&lt;ntim> fantasai: I'm trying to understand this proposal. So you have a link and you're trying to compare against what it links to<br>
&lt;ntim> dbaron:  navigation-params has a bunch of paramters that you can pull out from.<br>
&lt;ntim> fantasai: it's checking the to with the current URL right?<br>
&lt;ntim> dbaron: these selectors are going to setup VT-names against different pieces. It might be that you're going from the list to the details page or vice-versa. So you might care about the link from the list page or the link from the to page<br>
&lt;ntim> noamr: i wanna show a demo that might be easier to understand<br>
&lt;bramus> Demo URL: https://dwa-movies.onrender.com/ (Chrome Canary with flags)<br>
&lt;ntim> noamr: we have a list of movies, we can from a movie to a cast member, and we go back and forward<br>
&lt;ntim> noamr: the movie is always transitioning the hero<br>
&lt;ntim> if I have a thumbnail that goes to person with a matching movie, or a movie with a matching person, then we have this style<br>
&lt;dbaron> noamr, we can't see your devtools fwiw<br>
&lt;ntim> noamr: this is the most used type of VT in the few years its been around<br>
&lt;ntim> ...this manages the lifecycle, when does the navigation start<br>
&lt;ntim> &lt;missed><br>
&lt;ntim> fantasai: we've been talking about styling links, what if the thing you're trying to style isn't a link?<br>
&lt;bramus> Example of not styling the link, but the img: `a:link-to(items_detail with item_id: route_param(from, item_id)) img`<br>
&lt;ntim> dbaron: you can use :has()<br>
&lt;ntim> fantasai: what if there's no link, there's just a navigation?<br>
&lt;ntim> dbaron: the main use case is that the link is on one of the two pages, and the view transition name would be set globally on the other page<br>
&lt;ntim> bramus: if you wanna match something on a page based on its URL, you can use the `@navigation` to detect on which page you are right now and you can use selectors in there. `:link-to()` to sniff out the link<br>
&lt;ntim> fantasai: if you click on a link, and style on what navigation is happening?<br>
&lt;ntim> bramus: you'd use the `@navigation` at rule and use :link-to()` to sniff out the link<br>
&lt;ntim> fantasai: I might have 5 links from A to B, I need to know which link triggered th navigation<br>
&lt;ntim> bramus: there is a different issue<br>
&lt;ntim> dbaron: you might not want to condition the link on which link triggered it. because you make styles that don't work when you go back<br>
&lt;bramus> Issue to figure out which link triggered the navigation: https://github.com/w3c/csswg-drafts/issues/11801<br>
&lt;ntim> .... both pages might be conditioning based on a @navigation at rule, but both of them might also be on the :link-to<br>
&lt;fantasai> fantasai: If there are 5 links from A to B, how is :link-to being useful on page A?<br>
&lt;ntim> dbaron: it's not the core use case, this feature is targeting the use case where you have a list of different things.<br>
&lt;ntim> you might want to write other selectors<br>
&lt;ntim> fantasai: if you have a selector of the active navigation, why do you have to repeat the link in both @navigation / :link-to?<br>
&lt;ntim> dbaron: you might not want an exact match<br>
&lt;ntim> @navigation, you might want to match all actor pages or all movie pages. :link-to for a specific actor or movie<br>
&lt;bramus> q+<br>
&lt;ntim> fantasai: the spec needs more examples/explanation behind the motivation<br>
&lt;noamr> It's because string-matching URLs is super brittle<br>
&lt;astearns> q+<br>
&lt;ntim> dbaron: the part that's interesting in the example is that they're not using exact matches, you can match of pieces that are important to you<br>
&lt;astearns> ack ntim<br>
&lt;astearns> ack noamr<br>
&lt;ntim> noamr: string match URLs is brittle, you can't take an HREF and match it against an URL, it might have an extra parameter for analytics, using a URL pattern is the way we found<br>
&lt;bramus> https://gist.github.com/bramus/2e463f180be00e89d4b66b972ab95713<br>
&lt;emilio> bramus: this is a bunch of examples we want to cover<br>
&lt;emilio> ... this shows that knowing the link that triggered the navigation doesn't fix the issue<br>
&lt;emilio> ... this targets the link from details to navigation page<br>
&lt;emilio> ... on the details page then you want to have a photo<br>
&lt;emilio> ... on the other one you want to give the jaws link a specific v-t-name<br>
&lt;emilio> ... sniffing the link that triggers the navigation wouldn't be useful because that's only meaningful on the old page<br>
&lt;emilio> ... so that's why we need "link that linked to the old page"<br>
&lt;emilio> fantasai: thanks, that's helpful<br>
&lt;emilio> ChrisL: should we put those in the spec?<br>
&lt;emilio> bramus: probably<br>
&lt;emilio> fantasai: what's the diff between route-param and navigation-param?<br>
&lt;emilio> bramus: they're the same<br>
&lt;romain> q+<br>
&lt;emilio> ... extra from argument in route_param is the next item in the agenda<br>
&lt;astearns> ack bramus<br>
&lt;astearns> ack astearns<br>
&lt;emilio> astearns: perhaps we should just take this as an intro to the new part of the spec, add some more examples, come back when people have had time to digest it?<br>
&lt;emilio> romain: syntax feels a bit brittle / complicated<br>
&lt;emilio> ... lots of user idents / keywords<br>
&lt;emilio> ... maybe an at-rule is better to define how something should match<br>
&lt;emilio> ... then link-to just points to that at-rule<br>
&lt;emilio> astearns: makes sense to me<br>
&lt;emilio> ChrisL: don't we have a thing to separate user-defined identifiers to avoid conflicting with different names?<br>
&lt;emilio> bramus: yeah v-t-name is not a dashed idents<br>
&lt;TabAtkins> We only use strings when the value comes from outside and doens't necessariliy conform to ident rules, yeah<br>
&lt;emilio> fantasai: v-t-name is internal to css<br>
&lt;emilio> bramus: you could use dashed items but the spec only requires custom ident<br>
&lt;emilio> dbaron: sounds like we're not going to come to a conclusion right now<br>
&lt;emilio> ... want to keep trying to revise the spec<br>
&lt;emilio> ... if we're going to do the work of porting a bunch of examples I'd like to update the spec to something we're more likely to want<br>
&lt;emilio> astearns: yeah but without the examples it's hard to evaluate<br>
&lt;emilio> ... maybe we should iterate with the syntax and examples<br>
&lt;emilio> ... which is more work<br>
&lt;emilio> ... but probably worth it<br>
&lt;emilio> dbaron: I'd like to reiterate the syntax once before putting in the examples<br>
&lt;emilio> fantasai: that's fine<br>
&lt;emilio> astearns: still seems like we are not going to reiterate on the syntax right now<br>
&lt;emilio> dbaron: fair<br>
</details>
