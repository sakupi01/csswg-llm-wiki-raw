---
number: 13376
title: "[css-highlights] Highlight cascade behavior for shadows is rather unfortunate."
state: "open"
labels: ["Needs Design / Proposal"]
author: "emilio"
created_at: "2026-01-21T15:52:43Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13376"
comments: 6
has_resolution: true
synced_at: "2026-07-04T04:45:42Z"
---

Consider this test-case:

```html
<!DOCTYPE html>
<style>
#target {
  text-shadow: 10px 10px 10px;

  &::selection {
    color: green;
  }
}
</style>
<p id="target" style="font-size: 50px">What color is my shadow?</p>
<script>
  getSelection().selectAllChildren(target);
</script>
```

Gecko renders a green shadow because we don't implement the highlight cascade stuff. Chrome and WebKit renders a black shadow (reasonable, and what the spec calls for).

But now consider that I want to draw a green shadow for my selected text:

```html
<!DOCTYPE html>
<style>
#target {
  text-shadow: 10px 10px 10px;

  &::selection {
    text-shadow: 10px 10px 10px green;
    color: green;
  }
}
</style>
<p id="target" style="font-size: 50px">What color is my shadow?</p>
<script>
  getSelection().selectAllChildren(target);
</script>
```

Now WebKit and Gecko draw the green shadow (good, but not per spec). Chromium draws the green shadow _atop the black one_. This is what the spec says to do. IIUC the three behaviors are:

 * Chromium implements  the spec to the letter, but I think it causes unfortunate behavior.
 * Gecko inherits the text shadow from `#target` into `::selection`, and if the shadow exists it overrides the regular text's shadow.
 * WebKit draws the selection's shadow only if specified in the `::selection` style itself (but in that case it also skips the main text's decoration).

I think the Chromium behavior is rather unfortunate because there's no way to "undo" the regular shadow only for the selected text.

I was thinking we could draw a parallel with text-decoration maybe? But behavior there is all over the place:

```html
<!DOCTYPE html>
<style>
#target {
  text-shadow: 10px 10px 10px;
  text-decoration: underline;

  &::selection {
    text-shadow: 10px 10px 10px red;
    text-decoration: overline;
    color: green;
  }
}
</style>
<p id="target" style="font-size: 50px">What color is my shadow?</p>
<script>
  getSelection().selectAllChildren(target);
</script>
```

Blink draws both the `#target` and `::selection` decorations with the `::selection` color. Gecko draws both with the `#target` color. WebKit doesn't support text decoration in `::selection`.

I think for shadows the current behavior is rather bad, because they tend to have alpha so it just looks terrible and you can't work around it.

I _think_ the better behavior would be that if any highlight style had a `text-shadow` specified, then the regular text shadow wouldn't be drawn. (That'd match WebKit wrt shadows).

I think text-decorations would probably want to do the same for consistency with it? But not sure, I guess that'd remove the underline from a link when there's a `::grammar-error`, which is probably undesired.

cc @schenney-chromium 

<!-- comment id=3813297610 author=schenney-chromium created=2026-01-28T19:02:45Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-3813297610 -->
## @schenney-chromium — 2026-01-28

I've also run into this sort of unexpected behavior when writing tests and debugging. I guess the rule of thumb is to only draw one of the highlight or originating for attributes that interfere when rendered. Though on second thought this is really about having "replacement" behavior for shadows and decorations just as the selection text colors "replace" the originating.

I'm totally open to discussing changes to the existing spec behavior. It's no problem from an implementation perspective.

<!-- comment id=4437167265 author=delan created=2026-05-13T04:06:50Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-4437167265 -->
## @delan — 2026-05-13

note that per [#highlight-painting](https://drafts.csswg.org/css-pseudo/#highlight-shadows), originating shadows draw underneath highlight backgrounds, and some highlight shadows draw underneath other highlight backgrounds. that makes this issue less problematic when the highlight styles include a background, but i think it’s still worth solving.

no matter what, ‘text-decoration’ should be additive in highlight pseudos, because having spelling/grammar squigglies play nice with &lt;a> and &lt;del> is important. authors may already be familiar with [decoration propagation](https://drafts.csswg.org/css-text-decor/#line-decoration) outside of highlight pseudos, and can continue to think of highlight pseudos as in-flow children of a decorating box.

do we have any insight into how authors want to use ‘text-shadow’ in highlight pseudos? if no one uses ‘text-shadow’ in a way that expects it to be additive, and some authors say [one of the most helpful uses of ::selection is for removing ‘text-shadow’](https://web.archive.org/web/20240617162604/https://css-tricks.com/almanac/selectors/s/selection/), then maybe we can:

1. make ‘text-shadow’ on highlights override originating shadows, rather than adding to them

2. plus… make originating shadows draw over all highlight backgrounds? this is what Gecko does and what Blink used to do, and from what i remember it would make the Blink painting impl a bit simpler

3. or even… make originating shadows automatically get removed when text is highlighted? this is more radical, but does anyone actually want originating shadows to stay when text is highlighted?

<!-- comment id=4482714508 author=schenney-chromium created=2026-05-18T22:15:52Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-4482714508 -->
## @schenney-chromium — 2026-05-18

When considering my position on this, I was thinking that if you want to "replace" the originating text shadow with a highlight specific shadow, right now you would need to change the originating style when the highlight activates. Conversely, if we change the behavior and highlights replace originating by default, and you want to keep the originating shadow, you would need to re-add it in the highlight style.

Given these two completing desired outcomes, it seems easier for authors to re-add an existing shadow/underline in the highlight style than it is to remove it in the originating style when a highlight is active.

Hence right now I would argue for 3 above, not only for shadows but also text decorations.

I accept my reasoning may be flawed. Feel free to correct me.

<!-- comment id=4484388169 author=delan created=2026-05-19T04:29:46Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-4484388169 -->
## @delan — 2026-05-19

> but also text decorations.

i think the main blocker for adopting option 3 for line decorations is that ‘text-decoration-line’ can only add lines with a single ‘text-decoration-style’ and ‘text-decoration-color’ at a time. combining lines with different styles or colors can only be done by propagation (from ancestor to descendant, from originating to highlight, or from highlight to another highlight). this is not a problem for ‘text-shadow’, where you can set as many shadows as you like in one declaration.

if you have a &lt;del> with ‘text-decoration: currentColor solid line-through’, and you want ‘del::spelling-error’ to have that plus ‘text-decoration: red wavy underline’, the closest you would be able to do is ‘text-decoration: red wavy underline line-through’ or ‘text-decoration: currentColor solid line-through underline’. both ‘red wavy’, or both ‘currentColor solid’.

<!-- comment id=4500653578 author=LeaVerou created=2026-05-20T16:53:35Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-4500653578 -->
## @LeaVerou — 2026-05-20

Testcase: https://codepen.io/leaverou/pen/wBoddjg

Behavior in May 2026:
- Safari and Safari draw a text-shadow for the highlight if one is specified OR the inherited one otherwise 
- Chrome draws a text-shadow for the highlight if one is specified and none otherwise

Either behavior is defensible IMO, but we should pick one and standardize it.
But whatever we decide for the inherited shadow, we should also follow it for overlapping highlights, IMO.

<!-- comment id=4500664102 author=css-meeting-bot created=2026-05-20T16:55:05Z url=https://github.com/w3c/csswg-drafts/issues/13376#issuecomment-4500664102 resolution=true -->
## @css-meeting-bot — 2026-05-20

The CSS Working Group just discussed `[css-highlights] Highlight cascade behavior for shadows is rather unfortunate.`, and agreed to the following:

* `RESOLVED: intent to only draw one shadow when there are multiple conflicting ones to choose from. we'll figure out the rules in the issue.`

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> emilio: looked at this a while ago because we were implementing text shadows for custom highlights<br>
&lt;JoshT> ... find our pseudo behaviour different to other browser engines<br>
&lt;JoshT> ... find behaviour bizarre. do we want to keep original shadows when drawing highlights?<br>
&lt;JoshT> ... spec brhaviour says you should draw regular shadow and highlight on top<br>
&lt;JoshT> ... that sounds bad<br>
&lt;JoshT> ... do people have strong opinions?<br>
&lt;JoshT> ... some precedent for text decorations, but harder to override existing things to get them to go away<br>
&lt;JoshT> ... current spec behaviour seems bad<br>
&lt;lea> q+<br>
&lt;JoshT> ... rendering you get by default seems bad<br>
&lt;astearns> ack lea<br>
&lt;JoshT> lea: looking at these three, I've seen cases where highlighting reduced unreadable results<br>
&lt;JoshT> ... e.g. black text on light backgrounds with purple shadow that made sense when the text was dark, but not on a different background<br>
&lt;JoshT> ... webkit behaviour seems to work well for this<br>
&lt;JoshT> ... you don't get the footguns when you have a text shadow that doesn't make sense in this context<br>
&lt;JoshT> emilio: webkit only skips shadow, I think, if the highlight specifies text shadow<br>
&lt;JoshT> lea: that makes sense because it's explicit<br>
&lt;JoshT> ... in that case, it also skips the main text decoration<br>
&lt;JoshT> ... I do agree that it's weird if you have both shadows on top of each other<br>
&lt;JoshT> emilio: if you have text selection with no shadows involved, then ?????<br>
&lt;JoshT> lea: then I've misunderstood. then it's not appyling a shadow at all<br>
&lt;JoshT> ... if authors explicitly set it, even if to inherit it works, otherwise it doesn't<br>
&lt;JoshT> emilio: no, talking from memory so can't check, but you paint the regular shadow, but if the highlight specifies a shadow, so then you don't paint the main shadow<br>
&lt;kizu> q+<br>
&lt;astearns> ack kizu<br>
&lt;JoshT> kizu: quickly testing what happens in all three browsers<br>
&lt;JoshT> ... firefox and webkit behave the same, override text shadow<br>
&lt;JoshT> ... chrome keeps the old noe<br>
&lt;lea> https://codepen.io/leaverou/pen/wBoddjg<br>
&lt;JoshT> ... removing it makes sense<br>
&lt;JoshT> ... if you have span and another span and modify ??????, they're not composed together<br>
&lt;JoshT> ... I think this could also be a way to remove text decoration<br>
&lt;JoshT> ... kind of connected<br>
&lt;JoshT> ... from what I can see testing, I like behavuour of webkit and firefox more<br>
&lt;JoshT> astearns: are we agreed that highlights should not display the shadow from the originating text?<br>
&lt;JoshT> emilio: in firefox, the main text shadow is not displayed if the highlight specifies a selection<br>
&lt;JoshT> s/selection/shadow/<br>
&lt;JoshT> ... the details are tricky because in gecko it's about how the highlight style comes from the element, so it's sketchy<br>
&lt;JoshT> ... conceptually, we can say ... I prefer not to paint the main shadow if a highlight specifies a shadow<br>
&lt;astearns> ack fantasai<br>
&lt;JoshT> ... and what if there are multiple highlights specifying a shadow? draw one? draw all?<br>
&lt;JoshT> fantasai: I think drawing only shadows specifying on the highlight makes sense if we specify a new shadow<br>
&lt;JoshT> ... or if there's a background<br>
&lt;JoshT> ... we don't want the shadow to stop painting if there's no background<br>
&lt;JoshT> ... the shadow could be what's making the text readable<br>
&lt;JoshT> ... and if we say the background and colour of the higlight end up being the UA chosen colour, then we also turn off the highlights so the text and colour contrast<br>
&lt;JoshT> ... the author doesn't know those colours<br>
&lt;JoshT> ... otherwise we say shadow only suppressed if new text shadow or a new background colour<br>
&lt;JoshT> ... we shouldn't just remove the ????<br>
&lt;fantasai> s/????/shadow<br>
&lt;JoshT> astearns: I'm not sure if we can define the details in 5 mins. but can we resolve intent is to have only one shadow from the highlights is drawn, and we will make rules for how the shadows are suppressed<br>
&lt;JoshT> emilio: sounds good<br>
&lt;JoshT> PROPOSED: intent to only draw one shadow when there are multiple conflicting ones to choose from. we'll figure out the rules.<br>
&lt;JoshT> RESOLVED: intent to only draw one shadow when there are multiple conflicting ones to choose from. we'll figure out the rules in the issue.<br>
</details>
