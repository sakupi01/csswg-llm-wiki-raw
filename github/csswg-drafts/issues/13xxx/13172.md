---
number: 13172
title: "[css-values] Function notations without parameters should be keywords!?"
state: "closed"
labels: ["Closed Rejected as Wontfix by Editor Discretion", "css-values-5"]
author: "Crissov"
created_at: "2025-12-01T09:56:46Z"
closed_at: "2026-03-27T22:03:28Z"
url: "https://github.com/w3c/csswg-drafts/issues/13172"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:45:44Z"
---

The recently introduced [tree counting functions](https://drafts.csswg.org/css-values-5/#tree-counting) `sibling-count()` and `sibling-index()` come with a note:

> These functions may, in the future, be extended to accept an `of <complex-real-selector-list>` argument, similar to `:nth-child()`, to filter on a subset of the children.

Introducing selectors in property value functions would need to respect the resolution of #12590, although it already has been done before in special-needs cases like print-oriented `target-…()` functions. 

1. This possible future extension is all but certain or sound, 
2. the parentheses will always be empty till then, 
3. CSS uses keywords alongside functions and 
4. there is precedence established by [`inherit`/`inherit()`](https://drafts.csswg.org/css-values-5/#inherit-notation), [`fit-content`/`fit-content()`](https://drafts.csswg.org/css-sizing-3/#sizing-values), [`snap-block`/`snap-block()` / `snap-inline`/`snap-inline()`](https://drafts.csswg.org/css-page-floats-3/#float-property) and somewhat [`contents`](https://drafts.csswg.org/css-content-3/#element-content) / [`content()`](https://drafts.csswg.org/css-content-3/#content-function).

Therefore, should these rather be **keywords without parentheses** instead (for now at least)?

I understand that programmers are trained to think of these as functions and empty parentheses feel normal to them, but that is not how CSS syntax has traditionally been designed – keywords are the default. 

If the CSSWG follows my rationale, I further believe that single-word keywords usually work better, especially for global ones. Alas, while `siblings` could replace `sibling-count` rather well, `index` would feel too arbitrary compared to `sibling-index`.

PS: Since these have [started shipping already](https://caniuse.com/wf-sibling-count), a resolution to this issue is urgent, but it is not too late for a change yet. 

<!-- comment id=3595965256 author=SebastianZ created=2025-12-01T11:14:29Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-3595965256 -->
## @SebastianZ — 2025-12-01

I generally agree that we should introduce keywords for them.

> If the CSSWG follows my rationale, I further believe that single-word keywords usually work better, especially for global ones. Alas, while `siblings` could replace `sibling-count` rather well, `index` would feel too arbitrary compared to `sibling-index`.

Here I'd contradict, as it's more consistent to have a common prefix for both functions. Though I don't have a strong opinion on that.

> PS: Since these have [started shipping already](https://caniuse.com/wf-sibling-count), a resolution to this issue is urgent, but it is not too late for a change yet.

It's probably not _that_ urgent, because I'd argue that a function without parameters is equal to its keyword counterpart. So it's not a breaking change that holds up shipping.

Sebastian

<!-- comment id=3596117596 author=Loirooriol created=2025-12-01T11:52:42Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-3596117596 -->
## @Loirooriol — 2025-12-01

Currently `counter-increment: foo sibling-index` increments the counters `foo` and `sibling-index`. But if `sibling-index` becomes an `<integer>`, would it instead increment `foo` by a `sibling-index` amount? Seems potentially problematic.

<!-- comment id=3598338312 author=Crissov created=2025-12-01T18:50:49Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-3598338312 -->
## @Crissov — 2025-12-01

My preference for a single word is not a strong one either. 

In some cases, like the counter properties, due to unfortunate value syntax, any or certain new global keyword can indeed be (mis)interpreted as a custom ident. One unsatisfying way to circumvent this, would be to limit the places or contexts such keywords can be used in, e.g. `<calc-sum>` (like [`<calc-keyword>`](https://www.w3.org/TR/css-values-4/#calc-keywords)) or as the exclusive value of a property (like [explicit defaults](https://www.w3.org/TR/css-cascade/#defaulting-keywords)).

<!-- comment id=4145609271 author=tabatkins created=2026-03-27T22:03:28Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-4145609271 -->
## @tabatkins — 2026-03-27

No, we can't do this; it's *inherently* dangerous, as it would be introducing new global keywords to all properties and values. New global *functions* are safe to introduce (we just have to check that they don't clash with any property-specific functions, of which there are few), but global keywords end up clashing with author-defined names in a lot of places, and require special handling in math functions (explicitly making them a `<calc-keyword>` as well).

Having to use a `()` at the end of the word is not a big cost, and makes the dynamic-ness of the value clear.

<!-- comment id=4182900462 author=Crissov created=2026-04-03T10:23:55Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-4182900462 -->
## @Crissov — 2026-04-03

I understand that the _global keyword_ path is out of the question for *design* reasons. I find it less intuitive how these would also apply to adding them as [_numeric variables_](https://drafts.csswg.org/css-values-4/#calc-variables). Parentheses quickly pile up within (math) functions, so being able to drop some would be welcome.

By the way, perhaps the resolution to #12824 then should be to introduce a `page-count()` _global function_ (and probably `page-index()` as well), and prefer it over the existing `pages` _counter_. Although still an _expensive_ feature, it could be scope-reduced by an optional parameter – and likewise the other counters I vaguely suggested in [a comment there](https://github.com/w3c/csswg-drafts/issues/12824#issuecomment-3306957554).

<!-- comment id=4186264258 author=Loirooriol created=2026-04-04T03:53:58Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-4186264258 -->
## @Loirooriol — 2026-04-04

I don't think it would be intuitive for functions to magically become keywords inside `calc()`

<!-- comment id=4603133587 author=Crissov created=2026-06-02T13:58:41Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-4603133587 -->
## @Crissov — 2026-06-02

@Loirooriol Well, my initial point was that they should not be functions in the first place, unless they start taking arguments. I acknowledge the counterpoint that adding global keywords has more and worse side-effects than adding global functions. 

I‘m pretty sure that the vast majority of use cases does not assign their integer value to a property verbatim but does some calculations with them first. Therefore, treating them more like [math constants](https://drafts.csswg.org/css-values/#calc-keywords) (`pi`, `nan` etc.) or [RCS components](https://drafts.csswg.org/css-color-5/#component-keyword) (`r`, `g`, `b` etc.) would make sense to me. If the keywords were short enough, the encapsulation within a math function could still be shorter than the current function names, e.g. `calc(i)` = `sibling-index()` and `calc(n)` = `sibling-count()` (in the extreme case).

<!-- comment id=4606255266 author=Loirooriol created=2026-06-02T19:21:45Z url=https://github.com/w3c/csswg-drafts/issues/13172#issuecomment-4606255266 -->
## @Loirooriol — 2026-06-02

Well, I want them to take arguments: https://github.com/w3c/csswg-drafts/issues/9572
