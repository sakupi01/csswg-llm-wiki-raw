---
number: 13079
title: "[css-anchor-position] Scrollable Containing Block + Fallbacks/`position-visibility: no-overflow` & visibility of positioned element to users"
state: "open"
labels: []
author: "dshin-moz"
created_at: "2025-11-07T21:09:45Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13079"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:27Z"
---

So, we started using Scrollable Containing Block for things with default anchors, as of [#12552 (comment)](https://github.com/w3c/csswg-drafts/issues/12552#issuecomment-3210696721).

This makes sense in terms of anchors that are initially positioned outside of the viewport/scrollport, but I think in some other cases, the behaviour is a bit counter-intuitive (With or without changes proposed in #13067, I think?). Given [this](https://github.com/user-attachments/files/23425311/scb-vs-lcb.html) example:

- First scroller, using the scrollable containing block along with a block-direction fallback, on both Chrome and Safari, the fallback never seems to trigger. That said, WebKit seems to initially use the fallback?
- Second scroller, equivalent to using Local Containing Block, triggers whenever the positioned element would overflow the scroller. I think this is intuitively what is expected - It seems odd that the first example can have the anchor element in full view but the positioned element hidden out of users' view, even if fallbacks are available.

... That said, as far as `position-visibilty: no-overflow`, things seem trickier. 

- Third scroller, using the Scrollable Containing Block, the visibility never toggles. Though, one could argue the positioned element clipping by the scrollport is a graceful enough fallback.
- Fourth scroller, using the Local Containing Block equivalent, the bottom of the `position-area` grid has a height of 0, making the positioned element always overflow, therefore never showing the positioned element.
- Fifth scroller, using anchor functions instead, seem to work the most "as expected."

Overall, I think the evaluation of fallback and visibility should be more based on the ultimate visibility of the positioned element to the users, which would seem to involve checking overflow against the local containing block. FWIW, given the fourth scroller, it feels like use of Scrollable Containing Block is still valid for at least generating the `position-area` grid...

<!-- comment id=3517106581 author=emilio created=2025-11-11T14:10:27Z url=https://github.com/w3c/csswg-drafts/issues/13079#issuecomment-3517106581 -->
## @emilio — 2025-11-11

cc @fantasai @tabatkins

<!-- comment id=3582584156 author=css-meeting-bot created=2025-11-26T18:01:44Z url=https://github.com/w3c/csswg-drafts/issues/13079#issuecomment-3582584156 -->
## @css-meeting-bot — 2025-11-26

The CSS Working Group just discussed ``[css-anchor-position] Scrollable Containing Block + Fallbacks/`position-visibility: no-overflow` & visibility of positioned element to users``.

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> dshin: for certain cases with default anchors<br>
&lt;TabAtkins> q+<br>
&lt;JoshT> ... as a result, I noticed when we have higher overscroll area, when you have a fallback, the fallback doesn't trigger when you scroll something so that the positioned element isn't visible any more<br>
&lt;JoshT> ... it seems to defeat the purpose of having these fallbacka<br>
&lt;JoshT> ... it matters to authors that users can see it<br>
&lt;JoshT> ... the purpose of scroll-contained-block was that when you make the position ara grid<br>
&lt;JoshT> ... when you use it for an anchor that is initially outside the viewport, the grid will be 0 size<br>
&lt;JoshT> ... so you'll have items that are 0 size which doesn't make sense<br>
&lt;astearns> ack TabAtkins<br>
&lt;JoshT> ... but i don't think we can entirely get rid of it<br>
&lt;JoshT> TabAtkins: the issue here is one we recognise<br>
&lt;JoshT> ... problem is different use cases<br>
&lt;JoshT> ... if it's just a layout primative, you just want the scrollable containing block<br>
&lt;JoshT> ... but if using as a popover, you want the fixed containing block, not the local<br>
&lt;JoshT> ... these are two extremely different use cases<br>
&lt;JoshT> ... with the behaviour and sizing<br>
&lt;JoshT> ... so there is no way to magically determine the default<br>
&lt;JoshT> ... resolution before lets authors opt into one<br>
&lt;JoshT> ... and we just needed to choose one or the other as the default<br>
&lt;JoshT> ... current behaviour is largely worthless but it does live inside the scrollable area<br>
&lt;emilio> q+<br>
&lt;JoshT> ... so we thought it was better to use that<br>
&lt;JoshT> astearns: what is the switch?<br>
&lt;JoshT> TabAtkins: add to the position properrty another keyword to choose which containing block you use, local, scrollable or fixed<br>
&lt;JoshT> ... for legacy reasons, it didn't matter when we first decided, local is the default<br>
&lt;JoshT> ... so as soon as you start scrolling, that is off the screen<br>
&lt;JoshT> ... abspos didn't care about that much, but anchor pos does<br>
&lt;JoshT> ... they're on opposite sides of the spectrum<br>
&lt;astearns> ack emilio<br>
&lt;JoshT> emilio: the fixed containing block would still cause the popup to scroll along with the anchor, right?<br>
&lt;JoshT> TabAtkins: yes, would still scroll, but would treat the scroll port as its overflow bounds<br>
&lt;JoshT> emilio: so position-visibility do interact badly with this and it's not worth fixing with the scroll containing block bounds. is that what you mean?<br>
&lt;JoshT> ... dshin was proposing using the fixed containing block of the scroller so that the visibility checks work as they would otherwise<br>
&lt;JoshT> ... assuming you want the layout behaviour of the scrollable containing block, is there any reason for using it as a fallback mechanism<br>
&lt;JoshT> TabAtkins: so there is behaviour where the scrollable containing block-- the root scroller, if you use this as layout primative, it means if the anchor is off screen, you sitll get the behaviour you want<br>
&lt;dshin> q+<br>
&lt;JoshT> ... if you use the fixed containing block, you're just outside of the containing block below the fold<br>
&lt;JoshT> ... and you will go through your fallbacks because you're outside of the ?? by default<br>
&lt;JoshT> ... but yes there's two use cases and you can't satisfy both at the same time.<br>
&lt;JoshT> ... so we need to let authors opt in<br>
&lt;TabAtkins> s/??/CB/<br>
&lt;astearns> ack dshin<br>
&lt;JoshT> dshin: thank you emilio that is my position<br>
&lt;JoshT> ... so TabAtkins you say if you want the scroll containing block, you wouldn't want the fallback to trigger<br>
&lt;JoshT> s/you wouldn't want//<br>
&lt;TabAtkins> The fallback wouldn't ever trigger<br>
&lt;JoshT> TabAtkins: the fallback would trigger in cases<br>
&lt;JoshT> ... so if you put a caption below a table, if that table is at the bottom of the sscrollable area, it would overflow the scrollable area<br>
&lt;JoshT> ... or visa versa if it's at the top<br>
&lt;JoshT> ... it triggers less often, it requires the element to be near the edges, but it's only needed for the layout related cases, not the popover cases<br>
&lt;JoshT> dshin: authors who use it for layout, they don't care that it's not visible for authors, just that it's laid out in the right position?<br>
&lt;JoshT> TabAtkins: correct. so even if they're off screen, does it matter?<br>
&lt;JoshT> ... you don't need to go through your fallbacks because it's below the fold<br>
&lt;JoshT> ... but with a popup, you only trigger it if the anchor is on screen, different use case<br>
&lt;JoshT> dshin: so we think if people want to use it for layout, people won't care about visibility?<br>
&lt;JoshT> ... as long as people can choose, that seems OK<br>
&lt;JoshT> astearns: are you OK with us closing this issue, or should we work through the cases with the switch?<br>
&lt;JoshT> dshin: yes I will investigate<br>
&lt;JoshT> astearns: OK!<br>
</details>
