---
number: 13925
title: "[cssom] newly performed scroll should only abort smooth scroll in the same axis"
state: "open"
labels: ["Needs Design / Proposal", "cssom-view-1"]
author: "nwidynski"
created_at: "2026-05-13T21:32:20Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13925"
comments: 3
has_resolution: false
synced_at: "2026-07-23T00:54:54Z"
---

Issue: https://bugzilla.mozilla.org/show_bug.cgi?id=2039246

> When a user agent is to perform a scroll of a scrolling box box, to a given position position, an associated element or pseudo-element element and optionally a scroll behavior behavior (which is "auto" if omitted), the following steps must be run:

 >   1. Abort any ongoing smooth scroll for box.

Mutating scroll offset in a different axis than an ongoing smooth scroll should not cause the ongoing scroll to be cancelled. This means that these two should be functionally equivalent, whereas they are currently not. 

```js
element.scrollLeft = 0;
element.scrollTop = 0;
```

```js
element.scrollTo({left: 0, top: 0});
```

<!-- comment id=4983195463 author=css-meeting-bot created=2026-07-15T16:55:40Z url=https://github.com/w3c/csswg-drafts/issues/13925#issuecomment-4983195463 -->
## @css-meeting-bot — 2026-07-15

The CSS Working Group just discussed `[cssom] newly performed scroll should only abort smooth scroll in the same axis`, and agreed to the following:

* `ACTION: Figure out how to make the expectations here work out (at least for the end result)`

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> smfr: proposal is to keep smooth scroll working if you do a programmatic scroll on the other axis<br>
&lt;emilio> ... I'd like to object to this<br>
&lt;emilio> ... it adds a lot of complexity both in the engine, but also on the iOS scroll view it's just impossible to implement<br>
&lt;emilio> astearns: we could put this as a "may" in the spec<br>
&lt;emilio> q+<br>
&lt;emilio> smfr: maybe, I think I'd like to say a compelling use case<br>
&lt;astearns> ack emilio<br>
&lt;emilio> ... if there is one it might be ok<br>
&lt;flackr> q+<br>
&lt;fantasai> emilio: I was going to say something similar.<br>
&lt;fantasai> emilio: I think it's conceptually nice if we could do this, but I think programmatic scrolling while smooth scrolling is rare enough<br>
&lt;fantasai> emilio: If there's a good use case for it, seems fine<br>
&lt;astearns> ack flackr<br>
&lt;smfr> q+<br>
&lt;emilio> flackr: main concern is whether we can tell the difference between a programmatic scroll that scrolls just one axis vs both<br>
&lt;emilio> q+<br>
&lt;astearns> ack fantasai<br>
&lt;emilio> fantasai: part of the problem is not that the animation is getting cut off but that the scroll ends up at the wrong position<br>
&lt;emilio> ... I think it'd be fine to cut off the animation as long as you end up where you're supposed to go<br>
&lt;emilio> ... so at least jump both to the intended destination<br>
&lt;emilio> ... so that'd be more consistent at least?<br>
&lt;emilio> flackr: what does it mean to programatically scroll in one axis<br>
&lt;fantasai> emilio: I think scrollBy allows it<br>
&lt;emilio> emilio: same as setting scrollTop, maybe you don't want to change scrollLeft<br>
&lt;astearns> ack smfr<br>
&lt;emilio> flackr: but if you set both scrollTop and scrollLeft but one to the same value you might break some use cases<br>
&lt;emilio> smfr: the impact of programmatic scroll with user scrolling is not super well defined<br>
&lt;emilio> ... but if a user scrolls it'd be weird to jank the other axis<br>
&lt;astearns> ack emilio<br>
&lt;fantasai> emilio: Related to that, there's case of on the phone, you might be scrolling both axes at the same time<br>
&lt;fantasai> ... feels weird<br>
&lt;fantasai> emilio: I would assume we'd consider ... setting scrollTop to same value would still programmatically scroll in the y axis<br>
&lt;fantasai> emilio: but, I think all these reasons at least hint at us, we should push back and say, what are the use cases?<br>
&lt;fantasai> emilio: If there's a good use case, let's tackle it<br>
&lt;fantasai> emilio: Same for scrollIntoView(). If it doesn't scroll in one axis, did you really programmatically scroll that axis??<br>
&lt;flackr> +1, we should do this, but it feels nuanced and we need to be careful to do this well<br>
&lt;fantasai> https://bug2039246.bmoattachments.org/attachment.cgi?id=9585948<br>
&lt;emilio> fantasai: If we look at the test-case for the person that filed this issue<br>
&lt;emilio> ... [linked above]<br>
&lt;emilio> ... load the test-case and scroll to square 11<br>
&lt;emilio> ... if you set scrollLeft = 0 and scrollTop = 0 it doesn't reset<br>
&lt;smfr> q+<br>
&lt;emilio> ... if there's a smooth scroll effect we forget about the previous request<br>
&lt;emilio> ... this is confusing<br>
&lt;astearns> ack fantasai<br>
&lt;emilio> flackr: I think we should fix this, but the right update is nuanced<br>
&lt;emilio> q+<br>
&lt;lea> agree we should fix this. Perhaps we can resolve to that first, before we resolve on a solution<br>
&lt;kizu> +1<br>
&lt;emilio> fantasai: I think the right thing to do is get to the right position<br>
&lt;astearns> ack smfr<br>
&lt;emilio> smfr: the content could fix this by calling scrollTo(0, 0)<br>
&lt;emilio> ... we could probably fix this by coalescing<br>
&lt;emilio> q+<br>
&lt;emilio> fantasai: why isn't this the right thing?<br>
&lt;emilio> smfr: the scrolling coalescing is pretty complicated already<br>
&lt;lea> q+<br>
&lt;emilio> ... but maybe we can make this work<br>
&lt;emilio> fantasai: there's no reason why scrollLeft=0 scrollTop=0 doesn't get you there<br>
&lt;emilio> ... there's no obvious reason, this is a weird subtlety that we introduced when animating the scroll<br>
&lt;astearns> ack emilio<br>
&lt;PaulG> Just "reset" when you only scrolled right doesn't work. That just seems broken and uncomplicated.<br>
&lt;emilio> ... so yes, but why would you think that'd be necessary<br>
&lt;emilio> q+<br>
&lt;emilio> lea: Agree with elika<br>
&lt;flackr> +1 i agree with fantasai<br>
&lt;emilio> ... yeah there's a workaround but it's unexpected that these things don't work as expected<br>
&lt;emilio> ... this is related to the fact that there's no way to run stuff at the end of these<br>
&lt;emilio> ... these don't return a promise<br>
&lt;emilio> ... maybe we can agree to fix this?<br>
&lt;astearns> ack lea<br>
&lt;astearns> ack emilio<br>
&lt;fantasai> emilio: You can run code at the end of the ?.<br>
&lt;fantasai> ... There's a ScrollEnd event<br>
&lt;astearns> s/?/scrolling animation/<br>
&lt;smfr> ...via promises<br>
&lt;fantasai> ... But more fundamentally, I agree with the sentiment. I won't object to trying to fix this. But fundamentally, the issue is that we're turning an asynchronous operation to a synchronous one.<br>
&lt;fantasai> ... If animating, you get a different value<br>
&lt;lea> authors should not have to write extra code to make things work that they've already communicated their intent for<br>
&lt;fantasai> ... These APIs will behave [missed]<br>
&lt;fantasai> ... The API is just a setter.<br>
&lt;fantasai> ... Maybe this shouldn't have been a setter to begin with, but oh well<br>
&lt;fantasai> ... But don't disagree necessarily to fix this but ... scrollTop/scrollLeft are pretty messed up<br>
&lt;fantasai> astearns: I don't think we'd be able to get those snippets to work identically, but makes sense to have them end up at the same position<br>
&lt;Zakim> fantasai, you wanted to propose that instead of aborting a smooth scroll, we abort the animation and jump to the intended destination<br>
&lt;smfr> q+<br>
&lt;emilio> fantasai: I don't care about what we do in the animations, but instead of saying that we abort any existing scroll operations, we synchronously scroll to the intended position<br>
&lt;emilio> q+<br>
&lt;astearns> ack smfr<br>
&lt;emilio> smfr: I think we can do this without aborting the animation<br>
&lt;emilio> ... effectively we'd coalesce the setters into a scrollTo()<br>
&lt;emilio> fantasai: I think if you want to coalesce it's fine, but we can spec that you end up where you want and leave the rest as quality of implementation<br>
&lt;PaulG> If you delete the line to scroll top and only scroll right, the button has no effect. I don't see a 2-axis problem.<br>
&lt;fantasai> emilio: I am not quite sure what you proposed works. Works when you have separate axes, but if you take it at face value, like setting scrollLeft/scrollTop you're supposed to commit pending scrolls<br>
&lt;PaulG> q+<br>
&lt;fantasai> ... breaks existing use cases where you change the target of an animation<br>
&lt;astearns> ack emilio<br>
&lt;fantasai> ... If you do what you want on the same axis, that creates bad behavior<br>
&lt;fantasai> ... That gets into coalescing thing<br>
&lt;fantasai> ... If we define the coalescing to happen once per frame or rendering loop, if everyone ok with that, seems reasonably simple to spec<br>
&lt;fantasai> smfr: I think that's effectively what we do now<br>
&lt;fantasai> smfr: Will have to check what we do now. But conceptually could work<br>
&lt;astearns> ack PaulG<br>
&lt;fantasai> PaulG: Trying to understand what's expected and what's a bug<br>
&lt;emilio> PaulG: trying to understand what the behavior is expected and what's a bug. If in the test-case you remove parent.scrollTop it will not move<br>
&lt;emilio> ... if you only scroll to the left it wont<br>
&lt;fantasai> PaulG: If you delete scrollTop line, and you leave the scrollLeft, the button does nothing.<br>
&lt;emilio> ... but if you scroll to the bottom left it does scroll up, what causes that?<br>
&lt;emilio> fantasai: let's say you call scrollBy twice<br>
&lt;emilio> ... shouldn't you end up scrolling 20px?<br>
&lt;fantasai> emilio: I think you keep track of relative scrolls and absolute scrolls, and commit once per frame if it's relative<br>
&lt;fantasai> ... and absolute position request ...<br>
&lt;fantasai> ... Yeah, it's complicated. But I agree with your expectation<br>
&lt;fantasai> astearns: Should we take back to issue and talk about how to make these code snippets work out?<br>
&lt;emilio> astearns: should we take it back to the issue and do a bit more analysis?<br>
&lt;TabAtkins> I also can't repro Paul's description, it works as I would expect when I remove scrollLeft<br>
&lt;fantasai> ... maybe a little more analysis?<br>
&lt;TabAtkins> scrollTop, rather<br>
&lt;fantasai> emilio: Testcase also uses ? which uses scrollIntoView(), so maybe extra complication<br>
&lt;emilio> s/?/focus<br>
&lt;emilio> astearns: I think we want to fix this but still not entirely sure how<br>
&lt;fantasai> ACTION: Figure out how to make the expectations here work out (at least for the end result)<br>
</details>

<!-- comment id=5047757086 author=flackr created=2026-07-22T14:56:40Z url=https://github.com/w3c/csswg-drafts/issues/13925#issuecomment-5047757086 -->
## @flackr — 2026-07-22

I think as a developer I would expect methods that set scroll left or scroll top (e.g. `scrollLeft=`, `scrollTo({left: )`) to behave similar to setting `left/top` on an element with a transition-duration on left and top. I'd be concerned about jumping immediately to the end. I think when setting left/top it should update that parameter of the animation target. See also #5679 which looks like the same issue.

This gets more complicated when we consider relative scrolls, user input and user expectations around that.

For relative scrolls and relative input:
1. If I press page down twice, I expect to go down two pages, but currently the second press interrupts the first smooth scroll. In a scroll snap carousel, if i press right arrow twice I expect it to jump two snap positions regardless of whether I pressed it quickly or slowly. This is analogous to the scrollBy issue #6897. I think when doing a **relative scroll** in the **same direction** as the ongoing smooth scroll, I would expect it to adjust the end position. 
2. If I have an ongoing smooth scroll and I scroll in the **opposite** direction, I think I expect it to interrupt from the current position rather than adjust the eventual target.

For absolute user input:
1. If I press home after a smooth scroll to the right, should it continue the smooth scroll to the right and adjust it to scroll to the top as well? Maybe? That would be in my mind most consistent with the initial point.

<!-- comment id=5048010276 author=nwidynski created=2026-07-22T15:19:28Z url=https://github.com/w3c/csswg-drafts/issues/13925#issuecomment-5048010276 -->
## @nwidynski — 2026-07-22

> If I have an ongoing smooth scroll and I scroll in the opposite direction, I think I expect it to interrupt from the current position rather than adjust the eventual target.

> If I press home after a smooth scroll to the right, should it continue the smooth scroll to the right and adjust it to scroll to the top as well? Maybe? That would be in my mind most consistent with the initial point.

Yes, I would agree. We currently get this done by coalescing multiple calls during a single frame and then flushing them at once. It would be nice if the engine could do this for us, since this approach only works within the scope of our code.
