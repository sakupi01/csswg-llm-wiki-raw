---
number: 13687
title: "[css-typed-om-1] Should partial reification of list-valued properties be allowed?"
state: "open"
labels: ["Closed Rejected as Wontfix by CSSWG Resolution", "Needs Testcase (WPT)", "css-typed-om"]
author: "janvarga"
created_at: "2026-03-19T15:41:47Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13687"
comments: 1
has_resolution: true
synced_at: "2026-07-04T04:45:42Z"
---

It is not entirely clear how CSS Typed OM Level 1 intends list-valued properties to behave when only some iterations are representable.

Example:

```js
const el = document.createElement("div");

el.style.transitionTimingFunction = "linear, ease";
console.log(el.attributeStyleMap.getAll("transition-timing-function"));
// [CSSKeywordValue("linear"), CSSKeywordValue("ease")]

el.style.transitionTimingFunction = "linear, step-start";
console.log(el.attributeStyleMap.getAll("transition-timing-function"));
// [CSSStyleValue]  (single property-tied unsupported value)
```

In the second case, `step-start` may be canonicalized to `steps(1, start)`, which is not representable in Typed OM Level 1.

The spec text in §5.2 (“Unrepresentable Values”) says:

> “When this is the case, the property is reified as a CSSStyleValue for a particular property...”

This suggests an all-or-nothing behavior at the property level.

However, list-valued properties are otherwise processed per-item (e.g. via `getAll()`), which makes it unclear whether partial reification should be allowed.

<!-- comment id=4556566101 author=css-meeting-bot created=2026-05-27T16:37:02Z url=https://github.com/w3c/csswg-drafts/issues/13687#issuecomment-4556566101 resolution=true -->
## @css-meeting-bot — 2026-05-27

The CSS Working Group just discussed `[css-typed-om-1] Should partial reification of list-valued properties be allowed?`, and agreed to the following:

* `RESOLVED: Affirm that reification of subcomponents is all-or-nothing`

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> TabAtkins: someone thought spec for typed om was unclear in cases where a list value property, which in typed om returns list of individual items<br>
&lt;kbabbitt> ... what happens when some items in list are representable in typed om objects now, but some aren't?<br>
&lt;kbabbitt> ... spec suggests that there's an all or nothing thing<br>
&lt;kbabbitt> ... if any part is not representable in typed om objects, whole thing becomes CSSStyleValue which gives you value as a string<br>
&lt;kbabbitt> ... author was wondering if it was meant to be a per-item behavior<br>
&lt;kbabbitt> ... I don't have a strong opinoin, I think original intention was all-or-nothing, if any part was unrepresentable, entire property is unrepresentable<br>
&lt;kbabbitt> ... but I'd be fine going the other way<br>
&lt;emilio> q+<br>
&lt;kbabbitt> ... wanted to check in with other implementers<br>
&lt;kbabbitt> weinig: what do current impls do?<br>
&lt;kbabbitt> TabAtkins: looks like they do what the spec says, entire thing becomes unrepresentable<br>
&lt;kbabbitt> weinig: we should stick with that<br>
&lt;kbabbitt> emilio: seems a bit more forward compatible<br>
&lt;kbabbitt> ... especially if we want to match typed subclass for some of these cases<br>
&lt;kbabbitt> +1 emilio<br>
&lt;kbabbitt> astearns: if I understand correctly, this is only about reification of property value in typed om<br>
&lt;kbabbitt> ... so in a case where you have a shorthand, and it has some component that is not representable, there's no implication here that when that shorthand applies its typed values to the various longhands, the longhands do not ...<br>
&lt;kbabbitt> weinig: [missed]<br>
&lt;kbabbitt> astearns: fact that whole shorthand becomes a string doesn't affect animatability of typed values for longhands<br>
&lt;kbabbitt> TabAtkins: purely a question of do we have object interfaces for those values, unrelated to what value is, animatability, etc.<br>
&lt;kbabbitt> ... does seem like people are leaning toward affirming spec's current behavior, that properties are all or nothing<br>
&lt;kbabbitt> astearns: lacking a particular use case for changing behavior, I think we should go with reality<br>
&lt;kbabbitt> PROPOSED: Affirm that reification of subcomponents is all-or-nothing<br>
&lt;kbabbitt> RESOLVED: Affirm that reification of subcomponents is all-or-nothing<br>
</details>
