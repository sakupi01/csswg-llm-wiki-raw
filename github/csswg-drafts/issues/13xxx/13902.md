---
number: 13902
title: "[css-values-4] Calc simplification of slightly non-trivial products"
state: "open"
labels: ["css-values-4"]
author: "tabatkins"
created_at: "2026-05-08T19:28:18Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13902"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:45:41Z"
---

While looking at #13069 (a PR adding another simplification case for calc()), I realized that the WPT linked in the comment actually depends on *another* simplification that Chrome does that isn't in the spec, which all three major browsers do *differently*. So we need to decide which way we actually want it to work.

The question is what happens when you have a Product of two values, one of which is a number and the second is a non-trivial Sum. For example:

`1 * (min(10px, 20%) + 30px)`

The [simplification algorithm](https://drafts.csswg.org/css-values-4/#calc-simplification) has a few cases for simplifying Products. In particular, Step 9.3 *almost* applies: it simplifies cases like `2 * (10px + 20%)` into `20px + 40%`, which contain just a number and a Sum. But it requires the Sum to be only numeric values; if it contains anything else (like a `min()` function), it doesn't apply per spec.

Here are a few variations on this theme across browsers. All three browsers *agree* on cases 2, 3, and 5. Chrome and Firefox agree on case 1, while Safari disagrees. All three browsers disagree on case 4.

<details><summary><a href="https://software.hixie.ch/utilities/js/live-dom-viewer/saved/14712">LiveDomViewer link</a></summary>
<pre>
&lt;!DOCTYPE html>
&lt;body>
&lt;script>
function test(val) {
 val = "calc(" + val + ")";
 document.body.style.marginLeft = val;
 w(`input:  ${val}`);
 w(`parsed: ${document.body.style.marginLeft}`);
 //w(`gCS():  ${getComputedStyle(document.body).marginLeft}`);
 w("==========")
}
test("1 * (min(10px, 20%) + 30px)");
test("-1 * (min(10px, 20%) + 30px)");
test("2 * (min(10px, 20%) + 30px)");
test("0px - 1 * (min(10px, 20%) + 30px)");
test("0px - 2 * (min(10px, 20%) + 30px)");
&lt;/script>
</pre>
</details>

The agreeing cases are all correct, per spec. For cases 2 and 5, Safari is correct per spec, while both Chrome and Safari do something different.

* In Case 1, the Product is `1 * ...`, and both Chrome and Firefox just return the `...`. (If the number is anything else, `-1` or `2`, they don't do that, and match Safari, as you can see in cases 2 and 3.)
* In case 4, same thing, producing `0px - (min(10px, 20%) + 30px)`. Firefox then additionally does a Negate simplification, turning the `30px` into `-30px` *and the `min(10px, 20%)` into a `max(-10px, -20%)`*! That's definitely not in the spec (but I think it's a *valid* simplification to perform).

# Chrome
```
log: input:  calc(1 * (min(10px, 20%) + 30px))
log: parsed: calc(30px + min(10px, 20%))
log: ==========
log: input:  calc(-1 * (min(10px, 20%) + 30px))
log: parsed: calc(-1 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(2 * (min(10px, 20%) + 30px))
log: parsed: calc(2 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(0px - 1 * (min(10px, 20%) + 30px))
log: parsed: calc(30px - min(10px, 20%))
log: ==========
log: input:  calc(0px - 2 * (min(10px, 20%) + 30px))
log: parsed: calc(0px - (2 * (30px + min(10px, 20%))))
```

# Firefox
```
log: input:  calc(1 * (min(10px, 20%) + 30px))
log: parsed: calc(30px + min(10px, 20%))
log: ==========
log: input:  calc(-1 * (min(10px, 20%) + 30px))
log: parsed: calc(-1 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(2 * (min(10px, 20%) + 30px))
log: parsed: calc(2 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(0px - 1 * (min(10px, 20%) + 30px))
log: parsed: calc(-30px + max(-10px, -20%))
log: ==========
log: input:  calc(0px - 2 * (min(10px, 20%) + 30px))
log: parsed: calc(0px - (2 * (30px + min(10px, 20%))))
```

# WebKit
```
log: input:  calc(1 * (min(10px, 20%) + 30px))
log: parsed: calc(1 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(-1 * (min(10px, 20%) + 30px))
log: parsed: calc(-1 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(2 * (min(10px, 20%) + 30px))
log: parsed: calc(2 * (30px + min(10px, 20%)))
log: ==========
log: input:  calc(0px - 1 * (min(10px, 20%) + 30px))
log: parsed: calc(0px - (1 * (30px + min(10px, 20%))))
log: ==========
log: input:  calc(0px - 2 * (min(10px, 20%) + 30px))
log: parsed: calc(0px - (2 * (30px + min(10px, 20%))))
```


============

So, questions: 

1. Do we want to accept this `1 * ...` => `...` simplification that Chrome and Firefox do?
2. Do we want to accept this `- min(X, Y)` => `max(-X, -Y)` simplification that Firefox does?

/cc @emilio @weinig @lilles 

<!-- comment id=4410947115 author=weinig created=2026-05-09T01:20:29Z url=https://github.com/w3c/csswg-drafts/issues/13902#issuecomment-4410947115 -->
## @weinig — 2026-05-09

I don’t really see a huge benefit in adding additional simplification rules, I don’t think there is much benefit to the user. 

Of the two suggested, I wouldn’t object to #1, especially if Chrome and Firefox already do it, but I don’t think #2 is worthwhile (it’s actually more characters in the simplified form, though I do realize the goal is not reducing character count). 

<!-- comment id=4412015939 author=emilio created=2026-05-09T08:16:59Z url=https://github.com/w3c/csswg-drafts/issues/13902#issuecomment-4412015939 -->
## @emilio — 2026-05-09

Yeah I'd be ok removing the min -> max transformation.

<!-- comment id=4412108362 author=jfkthame created=2026-05-09T09:03:31Z url=https://github.com/w3c/csswg-drafts/issues/13902#issuecomment-4412108362 -->
## @jfkthame — 2026-05-09

It's a separate issue from exactly what simplifications we want, but mentioning for clarification: in the 4th case here, surely Chrome's current result is erroneous?

> log: input:  calc(0px - 1 * (min(10px, 20%) + 30px))
> log: parsed: calc(30px - min(10px, 20%))

Those are not at all equivalent expressions: the 30px has lost its negation.

<!-- comment id=4435553932 author=tabatkins created=2026-05-12T23:06:29Z url=https://github.com/w3c/csswg-drafts/issues/13902#issuecomment-4435553932 -->
## @tabatkins — 2026-05-12

@jfkthame Whoops it certainly is a bug! And it appears to be *caused by* the `1 * ...` simplification - if you reduce it to just `calc(0px - (min(10px, 20%) + 30px))`, Chrome correctly serializes to `calc(-30px - min(10px, 20%))`.

<!-- comment id=4435565051 author=tabatkins created=2026-05-12T23:08:41Z url=https://github.com/w3c/csswg-drafts/issues/13902#issuecomment-4435565051 -->
## @tabatkins — 2026-05-12

I also think both edits are fairly low value. The first (`1 * ...`) is something that doesn't really happen in practice, I think, just in test cases. I just want to get opinions in before I close the issue and fix some WPTs. ^_^
