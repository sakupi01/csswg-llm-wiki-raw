---
number: 13414
title: "[css-flexbox] Flex lines count"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-flexbox-1"]
author: "tabatkins"
created_at: "2026-01-30T00:10:36Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13414"
comments: 5
has_resolution: true
synced_at: "2026-07-04T04:45:38Z"
---

We [just resolved](https://github.com/w3c/csswg-drafts/issues/3070) to add `flex-wrap: balance`, but deferred resolving on exactly how to ensure that you balance across a certain minimum amount of lines.

The use-case is something like the Wikipedia references section, which on most desktop screens lays out across two columns. Today, that's done with Multicol balancing + break-inside:avoid, and is very expensive as a result (fragmentation is hard). Switching to Flexbox, with a `flex-wrap: balance 2`, would solve much more efficiently.

@fantasai's main objection is twofold, I think:

1. We should have similar functionality to Multicol, where you can specify the number of columns *or* the minimum width of the columns; the latter will compute a number from the available space.
2. We should have the line-count control apply as a more general property of Flexbox, not shoved into flex-wrap:balance.

I agree with (1), definitely. The Wikipedia use-case even shows that off; if you zoom enough, it eventually switches to a single column. That's done with an MQ today, but it would be nice to have it be done purely through the balance declaration. (And on very wide screens, it could enable more than two columns, rather than sticking with two very wide ones. In practice, Wikipedia limits its content area size anyway to maintain reasonable line lengths, so that side probably isn't an issue.)

I'm less sure about (2), but not opposed to a good argument. I'm just not sure what having a more general flex-line control would *do*, outside of balancing.

<!-- comment id=4101156045 author=tabatkins created=2026-03-20T21:56:22Z url=https://github.com/w3c/csswg-drafts/issues/13414#issuecomment-4101156045 -->
## @tabatkins — 2026-03-20

Chatted with @bfgeek about this a bit more today.

Re: `column-width` + `column-count` comparison. I've *always* found `column-width`'s behavior weird, where the size you specify is *not* the size you'll actually get, but rather a *minimum* column size, and we'll actually grow the columns to make them snugly fit the container. `column-count` is similarly a bit limiting; it specifies an exact column count, but gives you no way to express a *minimum* or *maximum* number of columns. As written, we'd have to add either additional properties (`column-count-min`/`-max`) or additional syntax to `column-count` to achieve that. It would probably have made more sense initially to *just* have `column-count` and rely on math functions to do all these other things, except that math functions and unit division didn't exist at the time.

(Also, `column-count` doesn't accept length/percentages, so `round(down, 100% / 200px)` can't actually resolve as stated. That's addressed in the proposal below.)

-----------

So here's our proposal:

* Leave `flex-wrap: balance` as just the keyword, as already resolved.
* Add a `flex-line-count: <integer>` property.
* Add a new integer-returning function that calculates "how many lines can i squeeze into the available space?", only usable in `flex-line-count` and `column-count`. Provisionally call it `auto-fill( <length-percentage> )` - it evaluates to however many times you can fit the argument + gaps into the available space (the cross-axis size). %s resolve against the available space. Returns 1 if the available space is infinite.
	* If people want min/max counts, we can either tell them to use `clamp()`, like `clamp(2, auto-fill(100px), 5)`, or build it into the syntax directly like the Grid repeats, aka `auto-fill( <len-per> && <integer>{1,2}? )`. Ian and I are fine either way.
* When laying out flex items, the cross-axis available space is the line size (aka `full-available-space / line-count`, with gap subtracted) rather than the (today) full available space. (%s also rely on this divided space, as normal, and then are reevaluated, if necessary, after line sizing is fully determined, per existing rules.)


Open Questions:
1. How, exactly, do the lines size? Today, a `nowrap` flexbox's single line perfectly fills the flexbox, regardless of the size of the items. (You can observe this with `align-self:end` on an overflowing item.) A `wrap` flexbox's lines instead tightly wrap their contents. (Again, observable via alignment.)

	Balance is a third kind of wrap behavior. Today in Ian's prototype, the flex items get 1/N of the available space when sizing, so they'll *try* to fill the correct fraction of the space, but after that they act like `wrap`: a large item will still make its line bigger, and a bunch of small items will make the line smaller. In that latter case, `align-content` determines where the extra space goes, but none of the values try to keep the lines the *same* size.

	Outside of balance, how do we handle a line-count that produces empty lines with `wrap` or `nowrap`?

	Proposal: For `balance`, adopt Ian's current behavior. For `nowrap`, snugly fit the N lines (+gaps) into the available space, allowing items to overflow their lines if needed, guaranteeing all lines are the same size. For `wrap`, do the same thing as `balance`. We can address these sizing issues later if it proves necessary; whatever the solution is it won't change the properties proposed here.

2. Do we always preserve empty lines, or collapse them away? Wikipedia uses server-side code to switch their references section, usually two columns, to use only one column when there's only a few items in the list, which is an argument for 'collapse'. But authors could also want consistent line counts/sizes when repeating a component on a page with variable child counts, which is an argument for 'preserve'. Ian's prototype currently collapses.

	`nowrap` can't tell the difference between the two, assuming the filled lines don't change their size in response. `balance` and `wrap` definitely can tell the difference, but the existing `align-content` values don't treat the empty lines very well; they'll be *only* distributed extra space, so they'll typically be sized very differently from the filled lines (which have content size + distributed extra space).

	Proposal: Collapse empty lines away. Again, we can fix that in the future if it proves necessary, but the solution won't affect these properties being proposed today. (It'll either be a new property, or a new `align-content` value, or both.)

3. How does `nowrap` react to a >1 line count at all?
	
	Proposal: `nowrap` with a >1 line count *does* wrap as normal for all but the last line. The last line overflows if there are too many items.

4. How do we specify a reverse-filling balanced flexbox? Today, `wrap-reverse` is how you specify that lines should stack end-to-start. Should we add `balance-reverse`, or let `balance` be optionally combined with the `wrap-*` keywords?

	Proposal: Add `balance-reverse`, keep all the keywords exclusive. (We're fine with either.)

<!-- comment id=4121559638 author=tabatkins created=2026-03-24T21:33:45Z url=https://github.com/w3c/csswg-drafts/issues/13414#issuecomment-4121559638 -->
## @tabatkins — 2026-03-24

Hmmmm I just realized now that `nowrap` generating multiple lines from a >1 `flex-line-count` means that `nowrap` *also* needs a `nowrap-reverse` keyword added.

<!-- comment id=4121605899 author=Loirooriol created=2026-03-24T21:44:16Z url=https://github.com/w3c/csswg-drafts/issues/13414#issuecomment-4121605899 -->
## @Loirooriol — 2026-03-24

Why not say that nowrap forces a single line?

<!-- comment id=4171683563 author=tabatkins created=2026-04-01T17:19:52Z url=https://github.com/w3c/csswg-drafts/issues/13414#issuecomment-4171683563 -->
## @tabatkins — 2026-04-01

That's certainly possible as well! No matter what we do, `flex-line-count: 3; flex-wrap: nowrap;` has a conflict: either we (somewhat) ignore the nowrap, or we ignore the lines.

I think the `nowrap` behavior I described actually has the potential to be useful, so I lean toward resolving that way, but I'd be fine either way.

<!-- comment id=4172022385 author=css-meeting-bot created=2026-04-01T18:09:32Z url=https://github.com/w3c/csswg-drafts/issues/13414#issuecomment-4172022385 resolution=true -->
## @css-meeting-bot — 2026-04-01

The CSS Working Group just discussed `[css-flexbox] Flex lines count`, and agreed to the following:

* `RESOLVED: no-wrap should never wrap. line count is ignored for no-wrap flexboxes.`
* `RESOLVED: flex lines continue to shrink around their contents. balance is the same as wrap in terms of line sizing.`
* `RESOLVED: Percentages resolve against the container size, but available space is divided by the line count`
* `RESOLVED: used line count is capped by the number of items`
* `RESOLVED: flex-wrap: nowrap | [ wrap | wrap-reverse] || balance`
* `RESOLVED: flex-line-count: <integer>, will work on rest later`

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> TabAtkins: we resolved a little while ago to add flex-wrap-balance<br>
&lt;kbabbitt> ... we did not resolve on exactly how to ensure you balance across a minimum number of lines<br>
&lt;kbabbitt> ... if you know your layout will want 2 columns or rows, for examp[le<br>
&lt;kbabbitt> ... wikipedia's reference section on bottom of page is usually 2 columns wide<br>
&lt;kbabbitt> ... if you want to use flex-wrap-balance, without a minimum lines control, you have to use a max-height that will give 2 lines worth of stuff<br>
&lt;kbabbitt> ... you end up with overflow not great<br>
&lt;kbabbitt> ... better to just say I have 2 columns<br>
&lt;kbabbitt> ... had a little discussion about waht that syntax would be<br>
&lt;kbabbitt> ... after discussing with iank_ our proposal is<br>
&lt;kbabbitt> ... flex-line-count: &lt;integer><br>
&lt;kbabbitt> ... which provides minimum number of lines it will balance across<br>
&lt;kbabbitt> ... can result in more lnes than specified if you have enough items to fill up your main axis<br>
&lt;kbabbitt> ... but in wikipedia case I mentioned where References section doesn't have a height, it wont' ever overflow<br>
&lt;kbabbitt> ... fantasai in earlier discussion brought up functinoality like multicol<br>
&lt;kbabbitt> ... where instead of giving number of lines, you give size of line<br>
&lt;kbabbitt> ... very reasonable, there's a few ways we could have done that<br>
&lt;kbabbitt> ... could have adopted multicol approach with flex-line-size and have them fight like in multicol<br>
&lt;kbabbitt> ... proposal instead is to create a new function only usable in flex-line-count and column-count<br>
&lt;TabAtkins> auto-fill( &lt;length-percentage> )<br>
&lt;kbabbitt> ... a special purpose function that take a length-percentage and resolves to integer number of times you can repeat that into space taking gaps into account<br>
&lt;kbabbitt> ... column-width logic<br>
&lt;kbabbitt> ... benefit of doing it this way instead of copying multicol is that because it's an integer it can be used in math functions e.g. clamp()<br>
&lt;kbabbitt> ... can guarantee a min and max number of columns to resolve to<br>
&lt;kbabbitt> ... not currently possible in multicol<br>
&lt;kbabbitt> ... e.g. at least 2 columns 100px wide<br>
&lt;astearns> auto-fill() seems like it could be used in more places…<br>
&lt;kbabbitt> ... would allow that in multicol<br>
&lt;kbabbitt> ... could min-max it<br>
&lt;kbabbitt> ... get column count number determined appropriately<br>
&lt;kbabbitt> ... some layout details about line size<br>
&lt;kbabbitt> ... open questions:<br>
&lt;kbabbitt> ... how do lines size? flexbox have 2 modes<br>
&lt;kbabbitt> ... nowrap, line fills available space<br>
&lt;kbabbitt> ... wrap, wraps the elements regardless of how large flexbox is<br>
&lt;kbabbitt> ... have to decide between these two<br>
&lt;kbabbitt> ... proposal is, since balance is close to wrapping flexbox, we adopt wrapping behaviort<br>
&lt;kbabbitt> ... balanced flexbox's lines are sized according to contents<br>
&lt;kbabbitt> ... only change is, while laying out lines, if we have a line count, we adjust available space accordingly<br>
&lt;kbabbitt> .<br>
&lt;kbabbitt> .. if you say you want 2 columns, text will flow into half of the space and wrap appropriatelu<br>
&lt;kbabbitt> ... if you have a large element due to unbreakable word, or a whole bunch of small eleemnts, line wll shink or expand to fit<br>
&lt;kbabbitt> ... in common case, esp vertical flexboxes, will match natural size that flexbox dictates<br>
&lt;kbabbitt> ... second question, what if you don't have enough items?<br>
&lt;kbabbitt> ... 3 column sbut only 2 flex items<br>
&lt;kbabbitt> ... what do we do with 3rd line?<br>
&lt;kbabbitt> ... could preserve or collapse away<br>
&lt;kbabbitt> ... arguments both from impl and usability<br>
&lt;kbabbitt> ... usability: while I think there are arguments for keeping lines around, looking at use cases in the wild, if you only have 1 or 2 refs on wikipedia page, it does not use 2 columns<br>
&lt;kbabbitt> ... it switches to 1 column and fills entire space<br>
&lt;kbabbitt> ... suggests that when there's a tiny amount of space, people would prefer to fill space available rather than leave empty space behind<br>
&lt;kbabbitt> ... proposal for initial value is that if a line does not get filled it is discarded<br>
&lt;kbabbitt> ... does change sizing behavior<br>
&lt;kbabbitt> ... if you ask for 3 lines but have 2 items, they go in 2 50% sized lines<br>
&lt;kbabbitt> ... third question, how does no-wrap react to flex line count > 1?<br>
&lt;kbabbitt> ... could ignore it, that's the simplest<br>
&lt;kbabbitt> ... or could somewhat ignore no-wrap and preserve part of it<br>
&lt;kbabbitt> ... still however many lines you asked for but container acts similar to no-wrap<br>
&lt;kbabbitt> ... no line will overflow except the last<br>
&lt;kbabbitt> ... line sizing is more like no-wrap lines where they don't care about contents<br>
&lt;kbabbitt> ... 3 lines in no-wrap will be 1/3 of available space<br>
&lt;kbabbitt> ... similar to no-wrap single line<br>
&lt;kbabbitt> ... fourth question, wrap flexboxes have a wrap-reverse keyword<br>
&lt;kbabbitt> ... balance needs the same<br>
&lt;kbabbitt> ... might want to stack lines oppositely<br>
&lt;kbabbitt> ... suggestion is to address this with balance-reverse keyword<br>
&lt;kbabbitt> ... if no-wrap honors line counts, also need no-wrap-reverse for when no-wrap wraps<br>
&lt;oriol> q+<br>
&lt;kbabbitt> fantasai: that was a lot of information and a lot of open questions<br>
&lt;kbabbitt> ... first thing is, on question of how should sizing behavior be, like no-wrap or wrapping?<br>
&lt;kbabbitt> ... should go with, it's a wrapping flexbox and should size itself like one<br>
&lt;Rossen> ack fantasai<br>
&lt;kbabbitt> ... not sure if we want to split this up and take separate resolutions?<br>
&lt;kbabbitt> oriol: no-wrap and line count, think we should honor the no-wrap<br>
&lt;kbabbitt> ... and avoid things like no-wrap-reverse<br>
&lt;kbabbitt> fantasai: agree if we said no-wrap we shouldn't be wrapping<br>
&lt;kbabbitt> TabAtkins: one argument for no-wrap behavior. say you have a bunch of flexbox elements on page<br>
&lt;kbabbitt> ... variable amounts of items in them, want each to have 3 balanceed lines<br>
&lt;kbabbitt> ... if some don't have enough items to fill lines, we will drop lines and that container will have only 2 lines worth<br>
&lt;kbabbitt> ... if you want a consistent layout<br>
&lt;kbabbitt> fantasai: we need a switch, it shouldn't be no-wrap<br>
&lt;dholbert> +1<br>
&lt;kbabbitt> TabAtkins: okay with that switch, no-wrap was convenient to use, but I'm fine with deferring that<br>
&lt;sgill> q+<br>
&lt;kbabbitt> fantasai: seems like we all agree no-wrap should never wrap so maybe we can resolve on that?<br>
&lt;kbabbitt> TabAtkins: fine with that<br>
&lt;kbabbitt> fantasai: proposed is that no-wrap should not wrap<br>
&lt;kbabbitt> TabAtkins: line count is ignored for no-wrap flexboxes<br>
&lt;oriol> q-<br>
&lt;kbabbitt> Rossen: objections?<br>
&lt;kbabbitt> RESOLVED: no-wrap should never wrap. line count is ignored for no-wrap flexboxes.<br>
&lt;kbabbitt> fantasai: second question, how are the lines sized, should they size like no-wrap or wrapping flexbox?<br>
&lt;kbabbitt> ... think we should be consistent and size like wrapping flexbox<br>
&lt;kbabbitt> Rossen: other opinions?<br>
&lt;kbabbitt> ... or objections?<br>
&lt;kbabbitt> iank_: fantasai are you proposing we don't alter space given to items?<br>
&lt;kbabbitt> fantasai: haven't said anything about that yet<br>
&lt;kbabbitt> RESOLVED: flex lines continue to shrink around their contents. balance is the same as wrap in terms of line sizing.<br>
&lt;Rossen> ack sgill<br>
&lt;kbabbitt> sgill: was going to touch on what iank_ was going to mention<br>
&lt;kbabbitt> ... when you specify a line count, that is going to change the available space for the flex item when you're determining cross size right?<br>
&lt;kbabbitt> ... changes how we compute cross size<br>
&lt;kbabbitt> ... percentages resolve differentlyu<br>
&lt;kbabbitt> iank_: not percentages ,there's an open question there<br>
&lt;kbabbitt> ... just the available size, thinks like stretch and when calculating shrink-to-fit space<br>
&lt;kbabbitt> ... basically if you take the wikipedia example which has text content<br>
&lt;kbabbitt> ... and say flex-lines 2 if you don't have this logic the items will stretch across container width and then overflow<br>
&lt;kbabbitt> sgill: does this mean flex items will always have a definite available space in that dimension?<br>
&lt;kbabbitt> iank_: no<br>
&lt;kbabbitt> TabAtkins: if space is currently definite, we'll divide by 3, if indefinite will continue to be<br>
&lt;kbabbitt> fantasai: iank_ do you want to continue discussion on available space and resolve on that?<br>
&lt;kbabbitt> iank_: yes if there are no more questions<br>
&lt;kbabbitt> TabAtkins: this is the part of the proposal, if you request 3 lines, we divide available space by 3 modulo gaps<br>
&lt;kbabbitt> fantasai: what do we resolve percentages against, divided or original available space?<br>
&lt;kbabbitt> TabAtkins: don't care either way, iank_ wants to leave them as they are, I can see arguments either direction<br>
&lt;kbabbitt> ... given we have calc-size() you can achieve either thing<br>
&lt;kbabbitt> Rossen: what are they going to resolve against?<br>
&lt;kbabbitt> TabAtkins: percentages rely on full available space, auto-fill uses divided space<br>
&lt;kbabbitt> iank_: for the use case where you want content to overflow<br>
&lt;kbabbitt> ... definitely want my min size to be 100% for examplke<br>
&lt;kbabbitt> ... will still balance but will overflow in inline direction<br>
&lt;kbabbitt> ... satisfies that use case<br>
&lt;kbabbitt> ... without that you have to set width to 200% to achieve that behavior<br>
&lt;kbabbitt> ... little more flexible for that use case<br>
&lt;kbabbitt> fantasai: think that makes sense from use case perspective, bit weird when these things resolve against different size<br>
&lt;kbabbitt> ... don't want to think about how we implement in webkit but makes sense why you'd want it<br>
&lt;kbabbitt> Rossen: we can then resolve on percents resolving against available space<br>
&lt;kbabbitt> iank_: percentages resolve against the container size<br>
&lt;kbabbitt> ... and the available space is divided by the line count<br>
&lt;kbabbitt> ... we also have, if yuou have 2 items 3 flex lines divide by ?<br>
&lt;kbabbitt> Rossen: that's a follow up<br>
&lt;kbabbitt> ... objections<br>
&lt;fantasai> PROPOSED: Percentages resolve against the container size, but available space is divided by the line count<br>
&lt;kbabbitt> kbabbitt: available space divided by line count minus gaps?<br>
&lt;kbabbitt> iank_: yes<br>
&lt;fantasai> RESOLVED: Percentages resolve against the container size, but available space is divided by the line count<br>
&lt;kbabbitt> TabAtkins: proposed behavior, not enough behavior to fill lines, collapse away empty ones<br>
&lt;kbabbitt> ... use effective line count in previous discussion<br>
&lt;kbabbitt> ... 3 items 2 lines, use half the space<br>
&lt;kbabbitt> fantasai: line count is clamped by number of items?<br>
&lt;kbabbitt> TabAtkins: yes<br>
&lt;kbabbitt> fantasai: what about 3 items, one 100px tall others 20px tall?<br>
&lt;kbabbitt> ... will only fill 2 lines<br>
&lt;kbabbitt> TabAtkins: iank_ doesn't every line take an item first?<br>
&lt;kbabbitt> iank_: yes every liine will have one item<br>
&lt;kbabbitt> TabAtkins: because that's less unbalanced, 100/20/20 vs 100/40/0<br>
&lt;kbabbitt> fantasai: ok, given that, proposal for this one is: used line count is clamped by number of items<br>
&lt;kbabbitt> TabAtkins: yes<br>
&lt;kbabbitt> kbabbitt: clamped on lower end?<br>
&lt;kbabbitt> fantasai: yes, used line count is capped by the number of items<br>
&lt;kbabbitt> dholbert: [missed]<br>
&lt;kbabbitt> fantasai: dholbert was asking, what if break-before was used to keep items together<br>
&lt;kbabbitt> ... but we don't apply that now<br>
&lt;kbabbitt> ... if at some point that becomes a thing we'll have to rethink<br>
&lt;kbabbitt> Rossen: objections?<br>
&lt;kbabbitt> RESOLVED: used line count is capped by the number of items<br>
&lt;kbabbitt> aaronei: follow up, might we add a property that allows either option?<br>
&lt;kbabbitt> TabAtkins: yes that's possible in the future<br>
&lt;kbabbitt> TabAtkins: final easy one here, balance-reverse<br>
&lt;fantasai> s/aaronei/arronei/<br>
&lt;kbabbitt> ... everyone okay with that or do we need to spell differently<br>
&lt;kbabbitt> ... normally your first line is at cross start side and they stack inward this makes them stack opposite<br>
&lt;kbabbitt> fantasai: I think we should just say balance wrap reverse<br>
&lt;kbabbitt> TabAtkins: that's the other possibility but it suggests balance-wrap<br>
&lt;kbabbitt> iank_: I think that's fine<br>
&lt;kbabbitt> TabAtkins: I'm fine either way<br>
&lt;fantasai> s/balance-wrap/balance wrap/<br>
&lt;kbabbitt> ... would be less fine if we were still doing no-wrap<br>
&lt;kbabbitt> Rossen: balance wrap reverse?<br>
&lt;fantasai> flex-wrap: nowrap | [ wrap | wrap-reverse] || balance<br>
&lt;astearns> s/say balance wrap reverse/say balance wrap-reverse/<br>
&lt;kbabbitt> iank_: yes<br>
&lt;kbabbitt> Rossen: objections?<br>
&lt;astearns> +1<br>
&lt;kbabbitt> RESOLVED: flex-wrap: nowrap | [ wrap | wrap-reverse] || balance<br>
&lt;kbabbitt> TabAtkins: final one is the first thing, core proposal, flex-line-count taking an integer and a function<br>
&lt;kbabbitt> ... that calculates a number of columns out of available space<br>
&lt;kbabbitt> ... are we okay with that?<br>
&lt;kbabbitt> fantasai: it's a cool idea, what I'm struggliing with is getting us to a point where multicol, and flex, and ideally also grid have some way of expressing same concepts in same ways<br>
&lt;kbabbitt> ... instead of different way s which is where this proposal is getting us to<br>
&lt;astearns> q+<br>
&lt;kbabbitt> ... don't have a counter proposal but would be helpful from a design perspective if we can make ways of expressing this consistent<br>
&lt;kbabbitt> TabAtkins: part of the proposal is taht we add this to column-count as well<br>
&lt;kbabbitt> ... could express multicol and flex in same way<br>
&lt;kbabbitt> ... better because you get more functionality out of this<br>
&lt;iank_> `column-count: 3; column-width: 100px` is very confusing to what it does<br>
&lt;kbabbitt> ... gives us ability to clamp to max count in multicol which you can't do today<br>
&lt;iank_> q+<br>
&lt;kbabbitt> fantasai: had another question, dont' functions in calc need to compute?<br>
&lt;kbabbitt> TabAtkins: don't need to compute, can resolve at any time<br>
&lt;kbabbitt> fantasai: where else do we have that?<br>
&lt;kbabbitt> TabAtkins: percentages in layout<br>
&lt;kbabbitt> fantasai: but your count is still down to a length or percentage<br>
&lt;kbabbitt> ... this can't be resolved down to that<br>
&lt;kbabbitt> TabAtkins: calc-size cant resolve until layout either<br>
&lt;kbabbitt> fantasai: that can be put inside calc?<br>
&lt;kbabbitt> TabAtkins: yes just a math function<br>
&lt;Rossen> ack astearns<br>
&lt;kbabbitt> astearns: I would rather see this as a separate issue<br>
&lt;kbabbitt> ... to consider it a little more before we decide<br>
&lt;kbabbitt> ... I do like the idea of using this functionality in more places<br>
&lt;kbabbitt> ... dividing a length by another length seems generally useful<br>
&lt;kbabbitt> TabAtkins: you can already do that everywhere by calc()<br>
&lt;kbabbitt> ... trick here is you have access ot information the property doesn't natively have access ot<br>
&lt;kbabbitt> astearns: would like to surface other use cases for this and see if we can come up with something that can be generally applied<br>
&lt;kbabbitt> TabAtkins: ok with deferring this functionality, fantasai wanted to make sure we could do flex lines by size<br>
&lt;kbabbitt> ... okay deferring<br>
&lt;Rossen> ack iank_<br>
&lt;kbabbitt> iank_: the interaction between column-count 3 and column-width 100px is very confusing<br>
&lt;kbabbitt> ... I always have to check it<br>
&lt;kbabbitt> ... because column-width doesn't mean columns are set to that width, it means something else<br>
&lt;kbabbitt> ... this is more explicit for what author is doing vs 2 properties in flex<br>
&lt;kbabbitt> ... it's confusing in multicol, even when just using column-width<br>
&lt;kbabbitt> ... but I'm happy to resolve on just flex-lines-count and consider autofill behavior later<br>
&lt;kbabbitt> TabAtkins: that's consistent with us deciding later if we want to copy multicol<br>
&lt;kbabbitt> ... we are wide open if we defer the line sizing bit until later<br>
&lt;kbabbitt> Rossen: but you still want to resolve on line count?<br>
&lt;kbabbitt> TabAtkins: yes we need to resolve on that, core use case<br>
&lt;kbabbitt> ... doesn't work in practice without that<br>
&lt;kbabbitt> Rossen: resolve on line-count taking integer, work on rest later?<br>
&lt;kbabbitt> TabAtkins: yup<br>
&lt;kbabbitt> Rossen: objections?<br>
&lt;kbabbitt> RESOLVED: flex-line-count: &lt;integer>, will work on rest later<br>
</details>
