---
number: 13090
title: "[css-animations-2] Allow preserving animation state after regaining box"
state: "open"
labels: ["css-animations-2"]
author: "ydaniv"
created_at: "2025-11-11T09:03:35Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13090"
comments: 7
has_resolution: false
synced_at: "2026-07-04T04:45:42Z"
---

According to web-animations-2 the state of an animation is reset if the element owning the animation loses its box and then gets it recreated, i.e. get a computed `display: none` and then back to anything other than `none`. From the spec:

> If an element has a display of none and its display value had computed to none when ignoring the Transitions and Animations cascade origins, updating display to a value other than none will start all animations applied to the element by the [animation-name](https://drafts.csswg.org/css-animations-1/#propdef-animation-name) property.

There are cases, though, where you would not want this to happen, and after getting the animation played once, you'd want it to stay in its finished or removed state. Especially when considering animations that use an Animation Trigger, e.g. with a type of `once`. So, for example, you'd want the element to not get the animation replay if the element got hidden and shown again.

Here is a simple demo: https://jsbin.com/pixixolife/edit?html,css,js,output

In the above demo `animation-trigger` is polyfilled using an animation that starts `paused` with a `delay: 1ms` and `fill: backwards`.
I'm getting the animations and playing them via JS.
Then when clicking the `Toggle display` button the divs get `display: none` and then back to `block`.
Since one of them gets a `data-done="true"` attribute it gets `animation: none`, while the other gets its animation reset, and hence remains invisible.

To give a simple real-life use-case, imagine these divs live inside panes of a Tabs component that get toggled display on and off.

### Solution

Probably the only way to get around backwards compatibility here is to have a new opt-in property for preserving state.

### Questions

1. Do we want/have to set it per animation? Or could we have an inhertited-by-default property, like `interpolate-size`, that can be set once? e.g. on `:root`.
2. Should this be a sub-property of `animation`? Or should it be just named `animation-*` so that it can live on the `:root` and not be reset by `animation`?

### Proposal

My preference is adding a new property like `animation-state`, which is _NOT_ a sub-property of `animation`, and is inherited by default.
Possible values can be `normal` and `keep`, and of course `normal` as initial value.
All is bikeshedable.
This property should probably be a coordinated-list property that corresponds to `animation-name`.
In case of `animation-state: keep` the state of the animation is kept on the element for its entire lifespan, even if it loses its box, or is removed from the DOM. So that if it gets its box recreated, or is re-inserted into the DOM, the state of that animation is maintained.

<!-- comment id=3596299401 author=ydaniv created=2025-12-01T12:28:29Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3596299401 -->
## @ydaniv — 2025-12-01

/cc @birtles @flackr @DavMila @bramus 

Would appreciate your thoughts on this proposal, I think this will be more critical when we introduce `animation-trigger`.

<!-- comment id=3596361281 author=birtles created=2025-12-01T12:41:29Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3596361281 -->
## @birtles — 2025-12-01

> According to web-animations-2 the state of an animation is reset if the element owning the animation loses its box and then gets it recreated, i.e. get a computed `display: none` and then back to anything other than `none`. From the spec:
> 
> > If an element has a display of none and its display value had computed to none when ignoring the Transitions and Animations cascade origins, updating display to a value other than none will start all animations applied to the element by the [animation-name](https://drafts.csswg.org/css-animations-1/#propdef-animation-name) property.

Where is that spec text from? I couldn't see it in [Web Animations 2](https://drafts.csswg.org/web-animations-2/). (And, for what it's worth, I wouldn't expect it to be in Web Animations—having `display: none` reset the state of animations seems like something that belongs in CSS Animations where it causes the underlying Web Animations to be cancelled and then re-generated.)

<!-- comment id=3596415734 author=birtles created=2025-12-01T12:55:26Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3596415734 -->
## @birtles — 2025-12-01

Oh, nevermind, I found it in CSS Animations Level 2, [2.1 Owning Element](https://drafts.csswg.org/css-animations-2/#owning-element-section).

(The "terminate running animations" language should probably say "cancel running animations" so it's clear it invokes [the cancel procedure](https://drafts.csswg.org/web-animations-1/#canceling-an-animation-section) and causes, e.g. finish promises to be rejected.)

(And all that prose about the effect of the `display` property should come _after_ the description of what the owning element is too.)

Anyway, back to the actual proposal, is there some way to handle this in triggers? Outside of triggers it seems like this is an uncommon situation so I wonder if a new property just for this is warranted?

<!-- comment id=3598300754 author=ydaniv created=2025-12-01T18:43:17Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3598300754 -->
## @ydaniv — 2025-12-01

> Anyway, back to the actual proposal, is there some way to handle this in triggers? Outside of triggers it seems like this is an uncommon situation so I wonder if a new property just for this is warranted?

I ran into this when implementing support for CSS animations to our WAAPI-based library, and suddenly relying on finished CSS animations being removed wasn't enough to not have them play again if suddenly that element, or some ancestor, had its `display` set to `none` for some reason.
In this case this was done by using JS, but with newer native APIs I believe authors could come across this even without using JS at all.

Since I think this is more urgent for triggers, I think we could say that the triggers maintain their state, regardless of the element's box, which, if I'm not mistaken, could solve this state for connected animations.
This won't require adding anything new, just baking this in into triggers.

<!-- comment id=3598644500 author=flackr created=2025-12-01T20:11:44Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3598644500 -->
## @flackr — 2025-12-01

> Since I think this is more urgent for triggers, I think we could say that the triggers maintain their state, regardless of the element's box

For WAAPI created triggers, they will maintain their state, just as WAAPI animations do.

For CSS created triggers, the style of that element is what results in the construction or destruction of triggers. When we set `display: none`, we're no longer tracking style changes for that subtree so when any sub-elements come back we don't know if the same triggers continued to exist while it was `display: none`. I'm very concerned with the complexity required for continuing to maintain the style of elements in `display: none` subtrees that would be required to correctly track this.

<!-- comment id=3598653030 author=DavMila created=2025-12-01T20:13:49Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-3598653030 -->
## @DavMila — 2025-12-01

I don't think this is a bad idea and i could see an author wanting this though I'm not so sure if triggers make the need for this more critical. The current CSS animations behavior is essentially a `once`-triggered behavior so perhaps this is a pattern authors are well acquainted with?

Separately, I'd imagine that if we were to do this it probably shouldn't be restricted to triggered animations. But that's just my first guess.

<!-- comment id=4517914242 author=ydaniv created=2026-05-22T10:34:39Z url=https://github.com/w3c/csswg-drafts/issues/13090#issuecomment-4517914242 -->
## @ydaniv — 2026-05-22

With the resolution on #13710 which basically says that a trigger instance is created once per name and scope - meaning, per scope each unique trigger name creates a single instance in that scope, and all triggers with same name are attached to that instance - the state of that instance is now hoisted to the scope, and is not necessarily bound to a specific element declaring that trigger.

Now when an element is added to the DOM (or its box is recreated) with a specified trigger, it either creates a new instance with an initial state, or attaches to existing instance with an existing state.
This behavior is still expected, but what happens if that element is the only element specifying this trigger, and gets removed and re-entered?

We have a few options:

1. The browser maintains state (per session?) and when the element is re-entered it re-attaches to the "cached" trigger instance
2. The browser initiates cleanup of that trigger instance since it was the last element attached to that instance - which causes recreating a fresh trigger instance with initial state

I guess option 2 is safer. But that would behave differently from elements being recreated and are not the last attached to an instance.

We could argue that if you remove elements you're reducing user-agency, so it's expected you lose state. But perhaps it wouldn't be the case of losing a box and recreating it, which is something the browser can natively do. So if you caused a triggering element to be hidden and appear again, you wouldn't want it to re-trigger a `play-once` animation, right?
