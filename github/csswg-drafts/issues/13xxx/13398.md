---
number: 13398
title: "Need for detecting the typical viewing distance in media queries"
state: "open"
labels: ["mediaqueries-4", "mediaqueries-5"]
author: "Zhang-Junzhi"
created_at: "2026-01-26T17:20:37Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13398"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:45:30Z"
---

### Summary

Currently, developers lack a reliable way to detect the typical viewing distance from the current media (as defined in [CSS Values and Units § 4.1](https://drafts.csswg.org/css-values/#reference-pixel)). This can hinder the creation of adaptive designs that account for both visual and tactile interaction needs across different types of media.

### Problem

User agents may interpret CSS pixels differently—for instance, mobile devices, typically held at close reading distance, often define one CSS pixel to a smaller physical size, while desktop screens, viewed at a longer distance, often define it to a larger physical size. This ensures consistent visual perception across viewing distances, but it results in varying physical sizes of CSS pixels across different media.

Without knowledge of the underlying pixel size-anchoring method, developers cannot reliably fine‑tune tactile‑oriented properties, such as minimum touch target sizes. For example, a button that is comfortably tappable on a desktop touch screen (where `1px` maps to a larger physical pixel) may be too small on a phone (where `1px` maps to a smaller physical size), even if both report `any-pointer: coarse`.

### Use Case Example

A developer wants to ensure touch targets are physically large enough to avoid accidental activation. Today, they might write:

```
@media (any-pointer: coarse) {
  button {
    min-block-size: 44px;
    min-inline-size: 44px;
  }
}
```

But this applies the same CSS pixel dimensions regardless of the typical viewing distance of a media. If the user agent provided a way to distinguish the typical viewing distance, authors could adapt sizes more precisely:

```
/* Example of how authors might adjust sizes if the typical viewing distance of a current media were detectable */
@media (typical-viewing-distance: handheld-like) and (any-pointer: coarse) and (not (any-pointer: fine)) {
  button {
    min-block-size: 44px;
    min-inline-size: 44px;
  }
}

@media (typical-viewing-distance: desktop-like) and (any-pointer: coarse) and (not (any-pointer: fine)) {
  button {
    min-block-size: 25px;
    min-inline-size: 25px;
  }
}
```

### Why Existing Features Are Insufficient

- `width` / `height` / `resolution` media queries do not reveal the pixel size-anchoring model.

- `any-pointer: coarse` indicates the presence of a coarse pointer, but not the physical scale of CSS pixels.

- `hover` and `pointer` features describe interaction capabilities, not the underlying pixel interpretation.

### Conclusion

I believe exposing the typical viewing distance would improve developers’ ability to create tactile‑aware, accessible interfaces across diverse devices. I hope this use case can be considered for future CSS standards.

<!-- comment id=3801367810 author=Crissov created=2026-01-26T19:34:22Z url=https://github.com/w3c/csswg-drafts/issues/13398#issuecomment-3801367810 -->
## @Crissov — 2026-01-26

Are you sure that a new MQ feature is the best approach to solve this use case?

I believe touch targets should be specified in a _physical_ unit like `mm`, possibly within `min()`, `max()` or `clamp()` together with an _optical_ unit like `px`. 

For what it’s worth, considering that the dimensions of the body part used for pointing and touching do not change by viewing distance, but the choice of body part may depend on that and also their sizes slightly differ among users, I (unsuccessfully) proposed a bunch of human-sized units like `tip` and `tap` a couple of years ago in #837.

<!-- comment id=3803187518 author=Zhang-Junzhi created=2026-01-27T05:25:55Z url=https://github.com/w3c/csswg-drafts/issues/13398#issuecomment-3803187518 -->
## @Zhang-Junzhi — 2026-01-27

Thank you for your feedback. However, there is one common misunderstanding about CSS units that I’d like to clarify:

> I believe touch targets should be specified in a _physical_ unit like `mm`, possibly within `min()`, `max()` or `clamp()` together with an _optical_ unit like `px`.

Although `mm` is called a physical unit, it is still not a _true_ physical unit on screens.
Contrary to what many believe, `mm` and `px` are either both physical (e.g., in print media) or both non‑physical (e.g., on screens). On screens, all absolute length units—`px`, `mm`, `cm`, `in`, etc.—are anchored to the reference pixel, not to actual physical measurements. Their ratios are fixed by definition:
`1in = 96px = 2.54cm`, thus `1mm = (96px / 25.4) ≈ 3.7795px` in all media.
Therefore, combining `px` and `mm` with `clamp()` or similar functions does not solve the problem, because their relationship never changes across media.

<!-- comment id=3803668835 author=Crissov created=2026-01-27T07:57:40Z url=https://github.com/w3c/csswg-drafts/issues/13398#issuecomment-3803668835 -->
## @Crissov — 2026-01-27

Everybody around here very well knows this; sorry for not clearly restating it in my comment. My old proposal spun off from exactly that discussion in #614 and hence the new units would “approximate physical units that do *not* depend on the _anchor unit_ (usually `px`)”.
