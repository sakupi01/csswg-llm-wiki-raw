---
number: 13765
title: "Add outline-offset: inset"
state: "open"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Edits", "Needs Testcase (WPT)", "css-ui-4", "css-ui-5"]
author: "annevk"
created_at: "2026-04-03T07:53:11Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13765"
comments: 2
has_resolution: true
synced_at: "2026-07-04T04:45:46Z"
---

While implementing `appearance: base` for `<select>` I noticed that `outline` doesn't work as-is for the `option` elements shown in `::picker(select)`. That's because it has `overflow: auto` by default, clipping the outline.

So instead we need to draw the `outline` in the other direction (assuming we don't want to invent a new type of outline that's not clipped).

Now for auto outlines there's no `width` so you can't use a negative `outline-offset` and even though in practice you could it would not be a cross-platform value. So ideally you have something like `inset`, which negates the used auto outline width.

WebKit trunk uses such a value now for this purpose. It computes to an actual width so it doesn't violate the `outline-offset` contract, but I think if it were standardized we would want to compute to `inset` so we don't reveal the actual auto outline width value used.

For non-auto outlines just negating the used width suffices.

A concern in https://github.com/whatwg/html/issues/12286 was that user style sheets might have very large outline values and this would obscure their `appearance: base` `<option>`s. I think I can live with that, but I would also be okay with always imposing an upper limit of 5 CSS pixels or some such. But maybe we should only do that if it's actually becoming a problem. Not sure how many user style sheets there still are and users could also adjust `outline-width` on `<option>` if they wanted to.

<!-- comment id=4261553948 author=astearns created=2026-04-16T16:06:08Z url=https://github.com/w3c/csswg-drafts/issues/13765#issuecomment-4261553948 -->
## @astearns — 2026-04-16

In https://github.com/whatwg/html/issues/12286 the forms control task force decided to add the `inset` value. We should discuss this issue in the full group to see if there are any objections and go over details if not.

<!-- comment id=4791531637 author=css-meeting-bot created=2026-06-24T16:35:10Z url=https://github.com/w3c/csswg-drafts/issues/13765#issuecomment-4791531637 resolution=true -->
## @css-meeting-bot — 2026-06-24

The CSS Working Group just discussed `Add outline-offset: inset`, and agreed to the following:

* `RESOLVED: Add inset keyword to the outline-offset property`

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> astearns: we discussed this in form controls meeting<br>
&lt;kbabbitt> ... and we decided to add the inset value in that discussion<br>
&lt;astearns> https://github.com/whatwg/html/issues/12286#issuecomment-4261456319<br>
&lt;kbabbitt> ... wanted to take this back to the full group to make sure no one here had any concerns<br>
&lt;florian> q+<br>
&lt;kbabbitt> ... this is meant to be used in appearance:base situations<br>
&lt;astearns> ack florian<br>
&lt;kbabbitt> florian: I think it makes sense<br>
&lt;kbabbitt> ... the observation nthat there are contexts where the only way to make the outline visible by insetting it is valid<br>
&lt;kbabbitt> ... auto can't know how much, so we need the keyword<br>
&lt;kizu> +1<br>
&lt;kbabbitt> ... makes sense to me, supportive<br>
&lt;JoshT> q+<br>
&lt;lea> q+<br>
&lt;lea> +1<br>
&lt;lea> q-<br>
&lt;kbabbitt> JoshT: wanted to clarify, if you're able to combine it with a length value as well?<br>
&lt;kbabbitt> astearns: I believe it's a standalone keyword<br>
&lt;kbabbitt> JoshT: ok so messaging is still clear for authors, if it's not auto, use negative numbers<br>
&lt;kbabbitt> florian: could still use keyword s well, equivalent to a length<br>
&lt;kbabbitt> ... if using auto could know what that length is<br>
&lt;kbabbitt> ... if not, keyword would work anyeay<br>
&lt;kbabbitt> ... to your point I think it could be extended in the future to accept a length as well<br>
&lt;fantasai> s/using auto/not using auto/<br>
&lt;lea> +1 for the functionality, but I wish we had an actual way to get the computed outline-width instead of all these ad hoc keywords…<br>
&lt;kbabbitt> ... which would start with that value and extend further, let's wait until people ask for this<br>
&lt;JoshT> env(outline-width)?<br>
&lt;kbabbitt> fantasai: on some OSes outline is a glow effect, what's the width of that?<br>
&lt;kbabbitt> florian: here we're saying, whatever that value is, the inset keyword insets by just enough so yuou can see it<br>
&lt;kbabbitt> ... so you still need to be able to determine it<br>
&lt;kbabbitt> ... is it a keyword, an env var? I think a keyword is much simpler<br>
&lt;JoshT> +1<br>
&lt;kbabbitt> ... would rather not think about scope we'd have to expose it<br>
&lt;lea> `current(outline-width)` 😇<br>
&lt;kbabbitt> astearns: something we could think about in the future after adding this keyword<br>
&lt;fantasai> +1 to just adding the keyword<br>
&lt;kbabbitt> ... proposed resolution is to add an inset keyword to the outline-offset property<br>
&lt;kbabbitt> ... any other questions or comments?<br>
&lt;kbabbitt> ... objections?<br>
&lt;kbabbitt> RESOLVED: Add inset keyword to the outline-offset property<br>
</details>
