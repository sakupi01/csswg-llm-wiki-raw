---
number: 13012
title: "[css-conditional] Better `<supports-condition>` syntax"
state: "open"
labels: ["css-conditional-3", "css-conditional-5"]
author: "cdoublev"
created_at: "2025-10-24T07:32:08Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13012"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:45:38Z"
---

The main motivations are to accept all support features in `supports()` and prevent (almost) any invalid argument from making its context invalid ([`if()`](https://drafts.csswg.org/css-values-5/#funcdef-if), [`@when`](https://drafts.csswg.org/css-conditional-5/#at-ruledef-when), [`@import`](https://drafts.csswg.org/css-cascade-4/#at-ruledef-import)).

`@supports` prelude is defined with [`<supports-condition>`](https://drafts.csswg.org/css-conditional-3/#typedef-supports-condition), therefore it accepts (almost) any value wrapped in parens. `supports()` takes `<supports-condition>` but also other values that can make it invalid, like `supports(garbage)`.

The current (simplified) syntax is:

```
<supports-in-parens> = (<supports-condition>) | <supports-feature> | <general-enclosed>
<supports-feature> = (<declaration> | <extension-name>)
                   | at-rule(<at-keyword-token>)
                   | font-format(<font-format>)
                   | font-tech(<font-tech>)
                   | selector(<complex-selector>)
<supports()> = supports(<supports-condition> | <declaration>)
```

The suggested syntax:

```
<supports-in-parens> = (<supports-condition> | <supports-feature>)
                   | at-rule(<at-keyword-token>)
                   | font-format(<font-format>)
                   | font-tech(<font-tech>)
                   | selector(<complex-selector>)
<supports-feature> = <declaration>
                   | <extension-name>
                   | at-rule(<at-keyword-token>)
                   | font-format(<font-format>)
                   | font-tech(<font-tech>)
                   | selector(<complex-selector>)
                   | <any-value>
<supports()> = supports(<supports-condition> | <supports-feature>)
```

Related: #12903.

<!-- comment id=4175284901 author=cdoublev created=2026-04-02T07:35:58Z url=https://github.com/w3c/csswg-drafts/issues/13012#issuecomment-4175284901 -->
## @cdoublev — 2026-04-02

I am closing this issue because I think what is defined for `supports()` in `@import` is a simpler solution:

  > If a `<declaration>` is given in place of a `<supports-condition>`, it must be interpreted as a `<supports-decl>` (i.e. the extra set of parentheses is implied) and treated as a `<supports-condition>`.

Besides, `<any-value>` would have been constrained by the rules for [comma-containing productions](https://drafts.csswg.org/css-values-5/#comma-containing-productions).

**(edit)**

Re-opening because the aforementioned requirement does not apply to an *invalid* `<declaration>`, which I suspect was its intent though, ie. to match the input against `(<declaration>) | <general-enclosed>` ignoring the block context, which is equivalent to matching `<declaration> | <any-value>`, as suggested in the op, and I do not see how applying this requirement to an invalid `<declaration>` could be translated in parsing terms anyway.
