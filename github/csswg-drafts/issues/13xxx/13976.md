---
number: 13976
title: "Image Orientation in <feImage> filter nodes"
state: "open"
labels: ["Needs Thought", "css-filter-effects-2"]
author: "schenney-chromium"
created_at: "2026-05-28T01:52:59Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13976"
comments: 3
has_resolution: false
synced_at: "2026-07-23T00:54:54Z"
---

The application of image orientation EXIF data in `<feImage>` nodes is not defined. On the one hand, cross-origin content should always be oriented as a privacy feature. On the other hand, Chrome at least was never orienting images in `<feImage>`. This was recently flagged as a privacy bug and we have just made a change to switch to always applying orientation.

The decision to go with "always" is because we need to orient for cross-origin and there's no good way to plumb the CSS `image-orientation` property into the filter. I also think it would be very confusing to potentially have a single filter applied to multiple elements change appearance depending on the CSS properties of each element.

I see at least 3 options:
(a) Always apply image orientation EXIF data in `<feImage>`
(b) Use the CSS `image-orientation` property from the element with the `filter` property for that instance of the filter.
(c) Define an attribute on `<feImage>` to control the orientation (analogous to the CSS property).

Option (b) is messy to implement in Chrome but could be done. I prefer option (c) because it is very obvious in how it applies and would be consistent across instances of the filter. I have implemented option (a) in Chrome to fix the privacy problem but the web-compat issues may force a roll-back.

<!-- comment id=5045280553 author=svgeesus created=2026-07-22T11:34:11Z url=https://github.com/w3c/csswg-drafts/issues/13976#issuecomment-5045280553 -->
## @svgeesus — 2026-07-22

> (c) Define an attribute on `<feImage>` to control the orientation (analogous to the CSS property).

That would be my preferred option (and it would be a presentation attribute, ie exactly the same as the property, with 0 specificity)

<!-- comment id=5045309276 author=svgeesus created=2026-07-22T11:37:14Z url=https://github.com/w3c/csswg-drafts/issues/13976#issuecomment-5045309276 -->
## @svgeesus — 2026-07-22

Somewhat related (and needs WPT):

 - https://github.com/w3c/png/issues/310

<!-- comment id=5048993492 author=css-meeting-bot created=2026-07-22T16:53:40Z url=https://github.com/w3c/csswg-drafts/issues/13976#issuecomment-5048993492 -->
## @css-meeting-bot — 2026-07-22

The CSS Working Group just discussed `Image Orientation in <feImage> filter nodes`.

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> schenney: this came out of a privacy issue filed in Chromium<br>
&lt;kbabbitt> ... exif image orientation was being revealed for cross origin images<br>
&lt;kbabbitt> ... back in the day, for html / css image orientation property<br>
&lt;kbabbitt> ... the decision was that all cross-origin images would always be oriented<br>
&lt;kbabbitt> ... so to resolve the privacy issue in Chrome, I switched it so we always apply exif orientation inside an feImage image which puts image into filter<br>
&lt;TabAtkins> q+<br>
&lt;kbabbitt> ... this matters for filters spec because SVG references CSS filters<br>
&lt;kbabbitt> ... I agree with chris who commented in issue, best thing is to add attribute to feImage to control orientation and then solve this in SVG land<br>
&lt;kbabbitt> ... gives users control and sovles the immediate problem<br>
&lt;kbabbitt> ... only counterargument is that again a long time ago there was a desire to deprecate image orientation property entirely and always apply exif orientation<br>
&lt;kbabbitt> ... but there's been no action on that in last 10 years so I don't think that's going to happen<br>
&lt;kbabbitt> ... my proposal is option 3 in issue, work through svg and filters spec to add attribute to feImage about orientation<br>
&lt;kbabbitt> ... open to any other ideas<br>
&lt;ChrisL> +1 to option 3<br>
&lt;astearns> ack TabAtkins<br>
&lt;emilio> q+<br>
&lt;kbabbitt> TabAtkins: quick question, for images used elsewhere in css or html, is the default now to always respect orientation<br>
&lt;kbabbitt> ... and it's only if you use image-orientation that you can get original encoded orientation out?<br>
&lt;kbabbitt> schenney: yes. any place where there's no reasonable way to find an element with style that specifies image-orientation, we orient the image<br>
&lt;kbabbitt> ... there's canvas cases for this<br>
&lt;kbabbitt> ... only one this applies to is when image is drawn into canvas<br>
&lt;kbabbitt> ... there's an open issue for hta<br>
&lt;kbabbitt> TabAtkins: spec currently says for image-orientation, inital values is from-iage, ist that what we do?<br>
&lt;kbabbitt> emilio: yes<br>
&lt;kbabbitt> TabAtkins: in that case I agree, let's do it<br>
&lt;kbabbitt> ... don't care whether we add to feImage, was just waiting on information on whether image-orientation was web compatible<br>
&lt;kbabbitt> ... don't see need to propagate anywhere else<br>
&lt;astearns> ack emilio<br>
&lt;kbabbitt> emilio: agree but do we need the attribute?<br>
&lt;kbabbitt> .. can we say feImage respects image-orientation?<br>
&lt;kbabbitt> schenney: certainly an option<br>
&lt;kbabbitt> ... haven't gotten any bug reports since I changed it<br>
&lt;TabAtkins> (my opinion is that we don't need an attribute, yeah)<br>
&lt;ChrisL> I would be okay with that too<br>
&lt;kbabbitt> emilio: then my preference would be feImage respects image-orientation property<br>
&lt;kbabbitt> ... do we need a mapped attribute for any reason?<br>
&lt;kbabbitt> astearns: I thought purpose of attribute is, what's implemented is applying orientation from exif image, no way for author to change it<br>
&lt;ChrisL> q+<br>
&lt;kbabbitt> emilio: but image-orientation property would be that, right?<br>
&lt;TabAtkins> (see the note at the end of &lt;https://drafts.csswg.org/css-images-3/#propdef-image-orientation><br>
&lt;kbabbitt> schenney: problem for filters is that it's tricky to ... filter can be defined once and used all over the place<br>
&lt;kbabbitt> ... could in theory pull image-orientation from element filter is applying to, but then same filter could be different in different places<br>
&lt;kbabbitt> emilio: need to check who is embedding the filter<br>
&lt;TabAtkins> we *first* defined "none" as the initial value (no exif), but then changed it to from-image and waited for impl feedback<br>
&lt;kbabbitt> schenney: makes implemenation messin in that situation<br>
&lt;kbabbitt> ... there remain cases where you can't get an element to use<br>
&lt;kbabbitt> emilio: style would be on feImage, the thing you need to check is same-origin-ness of filter chain<br>
&lt;kbabbitt> ... whether your filter is itself cross-origin<br>
&lt;TabAtkins> now that the feedback is "everyone changed their initial value and didn't hit any problems", we should pursue what the note said, and *try* to remove 'image-orientation' entirely, and not spread image-orientation-likes to any other APIs<br>
&lt;kbabbitt> ... in which case you don't honor image-orientation<br>
&lt;kbabbitt> schenney: this issue is about whether image referenced by filter is cross-origin<br>
&lt;kbabbitt> emilio: to what?<br>
&lt;kbabbitt> schenney: to the document using the filter<br>
&lt;kbabbitt> ... security origin to which filter is applied<br>
&lt;kbabbitt> emilio: so that's the thing you need to check<br>
&lt;kbabbitt> ... you could set feImage style=image-orientation:none<br>
&lt;kbabbitt> ... but you need to tell if image is cross origin anyway<br>
&lt;kurt> q+<br>
&lt;kbabbitt> schenney: I guess feImage is an element,. could attach style to that element<br>
&lt;kbabbitt> emilio: thought that's what mapped attribute would do<br>
&lt;kbabbitt> schenney: let me test what happens, see if that's possible, in which case this issue may go away<br>
&lt;kbabbitt> ... sounds like people are okay with always applying image orientation as fallback position<br>
&lt;astearns> ack dbaron<br>
&lt;Zakim> dbaron, you wanted to react to emilio<br>
&lt;kbabbitt> dbaron: one thing schenney said I wasn't sure<br>
&lt;kbabbitt> ... there were cases that might cache an svg filter from element and use it both cross-origin and same origin?<br>
&lt;kbabbitt> ... is that an issue you were bringing up?<br>
&lt;kbabbitt> schenney: that's not an issue, only that you could have 2 elements on page with different values for image-orientation, applying same filter to each one<br>
&lt;kbabbitt> ... I don't think we're proposing that<br>
&lt;astearns> ack ChrisL<br>
&lt;kbabbitt> ChrisL: reminder, presentation attributes are not analagous to a css property, they're exactly that property with 0 specificity<br>
&lt;kbabbitt> ... any style will override<br>
&lt;kbabbitt> ... not clear when you'd want an attribute on there instead of styling it<br>
&lt;kbabbitt> ... unless you have an external styolesheet, in which case images don't allow external anything<br>
&lt;kbabbitt> schenney: I have a better understanding of issue with attribute, will confirm whether there's actually a workaround, will update issue<br>
&lt;astearns> ack kurt<br>
&lt;kbabbitt> kurt: to emilio's point I think it makes sense to look at css property on feImage because that's where href is defined, that's where we look at origin<br>
&lt;emilio> right :)<br>
&lt;kbabbitt> schenney: yes I'll test and verify that works<br>
</details>
