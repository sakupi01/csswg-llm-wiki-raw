---
number: 13774
title: "[css-text-4] Allow text-autospace: normal replace"
state: "open"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Edits", "Needs Testcase (WPT)", "css-text-4", "i18n-clreq", "i18n-jlreq", "i18n-needs-resolution"]
author: "r12a"
created_at: "2026-04-07T06:00:56Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13774"
comments: 4
has_resolution: true
synced_at: "2026-08-20T13:53:49Z"
---

https://www.w3.org/TR/css-text-4/#propdef-text-autospace

Currently, the `replace` keyword for `text-autospace` has to be used with `ideographic-alpha ideographic-numeric`, and cannot be used with the `normal` keyword.

```
normal | <autospace> | auto 
<autospace> = no-autospace |
              [ ideograph-alpha || ideograph-numeric || punctuation ]
              || [ insert | replace ]
```

Not least because `normal` is the most widely implemented part of `text-autospace`, but also because it just makes sense to allow the `replace` function to be applied while using `normal`, i propose that we alter the syntax to allow `normal replace`.  That will allow content authors to normalise gap widths while using the default setting for `text-autospace`.

Since `replace` isn't yet implemented anywhere, this should hopefully be unproblematic.

<!-- comment id=5071174341 author=r12a created=2026-07-24T14:50:41Z url=https://github.com/w3c/csswg-drafts/issues/13774#issuecomment-5071174341 -->
## @r12a — 2026-07-24

Note that the i18n WG voted to raise the status of this issue to needs-resolution, and we would be happy if it could be considered before TPAC.

<!-- comment id=5340573570 author=MurakamiShinyu created=2026-08-19T10:04:33Z url=https://github.com/w3c/csswg-drafts/issues/13774#issuecomment-5340573570 -->
## @MurakamiShinyu — 2026-08-19

I understood that `replace` was the same as `ideograph-alpha ideograph-numeric replace` (= `normal replace`). Was I wrong?

<!-- comment id=5345036821 author=css-meeting-bot created=2026-08-19T16:29:04Z url=https://github.com/w3c/csswg-drafts/issues/13774#issuecomment-5345036821 resolution=true -->
## @css-meeting-bot — 2026-08-19

The CSS Working Group just discussed `[css-text-4] Allow text-autospace: normal replace`, and agreed to the following:

* `RESOLVED: we allow 'normal replace' as a value for text-autospace`

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> florian: text-autospace fundamentally have values that are punctuation, idiograph, etc.<br>
&lt;fantasai> &lt;autospace> = no-autospace | [ ideograph-alpha || ideograph-numeric || punctuation ] || [ insert | replace ]<br>
&lt;JoshT> ... it has no autospace, ideograph apha, alphaneumetic and punctionation<br>
&lt;JoshT> ... and it has two modes. default is insert where it adds the space if it's not there<br>
&lt;JoshT> ... and then replace. if there is a space already but the wrong size, it will put the right size<br>
&lt;JoshT> ... so if you have a space glyth between a kanji, we remove that and add the right space<br>
&lt;JoshT> ... now you can only choose to insert and replace with the keywords here. you can't do that with the normal keyword.<br>
&lt;JoshT> ... idioalpha and idio-alphaneumeric together are the same as normal<br>
&lt;JoshT> ... and the i18n people want to use the replace keyword together with normal without the expanded version<br>
&lt;JoshT> ... depends on what magic we want normal to do<br>
&lt;JoshT> ... not having options next to it will keep our hands free for the future<br>
&lt;JoshT> ... but this might be a nice syntax convinience<br>
&lt;JoshT> ... but normal is the only thing implemented so far. so you can't opt into replace with the longhand versions because it's not there yet<br>
&lt;vitorroriz> WebKit also implements auto, but I think no browser implements replace (?)<br>
&lt;JoshT> ... if the problem is that it's not implemented, it should be implemented. another thing doesn't help<br>
&lt;vitorroriz> sorry, I mean normal*<br>
&lt;JoshT> ... so do we want to require people to use the expanded version in case we want normal to be more magic in the future<br>
&lt;JoshT> ... or maybe we want auto for magic?<br>
&lt;JoshT> ... I'm not convinced for the implementation argument, but can go either way otherwise<br>
&lt;JoshT> astearns: one thing about the impl issue: when a browser impl everything, is the shorthand still useful?<br>
&lt;JoshT> florian: behaviour wise it's the same but less typing<br>
&lt;jfkthame> q+<br>
&lt;vitorroriz> q+<br>
&lt;JoshT> ... but if we want normal to evolve...<br>
&lt;astearns> ack jfkthame<br>
&lt;JoshT> jfkthame: the ideograph-alpha and -alphaneumeric are implemented in gecko, but not replace<br>
&lt;JoshT> ... depends on whether insert or replace is the one people will most commonly want<br>
&lt;JoshT> ... normal now means ig-alpha<br>
&lt;JoshT> ... and it's insert<br>
&lt;JoshT> ... so it would be a change to what it currently means<br>
&lt;JoshT> florian: oh they're not asking for the normal behaviour to be switched to replace. they want to write 'normal replace'<br>
&lt;JoshT> jfkthame: so normal is simply a shorthand for the two other values? I guess I don't have a strong opion either way. I wonder how commonly people will want the replace vlaue?<br>
&lt;astearns> ack vitorroriz<br>
&lt;JoshT> vitorroriz: when you say concern is that normal could evolve somehow where we don't want to pair it with replace, if we want to have this magic, we shouldn't change it... can you give an example how normal could evolve?<br>
&lt;JoshT> florian: this is theoretical. this assumes that normal or its expansion will have two modes. if we want it to change to different modes, if the thing that normal expands to isn't a thing with two modes, etc, then having a mode switch would be weird<br>
&lt;JoshT> ... but nothing like that has been proposed<br>
&lt;JoshT> astearns: some language where if normal processing is not the same as ig-alpha, for that language normal would have some processing not part of the spec... but I don't have an example of where that would happen<br>
&lt;JoshT> ... given the request from the language experts and no clear way how we would want to evolve normal, I am inclined to support this syntax<br>
&lt;JoshT> florian: I don't think it's too needed but I don't think it's a problem<br>
&lt;JoshT> PROPOSED: we allow 'normal replace' as a value for text-autospace<br>
&lt;JoshT> RESOLVED: we allow 'normal replace' as a value for text-autospace<br>
</details>

<!-- comment id=5345178950 author=r12a created=2026-08-19T16:42:18Z url=https://github.com/w3c/csswg-drafts/issues/13774#issuecomment-5345178950 -->
## @r12a — 2026-08-19

Fwiw, [this section in the article Managing Inline Spaces in Chinese & Japanese](https://www.w3.org/International/articles/styling/inline-space#autospace) was written after this issue was raised, and it provides information about what does and doesn't work for `text-autospace` currently.
