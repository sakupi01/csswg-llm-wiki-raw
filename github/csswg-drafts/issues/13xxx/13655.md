---
number: 13655
title: "[css-values][css-conditional][css-animations] Proposal for time-based CSS"
state: "open"
labels: ["css-animations-2", "css-conditional-5", "css-values-5"]
author: "johannesodland"
created_at: "2026-03-14T11:33:08Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13655"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:36Z"
---

I'm adding this proposal to start the discussion and to see if it is possible to support conditions, animations and/or values based on the local time of the user. 

## Motivation
Over the years I've stumbled upon many uses for the users local time in layout and styles. This is currently possible my running small blocking scripts that set custom properties or start animations then can be used to style the page or component. If these values could be set directly in CSS, the code would be more cohesive and less brittle.

## Use cases

### 1. Time based theming
A mini-game is set up to have a generative color theme based on the day of the year. Each day the game has a new combination of colors. 

```css
--background-color-1: ...;
--background-color-2: ...;
--background-color-3: ...;
--background-index: mod(day-of-year(), 3);


background-color: var(ident("--background-color-" var(--background-index)));
```

### 2. Time tables/schedules
A time table is showing the current programming for a big event. The current row should be highlighted.

```css
--start-time: attr(data-start-time, <timestamp>);

background-color: if(
  time(now >= var(--start-time)): var(--highlight-background); 
  else: transparent;
);
scroll-snap-align: if(
  time(now >= var(--start-time)): start; 
  else: none;
);
```

### 3. Showing and hiding
A banner for a big event should show at a specific time.

```css
display: none;

@media time(now >= 2026-03-14T10:30:00Z) {
  display: block;
}
```

### 3. Animations
A clock-animation is supposed to follow the time of the day. 

```css
animation-timeline: time-of-day();
```

Your page should start an animation at midnight new years eve: 

```css
timeline-trigger-name: --midnight;
timeline-trigger-source: time(2026-01-01T00:00:00Z);
```

<!-- comment id=4060728379 author=Crissov created=2026-03-14T15:35:23Z url=https://github.com/w3c/csswg-drafts/issues/13655#issuecomment-4060728379 -->
## @Crissov — 2026-03-14

See #9710 for my proposal to do this with Mediaqueries. 

<!-- comment id=4062388539 author=SebastianZ created=2026-03-15T06:36:59Z url=https://github.com/w3c/csswg-drafts/issues/13655#issuecomment-4062388539 -->
## @SebastianZ — 2026-03-15

@johannesodland CamelCase is considered a [mistake](https://wiki.csswg.org/ideas/mistakes/#:~:text=currentColor%20keyword%20should%20have%20retained%20the%20dash). So the keyword in your `time()` function should rather be `current-time`, or to be more precise, `current-timestamp`, or just `now`. And the `timeOfDay()` function should be called `time-of-day()` following that rule. Though since it refers to the current day as the animation timeline, it might just be called `day()`. And that might even be generalized to a `timespan()` function that takes two timestamps or some predefined keywords for the current day and other common time spans.
Also, your use of `<time>` seems to be different from the [existing `<time>` data type](https://drafts.csswg.org/css-values/#time), as it reflects a point in time rather than a duration. Therefore it should rather be called something like `<timestamp>`, `<point-in-time>`, or `<date-time>`. (And maybe `<time>` should be renamed to `<duration>`.)

So to iterate on the initial proposal, the concrete additions would be:

* a `day-of-year()` function that returns a `<number>` value referencing the number of the current day within a year.
* a `<timestamp>` data type that refers to a specific point in time and includes the `current-time` keyword.
* a `now` keyword that represents the current timestamp.
* a `time()` function that can be used in conditions to compare two `<timestamp>` values.
* a `timespan()` function for `animation-timeline` that defines a timespan to be used as the animation timeline, referred to as a new `<timespan>` data type, which either takes two `<timestamp>` values or a keyword like `current-day`. (It might even allow to do calculations, meaning a `<timestamp>` value plus or minus a `<time>` value, but that probably also requires to extend `<time>` to take other values than seconds and milliseconds.)

With the above, I think this proposal should probably be split into multiple issues so they can be discussed separately with the media query part maybe being merged with #9710.

Sebastian

<!-- comment id=4062541759 author=johannesodland created=2026-03-15T08:19:04Z url=https://github.com/w3c/csswg-drafts/issues/13655#issuecomment-4062541759 -->
## @johannesodland — 2026-03-15

Thanks @SebastianZ. I went back and edited the proposal. 
I'll come back and split it into separate issues when I've had time to structure it a bit.

<!-- comment id=4070197832 author=johannesodland created=2026-03-16T19:50:31Z url=https://github.com/w3c/csswg-drafts/issues/13655#issuecomment-4070197832 -->
## @johannesodland — 2026-03-16

I've created https://github.com/w3c/csswg-drafts/issues/13659 to discuss numerical functions for calculations, and https://github.com/w3c/csswg-drafts/issues/13664 for discussing syncing animations with wall time. I amended that proposal a bit, seeing that a new timeline should not be necessary.

 Conditional rules are probably best discussed in the existing https://github.com/w3c/csswg-drafts/issues/9710 proposal?

I think what remains is a proposal for a type that we can use both to specify wall time and relative time, for use in these proposals.
