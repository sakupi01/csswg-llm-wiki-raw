---
number: 13959
title: "[scroll-animations] ViewTimeline IDL specifies that the `subject` attribute is non-null, but allows construction with no `subject` specified"
state: "open"
labels: ["scroll-animations-1"]
author: "weinig"
created_at: "2026-05-23T22:18:51Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/13959"
comments: 2
has_resolution: true
synced_at: "2026-07-30T00:49:41Z"
---

The [ViewTimeline IDL](https://drafts.csswg.org/scroll-animations/#viewtimeline) specifies that the `subject` attribute is non-null

```
readonly attribute Element subject;
```

but allows construction with no `subject` set since [ViewTimelineOptions](https://drafts.csswg.org/scroll-animations/#dictdef-viewtimelineoptions) does not make `subject` a required member.

Later in the prose, it states:

"The values of [subject](https://drafts.csswg.org/scroll-animations/#dom-viewtimeline-subject), [source](https://drafts.csswg.org/scroll-animations/#dom-scrolltimeline-source), and [currentTime](https://drafts.csswg.org/web-animations-1/#dom-animationtimeline-currenttime) are all computed when any of them is requested or updated."

but I am not entirely clear on what it would be mean to compute the subject.

I think the spec probably needs to update the IDL to make `subject` nullable (and ensure all uses of `subject` by the processing model account for it potentially being null).

(A similar conflict exists for the `startOffset`/`endOffset` attributes, and was filed separately as https://github.com/w3c/csswg-drafts/issues/13844). 

<!-- comment id=4531651144 author=BorisChiou created=2026-05-25T05:01:18Z url=https://github.com/w3c/csswg-drafts/issues/13959#issuecomment-4531651144 -->
## @BorisChiou — 2026-05-25

A similar topic related to this: https://github.com/w3c/csswg-drafts/issues/9584

> I think the spec probably needs to update the IDL to make subject nullable (and ensure all uses of subject by the processing model account for it potentially being null).

It seems WebKit and Chromium define `element? subject` in their idl files now. (And I'm planning to make the subject nullable in Gecko as well.)

<!-- comment id=5120698511 author=css-meeting-bot created=2026-07-29T16:32:44Z url=https://github.com/w3c/csswg-drafts/issues/13959#issuecomment-5120698511 resolution=true -->
## @css-meeting-bot — 2026-07-29

The CSS Working Group just discussed ``[scroll-animations] ViewTimeline IDL specifies that the `subject` attribute is non-null, but allows construction with no `subject` specified``, and agreed to the following:

* `RESOLVED: change the spec to make the constructor take a non-optional dictionary, and dictionary makes element required, and impls check compat of that`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> weinig: I think this is just an IDL bug<br>
&lt;TabAtkins> weinig: in ViewTimeline, there's a constructor that takes ViewTimelineOptions, and one of the options is the element. Because the whole constructor is optional, and element isn't required in the dict, you can call the constructor and the element is null<br>
&lt;TabAtkins> weinig: But in the interface itself the element isn't nullable. So that's a contradiction.<br>
&lt;ydaniv> q+<br>
&lt;TabAtkins> weinig: So I think the element just needs to be nullable, or we change the ViewTimeline constructor.<br>
&lt;TabAtkins> ydaniv: since it's also readonly, it's practically useless without a subject. I'm not sure what use-case it woudl serve to make it nullable<br>
&lt;TabAtkins> weinig: Yeah, that's an option. Just make the dictionary non-optional and make element a required piece<br>
&lt;TabAtkins> ydaniv: if that's compatible<br>
&lt;flackr> q+<br>
&lt;Rossen> ack ydaniv<br>
&lt;TabAtkins> weinig: well like you said, what would people be doing with a null ViewTimeline object?<br>
&lt;bramus> q+<br>
&lt;TabAtkins> ydaniv: probably getting an inactive VT object, but now they'd be throwing an error, so it might be a compat issue<br>
&lt;Rossen> ack flackr<br>
&lt;TabAtkins> flackr: I was going to ask what it does today, but I checked, and it's just returning a null subject<br>
&lt;TabAtkins> flackr: I'm fine with trying to throw an error<br>
&lt;Rossen> ack bramus<br>
&lt;TabAtkins> flackr: But I"m also fine with the element being null and the timeline being useless is okay if compat requires that<br>
&lt;TabAtkins> bramus: yeah just reiterating that the VT object is worthless without a subject, so it makes sense to require it<br>
&lt;TabAtkins> bramus: ignoring compat, we shoudl require it<br>
&lt;ydaniv> +1<br>
&lt;TabAtkins> flackr: So maybe we should try to require it, but check usage first?<br>
&lt;ydaniv> +1 to flackr<br>
&lt;TabAtkins> Rossen: Can we just resolve to require it, and then bring it back if compat says we can't do it?<br>
&lt;TabAtkins> bramus: One thought, maybe it was nullable in the case that we create a viewtimeline linked to an existing element, but later destroy that element, you still have a lingering VT<br>
&lt;TabAtkins> weinig: That's the opposite problem, currently the interface's element is non-nullable<br>
&lt;TabAtkins> weinig: But usually the way this works is the VT would be a strong reference to the element so it's not destroyed<br>
&lt;ydaniv> and the subject is readonly<br>
&lt;flackr> +1<br>
&lt;TabAtkins> (no dangling pointers)<br>
&lt;TabAtkins> weinig: so proposed resolution is we change the spec to make the constructor take a non-optional dictionary, and dictionary makes element required, and impls check compat of that<br>
&lt;TabAtkins> RESOLVED: change the spec to make the constructor take a non-optional dictionary, and dictionary makes element required, and impls check compat of that<br>
</details>
