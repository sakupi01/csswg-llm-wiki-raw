---
number: 14395
title: "[css-forms-1] Use `::field-content { flex: 1 }` instead of `::picker-icon { margin-inline-start: auto; }`"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-forms-1"]
author: "fantasai"
created_at: "2026-08-27T02:39:25Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14395"
comments: 3
has_resolution: true
synced_at: "2026-09-16T12:47:58Z"
---

Splitting this out from https://github.com/w3c/csswg-drafts/issues/14323 ...

Now that we have the ability to style the content outside the `::picker-icon` as its own addressable box, we should switch how we're managing spacing within the select button.

Currently we're using an `auto` margin. But this has two disadvantages:
- It means the margin can't be used for spacing around the `::picker-icon`, in case someone wants to style that with a background or border and can't use padding.
- It's not a pattern that works well when we have multiple button-y things inside the form control, which might happen with certain `<input>`s.

Proposal is to switch to `::field-content, selectedcontent { flex: 1; }` which flexes the option-text wrapper itself, rather than the space between it and the picker-icon.

<!-- comment id=5511812824 author=css-meeting-bot created=2026-09-02T15:13:12Z url=https://github.com/w3c/csswg-drafts/issues/14395#issuecomment-5511812824 resolution=true -->
## @css-meeting-bot — 2026-09-02

The CSS Working Group just discussed ``[css-forms-1] Use `::field-content { flex: 1 }` instead of `::picker-icon { margin-inline-start: auto; }` ``, and agreed to the following:

* `RESOLVED: Proposed resolution is to switch to ::field-content, selectedcontent { flex: 1; }`

<details><summary>The full IRC log of that discussion</summary>
&lt;ydaniv> fantasai: we have picker-icon, and field-content, and selected-content for the text<br>
&lt;ydaniv> ... we have the margin-inline-start: auto on the selected-icon<br>
&lt;ydaniv> ... so the text is on the left-hand side<br>
&lt;ydaniv> ... the problem is, it's better to set flex on it, so if it stretches it becomes to big, rather then an auto margin<br>
&lt;ydaniv> ... in the future we may want to have more things in the form controls, and more custom things in the button that could also go in there<br>
&lt;ydaniv> ... so it's not a pattern we can reuse for other things<br>
&lt;ydaniv> ... so it's a better pattern, going forward, to do flex: 1, so proposal is to switch all of the elements to flex: 1<br>
&lt;emilio> sgtm<br>
&lt;ydaniv> astearns: which is same effect for the current contents we have now?<br>
&lt;ydaniv> fantasai: yes<br>
&lt;lwarlow> +1<br>
&lt;astearns> Proposed resolution is to switch to ::field-content, selectedcontent { flex: 1; }<br>
&lt;masonf> +1<br>
&lt;ydaniv> astearns: objections?<br>
&lt;flackr> +1<br>
&lt;fantasai> (and remove ::picker-icon { margin-inline-start: auto; })<br>
&lt;ydaniv> RESOLVED: Proposed resolution is to switch to ::field-content, selectedcontent { flex: 1; }<br>
</details>

<!-- comment id=5525202206 author=lukewarlow created=2026-09-03T11:44:03Z url=https://github.com/w3c/csswg-drafts/issues/14395#issuecomment-5525202206 -->
## @lukewarlow — 2026-09-03

Should this be select::field-content or do we expect to apply it to all usages? I suspect it's fine to just apply to all of them?

<!-- comment id=5525728097 author=JoshTumath created=2026-09-03T12:26:22Z url=https://github.com/w3c/csswg-drafts/issues/14395#issuecomment-5525728097 -->
## @JoshTumath — 2026-09-03

Should we also set `flex: none` on the `::picker-icon` to prevent shrinking?
