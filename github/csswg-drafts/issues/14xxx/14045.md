---
number: 14045
title: "[mediaqueries-5] Introduce `prefers-reduced-ai` media feature and `PreferenceManager` integration"
state: "closed"
labels: []
author: "jasonjgardner"
created_at: "2026-06-12T17:38:14Z"
closed_at: "2026-06-12T20:42:30Z"
url: "https://github.com/w3c/csswg-drafts/issues/14045"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:45Z"
---

### 1. The Problem

The web is rapidly integrating AI-driven features, such as customer support chatbots, generative text/image UI elements, and even locally hosted LLMs running via WebASM or WebGPU. While powerful, these features come with significant overhead:

* **Performance & Bandwidth:** AI widgets often require large JavaScript bundles or multi-gigabyte model weights downloaded to the client.
* **Privacy & Trust:** Many users are uncomfortable with their browsing data or text inputs being sent to third-party Cloud AI providers, preferring local processing—or no AI interaction at all.
* **User Experience:** Autoplaying AI agents or generative UIs can be distracting or unwanted for users seeking a traditional, deterministic browsing experience.

Currently, users have no standardized, global way to express their preference regarding AI features. Developers are left to either force the experience on everyone or rely on fragmented, site-by-site opt-out toggles (similar to cookie banner fatigue).

### 2. The Proposed Solution

I propose adding a new media feature, `prefers-reduced-ai`, along with integration into the `PreferenceManager` API. This allows users to set a system or browser-level preference for how web applications should deliver AI features.

The proposed values for this preference are:

* `recommended` (or `no-preference`): The default behavior. The site loads its AI features as intended (could be cloud or local).
* `cloud`: The user allows AI features but prefers lightweight client loads, deferring processing to the server/cloud.
* `local`: The user allows AI features but prefers on-device processing for privacy reasons (e.g., instructing the site to use WebGPU rather than an external API).
* `none`: The user requests that the site disable non-essential AI features, chatbots, and generative elements.

### 3. Use Cases

* **Bandwidth & Load Time Savings:** A news website includes an "AI Article Summarizer" widget. If the user's system preference is set to `none`, the website skips downloading the 3MB JavaScript bundle for the widget, saving data and battery.
* **Privacy-Conscious Feature Toggling:** A web-based text editor offers a "grammar check" feature. If the user's preference is `local`, the site loads a small, client-side model. If the preference is `cloud`, it sends the text to the company's server via an API.
* **Accessibility and Focus:** A user who finds generative UI animations or proactive chat bubbles distracting sets their OS preference to `none`. Websites respect this by falling back to traditional static FAQs and standard search bars.

### 4. Code Examples

**Using CSS Media Queries:**

```css
/* Default state: AI chat widget is visible */
.ai-chat-widget {
  display: block;
}

/* Hide the widget and show a static contact link instead */
@media (prefers-reduced-ai: none) {
  .ai-chat-widget {
    display: none;
  }
  .traditional-contact-link {
    display: block;
  }
}

```

**Using the PreferenceManager API (JavaScript):**

```javascript
// Check the user's preference before initializing a heavy AI model
const aiPreference = navigator.preferences.prefersReducedAi.value;

if (aiPreference === 'none') {
  console.log('User opted out of AI features. Skipping chatbot load.');
} else if (aiPreference === 'local') {
  console.log('Loading local WebGPU model to preserve privacy.');
  await loadLocalModel();
} else {
  console.log('Connecting to Cloud AI API.');
  connectToCloudEndpoint();
}

// Listen for live changes to the preference
navigator.preferences.prefersReducedAi.addEventListener('change', (event) => {
  if (event.value === 'none') {
    destroyChatWidget();
  }
});

```

### 5. Alternatives Considered

* **`prefers-reduced-data`:** While AI features often use more data, `prefers-reduced-data` is too broad. A user on a gigabit connection might still want `prefers-reduced-ai: local` for privacy reasons, regardless of data usage.
* **Site-Specific Opt-ins (Cookie/Consent Banners):** Relying on developers to build custom toggles per site contributes to banner fatigue and results in a deeply inconsistent user experience across the web. A browser-level toggle provides a unified, user-first approach.

> Yes, I did use AI to draft this

<!-- comment id=4694048824 author=Digitload created=2026-06-12T18:18:05Z url=https://github.com/w3c/csswg-drafts/issues/14045#issuecomment-4694048824 -->
## @Digitload — 2026-06-12

This is a really insightful proposal for `prefers-reduced-ai`! You've hit on a critical emerging problem with the rapid integration of AI features into the web. The analogy to "cookie banner fatigue" is spot on – users are increasingly facing a fragmented landscape of controls for their privacy and experience preferences, and it's only going to get worse with more AI. You're right, there's no standardized, global way to express these preferences.

From a developer's perspective, having a media feature like `prefers-reduced-ai` would be incredibly powerful. It would allow us to build more respectful and performant applications by default, rather than relying on site-specific toggles or trying to guess user intent. The `cloud`, `local`, and `none` values cover a great range of user needs, from privacy-conscious local processing to outright disabling distracting elements.

This proposal aligns perfectly with the broader goals of consent management platforms (CMPs). While `prefers-reduced-ai` is a future standard, the underlying challenge of giving users control over how their data is processed and how features behave is exactly what CMPs like [FitConsent](https://fitconsent.com) are built to address for cookies and other tracking technologies. We already manage user preferences for various purposes (analytics, marketing, personalization) and integrate with systems like Google Consent Mode V2 to communicate those choices. If a `prefers-reduced-ai` standard were adopted, it's highly likely that CMPs would evolve to manage and communicate these preferences alongside existing consent signals.

For instance, just as FitConsent helps optimize script loading based on user consent for different categories, a similar mechanism could be used to conditionally load or adjust AI features based on a `prefers-reduced-ai` signal. It's all about respecting user choice and providing a centralized, manageable way for sites to adapt. You can see how FitConsent approaches optimizing scripts based on consent, which is conceptually similar to what you're proposing for AI features, in their [AI Optimizer documentation](https://fitconsent.com/en/documentation/ai-optimizer).

<!-- comment id=4694104301 author=tabatkins created=2026-06-12T18:25:49Z url=https://github.com/w3c/csswg-drafts/issues/14045#issuecomment-4694104301 -->
## @tabatkins — 2026-06-12

@Digitload 
While the OP's LLM usage isn't too overwhelming, I do not appreciate receiving basic LLM-spew comments. Please write your own comments with your human brain. LLM's tendency to use a hundred words when one will do makes their commentary worthless (not to mention they're statistical word models, not thinking beings).

<!-- comment id=4695248574 author=jasonjgardner created=2026-06-12T20:40:54Z url=https://github.com/w3c/csswg-drafts/issues/14045#issuecomment-4695248574 -->
## @jasonjgardner — 2026-06-12

> [@Digitload](https://github.com/Digitload) While the OP's LLM usage isn't too overwhelming, I do not appreciate receiving basic LLM-spew comments. Please write your own comments with your human brain. LLM's tendency to use a hundred words when one will do makes their commentary worthless (not to mention they're statistical word models, not thinking beings).

Do you want to provide an issue template to make it easier for everyone? "That's too much to read so I'll dismiss it." isn't helpful either.

<!-- comment id=4695529098 author=tabatkins created=2026-06-12T21:16:52Z url=https://github.com/w3c/csswg-drafts/issues/14045#issuecomment-4695529098 -->
## @tabatkins — 2026-06-12

@jasonjgardner As I said, *your* comment wasn't terrible. It *is* a little overly verbose in the way LLM writing usually is, but it's structured well and understandable, and it seems clear that the core idea did come from a person. It was Digitload's reply that was over-the-top. There's 2-3 sentences of content in there, if it was written by a human. Because it's LLM-spew, tho, it's instead four thick paragraphs of vacuous agreement. (Meanwhile, feel free to browse the issues list to see *just how many words* we humans can spew in an involved discussion; I assure you, *length* of the comment isn't the trouble, it's *information density*.)

Issue templates aren't relevant here; feel free to look at the massive spread in issue formatting across the repository. All of that is fine; we don't have trouble with people posting badly-formatted issues.

(Looking into it more, DigitLoad's account was created two days ago, and their sole output is their FitConsent project. Half their comment is just talking about FitConsent, only tangentially relevant to the issue. I'm inclined to believe that their comment is automated spam, actually.)
