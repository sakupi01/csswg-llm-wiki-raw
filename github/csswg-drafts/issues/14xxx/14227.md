---
number: 14227
title: "[css-properties-values] CSSOM for multi-name and shorthand `@property`"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-properties-values-api-1"]
author: "LeaVerou"
created_at: "2026-07-25T12:33:40Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14227"
comments: 6
has_resolution: false
synced_at: "2026-08-13T02:45:19Z"
---

In https://github.com/w3c/csswg-drafts/issues/7523#issuecomment-3683970305 we resolved to adopt a comma-separated syntax for `<custom-property-name>` to support registering multiple properties that share the same descriptors in one go.

However, we do not seem to have consensus on the best CSSOM to represent this.

Opening this so we can discuss without clobbering the shorthand syntax discussed in #7523, though the design should also take it into account.

## Discussion

### Requirements

- Existing single-name `@property` rules MUST produce the same CSSOM
- Any syntax that only fans out on `name` only (e.g. `CSSPropertyRule.prototype.names`) will have trouble once the shorthand `syntax` syntax is introduced (#7523)

### Nice to haves

- Roundtripping. Plenty of places where CSS doesn't roundtrip, so not a biggie if it doesn't, but it's _nice_ if it does.
- Authors should not need to parse property names and split by commas. This will get very awkward once #7523 is introduced, as they'll now need to parse a full blown microsyntax.
- `CSSPropertyRule.prototype.name` is read-only so we don't need to answer the question "how does the setter behave?". That said, it would be _nice_ if the design did not actively get in the way of making it writable later.

## Options

### Option 1: Expand to multiple `CSSPropertyRule` rules at parse time

No `CSSPropertyRule` changes. The rule just expands to multiple `CSSPropertyRule` rules at parse time.

Pros:
- **No CSSOM changes**
- Supports almost any future syntax
- Authors don't need to parse any microsyntax
- Backwards compatible with existing consumers

Cons:
- No roundtripping
- Making `name` writable later would be hard
- `insertRule()` can insert multiple rules, which is weird

### Option 2: `name`/`names`

This includes:
1. `name` includes the full comma-separated list, no other change
2. `name` includes the full comma-separated list, `names` as a `FrozenArray`
3. `name` is empty when multiple names, `names` as a `FrozenArray`

Pros:
- Serialization roundtrips

Cons: 
- All of these special case the name fanout, unclear how this expands to the `syntax` shorthand. In theory we could add plural versions of all descriptors as shorthands are added (`syntaxes` etc), but we need to keep multiple arrays in sync, which is awkward
- For 2.1: Authors need to do their own parsing, which will get even more complex with the shorthand expansions


### Option 3: Nested `CSSPropertyRule`

`CSSPropertyRule` gains a `cssRules` property which is a `CSSPropertyRule[]`. Its properties represent base values as specified in the actual rule, but child rules can override them.

Single-property usage is backwards compatible, consumers need to be updated to read multi-property rules correctly.

Pros:
- Most faithful representation, objects correspond exactly to what was written
- Roundtrips correctly
- Scales better to future syntax extensions like shorthand types, nesting etc
- Even supports a future writable `name` — it would just generate `cssRules`.

Cons:
- Biggest lift, incompatible with code consuming the existing schema
- Requires additional design decisions for every single descriptor on the root object
  - What would `name` be? Empty? A comma-separated list? The first name?
  - What would `syntax` be once we have the shorthand syntax? Empty? A union of all? The first one?
  - etc

------

I’m moderately strongly against 2, but I'm fine with 1 or 3.

I originally recommended 1 as I think it would be unfortunate to gate this feature on a big CSSOM change, but 3 is the "proper" way, and can scale to cover a lot of future expansion.

<!-- comment id=5079051689 author=emilio created=2026-07-25T15:26:46Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5079051689 -->
## @emilio — 2026-07-25

There are other options, like 2 but multiple names return empty for `name`. That's somewhat similar to what we did for `@container` when we added multiple conditions: https://github.com/w3c/csswg-drafts/pull/13635. We could do something like that? It's pretty simple. Not sure what the 'authors need to do their own parsing' concern is about?

Potential writable names and so on are also not a concern, IMO. These interfaces should be kept read-only unless there's extremely good reason to do otherwise, we resolved on making the OM as readonly as possible quite a while ago.

<!-- comment id=5083964496 author=LeaVerou created=2026-07-26T14:42:24Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5083964496 -->
## @LeaVerou — 2026-07-26

> There are other options, like 2 but multiple names return empty for `name`. 

I added this to the post. Is this an example or do you have more options in mind?

> That's somewhat similar to what we did for `@container` when we added multiple conditions: [#13635](https://github.com/w3c/csswg-drafts/pull/13635). We could do something like that? It's pretty simple. 

That works for `@container` because the prelude is a separate thing from the contents, similarly to a media query or even a regular CSS rule. Here once we add the shorthand syntax, the prelude is basically setting descriptors that normally live in the rule itself (which also brings up a question about serialization…).

> Not sure what the 'authors need to do their own parsing' concern is about?

It was specific to the option where `name` holds the whole thing. Clarified in the OP.

> Potential writable names and so on are also not a concern, IMO. These interfaces should be kept read-only unless there's extremely good reason to do otherwise, we resolved on making the OM as readonly as possible quite a while ago.

Yes, I don't see it happening anytime soon, but still good to keep options open. We've reversed many things we said we'd never do. It's only a nice-to-have, and I'd give it pretty low weight.

<!-- comment id=5084262598 author=Loirooriol created=2026-07-26T16:03:14Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5084262598 -->
## @Loirooriol — 2026-07-26

> unclear how this expands to the `syntax` shorthand

What do you mean with the syntax shorthand?

<!-- comment id=5084295388 author=LeaVerou created=2026-07-26T16:11:45Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5084295388 -->
## @LeaVerou — 2026-07-26

> > unclear how this expands to the `syntax` shorthand
> 
> What do you mean with the syntax shorthand?

The `@property --name <syntax>: initial-value, --name2 <syntax2>: initial-value2, ...` shorthand discussed in #7523 

<!-- comment id=5084528369 author=Loirooriol created=2026-07-26T17:10:47Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5084528369 -->
## @Loirooriol — 2026-07-26

Isn't it the other way round, that the shorthand statement syntax would expand to the normal rule syntax? So I'm not sure why it would be a concern here. I'm not getting the "plural versions of all descriptors" thing.

<!-- comment id=5205129415 author=css-meeting-bot created=2026-08-06T13:17:37Z url=https://github.com/w3c/csswg-drafts/issues/14227#issuecomment-5205129415 -->
## @css-meeting-bot — 2026-08-06

The CSS Working Group just discussed ``[css-properties-values] CSSOM for multi-name and shorthand `@property` ``, and agreed to the following:

* ``RESOLVED: if single @property has multiple names, it will be represented in CSSOM as a single object with a `names` array and not expand into multiple objects and return empty string for the `name` in that case``

<details><summary>The full IRC log of that discussion</summary>
&lt;keithamus> q?<br>
&lt;keithamus> q+<br>
&lt;lea> lea: We already have a resolution to support a, separated syntax for @Property, but it was never clarified what the CSSOM side of things would look like.<br>
&lt;TabAtkins> we resolved to support `@property --foo, --bar { syntax: "&lt;integer>"; initial: 1; }`<br>
&lt;lea> lea: Also, there are proposals for potentially wild carding at property. And while I don't think it would be a productive use of time to discuss whether we want to do that right now, it would be good to not shoot ourselves in the foot for things like that in the future. And whatever CSSOM shape we end up with also supports expansions like that.<br>
&lt;TabAtkins> we also want to support `@property --foo &lt;integer>: 1;` (aka borrowing the @function argument syntax)<br>
&lt;lea> lea: There are multiple proposals in the thread about what this might look like.The simplest one is probably to expand to multiple CSSPropertyRule objects at parse time but that has several issues.<br>
&lt;lea> lea: The more flexible one is probably a nesting model where you have children of a CSSPropertyRule, but that's the biggest delta and the most complex.<br>
&lt;astearns> q+<br>
&lt;oriol> q+<br>
&lt;lea> lea: And another one might be to add a names property and then name is either empty or a serialized version of all names. The problem is that this does not expand to the shorthand syntax.<br>
&lt;bramus> ChrisL: Do you want to have a breakout with yourself?<br>
&lt;lea> lea: It is a very important bit I forgot to mention in the beginning when I was introducing the issue. This is not about wildcarding. Forget wild carding. This is about the shorthand syntax we're thinking of introducing. Like not just the shorthand syntax where we add short versions of all the descriptors<br>
&lt;astearns> s/potentially wild carding/potentially shorthanding/<br>
&lt;lea> lea: For example `@property ---foo &lt;length>, --bar &lt;number>`<br>
&lt;lea> lea: and a `names` key would not actually help with that.<br>
&lt;TabAtkins> q+<br>
&lt;lea> lea: I mean you could have plural properties of every possible descriptor and then have parallel arrays and associate them that way but then it becomes kind of annoying.<br>
&lt;bramus> astearns: we  have most of the context, lets go to queueu<br>
&lt;bramus> keithamus: inhertiance is required in @prpoerty?<br>
&lt;astearns> ack keithamus<br>
&lt;bramus> lea: not anymore<br>
&lt;bramus> keithamus: point of duplication: could be 2 syntaxes: `@property dashed-ident dashed-ident dashed-ident { … } ` and coul dhave one with just prelude<br>
&lt;emilio> q+<br>
&lt;lea> `@property ---foo &lt;length>, --bar &lt;number>, --color &lt;transparent>: transparent`<br>
&lt;astearns> q-<br>
&lt;bramus> … and you could pick and choose based on similarly syntaxed properties or multiple props all iwht different syntaxes<br>
&lt;astearns> ack oriol<br>
&lt;bramus> oriol: think we should go with option 2<br>
&lt;bramus> … (order in the issue)<br>
&lt;bramus> … so adding `names`<br>
&lt;bramus> … list of value’s names<br>
&lt;bramus> … if you have more htan one, and access singular you get empy string<br>
&lt;bramus> … consisten with @container<br>
&lt;bramus> … arguments about new proposal shorthand, still have not discussed it nor resolutions<br>
&lt;keithamus> q?<br>
&lt;bramus> … tend to think its a bit ugly<br>
&lt;bramus> … but could be sth to consider once we add it<br>
&lt;bramus> … think we have resolutin for multiple propertiesin at-rule<br>
&lt;bramus> … and go for more straightforward soolution that is consistent with existing features<br>
&lt;keithamus> q+<br>
&lt;bramus> TabAtkins: did oriol’s comment about multiple syntaxes and efault values?<br>
&lt;astearns> ack TabAtkins<br>
&lt;bramus> oriol: we have not discussed that yet … so shoul dnot decide on sth we have not discussed yet<br>
&lt;bramus> TabAtkins: part of reason for discussing here<br>
&lt;bramus> … these 2 do line up together<br>
&lt;bramus> … my preferred one is to do what oriol is saying but also allow really shorthand bu t not let that be multiple<br>
&lt;bramus> … you can say `@property --foo, --bar, --baz {}`<br>
&lt;bramus> … and reflect that as `.names`<br>
&lt;bramus> … or could say `@property --foo &lt;integer>: 1` and would singular only<br>
&lt;bramus> … turn into normal singular @property rule<br>
&lt;bramus> … main reason for comma separte names is that rest of block is lots of stuff to repeat<br>
&lt;bramus> … only thing you save from comma separteing the inline form you are not having to type `@property`<br>
&lt;bramus> … again.<br>
&lt;bramus> … and the other thing is if you do allow the inline form to become comma=separted, you ahve to be careful about cmmas in the value (like function definitions, use {} iof necessary)<br>
&lt;bramus> … but dont want authors to be ?? of that<br>
&lt;kbabbitt> s/??/cognizant/<br>
&lt;astearns> s/??/cognizant/<br>
&lt;keithamus> q+<br>
&lt;lea> me I don't understand what TabAtkins is proposing<br>
&lt;bramus> … so my preferred is to make inline registariotn be singular, the multiple be just names, and we use option 2 for CSSOM (like oriol)<br>
&lt;bramus> +1<br>
&lt;emilio> q+<br>
&lt;emilio> +1*<br>
&lt;bramus> TabAtkins: (reexplains singular form)<br>
&lt;bramus> lea: (has concerns)<br>
&lt;bramus> astearns: is this separable?<br>
&lt;bramus> TabAtkins: if we are OK with what I said as a possibliyt, then it doesn taffect OM for multi name case. if its not, then it needs to be part of this<br>
&lt;bramus> emilio: disagree<br>
&lt;lea> q?<br>
&lt;astearns> ack emilio<br>
&lt;bramus> … dont disagree with tab’s proposal<br>
&lt;lea> q+<br>
&lt;bramus> … we also have precedent for separate interface for block vs statement version of a rule<br>
&lt;bramus> TabAtkins: thats different<br>
&lt;bramus> emilio: my point is that i dont think we paint ourselves into a corner<br>
&lt;astearns> qq+<br>
&lt;TabAtkins> (block layer puts rules in a layer; statement layer just declares layer names; they're semantically different)<br>
&lt;bramus> … would be a bit weird to serialize the inline form as the block form<br>
&lt;bramus> … its fine but point is that we ar enot that constrained as we think we are<br>
&lt;bramus> … and should resolve to what oriol said<br>
&lt;bramus> TabAtkins: you’re right<br>
&lt;TabAtkins> emilio's right, there are other approaches we can do<br>
&lt;bramus> emilio: and if we decide on the shorthand being not compat, we can add an @property statement rule in the OM and thats fine<br>
&lt;bramus> JohnJansen: so did you agree with tab?<br>
&lt;kbabbitt> +1 emilio<br>
&lt;bramus> emilio: with the thing for this issue yes, not to resolve on all details of shorthand<br>
&lt;bramus> +1 to emilio<br>
&lt;bramus> TabAtkins: fine with that<br>
&lt;astearns> ack astearns<br>
&lt;Zakim> astearns, you wanted to react to emilio<br>
&lt;astearns> ack keithamus<br>
&lt;bramus> keithamus: clarifiying question about inherits<br>
&lt;bramus> … what is default?<br>
&lt;bramus> TabAtkins: need to check the minutes<br>
&lt;bramus> … defaults to true (similar to untyped)<br>
&lt;bramus> keithamus: so syntax is also omissable<br>
&lt;bramus> TabAtkins: and defaults to *<br>
&lt;astearns> ack lea<br>
&lt;bramus> astearns: so we have arrive at conclusion that syntax is separable from this OM issue<br>
&lt;bramus> TabAtkins: yes<br>
&lt;astearns> s/arrive at/arrived at the/<br>
&lt;bramus> lea: think once you ahve multiple names and once you have shorthand, combining two is what authors will ask for, so dont think we should separate<br>
&lt;bramus> emilio: that is tangentional to this issue<br>
&lt;bramus> … can decided anything we want for that<br>
&lt;bramus> … dont need to resolve on not allowing that<br>
&lt;bramus> … what tab said with commas for example, that is tangentional<br>
&lt;bramus> astearns: which is why I wanted to separate it<br>
&lt;bramus> … to only focu son OM<br>
&lt;bramus> lea: but the OM needs to suppport both<br>
&lt;bramus> emilio: can introduce new thing for @proeprty statement if we really need to<br>
&lt;bramus> TabAtkins: can reflect them if needed<br>
&lt;bramus> lea: but its a different syntax<br>
&lt;bramus> … would be fine with having different syntax for plural<br>
&lt;bramus> TabAtkins: name of interface would not trip up folks<br>
&lt;bramus> astearns: think i have heard concensus for option 2 inthe issue<br>
&lt;bramus> … ccssom with .names array<br>
&lt;bramus> emilio: if we wanted to keep CSSPropertyRule we could and stash more stuff in OM but dont see how that is a problem<br>
&lt;bramus> … like names, initialvalues, etc<br>
&lt;bramus> … in a way that does not risk compat issues<br>
&lt;bramus> … like name returning emptry string<br>
&lt;lea> https://www.irccloud.com/pastebin/QE75JJNy/<br>
&lt;lea> lea: a lot of value in combining, e.g. inside the rule these are the defaults, this can be very powerful<br>
&lt;bramus> emilio: I thkn you are talking about some sort o fthird syntax; both a block and stuff in prelude, which is another thing<br>
&lt;bramus> lea: that was plan all along?<br>
&lt;bramus> emilio: I thought about a statement<br>
&lt;bramus> … dont think we are blocked on that for this<br>
&lt;lea> `@property --color-accent: red, --color-canvas: white { syntax: "&lt;color>"; }`<br>
&lt;TabAtkins> The "inline" version is just `@property --foo &lt;integer> 1;`, a statement with no block<br>
&lt;bramus> astearns: so can we resolve on option 2 in issue?<br>
&lt;TabAtkins> literally "exactly the syntax of a function argument"<br>
&lt;bramus> emilio: with name returning emptry string when there are multi-names<br>
&lt;bramus> lea: I think option 2 is terrible, but dont feel strongly enough to object<br>
&lt;bramus> … in the grand scheme of things<br>
&lt;bramus> astearns: we have to pick something<br>
&lt;bramus> … should we take back to issue?<br>
&lt;emilio> `@property --foo, --bar {}` returns `names = ["--foo", "--bar"], name = ""`<br>
&lt;bramus> lea:  if it is just me, then no/.<br>
&lt;bramus> astearns: if single @property has multiple names, it will be represented in CSSOM as a single object with a `names` array and not expand into multiple objects and return empty string for the `name` in that case<br>
&lt;bramus> lea: so shorthand  form is not part of resolution<br>
&lt;bramus> TabAtkins: correct<br>
&lt;bramus> keithamus: would names be populated if it has a single name?<br>
&lt;bramus> emilio: yes, 1 length array<br>
&lt;bramus> astearns: objections?<br>
&lt;bramus> PROPOSED: if single @property has multiple names, it will be represented in CSSOM as a single object with a `names` array and not expand into multiple objects and return empty string for the `name` in that case<br>
&lt;tuankiet65> +1<br>
&lt;TabAtkins> +1<br>
&lt;keithamus> +1<br>
&lt;kbabbitt> +1<br>
&lt;florian> +1<br>
&lt;fantasai> +1 but clarify that names exists even if only one name<br>
&lt;bramus> RESOLVED: if single @property has multiple names, it will be represented in CSSOM as a single object with a `names` array and not expand into multiple objects and return empty string for the `name` in that case<br>
</details>
