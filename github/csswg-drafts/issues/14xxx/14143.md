---
number: 14143
title: "[css-ui] Maybe reconsider the :hover / :active top layer propagation rules?"
state: "open"
labels: ["Agenda+", "css-ui-4"]
author: "emilio"
created_at: "2026-07-08T11:31:10Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14143"
comments: 7
has_resolution: false
synced_at: "2026-07-09T01:00:14Z"
---

So... in #11185 we resolved that `:hover` / `:active` broke at top layer boundaries. We decided that that wouldn't affect how `mouseenter` / `mouseleave` etc worked, which is ok I guess, but a bit weird.

I don't _hate_ that behavior, but I'm bringing this up again because that discussion didn't mention a couple things:

 * That non-custom select doesn't behave like that at all. It seems to keep `:hover` while open and hovering the picker, at least in Chrome and Firefox. In Chrome it seems to keep it even when the mouse is elsewhere.

 * That we could've maybe achieved a reasonable result with `select:not(:open):hover` in the UA stylesheet? Ok, it's not _exactly_ the same, but... given the action of clicking on the select is the same as the action of clicking anywhere else on the page, it seems ok?

Maybe the ship has sailed, but maybe we could reconsider this? Am I missing something obvious?

Note that this only affects the "there's no custom `<button>` case". I think making them not consistent with `mouseenter` / `mouseleave` etc is rather unfortunate.

Anyways, I'd be ok keeping the behavior, but it seems we maybe overcomplicated the model a bit for nothing?

cc: @smaug---- @keithamus @nt1m @mfreed7 @josepharhar @jakearchibald @tabatkins 

<!-- comment id=4914368030 author=emilio created=2026-07-08T11:36:24Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4914368030 -->
## @emilio — 2026-07-08

I don't think Firefox and Safari are shipping this yet (but correct me if I'm wrong).

<!-- comment id=4914401939 author=jakearchibald created=2026-07-08T11:40:35Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4914401939 -->
## @jakearchibald — 2026-07-08

> Note that this only affects the "there's no custom `<button>` case".

I'm not sure this is true. Chrome gives the button styles that prevent hover effects within it, and it doesn't seem like you can override those styles.

<!-- comment id=4914421419 author=jakearchibald created=2026-07-08T11:43:16Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4914421419 -->
## @jakearchibald — 2026-07-08

I kinda wish the custom select button (as in, the button regardless of whether there's a `<button>`) was a pseudo-element that was sibling to the picker, which avoids this issue entirely. Although maybe that causes other problems.

<!-- comment id=4914545040 author=emilio created=2026-07-08T11:59:52Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4914545040 -->
## @emilio — 2026-07-08

> Chrome gives the button styles that prevent hover effects within it, and it doesn't seem like you can override those styles.

That seems... a choice? But that seems tangential to the propagation, right? When the mouse is over that button the hover chain doesn't include any top layer element, nor is there any top layer descendants...

<!-- comment id=4914908926 author=jakearchibald created=2026-07-08T12:45:35Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4914908926 -->
## @jakearchibald — 2026-07-08

Yes but it's useless, as it can't be used to apply any styles on hover. So, even with a custom `<button>`, you can only add hover styles to the 'button' via `select:hover`, which gets you back to the same problem.

<!-- comment id=4916743511 author=josepharhar created=2026-07-08T16:02:39Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4916743511 -->
## @josepharhar — 2026-07-08

> That non-custom select doesn't behave like that at all. It seems to keep :hover while open and hovering the picker, at least in Chrome and Firefox. In Chrome it seems to keep it even when the mouse is elsewhere.

I don't have a preference on whether the appearance:auto behavior is changed or not, but actually determining whether the pointer is inside the native picker sounds hard, so hopefully we can avoid that with whatever we do for appearance:auto.

> That we could've maybe achieved a reasonable result with select:not(:open):hover in the UA stylesheet? Ok, it's not exactly the same, but... given the action of clicking on the select is the same as the action of clicking anywhere else on the page, it seems ok?

I don't think it would hurt to add `:not(:open)` to the hover and active styles, but if authors put their own hover or active styles on the select then they would have to also add `:not(:open)`, which they probably wouldn't just know they need to do - the current magic helps authors out.

> but it seems we maybe overcomplicated the model a bit for nothing?

I think that the benefits of the complicated model are:
1. Authors don't have to add `:not(:open)` and are less likely to accidentally create broken experiences.
2. Other cases of nesting top layer elements inside invokers are also fixed, which also might not have an `:open` selector to use when styling. I admit that there aren't other good examples of where this happens yet for other builtin elements, but authors could already be nesting popovers inside invokers.

The implementation complexity doesn't look like that much tome: https://chromium.googlesource.com/chromium/src/+/9a341b91cb6d194a326fb947554171d7befd36d9

<!-- comment id=4916853582 author=emilio created=2026-07-08T16:15:23Z url=https://github.com/w3c/csswg-drafts/issues/14143#issuecomment-4916853582 -->
## @emilio — 2026-07-08

Yeah, I meant complexity in the author sense, not necessarily implementation wise. As an author it's not super easy to distinguish when out of flow elements block hover vs not. Authors are used to hover menus where the abspos descendant makes the menu item hovered as well, after all, right?
