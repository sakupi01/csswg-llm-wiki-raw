---
number: 14035
title: "[Req] backdrop occlusion."
state: "open"
labels: []
author: "NicTanghe"
created_at: "2026-06-11T15:04:27Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14035"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:45:45Z"
---

Hello when trying to match my designers mockup I ran into the issue that I needed to occlude a gradient rendered behind my div.

I implemented it in my frontend library that is sort of a CSS renderer.

I think it would be a great addition to the main spec.

I'll add some images and the spec sheets i used.

mockup
<img width="1198" height="1313" alt="Image" src="https://github.com/user-attachments/assets/327bef2a-5f7f-44a7-b851-9bf48a4bfab2" />

Current personal implementation

<img width="836" height="486" alt="Image" src="https://github.com/user-attachments/assets/58297771-72cc-45a4-8a0d-a4c6e6b87674" />



## Specsheets

### T3. CSS `backdrop-occlude` paint barrier

Purpose:
- Add a controlled scene paint barrier that lets an element hide all previously painted CSS/UI renderer content behind its own visual box, without cutting, masking, or modifying the app-level acrylic/window backdrop



Support:
- `backdrop-occlude: scene`
- `backdrop-occlude: false`
- Default value: `false`
- Occlusion shape derived from the element's used border box and used border radius
- The occlusion operation happens before painting the element's own shadow, background, border, and children
- The `scene` operation clears all previously painted CSS/UI scene content in the current renderer layer
- The operation does not clear or modify the platform/app-level acrylic backdrop behind the renderer
- Deterministic behavior for nested occluding elements based on normal painter order

Paint order:
```text
platform/app acrylic background
↓
previous CSS/UI scene content
↓
backdrop-occlude: scene clear inside this element's rounded border shape
↓
element shadow
↓
element background
↓
element border
↓
element children
```

Non-goals:
- Do not behave like `backdrop-filter`
- Do not blur, sample, brighten, darken, or otherwise filter pixels
- Do not cut holes in the app/window acrylic layer
- Do not affect layout, hit testing, event routing, or scroll bounds
- Do not clear content outside the element's used border shape
- Do not clear the element's own shadow, background, border, or descendants
- Do not target one specific element, selector, layer, or z-level; that is T4

Acceptance:
- A card with `backdrop-occlude: scene` can hide page-level gradients, glows, and decorative CSS effects that were painted earlier behind the card
- The app-level acrylic backdrop remains visible through transparent or translucent parts of the card background
- Rounded corners do not show stale gradient or glow pixels behind the card
- Nested occluding elements produce deterministic output using normal painter order
- CPU renderer behavior is treated as the reference result
- GPU renderer either matches the CPU behavior or reports the feature as unsupported through the backend capability/fallback policy

Implementation notes:
- Treat `backdrop-occlude` as a compositing operation, not as a filter.
- In the CPU renderer, this can be implemented by clearing the current CSS/UI scene buffer inside the element's rounded border mask before drawing that element's own visual primitives.
- In a GPU renderer, this can be implemented with destination-out alpha, a stencil mask, a scene-layer clear pass, or an equivalent compositing strategy.
- The extracted scene should preserve an explicit `BackdropOcclude` or `PaintBarrier` primitive so render backends do not need to infer the operation from style during painting.

---

### T4. Element-targeted `backdrop-occlude`
Status: planned  

Purpose:
- Extend `backdrop-occlude` so an element can hide the previously painted output of one named element or subtree behind its own visual box, while leaving other earlier CSS/UI scene content and the app-level acrylic/window backdrop intact

Support:
- `backdrop-occlude: element(#element-id)`
- Target lookup is limited to one element id in the same extracted scene
- The target includes the named element's own paint and descendant paint that was produced before the occluding element in normal painter order
- Occlusion shape is still derived from the occluding element's used border box and used border radius
- The operation happens before painting the occluding element's own shadow, background, border, and children
- If the target element is missing or has not painted before the occluder, the operation is a deterministic no-op
- Extracted paint items preserve enough element path/id metadata for render backends to determine the targeted contribution without selector matching during paint

Non-goals:
- Do not support arbitrary CSS selectors, class selectors, combinators, pseudo-classes, or multiple targets in the first targeted version
- Do not clear future paint from the target element if the target paints after the occluding element
- Do not clear or modify the platform/app-level acrylic backdrop
- Do not affect layout, hit testing, event routing, or scroll bounds for either element

Acceptance:
- A card with `backdrop-occlude: element(#accent-glow)` can hide that earlier accent/glow element behind the card while leaving unrelated earlier scene content visible
- Targeted occlusion respects the occluding element's rounded border shape
- Missing or later-painted targets produce stable output and do not crash
- Nested targeted occluders remain deterministic under normal painter order
- CPU renderer behavior is treated as the reference result
- GPU renderer either matches the CPU behavior or reports the feature as unsupported through the backend capability/fallback policy

Implementation notes:
- Prefer explicit extracted-scene paint identity over repaint-time selector evaluation.
- A straightforward CPU implementation can collect or replay the targeted element's earlier paint contribution into a mask/source buffer, then clear only those pixels inside the occluder's rounded shape.
- If the first implementation cannot isolate the target cheaply, it should report unsupported rather than silently falling back to `scene`.

---

<!-- comment id=4694684162 author=tabatkins created=2026-06-12T19:43:19Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4694684162 -->
## @tabatkins — 2026-06-12

Sorry, you've supplied a *lot* of detail in this issue, most of which appears to be irrelevant to the actual request.

Can you elaborate on what you're actually asking for? What part of those example images are illustrating your request?

<!-- comment id=4698663144 author=NicTanghe created=2026-06-13T13:31:16Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4698663144 -->
## @NicTanghe — 2026-06-13

I'll add another image later but it is this part.

<img width="38" height="132" alt="Image" src="https://github.com/user-attachments/assets/3e48fbd1-5ef7-4bae-a1e6-2122811935aa" />

it cuts evrything behind the button revealing only the app background wich is in this case blurred/ windows acrylic.
(its cutting the div with the gradient on it)
So its basically an alpha mask for evrything behind a div.

In this case cutting out the shape of the button.

<!-- comment id=4700279039 author=Maya-Fey created=2026-06-14T00:57:37Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4700279039 -->
## @Maya-Fey — 2026-06-14

I think I understand. 

I wonder if this need could be served by using the proposed [element()](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/element) selector alongside `mask` and `background-clip` (extended to accept an image perhaps). Defining the property on the *child* and having the child essentially "reach back" end erase content from its parents means you're requiring implementors to do GPU readback and replay paint/composition with arbitrary selections removed, and this could become quite expensive.

If you could just clip the parents directly based off of the geometry of the children, I think you achieve your goals without needing any more render passes.

<!-- comment id=4705755110 author=NicTanghe created=2026-06-15T07:56:14Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4705755110 -->
## @NicTanghe — 2026-06-15

Yes that whould be great !

<!-- comment id=4710945281 author=tabatkins created=2026-06-15T18:11:54Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4710945281 -->
## @tabatkins — 2026-06-15

I apologize, but I'm still not certain I understand. Let me try and describe what I think you might be getting at, and you can correct me.

In that image you most recently posted, you have a gradient background passing thru shades of blue, which is being used as a border. You also have a "frosted"/"blurred" backdrop-filter showing what's underneath the element. Right?

This feels like something that's already solved by backdrop-filter (to get the blurred background) and any of several possible border-* properties (to get the gradient border). This could *maybe* be done well with `border-image`, depending on exactly what effect you're looking for. It can *almost* be done with the new `border-shape`, except you can't supply a fill image there, just colors. We should probably fix that.

Is this correct? Or are there aspects of your request that I'm missing?

<!-- comment id=4713650078 author=Maya-Fey created=2026-06-16T00:21:03Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4713650078 -->
## @Maya-Fey — 2026-06-16

Of course, tab is right - getting that specific "double rounded border" in OP's example could be done by a combination of `border-*` properties. Worst comes to worst, you could also brute-force a `clip-path` on an positioned sibling.

Something more advanced would only come into play if you wanted a 'swiss cheese' kind of configuration with multiple elements appearing to poke through an intermediate surface. But I can't recall seeing that kind of layout in practice.

<!-- comment id=4721418247 author=NicTanghe created=2026-06-16T17:19:25Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4721418247 -->
## @NicTanghe — 2026-06-16

> In that image you most recently posted, you have a gradient background passing thru shades of blue, which is being used as a border. You also have a "frosted"/"blurred" backdrop-filter showing what's underneath the element. Right?

Hello this has nothing to do with the blur except that that makes it more important for it to work the way it does else you could just make the div fully white if the bg is white  to make it pretend see trough but you cant do that when you have a blurred background on the application level.

I dont cee how i can make a gradient stick out on the left using only the border property.

Path clip i think might work but it would be extremely clunky to set up instead of just saying.
clip everything behind this div except shadows until you hit the bg or until you hit ?

<!-- comment id=4722996801 author=tabatkins created=2026-06-16T20:07:25Z url=https://github.com/w3c/csswg-drafts/issues/14035#issuecomment-4722996801 -->
## @tabatkins — 2026-06-16

Okay, so the "backdrop" bit is a distraction, that's good to know. Your request was focused *solely* on the gradient effect on the side, then?

So, just checking, the part you care about is the black border in [this image](https://software.hixie.ch/utilities/js/live-dom-viewer/?%3C!DOCTYPE%20html%3E%0A%3Cdiv%3Efoo%3C%2Fdiv%3E%0A%3Cstyle%3E%0Adiv%20%7B%0A%20border%3A%200px%20solid%3B%0A%20border-radius%3A%201em%3B%0A%20border-left-width%3A%205px%3B%0A%20background%3A%20%23eee%3B%0A%20padding%3A%20.5em%3B%0A%20height%3A%205em%3B%0A%20width%3A%2030em%3B%0A%3C%2Fstyle%3E):

<img width="689" height="158" alt="Image" src="https://github.com/user-attachments/assets/596cf082-295f-4c65-9718-08551bb17df6" />

You are just looking for a way to make that use an image (in particular, a gradient) rather than being solid black, right?
