---
number: 14004
title: "[css-variables][css-mixins] In-place hygienic rewriting?"
state: "open"
labels: ["Agenda+ F2F", "css-mixins-1", "css-mixins-2", "css-variables-2"]
author: "LeaVerou"
created_at: "2026-06-03T15:47:28Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14004"
comments: 18
has_resolution: false
synced_at: "2026-07-28T13:21:55Z"
---

Hygienically rewritten variables are useful *on their own*, even without mixins. Even when just writing styles for a certain rule, it is incredibly common to need intermediate variables that you don't want to expose to the outside world.

Today, [this is done by convention](https://www.google.com/search?q=pseudo-private+CSS+variables): a leading underscore in the variable name indicates that this is meant to be private.

For example, a real code snippet to apply a parameterizable transparency checkerboard as a background to a color-slider component:

```css
.color-slider,
:host(color-slider) {
	--_transparency-cell-size: var(--transparency-cell-size, 0.5em);
	--_transparency-background: var(--transparency-background, transparent);
	--_transparency-darkness: var(--transparency-darkness, 5%);
	--_transparency-bg-size: calc(2 * var(--_transparency-cell-size));
	--_transparency-pattern: var(
		--transparency-pattern,
		repeating-conic-gradient(
				transparent 0 25%,
				rgb(0 0 0 / var(--_transparency-darkness)) 0 50%
			)
			0 0 / var(--_transparency-bg-size) var(--_transparency-bg-size)
			content-box border-box var(--_transparency-background)
	);

	background: var(--_transparency-pattern);
}
```

Besides holding intermediate calculations, pseudo-private variables are also used to initialize a variable while still allowing it to inherit and be trivially overridden.

Today, making these *actually* private would require a dummy, single-callsite mixin:

```css
@mixin --transparency-pattern() {
	--_transparency-cell-size: var(--transparency-cell-size, 0.5em);
	--_transparency-background: var(--transparency-background, transparent);
	--_transparency-darkness: var(--transparency-darkness, 5%);
	--_transparency-bg-size: calc(2 * var(--_transparency-cell-size));
	--_transparency-pattern: var(
		--transparency-pattern,
		repeating-conic-gradient(
				transparent 0 25%,
				rgb(0 0 0 / var(--_transparency-darkness)) 0 50%
			)
			0 0 / var(--_transparency-bg-size) var(--_transparency-bg-size)
			content-box border-box var(--_transparency-background)
	);

	background: var(--_transparency-pattern);
};

.color-slider,
:host(color-slider) {
	@apply --transparency-pattern;
}
```

This seems cheap (I just copied the existing code!) but in practice, CSS rules can be quite nested, whereas mixins need to be hoisted out, which creates an indirection that actively hinders understanding.

Imagine if I could actually apply the mixin in place, like a JS self-executing anonymous function: 

```css
.color-slider,
:host(color-slider) {
	@apply () {
		--_transparency-cell-size: var(--transparency-cell-size, 0.5em);
		--_transparency-background: var(--transparency-background, transparent);
		--_transparency-darkness: var(--transparency-darkness, 5%);
		--_transparency-bg-size: calc(2 * var(--_transparency-cell-size));
		--_transparency-pattern: var(
			--transparency-pattern,
			repeating-conic-gradient(
					transparent 0 25%,
					rgb(0 0 0 / var(--_transparency-darkness)) 0 50%
				)
				0 0 / var(--_transparency-bg-size) var(--_transparency-bg-size)
				content-box border-box var(--_transparency-background)
		);
	
		background: var(--_transparency-pattern);
	}
}
```

The UA would take care of creating a one-off mixin with a guaranteed unique name and applying it there on the fly. TBD how the CSS OM would represent this.

Alternatively, we could have an actual syntactic distinction (like JS does), which is more elegant:

```css
.color-slider,
:host(color-slider) {
	--#transparency-cell-size: var(--transparency-cell-size, 0.5em);
	--#transparency-background: var(--transparency-background, transparent);
	--#transparency-darkness: var(--transparency-darkness, 5%);
	--#transparency-bg-size: calc(2 * var(--_transparency-cell-size));
	--#transparency-pattern: var(
		--transparency-pattern,
		repeating-conic-gradient(
				transparent 0 25%,
				rgb(0 0 0 / var(--#transparency-darkness)) 0 50%
			)
			0 0 / var(--#transparency-bg-size) var(--#transparency-bg-size)
			content-box border-box var(--#transparency-background)
	);

	background: var(--#transparency-pattern);
}
```

`--#*` props are visible within their lexical `{ ... }` and inherit by default (or should they not?) but not visible from the outside (which can be implemented as hygienic rewriting). Note that visible does not mean they have a value, e.g. 

```css
.foo {
	--#foo: green;

	& + p {
		/* This would be red */
		background: var(--#foo, red);
	}
}
```

A syntactic switch is definitely a bigger lift, and probably not worth it just for this, but it may become more appealing if we could then adopt this new syntax in mixins as well. It could then help us do away with a lot of problems around hygienic rewriting and distinguishing when `--foo` means `--foo` vs `--rewritten-foo-43gh2gwlge`. It also has a nice consistency with JS.

<!-- comment id=4668119423 author=kizu created=2026-06-10T08:25:03Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-4668119423 -->
## @kizu — 2026-06-10

I wonder if something like https://github.com/w3c/csswg-drafts/issues/11798 could help here. In https://github.com/w3c/csswg-drafts/issues/13524#issuecomment-4173368648 one of the potential names for that was `@locals` and that we could reuse it for this type of lexical scoping.

I could see it working as 

```CSS
.foo {
	@locals {
		--foo: green;

		/* This would be green */
		background: var(--foo, red);
	}

	/* This would be red */
	border: 2px solid var(--foo, red);
}
```

But that will need more thought for it to be a proper proposal: how to differentiate between locals and globals, nested locals, naming them, accessing them from other scopes, etc. (all of which could go into the 11798 eventually).

I think sugar like `--#` could also work (and could be more convenient for simpler cases), just wanted to link the bigger scoping issue here :)

<!-- comment id=4929271688 author=tabatkins created=2026-07-09T20:32:06Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-4929271688 -->
## @tabatkins — 2026-07-09

Yeah, I recognize the value of doing something like this. I've thought about doing it inline before, to simplify potentially-complex math expressions that use a lot of vars, but doing it via something like a mixin body does look better.

I don't really want a syntactic distinction in the *name* of the variable; that requires rewriting and means you have to be aware of whether a var is defined locally or coming in from the outside. I would prefer something more like what @kizu is talking about, but we *also* need a way to still set *actual* vars, not solely the local ones.

So I think my preference might be something like a `@locals` block that can *only* contain custom properties (and conditional rules), defining those as locals. These locals are then usable in the *surrounding* block, via the rewriting rules.

So given a slightly elaborated version of kizu's example:

```css
.parent
	.foo {
		@locals {
			--foo: green;

			/* This is invalid/ignored, only custom properties in @locals */
			background: var(--foo, red);
		}

		/* This would be green, it sees the @locals --foo */
		border: 2px solid var(--foo, red);
	}
	/* This is red due to fallback; the @locals --foo isn't visible and there's no "normal" --foo either.
	border: 2px solid var(--foo, red);
}
```

And yes, because it'll make Lea excited, I'd probably be okay with reverting our previous resolutions around #13138 for this. Our previous idea of requiring an `@local` prefix on *every* local variable was annoyingly verbose, but using a single wrapping rule makes it not as bad once you have 3 or more, and we'd be leaning on *general* CSS tech instead of having a mixin-specific syntax.

This resolves the drawbacks that Anders lists in that first #13138 comment; you *can* use rule-specific locals rather than only mixin-global ones, and this makes no-local mixins minimally verbose, since we wouldn't be using @result anymore. It also negates the issue we were having with mixins and macros having slightly different syntax, which was proving to be an annoyance now that we have resolutions that remove their scoping distinction. The arglist of a mixin would just define an "implicit" locals block that applies to the body of the mixin.

---------

Some remaining issues:

* This does run into the same problem that I was pushing back against in mixins, namely that the rewriting occurs syntactically based on *rule nesting*, but the value inheritance occurs dynamically based on *dom structure*, and those two don't necessarily coincide. So, just like in non-scoped mixins, you can *use* a var() reference that doesn't actually resolve to anything because its nested rule is actually applying to a sibling/ancestor/etc of the @locals-defining element. Presumably the WG is okay with this in a "well don't do that then" fashion.

* This brings the question of whether/how these scoped properties are exposed in the style model more to the front. We could somewhat paper over them when it was just mixins doing it, but if *any* rule can have locals, this becomes more troublesome. This is a bit subtle, because there's only a limited window when they *do* need to know their real name but haven't yet been substituted away - reading a style rule's `cssText` won't do the rewrite yet, and `getComputedStyle()` is post-substitution so you won't see the name there either. Inline styles (`el.style.foo`, `<el style="">`) presumably will continue to not support nested rules for now so this isn't an issue, but we should keep that in mind. I think only TypedOM is capable of exposing the specified style on an element, which would have to reflect the rewritten name; it can instead reflect it as an opaque reference that doesn't expose a name, so we don't have to worry about it being reused somewhere else where it doesn't make sense.

<!-- comment id=4930082270 author=kbabbitt created=2026-07-09T22:23:02Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-4930082270 -->
## @kbabbitt — 2026-07-09

I like the direction. Not only does it pave the way towards not needing `@result` anymore, it also makes it cleaner to ship parameter-less mixins first and layer in parameters+locals later.

<!-- comment id=4986317684 author=tabatkins created=2026-07-15T23:38:31Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-4986317684 -->
## @tabatkins — 2026-07-15

@kbabbitt If you play with this in implementation, I'm curious about your opinion on the final bullet point in my remaining issues. Should we expose the rewritten name in the few odd cases that make it visible, either with a not-usable-in-a-normal-stylesheet syntax a la Lea's suggestion in the OP, or with an unpredictable-but-technically-usable name like `--ua-[random-hash-goes-here]`? Or just come up with some method to hide it in all cases?

<!-- comment id=5003869201 author=LeaVerou created=2026-07-17T13:40:11Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5003869201 -->
## @LeaVerou — 2026-07-17

I think we should hide it, otherwise that defeats the purpose of having locals (also I wonder if `@private` would be a clearer name, but that's orthogonal).

It’s unfortunate that there are certain cases where authors do need to set all inherited properties of a certain type, e.g. see #13836. But even exposing it wouldn't help there…

<!-- comment id=5005611877 author=tabatkins created=2026-07-17T17:05:47Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5005611877 -->
## @tabatkins — 2026-07-17

> otherwise that defeats the purpose of having locals 

I don't see how that's true; the point of locals is to avoid accidental collisions. If the names are sufficiently unguessable, you can't even purposely collide unless you go out of your way with JS to observe the rewritten name and use it. And if we make the name unusable in author-land you can't even do that.

That said, if the names are observable in *any* way, it encourages us to specify precisely how and when the names are generated (and regenerated), which is annoying. We could probably get away with not caring if we make the names unusable, but still.

My point, really, is just that I think I don't care too much how we handle this, and am happy to defer to what's most reasonable in implementations.

<!-- comment id=5005846211 author=LeaVerou created=2026-07-17T17:36:30Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5005846211 -->
## @LeaVerou — 2026-07-17

There is code that goes over every property that is set in an element's computed style, e.g. devtools, which would make this a leaky abstraction.

<!-- comment id=5005879146 author=tabatkins created=2026-07-17T17:40:54Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5005879146 -->
## @tabatkins — 2026-07-17

Oh, I agree that we should hide *the properties themselves*. I'm talking about the rewritten references, turning `var(--foo)` into `var(--ua-1234cafe)`.

Because of the timing of rewriting (not visible in the stylesheet itself, and already gone by computed value), the altered references would only *barely* be visible in TypedOM.

<!-- comment id=5006401454 author=kbabbitt created=2026-07-17T18:36:46Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5006401454 -->
## @kbabbitt — 2026-07-17

I don't see how the rewritten names would be visible via TypedOM. Why would what's reflected in `StylePropertyMap` be different from what's reflected in `cssText`?

<!-- comment id=5011377363 author=LeaVerou created=2026-07-18T13:10:30Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5011377363 -->
## @LeaVerou — 2026-07-18

> Oh, I agree that we should hide _the properties themselves_. I'm talking about the rewritten references, turning `var(--foo)` into `var(--ua-1234cafe)`.
> 
> Because of the timing of rewriting (not visible in the stylesheet itself, and already gone by computed value), the altered references would only _barely_ be visible in TypedOM.

Oh I see. Yes, I think that's fine.

<!-- comment id=5017287462 author=tabatkins created=2026-07-19T20:36:17Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5017287462 -->
## @tabatkins — 2026-07-19

> Why would what's reflected in StylePropertyMap be different from what's reflected in cssText?

Oh wait, I'm misremembering, lol. StylePropertyMap on an *element* only reflects computed values, same as gCS() (it just does computed for *all* values, rather than a mix of computed and used values like gCS() does). I was remembering the other variant, which is getting a typedom version of a style rule.

So in both cases, they're *essentially* equivalent to what you'd get from the string APIs, and notably, are either pre-rewrite, or post-replacement, so either way the rewritten names aren't exposed.

So never mind! All we need to do is make sure the rewritten *declarations* are hidden from user-facing APIs, which is probably fine. Can just use a flag or name pattern or something and not expose those.

<!-- comment id=5022611567 author=keithamus created=2026-07-20T13:11:00Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5022611567 -->
## @keithamus — 2026-07-20

At the risk of scope-creep, it would be nice for developers to be able to wholesale declare properties are safe to elide for tools like minifiers; if we could also guarantee that, for e.g. they aren't exposed in OM. `@locals` certainly looks like something a minifier could reasonably shrink down (so the author retains reuse but shipping CSS can swap out for the underlying component values). 

One way could be to add `@property --foo { private: true }` or some such. This would guarantee that declarations of, say, `:root { --foo: 1; }` don't appear in the JS OM.

<!-- comment id=5023039567 author=LeaVerou created=2026-07-20T13:54:52Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5023039567 -->
## @LeaVerou — 2026-07-20

How do these interact with property registration?

```css

@property --foo {
	syntax: "<length>";
	inherits: true;
	initial-value: 0px;
}

.foo {
	--foo: 1em; /* gets resolved */
}

.bar {
	@locals {
		--foo: 1em; /* does this get resolved? */
	}
}
```

This is not specific to this proposal, it applies to mixins too, but somehow it now seems harder.

It seems reasonable that it should still apply, but then how does that work, since what's _actually_ being set is a different property — or multiple, since presumably the rewrite is separate per callsite. Do we generate `@property` rules for them? Do we keep them in sync if the original rule gets mutated or removed?

<!-- comment id=5025995597 author=kbabbitt created=2026-07-20T18:53:52Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5025995597 -->
## @kbabbitt — 2026-07-20

Would it be possible, and make sense, for locals to adopt the "optionally declare a type in-line" syntax we have for mixin parameters? e.g.

```css
.foo {
  @locals {
    --foo <length>: 1em;
  }
  font-size: 16px;
  &, & > * {
    border-width: var(--foo);
  }
}
```

<!-- comment id=5028051061 author=tabatkins created=2026-07-20T22:55:27Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5028051061 -->
## @tabatkins — 2026-07-20

> How do these interact with property registration?

Identically to mixin arguments/locals, they don't - as you say, they're an unrelated custom property, with no connection to the registered one. They'll be untyped, unless/until we define inline typing for properties like we've talked about in another issue.

> Would it be possible, and make sense, for locals to adopt the "optionally declare a type in-line" syntax we have for mixin parameters?

Yes, I think so!

<!-- comment id=5047834293 author=LeaVerou created=2026-07-22T15:03:20Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5047834293 -->
## @LeaVerou — 2026-07-22

1. We should make sure `@locals` also works at the top-level, for sheet-level private variables.
2. Btw how did the name `@locals` come to be, have we bikeshed it yet?
3. Is there a way to reference a non-private variable with the same name from inside a `@locals` block?

<!-- comment id=5048113592 author=kizu created=2026-07-22T15:29:29Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5048113592 -->
## @kizu — 2026-07-22

@LeaVerou 

> We should make sure @locals also works at the top-level, for sheet-level private variables.

Yep, that would be great! Although, would be nice to see if we could eventually extend it to cover more than just variables, but other idents as well, see https://github.com/w3c/csswg-drafts/issues/11798

> Btw how did the name @locals come to be, have we bikeshed it yet?

I think it was briefly mentioned as a possible name during one of the calls; I think I like it more than many alternatives given `@namespace` is already used and there were objections about reusing it, and `@scope` is also already used.

> Is there a way to reference a non-private variable with the same name from inside a @locals block?

Yep, we for sure need to have some way to do this. I guess, when you use the `var()` _inside_ the `@locals`, would it refer to the local or global values?

Also, something to think about:

```css
a {
  --foo: 1em;
  @locals {
    --foo: 2em;
  }
}
```

```css
b {
  @locals {
    --bar: 1em;
  }
  --bar: 2em;
}
```

Does defining some variable inside the `@local` only be applied to `var()` of the same variable, or to the non-local definitions as well? It is a very common case where you'd want to override a local variable after defining it. Basically,

```css
a {
  @locals {
    --color: blue;
  }
  &:hover {
    --color: deepPink;
  }
  color: var(--color);
}
```

Should this work? If not, we need to be able to achieve this, and ideally without doing something like

```css
a {
  @locals {
    --color: blue;
    &:hover {
      --color: deepPink;
    }
  }
  color: var(--color);
}
```

Or, not _only_ like this, as you’d want to reuse the variables in nested rules without duplicating their selectors both inside and outside `@locals`.

(which is why my ideas around lexical scoping wanted to be able to nest any rules with any declarations inside instead of applying the `@locals` to the rule where it was used)

<!-- comment id=5050748895 author=kbabbitt created=2026-07-22T19:50:14Z url=https://github.com/w3c/csswg-drafts/issues/14004#issuecomment-5050748895 -->
## @kbabbitt — 2026-07-22

My mental model has been that, functionally, locals are just custom property declarations that get hoisted into the parent scope given super-duper-secret names, and rewrite variable references within that parent scope and descendant scopes to that super-duper-secret name. I base that model on the direction we've taken for mixin locals. Though I don't think I would want to *implement* them that way, because inheritance would leak them out of the scope where they can be referenced. That's not author-visible because of the super-duper-secret name, but it would be unfortunate for performance to carry them around, and they could potentially trigger invalidation heuristics that they don't need to. Maybe there's an alternate implementation where locals are bucketed separately from other variables and we just attach locals to the element where they get defined and search up the parent chain for them for descendant references, not sure yet.

Here's an example I was working; I'm assuming no other declarations e.g. inline styles that would override anything in here:

```css
@property --real-size {
    syntax: "<length>";
    inherits: true;
    initial-value: 29px;
}
.a {
    @locals {
        --local-size <length>: 1em;
    }
    --real-size: 10em;
    font-size: 20px;
    /* --local-size resolves to 20px; --real-size resolves to 200px */
    width: var(--local-size, 9em); /* 20px */

    & > .b {
        font-size: 30px; 
        width: var(--local-size, 93em); /* 20px: resolved --local-size inherits */
        height: var(--real-size, 87em); /* 200px: resolved --real-size inherits */
    }

    & > .c {
        @locals {
            /* I think this is an eclipsing definition rather than an override, */
            /* so if <length> were not re-specified it would be untyped in this scope? */
            --local-size <length>: 4em;
        }
        /* inherits font-size: 20px, so --local-size resolves to 80px */
        width: var(--local-size, 75em); /* 80px */
    }

    & + .d {
        width: var(--local-size, 93em); /* 93em: vars do not inherit to siblings */
        height: var(--real-size, 87em); /* 29px: registered initial value takes effect */
    }
}
.a > .b {
    margin: var(--local-size, 75em); /* 75em: reference to local does not resolve outside of its scope */
    padding: var(--real-size, 63em); /* 200px: normal var inherits, and it was resolved on parent */
}
```

-----

> We should make sure `@locals` also works at the top-level, for sheet-level private variables.

I think that would work under this model if we attach sheet-level private variables to the root element.

> ```css
> a {
>   @locals {
>     --color: blue;
>   }
>   &:hover {
>     --color: deepPink;
>   }
>   color: var(--color);
> }
> ```

I think that could be made to work as well; it would just mean that the rewrite triggered by the local definition rewrites both the name and value sides of declarations, not just the name side. But going that route would rule out referencing a non-private variable with the same name from inside a `@locals` block, I think.
