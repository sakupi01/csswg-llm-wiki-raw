---
number: 14410
title: "[css-mixins-2] How do private properties affect style queries?"
state: "open"
labels: ["css-mixins-2"]
author: "romainmenke"
created_at: "2026-08-30T12:54:02Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14410"
comments: 3
has_resolution: false
synced_at: "2026-09-03T04:21:11Z"
---

https://drafts.csswg.org/css-mixins-1/#private

> Within the style rule (and any nested rules), any custom property with that name is actually a private property, and any reference to a custom property of that name (for example, with a `var()`) is a reference to the private property.

------

```css
.foo {
	@private {
		--alpha: private;
	}

	--alpha: public;

	--bar: if(
		style(--alpha: private): cyan; 
		else: orange
	);
}
```

I expect `style(--alpha: private)` to refer the private property and have a value of `private` in this style query and for `--bar` to then have a value of `cyan`.

Correct?

-----

```css
.foo {
	@private {
		--alpha: private;
	}

	--alpha: public;

	@container style(--alpha: private) {
		--bar: cyan;
	}
}
```

Here I would also expect `@container style(--alpha: private)` to reference the private property.
But the style query itself can never be true as no container can possibly have this private property.

Correct?

Although this style query would match:

```css
.foo {
	@private {
		--alpha: private;
	}

	--alpha: public;

	& .bar {
		@container style(--alpha: private) {
			--bar: cyan;
		}
	}
}
```

<!-- comment id=5510092973 author=sorvell created=2026-09-02T13:12:47Z url=https://github.com/w3c/csswg-drafts/issues/14410#issuecomment-5510092973 -->
## @sorvell — 2026-09-02

Based on this:

> any reference to a custom property of that name (for example, with a [var()](https://drafts.csswg.org/css-variables-2/#funcdef-var)) is a reference to the private property

```css
@private {
  --alpha: private;
}
--alpha: public;
```

I would think that the private `--alpha` is now set to `public`.

I would also think that this does not set the non-private `--alpha` but it's value is `private` since that's last:

```css
--alpha: public;
@private {
  --alpha: private;
}
```

<!-- comment id=5510274631 author=sorvell created=2026-09-02T13:26:26Z url=https://github.com/w3c/csswg-drafts/issues/14410#issuecomment-5510274631 -->
## @sorvell — 2026-09-02

> Here I would also expect @container style(--alpha: private) to reference the private property.
But the style query itself can never be true as no container can possibly have this private property.

Agree, and I think you just have to make sure to not clobber public names you care about with your private names. That's maybe just a reasonable restriction?

<!-- comment id=5510301992 author=romainmenke created=2026-09-02T13:28:28Z url=https://github.com/w3c/csswg-drafts/issues/14410#issuecomment-5510301992 -->
## @romainmenke — 2026-09-02

Is an assignment a reference to a custom property of that name? @tabatkins
