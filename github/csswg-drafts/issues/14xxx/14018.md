---
number: 14018
title: "[css-scroll-snap] Clarify snapping behavior with multiple single-axis scrollers"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-scroll-snap-1"]
author: "vmpstr"
created_at: "2026-06-05T15:59:14Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14018"
comments: 2
has_resolution: true
synced_at: "2026-07-23T00:54:54Z"
---

### Context
We resolved to create single-axis scrollers by allowing a scrollable value in one axis and clip in the other: https://github.com/w3c/csswg-drafts/issues/12289
This allowed us to create interesting effects and we resolved that sticky, for example, tracks per axis scrollers: https://github.com/w3c/csswg-drafts/issues/865#issuecomment-4040585511
@tabatkins put together a draft PR to update a bunch of specs to recognize single-axis scrollers: https://github.com/w3c/csswg-drafts/pull/13903

### Problem & Proposal

One of the questions is whether scroll snap also tracks per-axis scrollers. I think that would be reasonable. One hiccup is that [`scroll-snap-type`](https://drafts.csswg.org/css-scroll-snap/#scroll-snap-type) includes an axis in which this scroller snaps.

So, it's pretty clear what happens in a y-only scroller with a y-only scroll-snap-type: it should only snap in y and snapping in x can happen in some ancestor scroller:

```
/* does _not_ capture x snapping, it's not a scroller in that axis and scroll-snap-type doesn't include that axis */
.scroller {
  overflow-y: scroll;
  overflow-x: clip;
  scroll-snap-type: y mandatory;
}
```

Similarly, if the scroller is a scroller in both axis, then that's just the current state: nothing propagates to ancestor scrollers

However, what happens if we have a y-only scroller with a snap that includes the x axis?

```
.scroller {
  overflow-y: scroll;
  overflow-x: clip;
  scroll-snap-type: both mandatory;
}
```
I think it's reasonable to say that because this container is a not a scroller in the x direction, then it cannot prevent some ancestor from snapping in the x direction, so the scroll-snap-type here is essentially ignored when it comes to x direction of some ancestor scroller. That seems to be similar to how we deal with sticky.

For completeness, @flackr also pointed out that if the developer doesn't want some ancestor scroller to snap in the x axis, they can always say `scroll-snap-align: center none` (in horizontal-tb)

The proposal is to clarify that scroll snap also respects single axis scrollers and "propagates" across such scrollers in the axis for which the scroller clips

<!-- comment id=4633584304 author=flackr created=2026-06-05T16:39:27Z url=https://github.com/w3c/csswg-drafts/issues/14018#issuecomment-4633584304 -->
## @flackr — 2026-06-05

This makes sense to me, since it's establishing the same behavior we have for non-scroll containers on these non-scrolling axes. Just to play devil's advocate, we could only propagate the other axis if the inner snap container didn't claim any snap behavior for it, but this would be inconsistent with what we do for `overflow: clip` on both axes, so i have a preference for ignoring the clip axis snap type.

<!-- comment id=5048738521 author=css-meeting-bot created=2026-07-22T16:28:22Z url=https://github.com/w3c/csswg-drafts/issues/14018#issuecomment-5048738521 resolution=true -->
## @css-meeting-bot — 2026-07-22

The CSS Working Group just discussed `[css-scroll-snap] Clarify snapping behavior with multiple single-axis scrollers`, and agreed to the following:

* `RESOLVED: Snap positions affect the nearest ancestor scroll container in each axis they align to`

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> vmpstr: we have resolved to be able to create single axis scrollers by not coercing overflow clip into hidden<br>
&lt;kbabbitt> ... when the other axis is something like scroll<br>
&lt;kbabbitt> ... this means that the element becomes a scroller in one axis but not the other<br>
&lt;kbabbitt> ... TabAtkins put together a draft PR to update a bunch of specs for this<br>
&lt;TabAtkins> (no longer a draft PR, it's merged now)<br>
&lt;kbabbitt> ... a lot of cases you have to find nearest scroll container and which axis matters<br>
&lt;kbabbitt> ... this issue is about scroll-snap<br>
&lt;kbabbitt> ... I think we want to say it's reasonable to want to snap 2 different scrollers for 2 different axes<br>
&lt;kbabbitt> ... one smallproblem here is that scroller, when it specifies snap type, also specifies an axis<br>
&lt;kbabbitt> ... question is what do we do in these cases<br>
&lt;kbabbitt> ... think it's obvious for vertical scroller with vertical axis<br>
&lt;TabAtkins> definitely +1 to the proposal in the issue, it's consistent with the non-scrolling behavior and how we treat single-axis scrollers elsewhere<br>
&lt;kbabbitt> ... question is what do we do if we have a vertical scroller with snap in horizontal axis<br>
&lt;kbabbitt> ... proposal I have is that we ignore the fact that scroll-snap-type has horizontal axis since scroller is not horizontal<br>
&lt;kbabbitt> ... would find scroller higher up<br>
&lt;kbabbitt> ... examp[les in the issue<br>
&lt;kbabbitt> flackr: this is the same as what we do for scroll-snap-type on non-scroller<br>
&lt;kbabbitt> vmpstr: right, first we find a scroller then check its scroll-snap-type<br>
&lt;kbabbitt> astearns: questions or concerns?<br>
&lt;flackr> +1 to the proposal<br>
&lt;smfr> q+<br>
&lt;TabAtkins> non-scrolling axises don't capture snaps<br>
&lt;kbabbitt> vmpstr: scroll-snap-type which doesn't affect a scroller doesn't contribute scroll points<br>
&lt;kbabbitt> smfr: if you have nested scrollers and inner scroller is horizontal, when you're selecting snap candidates in parent scroller, you are looking at ones in inner scroller?<br>
&lt;kbabbitt> vmpstr: the ones for other axis, yes<br>
&lt;kbabbitt> smfr: when you specify a snap target, do you give it an axis?<br>
&lt;kbabbitt> flackr: yes, you specify scroll-snap-align in each axis<br>
&lt;kbabbitt> smfr: guess that makes sense<br>
&lt;kbabbitt> astearns: this makes sense to me but summary in minutes doesn't yet<br>
&lt;kbabbitt> ... when there's a mismatch between scroll-snap-type and scroll axes, we ignore the mismatched scroller?<br>
&lt;kbabbitt> vmpstr: essentially you do bitwise and between scroll-snap-type axis and which axis the element is a scroller<br>
&lt;flackr> Proposed resolution: Snap positions affect the nearest ancestor scroll container in each axis they align to<br>
&lt;kbabbitt> ... if it has both but scroller is only vertical only consider snapping in vertical axis, horizontal snap can be in ancestor scroller<br>
&lt;flackr> existing spec text is "Snap positions only affect the nearest ancestor scroll container on the element’s containing block chain."<br>
&lt;TabAtkins> yeah +1 to resolution<br>
&lt;kbabbitt> flackr: wrote a resolution based on existing spec text<br>
&lt;kbabbitt> ... I am proposing that we just say "in each axis which they align to"<br>
&lt;ChrisL> +1<br>
&lt;kbabbitt> astearns: right because really we are affecting scrolling of scroll container based on what scroll positions<br>
&lt;kbabbitt> astearns: any objections to flackr's proposed resolution?<br>
&lt;kbabbitt> RESOLVED: Snap positions affect the nearest ancestor scroll container in each axis they align to<br>
</details>
