---
number: 14338
title: "[css-mixins] Evaluation context for custom function arguments: typed vs untyped"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-mixins-1"]
author: "tabatkins"
created_at: "2026-08-17T19:56:37Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14338"
comments: 10
has_resolution: true
synced_at: "2026-09-10T04:39:57Z"
---

Take this example:

```css
.foo {
	width: calc(1px * random(property-scoped, 1, 10, 1) * hypot(random(property-scoped, 1, 10, 1), random(property-scoped, 1, 10, 1)));
	height: calc(1px * random(property-scoped, 1, 10, 1) * --hypot(random(property-scoped, 1, 10, 1), random(property-scoped, 1, 10, 1)));
	padding: calc(1px * random(property-scoped, 1, 10, 1) * --hypot-un(random(property-scoped, 1, 10, 1), random(property-scoped, 1, 10, 1)));
}
@function --hypot(--x <number>, --y <number>) returns <number> {
  result: sqrt(pow(var(--x), 2) + pow(var(--y), 2));
}
@function --hypot-un(--x, --y) {
  result: sqrt(pow(var(--x), 2) + pow(var(--y), 2));
}
```

The question is: what's the random() behavior here? In `width` the behavior is clear - you've got three random()s, all just `property-scoped`, so they'll all gain an `<ua-random-ident>` of `ua-width` and end up spitting out the same number. There's only 10 possible results, equivalent to `1 * hypot(1, 1)`, `2 * hypot(2, 2)`, `3 * hypot(3, 3)`, etc.

In 'height', the spec has [a clear answer, too](https://drafts.csswg.org/css-mixins/#evaluate-a-custom-function) (step 8 loads up the argument values into the --x and --y custom properties, then step 9 evaluates the styles on the hypothetical element created for the function) - the first random gets a `<ua-random-ident>` of `ua-height`, while the two random()s passed to the function end up with distinct `<ua-random-ident>`s (`ua---some-fn---x` and `ua---some-fn---y`, respectively). All three spit out distinct random numbers, so there's a thousand possible results, equivalent to `1 * --hypot(2, 3)`, `9 * --hypot(8, 7)`, `2 * --hypot(10, 6)`, etc.

In 'padding' the behavior is *also* clear - neither the arguments nor the result have types, so leave their values unresolved, and spit them out unchanged, just wrapped in the sqrt(...) math. So all the randoms again get parsed as part of parsing 'padding', all sharing the same `<ua-random-ident>` of `ua-padding`, and generate the same values; back to 10 possible results.

It seems a little unfortunate that what is mostly an internal detail of the function - whether an argument is typed or not - has such a dramatic effect on random(). The same effect can be observed for other cases where there is a difference between evaluating a value on the element vs on a child; for example, `font-size: calc(1em + --some-fn(1em))` will evaluate the *first* 1em in the context of "used in `font-size`", so it'll use the parent's font-size instead, while the *second* 1em will be evaluated inside the function (assuming the argument or return type is typed as `<length>`), and will end up being cyclic (relies on `font-size` but has `font-size` in its substitution context).

The author is writing these values all at the same level, in the same "mental space", and it's a little weird if they evaluate completely differently. I think it is a big author surprise, and makes custom functions more confusing to use in general.

It turns out that Chrome has already been violating the spec here; it evaluates arguments in the outer context, as if the custom function were a normal function with its arguments grammars dropped in place. (That is, it treats `--hypot()` as if it were a CSS-defined function with grammar `--hypot(<number>, <number>)`, more or less.)  I think this is probably a better behavior for us to adopt.

Agenda+ to see if anyone has any thoughts or objections before I make edits.

<!-- comment id=5319591004 author=tabatkins created=2026-08-17T20:00:00Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5319591004 -->
## @tabatkins — 2026-08-17

(To be clear: function arg defaults and local variables should continue to work as they do, evaluating inside the function on the hypothetical element. That's clear and easy to understand. It's just at the boundary layer, where the author is writing a value *outside* the function and passing it as an argument, that I think the spec behavior is less good.)

<!-- comment id=5416224533 author=fantasai created=2026-08-25T20:19:03Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5416224533 -->
## @fantasai — 2026-08-25

Retitling this because it's not about random(), it's about when we compute the arguments.

<!-- comment id=5420343066 author=tabatkins created=2026-08-26T03:53:48Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5420343066 -->
## @tabatkins — 2026-08-26

Yup, thanks. I only realized partway thru writing the issue that it was actually a wider question of value evaluation, but didn't change the title I'd already written. ^_^

<!-- comment id=5423068690 author=anttijk created=2026-08-26T09:07:09Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5423068690 -->
## @anttijk — 2026-08-26

Seems like the right thing to do. The current model is confusing and buys nothing (the context inside function does not differ from the outside one in ways that would be useful). I don't understand why default argument evaluation would have different behavior though. That is surprising and more complex to implement. 
```
@function --foo(--len <length>: 1em) { result: var(--len); }
font-size: --foo(1em);
font-size: --foo(); // cyclic
```

Note that Chrome currently also appears to resolve locals and the result descriptor in outside context.

<!-- comment id=5427887665 author=css-meeting-bot created=2026-08-26T16:13:45Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5427887665 resolution=true -->
## @css-meeting-bot — 2026-08-26

The CSS Working Group just discussed `[css-mixins] Evaluation context for custom function arguments: typed vs untyped`, and agreed to the following:

* `RESOLVED: Function arguments (including defaults), as well as locals and result, are evaluated in the context in which the function is called.`

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> TabAtkins: see the issue example<br>
&lt;emilio> ... it shows the different behavior for passing it arguments<br>
&lt;emilio> ... in particular the random() function but anything that has computation<br>
&lt;emilio> ... for custom functions we create a property<br>
&lt;emilio> ... so whatever you pass is computed based on that hypothetical element<br>
&lt;emilio> ... that's not the case for regular functions<br>
&lt;emilio> ... e.g. if you write 1em it gets resolved against that element<br>
&lt;emilio> ... seems unfortunate<br>
&lt;emilio> ... turns out chrome did not implement what the spec says<br>
&lt;emilio> ... it seems they're evaluating the arguments in the context of where the function is called<br>
&lt;emilio> ... in the example with --hypot() behaves as if they had two number arguments<br>
&lt;emilio> ... the example uses random() but also ems and such are affected<br>
&lt;emilio> ... needs to know whether you're in the font-size property or what not<br>
&lt;emilio> ... e.g. if you use 1em in a custom function in font-size they resolve against the parent<br>
&lt;fantasai> PROPOSED: Function arguments are evaluated in the context in which the function is called.<br>
&lt;lea> +1, the opposite seems extremely bizarre<br>
&lt;emilio> ... proposal is to evaluate the arguments in the context outside the function<br>
&lt;emilio> ... generally would make stuff more like people would expect<br>
&lt;ydaniv> +1<br>
&lt;emilio> ... currently planned to keep function default arguments<br>
&lt;lea> q+<br>
&lt;lea> q-<br>
&lt;emilio> ... but I could be convinced<br>
&lt;lea> q+<br>
&lt;emilio> ... thoughts?<br>
&lt;emilio> fantasai: Antti replied that default argument would be confusing and hard to implement if it wasn't evaluated in the outside context<br>
&lt;astearns> ack fantasai<br>
&lt;emilio> ... also note that chrome resolves locals and the result descriptor in the outside context<br>
&lt;emilio> TabAtkins: that doesn't make any sense to me<br>
&lt;emilio> ... what's the inside context even for in that case?<br>
&lt;emilio> fantasai: for your convenience<br>
&lt;emilio> TabAtkins: what does it even do in that case?<br>
&lt;emilio> q+<br>
&lt;fantasai> emilio: It's just a scoping mechanism<br>
&lt;kbabbitt> scribe+<br>
&lt;kbabbitt> emilio: keeps locals, it's a scope effectively<br>
&lt;emilio> ack emilio<br>
&lt;kbabbitt> ... it is weird<br>
&lt;emilio> astearns: should we consider the locals stuff elsewhere?<br>
&lt;emilio> TabAtkins: ok chrome's behavior is weird<br>
&lt;emilio> ... I'm willing to take this as a tentative evaluation<br>
&lt;astearns> ack lea<br>
&lt;emilio> s/evaluation/resolution<br>
&lt;fantasai> PROPOSED: Function arguments (including defaults), as well as locals and result, are evaluated in the context in which the function is called.<br>
&lt;emilio> lea: first, huge +1<br>
&lt;emilio> ... I'm confused about not changing the defaults<br>
&lt;emilio> ... that seems surprising, what's the rationale?<br>
&lt;emilio> TabAtkins: happy to make them consistent<br>
&lt;emilio> ... seems to be what impls are going for anyway<br>
&lt;emilio> ... I thought the inner context would be easier to reason about<br>
&lt;fantasai> You don't have a local font-size, Tab. How are you going to compute 1em? ...<br>
&lt;emilio> ... seems impls went for that but didn't tell me<br>
&lt;emilio> astearns: are there multiple impls?<br>
&lt;TabAtkins> yes you do, the ehypothetical element inherits from the element being invoked on<br>
&lt;emilio> TabAtkins: chrome ships, safari is implementing<br>
&lt;emilio> PROPOSED: [what fantasai wrote up thread]<br>
&lt;fantasai> RESOLVED: Function arguments (including defaults), as well as locals and result, are evaluated in the context in which the function is called.<br>
&lt;CameronPsychpsyo> Ladybird is working on custom functions as well IIRC<br>
&lt;lea> also this means that they don't need to be computationally independent, like regular initial values<br>
&lt;romain> Safari is shipping<br>
&lt;TabAtkins> Lea, correct<br>
</details>

<!-- comment id=5540208742 author=andruud created=2026-09-04T12:08:26Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5540208742 -->
## @andruud — 2026-09-04

On the surface, parts of that resolution makes no sense. Resolve _locals_ in the outer context? Unless we are strictly talking about resolving things like `em` units (not e.g. `var()`)?

> <emilio> [fantasai] ... also note that chrome resolves locals and the result descriptor in the outside context
> <emilio> TabAtkins: that doesn't make any sense to me
> <emilio> TabAtkins: ok chrome's behavior is weird

Can I get an example that demonstrates the weird Chrome behavior?

If relevant, the spec has already said for a long time that things other than `var()` [resolve as if on the "root element"](https://drafts.csswg.org/css-mixins-1/#:~:text=Aside%20from%20references%20to%20custom%20properties%20(which%20use%20the%20values%20on%20el%20as%20normal)%20and%20numbers/percentages%20(which%20are%20left%20unresolved%20in%20custom%20properties%2C%20as%20normal)%2C%20all%20values%20which%20would%20normally%20refer%20to%20the%20element%20being%20styled%20instead%20refer%20to%20calling%20context%E2%80%99s%20root%20element.).

<!-- comment id=5542042550 author=anttijk created=2026-09-04T14:38:38Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5542042550 -->
## @anttijk — 2026-09-04

I just meant that `em` units etc resolve against the parent font-size in locals/result too and don't create cycles.
```
@property --foo { inherits:false; syntax:"<length>"; initial-value:100px; }
@function --double() returns <length> {
    --foo: 1em;
    result: calc(var(--foo) * 2);
}
div { font-size: --double(); }
```

<!-- comment id=5544893846 author=tabatkins created=2026-09-04T18:33:20Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5544893846 -->
## @tabatkins — 2026-09-04

@anttijk note that `@property` registrations don't affect locals in a function at all; they're unrelated properties. Locals are (currently) always untyped.

[Here's a testcase](https://software.hixie.ch/utilities/js/live-dom-viewer/saved/14856) that does show off the behavior, tho:

```html
<!doctype html>
<div class=em>foo</div>
<div class=em-in-arg>foo</div>
<div class=em-in-defaults>foo</div>
<div class=em-in-result>foo</div>

<style>
body { font-size: 10px; }
.em { font-size: 2em; border-left: 2em solid;}
.em-in-arg { font-size: --em-arg(2em); border-left: --em-arg(2em) solid; }
.em-in-defaults { font-size: --em-default(); border-left: --em-default() solid; }
.em-in-result { font-size: --em-result(); border-left: --em-result() solid; }

@function --em-arg(--arg <length>) { result: var(--arg); }
@function --em-default(--arg <length>: 2em) { result: var(--arg); }
@function --em-result() returns <length> { result: 2em; }
</style>
```

Starting from an inherited font-size of 10px, each child uses `2em` in font-size (getting 20px) and `2em` in border-left (getting 40px). All four examples act the exact same, meaning they resolve those values in the context they're called from, regardless of where precisely they're actually resolved in the function. The current spec instead requires them to evaluate on a "hypothetical element" which is a child of the invoking element, meaning the calls in `font-size` should be cyclic and return the GIV, while the calls in `border-left` will see the font-size inherited to the hypothetical element (which would end up being 10px in these examples, since the `div`s don't have a valid `font-size` declaration) and return 20px.

---------

@andruud the variable shadowing from args and locals would, of course, continue to apply, but *in all other aspects* it would resolve the values (of args, arg defaults, `result`, and eventually typed locals) as if they were arguments passed to a normal CSS function used in that context, with the appropriate grammars. So `hypot()` and `--hypot()` will always resolve things like lengths, `random()`, etc in the same way. (`random()` still needs *some* special-casing to make defaults and `result` work, but arg values, at least, would act the same as if they were passed to normal functions).

<!-- comment id=5545246167 author=tabatkins created=2026-09-04T19:05:51Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5545246167 -->
## @tabatkins — 2026-09-04

> If relevant, the spec has already said for a long time that things other than var() [resolve as if on the "root element"](https://drafts.csswg.org/css-mixins-1/#:~:text=Aside%20from%20references%20to%20custom%20properties%20(which%20use%20the%20values%20on%20el%20as%20normal)%20and%20numbers/percentages%20(which%20are%20left%20unresolved%20in%20custom%20properties%2C%20as%20normal)%2C%20all%20values%20which%20would%20normally%20refer%20to%20the%20element%20being%20styled%20instead%20refer%20to%20calling%20context%E2%80%99s%20root%20element.).

Sigh, that is indeed exactly the text I was missing/had forgotten about. Tho it's incomplete, as the property context you're calling the function from also affects things, as demonstrated by the testcase above.

<!-- comment id=5568886561 author=andruud created=2026-09-07T09:58:13Z url=https://github.com/w3c/csswg-drafts/issues/14338#issuecomment-5568886561 -->
## @andruud — 2026-09-07

@tabatkins OK, then Chrome is working as intended here, as far as I can tell.

> Tho it's incomplete, as the property context you're calling the function from also affects things, as demonstrated by the testcase above.

Yeah, it should be referring to the root "calling context" instead:

> "Either way, this provides a _calling context_, which **contains the property or descriptor name** containing the `<dashed-function>`, and the element (or "hypothetical" element) that property/descriptor is being applied to." [[1]](https://drafts.csswg.org/css-mixins-1/#calling-context:~:text=provides%20a%20calling%20context)
