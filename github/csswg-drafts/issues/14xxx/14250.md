---
number: 14250
title: "[css-forms-1] Inherit all typography, but not into icons"
state: "open"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Edits", "css-forms-1"]
author: "fantasai"
created_at: "2026-07-31T21:42:03Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14250"
comments: 2
has_resolution: true
synced_at: "2026-08-27T09:45:10Z"
---

We currently inherit font and all its longhands explicitly, however there are other typesetting properties that we do not inherit because they are currently blocked by the HTML UA default stylesheet, see https://html.spec.whatwg.org/multipage/rendering.html#form-controls which currently says:

```
input, button, textarea {
  letter-spacing: initial;
  word-spacing: initial;
  line-height: initial;
}

input, select, button, textarea {
  text-transform: initial;
  text-indent: initial;
  text-shadow: initial;
  appearance: auto;
}
```

Proposal:

1. Merge these two selector lists in the HTML UA stylesheet (for `auto` and `base`) so that the rules apply consistently including to `select`.
2. Keep the `text-transform`, `text-indent`, and `line-height` resets for `base` appearance (no change).
3. Switch `letter-spacing`, `word-spacing`, and `text-shadow` to all `inherit` for base appearance.
4. Assign `system-ui` to `::picker-icon` and `::checkmark` so it doesn’t inherit random symbol glyphs from random fonts; and make sure it doesn’t get obliqued.

```
input, select, button, textarea {
  letter-spacing: inherit;
  word-spacing: inherit;
  text-shadow: inherit;
}

::picker-icon, ::checkmark {
  font-family: system-ui;
  font-style: normal;
}
```

<!-- comment id=5166415503 author=Psychpsyo created=2026-08-03T12:40:20Z url=https://github.com/w3c/csswg-drafts/issues/14250#issuecomment-5166415503 -->
## @Psychpsyo — 2026-08-03

Note that (all?) engines currently override the line-height for select elements in native code: https://github.com/whatwg/html/issues/4481

So effectively, there is something akin to `select { line-height: initial !important; }` going on already (although not specified anywhere).

<!-- comment id=5358336573 author=css-meeting-bot created=2026-08-20T15:50:43Z url=https://github.com/w3c/csswg-drafts/issues/14250#issuecomment-5358336573 resolution=true -->
## @css-meeting-bot — 2026-08-20

The CSS Working Group just discussed `[css-forms-1] Inherit all typography, but not into icons`, and agreed to the following:

* `RESOLVED: letter-spacing, word-spacing, text-shadow inherit on <select> for appearance: base`
* `RESOLVED: Apply 'font: 100% system-ui' to ::picker-icon and ::checkmark and any other icon pseudos we add in the future`

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> https://wpt.fyi/results/html/rendering/non-replaced-elements/form-controls/resets.html?label=master&amp;label=experimental&amp;aligned<br>
&lt;jarhar> q+<br>
&lt;emilio> q+<br>
&lt;JohnJansen> ack jarhar<br>
&lt;ntim> fantasai: we decided to inherit typography into text controls. The current sheet excludes certain props. For appearance: base, we should definitely inherit stuff, including select. Second thing is to set system fonts/font-style onto ::checkmark / ::picker-icon to avoid putting weird fonts on those.<br>
&lt;ntim> jarhar: reshuffling text things to inherit seems fine. Second thing is hopefully fine. We should check if system-ui looks ok on windows / linux<br>
&lt;ntim> fantasai: it's definitely going to look better than random fonts<br>
&lt;fantasai> input, button, textarea { letter-spacing: initial; word-spacing: initial; line-height: initial;<br>
&lt;fantasai> }<br>
&lt;ntim> jarhar: what does exactly merging the two style rules mean?<br>
&lt;fantasai> input, select, button, textarea { text-transform: initial; text-indent: initial; text-shadow: initial; appearance: auto;<br>
&lt;fantasai> }<br>
&lt;ntim> fantasai: it would be merging the two above<br>
&lt;ntim> masonf: are there changes for appearance: auto or just base?<br>
&lt;ntim> fantasai: either way is ok<br>
&lt;emilio> Gecko does have select here: https://searchfox.org/firefox-main/rev/ad5db99e4d2e00e9b5db2700c69050d709c7f340/layout/style/res/forms.css#118-129<br>
&lt;ntim> masonf: i think our general focus should be appearance: base. auto is probably harder. select is used everywhere, so breakage is very likely<br>
&lt;JohnJansen> ack emilio<br>
&lt;fantasai> PROPOSED: letter-spacing, word-spacing, text-shadow inherit on &lt;select><br>
&lt;lwarlow> q+<br>
&lt;masonf> you mean   `s/on &lt;select>/on appearance:base &lt;select>`, right?<br>
&lt;ntim> emilio: we probably want to reset most of font on the items<br>
&lt;JohnJansen> ack lwarlow<br>
&lt;ntim> emilio: gecko already does reset some stuff globally<br>
&lt;fantasai> PROPOSED: letter-spacing, word-spacing, text-shadow inherit on &lt;select> for appearance: base<br>
&lt;ntim> fantasai: auto appearance is resetting stuff to the default value. For appearance: base the intention is to inherit font into the control. Proposal includes multiple things<br>
&lt;JohnJansen> q?<br>
&lt;jarhar> +1 because we dont have to change appearance:auto<br>
&lt;masonf> +1 for the same reason<br>
&lt;keithamus> +1<br>
&lt;lwarlow> +1<br>
&lt;fantasai> PROPOSED: Apply font-family: system-ui; font-style: normal to ::picker-icon and ::checkmark<br>
&lt;fantasai> RESOLVED: letter-spacing, word-spacing, text-shadow inherit on &lt;select> for appearance: base<br>
&lt;jarhar> +1 but id like to look at windows and linux<br>
&lt;keithamus> +1<br>
&lt;fantasai> PROPOSED: Apply font-family: system-ui; font-style: normal to ::picker-icon and ::checkmark and any other icon pseudos we add in the future<br>
&lt;emilio> q+<br>
&lt;masonf> q+<br>
&lt;lwarlow> +1<br>
&lt;JohnJansen> ack emilio<br>
&lt;fantasai> 'font: 1em system-ui'<br>
&lt;ntim> emilio: should we set the font shorthand?<br>
&lt;ntim> emilio: there's also other weird font properties<br>
&lt;JohnJansen> ack masonf<br>
&lt;ntim> masonf: this does change existing current stuff right?<br>
&lt;fantasai> PROPOSED: Apply 'font: 100% system-ui' to ::picker-icon and ::checkmark and any other icon pseudos we add in the future<br>
&lt;ntim> fantasai: I suspect people who care about the checkmark probably already changed it<br>
&lt;lwarlow> +1<br>
&lt;ntim> masonf: you're probably right<br>
&lt;ntim> keithamus: do we need this if we're going to use custom icons?<br>
&lt;keithamus> +1<br>
&lt;fantasai> RESOLVED: Apply 'font: 100% system-ui' to ::picker-icon and ::checkmark and any other icon pseudos we add in the future<br>
&lt;ntim> fantasai: we're still using unicode<br>
&lt;fantasai> for ::checkmark<br>
</details>
