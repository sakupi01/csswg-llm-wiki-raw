---
number: 14092
title: "[web-animations-2] How do relative units get evaluated when being processed as offsets in KeyframeEffect.setKeyframes"
state: "open"
labels: ["web-animations-2"]
author: "weinig"
created_at: "2026-06-27T19:24:31Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14092"
comments: 0
has_resolution: false
synced_at: "2026-07-04T04:45:46Z"
---

WebAnimations 2 [extends](https://drafts.csswg.org/web-animations-2/#keyframe-offset-type) the types allowed for offsets in  KeyframeEffect.setKeyframes (and the [process a keyframes argument](https://drafts.csswg.org/web-animations-1/#process-a-keyframes-argument) algorithm) from `double` to `(CSSNumberish? or TimelineRangeOffset or DOMString)`, stating:

> `DOMString` is parsed into a `TimelineRangeOffset` according to the syntax of `<keyframe-selector>`, and the resulting `TimelineRangeOffset` or `CSSNumericValue` is interpreted as defined for `@keyframes`.

This fails to define how non-trivial numeric values, like a calc() with relative length units, in either the `DOMString` or `TimelineRangeOffset` should be evaluated.

This is a similar issue to other places that TimelineRangeOffset is allowed to be set in the animations specs:

- https://github.com/w3c/csswg-drafts/issues/13852
- https://github.com/w3c/csswg-drafts/issues/13853

My proposal here, like with those two, would be to restrict restrict allowed values from the `TimelineRangeOffset`, `CSSNumericValue` or `DOMString` input to absolute units as was done with DOMMatrix and IntersectionObservers.

cc @birtles @flackr @Emilio 
