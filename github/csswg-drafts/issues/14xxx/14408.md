---
number: 14408
title: "[css-mixins-2] Why do mixins see private properties visible to the style rule it is substituted into?"
state: "open"
labels: ["css-mixins-2"]
author: "romainmenke"
created_at: "2026-08-29T16:43:03Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14408"
comments: 9
has_resolution: false
synced_at: "2026-09-10T04:39:57Z"
---

https://drafts.csswg.org/css-mixins-1/#private

> Applying a mixin with `@apply` effectively substitutes the mixin in-place, so a mixin sees any private properties visible to the style rule it’s substituted into (if it doesn’t override them with its own mixin parameters or private properties).

This seems weird to me. It kinda breaks the entire premise of `@private` making things private.
I can easily imagine cases where authors use both `@private` and `@mixin` but not in a coordinated fashion and expect custom props to be isolated from each other.

What is the motivation for the current spec?

<!-- comment id=5464289517 author=romainmenke created=2026-08-29T19:04:57Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5464289517 -->
## @romainmenke — 2026-08-29

> Issue 3 What about custom functions? If they refer to a custom property not defined in the function, should they see an outer private property? I’m inclined to say yes; extracting some styles to a custom function should, generally, act identically to inlining them.

Same here, I would expect these not to be visible.
If authors use `@private` I think they will expect these to be separate.

Authors can always opt-in to sharing props by not using `@private` or using arguments.
But, by making private props visible, there is no method of making them invisible to `@function` and `@mixin`. 

----

Maybe `@private` could take params? :)
`@private not-even-share-with-mixins {}`

<!-- comment id=5502566731 author=sorvell created=2026-09-02T00:37:56Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5502566731 -->
## @sorvell — 2026-09-02

This also initially struck me as really weird and wrong, but I think I'm coming around to it maybe being ok [1].

If you are not using a param or private var, then all bets are off, the value could be unset, a global, an enclosing private.

I do think it's problematic if a `@private` overrides the calling scope via an `@apply`. I think maybe the spec handles that via this but it's not clear to me:

> Two [private properties](https://drafts.csswg.org/css-mixins-1/#private-property) with the same name are considered the same property if and only if they have the same nearest [style rule](https://drafts.csswg.org/css-syntax-3/#style-rule) ancestor that has their name in its [private property names](https://drafts.csswg.org/css-mixins-1/#private-property-names).

But clearly (?) the following must be avoided:

```css
@mixin --a() {
  @private { --foo: 10px; }
  padding: var(--foo);
}

div { 
  @private { --foo: 2px; }
  @apply --a();
  margin: var(--foo); /* this must be 2px, not 10px */
}
```

Note, I think the previous model with `@result` also had this kind of "spooky action at a distance" behavior. For example:

```css
@mixin --sub() {
  @result {
    background: var(--bg);
    color: var(--color);
  }
}
@mixin --mix(--bg: blue) {
  --color: var(--bg);
  @result {
    @apply --sub();
  }
}
div { @apply --mix(); /* background: blue; color: blue */
```

[1] Note, it's a little hard to follow the evolution of this feature so I hope this feedback isn't off.

<!-- comment id=5505473051 author=romainmenke created=2026-09-02T06:30:45Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5505473051 -->
## @romainmenke — 2026-09-02

I might also be coming around to it :)
At least if mixins apply as if they are in their own style rule.

So 

```css
div { 
  @apply --a();
}
```

acts somewhat similar to

```css
div { 
  & { @apply --a() }
}
```

This would make private properties visible to the mixin without allowing the mixin to bleed into the private properties of the rule it is being substituted in.

I agree that this example should result in a margin of `2px`

```css
@mixin --a() {
  @private { --foo: 10px; }
  padding: var(--foo);
}

div { 
  @private { --foo: 2px; }
  @apply --a();
  margin: var(--foo); /* this must be 2px, not 10px */
}
```

I think this is fine:

```css
@mixin --a() {
  padding: var(--foo); /* padding is 2px */
}

div { 
  @private { --foo: 2px; }
  @apply --a();
}
```

<!-- comment id=5506499755 author=romainmenke created=2026-09-02T08:07:35Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5506499755 -->
## @romainmenke — 2026-09-02

> > Applying a mixin with `@apply` effectively substitutes the mixin in-place, so a mixin sees any private properties visible to the style rule it’s substituted into (if it doesn’t override them with its own mixin parameters or private properties).
> This seems weird to me. It kinda breaks the entire premise of `@private` making things private.

I think I initially also interpreted the current spec as implying that mixins could influence private properties outside of themselves.

Basically:

```css
@mixin --a() {
  @private { --foo: 10px; }
}

div { 
  @private { --foo: 2px; }
  @apply --a();
  margin: var(--foo); /* influenced by the mixin and now 10px */
}
```

<!-- comment id=5509873901 author=sorvell created=2026-09-02T12:56:16Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5509873901 -->
## @sorvell — 2026-09-02

@romainmenke

> I might also be coming around to it :)
At least if mixins apply as if they are in their own style rule.

> I think I initially also interpreted the current spec as implying that mixins could influence private properties outside of themselves.

Yup, I had the same confusion reading the spec, and I agree that it seems ok if that's clarified *not* to happen, as is likely intended.

<!-- comment id=5546145240 author=tabatkins created=2026-09-04T20:34:41Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5546145240 -->
## @tabatkins — 2026-09-04

The mental model you should be operating on here is that `@mixin` works, as exactly as we can manage, as if you'd just substituted the mixin body in place of the `@apply` rule. You should always be able to extract an existing set of props and rules from a style rule, move it to a mixin and leave an `@apply` in place, and have the behavior remain the same.

We just do a little bit of desugaring to handle arguments (with some implicit private properties; there's an example in the spec), otherwise it's a copy-paste. An arg-less mixin should really be a no-op.

> I think I initially also interpreted the current spec as implying that mixins could influence private properties outside of themselves.

Right, this example **does not** work, because the defined desugaring wraps the mixin result in a "nested declarations rule", so any args/privates defined in the mixin are indeed local to just the mixin's rules. But if you're just manipulating custom properties normally in the mixin body (not ones created by an arg or local private, just implicitly referring to properties from the outside world), then those would see the outside rule's `@private`.

<!-- comment id=5547811886 author=sorvell created=2026-09-04T23:56:47Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5547811886 -->
## @sorvell — 2026-09-04

@tabatkins 

Given this spec text:

> Two [private properties](https://drafts.csswg.org/css-mixins-1/#private-property) with the same name are considered the same property if and only if they have the same nearest [style rule](https://drafts.csswg.org/css-syntax-3/#style-rule) ancestor that has their name in its [private property names](https://drafts.csswg.org/css-mixins-1/#private-property-names).

Example:

```css
@mixin --m() {
  @private --l: 4px;
  padding: var(--l);
}

.r {
  @apply --m();
  --l: 2px;
}
```

Your 2 statements here might conflict so I want to clarify:

> The mental model you should be operating on here is that @mixin works, as exactly as we can manage, as if you'd just substituted the mixin body in place of the @apply rule. 

If that's true, then the spec says (I think) the padding is `2px` since this would be the case if the mixin was inlined.

> Right, this example does not work, because the defined desugaring wraps the mixin result in a "nested declarations rule", so any args/privates defined in the mixin are indeed local to just the mixin's rules. 

If this is true, then the spec says (again, I think) the padding is `4px` since the mixin application is a separate rule.

I hope that's true as `4px` makes sense to me and `2px` does not. If this is the case, can/should we clarify the spec text?

Edit: I do see the spec [already says](https://drafts.csswg.org/css-mixins-1/#mixin-rule):

> The [mixin body](https://drafts.csswg.org/css-mixins-1/#mixin-body) is parsed as if it were a [nested declarations rule](https://drafts.csswg.org/css-nesting-1/#nested-declarations-rule).

It'd help if that's just specifically cited in the [privates section](https://drafts.csswg.org/css-mixins-1/#private).

<!-- comment id=5555760338 author=tabatkins created=2026-09-06T00:22:19Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5555760338 -->
## @tabatkins — 2026-09-06

@sorvell I did say "as much as possible". ^_^ Explicitly using a shadowing variable is one of the things that makes it not possible, yeah.

So yes, it's 4px in that example.

<!-- comment id=5571720267 author=romainmenke created=2026-09-07T13:58:43Z url=https://github.com/w3c/csswg-drafts/issues/14408#issuecomment-5571720267 -->
## @romainmenke — 2026-09-07

> because the defined desugaring wraps the mixin result in a "nested declarations rule"

That is indeed my mental model now :)

Can be closed as answered for my part
