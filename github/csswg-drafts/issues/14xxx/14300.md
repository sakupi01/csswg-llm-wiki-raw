---
number: 14300
title: "[css-values] Percentages and unit arithmetic"
state: "open"
labels: ["Agenda+", "css-values-4"]
author: "emilio"
created_at: "2026-08-09T09:20:03Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14300"
comments: 9
has_resolution: false
synced_at: "2026-08-20T01:50:13Z"
---

So for context, this discussion comes from [this Firefox commit](https://phabricator.services.mozilla.com/D317242).

Consider this:

```css
z-index: calc(10% / 10%);
line-height: calc(10% / 10%);
opacity: calc(10% / 10%);
```

It seems to me these should all behave identically, either computing to `1` (`z-index: 1` / `line-height: 1` / `opacity: 1`), or being rejected?

That doesn't match browser behavior tho. There's some informal notes in https://drafts.csswg.org/css-values-4/#calc-simplification about how percentages generally might block simplification vs. not:

> However, "raw" percentages—​ones which do not resolve against another value, such as in [opacity](https://drafts.csswg.org/css-color-4/#propdef-opacity)—​might not block simplification.

However browsers behave differently here, and it's unclear from the spec what the right behavior is.

Similar weird example would be [this test](https://searchfox.org/firefox-main/rev/b38f633bdaaa555f64abe8095bbf7ad09ac0d80d/testing/web-platform/tests/css/css-values/typed_arithmetic.html#46), effectively `line-height: calc(10% / 1px)`, expecting it to compute to a number:

```html
<!doctype html>
<style>
 body {
   font-size: 10px;
 }
 #target {
   line-height: calc(10% / 1px);
 }
</style>
<div id=target>What's my line height?</div>
<pre><script>
  document.writeln(getComputedStyle(target).lineHeight);
  document.writeln(target.computedStyleMap().get("line-height").toString());
</script></pre>
```

I think making it compute to a number and making the percentage disappear (what Chromium does) is wrong. Because percentage line-heights generally inherit differently...

cc @kbhomes 

<!-- comment id=5233307912 author=tabatkins created=2026-08-09T18:54:03Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5233307912 -->
## @tabatkins — 2026-08-09

* `z-index: calc(10% / 10%);` - invalid (type resolved to `<number> with a hint of "percent"`, which *does not* match `<number>` in this context as %s aren't normally valid; see [Typed OM's "match" algo](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue-match))
* `line-height: calc(10% / 10%);` - valid, equal to 1 (it's a `<number> with a hint of "length"`, which per ["match" algo](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue-match) is okay here, as %s are normally resolved against `<length>` in this context). Simplifies at computed-value time, as %s are resolved at computed-value time against font-size.
* `opacity: calc(10% / 10%);` - valid, equal to 1 (`<number> with a hint of "percent"`, which is valid in this context per ["match" algo](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue-match)). Also simplifies at computed-value time.

> Similar weird example would be [this test](https://searchfox.org/firefox-main/rev/b38f633bdaaa555f64abe8095bbf7ad09ac0d80d/testing/web-platform/tests/css/css-values/typed_arithmetic.html#46), effectively line-height: calc(10% / 1px), expecting it to compute to a number:

Yes, it computes to a number per spec, and thus inherits as a number.

What part of the spec isn't clear here, @emilio ? Happy to fix or clarify.

Edit: I completely got my line-height behavior backwards. Fixed now.

<!-- comment id=5233378647 author=kbhomes created=2026-08-09T19:11:11Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5233378647 -->
## @kbhomes — 2026-08-09

> You're correct, the % can't be resolved at computed-value time, so the calculation remains unresolved and inherits as-is, so the child will see an inherited line-height of `calc(10% / 1px)` as well.

Isn't it resolvable at computed-value time? The percentages are computed against `1em` which is a computed-value time value, right? So I expected this `line-height` to have a computed value of 1, at least from my reading of the specs for `line-height` and `em`. 

<!-- comment id=5233435715 author=kbhomes created=2026-08-09T19:25:06Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5233435715 -->
## @kbhomes — 2026-08-09

> I think making it compute to a number and making the percentage disappear (what Chromium does) is wrong. Because percentage line-heights generally inherit differently...

I think this is where my confusion comes in, which is that my understanding was `line-height` _doesn't_ leave percentages unresolved, and always computes them to a length (whether or not `calc()` is involved). There's a note in the `line-height` definition that says ["The fact that percentages compute to lengths is annoying"](https://drafts.csswg.org/css-inline-3/#preferred-line-height:~:text=The%20fact%20that%20percentages%20compute%20to%20lengths%20is%20annoying), and I think that's what the discussion in #2165 is about, too. And a `line-height: 10%` declaration will result in a computed value in pixels using both `getComputedStyle` and `computedStyleMap` in all browsers. 

So I think a percentage value should be replaced at computed-value time and that Blink's removal of the percentage in that `calc()` expression is expected. It's weird for inheritance, but I think it's what's specified by `line-height`?

<!-- comment id=5233706149 author=tabatkins created=2026-08-09T20:32:31Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5233706149 -->
## @tabatkins — 2026-08-09

> > You're correct, the % can't be resolved at computed-value time, so the calculation remains unresolved and inherits as-is, so the child will see an inherited line-height of `calc(10% / 1px)` as well.
> 
> Isn't it resolvable at computed-value time? The percentages are computed against `1em` which is a computed-value time value, right? So I expected this `line-height` to have a computed value of 1, at least from my reading of the specs for `line-height` and `em`.

Argh sorry I'm being dumb. It's *numbers* that inherit weird, %s resolve at computed-value time, right. Brain fart.

Yes, `line-height: calc(10% / 1px);` will simplify into a number at computed-value time, yes. Then it'll inherit as that number, which is treated as a ratio, producing perhaps-surprising results. I'll go correct my previous post. ^_^

<!-- comment id=5268228675 author=emilio created=2026-08-12T14:32:55Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5268228675 -->
## @emilio — 2026-08-12

@tabatkins I guess the thing I find it weird is that `<number>` parses differently depending on which property it is set on, but...

<!-- comment id=5344750061 author=tabatkins created=2026-08-19T16:05:23Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5344750061 -->
## @tabatkins — 2026-08-19

@emilio This is an old behavior we've had for a while, *specifically* to handle these exact cases, where a % might not make any sense for a given context. %s are always relative to *something* (sometimes an external value so we keep the % as-is in CSS, but that's still relative to something), and if a property doesn't have a *something* to resolve it against, using the % is nonsense. I could try and dig up the issue that caused us to add this behavior originally, if you want.

<!-- comment id=5344801349 author=emilio created=2026-08-19T16:09:08Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5344801349 -->
## @emilio — 2026-08-19

I'd be curious if you can, yeah, because that complicates a bit the typed OM as well right?

<!-- comment id=5344886452 author=tabatkins created=2026-08-19T16:16:33Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5344886452 -->
## @tabatkins — 2026-08-19

I'll go dig it up, but: no, it doesn't complicate TypedOM at all. Building a calculation tree that contains percentages is just fine in all cases, assuming you use your percentages consistently; it's just when you *apply* it to a property that we check if the numeric type is okay with a percent hint existing or not.

(Like, `(10% + 10px) / (10% + 10deg)` is illegal to even construct, because the numerator's type is length with a "length" hint, and the denominator's type is angle with an "angle" hint, and when you divide we "multiply two types" which sees the inconsistent hint and fail the operation. But `10% / 10%` is always valid to construct, with a type of number with "percent" hint, and we'll check if that's actually okay when you apply it to a property and we have grammar knowledge.)

<!-- comment id=5345461814 author=kizu created=2026-08-19T17:07:17Z url=https://github.com/w3c/csswg-drafts/issues/14300#issuecomment-5345461814 -->
## @kizu — 2026-08-19

@tabatkins I think it is unfortunate that `z-index: calc(10% / 10%)` will not work while in other places it will. I imagine authors will stumble upon this eventually.

Also, I guess, that “hint” is not preserved through the registered custom properties?

Doing this currently works in Chrome/Safari (https://codepen.io/editor/kizu/pen/01a01ae6-f22e-7617-8200-bbe67a774b27):

```css
@property --num {
  syntax: "<number>";
  initial-value: 0;
  inherits: true;
}

.test {
  --num: calc(10% / 10%);
  z-index: var(--num);
}
```

This feels a bit weird, although nice that there is at least a workaround.

I think if we still can’t accept the `z-index: calc(10% / 10%)` while the indirection works, could the note in the spec explain why exactly in even more details?
