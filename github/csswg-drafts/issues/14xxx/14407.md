---
number: 14407
title: "[css-mixins-2] how do private properties interact with non-private properties in the same style rule?"
state: "open"
labels: ["css-mixins-2"]
author: "romainmenke"
created_at: "2026-08-29T16:38:23Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14407"
comments: 2
has_resolution: false
synced_at: "2026-09-03T04:21:11Z"
---

https://drafts.csswg.org/css-mixins-1/#private

The spec doesn't currently explain or give examples for using the same name in the same style rule.

For example:

```css
.foo {
	--alpha: public;

	@private {
		--alpha: private;
	}

	--beta: var(--alpha);
}
```

```css
.foo {
	@private {
		--alpha: private;
	}

	--alpha: public;

	--beta: var(--alpha);
}
```

> and any reference to a custom property of that name (for example, with a `var()`) is a reference to the private property.

From that I understand that the non-private property becomes essentially invisible in that style rule.
So `--beta` always has a value of `private`.

----

But `--alpha: public;` would still be present and work perfectly fine.
And any other style rule matching the same element (or descendants) would have a value of `public`.

```css
.foo {
	@private {
		--alpha: private;
	}

	--alpha: public;

	--beta: var(--alpha);
}

.foo {
  --delta: var(--alpha); // `public`
}

.foo * {
  --delta: var(--alpha); // `public`
}
```

Is this correct?

<!-- comment id=5510354179 author=sorvell created=2026-09-02T13:32:20Z url=https://github.com/w3c/csswg-drafts/issues/14407#issuecomment-5510354179 -->
## @sorvell — 2026-09-02

I don't interpret the spec the same way. In particular:

> But --alpha: public; would still be present and work perfectly fine.

Right, but it would set the private `--alpha` available only in that rule.

The value of `--alpha` in another rule without that name in it's private names is the public version of `--alpha` which is not set in your examples.

<!-- comment id=5510460971 author=romainmenke created=2026-09-02T13:40:12Z url=https://github.com/w3c/csswg-drafts/issues/14407#issuecomment-5510460971 -->
## @romainmenke — 2026-09-02

@tabatkins can you clarify?
