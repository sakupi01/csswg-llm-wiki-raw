---
number: 14047
title: "[css-overflow-4] `block-ellipsis`, floats, and line-box clearance"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "Tested", "css-overflow-4", "topic: line-clamp"]
author: "frivoal"
created_at: "2026-06-15T02:14:18Z"
closed_at: "2026-08-11T15:22:22Z"
url: "https://github.com/w3c/csswg-drafts/issues/14047"
comments: 5
has_resolution: true
synced_at: "2026-08-27T09:45:10Z"
---

When placed next to a float, a line whose smallest unwrappable content is to long to fit the remaining space is pushed down. However, `block-ellipsis` (as used by `line-clamp`) can make a line shorter when it removes content to make room for the ellipsis.

When that happens, if the line would now fit next to the float, should it be moved back up next to the float since it now fits?

What should this look like:
```css
div, aside { font-family: monospace; }
aside {
  float: left;
  width: 2ch;
}
div {
  width: 3ch;
  line-clamp: 2;
}
```
```html
<div>
  <aside>
    AB
    CD
  </aside>
  1
  234
  567
</div>
```

Is it:
```
AB1
CD
…
```
or
```
AB1
CD…
```
In https://drafts.csswg.org/css-overflow-4/#block-ellipsis, there are already indication that we want to make sure that removing the content of the last line and inserting the ellipsis doesn't make the block taller, but that we are ok with this making the block shorter. But this particular scenario wasn't considered.

<!-- comment id=4704005112 author=frivoal created=2026-06-15T02:29:47Z url=https://github.com/w3c/csswg-drafts/issues/14047#issuecomment-4704005112 -->
## @frivoal — 2026-06-15

Come to think of it, we may lack a clear answer to the distinct but related problem of whether lines where you insert the ellipsis can move _down_ to clear floats, or must stay in place.
```css
div, aside { font-family: monospace; }
aside {
  float: left;
  width: 2ch;
}
div {
  width: 3ch;
  line-clamp: 2;
}
```
```html
<div>
  <aside>
    AB
    CD
    EF
  </aside>
  1
  2
  3
</div>
```
Which of these two renderings should it be:
```
AB1
CD…
```
or
```
AB1
CD
EF
2…
```
The logic of the first one is that the line where we insert the ellipsis must not increase its vertical position, so you have to remove the `2` to make room for the `…` (and then the remainder of the float gets clipped). The second one is that you can add the `…` next to the `2` as long as you push the line down to clear the float, so do that.

I am not a fan of the idea that `line-clamp` can make things grow taller, so I think I have a strong preference for the first one.

<!-- comment id=5156621986 author=andreubotella created=2026-08-02T08:44:08Z url=https://github.com/w3c/csswg-drafts/issues/14047#issuecomment-5156621986 -->
## @andreubotella — 2026-08-02

> ```
> AB1
> CD
> EF
> 2…
> ```

This is actually one case where, if we removed the ellipsis but kept the layout the same otherwise, the entire contents of the line-clamp container would fit in two lines. So it'd be awkward to clamp like that also for that reason.

<!-- comment id=5203745820 author=css-meeting-bot created=2026-08-06T10:57:55Z url=https://github.com/w3c/csswg-drafts/issues/14047#issuecomment-5203745820 resolution=true -->
## @css-meeting-bot — 2026-08-06

The CSS Working Group just discussed ``[css-overflow-4] `block-ellipsis`, floats, and line-box clearance``, and agreed to the following:

* `RESOLVED: If the ellipsized line would fit back up next to the float, it moves up accordingly`
* `RESOLVED: as line-clamp is interacting with floats, never move a line down that would fit next to a float without the ellipsizing?`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> florian: this issue has subissues, but ther's a main topic and followups<br>
&lt;TabAtkins> florian: mainly about interaction for block-ellipsis and lineboxes on the side of floats and may or may not need clearance<br>
&lt;TabAtkins> florian: if you have a float that is reducing available width for line boxes<br>
&lt;TabAtkins> florian: and you have a linebox that doesn't fit, because it's shortest word is longer than that available width, the line is cleared below the float<br>
&lt;TabAtkins> florian: if that's the line where you place the block ellipsis, and the ellipsis is much shorter, now it *would have* fit next to the float<br>
&lt;TabAtkins> florian: do we keep it down? or move it back up given that it now fits?<br>
&lt;TabAtkins> q+<br>
&lt;TabAtkins> florian: I think the rest of the issues are probably easier, but let's discuss this one now<br>
&lt;TabAtkins> andreubotella: [demo]<br>
&lt;TabAtkins> andreubotella: with line-clamp:2, one behavior is the ellipsis fits here after the float, it gets positioned there, displacing the line<br>
&lt;TabAtkins> andreubotella: another is we put the ellipsis with he line, below the float<br>
&lt;TabAtkins> florian: the main one i'm talking about right now is just the "line would clear without ellipsis, can fit with ellipsis"<br>
&lt;TabAtkins> florian: there's also the other way around where the line does fit, but content+ellipsis doesn't fit, but would if you cleared<br>
&lt;TabAtkins> florian: so sticking with the first case for now<br>
&lt;TabAtkins> florian: shortening the line, do we bring it back up or not<br>
&lt;SebastianZ> q?<br>
&lt;astearns> ack TabAtkins<br>
&lt;emilio> TabAtkins: Given you can line-clamp based on a length<br>
&lt;emilio> ... your line-clamp instead of an int is a length<br>
&lt;emilio> ... the clear length causes that line to get ellipsized, would that matter?<br>
&lt;emilio> ... e.g. if line-clamp would be 2.5em<br>
&lt;emilio> andreubotella: in this example if it's only 2.5lh it'd be better impl wise to look at what's the last line fitting if we're not ellipsizing and work as if we had set liine-clamp to that number  of lines<br>
&lt;emilio> ... e.g for 2.5lh the result would be 2lh but it's fine for the content to be shorter than that<br>
&lt;emilio> ... if we agree that ellipsizing brings you back up<br>
&lt;SebastianZ> q+<br>
&lt;sgill> q+<br>
&lt;emilio> florian: if we agree that ellipsizing brings you back up, do we start pulling up more content?<br>
&lt;emilio> TabAtkins: that's cyclic, so no<br>
&lt;emilio> florian: so we agree on the second part<br>
&lt;emilio> ... but what about the first, would we bring that back up?<br>
&lt;TabAtkins> andreubotella: I think for this case, depending on the impl it might be hard to figure out where the line would be without ellipsizing<br>
&lt;TabAtkins> andreubotella: there are impls where you might lay out the elite without ellipsizing, then ellipsize it<br>
&lt;TabAtkins> andreubotella: but there are also ones where you know the second line will ellipsize so you'll do it as you lay it out originally<br>
&lt;astearns> ack SebastianZ<br>
&lt;TabAtkins> andreubotella: so I'd prefer to not depend on knowledge for where it "will go"; want to bring it up if it fits<br>
&lt;TabAtkins> SebastianZ: clarifying: di dyou set height or line-height on this example?<br>
&lt;TabAtkins> florian: that's the next sub issue<br>
&lt;TabAtkins> SebastianZ: I think moving the ellipsis back up sounds reasonable<br>
&lt;astearns> ack sgill<br>
&lt;TabAtkins> florian: I made that assumption when writing tests, just spec didn't support it so i'm asking<br>
&lt;TabAtkins> sgill: if you don't re-evaluate after ellipsizing, do you get a little more info on the context of that content?<br>
&lt;TabAtkins> sgill: would you know it was something extraordinarily long that didn't fit?<br>
&lt;TabAtkins> sgill: from a user persepctive<br>
&lt;TabAtkins> florian: you woudln't know more text... if you bring it up you know you miss something, if you keep it down you see a little more text and do know it's too long<br>
&lt;TabAtkins> florian: arguably it's some extra knowledge, but dont' think it's useful knowledge<br>
&lt;TabAtkins> astearns: queue is empty, do we resolve that an ellipsize line that fits next to the float moves back up?<br>
&lt;TabAtkins> andreubotella: note that block-ellpisis finds the previous linebreak point, so this only happens when you have a single unbreakable word<br>
&lt;TabAtkins> RESOLVED: If the ellipsized line would fit back up next to the float, it moves up accordingly<br>
&lt;TabAtkins> florian: the other part of the issue is a bit weirder, spec might already give an answer implciitly<br>
&lt;TabAtkins> florian: if your text does fit next to the float, and your text+ellipsis would fit after clearing without removing any additional text, should it clear?<br>
&lt;andreubotella> q+<br>
&lt;TabAtkins> florian: or should you remove text to keep the ellipsis on the same line?<br>
&lt;astearns> ack andreubotella<br>
&lt;TabAtkins> florian: my pref is the line is where it is, if you need to remove more text you remove it. don't search for another place you can move the line just to avoid removing text<br>
&lt;TabAtkins> andreubotella: if we do this, pushing the line down to add ellipsis, the result is taller than not clamping at all<br>
&lt;TabAtkins> andreubotella: so if we're clamping by a height, that's a problem<br>
&lt;TabAtkins> florian: and even by count, the goal is usually to keep it short, to having it get taller seems weird<br>
&lt;TabAtkins> astearns: I can't think of a reason why an author would want the more complex behavior<br>
&lt;TabAtkins> TabAtkins: checking, this isn't about if the ellipsis itself is too long to fit?<br>
&lt;TabAtkins> [discussion]<br>
&lt;TabAtkins> florian: we should word the resolution that we don't move the line down ever<br>
&lt;TabAtkins> andreubotella: we dont' move it down but we can move it up<br>
&lt;TabAtkins> florian: yes<br>
&lt;TabAtkins> florian: if the ellipsis doesn't fit, you clip the ellipsis; if you can't even show a single character you just don't show it. but we never move the line down<br>
&lt;TabAtkins> astearns: so as line-clamp is interacting with floats, never move a line down that would fit next to a float without the ellipsizing?<br>
&lt;TabAtkins> florian: yeah<br>
&lt;TabAtkins> RESOLVED: as line-clamp is interacting with floats, never move a line down that would fit next to a float without the ellipsizing?<br>
</details>

<!-- comment id=5255135268 author=frivoal created=2026-08-11T15:20:10Z url=https://github.com/w3c/csswg-drafts/issues/14047#issuecomment-5255135268 -->
## @frivoal — 2026-08-11

tests in https://github.com/web-platform-tests/wpt/pull/61854 and https://github.com/web-platform-tests/wpt/pull/61859

<!-- comment id=5412976942 author=andreubotella created=2026-08-25T15:46:28Z url=https://github.com/w3c/csswg-drafts/issues/14047#issuecomment-5412976942 -->
## @andreubotella — 2026-08-25

> RESOLVED: as line-clamp is interacting with floats, never move a line down that would fit next to a float without the ellipsizing?

Should this also be the case if the line originally would be smaller than the ellipsis? For example:

```css
.clamp {
  font: 16px Ahem;
  width: 2.5ch;
  line-clamp: 1;
}
.float {
  float: left;
  width: 2ch;
  height: 2ch;
}
.small {
  font-size: 1px;
}
```

```html
<div class=clamp>
  <div class=float></div>
  <span class=small>X</span><br>
  After clamp
</div>
```

The contents of the first line without ellipsizing fit within the available width, but the ellipsis by itself doesn't. So does the ellipsis overflow in this case?
