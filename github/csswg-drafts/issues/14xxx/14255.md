---
number: 14255
title: "[css-forms-1] Apply overflow and wrapping control to select buttons"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-forms-1"]
author: "fantasai"
created_at: "2026-07-31T23:57:27Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14255"
comments: 4
has_resolution: true
synced_at: "2026-08-27T09:45:10Z"
---

Currently if the contents of an option are too long but wrappable, they will wrap onto multiple lines. But this can disturb the general page layout, so option text is typically restricted to one line in a `<select>` control. We should disable wrapping for the base appearance in-page select button text also, to help maintain this height expectation, by moving  `white-space: nowrap` from `option` to `select` itself.

```
select <del>option</del> {
  white-space: nowrap;
}
```

Also, if the text is too long and can’t be wrapped, it will overflow the in-page select button, which looks terrible and can be confusing. We should control overflow by clipping it and using `text-overflow: ellipsis`.

```
select {
  overflow: clip;
}
selectedcontent {
  overflow: clip;
  text-overflow: ellipsis;
  min-inline-size: 0; /* Allow flex shrinking. */
}
```

<hr>

This brings up a related problem: the `selectedcontent` is only styleable when the author provides `<button><selectedcontent></selectedcontent></button>`. This creates some awkward situations for the more basic `<select>` markup pattern, for example:

- We can't use `text-overflow: ellipsis` to control clipping.
- If the option content is too long, it pushes the `::picker-icon` out of the `<select>`'s box, instead of fitting it while clipping the option text.

We should consider either a) automatically generating these elements when they aren't present or b) introducing a pseudo-element (maybe reuse `::field-content`?) that allows them to be styled. This would let us use `flex` on the text rather than `margin-inline-start: auto` on the `::picker-icon`, freeing up the margins to control spacing, and giving us a better layout pattern that we can re-use for `<input>`.

<!-- comment id=5345122288 author=josepharhar created=2026-08-19T16:36:53Z url=https://github.com/w3c/csswg-drafts/issues/14255#issuecomment-5345122288 -->
## @josepharhar — 2026-08-19

I'm supportive of adding a part-like pseudo-element for the [select fallback button text div](https://html.spec.whatwg.org/multipage/rendering.html#select-fallback-button-text).

I'm not sure exactly which styles would achieve this, but I think it would be best if we made the fallback button text div have some sort of clipping so that when the text gets too long, the picker icon stays inside the control and clips the text so that it looks like appearance:auto does here, which might not be possible with customizable select currently:

<img width="342" height="386" alt="Image" src="https://github.com/user-attachments/assets/0931352b-9050-48ff-b811-2b78216a070b" />

<details>
<summary>HTML of screenshot</summary>

```html
<!DOCTYPE html>
<style>
.grid {
  display: grid;
  grid-template-columns: auto 1fr;
  grid-gap: 0.5em;
}
select {
  width: 60px;
}
.base select {
  appearance: base-select;
}
.none select {
  appearance: none;
}
</style>

<h3>appearance:base-select</h3>
<div class="grid base"></div>
<hr>
<h3>appearance:auto</h3>
<div class="grid auto"></div>
<hr>
<h3>appearance:none</h3>
<div class="grid none"></div>

<script>
document.querySelectorAll('.grid').forEach(grid => {
  [null, 'hidden', 'clip', 'visible'].forEach(overflowValue => {
    grid.insertAdjacentHTML('beforeend',
      `<div>${overflowValue ? 'overflow:' + overflowValue : 'default'}</div>
       <div>
         <select style="${overflowValue ? 'overflow:' + overflowValue : ''}">
           <option>loooooooooooooooooong</option>
           <option>short</option>
         </select>
       </div>`);
  });
});
</script>

```

</details>


This was brought up here:
- https://issues.chromium.org/issues/521434907
- https://github.com/whatwg/html/issues/12597

<!-- comment id=5427526780 author=fantasai created=2026-08-26T15:31:52Z url=https://github.com/w3c/csswg-drafts/issues/14255#issuecomment-5427526780 -->
## @fantasai — 2026-08-26

See also https://github.com/w3c/csswg-drafts/issues/14323 where we introduced `::field-content` to wrap the contents when `selectedcontent` isn't there.

<!-- comment id=5427718045 author=css-meeting-bot created=2026-08-26T15:55:43Z url=https://github.com/w3c/csswg-drafts/issues/14255#issuecomment-5427718045 resolution=true -->
## @css-meeting-bot — 2026-08-26

The CSS Working Group just discussed `[css-forms-1] Apply overflow and wrapping control to select buttons`, and agreed to the following:

* `RESOLVED: Move 'white-space: nowrap' from 'select option' to 'select' (not !important)`
* `RESOLVED: select, selectedcontent, select::field-content { overflow: clip; }`
* `RESOLVED: selectedcontent, select::field-content { min-inline-size: 0; }`
* `RESOLVED: selectedcontent, select::field-content { text-overflow: inherit; }`
* `ACTION: Open separate issue wrt select { text-overflow: ellipsis; /* base appearance only */ }`

<details><summary>The full IRC log of that discussion</summary>
&lt;kbabbitt> fantasai: some very nice illustrations from joey that show ... when the text of the select is too long<br>
&lt;kbabbitt> .... it overflows and that looks stupid<br>
&lt;kbabbitt> ... we would like to not do that<br>
&lt;kbabbitt> ... what we're proposing to do is clip it, have both select and selected content apply clipping<br>
&lt;kbabbitt> ... slightly more complicated part is, in current UA stylesheet for select, we set white-space no-wrap on options<br>
&lt;kbabbitt> ... when we put option text into select button it is not styled with option style rules<br>
&lt;kbabbitt> .... we want to delete the option so it continues to not wraP<br>
&lt;kbabbitt> ... we don't want things wrapping in select unless author requested it<br>
&lt;kbabbitt> ... also selected content is kind of flex-y so we want to allow it to shink down to 0 to make room for picker icon<br>
&lt;kbabbitt> ... there are additional complications when selected content does not exist, that's next issue we already resolved by creating pseudo element<br>
&lt;kbabbitt> ... so it's the pseudo we want to style here<br>
&lt;astearns> q?<br>
&lt;kbabbitt> ... several pieces to the proposal here for controlling overflow nicely<br>
&lt;kbabbitt> ... one is white-space no-wrap to selected option<br>
&lt;emilio> q+<br>
&lt;kbabbitt> ... set overflow clip on select button itself and field content<br>
&lt;fantasai> see https://github.com/w3c/csswg-drafts/issues/14323<br>
&lt;kbabbitt> ... pseudo element<br>
&lt;kbabbitt> ... third part is to set min inline size on it to 0 so it can shrink down when there's not space and leave room for picker icon<br>
&lt;kbabbitt> ... there's a 4th part we can talk about later<br>
&lt;kbabbitt> emilio: fwiw Gecko already applies white-space no-wrap on select with important<br>
&lt;kbabbitt> ... fine if we want to remove important for appearance base, maybe<br>
&lt;lwarlow> Generally I think the less !important the better<br>
&lt;kbabbitt> astearns: shall we decide on white-space part first?<br>
&lt;kbabbitt> fantasai: yes let's take 1 at a time<br>
&lt;CameronPsychpsyo> q+<br>
&lt;masonf> 1+<br>
&lt;CameronPsychpsyo> q-<br>
&lt;kbabbitt> astearns: any opinion on whether it should be !important for appearance base?<br>
&lt;miriam> +1<br>
&lt;kbabbitt> ... think 'no' so authors can override it<br>
&lt;lwarlow> Yeah I "veto" important.<br>
&lt;davidjmarland> +1<br>
&lt;masonf> Not sure what 1+ was but I meant to say "no"<br>
&lt;miriam> that it should not be important<br>
&lt;emilio> Sg<br>
&lt;kbabbitt> ... anyone with concerns about applying white-space: no-wrap to select in appearance: base at all?<br>
&lt;fantasai> PROPOSED: Move 'white-space: nowrap' from 'select option' to 'select' (not !important)<br>
&lt;masonf> +1<br>
&lt;lwarlow> +1<br>
&lt;kbabbitt> ... objections to applying that non-important?<br>
&lt;kbabbitt> jarhar: are we also going to do this for appearance auto if web compatible?<br>
&lt;lwarlow> Chrome uses pre for appearance: auto currently<br>
&lt;kbabbitt> emilio: what do you do for... kind of need to on appearance auto right?<br>
&lt;kbabbitt> fantasai: gecko is doing it already, that shows it's web compatible<br>
&lt;kbabbitt> astearns: and hopefully the important part is not needed for web compat<br>
&lt;kbabbitt> emilio: hopefully not<br>
&lt;kbabbitt> lwarlow: is there a big differenc between pre and no-wrap?<br>
&lt;kbabbitt> fantasai: difference is whether white space is preferred<br>
&lt;kbabbitt> ... now that all of this is CSS formatted I don't think we want to preserve whitespasce for indentation etc<br>
&lt;kbabbitt> emilio: right, if option has newline don't want it<br>
&lt;kbabbitt> astearns: calling for objections again to proposed resolution<br>
&lt;CameronPsychpsyo> The fact that it's inherited is a little worrying for web compat<br>
&lt;fantasai> RESOLVED: Move 'white-space: nowrap' from 'select option' to 'select' (not !important)<br>
&lt;fantasai> CameronPsychpsyo, it's already applied to option.<br>
&lt;kbabbitt> astearns: next part<br>
&lt;kbabbitt> fantasai: next part is applying overflow: clip to select button and to selected option<br>
&lt;fantasai> PROPOSED: select, selectedcontent, select::field-content { overflow: clip; }<br>
&lt;kbabbitt> ... reason we apply it to both outer and inner thing is because it looks stupid if either of them overflow<br>
&lt;kbabbitt> ... don't want to overflow button, also don't want to push picker button outside of icon<br>
&lt;emilio> q+<br>
&lt;kbabbitt> ... want to clip all of that<br>
&lt;jarhar> q+<br>
&lt;kbabbitt> astearns: this is only affecting rendering of button once they're in popup right?<br>
&lt;kbabbitt> fantasai: yes<br>
&lt;astearns> ack emilio<br>
&lt;kbabbitt> emilio: think it sounds good but if the text is very long, clipping or not doesn't prevent you from pushing icon away right?<br>
&lt;kbabbitt> fantasai: yes we also have to tweak a few other things but it will help<br>
&lt;astearns> s/popup right/popup there is no effect from this, right/<br>
&lt;kbabbitt> ... because it's a flex item, setting overflow clip makes auto min size 0 which allows it to shrink<br>
&lt;kbabbitt> emilio: that's only for scrollable overflow though<br>
&lt;kbabbitt> fantasai: but picker icon is not going to be shrinkable in that way<br>
&lt;kbabbitt> ... thing that ends up shrinking is going to be this<br>
&lt;kbabbitt> emilio: might also need min inline size<br>
&lt;kbabbitt> astearns: second part is just overflow: clip for these selectors?<br>
&lt;fantasai> PROPOSED: select, selectedcontent, select::field-content { overflow: clip; }<br>
&lt;kbabbitt> fantasai: yeah<br>
&lt;jarhar> q-<br>
&lt;kbabbitt> astearns: and then we figure out how to deal with sizing<br>
&lt;kbabbitt> ... if button is too small to even show the whole icon, does this make the icon clip?<br>
&lt;kbabbitt> fantasai: yes, because the select is clipped<br>
&lt;kbabbitt> astearns: maybe the icon should overflow?<br>
&lt;kbabbitt> fantasai: no, it will always look wrong<br>
&lt;lwarlow> +1<br>
&lt;masonf> +1<br>
&lt;fantasai> RESOLVED: select, selectedcontent, select::field-content { overflow: clip; }<br>
&lt;kbabbitt> lwarlow: we also have min sizing rules so select should remain reasonable size<br>
&lt;kbabbitt> fantasai: next piece, we'll deal with inline size bit<br>
&lt;fantasai> PROPOSED: selectedcontent, select::field-content { min-inline-size: 0; }<br>
&lt;kbabbitt> ... that allows it to shrink down when it's a flex item<br>
&lt;kbabbitt> ... which it is by default<br>
&lt;emilio> +1<br>
&lt;kbabbitt> ... select is display:flex for appearance:base<br>
&lt;kbabbitt> jarhar: the picker icon stays inside, doesn't get clipped?<br>
&lt;lwarlow> +1<br>
&lt;kbabbitt> fantasai: yes<br>
&lt;kbabbitt> astearns: except in those cases where button is too small even for the icon<br>
&lt;kbabbitt> jarhar: not worried about those<br>
&lt;lwarlow> q+<br>
&lt;kbabbitt> lwarlow: question, do these styles need to be conditional for dropdown select or okay for listbox select as well?<br>
&lt;astearns> ack lwarlow<br>
&lt;kbabbitt> fantasai: listbox doesn't show selected content right?<br>
&lt;kbabbitt> emilio: nor field content<br>
&lt;kbabbitt> lwarlow: no but... it does match select<br>
&lt;kbabbitt> [crosstalk]<br>
&lt;kbabbitt> fantasai: listbox is overflow: scroll by default<br>
&lt;kbabbitt> lwarlow: so we don't want it to be clipped?<br>
&lt;kbabbitt> fantasai: yes we should keep listbox as scrollable<br>
&lt;kbabbitt> astearns: is there a change to the selector to restrict it?<br>
&lt;kbabbitt> emilio: needs to be done by prose<br>
&lt;masonf> internal pseudo class<br>
&lt;CameronPsychpsyo> `:not([multiple])`?<br>
&lt;kbabbitt> astearns: if we don't have a selector for this, is there a way for authors to override in a safe way?<br>
&lt;lwarlow> (We should add the selector for this ;) )<br>
&lt;masonf> you can have a single select listbox<br>
&lt;kbabbitt> fantasai: yes, set what you want, none of this is !important. this is done internally with a pseudoclass so it's just a UA stylesheet rule<br>
&lt;kbabbitt> emilio: an author doing a design system and dealing with these edge cases ... there are reasonable selectors to use but the ones that follow HTML parsing rules for attrs, that's the hard thing<br>
&lt;kbabbitt> ... we're not introducing a new problem here<br>
&lt;ntim> fantasai: is flex-basis: 0 better than min-width: 0 ?<br>
&lt;ntim> (i don't know just asking)<br>
&lt;kbabbitt> ... could use something like :is multiple[size>1]   (??)<br>
&lt;fantasai> ntim, no it'll still floor at the minimum size<br>
&lt;kbabbitt> astearns: not entirely convinced but not too worried either<br>
&lt;emilio> select:is([multiple], [size]:not([size="1"])) or so<br>
&lt;kbabbitt> ... ntim had a question on IRC about flex-basis instead of min-width?<br>
&lt;kbabbitt> fantasai: no wouldn't work, min size still takes effect<br>
&lt;kbabbitt> ... what we're trying to do is 0 out the automatic minimum<br>
&lt;lwarlow> +1 to this given the listbox clarification<br>
&lt;ntim> There's an issue for adding the listbox selector. It's not currently expressible using attribute selectors<br>
&lt;fantasai> RESOLVED: selectedcontent, select::field-content { min-inline-size: 0; }<br>
&lt;kbabbitt> astearns: proposed resolution is add min inline size to selectedcontent, select::field-content<br>
&lt;kbabbitt> fantasai: last bit in this issue is about applying text-overflow: ellipsis to selected content<br>
&lt;kbabbitt> ... makes clipping effect less abrupt when it happens<br>
&lt;kbabbitt> astearns: on the other hand it's showing less info inside this constrained button<br>
&lt;kbabbitt> ... would prefer not to ellipsize but not a strong opinion<br>
&lt;kbabbitt> ... other ideas?<br>
&lt;kbabbitt> jarhar: we want ellipses when clipping?<br>
&lt;kbabbitt> fantasai: yes for inner element<br>
&lt;fantasai> PROPOSED: selectedcontent, select::field-content { text-overflow: ellipsis; }<br>
&lt;kbabbitt> ... what would happen is, as the text starts to get clipped, still show picker icon but text of option would get an ellipsis<br>
&lt;kbabbitt> jarhar: we're not doing that for appearance:auto right now, not that any browser has done it for appearance:auto right now because maybe it's magic<br>
&lt;lwarlow> q+<br>
&lt;kbabbitt> ... a little hesitant because we're not doing it<br>
&lt;astearns> ack lwarlow<br>
&lt;kbabbitt> lwarlow: for appearance:auto isn't this only a problem for field sizing content? doesn't select size on largest option?<br>
&lt;kbabbitt> fantasai: not for appearance:base, that's another issue that will be quite complicated to solve<br>
&lt;kbabbitt> ... ideally it would size itself<br>
&lt;jarhar> q+<br>
&lt;kbabbitt> ... right now it sizes itself based on the currently selected option<br>
&lt;kbabbitt> ... which also means it's not clipped by default<br>
&lt;kbabbitt> ... so you only get clipping if you constrained size of select which you might do if you have really long options<br>
&lt;kbabbitt> lwarlow: can you do that in appearance:auto?<br>
&lt;kbabbitt> fantasai: yes, width propert?<br>
&lt;kbabbitt> lwarlow: i forget how constrained sizing is by default?<br>
&lt;kbabbitt> ... does the ... end up removing .... you end up losing what would give you most of context<br>
&lt;kbabbitt> fantasai: that's the downside, you lose some content to make room for ellipsis<br>
&lt;kbabbitt> lwarlow: don't mind either way<br>
&lt;astearns> ack jarhar<br>
&lt;emilio> q+<br>
&lt;davidjmarland> q+<br>
&lt;kbabbitt> jarhar: to lwarlow's point, this is something that happens, people setting on select element, started overflowing instead of clipping, people were very unhappy<br>
&lt;kbabbitt> ... doing clip with no ellipses there, not sure authors would care<br>
&lt;kbabbitt> fantasai: ok<br>
&lt;astearns> ack emilio<br>
&lt;kbabbitt> emilio: setting text-overflow: ellipsis on appearance: auto select works now<br>
&lt;kbabbitt> ... should we just set, instead of text-overflow: ellipsis, set text-overflow: inherit<br>
&lt;kbabbitt> ... that way it works in both cases<br>
&lt;kbabbitt> ... can leave as opt-in for author to set text-overflow on selecty<br>
&lt;kbabbitt> ... for option it's trickier since they don't inherit from select directly<br>
&lt;kbabbitt> ... two places it matters would work<br>
&lt;emilio> data:text/html,&lt;select style="width: 100px; text-overflow: ellipsis;">&lt;option>abcdefabcdefabcdefabcdef&lt;/select> for example<br>
&lt;kbabbitt> ... selectedcontent and field content would inherit from select<br>
&lt;kbabbitt> fantasai: so what you're proposing is to not set text-overflow: ellipsis on select, but make it so if you do it works?<br>
&lt;kbabbitt> emilio: yes<br>
&lt;kbabbitt> fantasai: that could be interesting<br>
&lt;kbabbitt> ... if we don't want to set it by default ourselves, makes sense to me<br>
&lt;masonf> +1 to not doing it by default but making it work if the developer does it<br>
&lt;kbabbitt> emilio: that's how it works in Gecko, I think Blink supports this as well?<br>
&lt;kbabbitt> astearns: alternatively we could wait and see whether developers do this and then decide to add this inherit rule<br>
&lt;masonf> explanation: magic<br>
&lt;kbabbitt> emilio: don't know how you'd explain how it works otherwise in appearance:auto<br>
&lt;kbabbitt> ... more divergence we introduce between the two the harder it gets to explain<br>
&lt;kbabbitt> ... if we can explain it with inheritance that seems nice<br>
&lt;fantasai> https://software.hixie.ch/utilities/js/live-dom-viewer/?%3C!DOCTYPE%20html%3E%0A%3Cstyle%3Eselect%20%7B%20text-overflow%3A%20ellipsis%3B%20%7D%3C%2Fstyle%3E%0A%3Cselect%20style%3D%22width%3A%205em%22%3E%3Coption%3ELoooooong%20option%3C%2Fselect%3E%0A%3Cselect%20style%3D%22width%3A%205em%3B%20appearance%3A%20none%22%3E%3Coption%3ELoooooong%20option%3C%2Fselect%3E%0A%3Cselect%20style%3D%22width%3A%205em%3B%20a<br>
&lt;fantasai> ppearance%3A%20base-select%22%3E%3Coption%3ELoooooong%20option%3C%2Fselect%3E<br>
&lt;fantasai> https://software.hixie.ch/utilities/js/live-dom-viewer/saved/14847<br>
&lt;jarhar> q+<br>
&lt;kbabbitt> fantasai: webkit doesn't do that currently but suspect we'd be happy to align with Firefox<br>
&lt;astearns> ack davidjmarland<br>
&lt;kbabbitt> davidjmarland: behavior I see quite often people do, they make first option a placeholder e.g. "Please select..."<br>
&lt;kbabbitt> ... could that cause some conflict?<br>
&lt;kbabbitt> fantasai: if "Please select" doesn't fit it will get ellipsized at an earlier point<br>
&lt;emilio> This is how it works in Gecko FWIW: https://searchfox.org/firefox-main/rev/2aa0bcf95c98681376e30d6a9aa4df2b27885abc/layout/style/res/forms.css#656,659<br>
&lt;kbabbitt> ... if they're using . . . rather than actual ellipsis and actual ellipsis is smaller such that it's only two dots, you could get four dots...<br>
&lt;kbabbitt> lwarlow: they should be using an ellipsis character<br>
&lt;emilio> ::-moz-select-content is basically select::field-content<br>
&lt;kbabbitt> fantasai: might remove ellipsis to add ellipsis but would work the same<br>
&lt;lwarlow> Also they should probably be sizing stuff correctly<br>
&lt;emilio> q+<br>
&lt;astearns> ack jarhar<br>
&lt;fantasai> PROPOSED: selectedcontent, select::field-content { text-overflow: inherit; }<br>
&lt;kbabbitt> jarhar: setting text-overflow works in Chrome because we manually inherit style from select element to div warpping text<br>
&lt;kbabbitt> emilio: which is the field content right, so it happens in C++ instead of CSS?<br>
&lt;kbabbitt> jarhar: yes, would be happy to change it<br>
&lt;kbabbitt> ... looking up equivalent, feel like these look fine<br>
&lt;kbabbitt> ... probably okay with ellipses for base appearance but would ideally not change for auto<br>
&lt;kbabbitt> emilio: we could say text overflow ellipsis on select for base appearance and then it works?<br>
&lt;kbabbitt> jarhar: sounds good<br>
&lt;astearns> ack emilio<br>
&lt;fantasai> PROPOSED: selectedcontent, select::field-content { text-overflow: inherit; unicode-bidi: inherit; }<br>
&lt;kbabbitt> emilio: I pasted some links on how Gecko does it, we inherit unicode-bidi and text-overflow on equivalent to fieldcontent<br>
&lt;kbabbitt> ... don't have context on unicode-bidi, do have context on other<br>
&lt;kbabbitt> fantasai: [thinking]<br>
&lt;kbabbitt> astearns: let's make unicoide-bidi a separate issue if needed<br>
&lt;fantasai> PROPOSED: selectedcontent, select::field-content { text-overflow: inherit; }<br>
&lt;kbabbitt> astearns: proposed resolution is (what's in IRF)<br>
&lt;lwarlow> +1<br>
&lt;kbabbitt> s/IRF/IRC/<br>
&lt;fantasai> RESOLVED: selectedcontent, select::field-content { text-overflow: inherit; }<br>
&lt;kbabbitt> fantasai: last part I heard, wasn't clear we want to or not ...<br>
&lt;fantasai> PROPOSED: select { text-overflow: ellipsis; /* base appearance only */ }<br>
&lt;emilio> I'm ambivalent about htat<br>
&lt;emilio> s/htat/that<br>
&lt;lwarlow> I'm agnostic about that<br>
&lt;kbabbitt> astearns: I would like that to be a separate issue with more input on it<br>
&lt;kbabbitt> ... we have a bunch of people who are ambivalent, would rather not make a decision we have to reverse<br>
&lt;fantasai> ACTION: Open separate issue wrt select { text-overflow: ellipsis; /* base appearance only */ }<br>
</details>

<!-- comment id=5427739999 author=josepharhar created=2026-08-26T15:58:18Z url=https://github.com/w3c/csswg-drafts/issues/14255#issuecomment-5427739999 -->
## @josepharhar — 2026-08-26

For posterity, it sounds like this set of rules should achieve the behavior of appearance:auto in the screenshot in my last comment, where the text gets clipped and the picker icon does not get clipped and everything stays inside the button without overflowing, right?
