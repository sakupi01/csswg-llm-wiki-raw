---
number: 14447
title: "[css-link-params] Passing `currentColor` (and other state) without requiring the receiving SVG to have a param"
state: "open"
labels: ["Closed Accepted by Editor Discretion", "css-link-params-1"]
author: "LeaVerou"
created_at: "2026-09-04T18:42:10Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14447"
comments: 14
has_resolution: false
synced_at: "2026-09-25T09:10:06Z"
---

We had discussed this earlier, but I don't see it in the spec and didn't find any other issues.

If we expect icon libraries to add `--color` params, it will be a very steep adoption curve.
We should try to support *at least* the types of customizations that are already possible from the outside without a double handshake so that once this ships, it can be used with existing icons out of the box.
Propagating `currentColor` being the top one.

cc @tabatkins 

<!-- comment id=5546620591 author=tabatkins created=2026-09-04T21:24:54Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5546620591 -->
## @tabatkins — 2026-09-04

Good point. I think we can just special-case some keywords to have this behavior as `param()` args. Since we already require your actual params to be dashed-ident, this is compatible.

That is, something like `img { link-parameters: param(currentcolor); }`, which would resolve currentcolor fully then pass it down into the image, *using it as the initial value of `color`*. (And probably `env(currentcolor)` too, just for consistency? It's possible that not all values we want to give this treatment are suitable for exposing as the initial value of some existing property.)

These would have to be a special allowlist of keywords with specific behaviors, but I think it's a good idea. `accent-color` would be another one. If you have ideas on what all would be good, that would be a helpful start.

We could actually still use the "value" arg of `param()` too, to let you optionally set a specific value (rather than using the value from the element).

<!-- comment id=5560732083 author=LeaVerou created=2026-09-06T16:55:51Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5560732083 -->
## @LeaVerou — 2026-09-06

Yes, setting a specific value is *definitely* a requirement. `currentColor` is a good default, but often icons are a different color than the text (e.g. less visually prominent, or accented etc). Note that it should either be `currentColor` and `accentColor` (i.e. the keywords) OR `color` and `accent-color` (i.e. the properties). Passing properties is _by_ far more understandable and more flexible, so big +1 for the latter.

In terms of other properties that could be useful, offhand:
- `fill`
- `stroke` and `stroke-*`
- `transform`

But `color` is definitely the 80% case.

`font-family` could be a huge one if we can pass resolved font-families from the outer context, since it's currently a big pain point that it's impossible to use web fonts from SVGs (e.g. excalidraw base64 encodes an entire webfont in their SVG exports…). But that's definitely a separate issue, and not worth complicating the base case for.

<!-- comment id=5624795231 author=tabatkins created=2026-09-10T20:11:07Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5624795231 -->
## @tabatkins — 2026-09-10

Hm, how do you see fill/stroke working? They don't have dedicated keywords in SVG, they just use `currentcolor` or `transparent`, and you rarely want to actually set the fill/stroke on the root, I think. Any files not explicitly written to use a link param would either be relying on currentcolor (so we can inject it via that keyword) or setting the values explicitly (so nothing we do will help; the pages need to be rewritten slightly).

Similarly, I'm not sure what `transform` would do *at all*. Can you give an example of what an icon could even *do* with the knowledge of what its `img` element's transform is?

<!-- comment id=5625195480 author=tabatkins created=2026-09-10T20:45:09Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5625195480 -->
## @tabatkins — 2026-09-10

For now I've gone ahead and just added 'color' and 'accent-color'. Since these take from the corresponding property for their default value, it just makes sense to go ahead and integrate your other request to make --foo also take from the corresponding property by default, so I've done so.

<!-- comment id=5682602486 author=fantasai created=2026-09-15T15:03:24Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5682602486 -->
## @fantasai — 2026-09-15

This... is a pretty significant change, actually, so while I agree with it I think it does need a WG resolution.

Tab's proposal is (afaict):
- Params take their value from the correspondingly-named property by default.
- `color` and `accent-color` are allowed, in addition to custom properties, and set the initial value of those properties on the sub-document.

Open question from my perspective: should these set the initial value, or should they only affect the root?

E.g. if I set `color: blue` on an SVG element and `color: initial` on its child, what color is the child: CanvasText or whatever got passed into the document as the `color` parameter? CC @anttijk @emilio 

<!-- comment id=5682791692 author=emilio created=2026-09-15T15:14:43Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5682791692 -->
## @emilio — 2026-09-15

Strong preference for not messing with the behavior of `initial` / etc, for what is worth.

Setting the initial value seems unfortunate and also somewhat limiting? Can't we make `env(color)` / `env(accent-color)` (or something along those lines) work? That seems like it'd be more consistent?

<!-- comment id=5688570937 author=tabatkins created=2026-09-15T21:49:57Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5688570937 -->
## @tabatkins — 2026-09-15

Lol I was just coming in to reopen this again anyway.

Two things:

1. Re: @emilio , if all we do is have it set `env(color)`/etc, that doesn't satisfy Lea's original use-case. It *does* still allow for some convenience on the *passing* side - `param(color)` is easier to write than `param(--color <color>, currentcolor)` - which I think is still quite useful. (and see #14476 where the convenience can be even larger, with one `param(font)` setting a suite of `env()`s on the linked resource)
2. Adjusting the initial value actually defeats the handshake security property that I built into the spec, where a linked resource *must* be using the param explicitly for it to have any effect at all. It probably makes sense to preserve this aspect, so I think we *do* want to just have this set `env(color)`/etc.

<!-- comment id=5798522447 author=css-meeting-bot created=2026-09-23T16:22:37Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5798522447 -->
## @css-meeting-bot — 2026-09-23

The CSS Working Group just discussed ``[css-link-params] Passing `currentColor` (and other state) without requiring the receiving SVG to have a param``.

<details><summary>The full IRC log of that discussion</summary>
&lt;ydaniv> TabAtkins: the spec has values in src, mainly in SVG, there's a bit of interest adding things like currentColor<br>
&lt;TabAtkins> param(--color &lt;color>, currentcolor)<br>
&lt;ydaniv> ... now spec allows you to do the bove ^<br>
&lt;JoshT> s/bove/above/<br>
&lt;TabAtkins> param(color)<br>
&lt;ydaniv> ... which allows you to hook up the color with currentColor<br>
&lt;ydaniv> ... suggestion is to allow grabbing the color and exposing it to the linked doc<br>
&lt;ydaniv> ... spec is currently allows you to do it for some properties<br>
&lt;kbabbitt> q+<br>
&lt;ydaniv> ... so by default, anything that doesn't touches the color property will get that automatically<br>
&lt;ydaniv> ... however, emilio has an objection on changing initial value, and doing it this way defeats security boundary, also in the spec<br>
&lt;ydaniv> ... by using the env of given name<br>
&lt;kbabbitt> q-<br>
&lt;ydaniv> ... so proposal is to define some pairs of named vars and the effect of this special params set to env variables<br>
&lt;ydaniv> ... probably take initial value by default<br>
&lt;emilio> q_<br>
&lt;emilio> q+<br>
&lt;ydaniv> ... my preference is to do that, add the special params syntax which allows these, like color and currentColor<br>
&lt;ydaniv> ... and have well defined names to do that<br>
&lt;ydaniv> ... and maintain a security boundary<br>
&lt;astearns> ack emilio<br>
&lt;ydaniv> emilio: I think that's fine, no strong opinion on naming, just that accent color works with outer accent color<br>
&lt;ydaniv> ... avoids the issue of when overiding accent-color property you can't get the accentColor value<br>
&lt;ydaniv> ... since we resolved on accentColor keyword to resolving to what accent-color was set<br>
&lt;lea> +1<br>
&lt;lea> oops, sorry, I meant to type q+<br>
&lt;lea> q+<br>
&lt;ydaniv> ... another thing is, do we want to expose the accentColorText?<br>
&lt;ydaniv> ... could be up for debate<br>
&lt;ydaniv> TabAtkins: I think accent-color doesn't allow you to pass 2 colors?<br>
&lt;ydaniv> emilio: no<br>
&lt;emilio> env(accent-color) + env(accent-color-text) or so<br>
&lt;ydaniv> TabAtkins: then agree, if you use parm(accentColor) it should provide both on the other side<br>
&lt;ydaniv> ... similarly with fonts passing, get the family on the other side<br>
&lt;emilio> ack lea<br>
&lt;ydaniv> lea: IIUC, you use the env() to pass these values?<br>
&lt;ydaniv> TabAtkins: yea<br>
&lt;emilio> `:root { color: env(color, initial); }` or so<br>
&lt;ydaniv> lea: how do we avoid every icon library passing every new stuff?<br>
&lt;ydaniv> ... plus this is something authors can do today, so feels like a degredation<br>
&lt;ydaniv> ... of using icons today without params<br>
&lt;emilio> q+<br>
&lt;ydaniv> ... for other customization sure, but not for things they can do today<br>
&lt;ydaniv> TabAtkins: disagree that it's already settled, it's already hand-shaking that is used<br>
&lt;ydaniv> lea: no, if it's a black icon they can mask it over any color<br>
&lt;ydaniv> TabAtkins: that's a lesser problem, since you use over the entire icon<br>
&lt;ydaniv> ... so still different sort of problematic case<br>
&lt;ydaniv> ... you can change the color of entire image, but changing specific part introduces issues<br>
&lt;ydaniv> ... now you can change 1 color, and that can change how the image look in significant way<br>
&lt;ydaniv> lea: so we're back to square 1, where library has to ship all values<br>
&lt;fantasai> s/all values/a new version/<br>
&lt;ydaniv> TabAtkins: you still get the convinience of shorter params<br>
&lt;ydaniv> lea: very minor benefit, every icon needs to be adapted<br>
&lt;lea> s/where library has to ship all values/where a library must ship a new version to work with link-parameters/<br>
&lt;ydaniv> TabAtkins: yes, but that was an unintentional security issue<br>
&lt;ydaniv> ... currently you control the source, so you drop a new version which includes the variables<br>
&lt;ydaniv> lea: it's a big thing going into every icon and changing it<br>
&lt;ydaniv> astearns: we may have to take back to the issue<br>
&lt;astearns> ack fantasai<br>
&lt;lea> fwiw I'm not opposed to this change. I just don't think it addresses 99% of the problem.<br>
&lt;ydaniv> fantasai: want to support lea's concerns. doesn't solve if we just give some specific keywords<br>
&lt;lea> icons are black boxes to authors. They either copy them or use them from a package. They don't actually author SVG.<br>
&lt;lea> And often they are hidden behind abstractions like React components.<br>
&lt;ydaniv> ... what is the problem with being able to set the color on a specific parts of the SVG?<br>
&lt;JoshT> +1<br>
&lt;lea> +1 fantasai<br>
&lt;ydaniv> ... makes sense for arbitrary, but for color which is the most important should be straightforward<br>
&lt;ydaniv> TabAtkins: not member of security team, so would like to take that back with tham<br>
&lt;emilio> `:root { color: env(color); }<br>
&lt;ydaniv> emilio: could do something like the above ^<br>
&lt;ydaniv> ... better with messing with the initial value<br>
&lt;lea> q?<br>
&lt;lea> q+<br>
&lt;ydaniv> ... most icons I've seen they have a hash coloring<br>
&lt;ydaniv> ... is there any library that does this problematic behavior?<br>
&lt;ydaniv> astearns: not seeing concensus today, so let's take it back to the issue<br>
&lt;emilio> s/does this problematic behavior/would start working with the UA proposal<br>
&lt;ydaniv> TabAtkins: would like to preserve the changes, and add issue about how solving these specific problems<br>
&lt;ydaniv> astearns: anyone would prefer to revert?<br>
&lt;fantasai> s/changes/changes to allow the color+accent-color names, but revert the part about them setting values automatically/<br>
&lt;ydaniv> fantasai: TabAtkins proposes to revert<br>
&lt;emilio> ack emilio<br>
&lt;fantasai> s/revert/partially revert/<br>
&lt;ydaniv> astearns: taking back to the issue and going to more input from security<br>
&lt;astearns> ack lea<br>
</details>

<!-- comment id=5798564345 author=mirisuzanne created=2026-09-23T16:25:26Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5798564345 -->
## @mirisuzanne — 2026-09-23

To answer @emilio's final question in the call - here's a default icon download from Font Awesome, which does not set a fill value of its own. That's what I expect as an author when using an icon font library. The icons should inherit and use my current color by default. 

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 640"><!--!Font Awesome Free 7.3.1 by @fontawesome - https://fontawesome.com License - https://fontawesome.com/license/free Copyright 2026 Fonticons, Inc.--><path d="M32 400C32 479.5 96.5 544 176 544L480 544C550.7 544 608 486.7 608 416C608 364.4 577.5 319.9 533.5 299.7C540.2 286.6 544 271.7 544 256C544 203 501 160 448 160C430.3 160 413.8 164.8 399.6 173.1C375.5 127.3 327.4 96 272 96C192.5 96 128 160.5 128 240C128 248 128.7 255.9 129.9 263.5C73 282.7 32 336.6 32 400z"/></svg>
```

<!-- comment id=5798609251 author=LeaVerou created=2026-09-23T16:28:28Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5798609251 -->
## @LeaVerou — 2026-09-23

Icons are black boxes to authors. They either copy them or use them from a package. They don't actually author SVG by hand, like, almost ever. Often they are even hidden behind abstractions like React components. E.g. https://www.npmjs.com/package/lucide-react is the 9th most used package by websites today. With this, this is how authors use a camera icon:

```js
import { Camera } from 'lucide-react';
// Usage
const App = () => {
  return <Camera />;
};
export default App;
```

Please explain to me how authors would do any of the things you're suggesting in this case. 

Realistically, this would mean every single icon library needs to adapt every icon to conditionally use linked parameters if they are available. So it is now up to the individual icon libraries to adopt this, rather than the individual authors, which makes the adoption curve _very_ steep (and every single icon larger…).

<!-- comment id=5798675341 author=emilio created=2026-09-23T16:32:45Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5798675341 -->
## @emilio — 2026-09-23

@mirisuzanne Cool, good to know that there are icons that do that.

FWIW, those already doesn't work e.g. on dark mode today, right? Do folks using that rely on masking or so?

For monochrome icons, I'd expect icons to have an `<style>:root { color-scheme: light dark; color: env(color); }</style>` or so. That doesn't seem like too much to ask for?

My main problem with doing that auto-magically is that this _only_ works for monochrome icons. Anything else can get subtly broken instead.

<!-- comment id=5798937924 author=nullhook created=2026-09-23T16:50:09Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5798937924 -->
## @nullhook — 2026-09-23

> Icons are black boxes to authors. They either copy them or use them from a package. They don't actually author SVG by hand, like, almost ever. Often they are even hidden behind abstractions like React components. E.g. https://www.npmjs.com/package/lucide-react is the 9th most used package by websites today. With this, this is how authors use a camera icon:
> 
> import { Camera } from 'lucide-react';
> // Usage
> const App = () => {
>   return <Camera />;
> };
> export default App;
> Please explain to me how authors would do any of the things you're suggesting in this case.
> 
> Realistically, this would mean every single icon library needs to adapt every icon to conditionally use linked parameters if they are available. So it is now up to the individual icon libraries to adopt this, rather than the individual authors, which makes the adoption curve _very_ steep (and every single icon larger…).

This is right in principle but in this example `<Camera />` is an inline SVG, not an img tag. Most libraries like this out there will bundle standalone SVG files and emit inline SVG.

The use-case you're describing here applies to authors consuming libraries via third party CDNs.

<!-- comment id=5799970561 author=mirisuzanne created=2026-09-23T17:48:18Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5799970561 -->
## @mirisuzanne — 2026-09-23

> FWIW, those already doesn't work e.g. on dark mode today, right? Do folks using that rely on masking or so?

@emilio it's pretty common for a site to include `fill: currentcolor;` usually on the `svg` selector – which solves the dark mode issue with requiring the library to do anything. 

<!-- comment id=5800103881 author=mirisuzanne created=2026-09-23T17:57:30Z url=https://github.com/w3c/csswg-drafts/issues/14447#issuecomment-5800103881 -->
## @mirisuzanne — 2026-09-23

> For monochrome icons, I'd expect icons to have an `<style>:root { color-scheme: light dark; color: env(color); }</style>` or so. That doesn't seem like too much to ask for?

I would consider it pretty risky for any icon library to style `:root`. If you embed that icon code in html, you're suddenly styling the page root instead of the icon root. And since svg styles are not from the shadow-context or scoped by default, svg styles can be extremely risky.

Ideally, this feature means I can choose between embedding or linking the svg – but currently there's a good reason library authors are not providing default styles.
