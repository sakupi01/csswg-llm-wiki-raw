---
number: 14289
title: "[selectors] Relax `|=` to also match value parts separated by spaces"
state: "open"
labels: ["selectors-5"]
author: "SebastianZ"
created_at: "2026-08-06T13:41:43Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/14289"
comments: 3
has_resolution: false
synced_at: "2026-08-09T13:23:14Z"
---

There's a common use case to match class names with prefixes. To handle this, authors very often use the pipe equals attribute selector (`|=`). So they use selectors like `[class|="prefix"]`. Though that selector was mainly created for matching `lang` attribute values. So using this operator means the value can either be exactly the given value or can begin with the value immediately followed by a hyphen, e.g. `en`, `en-US`, `en-GB`, etc. I.e. `[class|="prefix"]` does _not_ match, in case the class is somewhere in the middle, e.g. `class="first prefix-second"`.

Due to that restriction, authors instead sometimes fall back to some combination of `[class^="prefix-"], [class*=" prefix-"]`. Though that's rather hacky for the given use case.

Therefore, in #10001 the suggestion came up to relax `|=` to also match after spaces, meaning it would work as before but also match prefixes on whitespace-separated values.

Note that this loosening up of the matching is a little different to the wildcard solution resolved on in #10001 in that it also matches values that are not followed by a dash when they exactly match the given value.
So `[class|="prefix]` would match `class="prefix"`, `class="prefix-foo"`, and `class="foo prefix-bar"` (but not `class="prefixfoo"`), while the wildcard solution (currently) requires the value to be hyphenated.

This of course requires web compatibility, which seems to be given from the research that was done so far, see https://github.com/w3c/csswg-drafts/issues/10001#issuecomment-2589079152 and following comments.

Sebastian

<!-- comment id=5217745170 author=kizu created=2026-08-07T13:36:28Z url=https://github.com/w3c/csswg-drafts/issues/14289#issuecomment-5217745170 -->
## @kizu — 2026-08-07

In theory, this sounds good, but yes, there is a pretty likely chance of interop issues: just looking for `[class|=` in GH search results in quite many results: https://github.com/search?q=%22%5Bclass%7C%3D%22+language%3ACSS&type=code&l=CSS — unknown how any will actually lead to any visible changes, but given some previously unmatched selectors would activate, chances are not zero.

If that won't be possible due to compat, I think we should still find a way to get this, with some new attribute selector.

Another reason to introduce a new attribute selector could be feature-detection, as otherwise the only way to do it would be through introducing a new named supports feature.

<!-- comment id=5226588543 author=Crissov created=2026-08-08T14:41:26Z url=https://github.com/w3c/csswg-drafts/issues/14289#issuecomment-5226588543 -->
## @Crissov — 2026-08-08

If a new [attribute value selector](https://drafts.csswg.org/selectors/#attribute-representation) would be necessary, it should be one of these, probably:

- `[class|~=prefix]`

- `[class~|=prefix]`

- `[class|=~prefix]`

- `[class~=|prefix]`

- `[class|~prefix]`

- `[class~|prefix]`

- `[class~prefix]`

- ~~`[class|prefix]`~~ clashes with [namespace syntax](https://drafts.csswg.org/selectors/#attrnmsp), i.e. matches `<foo class:prefix="bar"/>`

However, to accommodate more authoring conventions, a brand new attribute value selector could split the value at *any* character commonly used for separation (e.g. `-`, `_`, `,`, ` ` (whitespace)) and match any of the resulting components (i.e. prefix`!`, infix`*`, postfix`?`).

<!-- comment id=5228783157 author=bradkemper created=2026-08-08T23:38:58Z url=https://github.com/w3c/csswg-drafts/issues/14289#issuecomment-5228783157 -->
## @bradkemper — 2026-08-08

We could also reverse the order of the two characters in `|=`, to `=|` to give it new meaning. It could mean that the last character of the term is the joiner, and would match against space-separated terms, so that this...
```
[class=|"foo!"] {}
```
...would match `class="foo bar"` and `class="baz foo!bar"`, etc. but not `class="foo-bar"` or `class="foobar"`
