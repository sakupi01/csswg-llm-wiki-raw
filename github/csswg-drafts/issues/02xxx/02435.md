---
number: 2435
title: "[css-animations] Serialize all keyframe names as strings"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-animations-1"]
author: "csnardi"
created_at: "2018-03-13T00:43:09Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/2435"
comments: 25
has_resolution: true
synced_at: "2026-07-04T04:45:37Z"
---

After #118 and #801, support for naming a keyframe the same as a reserved keyword by using a `<string>` was added to parsing and CSSOM setting. [The WPT test](https://github.com/w3c/web-platform-tests/blob/master/css/cssom/CSSKeyframesRule.html#L59-L69) for this part of the spec seems to assume that if a keyword could be parsed as an `<ident>`, then it would be serialized as a string (with quotation marks); otherwise, no quotation marks would be used. However, this seems needlessly complex; wouldn't it be easiest just to serialize all keyframe names as strings? That way, it would be ensured that re-parsing the output would generate an identical rule, without having to check if the name is an `<ident>`.

<!-- comment id=424202009 author=fantasai created=2018-09-25T04:16:12Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-424202009 -->
## @fantasai — 2018-09-25

This is likely Web-incompatible, since Firefox and Blink currently return bare identifiers in getComputedStyle.
It's also inconsistent with the principle that CSS-internal user-chosen names use identifiers and strings are only used when interfacing with an external system.

I think what would make more sense is to exclude the CSS-wide keywords and 'none' from being animation names, as the `<custom-ident>` type recommends. We could still parse in strings as required by https://github.com/w3c/csswg-drafts/issues/118 but they would compute to `<custom-ident>` and any that match an excluded keyword would be invalid. This is effectively what we do for line names in CSS Grid, for example.

<!-- comment id=428646888 author=css-meeting-bot created=2018-10-10T16:49:31Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-428646888 resolution=true -->
## @css-meeting-bot — 2018-10-10

The CSS Working Group just discussed `Serialize all keyframe names as strings`, and agreed to the following:

* `RESOLVED: serialize all keyframes as identifiers`

<details><summary>The full IRC log of that discussion</summary>
&lt;dael> Topic: Serialize all keyframe names as strings<br>
&lt;dael> github: https://github.com/w3c/csswg-drafts/issues/2435<br>
&lt;fantasai> https://github.com/w3c/csswg-drafts/issues/2435<br>
&lt;dael> fantasai: Question about...as we were going through c omputed values do we need to preserve a distinction between animation names that are strings and ones that are identifies<br>
&lt;dael> fantasai: You can use either for keyframes<br>
&lt;dael> fantasai: Main issue afaict is we can't serialize things as strings. They have to fundimentally be identifiers. One case we can't treat them to compute as the same there's CSS-wide identifiers like 'none' where if you wanted to name your animation that you can't refer to it in the property<br>
&lt;dael> fantasai: I don't think it's an issue.<br>
&lt;dael> fantasai: Nice if we didn't have to maintain the difference<br>
&lt;dael> myles_: Web compat risk?<br>
&lt;Rossen_> 'auto'<br>
&lt;dael> fantasai: If someone relies o n web animation name as 'initial' 'inherit' or 'none' yes. Otherwise no.<br>
&lt;dael> Rossen_: Any other concerns, comments, suggestions?<br>
&lt;dael> Rossen_: Objections?<br>
&lt;dael> fantasai: Proposal is serialize all keyframes as identifiers<br>
&lt;dael> Rossen_: Objections?<br>
&lt;dael> RESOLVED: serialize all keyframes as identifiers<br>
&lt;dael> dbaron: Somethinggs wouldn't round trip?<br>
&lt;dael> fantasai: CSS wide things. I propose we make them invalid and that's why they don't round trip.<br>
&lt;dael> dbaron: Is someone volunteering to change first?<br>
&lt;dael> Rossen_: I don't hear any. Are you dbaron ?<br>
&lt;dael> dbaron: Not particularly<br>
&lt;dael> fantasai: I believe current behavior was a recent change. 2016.<br>
&lt;dael> fantasai: Before 2016 you could not have a keyframe name that is a string. That was a webcompat problem because webkit supported a string in that position as animation name. We changed to allow strings or idents but decided to keep them distinct. Only reason is if you have 'initial' or one of those values.<br>
&lt;dael> fantasai: It's r eally about will wemaintain that if you put in a string or an ident both end up as an ident<br>
&lt;dael> fremy: We still don't support strings in Edge. If someone is using 'none' they're doubly failing. I don't feel like making the change is end  of the  world<br>
&lt;dael> dbaron: Other question; are you proposing this for values of prop, @keyframes or both<br>
&lt;fantasai> https://github.com/w3c/csswg-drafts/issues/118<br>
&lt;dael> fantasai: Anywhere accepting a string as an animation name.<br>
&lt;dael> fantasai: Proposal is everywhere issue #﻿118 says we can accept a string we treat it at parse time as an ident<br>
&lt;dael> myles_: Do we need a rule for idents that aren't 'auto' and the list?<br>
&lt;dael> fantasai: Have that rule already.<br>
&lt;fantasai> https://www.w3.org/TR/css-values-3/#custom-idents<br>
&lt;dael> fremy: Yes, because you need it for font-family<br>
&lt;dbaron> font-family is extra-weird<br>
&lt;dael> Rossen_: Still not hearing reason to change resolution<br>
</details>

<!-- comment id=445630241 author=ewilligers created=2018-12-10T02:18:24Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-445630241 -->
## @ewilligers — 2018-12-10

Do we need a more precise resolution before updating the spec and tests? I gather from a [PR discussion](https://github.com/w3c/csswg-drafts/pull/3296) that we need to serialize as strings whenever we do not have a valid animation-name custom identifier (e.g. `'"Initial"'`, `'"None"'`, `'" x "'`). Also, do we want to ban the empty string `'""'` ?

<!-- comment id=1269352890 author=yisibl created=2022-10-06T05:43:33Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-1269352890 -->
## @yisibl — 2022-10-06

Is there any progress here?

<!-- comment id=3423832719 author=javifernandez created=2025-10-20T21:41:06Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3423832719 -->
## @javifernandez — 2025-10-20

Is the CSSWG resolution mentioned https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-428646888 still valid ? 

I've seen that WebKit has [updated the implementation](https://github.com/WebKit/WebKit/pull/38484) to match the current spec, which is what Firefox already implements; Chrome diverges, though. 

<!-- comment id=3424043173 author=javifernandez created=2025-10-20T23:04:55Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3424043173 -->
## @javifernandez — 2025-10-20

@fantasai is the PR #12985 enough to cover the requested edits ? Id so, perhaps we can close this issue.

<!-- comment id=3424780781 author=cdoublev created=2025-10-21T05:44:38Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3424780781 -->
## @cdoublev — 2025-10-21

I am confused. :worried:

Quoting the new text:

  > The `<string>` should be treated as `<custom-ident>`, hence parsed as invalid if is one of the CSS-wide keywords or "default"

This is not what I understand from the above resolution, tests on WPT, and output in Firefox.

`"<css-wide-keyword>"`, `"default"`, `"none"`, `"1"`, `"not a custom ident"`, etc... are all valid on WPT and in Firefox, which serializes them as an identifier when possible. `normal` and `auto` are valid. `none` is invalid.

(edit) `""` (empty `<string>`) is also invalid in Firefox, which is not tested on WPT and was not specified before.

<!-- comment id=3425129440 author=javifernandez created=2025-10-21T07:22:19Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3425129440 -->
## @javifernandez — 2025-10-21

> This is not what I understand from the above resolution, tests on WPT, and output in Firefox.

I the discussion log this was the proposal made before the resolution:

> fantasai: Proposal is everywhere issue #﻿118 says we can accept a string we treat it at parse time as an ident

And then in the discussion held in https://github.com/w3c/csswg-drafts/pull/3296#issuecomment-437718506 it was mentioned again: 

> if ```<string>``` is one of the CSS-wide keywords or "default" then it is invalid (at parse / variable-resolution time) as per the rules for custom-idents: https://www.w3.org/TR/css-values-3/#custom-idents

It's true that the WPT have not been updated yet, but I think the spec should be updated first. 

<!-- comment id=3425259010 author=cdoublev created=2025-10-21T07:52:39Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3425259010 -->
## @cdoublev — 2025-10-21

  > It's true that the WPT have not been updated yet, but I think the spec should be updated first.

I assume that Webkit and Firefox should change their implementation, and there is no back-compat issue.

If `<string>` value must match `<custom-ident>`, when does it need to serialize as `<string>`?

<!-- comment id=3425331773 author=javifernandez created=2025-10-21T08:10:05Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3425331773 -->
## @javifernandez — 2025-10-21

> > It's true that the WPT have not been updated yet, but I think the spec should be updated first.
> 
> I assume that Webkit and Firefox should change their implementation, and there is no back-compat issue.

Yes, it was my understanding that they agreed on the CSSWG resolution and this interpretation. Regarding the web compat issue, it's been mentioned during the discussion as well:

> myles_: Web compat risk?
> Rossen_> 'auto'
> fantasai: If someone relies o n web animation name as 'initial' 'inherit' or 'none' yes. Otherwise no.

I don't think using a css-wide keyword as an animation identifier is a common use case.

> 
> If `<string>` value must match `<custom-ident>`, when does it need to serialize as `<string>`?

As far as I have understood, only quoted strings (eg, "a\ custom\ ident")

<!-- comment id=3426788936 author=nt1m created=2025-10-21T13:50:18Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3426788936 -->
## @nt1m — 2025-10-21

That is not how I interpreted the resolution fwiw. Nothing in the resolution says that `<string>` should get custom-ident restrictions. The resolution only says to serialize to strings to the extent possible?

It's not a hard change to make either way, but it's rather a waste of time and unnecessary compat risk to change 2 implementations as opposed to one. It wouldn't be unreasonable for a website to name their animation "default" fwiw.

<!-- comment id=3427244801 author=javifernandez created=2025-10-21T15:23:25Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3427244801 -->
## @javifernandez — 2025-10-21

>  It wouldn't be unreasonable for a website to name their animation "default" fwiw.

I think the resolution tried to make invalid the use of "inherit" as valid animation-name, or "unset". Given that were invalid as custom-ident, it'd made sense to consider those strings as invalid. 

Additionally, if those strings are going to be serialized as identifiers, wouldn't be confusing to see the computed value as "inherit" ? just the subtle different of the use of "" to differentiate the reserved keyword. 

In any case, I think it's understandable to match what 2 implementors already have, but at least we should be able to get a new agreement now. 

<!-- comment id=3437332984 author=javifernandez created=2025-10-23T14:27:55Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3437332984 -->
## @javifernandez — 2025-10-23

> That is not how I interpreted the resolution fwiw. Nothing in the resolution says that `<string>` should get custom-ident restrictions. The resolution only says to serialize to strings to the extent possible?

You meant "serialize to identifiers, to the extent possible" right ? 

<!-- comment id=3437878116 author=nt1m created=2025-10-23T16:11:01Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3437878116 -->
## @nt1m — 2025-10-23

> > That is not how I interpreted the resolution fwiw. Nothing in the resolution says that `<string>` should get custom-ident restrictions. The resolution only says to serialize to strings to the extent possible?
> 
> You meant "serialize to identifiers, to the extent possible" right ?

Yes. If it's not possible, it should serialize as a string

<!-- comment id=3438167022 author=javifernandez created=2025-10-23T17:11:59Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3438167022 -->
## @javifernandez — 2025-10-23

BTW, it seems that both Firefox and Safari accept CSS-wide keywords as valid, serialized as identifiers, although the computed value is 'none'. This is not spec compliant, right ? 

<!-- comment id=3438184534 author=nt1m created=2025-10-23T17:16:01Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3438184534 -->
## @nt1m — 2025-10-23

> BTW, it seems that both Firefox and Safari accept CSS-wide keywords as valid, serialized as identifiers, although the computed value is 'none'. This is not spec compliant, right ?

That sounds like a bug

<!-- comment id=3438889913 author=javifernandez created=2025-10-23T19:58:09Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3438889913 -->
## @javifernandez — 2025-10-23

> > BTW, it seems that both Firefox and Safari accept CSS-wide keywords as valid, serialized as identifiers, although the computed value is 'none'. This is not spec compliant, right ?
> 
> That sounds like a bug

Nah, nevermid, css-wide keywords are always accepted, although ignored if they don't apply to the specific property. 
I think it's all clear now. Thanks !

<!-- comment id=3467413545 author=yisibl created=2025-10-30T11:08:35Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3467413545 -->
## @yisibl — 2025-10-30

@javifernandez So all of these are valid, right?

```
animation-name: "default";
animation-name: "none";
animation-name: "initial";
animation-name: "inherit";
animation-name: "unset";
animation-name: "revert";
animation-name: "revert-layer";
```

<!-- comment id=3489944078 author=javifernandez created=2025-11-05T08:37:24Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3489944078 -->
## @javifernandez — 2025-11-05

> [@javifernandez](https://github.com/javifernandez) So all of these are valid, right?
> 
> ```
> animation-name: "default";
> animation-name: "none";
> animation-name: "initial";
> animation-name: "inherit";
> animation-name: "unset";
> animation-name: "revert";
> animation-name: "revert-layer";
> ```


Yes, that's my understanding now; and what's defined in the current WPT test cases.

<!-- comment id=3609185880 author=javifernandez created=2025-12-03T22:56:59Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3609185880 -->
## @javifernandez — 2025-12-03

I think all the browser are interoperable now, and no need to change the spec at all, so I believe we can close this issue now.

The only related pending issue would be perhaps #7762, which already has PRs for the spec and the WPT. 

<!-- comment id=3799761471 author=cdoublev created=2026-01-26T14:00:01Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3799761471 -->
## @cdoublev — 2026-01-26

  > I think all the browser are interoperable now [...]

This seems true only for `animation-name`. Keyframe names also exist in `@keyframes`'s prelude, and are serialized in `CSSKeyframesRule.cssText` and `CSSKeyframesRule.name`.

Based from my observations in [this CodePen](https://codepen.io/creativewave/full/ByjYYrN):

  - Chrome serializes the `<string>` value as an identifier in `@keyframes`, **even if it is an excluded identifier**, and as is in `CSSKeyframesRule.name`
  - Firefox serializes the `<string>` value as an identifier in `@keyframes`, except if it is an excluded identifier, and as is in `CSSKeyframesRule.name`
  - Chrome and Firefox accept any value for `CSSKeyframesRule.name` and serializes it as specified
  - Firefox replaces `""` with `none` in `animation-name` and `@keyframes`, and serializes it as the empty string in `CSSKeyframesRule.name`

The non-conforming behaviors with `""` and `"none"` are unrelated to this issue.

For what is worth, these are the WPT tests related to this issue:

  1. [`animation-name`](https://github.com/web-platform-tests/wpt/blob/master/css/css-animations/parsing/animation-name-valid.html)
  2. [`@keyframes`](https://github.com/web-platform-tests/wpt/blob/master/css/css-animations/parsing/keyframes-name-valid.html)
  3. [`CSSKeyframesRule.name`](https://github.com/web-platform-tests/wpt/blob/master/css/cssom/CSSKeyframesRule.html)

(1) seems complete.

(2) does not test CSS-wide keywords, `default`, and `none`.

(3) does not test `<string>` with a CSS-wide keyword, `default`, or `none` as its value, or a value that is not an identifier or `<string>`.

<!-- comment id=3994226502 author=tabatkins created=2026-03-03T23:34:25Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-3994226502 -->
## @tabatkins — 2026-03-03

I'm not clear at this point what our resolution is, and what, if anything needs edits (specs and/or tests).

<!-- comment id=4000946384 author=javifernandez created=2026-03-04T23:27:27Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-4000946384 -->
## @javifernandez — 2026-03-04

> I'm not clear at this point what our resolution is, and what, if anything needs edits (specs and/or tests).

My understanding is that the [resolution](https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-428646888) means that the value of the 'animation-name' should be serialized as 'identifies' when possible, and as ```<string>``` otherwise. IMO, there is no need to touch the spec, which [states](https://drafts.csswg.org/css-animations/#at-ruledef-keyframes) as follows:

> When serialized, the value is serialized as an [`<ident>`](https://drafts.csswg.org/css-values-4/#typedef-ident) unless it’s a disallowed keyword, in which case it’s serialized as a `<string>`.

This is how Firefox and Safari interpreted the resolution mentioned before, so they adapted their implementation accordingly. I have recently implemented the same behavior in Chrome. 

However, the last comment mentions some lack of interop in other usages of the ```@keyframe```  value and some missing test cases. As far as I could understand, no spec change is needed. 

<!-- comment id=4002216888 author=cdoublev created=2026-03-05T05:03:30Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-4002216888 -->
## @cdoublev — 2026-03-05

  > My understanding is that the resolution the [resolution](https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-428646888) means that the value of the 'animation-name' should be serialized as 'identifier' when possible, and as `<string>` otherwise

This resolution is *"serialize **all keyframes** as identifiers"*, not just in `animation-name`. And the requirement to serialize *"as 'identifier' when possible, and as `<string>` otherwise"* is currently specified for `<keyframes-name>`, which is not only used in `animation-name`, but also in other places. 

In these other places, browsers do not always serialize as 'identifier' when possible. So authors could complain to these browsers that their serialization is non-conforming, if the spec does not define how `<keyframes-name>` must serialize in these other places.

I am sorry if I misunderstood this resolution.

<!-- comment id=4128067883 author=css-meeting-bot created=2026-03-25T16:37:45Z url=https://github.com/w3c/csswg-drafts/issues/2435#issuecomment-4128067883 resolution=true -->
## @css-meeting-bot — 2026-03-25

The CSS Working Group just discussed `[css-animations] Serialize all keyframe names as strings`, and agreed to the following:

* `RESOLVED: No change to spec. Allowed keyframe names that can serialize as idents do so, and all others (requiring escapes or disallowed keywords like 'initial') serialize as strings.`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> TabAtkins: Some discussions awhile back about exactly how keyframe names get serialized<br>
&lt;fantasai> TabAtkins: whether varies depends on keyframe name being an ident or some CSS-wide reserved keyword or something<br>
&lt;fantasai> TabAtkins: We had a resolution, and then backed that out<br>
&lt;fantasai> TabAtkins: I believe that browsers currently have interop on current spec<br>
&lt;fantasai> TabAtkins: which is that we serialize to a keyword if it doesn't require an escape and not a CSS-wide keyword<br>
&lt;fantasai> TabAtkins: If they need escapes or would match a disallowed keyword, we serialize as a string<br>
&lt;fantasai> TabAtkins: So I think we can close this issue as no change<br>
&lt;fantasai> RESOLVED: No change to spec. Allowed keyframe names that can serialize as idents do so, and all others (requiring escapes or disallowed keywords like 'initial') serialize as strings.<br>
</details>
