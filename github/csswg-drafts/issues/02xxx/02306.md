---
number: 2306
title: "[cssom][css-escape][css-selectors]: Using CSS.escape with nth-child"
state: "closed"
labels: ["Closed Rejected as Invalid"]
author: "jridgewell"
created_at: "2018-02-13T00:06:51Z"
closed_at: "2018-02-14T01:35:25Z"
url: "https://github.com/w3c/csswg-drafts/issues/2306"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:49:11Z"
---

We've just gone though our codebase to enforce using `CSS.escape` on any dynamic parts of a selector. The only caveat we've found so far is with `:nth-child` and friends, due to the very limited set of parsing of [An+b](https://drafts.csswg.org/css-syntax-3/#anb-microsyntax)

```js
// Works fine
querySelector(`div.${CSS.escape(3)}`);

// Throws parse error
querySelector(`div:nth-child(${CSS.escape(3)})`);
```

Is `CSS.escape` meant to handle this situation? Or, should the An+b syntax be loosened to allow escaped numbers?

<!-- comment id=365394810 author=Loirooriol created=2018-02-13T20:32:20Z url=https://github.com/w3c/csswg-drafts/issues/2306#issuecomment-365394810 -->
## @Loirooriol — 2018-02-13

`CSS.escape` is not a magical function. It just serializes a string as a CSS identifier. `:nth-child` does not expect an identifier argument so it won't work.

I would just do something like

```js
a = a >> 0; // ToInt32(a)
b = b >> 0; // ToInt32(b)
querySelector(`div:nth-child(${a}n ${b < 0 ? "-" : "+"} ${Math.abs(b)})`);
```
