---
number: 2987
title: "containment probably shouldn't apply to most SVG elements"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "SVG", "Tracked in DoC", "css-contain-1"]
author: "dbaron"
created_at: "2018-08-03T17:48:31Z"
closed_at: "2018-09-28T02:27:37Z"
url: "https://github.com/w3c/csswg-drafts/issues/2987"
comments: 11
has_resolution: true
synced_at: "2026-07-04T04:53:28Z"
---

layout, size, and paint containment probably shouldn't apply to SVG elements other than `svg` and `foreignObject`.  Currently the spec excludes elements that "do not generate a principal box"... but I don't actually see anything in the relevant definitions that says that these SVG elements don't generate a principal box.

I think it should be the case that they don't, and therefore that layout, size, and paint containment don't apply, but this ought to be defined.  (It's also not 100% clear about things like outer `svg`, `svg` within an SVG structure, and `foreignObject`.)

<!-- comment id=410374314 author=tabatkins created=2018-08-03T20:57:09Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410374314 -->
## @tabatkins — 2018-08-03

At the moment SVG elements don't generate any boxes at all, as they're not defined to interact with the CSS box model in any way, besides the outermost SVG in an HTML document.

<!-- comment id=410375111 author=dbaron created=2018-08-03T21:00:37Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410375111 -->
## @dbaron — 2018-08-03

What about a `foreignObject` containing HTML?  I'd think that has a box, and `contain: paint` could apply to it.

<!-- comment id=410376841 author=tabatkins created=2018-08-03T21:08:14Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410376841 -->
## @tabatkins — 2018-08-03

The `<foreignObject>` still doesn't, it just provides an ICB to the contained `<html>` element, which *does* generate a box.

<!-- comment id=410385899 author=AmeliaBR created=2018-08-03T21:53:30Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410385899 -->
## @AmeliaBR — 2018-08-03

> The `<foreignObject>` still doesn't, it just provides an ICB to the contained `<html>` element, which does generate a box.

You can't have an `<html>` or `<body>` inside a `<foreignObject>` in inline SVG parsed by the HTML parser.

Ideally, the foreignObject would provide a fixed-height containing block (most browsers do _not_ currently treat it as if it had a fixed height, so you can't set `height: 100%` on a child) for all child CSS boxes, but I'm not sure whether it is necessary to define it as a CSS box.

Basically, a `foreignObject` is a CSS box in the sense of having an inner display type (flow-root), but it does not have an outer display type.

More generally, see @tabatkins's [comment on the filter / containing block issue](https://github.com/w3c/fxtf-drafts/issues/307#issuecomment-410373975):

> The proper resolution to this boils down to "actually define how SVG operates in the CSS box model finally", correct?

<!-- comment id=410388088 author=AmeliaBR created=2018-08-03T22:03:12Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410388088 -->
## @AmeliaBR — 2018-08-03

Focusing on the original issue, [containment](https://drafts.csswg.org/css-contain/):

The `<svg>` element that defines an SVG canvas (that is, root element or a direct child of a non-SVG-namespaced element), _does_ have an outer CSS layout box, and so `contain: paint` would be useful. These always currently `contain: size` in practice, although that could change if we ever add auto-calculated `viewBox` based on the content bounding box. Pending resolution on proper behavior of positioned elements inside foreignObject, such an element would also contain layout indirectly, although it would be the foreignObject that did the actual containing.

For other SVG elements on which [`overflow` applies](https://svgwg.org/svg2-draft/render.html#OverflowAndClipProperties), much the same points could be made, and so containment could make sense there, too.

For other SVG elements, which don't have boxes other than those defined by their contents or drawing commands, containment doesn't make sense without a lot of special definitions..

<!-- comment id=410809409 author=dbaron created=2018-08-06T18:38:15Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-410809409 -->
## @dbaron — 2018-08-06

So it seems like the right thing to implement is that containment should apply to outer `<svg>` but nothing else in SVG.

(Note that for containment to make sense, it needs to be clear what it means for an element to establish a containing block for absolutely positioned and fixed positioned elements; see w3c/fxtf-drafts#307.)

<!-- comment id=419020400 author=frivoal created=2018-09-06T09:03:31Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-419020400 -->
## @frivoal — 2018-09-06

@dbaron and @AmeliaBR , you seem to be in agreement. Could one of you propose a phrasing to be added to the spec?

Agenda+ to try and get closure on this.

<!-- comment id=420710732 author=css-meeting-bot created=2018-09-12T16:22:37Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-420710732 resolution=true -->
## @css-meeting-bot — 2018-09-12

The CSS Working Group just discussed `containment probably shouldn't apply to most SVG elements`, and agreed to the following:

* `RESOLVED: containment should apply to outer SVG but nothing else in SVG`

<details><summary>The full IRC log of that discussion</summary>
&lt;dael> Topic: containment probably shouldn't apply to most SVG elements<br>
&lt;dael> github: https://github.com/w3c/csswg-drafts/issues/2987<br>
&lt;dael> florian: Looking to raise attention and get a consentual answer. Last issue on containment.<br>
&lt;dael> florian: Seemed dbaron and ameliabr were getting somewhere<br>
&lt;dael> chrishtr: Agree with dbaron on outer svg and nothing else<br>
&lt;dael> florian: Can we say that or need to be more specific?<br>
&lt;dael> dbaron: I think applying to outer SVG is pretty straight forward<br>
&lt;dael> astearns: Don't have ameliabr on the call. Shall we resolve on outer SVG and ask Amelia?<br>
&lt;dael> chris: I think there's agreement on outer SVG. And it's what's outside foreign object. TabAtkins is correct foreign object est containing block but that's all it does.<br>
&lt;dael> Rossen_: eq. of iframe basically<br>
&lt;dael> astearns: Prop: containment should apply to outer SVG but nothing else in SVG<br>
&lt;dael> astearns: Obj?<br>
&lt;chris> +1 to proposed resolution<br>
&lt;dael> RESOLVED: containment should apply to outer SVG but nothing else in SVG<br>
&lt;dael> astearns: I'll tag ameliabr for review<br>
&lt;dael> florian: I expect next call to have spec ready for CR. Thanks to Gerard for the test suite<br>
</details>

<!-- comment id=420710900 author=astearns created=2018-09-12T16:23:10Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-420710900 -->
## @astearns — 2018-09-12

@AmeliaBR please let us know if you're good with the resolution above

<!-- comment id=420716107 author=AmeliaBR created=2018-09-12T16:39:34Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-420716107 -->
## @AmeliaBR — 2018-09-12

Sorry I missed the discussion.

I agree that the "outer" SVG should support the containment properties. The wording should be something like "an `<svg>` element that has an associated CSS layout box" to be consistent with other specs.

As selectors, that looks like:

``` css
@namespace svgns url(http://www.w3.org/2000/svg);

svg:root, /* root svg, starts an SVG canvas */
:not(svgns|*) > svg, /* svg that is a child of a non-SVG namespace element, also starts a canvas */
foreignObject > svg /* a new SVG canvas inside a foreignObject */
{ /* containment works */}
```

I still think it would be perfectly sensible for `<foreignObject>` and other elements that support `overflow: hidden` to also support containment, but I also think that's something that could be left undefined/unsupported for now.

<!-- comment id=425303107 author=frivoal created=2018-09-28T02:42:43Z url=https://github.com/w3c/csswg-drafts/issues/2987#issuecomment-425303107 -->
## @frivoal — 2018-09-28

Actually, I'm removing the "needs testcase" label here, as there isn't really anything to test. We just defined that the property does not apply to elements where it wouldn't be clear what applying means... It doesn't really seem meaningful to try and test for the lack of effect. If anyone catches a browser accidentally doing something when it should do nothing, we can add tests to wpt to make sure that doesn't happen again, but until then I think no test is needed for this.
