---
number: 2414
title: "[css-shadow-parts] IDL for attributes"
state: "open"
labels: ["css-shadow-parts-1", "topic: shadow"]
author: "fergald"
created_at: "2018-03-07T05:03:48Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/2414"
comments: 27
has_resolution: false
synced_at: "2026-07-04T04:49:42Z"
---

As discussed at Web Components F2F (2018-03, Tokyo) and in [TAG issue](https://github.com/w3ctag/design-reviews/issues/230#issuecomment-370644319) update [spec](https://drafts.csswg.org/css-shadow-parts/) to include IDL for part, partmap, theme to allow programmatic API.

<!-- comment id=371025073 author=domenic created=2018-03-07T05:12:42Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-371025073 -->
## @domenic — 2018-03-07

Since these are supposed to be "superglobal", the best precedent is things like https://dom.spec.whatwg.org/#dom-element-id

<!-- comment id=376403895 author=fergald created=2018-03-27T05:43:08Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-376403895 -->
## @fergald — 2018-03-27

Posting discussion about programmatic from #2411 to here.

@fergald
> > In my model the partmap is a dictionary where the values are Set()s of part names. So deleting would be partmap['a'].delete('b') and to remove "a" entirely, delete partmap['a'] (although partmap['a'].clear() would have the same effect).

> > What's the tradeoff between a dictionary of sets and something with an explicit API. Are there operations that dictionaries/sets allow that we wouldn't want or conversely are there operations dictionaries/sets don't allow that we would want and couldn't add later without breaking compatibility? I don't really want to write up an API for something that behaves essentially as a dictionary of sets if I can avoid it.

@rniwa
> Well, browser engines can't observe changes to a dictionary so your proposal as currently stands will make partmap readonly, and any changes to it will be discarded. It's even weirder because it has [SameObject] on it. It means the browser engine has to update the dictionary continuously? I don't think it works because then author scripts can override property descriptors of a dictionary, and all sorts of insanity ensues.

<!-- comment id=404556554 author=domenic created=2018-07-12T15:40:53Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-404556554 -->
## @domenic — 2018-07-12

I would strongly suggest just exposing these as strings in IDL for now. There is no need for a more complicated data structure, at least until authors ask for one.

We have precedent for microsyntaxes being reflected as simple strings in at least `<img srcset>`, `<iframe csp>`, `<iframe allow>`, and `<link sizes>`.

We can always add something more complex later, depending on how we find developers are using these things, e.g. feature policy is looking at a *read-only* API that gives a view onto the `allow` attribute. But for now, no need.

<!-- comment id=404558016 author=domenic created=2018-07-12T15:45:18Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-404558016 -->
## @domenic — 2018-07-12

Concretely, and with regard to the feedback in https://github.com/w3c/csswg-drafts/issues/2411#issuecomment-377768803, I think this means:

- Say "content attribute" in most (all?) places you current say "attribute". You can omit it if it's quite obvious, e.g. I think "the [part] attribute" is fine as long as [part] links to the `<dfn>` which will say "The **part** content attribute".
- Add the IDL, roughly `partial interface Element { [CEReactions] DOMString part; }` (and partmap).
- Define the IDL attribute [like the ID attribute is defined in DOM](https://dom.spec.whatwg.org/#dom-element-id), including the correct "reflect" link. Unlike DOM, you can use a proper link for the last part of the sentence, instead of just monospace font.

<!-- comment id=405446990 author=rniwa created=2018-07-17T03:19:11Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405446990 -->
## @rniwa — 2018-07-17

If we're exposing it simply as a string, we're probably better off not having IDL attribute at all. Just use good old `setAttribute` / `getAttribute` for now.

<!-- comment id=405449794 author=domenic created=2018-07-17T03:39:35Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405449794 -->
## @domenic — 2018-07-17

Well, that'd be inconsistent with the rest of the platform, and cause problems for developers or frameworks used to having omnipresent attribute/property reflection. So I think following the example of srcset, csp, allow, sizes, etc. (which all have simple string reflection) is probably the right path.

<!-- comment id=405451541 author=rniwa created=2018-07-17T03:53:30Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405451541 -->
## @rniwa — 2018-07-17

Then I'd argue that we should be adding IDL attribute which reflects the fact `partmap` is a map. I'm sorry but there is no consensus here.

<!-- comment id=405583715 author=domenic created=2018-07-17T13:34:44Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405583715 -->
## @domenic — 2018-07-17

Why do you not argue the same for the other attributes I list?

Note that we have no precedent in the platform for exposing a map-microsyntax as map-like IDL attribute.

<!-- comment id=405797417 author=rniwa created=2018-07-18T03:27:45Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405797417 -->
## @rniwa — 2018-07-18

I'd argue that those attributes should have had a better IDL attribute but that's somewhat of an orthogonal discussion to have. Anyhow, I think here's an idea on how these map-like IDL might work. Introduce a new interface like `DOMTokenMap` then have `stringifier` return the actual string. The IDL attribute would have `PutsForwards` to forward the assignment to `value` property of `DOMTokenMap`. Then `DOMTokenMap` can have named properties to set and get mapped values.

<!-- comment id=405800715 author=domenic created=2018-07-18T03:52:47Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405800715 -->
## @domenic — 2018-07-18

I think we should wait for web developers to ask for a new structured attribute manipulation technology, and gather their use cases in a way that services all such attributes, instead of designing one ourselves independently. Right now we know web developers have the use case of working with property-based frameworks like React, for which string reflection is preferable (e.g. they use className, not classList, and if we only reflected classList, React would be unable to use classes). We should wait for a similar ask for structured reflection before adding it, which we can easily do additionally.

Also note that named properties are deprecated in IDL.

<!-- comment id=405811049 author=rniwa created=2018-07-18T05:12:05Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-405811049 -->
## @rniwa — 2018-07-18

I don't know what to say other than that I disagree with your proposition that we'd have to wait for developer feedback to define a sensible IDL attribute.

I think the only path forward I can see here is to avoid adding any IDL attribute at all since that's the least common denominator we can all agree on, and doesn't block us from adding the feature and/or adding new IDL attribute in the future as needs come up.

<!-- comment id=409987946 author=annevk created=2018-08-02T16:30:20Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-409987946 -->
## @annevk — 2018-08-02

@rniwa defining a new object, such as `DOMTokenMap` is a lot of effort. It's taken us years to get `DOMTokenList` (actually a set) in a somewhat reasonable state.

So typically we don't start out with those, as they require quite a lot of work. We do start out with simple IDL attributes, even if they only reflect as a string, because they are a convenient feature detection mechanism (and also a slight convenience when using the feature).

It would seem inconsistent not to have that here. And note that even those attributes for which we have a complex object, they have a simple IDL attribute too.

In any event, if feature detection can be done in some other not too complex way I guess not doing anything at all for now in terms of IDL attributes might be okay for Mozilla, but it seems better to match precedent.

<!-- comment id=410005571 author=sorvell created=2018-08-02T17:27:43Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-410005571 -->
## @sorvell — 2018-08-02

I think web developers generally prefer to set properties rather than attributes in imperative code. It's more straightforward since we need to know less about the underlying system (oh, this thing is only settable via attribute...).

I don't feel like the `classList` like analog is needed for `partmap` right now, and it also seems easy to add later if we want. Therefore, simply reflecting to string seems simplest to me.

That said, `class` and `className` and `classList` seems like a reasonable precedent, if not the prettiest, for how to handle this.

I wonder if *part* of the concern stems from the word "map" in `partmap` since that feels like it should reflect to something more like `classList`. What if we did something like change `partmap` to `forwardedparts` and reflect that to `forwardedPartsName` and then that leaves room more easily to add `forwardedPartsMap` in the future? If this seems like an ok compromise we can obviously massage the names =).

<!-- comment id=410496446 author=rniwa created=2018-08-05T05:14:03Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-410496446 -->
## @rniwa — 2018-08-05

Sure, changing the name of to something like `partforwarding` or `forwardedparts` would help.

<!-- comment id=423072893 author=fergald created=2018-09-20T07:33:40Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-423072893 -->
## @fergald — 2018-09-20

I'm OK with renaming things

@rniwa 
Those alternative names are quite long. `exposedparts` is the shortest reasonable name I can think of. `parts` is comprehensible but maybe too confusing given that `part` is allowed to have multiple tokens.

@sorvell `forwardedPartsName` (or `exposedPartsName`) seems very odd to me, I realise this is copying the className and classList model but that also seems very odd to me and maybe shouldn't be used as a precedent. The difference between className and classList is that one is a string and one is a DOMTokenList, so I would prefer fooString, fooList, fooMap

I can't find another instance of Name vs List it in element.idl, so it doesn't seem like a convention has been established.

<!-- comment id=423075453 author=domenic created=2018-09-20T07:41:45Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-423075453 -->
## @domenic — 2018-09-20

className (and htmlFor) are just because of old language restrictions on the use of keywords from the 90s. If we had modern JavaScript when they were added to the DOM they would just use the same name as the corresponding attribute (i.e. class and for).

<!-- comment id=423076064 author=fergald created=2018-09-20T07:43:45Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-423076064 -->
## @fergald — 2018-09-20

OK, so then just `exposedParts` as the string version.

<!-- comment id=423384256 author=fergald created=2018-09-21T01:24:35Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-423384256 -->
## @fergald — 2018-09-21

I've renamed `partmap` to `exposedparts` and added IDL to the spec in 2b10ddf04f8704ee874e55ee9b37d125f524b83d. @domenic could you check the IDL and then I can close this.

<!-- comment id=423443819 author=annevk created=2018-09-21T07:40:59Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-423443819 -->
## @annevk — 2018-09-21

You also need some kind of formal definition as to how these IDL attributes work. And if these are indeed meant to be "superglobal" attributes perhaps they should be defined in the DOM Standard along with class/id/slot?

<!-- comment id=424978622 author=fergald created=2018-09-27T06:52:19Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-424978622 -->
## @fergald — 2018-09-27

Added a brief note on the definition and an issue to update the DOM standard once we're agreed.

<!-- comment id=424981300 author=rniwa created=2018-09-27T07:04:36Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-424981300 -->
## @rniwa — 2018-09-27

I don't think "exposed" adds any semantic value here. Also, that sounds like a list of parts defined in a single shadow tree which are exposed. So I don't think that naming works.

Also, we shouldn't be using a random new term like "exposed". We have to consider the cognitive stress of any non-English speakers who have to learn those random words.

<!-- comment id=424983407 author=rniwa created=2018-09-27T07:13:02Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-424983407 -->
## @rniwa — 2018-09-27

Maybe `inheritedparts`, `cascadedparts`, or `exportedparts` ?

<!-- comment id=424991475 author=fergald created=2018-09-27T07:43:17Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-424991475 -->
## @fergald — 2018-09-27

> I don't think "exposed" adds any semantic value here. Also, that sounds like a list of parts defined in a single shadow tree which are exposed. So I don't think that naming works.

It *is* a list of parts defined in a single shadow tree which are exposed. It just depends on what you think you're exposing them to - CSS in element's scope or the scope above. Maybe there's some ambiguity, people might think they need to list things here to expose them to the element's scope but I feel like that applies to other suggestions too.

Maybe `outerparts`.

Anyway I'm not too bothered with what we choose here. I'll leave this open, I've raised it as needing resolution in #2368.

<!-- comment id=436849602 author=fergald created=2018-11-08T02:19:35Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-436849602 -->
## @fergald — 2018-11-08

Final conclusion on attribute names is part and exportedparts.

For IDL, conclusion was to expose part only for now, pending specing a full map-like IDL for exportedparts.

We didn't explicitly agree this but based on earlier statements on how we only have both class *and* classList for historical reasons, part's IDL will be a DOMTokenList from the beginning, not a string.

<!-- comment id=436860806 author=rniwa created=2018-11-08T03:26:23Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-436860806 -->
## @rniwa — 2018-11-08

Given `DOMTokenList` has a stringifier, `part` always being a `DOMTokenList` seems totally fine.

<!-- comment id=437003426 author=annevk created=2018-11-08T14:02:03Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-437003426 -->
## @annevk — 2018-11-08

I think I verified that `part` holds a set and is case-sensitive. As such it's a reasonable match for `DOMTokenList`, though `supports()` would not work well here.

<!-- comment id=551449831 author=annevk created=2019-11-08T09:16:44Z url=https://github.com/w3c/csswg-drafts/issues/2414#issuecomment-551449831 -->
## @annevk — 2019-11-08

Now that some implementations are done, can we get this upstreamed to DOM as needed?
