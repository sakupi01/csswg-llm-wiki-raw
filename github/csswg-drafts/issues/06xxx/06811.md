---
number: 6811
title: "[css-nesting] Proposal to use `()` instead of `&`"
state: "closed"
labels: ["Closed as Retracted"]
author: "Lorp"
created_at: "2021-11-10T17:25:10Z"
closed_at: "2021-11-11T10:42:07Z"
url: "https://github.com/w3c/csswg-drafts/issues/6811"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:44:23Z"
---

Is it decided that `&` is to be used to represent the parent selector?

I get that it’s already used in some preprocessors, and perhaps has a conceptual relationship to C’s `&` (address of).

Yet it feels far from ideal to me. It has strong connotations with boolean operations, and I believe it will be mistaken for an operator for CSS newbies to learn alongside `+`, `>`, `~`, `,`. It also prevents future use of `&` as a boolean operator in selectors. A string such as `this` would be clearer and have precedent in web development.

The string `this` almost certainly wouldn’t do, but nevertheless it seems to me that we need something that represents “this”. I therefore propose `()` as a reasonably well-suited character sequence that represents “this” visually.

Examples:

```css
.foo {
	color: red; /* .foo */

	.bar {
		color: blue; /* .foo .bar (shorthand) */
	}

	().bar {
		color: green; /* .foo.bar */
	}

	() .bar {
		color: yellow; /* .foo .bar */
	}

	.bar() {
		color: cyan; /* .bar.foo */
	}

	.bar () {
		color: purple; /* .bar .foo */
	}
}
```

Thoughts?

<!-- comment id=965589067 author=tabatkins created=2021-11-10T17:49:03Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-965589067 -->
## @tabatkins — 2021-11-10

The `&` is already widely used as the nesting selector in a wide range of preprocessors, and people don't seem to have any problem with that. I think the fear of boolean-operator confusion isn't realistic, either; the combinators aren't perceived as "operators" in that sense, even those that *are already more commonly-used boolean operators in other languages*, like `+` and `>`.

<!-- comment id=965591205 author=Loirooriol created=2021-11-10T17:51:35Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-965591205 -->
## @Loirooriol — 2021-11-10

`&` is parsed as a delim-token. `()` can be parsed as either a block or a function token.
So `().bar` is a `()-block`, a `.` delim, and a `bar` ident. And `.bar()` is a `.` delim, and a `bar` function. Seems wrong.

Also, I don't think we should worry about `&` being an address-of operator in C, since that's completely unrelated to CSS selectors. And about logical AND, `S1` AND `S2` is expressed as `:is(S1):is(S2)` in CSS selectors.

<!-- comment id=965623043 author=Lorp created=2021-11-10T18:28:39Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-965623043 -->
## @Lorp — 2021-11-10

Thanks for the consideration. I note that @matthew-dean previously proposed `:()` as a pseudo-class in #5491.

<!-- comment id=965659722 author=LeaVerou created=2021-11-10T19:15:20Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-965659722 -->
## @LeaVerou — 2021-11-10

I would find `()` incredibly confusing, as it's basically an empty group, not a representation of the current context. As @tabatkins said, there is a ton of precedent for `&` and people have absolutely no problem with it. Let's not try to fix what ain't broken.

<!-- comment id=965919547 author=matthew-dean created=2021-11-11T01:51:50Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-965919547 -->
## @matthew-dean — 2021-11-11

> The & is already widely used as the nesting selector in a wide range of preprocessors

I've mentioned it before, of course, but the interpretation / behavior / semantics of `&` vary across each pre-processor, which also differ from the CSS spec (although they roughly align in concept?), so there is still the possibility of conflict / confusion, but that may be resolvable (the conflict / confusion may be slight?), and it may still be the best / favored option. I personally still prefer a no-conflict solution, but I'm probably in the minority.

<!-- comment id=966150583 author=Lorp created=2021-11-11T09:35:57Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-966150583 -->
## @Lorp — 2021-11-11

FWIW I don’t use preprocessors and found `&` a bit weird.

<!-- comment id=966170662 author=LeaVerou created=2021-11-11T10:05:09Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-966170662 -->
## @LeaVerou — 2021-11-11

*Any* symbol we use will feel a little weird to some people, but `&` already has a ton of precedent around it, and all things being equal [consistency](https://w3ctag.github.io/design-principles/#consistency) with existing precedent is good.

<!-- comment id=966194576 author=Lorp created=2021-11-11T10:40:43Z url=https://github.com/w3c/csswg-drafts/issues/6811#issuecomment-966194576 -->
## @Lorp — 2021-11-11

Ok, I wish `&` well.
