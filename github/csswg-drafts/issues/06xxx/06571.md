---
number: 6571
title: "[selectors-4] Attribute match syntax extended to cover tag selectors"
state: "open"
labels: ["selectors-4", "selectors-5"]
author: "castastrophe"
created_at: "2021-09-02T16:53:12Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/6571"
comments: 15
has_resolution: false
synced_at: "2026-07-04T04:45:38Z"
---

Link: https://www.w3.org/TR/2018/WD-selectors-4-20181121/

With the advent of custom elements and the growing popularity of web component libraries (especially those with common prefixes), the need for a way to target a tag selector using attribute match syntax has increased.

## Use case

I have library x with a set of components: `x-foo`, `x-bar`, `x-bat`.  I want to write styles influence only those components on the page but I'm not sure which components are in use.

## Suggestions

In this thread, a few ideas were proposed: https://twitter.com/castastrophee/status/1433418836364210182?s=20

I'll do my best to summarize:

1. Add support for wildcard syntax with selectors, i.e. `x-* { color: red; }`.
2. Add support for wildcard syntax inside the `:is()` pseudo-selector i.e. `:is(x-*)`
3. Add a tag or tagName attribute that can be queried i.e., `[tag^="x-"]`

<!-- comment id=911880542 author=emilio created=2021-09-02T16:56:39Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911880542 -->
## @emilio — 2021-09-02

I'm a bit concerned about this defeating many optimizations that engines have for tag selectors. If you have a tag selector you are guaranteed that it'll never impact performance of elements without that tag, but these selectors you'd need to match against all elements.

<!-- comment id=911882539 author=astearns created=2021-09-02T16:59:20Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911882539 -->
## @astearns — 2021-09-02

@emilio I was thinking something the same, which made me think that the [tag^="x-"] syntax might be best, to give some indication the selector had similar performance characteristics to attribute substring matches.

<!-- comment id=911886444 author=castastrophe created=2021-09-02T17:03:59Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911886444 -->
## @castastrophe — 2021-09-02

@astearns I'm partial to that approach myself. It feels clearer and easier for authors to learn/understand.  The `x-*` selector feels strange to me.

<!-- comment id=911919635 author=LeaVerou created=2021-09-02T17:48:10Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911919635 -->
## @LeaVerou — 2021-09-02

Note that attribute selectors with `[tag]` wouldn't work, because then there is no way to target an actual `tag` attribute. I suspect the `:is()` idea wouldn't work, as we likely don't want to have special parsing rules for within `:is()` compared to outside `:is()`. 

Note that `x*` might work for element selectors but not attribute names, since `[a*=b]` already has a defined meaning. Unless we mandate a space after the wildcard? There is precedent for that sort of thing, with space around `-` and `+` in `calc()`. Both `a*` as well as `[a* =b]` are currently invalid, so it should be backwards compatible. I think the wildcard approach would be my personal preference. It's simple, it's clear, and it feels very intuitive.

<!-- comment id=911929577 author=Westbrook created=2021-09-02T18:02:00Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911929577 -->
## @Westbrook — 2021-09-02

+1 🎉 

Questions:
- The use of wildcard has some implications of a "blob"-type syntax. Would this be best restricted to `{string}*` only, or would we have the ability for `*{string}` or `{string}*{string}`, etc.? While there is lots of prior art of custom element scoping that that leverages a prepositional namespace,`x-*`, there is certainly no requirement for this, and the possibility of supporting postpositional namespacing via `*{string}` is quite interesting.
- All the approaches here open the possibility for an `h*` selector, which is awesome! However, the ability for that to point to `h1-6` and `html` and `h-card`, etc. might be both undesired or just plain bad practice. Is that an acceptable future? Should the use of a `-` in the selector form a structural requirement as well?

<!-- comment id=911944398 author=castastrophe created=2021-09-02T18:22:38Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911944398 -->
## @castastrophe — 2021-09-02

@Westbrook There's a good point here that you could easily accidentally target tags you didn't mean to simply from a lack of knowing every tag and which are being used on a page.

Regex proposal would cover an `/h[1-6]/` syntax: https://github.com/w3c/csswg-drafts/issues/1010

<!-- comment id=911948215 author=castastrophe created=2021-09-02T18:28:22Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-911948215 -->
## @castastrophe — 2021-09-02

Another possibility:

4. Add support for wildcard syntax inside the matches pseudo-selector i.e. `:matches(x-*)`

<!-- comment id=912144703 author=Loirooriol created=2021-09-03T00:09:09Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-912144703 -->
## @Loirooriol — 2021-09-03

I wouldn't pollute `:is()` with an obscure syntax that can't be used outside of it. And `x-*` seems confusing because `*` typically means universal selector. So I would add a new pseudo-class, and borrow the inner syntax from attribute selectors:

```css
:tag(= x-foo) /* like `x-foo` but different specificity */
:tag(^= x-)
:tag($= -foo)
:tag(*= -foo-)
:tag(= div s) /* case sensitive: matches <div> but not <DIV> */
```

<!-- comment id=912818277 author=tabatkins created=2021-09-03T21:23:07Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-912818277 -->
## @tabatkins — 2021-09-03

Agree that a pseudo-class is probably the best way to go to allow for the complexity we need without making existing stuff messy. I suspect we want to override the specificity so it still counts as a tag selector for specificity purposes, tho.

Same with attribute names, but keep the normal specificity.

<!-- comment id=1730029665 author=andy-blum created=2023-09-21T17:42:53Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-1730029665 -->
## @andy-blum — 2023-09-21

I like the new pseudo-selector idea, but I wonder if instead of `:tag`, `:custom` might be better?

```css
:custom /* selects all non-native elements */
:custom(= x-foo) /* like `x-foo` but different specificity */
:custom(^= x-)
:custom($= -foo)
:custom(*= -foo-)
:custom(= div s) /* case sensitive: matches <div> but not <DIV> */
```

The reason for `:custom` over `:tag` is to also include [customized elements](https://developer.mozilla.org/en-US/docs/Web/API/Web_components/Using_custom_elements) using the `is=""` syntax.

For example, `<p is="word-count">` and `<word-count>` are the same element class, but different tag names.

<!-- comment id=1730708186 author=bleper created=2023-09-22T02:10:47Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-1730708186 -->
## @bleper — 2023-09-22

@andy-blum This would be quite cool, especially considering the combination of `:custom` with `:defined` or pseudos associated with Shadow DOM. So it is probably better to open a separate issue so that it does not get lost.

<!-- comment id=2457518124 author=LeaVerou created=2024-11-05T15:44:35Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-2457518124 -->
## @LeaVerou — 2024-11-05

Do note that many component libraries need rules to avoid FOUC. Currently, these look like this:

```css
:where(:not(:defined)), 
:where(:not(:defined)) * {
  opacity: 0;
}
```

This would target _any_ element on the page that is not defined (including elements that were never _meant_ to be defined), because it is *impossible* to target those that actually belong to the library (i.e. have a shared prefix).

I think wildcard matching is incredibly important for a number of use cases. I would personally prefer to fit it into the existing syntax so that it can be added across a variety of syntax without expanding the API surface (e.g. `foo-*` for elements, `.foo-*` for classes, `[foo-*]` for attribute names and so on) but the important thing is making it _possible_ at all, even with awkward pseudo-class syntax...

<!-- comment id=2483085602 author=Crissov created=2024-11-18T13:42:02Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-2483085602 -->
## @Crissov — 2024-11-18

~~~~ css
x-... {foo: bar;}
[y-...] {foo: bar;}
[is^=x-] {foo: bar;}
~~~~

Forget about the asterisk, introduce pseudo-ellipsis. 

<!-- comment id=4173054492 author=Aquaticat created=2026-04-01T21:24:35Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-4173054492 -->
## @Aquaticat — 2026-04-01

Just got blocked on this. My use case is to set `display: contents` for all of my custom elements:

```html
<my-button>
  <button type=button>
    button
  </button>
</my-button>
```

I cannot use web components or `:defined` because everything must display correctly without any JS execution.

A startsWith tag matcher would solve this perfectly.

<!-- comment id=4178041898 author=Crissov created=2026-04-02T13:50:43Z url=https://github.com/w3c/csswg-drafts/issues/6571#issuecomment-4178041898 -->
## @Crissov — 2026-04-02

For hyphen-prefixed class names, being discussed in #10001, I suggested to slightly change the existing attribute selector, so `[class|="my"]` would not only work for `<foo class="my-class">` but also with multiple or non-first values as in `<foo class="bar my-first my-second">`. (`[…|~=…]` or `[…~|=…]` could be a more explicit but new alternative.)

The pipe or vertical bar character `|` used therein is also found already in [namespaced element and attribute selectors][css-namespaces]. They [support matching](https://drafts.csswg.org/selectors-3/#univnmsp) all elements from a given namespace prefix, `ns|* {}`, and also matching a given element name in any (or no) namespace, `*|element {}` or – that’s not the same: – outside any namespace `|element {}`.

~~~~ ebnf
@namespace <namespace-prefix>? [ <string> | <url> ] ;
~~~~

This vague precedence or similarity could inform the WG to change or extend this at-rule syntax to support _pseudo-namespaces_ (in HTML) that do not use a colon but a hyphen after the prefix. For instance, this could be indicated by a local namespace URL, starting with `#`, which [should never occur in XML](https://www.w3.org/2000/09/xppa). Within selectors, it would work the same as with XML namespaces.

~~~~ html
<my-elem my-attr="my-value" data-my="1" data-attr="1"/>
<your-elem your-attr="your-value" data-your="1" data-attr="1"/>
<elem attr="value"/>
<my:elem my:attr="my:value"/>
~~~~
<!--
<myelem myattr="myvalue"/>
<elem xmlns="#"/>
<elem xmlns:my="#"/>
<my:elem xmlns="#"/>
<my:elem xmlns:my="#"/>
-->
~~~~ css
[my-attr|="my"] {/* already works for single values */}

my-elem {/* just works */}
[my-attr] {/* just works */}

@namespace my "#my"; /* example syntax, could make sense to restrict the prefix to <dashed-ident>; better use "#" or "#my-"? */

my|* {/* now selects <my-elem> */}
[my|*] {/* now selects <… my-attr>, but not <my-elem attr> */}

*|elem {/* now selects <my-elem> and still <elem> and <my:elem>, but not undeclared <your-elem> */}
[*|attr] {/* now selects <… my-attr> and still <… attr>, but not undeclared <… your-attr> nor <… my:attr> – I think */}

@namespace "#my"; /* example default namespace declaration */

|elem {/* now selects <my-elem>, and still <elem> */}
[|attr] {/* unaffected, does not select <… my-attr>, just <… attr>! */}
~~~~

[css-namespaces]: <https://drafts.csswg.org/css-namespaces-3/#declaration>
