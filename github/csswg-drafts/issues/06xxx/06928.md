---
number: 6928
title: "[selectors] A pseudo-element should start with `::` at its start"
state: "closed"
labels: ["Closed Rejected as Invalid", "selectors-4"]
author: "cdoublev"
created_at: "2022-01-03T20:21:11Z"
closed_at: "2022-01-03T21:07:52Z"
url: "https://github.com/w3c/csswg-drafts/issues/6928"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:10Z"
---

https://drafts.csswg.org/selectors/#grammar

> `<pseudo-element-selector> = ':' <pseudo-class-selector>`
>
> [...]
> 
> The four Level 2 pseudo-elements (`::before`, `::after`, `::first-line`, and `::first-letter`) may, for legacy reasons, be represented using the `<pseudo-class-selector>` grammar, with only a single ":" character at their start.

I think it should be:

> `<pseudo-element-selector> = ':'{1,2} <pseudo-class-selector>`

Or:

> `<pseudo-element-selector> = '::' <pseudo-class-selector>`
>
> [...]
>
> The four Level 2 pseudo-elements (`::before`, `::after`, `::first-line`, and `::first-letter`) may, for legacy reasons, be represented using the `<pseudo-class-selector>` grammar, with only a single ":" character at their start.

<!-- comment id=1004359383 author=dlibby- created=2022-01-03T21:07:52Z url=https://github.com/w3c/csswg-drafts/issues/6928#issuecomment-1004359383 -->
## @dlibby- — 2022-01-03

`<pseudo-class-selector>` is
```
<pseudo-class-selector> = ':' <ident-token> |
                          ':' <function-token> <any-value> ')'
```

which contains the first `:`. So the spec already represents your second proposal.
