---
number: 6467
title: "[css-borders] `border-radius` with a circular edge"
state: "open"
labels: ["css-borders-4"]
author: "jonathantneal"
created_at: "2021-07-21T12:16:47Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/6467"
comments: 26
has_resolution: false
synced_at: "2026-07-04T04:45:23Z"
---

Is there, or could there be a more semantic way to describe a border radius as being round rather than some current conventions?

From [TailwindCSS](https://github.com/tailwindlabs/tailwindcss/blob/v2.2.4/stubs/defaultConfig.stub.js#L150):

```css
.rounded-full {
  border-radius: 9999px;
}
```

From an [accepted StackOverflow answer](https://stackoverflow.com/a/31617496):

```css
#rect {
  border-radius: 100000000000000px;
}
```

Other examples include `9999em` or `100vmax`.

Perhaps we could introduce a keyword, like `round`?

```css
.rounded-full {
  border-radius: round;
}
```

```css
#rect {
  border-radius: round;
}
```

<!-- comment id=884255174 author=nickfla1-mxm created=2021-07-21T14:55:04Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884255174 -->
## @nickfla1-mxm — 2021-07-21

Just a minor note:

> From an [accepted StackOverflow answer](https://github.com/tailwindlabs/tailwindcss/blob/v2.2.4/stubs/defaultConfig.stub.js#L150):

Points to the wrong url (same as Tailwind's).

<!-- comment id=884276074 author=argyleink created=2021-07-21T15:21:39Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884276074 -->
## @argyleink — 2021-07-21

YES

<!-- comment id=884276905 author=Loirooriol created=2021-07-21T15:22:45Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884276905 -->
## @Loirooriol — 2021-07-21

Maybe `calc(1px / 0)` or `calc(infinity * 1px)` look less arbitrary than `100000000000000px`?

<!-- comment id=884286781 author=argyleink created=2021-07-21T15:36:09Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884286781 -->
## @argyleink — 2021-07-21

could use `border-radius: 1e5px` too

<!-- comment id=884295682 author=jonathantneal created=2021-07-21T15:48:43Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884295682 -->
## @jonathantneal — 2021-07-21

Thank you, @nickfla1-mxm! The link has now been updated.

<!-- comment id=884359609 author=anthonyrivas created=2021-07-21T17:23:42Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884359609 -->
## @anthonyrivas — 2021-07-21

As much as I would love to see this, is there any concern that the term round or something similar could be confusing? Perhaps something more like "circular" would be more descriptive? 

Generally any radius is making a corner round to some degree, but "circular" may better indicate that it is meant to be fully rounded?

<!-- comment id=884652892 author=yisibl created=2021-07-22T04:45:09Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884652892 -->
## @yisibl — 2021-07-22

This has indeed troubled developers for many years, and using `999px` or `9999px` is too hack.

<!-- comment id=884895297 author=jonathantneal created=2021-07-22T13:04:41Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-884895297 -->
## @jonathantneal — 2021-07-22

While I don’t want the request for a semantic value to get confused with bikeshedding a specific keyword solution, I hope it will be helpful to share something I’ve learned. Having reviewed the specification for `corner-shape` in [css-backgrounds-4](https://drafts.csswg.org/css-backgrounds-4/#corner-shaping), I now think `border-radius` would need a keyword that somehow describes the radii without describing its _shape_.

Here is an example of using `border-radius` and `corner-shape` from the current specification:

```css
a {
  border-radius: .3em .8em .8em .3em / .3em 50% 50% .3em;
  corner-shape: round angle angle round;
  padding: .5em 1em .5em .5em;
}
```

I think it would be confusing to write `border-radius: round; corner-shape: angle`. Therefore, a better term might be something like `fill`, `quarter`, etc.

<!-- comment id=992180115 author=yisibl created=2021-12-13T07:25:46Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-992180115 -->
## @yisibl — 2021-12-13

While Chrome and Safari already implement the `infinity` keyword, a separate `border-radius: round` might help optimize browser performance. And it's easier for developers to understand.

`calc(1px / 0)` or `calc(infinity * 1px)` still looks like a hack.

![image](https://user-images.githubusercontent.com/2784308/145769251-85c440a5-8ccf-4491-95c4-883a12eaf0b9.png)

<!-- comment id=1046010684 author=simevidas created=2022-02-19T12:51:52Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1046010684 -->
## @simevidas — 2022-02-19

Since a circular border is a the result of a radius that is as large as possible, maybe the optimal keyword for this would be one that describes exactly that.

```css
{
  border-radius: maximum;
}
```

<!-- comment id=1046015836 author=SelenIT created=2022-02-19T13:15:19Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1046015836 -->
## @SelenIT — 2022-02-19

Adding separate keyword for the corner shape (like `circle` vs. `ellipse` for radial gradient shapes) will have additional use cases though. e.g. something like `border-radius: 25% round` could create a shape with roundings taking up quarters of the shorter side length. Maybe this is not the most frequent case, but there seems to be no other reliable solution if this is needed.

<!-- comment id=1046022413 author=noamr created=2022-02-19T13:48:55Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1046022413 -->
## @noamr — 2022-02-19

I think this can be solved by https://github.com/w3c/csswg-drafts/issues/5881 (or https://github.com/w3c/csswg-drafts/issues/6997 which is kind of a duplicate)

Rather than forcing border-radius to be a circle, give `clip-path: circle` with some semantic to make it affect borders and play nice with filters.

<!-- comment id=1091754084 author=simevidas created=2022-04-07T13:40:35Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1091754084 -->
## @simevidas — 2022-04-07

CSS Values spec:

> [`infinity`] can be used to indicate a "largest possible value", since an infinite value gets clamped to the allowed range.

https://drafts.csswg.org/css-values-4/#calc-error-constants

I think this means that `border-radius: infinity` should result in a maximum circular border, so there’s no need for a new keyword (unless `infinity` isn’t readable enough).

If this is true, the next step would be to find/create the browser bugs and get this implemented.

CC @Afif13 

<!-- comment id=1091768423 author=Loirooriol created=2022-04-07T13:53:07Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1091768423 -->
## @Loirooriol — 2022-04-07

@simevidas You can't use `infinity` like that unless the property allows it (like `animation-iteration-count`). You need `calc(infinity * 1px)`, `calc(1px / 0)`, etc. Needs to be inside a math function, and have length unit.

<!-- comment id=1091773789 author=Afif13 created=2022-04-07T13:57:59Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1091773789 -->
## @Afif13 — 2022-04-07

@Loirooriol it seems that "infinity" is only allowed within a calculation. `z-index: infinity` didn't work for me but `z-index:calc(infinity)` works fine. `z-index: max(infinity)` seems valid as well. Same with `animation-iteration-count`

<!-- comment id=1092143204 author=Loirooriol created=2022-04-07T19:54:13Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1092143204 -->
## @Loirooriol — 2022-04-07

Yeah, bad example, `animation-iteration-count` accepts `infinite`, not `infinity`. Filed #7204

<!-- comment id=1124006844 author=jsnkuhn created=2022-05-11T16:43:55Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1124006844 -->
## @jsnkuhn — 2022-05-11

all this talk of infinity, I think, is leaning into the hack.

This pill shape is created on a horizontal element (lets say `width: 200px;` and `height: 50px;`) when the border-radius of the corners are the pixel equivalent of 50% of the elements height (the value in our example would be 25px).

https://codepen.io/jsnkuhn/pen/ZErOqwL

This creates a situation where the reference circles for the top and bottom corners are fully overlapping. (Note that this is also how we get a circle with `border-radius: 50%;` on a square element because all the reference circles for the corners overlap.)

It needs to be a equal value so that the radius reference is round. If you would do `border-radius: 50%;` in our horizontal example you get 50% of the element width and 50% of the element height which gives you a reference ellipse because the width and height of our element are different. In our example we can do `border-radius: 12.5% / 50%;` to get equal px values for the reference circle.

The problem comes in when the width and the height values are not fixed.

So 9999px works because:

1. it's a pixel value. So it will create a circle.
2. Border-radius must be clamped somehow to deal with unrealistically high numbers

So is what's needed here some sort of element unit? As in border-radius: 50eh;  eh = element height

Also of note: This has implications for `corner-shape` if that ever happens. There's similar situation with `angle/bevel` and `scoop` corners

<!-- comment id=1172811746 author=jsnkuhn created=2022-07-02T01:54:15Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1172811746 -->
## @jsnkuhn — 2022-07-02

for the record these are the `999999px` equivalent shapes that would be created from the proposed corner-shapes values. And no the notch example is not a mistake, it will just cut the sides off the element:

![99999px](https://user-images.githubusercontent.com/1286791/176982182-0f521e9b-4ea7-43bd-b8c1-da884d7cd286.png)
 
so hopefully crystal clear why a `round` or `pill` keyword just isn't going to work.

a live codepen of these with my corner-shape worklet (chrome only for now). FYI haven't clamped the corner-size values yet so 9999999px isn't going to work as expected. 

https://codepen.io/jsnkuhn/pen/xxWGOzJ?editors=0010

<!-- comment id=2284327015 author=SebastianZ created=2024-08-12T15:46:24Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-2284327015 -->
## @SebastianZ — 2024-08-12

Note that we already have `round` as keyword for [`corner-shape`](https://drafts.csswg.org/css-borders-4/#corner-shaping). We'd need another keyword for this to avoid conflicts with the corner shape.

Sebastian

<!-- comment id=3418670093 author=KTibow created=2025-10-18T17:11:29Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418670093 -->
## @KTibow — 2025-10-18

From the angle of animating between fully rounded and semi/no rounding:

There's a barely perceptible animation with 999px. A "maximum" value would be even worse, completely breaking animation. I strongly favor a "fully rounded" value that would essentially act as half the shortest side - it would make animating between fully rounded and slight rounding much easier and not require making assumptions about the element's height when setting the border radius.

<!-- comment id=3418761321 author=noamr created=2025-10-18T19:34:41Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418761321 -->
## @noamr — 2025-10-18

Sounds like the number this corresponds to is `min(50% * width, 50%  * height)`.
Those high numbers happen to produce this effect because of the way radius constraining works.

I wonder if the right direction here is [this comment](https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-1124006844), e.g. have additional units that allow you to use the percentage of the opposite dimension, the same way you can use `vh` on width or `vmin`.

Perhaps `%h`, `%v`, `%max`, `%min`.

Also I'm sure these would be handy in the `shape()` function as well (and in other places that use this kind of percentage? background-sizing?) and not only for border-radius.

<!-- comment id=3418761855 author=jsnkuhn created=2025-10-18T19:35:36Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418761855 -->
## @jsnkuhn — 2025-10-18

would this be an appropriate situation for adding an `auto` keyword to `border-radius`?

<!-- comment id=3418805270 author=noamr created=2025-10-18T20:49:00Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418805270 -->
## @noamr — 2025-10-18

> would this be an appropriate situation for adding an `auto` keyword to `border-radius`?

I think it would be confusing that auto means specifically `min(width, height) * 50%`.
But regardless of what keyword we use, when interpolating it would have to resolve to something that takes that kind of formula into account, which means something along the lines of a new unit (or a function, but units are more appropriate for this IMO).

Also it would allow using values that are not 50%, e.g. `25%min` could be a non-stretched border-radius that's relative to the shorter dimenstion.

<!-- comment id=3418971580 author=Loirooriol created=2025-10-18T22:44:49Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418971580 -->
## @Loirooriol — 2025-10-18

I think these percentages units would raise some complications. Like, presumably they wouldn't be `<percentage>` since they wouldn't make sense in other properties. But authors may expect `calc(25%min + 1px)` or even `calc(25%min + 25% + 1px)` to work, so would need to add a bunch of productions like `<something>`, `<length-something>`, `<percentage-something>`, `<length-percentage-something>`.

I think it's simpler to just make it a keyword separate from the `<length-percentage>`, e.g. width a width of 200px and height of 100px,
-  `border-radius: 10% min` is like `10px`
-  `border-radius: 10% max` is like `20px`
-  `border-radius: calc(50px - 10%) min` is like `40px`

On the other hand, this raises some questions when using multiple values, but I guess the grammar could be `<length-percentage [0,∞]>{1,4} [ min | max ]? [ / <length-percentage [0,∞]>{1,4} [ min | max ]? ]?`. Then
- `border-radius: 1% 2% 3% 4% min` uses `min` for all percentages, so `1px 2px 3px 4px`
- `border-radius: 1% 2% 3% 4% / 5% 6% 7% 8% min` uses `min` for the y components, so `2px 4px 6px 8px / 5px 6px 7px 8px`

<!-- comment id=3419253608 author=noamr created=2025-10-19T06:22:50Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3419253608 -->
## @noamr — 2025-10-19

> I think these percentages units would raise some complications. Like, presumably they wouldn't be `<percentage>` since they wouldn't make sense in other properties. But authors may expect `calc(25%min + 1px)` or even `calc(25%min + 25% + 1px)` to work, so would need to add a bunch of productions like `<something>`, `<length-something>`, `<percentage-something>`, `<length-percentage-something>`.

Agreed, it's not simple implementation wise. Btw the same complication was already introduced by "direction agnostic size" for arcs in shape(), which is a related problem. 

It can probably be expressed by two <length-percentage> though as a simplification, where the second one calculates percentages based on the opposite dimension, and the rest is done with calc(min/max) etc. but this would require some research/prototyping.

<!-- comment id=3421286402 author=noamr created=2025-10-20T09:45:14Z url=https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3421286402 -->
## @noamr — 2025-10-20

... another option that would be substantially less complex to implement was to have a separate (per-corner) attribute that defines how percentages are handled. e.g. `corner-sizing: by-min-edge | by-max-edge | both` or some such. Since this property is separate from the `border-radius` itself it wouldn't interfere with animations. I think it would be a bit easier to reason about than https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418971580 or https://github.com/w3c/csswg-drafts/issues/6467#issuecomment-3418761321, though it's also a lot less flexible than having a new unit. 
