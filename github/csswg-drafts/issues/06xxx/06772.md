---
number: 6772
title: "[css-color-5] Why is <hue-adjuster> at the end of color-mix()?"
state: "closed"
labels: ["css-color-5"]
author: "tabatkins"
created_at: "2021-10-27T22:24:56Z"
closed_at: "2021-11-01T18:47:18Z"
url: "https://github.com/w3c/csswg-drafts/issues/6772"
comments: 9
has_resolution: false
synced_at: "2026-07-04T04:44:08Z"
---

The [color-mix()](https://drafts.csswg.org/css-color-5/#color-mix) grammar is currently defined as:

```
color-mix() = color-mix( in <color-space> , [ <color> && <percentage [0,100]>? ]#{2} <hue-adjuster>?)
```

This implies that a correct instance could be something like:

```
color-mix(in hsl, red 20%, blue 50% shortest)
```

This looks really weird imo! The 'shortest' appears to be applying to the blue color, but it's actually applying to "in hsl" part, as a part of the directions for how to interpolate the two colors.

This grammar also implies that I can provide a hue-adjuster for any color space, like:

```
color-mix(in srgb, red 20%, blue 50% shortest)
```

which doesn't do anything and seems confusing.

I suggest we move the `<hue-adjuster>` to the first term of the function, with the color space name, and only allow it as part of the cylindrical spaces. The grammar would instead look like:

```
<color-mix()> = color-mix( in <color-mix-space> , [ <color> && <percentage [0,100]>? ]#{2})
<color-mix-space> = <color-mix-rect-space> | <color-mix-hue-space>
<color-mix-rect-space> = srgb | xyz | lab | oklab
<color-mix-hue-space> = [ hsl | hwb | lch | oklch ] && <color-mix-hue-adjuster>?
<color-mix-hue-adjuster> = shorter | longer | increasing | decreasing | specified
```

The above example would then be written as:

```
color-mix(in hsl shortest, red 20%, blue 50%)
/* or with "in shortest hsl" */
```

and the similar "in srgb" example wouldn't be able to take an adjuster at all.

(I think the ability to reorder the space name and hue adjuster seems reasonable, but it's not necessary; we could just use concatenation rather than `&&` if we want to force an ordering.)

<!-- comment id=953887762 author=svgeesus created=2021-10-28T14:12:28Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-953887762 -->
## @svgeesus — 2021-10-28

Seems reasonable.

To clarify: what happens if someone **does**  use a hue adjuster with a rectangular orthogonal colorspace? Is it an error, or is it ignored?

Currently the spec [says](https://drafts.csswg.org/css-color-5/#color-mix):

> If the specified color space is a cylindrical-polar-color space, then the `<hue-adjuster>` controls the interpolation of hue, as described in CSS Color 4 § 13.3 Hue interpolation. If no `<hue-adjuster>` is specified, it is as if shorter had been specified. If the specified colorspace is a rectangular-orthogonal-color space, then specifying a `<hue-adjuster>` is not an error, but has no effect.)

I would also like to be sure that this syntax change is really worthwhile, given that the current syntax is already shipping in a released (not preview/beta) major browser. Bikeshedding has a non-zero cost.

<!-- comment id=954118956 author=tabatkins created=2021-10-28T19:02:00Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-954118956 -->
## @tabatkins — 2021-10-28

> To clarify: what happens if someone does use a hue adjuster with a rectangular orthogonal colorspace? Is it an error, or is it ignored?

With my suggested grammar, it would be a syntax error; the rectangular spaces don't allow a `<hue-adjuster>`. Since a hue adjuster is *nonsensical* for rectangular spaces, I do think making it a syntax error is better than just ignoring it (but I understand why you're ignoring it currently, as otherwise the grammar would be really clumsy to write).

> I would also like to be sure that this syntax change is really worthwhile, given that the current syntax is already shipping in a released (not preview/beta) major browser. Bikeshedding has a non-zero cost.

Ugh, I didn't realize color-mix() was shipping already. In Safari, I presume? Yeah, we should check with the implementors.

<!-- comment id=954729769 author=svgeesus created=2021-10-29T13:07:43Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-954729769 -->
## @svgeesus — 2021-10-29

> Ugh, I didn't realize color-mix() was shipping already. In Safari, I presume?

Yes. Safari added support for `color-mix()` in [Tech Preview 122, March 2021](https://webkit.org/blog/11577/release-notes-for-safari-technology-preview-122/) and shipped it in [Safari 15, released September 2021](https://developer.apple.com/documentation/safari-release-notes/safari-15-release-notes)

To be clear I think this is probably a good idea, I am just worried about churn.

<!-- comment id=954730599 author=svgeesus created=2021-10-29T13:08:56Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-954730599 -->
## @svgeesus — 2021-10-29

@weinig @smfr 

<!-- comment id=954944477 author=smfr created=2021-10-29T18:07:13Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-954944477 -->
## @smfr — 2021-10-29

Safari shipped `color-mix()` as an experimental feature, off by default, so I'm fine changing it (this is exactly what experimental features are for!).

<!-- comment id=955262411 author=svgeesus created=2021-10-30T14:51:27Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-955262411 -->
## @svgeesus — 2021-10-30

Awesome!

In that case, any objections to making the change @tabatkins proposed? Maybe we can resolve this without needing telcon time.

<!-- comment id=956442611 author=LeaVerou created=2021-11-01T17:42:15Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-956442611 -->
## @LeaVerou — 2021-11-01

Yeah, it absolutely should be together with the `in <color-space>` token.
In fact, I planned to define a general `<interpolation-method>` token that other specs can reference. 

<!-- comment id=956464563 author=svgeesus created=2021-11-01T18:10:46Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-956464563 -->
## @svgeesus — 2021-11-01

Okay so the token would be defined in CSS Color 4, in the Interpolation section, I assume? And then referenced in CSS Color 5 plus other places that want it (gradients)

<!-- comment id=956465582 author=LeaVerou created=2021-11-01T18:12:03Z url=https://github.com/w3c/csswg-drafts/issues/6772#issuecomment-956465582 -->
## @LeaVerou — 2021-11-01

> Okay so the token would be defined in CSS Color 4, in the Interpolation section, I assume? And then referenced in CSS Color 5 plus other places that want it (gradients)

Yup. I'm working on these changes already.
