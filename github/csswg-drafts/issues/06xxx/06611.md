---
number: 6611
title: "[mediaqueries-5] Allow `@custom-media` inside `@supports` or add `@custom-supports`"
state: "open"
labels: ["mediaqueries-5"]
author: "argyleink"
created_at: "2021-09-15T16:29:04Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/6611"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:10Z"
---

https://drafts.csswg.org/mediaqueries-5/#at-ruledef-custom-media

Consider the following styles where it feels wrong to use a custom-media inside supports right?

```css
@custom-media --motionOK (prefers-reduced-motion: no-preference);
@custom-media --scrollTimeline (animation-timeline: works);

@media (--motionOK) { ... }
@supports (--scrollTimeline) { ... }
```

<br>

adding `@custom-supports` to the spec would allow proper type checking? making this the example instead:

```css
@custom-media --motionOK (prefers-reduced-motion: no-preference);
@custom-supports --scrollTimeline (animation-timeline: works);

@media (--motionOK) { ... }
@supports (--scrollTimeline) { ... }
```

thoughts?

I very much love using `@custom-media` btw, very rad.

<!-- comment id=920189464 author=dbaron created=2021-09-15T16:47:20Z url=https://github.com/w3c/csswg-drafts/issues/6611#issuecomment-920189464 -->
## @dbaron — 2021-09-15

Today the CSSWG resolved to accept [Tab's when-else draft](https://tabatkins.github.io/specs/css-when-else/) proposal into Conditional 4, which has `media()` and `supports()` functions.  Perhaps `@custom-media` should instead be something that goes in `@when`, and then there could be only one?  Alternatively, maybe there should be a way of doing `@supports` queries inside `@media` rules, or vice-versa, or both?
