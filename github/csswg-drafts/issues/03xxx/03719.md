---
number: 3719
title: "[css-transforms-1] SVG transform attributes: transforms don't need to be separated"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "css-transforms-1"]
author: "dirkschulze"
created_at: "2019-03-11T05:54:52Z"
closed_at: "2019-05-22T19:05:17Z"
url: "https://github.com/w3c/csswg-drafts/issues/3719"
comments: 5
has_resolution: true
synced_at: "2026-07-04T04:34:03Z"
---

https://drafts.csswg.org/css-transforms/#svg-transforms does not reflect implementations correctly.

Syntax says:

```
transform
| transform comma-wsp transforms
```

which requires a comma or a wsp between transform functions. Browsers do not require any separation between transform functions.

Example:
```xml
<rect width="20" height="20" transform="translate(20, 20)scale(20)"/>
```
... does translate _and_ scale the rect in Safari, Chrome and Firefox.

<!-- comment id=471582982 author=AmeliaBR created=2019-03-11T15:18:28Z url=https://github.com/w3c/csswg-drafts/issues/3719#issuecomment-471582982 -->
## @AmeliaBR — 2019-03-11

Interestingly, the [SVG 1.1](https://www.w3.org/TR/SVG11/coords.html#TransformAttribute) grammar supports _one or more_ `comma-wsp` tokens, but Firefox seems to be the only browser that allows arbitrary commas in between functions (Edge allows up to 2 commas, but no more, Chrome only allows zero or one).

We should definitely change to match reality where browsers are consistent (i.e., make the `comma-wsp` token optional in the grammar).  For the inconsistent bit (excess separators), I would say the spec should be more restrictive (max one `wsp*-comma?-wsp*` sequence), so that content that matches the spec works in all browsers.

<!-- comment id=477247641 author=css-meeting-bot created=2019-03-27T16:46:02Z url=https://github.com/w3c/csswg-drafts/issues/3719#issuecomment-477247641 resolution=true -->
## @css-meeting-bot — 2019-03-27

The CSS Working Group just discussed `SVG transform attributes: transforms don't need to be separated`, and agreed to the following:

* `RESOLVED: For SVG Transform attribute there is no requirement for space or comma between transform functions`

<details><summary>The full IRC log of that discussion</summary>
&lt;dael> TOpic: SVG transform attributes: transforms don't need to be separated<br>
&lt;dael> github: https://github.com/w3c/csswg-drafts/issues/3719<br>
&lt;dael> krit: There is a historical difference between SVG and CSS. If we jsut look at SVG there is a case of full interop that's not covered by spec. THer'es no space or comma between any transform list. Spec requires it but impl don't. I request we relax the syntax to follow impl<br>
&lt;dael> TabAtkins: THis falls out of CSS definition where you don't need spaces if you can distinguish<br>
&lt;dael> AmeliaBR: Not that simple b/c rest of syntax can't be desc with CSS syntax. Have special parsing for transform based on SVG1. THis happens to be where all browsers don't match SVG1 so might as well match other browsers<br>
&lt;dael> AmeliaBR: Found other weirdness that's inconsistent but I'm happy to ignore certain browsers allow mangled syntax. But if everyone supports we should include in spec<br>
&lt;rachelandrew> florian, I'm also an editor of page floats, if I can locate the list of resolutions I can at least do that<br>
&lt;dael> krit: Requested is For SVG Transform attribute there is no requiremenet for space or comma between transform functions<br>
&lt;dael> astearns: Just transform attribute?<br>
&lt;dael> krit: FOr CSS that falls out with the tokenizer so yes jsut for transform attribute<br>
&lt;dael> astearns: Objections?<br>
&lt;dael> myles: IN SVG transform definition jsut links to css<br>
&lt;dael> krit: Correct<br>
&lt;dael> AmeliaBR: There's a section in css transforms that gives special rules for parsing the attribute<br>
&lt;dael> krit: Link ^<br>
&lt;dael> astearns: It's in css draft where this is defined<br>
&lt;dael> krit: Yep<br>
&lt;dael> RESOLVED: For SVG Transform attribute there is no requirement for space or comma between transform functions<br>
</details>

<!-- comment id=477338419 author=valtlai created=2019-03-27T20:39:36Z url=https://github.com/w3c/csswg-drafts/issues/3719#issuecomment-477338419 -->
## @valtlai — 2019-03-27

Why there’s comma separators for CSS transform functions in the first place? Why not spaces, like `translate(10px 20px)`?

The color functions now accept space-separated syntax, e.g. `rgb(0 0 0)`, which is more CSS-ish. Should we do the same for the transform functions?

<!-- comment id=477351328 author=AmeliaBR created=2019-03-27T21:17:54Z url=https://github.com/w3c/csswg-drafts/issues/3719#issuecomment-477351328 -->
## @AmeliaBR — 2019-03-27

@valtlai This discussion is about the legacy SVG syntax, which allowed either commas or spaces.

If you want to argue for loosening up the CSS syntax, that would be a separate issue.

I believe the reason to restrict things in CSS was about enforcing consistency: pick one syntax and stick with it! And _(x,y)_ is a common idiom for describing vector translations in math, so that's probably why it was chosen. But you're correct that we've muddied the waters by switching the syntax for colors, based on the argument that commas should be reserved for lists.

<!-- comment id=477651372 author=tabatkins created=2019-03-28T15:42:17Z url=https://github.com/w3c/csswg-drafts/issues/3719#issuecomment-477651372 -->
## @tabatkins — 2019-03-28

Note that the transform functions were defined and shipped *before* @fantasai and I elucidated the "commas are for repeated lists, even in functions" principles.

The color-function switch happened at the same time as we added multiple other new color functions, *and* relaxes the arg-length requirements on a "no -a" versions of the existing functions; as a small part of a much larger shift, it wasn't a big deal. Switching transforms at this point would be *solely* a comma-removal, which isn't really worth the cost.
