---
number: 3466
title: "[css-ui] Introduce \"except\" property to exempt \"outline\" from \"all\""
state: "open"
labels: ["css-ui-4"]
author: "ByteEater-pl"
created_at: "2018-12-23T14:50:50Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/3466"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:44:00Z"
---

https://www.w3.org/TR/2017/WD-css-ui-4-20171222/#issue-11ec97e1

This issue (14) can be resolved by introducing an `except` property taking `none` (initial) or a list of property names (somewhat like `will-change`) which are unaffected by the `all` property and instructing UAs to add `except: outline` on form elements.

<!-- comment id=449670468 author=Loirooriol created=2018-12-23T23:50:16Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-449670468 -->
## @Loirooriol — 2018-12-23

Should the initial value be `direction, unicode-bidi`?

<!-- comment id=449673600 author=ByteEater-pl created=2018-12-24T01:04:24Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-449673600 -->
## @ByteEater-pl — 2018-12-24

You're right, I forgot about those. So `except: direction, unicode-bidi` as initial and `except: direction, unicode-bidi, outline` for form elements (possibly with different order, as it's retained by serialization which doesn't take into account whether for a given property it makes a difference).

Just rereading the drafts has reminded me also that (from [css-variables]):
> Custom properties are not reset by the all property. We may define a property in the future that resets all variables.

This might be the right opportunity to tackle that as well.

<!-- comment id=449686690 author=Loirooriol created=2018-12-24T05:05:10Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-449686690 -->
## @Loirooriol — 2018-12-24

> Custom properties are not reset by the all property. We may define a property in the future that resets all variables.

There is a resolution against this, because it would be problematic to implement.
https://github.com/w3c/csswg-drafts/issues/2518#issuecomment-402357491

<!-- comment id=449789143 author=Loirooriol created=2018-12-25T02:25:28Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-449789143 -->
## @Loirooriol — 2018-12-25

The problem that I see with this proposal is that it makes expansion of the shorthand into longhands (which usually happens at parse time) depend on the value of another property (which is only known at computed-value time).

You can address this by saying that the shorthand assigns an internal pending-resolution value to all longhands (except `except`), which cascades as normal, and is later resolved according to `except`.

The problem is the serialization of this value at specified-value time. Empty string would make more sense, but not backwards-compatible.

<!-- comment id=449837461 author=ByteEater-pl created=2018-12-25T10:28:03Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-449837461 -->
## @ByteEater-pl — 2018-12-25

Indeed, it seems to necessitate redefining the mechanism of `all`, abandonning its shorthand status. It being one is a stretch anyway, a specification device, not something reflected in implementations, which treat it as a special case, I suppose.

You mean empty string instead of `none` as a value of `except`? I thought about it too and wondered if the grammar allows it (didn't check), remembering however that no property I've ever seen standardized or even proposed admitted such syntax, I opted for a keyword. Not married to it though.

<!-- comment id=450389021 author=Loirooriol created=2018-12-28T16:42:36Z url=https://github.com/w3c/csswg-drafts/issues/3466#issuecomment-450389021 -->
## @Loirooriol — 2018-12-28

No, I didn't mean empty string instead of `none`. I meant that if `all` is a shorthand, at parse time you don't know which longhands will be affected, so you need to set them all to some special internal value. Later, according to `except`, this internal value can compute to the value specified in `all`, or else to the following winner of the cascade for that longhand. But you can use CSSOM to serialize the internal value, e.g.

```js
element.style.all = "initial";
element.style.direction; // ??
element.style.display; // ??
```

Both `direction` and `display` will be set to this internal value, which is not clear how should be serialized. For consistency with https://drafts.csswg.org/css-variables/#variables-in-shorthands it could be empty string. But it's a breaking change, because currently `display` serializes to `"initial"`.
