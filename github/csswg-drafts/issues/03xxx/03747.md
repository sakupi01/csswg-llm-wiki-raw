---
number: 3747
title: "[css-nesting] referencing an ancestor selector"
state: "closed"
labels: []
author: "Dan503"
created_at: "2019-03-19T22:28:11Z"
closed_at: "2019-03-19T23:52:48Z"
url: "https://github.com/w3c/csswg-drafts/issues/3747"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:57:32Z"
---

CSS nesting spec
https://drafts.csswg.org/css-nesting-1/

In SCSS you can do this to reference an ancestor selector:

```scss
.foo {
  $ancestor: &;
  &__bar {
    &:hover {
      #{$ancestor}__baz {
        // .foo__bar:hover .foo__baz { ... }
      }
    }
  }
}
```

The main use case for wanting to do this is when a parent has a hover/focus state that affects the styling of a nested child element.

This is a common desire for BEM users.

Just an idea, maybe do something like this in real CSS?

```scss
.foo {
  @nest reference: customName;
  &__bar {
    &:hover {
      & (customName)__baz {
        // .foo__bar:hover .foo__baz { ... }
      }
    }
  }
}
```

The idea being that `@nest reference: customName;` registers `customName` as the reference to that ancestor selector.

You can then use `(customName)` inside brackets to reference the registered ancestor selector. Brackets may or may not be the best way to identify it as the referenced ancestor selector.

<!-- comment id=474616514 author=jonjohnjohnson created=2019-03-19T22:58:04Z url=https://github.com/w3c/csswg-drafts/issues/3747#issuecomment-474616514 -->
## @jonjohnjohnson — 2019-03-19

Aside from...

> The Sass maintainers consider that syntax (building up a single simple selector thru concatenation of characters) to have been a serious mistake that they wish they could undo.  - @tabatkins – https://github.com/w3c/csswg-drafts/issues/2937

...your example showing single simple selector concatenation, I believe your "main use case for wanting to do this is when a parent has a hover/focus state that affects the styling of a nested child element" is covered in the current draft like so?

```css
.parent {
  @nest &:hover .child {
    /* .parent:hover .child { ... } */
  }
}
```
See Example 5 in [The Nesting At-Rule: @nest](https://drafts.csswg.org/css-nesting-1/#at-nest)

Related...
>...if env() is used in selectors, which is one of the things that author-defined env() is intended to be useful for. - @tabatkins – https://github.com/w3c/csswg-drafts/issues/3285#issuecomment-436760193

<!-- comment id=474619849 author=Dan503 created=2019-03-19T23:11:40Z url=https://github.com/w3c/csswg-drafts/issues/3747#issuecomment-474619849 -->
## @Dan503 — 2019-03-19

> I believe your "main use case for wanting to do this is when a parent has a hover/focus state that affects the styling of a nested child element" is covered in the current draft like so?

The example you gave is too simplified.

This request makes the most sense when you are using BEM.

The use case is that an element of a block has a hover state. A child element inside the hover element needs styling based on the hover state. Doing this without BEM doesn't reveal the issue.

If you use the current spec as is, you get this which is incorrect.

```scss
.foo {
  &__bar {
    &:hover {
      &__baz {
        // .foo__bar:hover__baz { ... }
      }
    }
  }
}
```
That produces this rule:
```
.foo__bar:hover__baz { ... }
```
What I'd like is this:
```
.foo__bar:hover .foo__baz { ... }
```
I'd like a way to reference and reuse an ancestor selector deeper in the nest without having to repeat it.

<!-- comment id=474629175 author=Dan503 created=2019-03-19T23:52:47Z url=https://github.com/w3c/csswg-drafts/issues/3747#issuecomment-474629175 -->
## @Dan503 — 2019-03-19

Oh apparently `&__element { ... }` isn't actually valid CSS syntax. https://github.com/w3c/csswg-drafts/issues/3748#issuecomment-474626924

That kind of makes this issue null and void unless a new use case presents itself.
