---
number: 3262
title: "[css-env-1] Should env() really use <custom-ident>?"
state: "open"
labels: ["css-env-1"]
author: "emilio"
created_at: "2018-10-29T11:27:07Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/3262"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:55:03Z"
---

https://drafts.csswg.org/css-env-1/#env-function says:

```
env() = env( <custom-ident> , <declaration-value>? )
```

However both Blink and WebKit use just `<ident>` for this.

Test-case can be:

```html
<!doctype html>
<style>
div {
    width: 100px;
    height: 100px;
    background-color: green;
    background-color: env(inherit, red);
}
</style>
<div></div>
```

Which should be green per spec but is red in Blink and WebKit.

Should the spec be changed to use just `<ident>`, or should bugs be filed on browsers?

I made the same mistake in https://bugzilla.mozilla.org/show_bug.cgi?id=1462233, but @heycam caught it during review.

<!-- comment id=434040759 author=tabatkins created=2018-10-29T19:08:38Z url=https://github.com/w3c/csswg-drafts/issues/3262#issuecomment-434040759 -->
## @tabatkins — 2018-10-29

Ah yeah, that's unfortunate. We exclude the global keywords with the assumption that most custom idents will be used top-level in a declaration, but function arguments don't have to obey that; they're already excluded from triggering the global behavior.

This is similar to [the distinction Syntax draws between `<declaration-value>` and `<any-value>`](https://drafts.csswg.org/css-syntax/#any-value); maybe that means we should define a less restrictive `<any-ident>` production that can be used when appropriate, that doesn't exclude anything by default?
