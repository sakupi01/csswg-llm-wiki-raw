---
number: 1016
title: "[css-conditional] Unbalanced brackets in @supports condition"
state: "closed"
labels: ["Closed Accepted as Obvious Bugfix", "Commenter Satisfied", "Needs Testcase (WPT)", "css-conditional-3"]
author: "upsuper"
created_at: "2017-02-10T03:18:10Z"
closed_at: "2020-09-28T22:41:00Z"
url: "https://github.com/w3c/csswg-drafts/issues/1016"
comments: 7
has_resolution: false
synced_at: "2026-07-04T04:43:52Z"
---

Gecko and Edge seem to reject (return false for `CSS.supports`) a condition if there is any unbalanced bracket, while Blink seems to accept it.

e.g. There are two conditions in Gecko's test suite:
* `not ({ something @with (unbalanced brackets })`
* `(color: green) or an-extension(that is [unbalanced)`

Gecko and Edge return `false` for both, while Blink returns `true` for both.

The spec doesn't seem to have anything about this. The question is, should it allow or disallow unbalanced brackets? Either way, it should probably be stated explicitly in the spec.

<!-- comment id=278897817 author=SimonSapin created=2017-02-10T09:32:03Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-278897817 -->
## @SimonSapin — 2017-02-10

[`general_enclosed`](https://drafts.csswg.org/css-conditional-3/#general_enclosed) links to [`any`](https://www.w3.org/TR/CSS21/syndata.html#tokenization) in CSS 2.1, which does require balancing. (Though [EOF with unclosed blocks is OK](https://www.w3.org/TR/CSS21/syndata.html#unexpected-eof).) So I think Blink is incorrect here.

By the way, maybe it should use the more general `value` instead of `any`.

If CSS Conditional syntax were to be re-expressed in terms of CSS Syntax 3 instead of the old CSS 2.1 grammar, it should probably use [`<any-value>`](https://drafts.csswg.org/css-syntax-3/#typedef-any-value) from CSS Values. (It corresponds to `(value | unused)*` in 2.1 terms.)

<!-- comment id=401979084 author=tabatkins created=2018-07-03T00:45:46Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-401979084 -->
## @tabatkins — 2018-07-03

Using Syntax 3 (and invoking the "parse as CSS" entry point) will auto-close any blocks. Using `<any-value>` (which I agree is correct there), however, will cause both of the OP's examples to be invalid, because of the unmatched } and ) tokens.

<!-- comment id=401979256 author=tabatkins created=2018-07-03T00:46:59Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-401979256 -->
## @tabatkins — 2018-07-03

In other words, `not ({ something @with (unbalanced brackets })` will parse into the equivalent of `not ({ something @with (unbalanced brackets })})`, with the first } being unmatched, while `(color: green) or an-extension(that is [unbalanced)` will parse into the equivalent of `(color: green) or an-extension(that is [unbalanced)])`, with the first ) after `unbalanced` being unamtched.

<!-- comment id=695782409 author=svgeesus created=2020-09-20T12:36:24Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-695782409 -->
## @svgeesus — 2020-09-20

The spec already seems to reference Syntax 3. The [only reference to CSS 2.1](https://drafts.csswg.org/css-conditional-3/#index-defined-elsewhere) is the definition of `width`. 

> general_enclosed links to any in CSS 2.1, which does require balancing.

It did in the [2013 CR](https://www.w3.org/TR/css3-conditional/#general_enclosed) but in [the current draft](https://drafts.csswg.org/css-conditional-3/#at-supports) `<general-enclosed>` [links to MQ5](https://drafts.csswg.org/mediaqueries-5/#typedef-general-enclosed) which does indeed use `<any-value`

So I think this issue is overtaken by events and can be closed.

<!-- comment id=695782481 author=svgeesus created=2020-09-20T12:37:16Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-695782481 -->
## @svgeesus — 2020-09-20

@upsuper do you agree?

<!-- comment id=695783337 author=upsuper created=2020-09-20T12:46:43Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-695783337 -->
## @upsuper — 2020-09-20

While I don't understand what do you mean by
> this issue is overtaken by events and can be closed.

As far as we are going to require it to be balanced I'm happy with it. Maybe it would be good to have a wpt and file a bug to those browsers after we close this.

<!-- comment id=700320579 author=tabatkins created=2020-09-28T22:39:37Z url=https://github.com/w3c/csswg-drafts/issues/1016#issuecomment-700320579 -->
## @tabatkins — 2020-09-28

Yeah, the specs now normatively reference Syntax 3 properly, so everything's well defined. *Missing* closing brackets will be auto-closed by the CSS parser (and thus are perfectly fine), but unbalanced brackets are invalid per the `<any-value>` production.

So `(color: green) or an-extension(that is [unclosed` is valid, `(color: green) or an-extension(that is [unbalanced)` is invalid.
