---
number: 1289
title: "[css-fonts-4] [varfont] Problem setting up a \"4-style family\" with variable fonts"
state: "open"
labels: ["css-fonts-5"]
author: "Lorp"
created_at: "2017-04-24T20:10:52Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/1289"
comments: 22
has_resolution: true
synced_at: "2026-07-04T04:40:16Z"
---

I’ve been trying to set up a typical family of 4 webfonts, but using a single variable font. Maybe I’m missing something, but there seems to be no easy way to specify the instances you want to be used when content calls for bold, italic and bold-italic members of the font family.

With normal non-variable fonts, I’d set up 4 separate @font-face requests, each with its own src property and each with its font-weight and font-style setting (when they were not default). So for the regular and bold styles I would use:

~~~~
@font-face {
        font-family: Gingham;
        src: url("Gingham-Regular.ttf");
    }

@font-face {
        font-family: Gingham;
        src: url("Gingham-Bold.ttf");
        font-weight: bold;
    }
~~~~

— and we’d get 2 more members of the family for the italics, each with updated src URL and font-style:italic property.

For variable, I would like to do something very similar, for example (note identical src URLs):

~~~~
@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf");
    }

@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf");
        font-weight: bold;
    }
~~~~

However this assumes I want to use the instances at font-weight 400 for the regular and font-weight 700 for the bold.

Let’s say I want my regular at font-weight 343 and my bold at font-weight 844. Of course I can set up CSS rule that, *for particular selectors*, obtains those instances. Yet they won’t be activated by any content that explicitly or implicitly calls for 'font-weight:normal' or 'font-weight:bold'.

What seems to be desirable, because of the vast amount of content that calls for it, is to decouple font-weight:normal from font-weight:400 and to decouple font-weight:bold from font-weight:700. So, is there a technique for this that I haven’t thought of?

https://drafts.csswg.org/css-fonts-4/#font-weight-prop

<!-- comment id=296815881 author=davelab6 created=2017-04-24T20:41:07Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-296815881 -->
## @davelab6 — 2017-04-24

I like this proposal; it seems to me that `font-weight: 700` and `font-weight: 750` would display different weights of a font with a weight axis; but `font-weight: bold` could display either weight, given this proposal, and `font-weight: 700` would always display the same weight. 

<!-- comment id=296877815 author=upsuper created=2017-04-25T02:50:26Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-296877815 -->
## @upsuper — 2017-04-25

I think this is a dupe of #528.

<!-- comment id=297084493 author=Lorp created=2017-04-25T16:20:51Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297084493 -->
## @Lorp — 2017-04-25

Back in October 2015, Jonathan Kew proposed an interesting solution for another case where multiple fonts exist in the same font file – TTC files.

https://lists.w3.org/Archives/Public/www-style/2015Oct/0158.html

His proposal was a new syntax using "#", the URL fragment identifier. In his example below it allows a fallback TTC font to be used for a regular and bold when the ideal fonts are not available:

~~~~
    @font-face {
      font-family: my-favorite-font;
      font-weight: 400;
      src: url(MyFavorite.woff), url(MyFallback.ttc#1);
    }
    @font-face {
      font-family: my-favorite-font;
      font-weight: 600;
      src: url(MyFavoriteBold.woff), url(MyFallback.ttc#3);
    }
~~~~

So we could potentially use similar syntax in selecting variations from within a variable font. Here is my CSS second code block above, rewritten using one potential syntax for variations:

~~~~
@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf#wght=343,wdth=130");
    }

@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf#wght=844,wdth=130");
        font-weight: bold;
    }
~~~~

Any thoughts?

<!-- comment id=297086521 author=twardoch created=2017-04-25T16:27:45Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297086521 -->
## @twardoch — 2017-04-25

I don't really like the idea of directly promoting the low-level axis tags to something as direct as being the URL fragment, without any mention of "font variation settings". 

The syntax "fontfile.ttf#AXIS=76.54" looks like a hack. I agree that the need is legitimate though. 

<!-- comment id=297095437 author=twardoch created=2017-04-25T16:58:53Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297095437 -->
## @twardoch — 2017-04-25

The [CSS Fonts 3](https://drafts.csswg.org/css-fonts/) spec includes a well-defined mechanism for dealing with pre-initialized settings for OpenType Layout features when defining a font via `@font-face`. The `font-feature-settings` keyword can be used as a **descriptor** within the `@font-face` rule, so it’s possible to specify: 

```
@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK.ttf");
        font-style: normal;
}

@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK.ttf");
        font-style: italic;
        font-feature-settings: "ital" 1; 
}
```

Here, upright and italic glyphs are embedded in a single font file, and the OpenType Layout feature `ital` is used to switch from the upright to italic forms. This syntax “maps” the `font-style: normal` CSS family variant to the default rendering of the font file, while it “maps” the `font-style: italic` CSS family variant to the same font file but with the OpenType Layout feature `ital` applied. 

Note that I don’t see `font-feature-settings` mentioned as a descriptor in the [CSS Fonts 4 draft](http://htmlpreview.github.io/?https://github.com/w3c/csswg-drafts/blob/master/css-fonts-4/Overview.bs) so I hope it’s not being removed. 

I think there is a legitimate case to also allow `font-variation-settings` as a descriptor within `@font-face`. This would allow for the following mechanism to work: 

```
@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK-VF.ttf");
        font-style: normal;
}

@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK-VF.ttf");
        font-style: italic;
        font-variation-settings: "ital" 1; 
}
```

The same mechanism should be used for the non-numeric `font-weight` properties, i.e. I should be allowed to say: 

```
@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK-VF.ttf");
        font-weight: normal;
        font-variation-settings: "wght" 321; 
}

@font-face {
        font-family: NotoSansCJK;
        src: url("NotoSansCJK-VF.ttf");
        font-weight: bold;
        font-variation-settings: "wght" 834; 
}
```

The numeric `font-weight` values would still be “hardlinked” to the numeric `wght` variation axis values, but the non-numeric `font-weight` values could be pre-mapped to arbitrary numeric `wght` variation axis if `font-variation-settings` is used as a descriptor within `@font-face`. 

<!-- comment id=297096646 author=Lorp created=2017-04-25T17:02:20Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297096646 -->
## @Lorp — 2017-04-25

Thanks Adam. This syntax is exactly what I tried first, and hoped would work. It’s interesting there is precedent for the mechanism, in ``font-feature-settings``.

<!-- comment id=297104423 author=twardoch created=2017-04-25T17:25:30Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297104423 -->
## @twardoch — 2017-04-25

Yes, it `font-feature-settings` was designed to be both a descriptor and a property. `font-variations-settings` could be the same. I think it should, since there will be many cases where the for example I might want to define my `font-style: italic` CSS family variant to be a specific instance of a variable font, using the `ital` axis, the `slnt` axis but also any other axis like `wght` for example. 

However, the behavior of the `font-weight` selector must be well-defined when it comes to interacting with `font-variation-settings`. For example, I may want to define my `font-weight: bold` CSS family variant to be an instance of my variable font that differs from the `font-weight: normal` CSS family variant on **something else than the `wght` value**. This would be especially important for "programmatic axes". A variable font may not even have the `wght` axis. 

```
@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-weight: normal;
        font-variation-settings: "GRAD" 123, "wdth" 56; 
}

@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-weight: bold;
        font-variation-settings: "GRAD" 185, "wdth" 73; 
}
```

should be possible to do. 

Note that in CSS Fonts 3, the `font-weight` descriptor is already restricted as it cannot use the values `bolder` and `lighter`, while the `font-weight` property can. The CSS Fonts 4 spec could say that if a `@font-face` rule uses a numeric `font-weight` descriptor, then the `"wght"` portion of the `font-variation-settings` descriptor must be ignored. 

BTW, using `font-variation-settings` as a descriptor would also be extremely important for `font-stretch`. I really want to be able to say: 

```
@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-stretch: condensed;
        font-variation-settings: "wdth" 56; 
}

@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-stretch: expanded;
        font-variation-settings: "wdth" 117; 
}
```

<!-- comment id=298148100 author=litherum created=2017-04-29T05:19:40Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-298148100 -->
## @litherum — 2017-04-29

We are discussing different points here.

**Point 1**
> I want my regular at font-weight 343 and my bold at font-weight 844.

This would require removing the statement that `normal` means `400` and `bold` means `700`, and instead would say that `normal` and `bold` are dependent on something in the `@font-face` block. This would mean that these keywords could have different meanings depending on which value in the `font-face` fallback list is used.

**Point 2**
> I think this is a dupe of #528.

It isn't. Redefining what the keywords mean is very different than using different values for font selection and application.

**Point 3**
> src: url("Gingham-Variable.ttf#wght=343,wdth=130");

This is a terrible idea. We already have `font-weight` and `font-stretch` descriptors in `@font-face` blocks.

**Point 4**

> I think there is a legitimate case to also allow `font-variation-settings` as a descriptor within `@font-face`.

Because of the clamping behavior of the existing descriptors in `@font-face`, it is already possible to apply a variation value with descriptors in an `@font-face` rule. Therefore, it seems reasonable to open it up to all axes and not just weight/width/slope.

<!-- comment id=298356252 author=svgeesus created=2017-05-01T15:35:55Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-298356252 -->
## @svgeesus — 2017-05-01

> in CSS Fonts 3, the font-weight descriptor is already restricted as it cannot use the values bolder and lighter, while the font-weight property can.

Naturally. The descriptor says _what the font supports_ while the property _requests a styling change_. `lighter` is clearly appropriate for a property (if one merely wants anything lighter, and not a specific value) and inappropriate for a descriptor where it would mean 'lighter than ... something`

> This is a terrible idea. We already have font-weight and font-stretch descriptors in @font-face blocks.

Agreed. We don't need fragments here, because descriptors already do this and are widely deployed.

<!-- comment id=298357676 author=svgeesus created=2017-05-01T15:42:44Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-298357676 -->
## @svgeesus — 2017-05-01

> Back in October 2015, Jonathan Kew proposed an interesting solution for another case where multiple fonts exist in the same font file – TTC files.
> https://lists.w3.org/Archives/Public/www-style/2015Oct/0158.html
> His proposal was a new syntax using "#", the URL fragment identifier. 

Using a fragment to select one of several distinct fonts in a collection is reasonable. The problem with the numeric fragment identifiers he proposed is fragility. Ken Lunde pointed out that for existing collections, when the collection is revised, fonts are inserted (not merely appended). So the meaning of a given numerical fragment would change from one revision to another, which is clearly inadvisable.

This is why, in RFC 8081, we changed to the fragment syntax suggested by Ken: the PostScript name (which is already used in CSS3 Fonts, for local fonts) is used as a fragment.

https://tools.ietf.org/html/rfc8081#section-4.4.4

<!-- comment id=298362321 author=litherum created=2017-05-01T16:04:17Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-298362321 -->
## @litherum — 2017-05-01

> The CSS Fonts 4 spec could say that if a `@font-face` rule uses a numeric `font-weight` descriptor, then the `"wght"` portion of the `font-variation-settings` descriptor must be ignored.

Where do you see this?

<!-- comment id=298365580 author=Lorp created=2017-05-01T16:19:34Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-298365580 -->
## @Lorp — 2017-05-01

Chris, @svgeesus, thanks for amplifying the reasons not to go for a url-fragment method.

Do you think the idea of using ``font-variation-settings`` as a descriptor is worth exploring?

<!-- comment id=335023461 author=jpamental created=2017-10-08T17:32:47Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-335023461 -->
## @jpamental — 2017-10-08

Seems like this discussion stalled. Has it moved elsewhere?

If we want to be able to define 'keyword norms' in the `@font-face` declaration, it seems like this syntax makes a lot of sense:

```
@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-stretch: normal;
        font-weight: normal,
        font-variation-settings: "wdth" 85, "wght" 327; 
}

@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-weight: bold,
        font-variation-settings: "wght" 572; 
}

@font-face {
        font-family: Hamstervar;
        src: url("Hamstervar-VF.ttf");
        font-stretch: expanded;
        font-weight: bold,
        font-variation-settings: "wdth" 112, "wght" 572; 
}

```
This would allow us to define what the default rendering of `font-family: Hamstervar` would look like, just as it would then allow us to define `font-weight bold` or `font-stretch: expanded; font-weight: bold`

I'd certainly like to see this more clearly defined so we know what we're working towards (and can then explain/teach it in our talks and workshops!)

<!-- comment id=409754784 author=svgeesus created=2018-08-01T23:10:43Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-409754784 -->
## @svgeesus — 2018-08-01

Note that this issue is related to https://github.com/w3c/csswg-drafts/issues/528 which we recently deferred to css-fonts-5

<!-- comment id=409757951 author=LeaVerou created=2018-08-01T23:29:44Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-409757951 -->
## @LeaVerou — 2018-08-01

This is something that is useful beyond variable fonts, e.g. I've often needed to define a Helvetica Neue with a `normal` weight of 300. So, I would be opposed to a syntax that is based on `font-variation-settings`, as the use cases are wider.
I think we need a way to define what the keywords resolve to, on a font-by-font basis, via a `@font-face` descriptor of some sort.
This will also be useful for defining fallbacks of local fonts that match the required font as closely as possible, things like that.

<!-- comment id=409760232 author=css-meeting-bot created=2018-08-01T23:44:00Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-409760232 resolution=true -->
## @css-meeting-bot — 2018-08-01

The Working Group just discussed `Problem setting up a "4-style family" with variable fonts`, and agreed to the following:

* `RESOLVED: defer this with issue #528 from last week`

<details><summary>The full IRC log of that discussion</summary>
&lt;dael> Topic: Problem setting up a "4-style family" with variable fonts<br>
&lt;dael> github: https://github.com/w3c/csswg-drafts/issues/1289<br>
&lt;dael> florian: I think it's a dup of something we deferred last week. It was...<br>
&lt;dael> chris: 528<br>
&lt;dael> florian: Yes<br>
&lt;dael> astearns: Objections to deferring this with issue #﻿528 from last week?<br>
&lt;dael> RESOLVED: defer this with issue #﻿528 from last week<br>
</details>

<!-- comment id=514227226 author=davelab6 created=2019-07-23T14:11:23Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-514227226 -->
## @davelab6 — 2019-07-23

Again I wonder if this discussion stalled and it moved elsewhere?

<!-- comment id=543943961 author=davelab6 created=2019-10-18T21:00:47Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-543943961 -->
## @davelab6 — 2019-10-18

>> `src: url("Gingham-Variable.ttf#wght=343,wdth=130");`
>
> This is a terrible idea. We already have font-weight and font-stretch descriptors in @font-face blocks.

However, we have no descriptor(s) for all the other axes. 

That must be addressed! :)

<!-- comment id=544130179 author=twardoch created=2019-10-19T11:12:12Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-544130179 -->
## @twardoch — 2019-10-19

The point of font-weight, font-stretch, font-style *descriptors* is that you can "initialize" a CSS font within the CSS font design space (which is controlled by those properties) from an arbitrary font resource.

Of course I want to be able to say: load this variable TTF at wght=650 and place it into the CSS font design space it as CSS font family "MyOwnFont" at font-weight: bold. 

That's the whole point. Just like CSS pixels are decoupled from real-life device pixels, CSS fonts are a virtual font system: 

You can define CSS fonts, that is, CSS font families and their members within the CSS family design space that is controlled using the font-weight, font-stretch and font-style  properties as well as Unicode ranges, and then you can "fill" those CSS fonts with font resources (multiple WOFFs or WOFF2s or SVG fonts or localfonts, picking the appropriate Unicode range and, what is also need, appropriate combination of font design axes).

That's how CSS works, and is supposed to work. 

The src: url("my.woff2#wght=650") describes the SOURCE of physical resources. The @font-face font-family, font-weight etc. describe the TARGET within the CSS font system. 

<!-- comment id=544137021 author=twardoch created=2019-10-19T12:06:36Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-544137021 -->
## @twardoch — 2019-10-19

Whether the URL fragment descriptor or the font-variation-settings secriptor is used — that is secondary. 

Today, I can load "lato-black.woff" (which has the internal OS/2.usWeightClass field, a precursor of the wght axis, set to 900) and map it to a CSS font via the font-family "MySans" and font-weight: 700 descriptors. 

Then I can load "roboto-light.woff2" that has usWeightClass 300 and map it to a CSS font via the font-family "MySans" and font-weight: 400 descriptors. 

@font-face is a mechanism to **CREATE** CSS fonts. 

By default, CSS font-weight is tied to the font resource’s OS/2.usWeightClass field. That works e.g. for local fonts.

But with @font-face, I can create CSS fonts that are invoked with a different font-weight numeric property than the OS/2.usWeightClass numeric value the font resource supplies. That works and is a cornerstone of the CSS fonts system. There is a gazillion of legitimate uses for it. 

If we cripple this ability for font resources where the built-in weight information is realized using a different technical implementation (fvar.wght rather than OS/2.usWeightClass) — that would be highly unfortunate.

Also, that would go against the principles of the CSS font system, which is format-agnostic. Three years ago, we didn't have OpenType Font Variations, 20 years ago, we didn't have OpenType.

The SFNT container in which OpenType and TrueType fonts are hosted is ancient, nearly 30 years old. It's roughly as old as GIF. And OpenType Font Variations are a slight update of TrueType GX Variations which are 25 years old, roughly as old as animated GIFs.

For images, several newer formats have emerged since, structured completely differently from GIF and free of GIF’s limitations. 

Fonts are a bit more durable, but I'm sure people here will agree that building foundations for a universal document styling format (which CSS is) by tying it strictly to a 30-year-old technology — that is equivalent to no better than animated PNG, but still limited to 16-bit glyph indices just like PNG is limited to 16-bit RGBA color, and that has been created before some of us were born — that is short-sighted.

Sure, font-variation-settings and font-feature-settings are **currently** implemented as format-specific nods, for convenience. And they're clearly labeled as such: low-level. 

But font-weight, font-stretch, font-style and font-size aren't format specific. They should remain so.

The fact that the numeric scale of the CSS font-weight property happens to mimic the scales of OS/2.usWeightClass and fvar.wght within TrueType/OpenType is a short-to-mid-term “hack”, a lucky coincidence that makes implementation easier. But it shouldn't be seen as a hard requirement.

Take CSS color "black". It currently is mapped to rgba(0, 0, 0, 1). But does it mean that it has to be? No. Right now CSS uses the RGBA color space, but who knows if in 10 years from now, black won't be overridable as cmyk(0, 0, 0, 1) or as pantone("shiny-black").

Same goes especially for font-weight *named* properties. It should be possible to map any fvar.wght setting from a loaded variable OpenType font resource to CSS font-weight "normal" and "bold" via a descriptor, and I think same should be possible to any font-weight numeric value (and only that value) — because that IS already possible with static fonts! 

Today I can map a static font to font-weight 405 and all other font-weight values will get some fallback behavior.

I should be able to load a variable font resource that provides fallback to the entire range of font-weight for a given family, but then load a static font resource and map it to font-weight 405 specifically. Or 400. Why? For example because I may have a superhinted static font resource for 400 or 405, but I want to use an unhinted variable font resource for all the other values. 

And similarly, I may want to load a variable font resource for the default weight mapping 1-1000 but then load a different (or the same) variable font resource with some custom numeric "wght" descriptor, and map it to a specific font-weight value. This is exactly what @font-face is for. 

Will it be possible that I get visual "surprises" if I use "lighter" or "bolder" then? Sure, it's possible — this is no different than creating a CSS font family and using two completely unrelated font resources for font-weight 400 and 700. 

But the fact that it's possible to get "surprising" results in CSS doesn't mean that CSS should disallow it. What's "surprising" or "bad" interms of visual, aesthetic results to some may be intentional for others. That's why we have designers, and we have engineers. 

We're creating a technical framework for design, called CSS. We're five or six people. That framework offers designers a full palette of colors and, in principle, allows them to render white text on white background that's "unreadable". 

Should we "patch" that technical framework because we "think" that it's "undesired" to have "unreadable" text? I don't think so. There are 4 billion web users and hundreds of thousands of designers. Let them decide what's best for the visual communication between them. The five or six of us won't be able to do it anyway, so let's concentrate on providing a well-functioning framework. 

<!-- comment id=544138754 author=twardoch created=2019-10-19T12:30:01Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-544138754 -->
## @twardoch — 2019-10-19

Similarly, I may want to load one (primary) variable font resource at one wght descriptor, and map it to the Latin, Cyrillic and Greek Unicode range of the CSS font that I'm creating, with font-family: "MyText" and font-weight: 400.

Then I may want to load another (secondary) variable font resource at a different wght descriptor, and map it to the Unicode ranges of Hebrew or Chinese or technical symbols (say, Google’s Noto Sans Symbols or Apple’s SF Symbols) of the very same CSS font. 

The visual weights of my primary and secondary font resources at the same wght value will almost always be **different** (that's the nature of type design), so I want to be able to create a composite CSS font from two or more variable font resources, each of them initialized at a different wght descriptor.

That (and sheer compression) is actually **the** primary and most compelling benefit of, and use case for, variable fonts with the wght axis on the web — the ability to have a small pool of variable fonts for the “long tail” of Unicode, and have them serve as a “close-to-perfect” fallback to the primary font that a CSS author chooses. 

This is really the scenario that prompted me in 2013 to propose that we resuscitate TrueType GX Variations, to spend weeks building prototype variable fonts in 2014-2015, to demo and evangelize them within the large companies (I built the first prototype of variable San Francisco and shared it with the Apple Fonts team). 

And I think this (and compression) is largely why the Google i18n team that worked on Noto (with @behdad at the forefront) spent the last few years improving fontTools, HarfBuzz and the Noto superfamily. 

The creative flexibilily that variable fonts offer, accessibility, animation — that comes on top, and is of interest to a much smaller community.

With the ability to custom-map fvar.wght to font-weight via descriptors (whatever their syntax may be), all these efforts can shine, and start bringing real benefits to hundreds of millions, or billions, of **normal** web users (readers). 

Without this ability, all the **wonderful** mechanisms of CSS font stack, fallback, Unicode range stuff, and the whole point of CSS font descriptors become rather useless when variable fonts become deployed, and the benefits of using variable fonts for compression and intelligent font fallback are extremely limited. 

And yes, it actually matters for weight **most**. 

<!-- comment id=544168951 author=Lorp created=2019-10-19T16:31:18Z url=https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-544168951 -->
## @Lorp — 2019-10-19

The issue that @twardoch describes well potentially arises in [FauxFoundry](https://www.fauxfoundry.com), the webfont service for parametric fallback fonts that @irenevl and I launched recently. Our current implementation supplies instantiated non-variable webfonts (the server uses fontTools to create them on-demand). CSS descriptors in each of the @font-face rules enable a family of fallback fonts to be set up correctly.

In many ways it would be preferable to supply the full 12-axis parametric font to all users. The same font could act as a fallback not only for all styles of the CSS font-family, but also work from browser cache on multiple websites for thousands of primary fonts. However the stylistic mapping problem prevents us deploying FauxFoundry in this way. Apart from the `wght` axis, there’s no CSS mechanism to inform the user agent of weight relationships within a variable font.

On this thread, 2½ years ago, I proposed [a solution using URL fragments](https://github.com/w3c/csswg-drafts/issues/1289#issuecomment-297084493), but it was inelegant, as @svgeesus noted. Yet I wonder if a better syntax would help.

Let’s use a simple example: a document set in Merriweather and Merriweather Bold, for which we wish to provide Greek fallback fonts parametrically. We drop the two Merriweather TTF files onto FauxFoundry, which yields two instances of FauxGrec. I list them here with their parametric axis settings:

* FauxGrec for Merriweather: Ytas=817 Ytde=-242 YTSE=8 Xtra=290 Yopq=54 Xopq=104 Ytra=567 YOSE=50 XOpq=107 XTra=355 YOpq=50 YTuc=743

* FauxGrec for Merriweather Bold: Ytas=817 Ytde=-242 YTSE=8 Xtra=230 Yopq=62 Xopq=140 Ytra=567 YOSE=61 XOpq=147 XTra=303 YOpq=61 YTuc=742

In the proposed syntax below, the src descriptor has a new ‘parameters’ term, that in this case supplies OpenType axis settings. (It’s delibrately not named ‘axes’ so it might potentially be used with other kinds of font resource.) Note the identical url in both rules.

```
@font-face {
	font-family: FauxGrec;
	src:url(fauxfoundry.com/FauxGrec.woff2) parameters("Ytas" 817, "Ytde" -242, "YTSE" 8, "Xtra" 290, "Yopq" 54, "Xopq" 104, "Ytra" 567, "YOSE" 50, "XOpq" 107, "XTra" 355, "YOpq" 50, "YTuc" 743);
	font-weight: normal;
}

@font-face {
	font-family: FauxGrec;
	src:url(fauxfoundry.com/FauxGrec.woff2) parameters("Ytas" 817, "Ytde" -242, "YTSE" 8, "Xtra" 230, "Yopq" 62, "Xopq" 140, "Ytra" 567, "YOSE" 61, "XOpq" 147, "XTra" 303, "YOpq" 61, "YTuc" 742);
	font-weight: bold;
}
```

Specifying `font-family: Merriweather, FauxGrec;` would then obtain the desired behaviour in both Regular and Bold (the same principle applies to italic and bold-italic).

And, rewriting the simple use case I originally mentioned:

```
@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf") parameters("wght" 343, "wdth" 130);
}

@font-face {
        font-family: Gingham;
        src: url("Gingham-Variable.ttf") parameters("wght" 844, "wdth" 141);
        font-weight: bold;
}
```

This would allow CSS authors to specify custom weights for Gingham Regular and Gingham Bold in variable fonts, just as they can already in non-variable fonts.

It would be good to have comments on this proposal.

Taking the idea further, I also wonder if there’s a good case for reusing [`font-named-instance`](https://www.w3.org/TR/css-fonts-4/#font-named-instance) as a descriptor for custom instance names. When CSS authors specify font-named-instance, the user agent would first search these custom names, and only if that search fails check the named instances in the font file. Webfont services might typically supply CSS that contained font-named-instance descriptors, along with their axis settings, taken from the font file.
