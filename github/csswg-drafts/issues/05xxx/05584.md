---
number: 5584
title: "[css-transforms] Computed value of the `rotate` property"
state: "open"
labels: ["css-transforms-2"]
author: "graouts"
created_at: "2020-10-05T18:10:03Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/5584"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:43:52Z"
---

The spec for the computed value of the [rotate](https://www.w3.org/TR/css-transforms-2/#propdef-rotate) property reads as follows:

> the keyword none, or an \<angle\> with an optional axis consisting of a list of three \<number\>s

My understanding of this text is that the only acceptable computed values for `rotate` would be:

- `none`
- `<angle>`
- `<angle> <number> <number> <number>`

That said, the text doesn't make it clear whether the "optional axis consisting of a list of three \<number\>s" should come before or after the angle.

I also find that browsers currently implementing this property (Firefox and Chrome) do not adhere to my reading of this spec. For instance, `rotate: y 45deg` yields `y 45deg` as the computed value instead of specifying the axis as three numbers.

I think we need clarification of what the computed value should be and then ensure we have the correct WPT coverage for this.

<!-- comment id=703905948 author=tabatkins created=2020-10-05T21:44:30Z url=https://github.com/w3c/csswg-drafts/issues/5584#issuecomment-703905948 -->
## @tabatkins — 2020-10-05

"Computed value" is one of the value computation stages; it describes a value in abstract terms, and is not directly relevant to the *serialization* of any particular value stage.

Serialization is defined by CSSOM, and generally by the "shortest serialization principle" and "in order according to the grammar".

The grammar for the property lists the axis information before the angle information in the relevant clause, so `y 45deg` is the correct serialization for that value - it's as short as possible (giving the axis by name, when possible, is preferable to giving it as three numbers), and the axis comes before the angle.

It's likely we do need better WPT coverage for this, tho. ^_^
