---
number: 5483
title: "[css-syntax-3] <percentage-token> type flag is never set"
state: "closed"
labels: ["Needs Edits", "Testing Unnecessary", "css-syntax-3"]
author: "Fayti1703"
created_at: "2020-08-28T15:27:31Z"
closed_at: "2023-05-22T21:41:33Z"
url: "https://github.com/w3c/csswg-drafts/issues/5483"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:44:30Z"
---

[Tokenization](https://drafts.csswg.org/css-syntax-3/#tokenization) defines the `type` flag on three tokens -- `<number-token>`, `<percentage-token>` and `<dimension-token>`, and assigns it a default value of "integer".

[Consume a numeric token](https://drafts.csswg.org/css-syntax-3/#consume-numeric-token) specifically sets the `type` flag on `<number-token>`s and `<dimension-token>`s to the one in the `number` returned from [Consume a number](https://drafts.csswg.org/css-syntax-3/#consume-a-number), using the wording
> create a &lt;xxx-token&gt; with the same value and type flag as *number*

However, this `type` flag isn't assigned for `<percentage-token>`s, as that just says
> create a &lt;percentage-token&gt; with the same value as *number*, and return it.

A strict reading of the spec suggests that therefore, a `<percentage-token>` always has a `type` of "integer", even if it's `value` is, for instance, `50.90`, which seems wrong.

<!-- comment id=682744263 author=tabatkins created=2020-08-28T15:47:05Z url=https://github.com/w3c/csswg-drafts/issues/5483#issuecomment-682744263 -->
## @tabatkins — 2020-08-28

Huh, that's a clear mistake. Thanks for the catch!

<!-- comment id=683161117 author=triple-underscore created=2020-08-28T21:48:20Z url=https://github.com/w3c/csswg-drafts/issues/5483#issuecomment-683161117 -->
## @triple-underscore — 2020-08-28

I don't think it was a mistake, because the type flag is not defined for `<percentage-token>`.
The spec states [in section 4](https://drafts.csswg.org/css-syntax-3/#tokenization):
> `<number-token>`, `<percentage-token>`, and `<dimension-token>` have a numeric value. `<number-token>` and `<dimension-token>` *additionally* have a type flag set to either "integer" or "number". The type flag defaults to "integer" if not otherwise set. `<dimension-token>` additionally have a unit composed of one or more code points.

(and there is no such kind of things defined for `<percentage-token>` anywhere else.)

<!-- comment id=687340625 author=tabatkins created=2020-09-04T19:33:11Z url=https://github.com/w3c/csswg-drafts/issues/5483#issuecomment-687340625 -->
## @tabatkins — 2020-09-04

Ah, you're right! Dang it, I *thought* it was weird I had such an obvious error! Okay, I'll revert, but put a note into the source to remind myself in the future.

<!-- comment id=996361460 author=fantasai created=2021-12-17T01:47:07Z url=https://github.com/w3c/csswg-drafts/issues/5483#issuecomment-996361460 -->
## @fantasai — 2021-12-17

Reverted the text; leaving this open for Tab to add his note later.
