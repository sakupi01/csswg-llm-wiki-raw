---
number: 5762
title: "[css-fonts-4] Animation type of font-size-adjust: none"
state: "closed"
labels: []
author: "cdoublev"
created_at: "2020-12-03T18:49:40Z"
closed_at: "2020-12-03T18:58:01Z"
url: "https://github.com/w3c/csswg-drafts/issues/5762"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:43:55Z"
---

Hello,

In the [draft of CSS Fonts 4](https://drafts.csswg.org/css-fonts-4/#font-size-adjust-prop), the animation type of `font-size-adjust` is `by computed value type` and the computed value is `a number or the keyword none`.

I believe other specs use `discrete` whenever at least one authorized value prevents interpolation. In Firefox, the animation type from `none` to `number` is discrete. 

To sum up, this is just a suggestion to change `by computed value` to `discrete`, for consistency with other specifications.

EDIT: actually I believe this should be the other way around (any property that can be computed to a value that can be interpolated) should be defined with `by computed value` and not with `discrete`, so I'm closing this issue, sorry.

EDIT 2: I was totally wrong because all property seems to have an animation type specified as "by computed value" when at least some computed value types allow interpolation, so it's definitively time to go to sleep, sorry again.

<!-- comment id=738236921 author=Loirooriol created=2020-12-03T19:16:42Z url=https://github.com/w3c/csswg-drafts/issues/5762#issuecomment-738236921 -->
## @Loirooriol — 2020-12-03

Note that "by computed value" already implies "discrete" when necessary. https://drafts.csswg.org/web-animations-1/#by-computed-value

> If the number of components or the types of corresponding components do not match, or if any component value uses discrete animation and the two corresponding values do not match, then the property values combine as discrete. 
