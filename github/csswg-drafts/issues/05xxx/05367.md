---
number: 5367
title: "[css-flexbox-1] flex-basis can not be smaller than min-content"
state: "closed"
labels: []
author: "vwkd"
created_at: "2020-07-28T10:01:50Z"
closed_at: "2020-07-28T11:49:00Z"
url: "https://github.com/w3c/csswg-drafts/issues/5367"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:43:51Z"
---

I can't find this behavior specified anywhere in the current [spec](https://drafts.csswg.org/css-flexbox/#valdef-flex-flex-basis). But all current versions of the major browsers (Chrome 84, Firefox 78, Safari 13) set the lower bound of `flex-basis` to `min-content`. This makes `flex-basis` behave different from `width`/`height`, since you can't create overflow by making the box smaller than its contents.

Is this a bug in the implementations or in the spec?

On the first glance it seems like a bug in the implementation, since there is no lower bound mentioned in the spec and it would be of little use, since the keyword `min-content` was made for exactly that.

But the implementation suddenly seems reasonable, given that the shorthand `flex` resets `flex-basis` to zero when omitted, which would make little sense if zero were not equivalent to `min-content`, although this is similarly nowhere specified.

> When omitted from the flex shorthand, its specified value is 0.

If the spec had instead defined the "reset value" of `flex-basis` in the `flex` shorthand as `min-content`, then a lower bound of `min-content` in `flex-basis` would reasonably be a bug in the implementation.

A minimal working example.

```html
<div class="container">
  <div class="mincontent">Lorem ipsum</div>
  <div class="smallerflexbasis">Lorem ipsum</div>
  <div class="smallerwidth">Lorem ipsum</div>
</div>
```

```css
.container {
  display: flex;
}

.container div {
  background-color: lightgrey;
  border: 1px solid black;
  margin: 0 10px;
  /* disable any flexible sizing */
  flex-grow: 0;
  flex-shrink: 0;
}

.mincontent {
  width: min-content;
}

.smallerflexbasis {
  flex-basis: 3ex;
}

.smallerwidth {
  width: 3ex;
}
```

<!-- comment id=664994026 author=vwkd created=2020-07-28T11:49:00Z url=https://github.com/w3c/csswg-drafts/issues/5367#issuecomment-664994026 -->
## @vwkd — 2020-07-28

Just found this small comment hidden at the end of [7.1.1](https://drafts.csswg.org/css-flexbox/#flex-common), which implicitly defines the lower bound of `flex-basis` as well. It could have been mentioned more explicitly, but the important thing is there is no bug in the spec or the implementation.

> By default, flex items won’t shrink below their minimum content size (the length of the longest word or fixed-size element). To change this, set the min-width or min-height property. (See § 4.5 Automatic Minimum Size of Flex Items.)
