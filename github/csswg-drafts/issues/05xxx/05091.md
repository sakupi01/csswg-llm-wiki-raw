---
number: 5091
title: "[css-transitions]/[css-animations] Proposal: Allow velocity values for transition-duration and animation-duration"
state: "open"
labels: ["css-animations-2", "css-transitions-2"]
author: "SetTrend"
created_at: "2020-05-19T22:43:13Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/5091"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:45:18Z"
---

Transitions and animations look weird on sibling elements having different sizes.

This is particularly true when, for example, one menu strip only contains a few items while the neighbour menu strip contains many.

See [this JSFiddle](https://jsfiddle.net/hjtyx3ng/1/) for a sample application: The same transition is applied to both menu items, but comparing the left transition to the right it's a rabbit versus a turtle.

That's why I'd like to propose to allow for providing transition and animation duration values in terms of velocity (i.e. length/time).

For instance, valid property values may be:

```css
transition-duration: 1em/185ms;
transition-duration: 2px/100ms;
animation-duration: 5em/1s;
```

The actual resulting speed value would be calculated from the length difference between the the element's currently used value and its target length value (If in doubt, please refer to [this comment](626#issuecomment-631070457) for in depth explanation on "currently used value" and "target length value"):

```js
var values = transitionDuration.split("/");
var speed = getSpeedInMs(values[1]) / getPixelsFromLength(values[0]);

speed *= Math.Abs(CurrentLengthInPx - TargetLengthInPx);
speed = Math.Floor(speed) + "ms";

Console.log(`target length shall be reached in ${speed}.`);
```

Velocity duration values could be applicable to all properties accepting length values.

<!-- comment id=631471785 author=Loirooriol created=2020-05-20T13:25:05Z url=https://github.com/w3c/csswg-drafts/issues/5091#issuecomment-631471785 -->
## @Loirooriol — 2020-05-20

You seem to assume that only lengths can be interpolated non-discretely. But you can also interpolate `<number>`, `<percentage>`, `<frequency>`, or even repeatable lists:

```css
#grid {
  display: grid;
  animation: anim 1s;
}
@keyframes anim {
  from { grid-template-columns: 0px 100px }
  to { grid-template-columns: 200px 50px }
}
```

the 1st track changes with an average velocity of `200px/1s`, while for the 2nd track it's `-50px/1s`.
How does this fit into your proposal?

<!-- comment id=632936205 author=SetTrend created=2020-05-22T22:50:39Z url=https://github.com/w3c/csswg-drafts/issues/5091#issuecomment-632936205 -->
## @SetTrend — 2020-05-22

Good question ...

I would suppose my proposal to apply to length values only. "Where applicable" would most closely describe it.

It's similar to `inset properties` being ignored when [`position: static`](https://www.w3.org/TR/css-position-3/#valdef-position-static). They are not applicable then.

<!-- comment id=632947487 author=Loirooriol created=2020-05-22T23:56:44Z url=https://github.com/w3c/csswg-drafts/issues/5091#issuecomment-632947487 -->
## @Loirooriol — 2020-05-22

But even with just lengths,

```css
#element {
  animation: anim 100px/1s;
}
@keyframes anim {
  from { width: 0px; height: 0px }
  to { width: 100px; height: 200px }
}
```

`width` wants to increase 100px with a speed of 100px/1s, so it wants the animation to last 1s.
`height` wants to increase 200px with a speed of 100px/1s, so it wants the animation to last 2s.

Then, either one of them wins (which one?) or I guess it's converted to something like

```css
#element {
  animation: anim 2s;
}
@keyframes anim {
  0% { width: 0px; height: 0px }
  50% { width: 100px }
  100% { height: 200px }
}
```

<!-- comment id=633227535 author=SetTrend created=2020-05-24T13:00:02Z url=https://github.com/w3c/csswg-drafts/issues/5091#issuecomment-633227535 -->
## @SetTrend — 2020-05-24

I would suspect them to be independant, because that would be the deterministic result of the rule statement given by the designer:

```css
#element {
  animation: anim1 1s, anim2 2s;
}

@keyframes anim1 {
  from { width: 0px }
  to { width: 100px }

@keyframes anim2 {
  from { height: 0px }
  to { height: 200px }
}
```

<!-- comment id=3214363876 author=bramus created=2025-08-22T13:20:23Z url=https://github.com/w3c/csswg-drafts/issues/5091#issuecomment-3214363876 -->
## @bramus — 2025-08-22

I have received several requests for this when talking to authors (e.g. https://bsky.app/profile/jpzwarte.bsky.social/post/3llnx3uavjc2i) and also have had this need myself.

Regarding the naming: velocity is a measured _speed_ in a certain _direction_ _([ref](https://www.britannica.com/story/whats-the-difference-between-speed-and-velocity))_ so I believe the thing that should be requested here is a way to set the _speed_, not the velocity.

On previous occasions I’ve chatted with @argyleink about this, and we were thinking of a `animation-speed` CSS property to set this. Something like `animation-speed: 500px/1s`. One of the immediate pieces of feedback I got on this from @flackr (and something that I also see mentioned in this thread) is that this would only be able to deal with things that are related to a distance.

My current thinking here is that the `animation-speed` could perhaps accept a dimension to deal with, which would make it derive things from there – e.g. `animation-speed: rotate 3deg/1s` would look at the rotation “distance” at the end state and then base the timing for the entire animation off of that. It’s still a rough idea, but perhaps it could inspire people to having better ideas for this.
