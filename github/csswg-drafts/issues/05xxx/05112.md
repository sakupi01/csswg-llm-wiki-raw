---
number: 5112
title: "[scroll-animations] Computed value of animation-timline"
state: "closed"
labels: ["scroll-animations-1"]
author: "andruud"
created_at: "2020-05-26T06:05:34Z"
closed_at: "2020-06-09T16:37:21Z"
url: "https://github.com/w3c/csswg-drafts/issues/5112"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:43:48Z"
---

The computed value of `animation-timeline` is currently "as specified". I think it should be as similar as possible to the computed value of `animation-name`, since it's identical (except `animation-timeline` also allows `auto`). It would appear `animation-name` isn't quite right in the spec either, as it doesn't handle string values vs. `<custom-ident>` correctly.

For `animation-name`, I think `<custom-ident>` is the canonical form, i.e. `animation-name:foo` rather than `animation-name:"foo"`, so probably the computed value for both properties should be a list, where each item is an identifier if it would match `<custom-ident>`, otherwise a string or a keyword.

In other words, if you specify `animation-name:"foo"` the computed value is `animation-name:foo`, but if you specify `animation-name:"none"`, the computed value is `animation-name:"none"`. And similarly for `animation-timeline`.

Alternatively make `animation-name` "as specified" too.

cc @majido 

<!-- comment id=633825742 author=andruud created=2020-05-26T06:08:18Z url=https://github.com/w3c/csswg-drafts/issues/5112#issuecomment-633825742 -->
## @andruud — 2020-05-26

"As specified, but `<string>` values are replaced with case-sensitive _css identifiers_ if they would match `<custom-ident>`"

Something like that, maybe.

<!-- comment id=636010558 author=majido created=2020-05-29T14:40:00Z url=https://github.com/w3c/csswg-drafts/issues/5112#issuecomment-636010558 -->
## @majido — 2020-05-29

I agree that we should match the `animation-name` semantic as close as possible here.

I don't have any particular opinion for the changes proposed for computed value of `animation-name`. Do you want to send a PR to css-animations to change the semantic of that and then we will update scroll-animations-1 to match it?

BTW, we should perhaps upstream `animation-timeline` property to css-animations. Feel free to own that as well. :)

<!-- comment id=641376403 author=majido created=2020-06-09T15:27:43Z url=https://github.com/w3c/csswg-drafts/issues/5112#issuecomment-641376403 -->
## @majido — 2020-06-09

I have upstreamed `animation-timeline` to css-animations-2 with its computed value matching `animation-name`. 

<!-- comment id=641420871 author=andruud created=2020-06-09T16:37:21Z url=https://github.com/w3c/csswg-drafts/issues/5112#issuecomment-641420871 -->
## @andruud — 2020-06-09

Thanks, then that's consistent at least. We can revisit how animation-name/timeline is computed in a different issue against css-animations-2.
