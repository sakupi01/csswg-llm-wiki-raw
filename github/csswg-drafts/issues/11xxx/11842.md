---
number: 11842
title: "[css-forms-1] For `control-value()` to be useful it needs a way to convert to other types."
state: "open"
labels: ["Needs Edits", "css-forms-1"]
author: "lukewarlow"
created_at: "2025-03-05T20:30:27Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11842"
comments: 12
has_resolution: true
synced_at: "2026-09-10T04:39:57Z"
---

Currently CSS Forms defines `control-value()` to be able to return a string or number, but for this to be useful we would need a way to convert to other CSS types (https://github.com/w3c/csswg-drafts/issues/6408 ?).

Take `<progress>` as an example, it has a position property (which is probably more useful than the value one for this CSS func as it handles the maths between value and max to determine actual progress value) between 0 and 1. This is useful for sizing the fill portion of the control, but for that we'd need a way to make it a (percentage) length instead.

```css
progress::fill {
    inline-size: convert(calc(content-value(type(<number>)) * 100), type(<length>), "%");
}
```

Alternatively if we could pass other types into `content-value()` and the browser could do the conversion for us that'd also work?

```css
progress::fill {
    inline-size: calc(content-value(type(<length>), "%") * 100);
}
```

Either of these could also help with styling the color input too (along with #11837 )

The default style could be something like:

```css
input[type=color]::color-swatch {
     background-color: content-value(type(<color>)); /* or convert(content-value(), type(<color>)) */
}
```

<!-- comment id=2702111112 author=lukewarlow created=2025-03-05T21:23:54Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2702111112 -->
## @lukewarlow — 2025-03-05

One possible way around this specifically for progress (and similar for meter( is to do something like below, because their values are encoded in the DOM.

```css
progress:not(:indeterminate)::fill {
    inline-size: calc(100% * calc(attr(value, 0) / attr(max, 100)));
}
```

<!-- comment id=2702135459 author=nt1m created=2025-03-05T21:37:19Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2702135459 -->
## @nt1m — 2025-03-05

The current draft includes a type argument, I suppose that argument could just be extended to whatever people find useful, you'd also be able to use it within calc().

I haven't really thought about it too much tbh

<!-- comment id=2702211969 author=SebastianZ created=2025-03-05T22:22:03Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2702211969 -->
## @SebastianZ — 2025-03-05

With explicit type conversion as discussed in #6408, I think your examples would look different.

```css
progress::fill {
    inline-size: convert(calc(convert(content-value(), <number>) * 100), <percentage>);
}
```

and

```css
input[type=color]::color-swatch {
     background-color: convert(content-value(), <color>);
}
```

Note that there is no need for a `type()` function. And as @nt1m pointed out in the previous comment, it also isn't part of the draft spec.
With the type attribute from the spec., those examples should look like this:

```css
progress::fill {
    inline-size: convert(calc(content-value(<number>) * 100), <percentage>);
}
```

and

```css
input[type=color]::color-swatch {
     background-color: content-value(<color>);
}
```

-----

Though maybe it is also possible to make the return value of the function context-sensitive, i.e. let it return a `<number>` value for `<progress>`, `<input type="range">` and other elements, a `<color>` value for `<input type="color">`, a `<url>` value for `<input type="url">`, etc.

With that, you could skip the explicit type definition/conversion for the `content-value()` function for the given use cases and write this:

```css
progress::fill {
    inline-size: convert(calc(content-value() * 100), <percentage>);
}
```

and

```css
input[type=color]::color-swatch {
     background-color: content-value();
}
```

Sebastian

<!-- comment id=2702222913 author=nt1m created=2025-03-05T22:29:05Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2702222913 -->
## @nt1m — 2025-03-05

I'd also be OK with just supporting `control-value(<percentage>)` directly fwiw, rather than having to deal with unit conversions.

<!-- comment id=2702299612 author=lukewarlow created=2025-03-05T23:20:05Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2702299612 -->
## @lukewarlow — 2025-03-05

Ah the `type()` bit was me guessing based on the new `attr()` function stuff.

<!-- comment id=2703613605 author=yisibl created=2025-03-06T11:44:10Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2703613605 -->
## @yisibl — 2025-03-06

I would prefer to see a generalized conversion function(https://github.com/w3c/csswg-drafts/issues/6408).

<!-- comment id=2705107420 author=tabatkins created=2025-03-06T22:44:55Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2705107420 -->
## @tabatkins — 2025-03-06

This function isn't doing parsing on a value; it shouldn't take a `<syntax>` argument. The form control *knows* what type of value it contains - a string, a number, a color, etc. It should just take a handful of keywords (probably just `number`, `percentage`, `color`, `string`?), and go IACVT if you request a type that the form control doesn't support.

* All controls would support `string`, resolving to the same string value you get from the `.value` property.
* `number` and `range` inputs support `number`.
* Progress/meter support `number` and `percentage` (with the default ranges being 0-1 and 0%-100%)
* Color inputs support `color`.
* Maybe `file` inputs can support an `image` keyword? There's many complaints thru the years of not being able to easily do "upload previews" on the web (tho today you can wrangle it with blob: urls)

<!-- comment id=2708166409 author=cdoublev created=2025-03-08T09:36:37Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2708166409 -->
## @cdoublev — 2025-03-08

Would it then be reasonable for it to take `<syntax-type-name>?` with `string` as its default?

<!-- comment id=2724473469 author=sorvell created=2025-03-14T11:59:00Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2724473469 -->
## @sorvell — 2025-03-14

Just want to confirm that for % we mean the percent of the value between min and max (e.g. `percentage = ((value - min) / (max - min)) * 100`).

<!-- comment id=2725542033 author=tabatkins created=2025-03-14T19:12:15Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-2725542033 -->
## @tabatkins — 2025-03-14

@cdoublev 
> Would it then be reasonable for it to take `<syntax-type-name>`? with string as its default?

No, because like I said, this is *not* doing CSS parsing on a value. The element has *already* parsed its value in an HTML-specified way, and is holding onto both a parsed and a string form of it. We just need to indicate which version of the value we want, in a way that tells CSS what that type is going to be at stylesheet parse time. Thus, the handful of specialized keywords.

@sorvell 
> Just want to confirm that for % we mean the percent of the value between min and max

Yup, that's what I meant too. 👍 

<!-- comment id=5527357884 author=LeaVerou created=2026-09-03T14:34:15Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-5527357884 -->
## @LeaVerou — 2026-09-03

I think we should just add a `convert()` function instead of adding ad hoc syntax in every function for this.

<!-- comment id=5528088083 author=css-meeting-bot created=2026-09-03T15:32:00Z url=https://github.com/w3c/csswg-drafts/issues/11842#issuecomment-5528088083 resolution=true -->
## @css-meeting-bot — 2026-09-03

The CSS Working Group just discussed ``[css-forms-1] For `control-value()` to be useful it needs a way to convert to other types.``, and agreed to the following:

* `RESOLVED: split control-value() into separate sub-type functions`
* `ACTION: Make a more detailed proposal`

<details><summary>The full IRC log of that discussion</summary>
&lt;keithamus> Luke: essentially, control-value (or whatever it ends up being called) ends up - depending on the type returned - what you can do with it in CSS differs. It makes sense for us to be able to define the types we want, like how attr() works.<br>
&lt;keithamus> Luke: for example styles to define the size of &lt;progress> or position of thumb in a range, or other examples... you can't do that in CSS. WIth this function you could maybe do it<br>
&lt;keithamus> Luke: but you'd need a way - like attr() - to treat it as e.g. a number or % in this case, or a fixed length value<br>
&lt;emilio> q+<br>
&lt;keithamus> Luke: I see lea mentioned adding a convert function instead of the ad-hoc syntax for every function. Might work in most case but potentially doesn't for control-value; depending on what you ask for and depending on what we specify it might change the actual value not just the way it is interpreted.<br>
&lt;keithamus> Luke: value on its own gives you some info, e.g. &lt;progress> is relative to the other attribute, e.g. 70 of you value might not be 70%.<br>
&lt;keithamus> Luke: that's the long and short of it<br>
&lt;astearns> ack emilio<br>
&lt;keithamus> emilio: so is the idea that this will be an arbitrary subsitution function like attr()? Or something else? If it works like attr() it makes sense to be consistent with syntax and so on<br>
&lt;keithamus> emilio: the first time I've heard about this function so, sorry. It seems there's a bunch of interesting... is the use case around progress and range and so on? Inputs with a numeric value make sense but a value of a date input can be a lot of different things, some we might not want to expose, they may depend on e.g. locale.<br>
&lt;keithamus> emilio: If you want it for display you want a localised version not a epoch relative number<br>
&lt;keithamus> Luke: good point. I think this is the bit thought about this least; the idea of having this function makes sense but perhaps just for numerical types: range, progress.<br>
&lt;keithamus> Luke: file input could give you file label as a string but that's got problems on its own<br>
&lt;emilio> q+<br>
&lt;keithamus> Luke: as for how it behaves as a function, I don't know much about how CSS functions work - I imagine similar to the new attr() but internally rather than looking up the attribute<br>
&lt;astearns> ack fantasai<br>
&lt;keithamus> fantasai: I think similar to attr() makes sense, similar pattern. They do similar things, just happens that one is internal not syntactically represented.<br>
&lt;keithamus> fantasai: emilio made a good point about how it would need to be presented for a number of different types. The most important type for it to retrieve is a number for numeric types. That should be the default.<br>
&lt;keithamus> fantasai: or the fallback value at CVT<br>
&lt;astearns> ack emilio<br>
&lt;keithamus> emilio: this would be a lot easier to scope if we define the - the numeric ones are straightforward, but it's all the others: passwords? checkboxes value? what about indeterminate?<br>
&lt;Luke> q+<br>
&lt;keithamus> emilio: would it be useful to split this into... it seems to be while nice in theory it has a lot of very weird edge cases, we'll be discussing it forever. Can we de-scope it? numeric-control-value()? or not even a function? That unblocks the most important use cases.<br>
&lt;astearns> ack Luke<br>
&lt;keithamus> Luke: There are separate issues, but it comes up. Which form controls should use this. Also FACE, being able to access their value in styles. Apparently it's a pattern to set it to a custom property to use it, but access to this would obviate that.<br>
&lt;keithamus> Luke: I would be in favour if the use cases pointed to something more narrow, but I guess I want to make sure we aren't cutting off potential use cases like FACE, if we just make it work for numeric.<br>
&lt;astearns> q+<br>
&lt;keithamus> Luke: raw strings... you might want to display it somehow... Custom Elements the main one vs built ins<br>
&lt;emilio> q+<br>
&lt;keithamus> astearns: Whether we have a single generic or a number of specific functions for various types - is it the case that if we have multiple functions, they'd all have to be callable on every element and some would just not work? That seems not great. I'd rather have a single function rather than a bunch.<br>
&lt;astearns> ack astearns<br>
&lt;keithamus> astearns: calling it with the wrong parameter you just get nothing<br>
&lt;keithamus> emilio: I mean, if you call this on a &lt;div> you're not getting a value<br>
&lt;keithamus> emilio: I'm happy dropping 5% of use cases if it gets us to 95% years earlier.<br>
&lt;keithamus> emilio: but syntax wise you could imagine like we did for ::picker() - only allow some to begin with, but eventually ::picker(color) will work, we don't need to just block on select.<br>
&lt;emilio> ack emilio<br>
&lt;astearns> ack emilio<br>
&lt;astearns> ack fantasai<br>
&lt;keithamus> fantasai: One advantage of splitting it the way emilio suggests; it's more obvious that its not going to work, a little bit higher level; "dont ask for a numeric value on a text input". From an ergonomic perspective that might be helpful<br>
&lt;keithamus> fantasai: the other thing is it lets us customise the arguments for each one - e.g. dates, we dont want to return ISO string, but date-control-value() can have a whole syntax space for controlling the value<br>
&lt;masonf> q+<br>
&lt;keithamus> fantasai: numeric-control-value() could incorporate min/max or so. Use the argument space to customise what we're getting out of it. emilio's idea has a lot of merit here.<br>
&lt;TabAtkins> Namespace it with a prefix if possible - control-number(), etc<br>
&lt;keithamus> fantasai: and it let's us do it piecemeal. Not painting us into a syntactic corner.<br>
&lt;TabAtkins> Also yes I agree this sounds good<br>
&lt;astearns> ack masonf<br>
&lt;keithamus> astearns: and not having to figure out all the date formatting arguments now will be a big win<br>
&lt;keithamus> masonf: people will want numeric-control-value() on colours, date as timestamp, etc.<br>
&lt;emilio> `.valueAsNumber`<br>
&lt;keithamus> fantasai: for css they're unlikely to want milliseconds since epoch, in JS maybe, but in css it's not helpful<br>
&lt;keithamus> masonf: so we wont support it explictly?<br>
&lt;keithamus> fantasai: yeah. Then for color we can use hex if you want hex.<br>
&lt;keithamus> emilio: input has `.valueAsNumber`, we can define it to returns whatever valueAsNumber returns. It means we dont need to make a bunch of random choices that are already made.<br>
&lt;keithamus> astearns: this issue is really about converting values. Maybe we should keep this and open up a separate one about breaking up control-value() into typed versions, or should we continue this discussion in this issue?<br>
&lt;keithamus> fantasai: the discussion is mixed up in this issue.<br>
&lt;masonf> valueAsNumber returns time since epoch<br>
&lt;keithamus> fantasai: I agree with lea if we want conversion doing something generic makes more sense.<br>
&lt;keithamus> TabAtkins: plan for converting to types is to lean on existing machinery for attr, out pops a value of whatever you requested.<br>
&lt;keithamus> fantasai: if you want to convert a number to a %, you can do that with calc.<br>
&lt;keithamus> fantasai: "numbers in this range convert to red" - that's an if(). String to ident is the ident() function. So a bunch of these exist, we just need to know what's missing. The ability to pull different data types out is the core of this issue, it just needs a better title.<br>
&lt;keithamus> astearns: so can we resolve on anything?<br>
&lt;keithamus> fantasai: convinced by emilio's argument to sub-type it. Maybe we go with that? Split?<br>
&lt;keithamus> PROPOSED: split control-value() into separate sub-type functions<br>
&lt;masonf> +1<br>
&lt;Luke> +1<br>
&lt;fantasai> PROPOSED: Split control-value() into subtyped functions e.g. control-number()<br>
&lt;keithamus> emilio: doesn't even need to be a separate function, could sub-type on first arg.<br>
&lt;keithamus> fantasai: ergonomic reasons it's better to do different functions<br>
&lt;keithamus> masonf: easier for @supports?<br>
&lt;keithamus> emilio: not necessarily, but whatever fantasai thinks is more convenient is more convenient<br>
&lt;keithamus> RESOLVED: split control-value() into separate sub-type functions<br>
&lt;keithamus> astearns: we'll come back to type conversion stuff and exactly what each function does. Let's move on<br>
&lt;fantasai> ACTION: Make a more detailed proposal<br>
&lt;vmpstr> https://github.com/w3c/csswg-drafts/issues/12750<br>
</details>
