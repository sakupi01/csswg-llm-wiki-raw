---
number: 11644
title: "[scroll-animations] Inset parameter of ViewTimelineOptions should be optional"
state: "open"
labels: ["Closed Rejected as Wontfix by CSSWG Resolution", "Commenter Timed Out (Assumed Satisfied)", "Needs Testcase (WPT)", "scroll-animations-1"]
author: "nmoucht"
created_at: "2025-02-04T02:51:36Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11644"
comments: 3
has_resolution: true
synced_at: "2026-07-16T00:51:21Z"
---

Based on the test `view-timelines/view-timeline-snapport.html`, it is expected that `ViewTimelines` without a user specified timeline inset should avoid using the scroll-padding property as the inset. However, inset is defined as below in [ViewTimelineOptions](https://drafts.csswg.org/scroll-animations-1/#viewtimeline-interface):

> ([DOMString](https://webidl.spec.whatwg.org/#idl-DOMString) or [sequence](https://webidl.spec.whatwg.org/#idl-sequence)<([CSSNumericValue](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue) or [CSSKeywordValue](https://drafts.css-houdini.org/css-typed-om-1/#csskeywordvalue))>) inset = "auto";

This definition doesn't make it possible to differentiate the user explicitly setting inset as auto vs the default value. My suggestion is to define it as an optional like below, so we can differentiate between these two cases:

> ([DOMString](https://webidl.spec.whatwg.org/#idl-DOMString)? or [sequence](https://webidl.spec.whatwg.org/#idl-sequence)<([CSSNumericValue](https://drafts.css-houdini.org/css-typed-om-1/#cssnumericvalue) or [CSSKeywordValue](https://drafts.css-houdini.org/css-typed-om-1/#csskeywordvalue))>) inset;

<!-- comment id=2632703927 author=nmoucht created=2025-02-04T02:55:17Z url=https://github.com/w3c/csswg-drafts/issues/11644#issuecomment-2632703927 -->
## @nmoucht — 2025-02-04

Cc @flackr, @kevers-google, @andruud

<!-- comment id=4695569902 author=BorisChiou created=2026-06-12T21:23:28Z url=https://github.com/w3c/csswg-drafts/issues/11644#issuecomment-4695569902 -->
## @BorisChiou — 2026-06-12

Put this in agenda because this blocks the interop. (I thought we should treat it `auto` for default but the test doesn't agree with this). The spec doesn't mention this case and doesn't say we shouldn't use scroll-padding, so it'd be better to address this case explicitly (because this is different from `view-timeline-inset` property).

<!-- comment id=4982811693 author=css-meeting-bot created=2026-07-15T16:15:46Z url=https://github.com/w3c/csswg-drafts/issues/11644#issuecomment-4982811693 resolution=true -->
## @css-meeting-bot — 2026-07-15

The CSS Working Group just discussed `[scroll-animations] Inset parameter of ViewTimelineOptions should be optional`, and agreed to the following:

* `RESOLVED: spec is right, fix the tests`
* `ACTION: Add a test for empty array`

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> emilio: Spec says default is auto<br>
&lt;emilio> ... but chrome uses = []<br>
&lt;emilio> flackr: I think there's no issue with using `auto`<br>
&lt;emilio> graouts: I think the main issue is that auto has the side effect of using scroll-padding<br>
&lt;dshin-moz> FWIW the change was made thru this PR: https://chromium-review.googlesource.com/c/chromium/src/+/3898695<br>
&lt;emilio> ... and there's no way to distinguish explicit from non-explicit auto<br>
&lt;emilio> https://source.chromium.org/chromium/chromium/src/+/main:third_party/blink/renderer/core/animation/view_timeline_options.idl;l=12;drc=4eac913913af0d1865ea2cdce0018d36a1d2322b fwiw<br>
&lt;emilio> flackr: I think it's an edge case enough to have scroll-padding set<br>
&lt;emilio> ... probably not an issue?<br>
&lt;emilio> graouts: This only applies to usage via the API and not CSS<br>
&lt;emilio> flackr: I don't think you shouldn't differentiate between empty value and auto<br>
&lt;emilio> ... we do that elsewhere<br>
&lt;emilio> graouts: So you'd need explicit [] to opt out of scroll-padding<br>
&lt;emilio> emilio: yes<br>
&lt;another-astearns> ack fantasai<br>
&lt;emilio> fantasai: default value of view-timeline-inset is 0<br>
&lt;emilio> s/0/auto<br>
&lt;dshin-moz> q+<br>
&lt;emilio> ... so it makes sense for it to be the default on the API<br>
&lt;fantasai> https://drafts.csswg.org/scroll-animations-1/#view-timeline-inset<br>
&lt;emilio> graouts: mdn differs from the spec<br>
&lt;another-astearns> ack dshin-moz<br>
&lt;fantasai> ??? MDN says initial value is 'auto' https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-timeline-inset<br>
&lt;emilio> dshin-moz: we were confused internally because auto is by default because the tests assume empty<br>
&lt;emilio> ... it seems chrome folks considered opting into snapport an optional behavior<br>
&lt;emilio> ... see the PR linked above<br>
&lt;emilio> fantasai: the default value is auto, if you set it to an invalid value (or no value) it should fall back to its default<br>
&lt;flackr> +1<br>
&lt;fantasai> emilio: [missed]<br>
&lt;fantasai> scribe+<br>
&lt;fantasai> emilio: An array is technically valid<br>
&lt;estelle> MDN says initial value is 'auto' b/c the spec says it's auto<br>
&lt;emilio> PROPOSED RESOLUTION: spec is right, fix the tests<br>
&lt;emilio> flackr: I think this is enough of an edge case<br>
&lt;emilio> fantasai: if an empty array is explicitly valid is it 0 or auto?<br>
&lt;weinig> The says "If it has zero values or more than two values, or if it contains a CSSKeywordValue whose value is not "auto", throw a TypeError."<br>
&lt;emilio> astearns: are there tests for that?<br>
&lt;emilio> flackr: the spec is explicit<br>
&lt;weinig> s/The says/The spec says/<br>
&lt;emilio> RESOLVED: spec is right, fix the tests<br>
&lt;emilio> graouts: It's part of interop, can fix the test asap<br>
&lt;fantasai> ACTION: Add a test for empty array<br>
&lt;fantasai> s/explicit/explicit that an empty array is a type error (like 3 values)/<br>
&lt;emilio> emilio: Empty array should probably be tested, since chrome presumably would fail that<br>
</details>
