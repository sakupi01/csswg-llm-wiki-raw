---
number: 11785
title: "[css-view-transitions] view-transition-* does not accept CSS variables"
state: "closed"
labels: ["Closed as Question Answered", "css-view-transitions-2"]
author: "tounsoo"
created_at: "2025-02-26T18:52:29Z"
closed_at: "2025-03-05T10:03:39Z"
url: "https://github.com/w3c/csswg-drafts/issues/11785"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:45:07Z"
---

As I am building libraries, I have a case where a component can be nested, an example would be Tabs.

In that case, I need the `::view-transition-class` and `::view-transition-new` etc to be different between the nested so that transition is isolated for each. I believe using CSS var would be the cleanest way to do this but it doesn't except them.

Ex)
```
.tab {
  // other styles
  view-transition-class: var(--tab-unique-name);
}
::view-transition-new(var(--tab-unique-name)) {
   // some animation
}

<!-- comment id=2697207104 author=bramus created=2025-03-04T11:29:32Z url=https://github.com/w3c/csswg-drafts/issues/11785#issuecomment-2697207104 -->
## @bramus — 2025-03-04

I believe the solution you are looking for is [Scoped Transitions](https://github.com/WICG/view-transitions/blob/main/scoped-transitions.md), which can be used to trigger transitions on only a subtree of the DOM.

```js
document.querySelector('.tabs').startViewTransition(() => {
  // …
});
```

This feature is currently WIP.

Also, there are some issues with the code example you give:

- You are using `--tab-unique-name` which hints that the name should be unique. The use-case for `view-transition-class` is that you can tag multiple elements with a _generic_ name. The unique naming is reserved for `view-transition-name`.
- You are using custom properties in selectors. This is not possible in CSS.

<!-- comment id=2699437981 author=tounsoo created=2025-03-05T01:01:07Z url=https://github.com/w3c/csswg-drafts/issues/11785#issuecomment-2699437981 -->
## @tounsoo — 2025-03-05

Gotcha.

The use case I have is a nested Tab(Group) component and that should work with the Scoped Transition WIP as I can add a unique set of selector per Tab component instances.

As for the `--tab-unique-name`, I was referring to unique name for tab group not each tab button.

EDIT: Actually, I'm not sure as I read more about the scoped transition. Since my use case in rendered component would look like below:
```
<div class="tabs">
  <div role="tablist" aria-label="set01>
    <button role="tab" id="set01-t-01" aria-controls="set01-p-01" aria-selected="true">001</TabButton>
    <button role="tab" id="set01-t-02" aria-controls="set01-p-02">001</TabButton>
  </div>
  <div role="tabpanel" id="set01-p-01" aria-labelledby="set01-t-01">content: set 01 panel 01</TabPanel>
  <div role="tabpanel" id="set01-p-02" aria-labelledby="set01-t-02">
    content: set 01 panel 02
    <div class="tabs">
      <div role="tablist" aria-label="set02">
        <button role="tab" id="set02-t-01" aria-controls="set02-p-01" aria-selected="true">001</TabButton>
        <button role="tab" id="set02-t-02" aria-controls="set02-p-02">001</TabButton>
      </div>
      <div role="tabpanel" id="set02-p-01" aria-labelledby="set01-t-01">content: set 02 panel 01</TabPanel>
      <div role="tabpanel" id="set02-p-02" aria-labelledby="set01-t-02">content: set 02 panel 02</TabPanel>
    </div>
</div>
```

The problem I had was that transition from `set02` was also affecting `set01` since it uses the same class.

<!-- comment id=2700271012 author=bramus created=2025-03-05T08:57:08Z url=https://github.com/w3c/csswg-drafts/issues/11785#issuecomment-2700271012 -->
## @bramus — 2025-03-05

I see how nesting and Scoped Transitions would work when invoking startViewTransition on the inner `.tabs` component, but not on the outer `.tabs` component. When invoking `$outerTabs.startViewTransition()` the contents from `$innerTabs` would also participate in that transition.

A workaround would be to set all `view-transition-name` values for the `$innerTabs` to `null` upon invoking `$outerTabs.startViewTransition()`. Ideally, there should be a way to automatically limit the reach.

If OK with you I’ll close this issue here and mark it as “Question Answered”, but I’ll also file a new issue about limiting the reach of Scoped Transitions.
