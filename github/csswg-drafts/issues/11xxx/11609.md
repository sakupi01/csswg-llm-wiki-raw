---
number: 11609
title: "[css-borders-4] Resolve on range for `superellipse` parameters"
state: "closed"
labels: ["css-borders-4"]
author: "noamr"
created_at: "2025-01-30T15:36:33Z"
closed_at: "2025-05-06T15:31:21Z"
url: "https://github.com/w3c/csswg-drafts/issues/11609"
comments: 11
has_resolution: true
synced_at: "2026-07-04T04:45:12Z"
---

Follow up on #10993 

The initial draft for `corner-shape` and `superellipse` uses the following parameters:
- 0 is notch
- 0.5 is scoop
- 1 is bevel
- 2 is circle
- 4 is squircle
- 1 is straight

We can also use the `log2` version of this:
notch -> -inf
scoop -> -1
bevel -> 0
round -> 1
squircle -> 2
straight -> inf

This would have an effect on the formula in #11608 as well.

<!-- comment id=2624861326 author=smfr created=2025-01-30T15:46:04Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2624861326 -->
## @smfr — 2025-01-30

`inf` values concern me because they aren't good endpoints for interpolation.

<!-- comment id=2624866014 author=smfr created=2025-01-30T15:47:41Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2624866014 -->
## @smfr — 2025-01-30

Actually, that makes me wonder if we need more detail on how the superellipse is computed, given a corner size (in pixels). It would be weird for a corner to appear sharp on a small box, but still slightly rounded on a large box because of how the math works out. We need math that is size-independent.

<!-- comment id=2624889357 author=noamr created=2025-01-30T15:56:12Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2624889357 -->
## @noamr — 2025-01-30

> `inf` values concern me because they aren't good endpoints for interpolation.

This is addressed in the draft

<!-- comment id=2624928356 author=noamr created=2025-01-30T16:10:39Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2624928356 -->
## @noamr — 2025-01-30

> Actually, that makes me wonder if we need more detail on how the superellipse is computed, given a corner size (in pixels). It would be weird for a corner to appear sharp on a small box, but still slightly rounded on a large box because of how the math works out. We need math that is size-independent.

You mean when we describe it as bezier curves? For the cartesian equation this shouldn't matter.

<!-- comment id=2625180031 author=tabatkins created=2025-01-30T17:48:49Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2625180031 -->
## @tabatkins — 2025-01-30

I *strongly* prefer the log2-scaled version of the superellipse parameter, so the symmetric inny and outy shapes use the same parameters, just negated; in general, it makes the "stuff poking out more than angle" and "stuff poking in more than angle" have the same value range and same value meaning, which is a lot easier to understand and use imo.

It also happens to make the most significant values even simpler - 0, 1, 2, inf.

> Actually, that makes me wonder if we need more detail on how the superellipse is computed, given a corner size (in pixels). It would be weird for a corner to appear sharp on a small box, but still slightly rounded on a large box because of how the math works out. We need math that is size-independent.

You get a (mathematically) sharp corner at infinity and -infinity (or infinity and 0, in the current spec). And that's stable regardless of box size. Any value *short* of those endpoints will produce a curve with some extremely high curvature, so at a sufficiently large border-radius it will indeed appear curved rather than sharp.

<!-- comment id=2625187326 author=noamr created=2025-01-30T17:52:26Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2625187326 -->
## @noamr — 2025-01-30

> I _strongly_ prefer the log2-scaled version of the superellipse parameter, so the symmetric inny and outy shapes use the same parameters, just negated; in general, it makes the "stuff poking out more than angle" and "stuff poking in more than angle" have the same value range and same value meaning, which is a lot easier to understand and use imo.

Thanks! I tend to agree.

<!-- comment id=2625399236 author=noamr created=2025-01-30T19:32:45Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2625399236 -->
## @noamr — 2025-01-30

@smfr with my proposed formula for interpolation applied on these values it would look like this:
| Name  | `superellipse()` value | 0..1 value for interpolation |
|--------| ----------------------| --------------------------| 
| `notch` | `-Infinity` | 0 |
| `scoop` | `-1` | 0.25 |
|  `bevel` | 0 | 0.5 |
| `round` | 1 | 0.7 |
| `squircle` | 2 | 0.84 |
| `straight` | `Infinity` | 1 |

The formula is intersecting the diagonal with the superellipse function, which resolves to this (given `n` as the arg):
```
// -Infinity => 0, Infinity -> 1, everything else given `n` as the `superellipse()` arg:
0.5^(1 / (2^n))
``` 

* there could be a way to simplify this formula further, my logarithmic math is rusty

<!-- comment id=2628310436 author=noamr created=2025-01-31T20:06:31Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2628310436 -->
## @noamr — 2025-01-31

An advantage for using the current spec'ed version (without `log2`) is that it's the commonly used exponent value for superellipse. So if designers say "this is a superellipse with k=5" you don't need to remember that in CSS this would be `log2(5)`.

<!-- comment id=2764780787 author=smfr created=2025-03-30T22:39:29Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2764780787 -->
## @smfr — 2025-03-30

The spec needs to say how to map the 0-1 range to a k value, so you need to describe the inverse of `0.5^(1 / (2^n))`, which is an ugly thing with lots of nested `ln()`; in JS;
```
            const log2 = Math.log(2);
            return (Math.log(- 1 / Math.log(fraction)) + Math.log(log2)) / log2;
```

And this is actually mapping `p` -> `s`, not `p` -> `k`.

I think the animation logic also needs to preserve symmetry by saying that if the fraction `p` < 0.5, you compute for `1-p` and negate the result (or somehow otherwise agree with the spec text about how to render the curve).

<!-- comment id=2765340116 author=noamr created=2025-03-31T07:20:03Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2765340116 -->
## @noamr — 2025-03-31

> The spec needs to say how to map the 0-1 range to a k value, so you need to describe the inverse of `0.5^(1 / (2^n))`, which is an ugly thing with lots of nested `ln()`; in JS;
> 
> ```
>             const log2 = Math.log(2);
>             return (Math.log(- 1 / Math.log(fraction)) + Math.log(log2)) / log2;
> ```
> 
> And this is actually mapping `p` -> `s`, not `p` -> `k`.

Sure, the spec currently specifies this for the regular k, it's not too difficult to add the log(2) bit.
Something along the lines of this in JS:
```js
function s_for_v(v) {
    const is_concave = v < 0.5;
    const k = ln(0.5) / ln(is_concave ? 1 - v : v);
    const s = Math.log2(k);
    return is_concave ? -s : s;
}
```

> 
> I think the animation logic also needs to preserve symmetry by saying that if the fraction `p` < 0.5, you compute for `1-p` and negate the result (or somehow otherwise agree with the spec text about how to render the curve).

Agreed about symmetry.

<!-- comment id=2769712709 author=css-meeting-bot created=2025-04-01T15:13:56Z url=https://github.com/w3c/csswg-drafts/issues/11609#issuecomment-2769712709 resolution=true -->
## @css-meeting-bot — 2025-04-01

The CSS Working Group just discussed ``[css-borders-4] Resolve on range for `superellipse` parameters``, and agreed to the following:

* `RESOLVED: Use the log2 range for the superellipse parameter`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> noamr: this and the next issue will resolve togheter<br>
&lt;TabAtkins> noamr: smfr and i have been working hard on the details for corner-shape. several questions to resolve<br>
&lt;TabAtkins> noamr: one is about the superellipse paramter<br>
&lt;TabAtkins> noamr: as you might recall, we have corner-shape keywords, but can also give it a number via a super-ellipse function<br>
&lt;TabAtkins> noamr: in the spec righ tnow, the number gives the exponent of the curvature<br>
&lt;TabAtkins> noamr: there was a proposal to use the log2 of that<br>
&lt;TabAtkins> noamr: if i go all the way to concave, k=0, log2=-inf<br>
&lt;TabAtkins> noamr: middle, bevel, k=1, log2=0<br>
&lt;TabAtkins> noamr: full sharp convex, k=inf, log=inf<br>
&lt;TabAtkins> noamr: round, k=2, log2=1<br>
&lt;TabAtkins> noamr: squircle, k=4, log2=2<br>
&lt;TabAtkins> noamr: there's a table in the issue<br>
&lt;TabAtkins> noamr: so the log2 version goes from -inf to inf, and maybe a bit more friendly<br>
&lt;TabAtkins> noamr: k version goes 0-inf<br>
&lt;astearns> https://noamr.github.io/squircle-testbed/corner-visualizer/corner-animation.html<br>
&lt;TabAtkins> noamr: advantage of k version, when people talk about super-ellipses in other contexts they're usually talking just about the k version<br>
&lt;TabAtkins> noamr: but i don't feel strongly about either<br>
&lt;TabAtkins> q+<br>
&lt;astearns> ack TabAtkins<br>
&lt;fantasai> TabAtkins: I do feel strongly that the log2 version is better here, because the shapes are symmetrical across the bevel<br>
&lt;fantasai> TabAtkins: It also makes the range of useful values more reasonable. Going out to log2=10 makes it effectively a sharp corner, and the range past there is infinite<br>
&lt;fantasai> TabAtkins: Same thing applies in negative direction.<br>
&lt;fantasai> TabAtkins: This is easy to work with.<br>
&lt;fantasai> TabAtkins: in the straight K version, sharp corner is a non-obvious fraction slightly above zero...<br>
&lt;fantasai> TabAtkins: Symmetry really helps out with usability. Can put a note in the spec that k in academic papers is usually different, but for authors they won't care. We should prioritize usability for authors, and log2 variant gets us there<br>
&lt;TabAtkins> fantasai: +1<br>
&lt;bramus> +1<br>
&lt;TabAtkins> noamr: literature could be Figma, for example<br>
&lt;TabAtkins> noamr: author tooling can expose it too<br>
&lt;Kurt> q+<br>
&lt;astearns> ack Kurt<br>
&lt;TabAtkins> Kurt: does this mean specifcying infinity you need to use calc()?<br>
&lt;TabAtkins> noamr: no, can just have an infinity keyword<br>
&lt;TabAtkins> noamr: that's in the spec right now for positive infinity<br>
&lt;TabAtkins> noamr: i think people are okay with this not aligning with other tools i'm okay with going with log2<br>
&lt;TabAtkins> s/think people/think if people/<br>
&lt;TabAtkins> astearns: so it sounds like proposed is to use log2 range?<br>
&lt;TabAtkins> jensimmons: would like to know what smfr thinks?<br>
&lt;TabAtkins> noamr: he's commented on the issues, he doesn't have a strong opinion<br>
&lt;TabAtkins> q+<br>
&lt;fantasai> TabAtkins: There's an interpolation issue later on...<br>
&lt;lea> weak agree for the log2 version from me too<br>
&lt;fantasai> TabAtkins: Other reason I really like log2, when we do interpolation, making sure that works symmetrically requires doing log2 anyway<br>
&lt;fantasai> TabAtkins: so exposing more directly to authors is a good thing<br>
&lt;TabAtkins> noamr: yeah, the interpolation function si clsoe to log2<br>
&lt;TabAtkins> astearns: so it sounds like people are moderate to weakly in favor of log2<br>
&lt;TabAtkins> fantasai: i think tab's arguments are convincing to me<br>
&lt;TabAtkins> astearns: so proposed reoslution is to use the log2 range<br>
&lt;TabAtkins> RESOLVED: Use the log2 range for the superellipse parameter<br>
</details>
