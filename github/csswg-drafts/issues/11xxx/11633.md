---
number: 11633
title: "[css-fonts] Should `font-style: oblique 14deg` serialize as `font-style: oblique`?"
state: "open"
labels: ["css-fonts-4"]
author: "weinig"
created_at: "2025-02-02T20:17:19Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11633"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:39Z"
---

Following on from the discussion in https://github.com/w3c/csswg-drafts/issues/11430, the additional question of the serialization of `font-style: oblique 14deg` was raised.

Following the rule of "shortest serialization", there is an argument to made that `font-style: oblique 14deg` should serialize as `font-style: oblique`, as the spec states that `font-style: oblique` (with no angle given) should use an angle of 14 degrees:

> [...] The lack of an `<angle>` represents 14deg. (Note that a font might internally provide its own mapping for "oblique", but the mapping within the font is disregarded.) [...] (https://drafts.csswg.org/css-fonts-4/#font-style-prop)

Whether or not this argument holds, the spec should be explicit if this behavior is expected.

<!-- comment id=2632777474 author=nt1m created=2025-02-04T04:14:19Z url=https://github.com/w3c/csswg-drafts/issues/11633#issuecomment-2632777474 -->
## @nt1m — 2025-02-04

Given the oblique angle used to be 20deg before (and changed to 14deg later), I would say serializing the angle would be better for compatibility (which is higher priority than shortest serialization) :)

<!-- comment id=3014929606 author=fantasai created=2025-06-28T04:14:27Z url=https://github.com/w3c/csswg-drafts/issues/11633#issuecomment-3014929606 -->
## @fantasai — 2025-06-28

Agree with @nt1m... another way to think about this is if `oblique` yields a _computed_ value of 14deg, in which case `oblique` is the correct serialization (since they are equal), or if it yields a _used_value of 14deg and the _computed_ value is `oblique` without an angle.

> The lack of an [<angle>](https://www.w3.org/TR/css-values-4/#angle-value) represents 14deg.

This wording is ambiguous on this point, but the Computed Value line seems to imply that the computed value distinguishes whether an angle is specified or not:

> Computed Value: the keyword specified, plus angle in degrees if specified

<!-- comment id=3023223022 author=jfkthame created=2025-07-01T10:22:11Z url=https://github.com/w3c/csswg-drafts/issues/11633#issuecomment-3023223022 -->
## @jfkthame — 2025-07-01

If it is decided that `font-style: oblique 14deg` should serialize as `oblique 14deg` (rather than just as `oblique`), then what of `font-style: oblique`? Should its serialization "insert" the implied `14deg` angle, or do we accept that two specified values that are defined to represent the exact same value will nevertheless serialize differently?

<!-- comment id=4264439433 author=felipeerias created=2026-04-17T00:48:29Z url=https://github.com/w3c/csswg-drafts/issues/11633#issuecomment-4264439433 -->
## @felipeerias — 2026-04-17

Existing WPT tests expect `oblique 14deg` to serialise as `oblique`, which introduces multiple Interop failures:

- https://wpt.fyi/results/css/css-fonts/parsing/font-style-computed.html
- https://wpt.fyi/results/css/css-fonts/variations/font-style-parsing.html

Note that this also affects `calc()` expressions that happen to resolve to the magic number: the last test expects `oblique calc(10grad + 5deg)` to serialise as `oblique`, losing authorial intent.

Beyond these tests, another illustrative example is **`oblique 14.2deg`**.

Naively, one might expect that value to serialise to itself. However:

- Chrome rounds the degree value `14.2deg` to `14deg` and then computes that as `italic`.
- WebKit also rounds to `14deg`, which then serialises as `oblique`.
- Firefox uses a different rounding, so the computed value becomes `oblique 14.1992deg`.

These differences arise because each engine quantises the angle to a different internal fixed-point precision.

The serialisation rule implicit in the WPT tests depends on "does this angle equal exactly `14deg`?" and will therefore produce different results across engines as a consequence of implementation-defined quantisation.
