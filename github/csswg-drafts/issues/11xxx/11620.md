---
number: 11620
title: "[css-shapes-2] Maybe add superellipse/squircle etc as basic shapes?"
state: "open"
labels: ["css-shapes-2"]
author: "noamr"
created_at: "2025-01-30T21:32:14Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11620"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:45:22Z"
---

With #10993, we're going to have the generic `superellipse()` and specifics like `squircle` as corner shapes.
Perhaps we could have 50%-radius version of them as basic shapes? thinking `clip-path: squircle(50%)` (similar to `circle`) is a no brainer.

<!-- comment id=2625664583 author=tabatkins created=2025-01-30T21:46:35Z url=https://github.com/w3c/csswg-drafts/issues/11620#issuecomment-2625664583 -->
## @tabatkins — 2025-01-30

We already have the ability to specify round border-radius on rect <https://drafts.csswg.org/css-shapes-1/#funcdef-basic-shape-rect> like `rect(... round 1em)`; with `corner-shape` we should edit the spec to be able to specify `rect(... squircle 1em)` instead.

(I'm not against a `squircle()`, just pointing out we have a more straightforward existing way to do it.)\

<!-- comment id=2625670761 author=noamr created=2025-01-30T21:50:27Z url=https://github.com/w3c/csswg-drafts/issues/11620#issuecomment-2625670761 -->
## @noamr — 2025-01-30

> We already have the ability to specify round border-radius on rect https://drafts.csswg.org/css-shapes-1/#funcdef-basic-shape-rect like `rect(... round 1em)`; with `corner-shape` we should edit the spec to be able to specify `rect(... squircle 1em)` instead.
> 
> (I'm not against a `squircle()`, just pointing out we have a more straightforward existing way to do it.)\

Oh yea, I forgot that basic-shapes already have a corner-shape variant (since the old corner-shapes were still on the drawing board)

<!-- comment id=3053235799 author=noamr created=2025-07-09T16:10:51Z url=https://github.com/w3c/csswg-drafts/issues/11620#issuecomment-3053235799 -->
## @noamr — 2025-07-09

Now that `corner-shape` is integrated, I think we can tackle this.
There are 3 places where I would add superellipse support:
1. `rect` / `inset` / `xywh`

Change everywhere that says `border-radius` to  `corner`, which would resolve to `[ border-radius && <<corner-shape-value>>?]`.

2. A new `shape()` command that allows drawing a corner

```
corner [to | by] [position/coordinates etc.] [ <<corner-shape-value>> ||  [rotate <<angle>>] || [cw | ccw]] 
```

This would rotate the axis, and would draw a clockwise/counterclockwise corner between the start and end points with the given curvature (square & clockwise by default). Note that ccw is equivalent to multiplying the corner-shape superellipse value by -1 but it's an ergonomic way to express it.

<!-- comment id=3205203792 author=SebastianZ created=2025-08-20T09:40:23Z url=https://github.com/w3c/csswg-drafts/issues/11620#issuecomment-3205203792 -->
## @SebastianZ — 2025-08-20

> Change everywhere that says `border-radius` to `corner`, which would resolve to `[ border-radius && <<corner-shape-value>>?]`.

I assume you mean `[ <'border-radius'> && <corner-shape-value>? ]`, right?

> ```
> corner [to | by] [position/coordinates etc.] [ <<corner-shape-value>> ||  [rotate <<angle>>] || [cw | ccw]] 
> ```

In CSS we generally refrain from having abbreviations, so I'd vote for using `clockwise` and `counterclockwise` as keywords.

So, writing the whole syntax out, I guess this should be

```ebnf
<corner-command> = corner
        [ <corner-shape-value> || rotate <angle> || [ clockwise | counterclockwise ] ]
        [ [ to <position> ]
        | [ by <coordinate-pair> ] ]
```

Sebastian

<!-- comment id=3206484494 author=noamr created=2025-08-20T13:58:18Z url=https://github.com/w3c/csswg-drafts/issues/11620#issuecomment-3206484494 -->
## @noamr — 2025-08-20

> > Change everywhere that says `border-radius` to `corner`, which would resolve to `[ border-radius && <<corner-shape-value>>?]`.
> 
> I assume you mean `[ <'border-radius'> && <corner-shape-value>? ]`, right?

Yes

> > ```
> > corner [to | by] [position/coordinates etc.] [ <<corner-shape-value>> ||  [rotate <<angle>>] || [cw | ccw]] 
> > ```
> 
> In CSS we generally refrain from having abbreviations, so I'd vote for using `clockwise` and `counterclockwise` as keywords.

We use `cw` and `ccw` inside `shape()` already for arcs.

> So, writing the whole syntax out, I guess this should be [grammar snippet]

Something like this, yes.
