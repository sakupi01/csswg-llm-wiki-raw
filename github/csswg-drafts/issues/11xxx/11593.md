---
number: 11593
title: "[css-grid-3][masonry] Discussion overview for masonry syntax discussions"
state: "open"
labels: ["css-grid-3", "topic: masonry"]
author: "tabatkins"
created_at: "2025-01-29T02:42:33Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11593"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:45:17Z"
---

This is just a summarizing/guiding issue for the Grid 3/Masonry/Flexbox syntax, outlining the goals for the January F2F discussion and the rough priority @fantasai and I are assigning to them:

Roughly, the discussion covers four groupings of properties:

1. template longhands (track sizes, track names)
2. the other longhands (directions, slack, pack)
3. new `display` value, or `display:grid` + some toggle
4. shorthands

Here's the issues in more detail:

1. **Templating and Placement Properties** Should "masonry layout" use its own set of template definition properties (`masonry-template-tracks`, `masonry-template-areas`) or just reuse the `grid-*` ones (`grid-template-rows`/`grid-template-columns`, `grid-template-areas`, ignoring whichever of `-rows`/`-columns` is in the wrong axis). #11243

    * We have *almost* completely unified the set of allowed track-sizing behaviors between Grid and Masonry. Remaining divergence is "auto-repeating intrinsically-sized tracks", which are allowed in Masonry but don't seem to have an obvious useful/meaningful behavior in Grid. Perhaps we could just define those to do a single repeat, like the auto-repetition does today when the grid itself is intrinsically sized.
    * `grid-template-areas` is well-suited for Grid with its syntax of multiple strings, and assuming they'll be formatted across multiple lines to form "ASCII art" of the Grid areas. Masonry only ever has one dimension to name, tho, which results in `grid-template-areas: "foo bar baz"` for columns and `grid-template-areas: "foo" "bar" "baz"` for rows.

   **⭐ Update: The CSSWG has [resolved](https://github.com/w3c/csswg-drafts/issues/11243#issuecomment-2627998471) to re-use the `grid-*` properties for templating and placement. ⭐**

2. **Flow Control Properties** Should we adopt the `item-*` set of properties unifying the *non-template* parts of Flexbox/Grid/Masonry definition? If so, which one? #11480
    * Original Apple proposal: <https://github.com/w3c/csswg-drafts/issues/11480#issue-2781495226>
    * Tab counter-proposal: <https://github.com/w3c/csswg-drafts/issues/11480#issuecomment-2591555448> (scroll to end of the comment)
    * Unified compromise proposal: <https://github.com/w3c/csswg-drafts/issues/11480#issuecomment-2620453349>
    * Something else?
    * If not, do we take the `masonry-*` properties from [Grid 3's "grid-independent" proposal](https://drafts.csswg.org/css-grid-3/#masonry-model-masonry-option)? (I'm [on record](https://github.com/w3c/csswg-drafts/issues/11243#issuecomment-2563983858) as being willing to Formally Object to the "grid-integrated" property set.)

   **⭐ Update: The CSSWG [resolved](https://github.com/w3c/csswg-drafts/issues/11480#issuecomment-2628118101) to pursue this idea; but there are still open debates on its possible variations (above). See [spec draft](https://www.w3.org/TR/2025/WD-css-grid-3-20250207/#flow-control). ⭐**

3. **Masonry Layout Switch** Should "masonry layout" be its own `display` keyword, or just use `grid` + a control that toggles the grid into "masonry mode"? #12022    * Item 1 is independent of this; we could still, in theory, re-use the `grid-template-*` properties with `display: masonry`.
    * In item 2, if we do `display:grid` + a toggle:
        * If we do the `item-*` properties, it'll be part of `item-pack`, which also controls denseness and balancing. 
        * If we don't do `item-*` and instead have `masonry-*` properties, it'll be part of [`masonry-flow`](https://github.com/w3c/csswg-drafts/issues/11243#user-content-compromise-proposal), which also controls the masonry directions.
 
4. **Shorthanding** The `grid`/`grid-template` shorthands, as written, doesn't work for "masonry layout", and we can design a much better syntax. Should it just be another `grid` syntax branch? Or should it be its own `masonry` shorthand property? #12023 
    * Wherever it lives, the syntax would be `<tracks> || <areas> || <direction> || <cross-direction>`, possibly + `|| slack <slack>`, setting all the template properties from item 1 and "other" properties from item 2.
    * If item 1 is resolved in favor of reusing `grid-template-*` properties, what do we do about the `grid-template` shorthand? (See the `grid-template` bullet point under "Specific Examples" in [this comment](https://github.com/w3c/csswg-drafts/issues/11243#issuecomment-2563983858) for the issues with reusing `grid-template` as-is for masonry layout.)

<!-- comment id=2626100881 author=alisonmaher created=2025-01-31T01:35:23Z url=https://github.com/w3c/csswg-drafts/issues/11593#issuecomment-2626100881 -->
## @alisonmaher — 2025-01-31

> Remaining divergence is "auto-repeating intrinsically-sized tracks", which are allowed in Masonry but don't seem to have an obvious useful/meaningful behavior in Grid.

We also haven't converged on the potential difference in the default value for `masonry-template-tracks` (i.e. if it should be `repeat(autofill, auto)`) which I suppose we can cover with this first set of topics.

<!-- comment id=2627873713 author=css-meeting-bot created=2025-01-31T17:33:16Z url=https://github.com/w3c/csswg-drafts/issues/11593#issuecomment-2627873713 -->
## @css-meeting-bot — 2025-01-31

The CSS Working Group just discussed `[css-grid-3][masonry] Discussion overview for masonry syntax discussions`.

<details><summary>The full IRC log of that discussion</summary>
&lt;bramus> fantasai: tab and i divided the discussion in 4 separate pieces<br>
&lt;bramus> … suggestign to talk abou twhether to use masonry-template or to reuse grid props for placement and declaring tracks<br>
&lt;bramus> … 2nd about directional flow control props<br>
&lt;bramus> … feedback from tag was to come up with unified syntax<br>
&lt;bramus> … 3rd on how to trigger a masonry layout specifically<br>
&lt;bramus> … new display value for new collapse value in grid-template values and new proposal about unified flow control props<br>
&lt;bramus> … and 4th to introduce any shorthands for masonry or integrate with grid shorthand or both<br>
&lt;bramus> … that is the overview<br>
&lt;bramus> … most complicated will be 2nd one<br>
&lt;bramus> … jen has prepared a slide deck with illustrations to help with that<br>
&lt;bramus> astearns: alison you added sth to 1st part?<br>
&lt;bramus> alisonmaher: could be combined with 1st. plan eon talk about repeat-auto-fille auto and that it should be default or not<br>
&lt;bramus> fantasai: gonna insert that before the template topic<br>
&lt;bramus> … its a bit more complicated<br>
&lt;bramus> astearns: any comments so far?<br>
</details>

<!-- comment id=3175458405 author=fantasai created=2025-08-11T15:46:55Z url=https://github.com/w3c/csswg-drafts/issues/11593#issuecomment-3175458405 -->
## @fantasai — 2025-08-11

Summarizing where we are:
- CSSWG [resolved](https://github.com/w3c/csswg-drafts/issues/11243#issuecomment-2627998471) to re-use the `grid-*` properties for templating and explicit placement. This is been [edited into the draft](https://www.w3.org/TR/css-grid-3/).
- CSSWG resolved, in principle, to pursue the `item-flow` proposal originally [suggested here](https://github.com/w3c/csswg-drafts/issues/11243#issuecomment-2584991716) and described further in https://github.com/w3c/csswg-drafts/issues/11480. The exact syntax is still being debated there.
- How we switch into masonry layout is still an open question. https://github.com/w3c/csswg-drafts/issues/12022
- Masonry-related shorthand additions and adjustments are still an open question. https://github.com/w3c/csswg-drafts/issues/12023
- The initial track listing is currently `none`. There's an open question whether it should be some kind of automatic value. https://github.com/w3c/csswg-drafts/issues/10869
