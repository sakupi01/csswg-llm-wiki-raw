---
number: 11783
title: "[css-values-4] \"Serialize a math function\" step 4 behaves wrong for some node types"
state: "open"
labels: ["css-values-4"]
author: "AtkinsSJ"
created_at: "2025-02-26T14:55:08Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/11783"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:45Z"
---

https://drafts.csswg.org/css-values-4/#serialize-a-math-function

Step 4 says:

> For each child of the root node, [serialize the calculation tree](https://drafts.csswg.org/css-values-4/#serialize-a-calculation-tree). If a result of this serialization starts with a "(" (open parenthesis) and ends with a ")" (close parenthesis), remove those characters from the result. [Concatenate](https://infra.spec.whatwg.org/#string-concatenate) all of the results using ", " (comma followed by space), then append the result to s.

There are a couple of issues with this:

1. The `clamp()` function has a `<rounding-strategy>` as its first child. Passing that to "serialize the calculation tree" isn't valid, but ignoring it would miss it from the serialization.
2. If `root` is a numeric value and we are serializing it before the computed-value stage, it has no children and should be passed to "serialize the calculation tree" directly.
3. If `root` is a calc-operator node, serializing its children as a comma-separated list is incorrect. It should also be passed to "serialize the calculation tree" directly.

When implementing this in Ladybird, I replaced step 4 with this behaviour, and it seems to be correct according to the WPT tests I've checked:

- If the root node is a numeric value, or a calc-operator node:
  - Call "serialize the calculation tree" on the root node, then any wrapping (), and append that to `s`.
- Otherwise:
  - If the root node is a Clamp function:
    - Serialize its rounding-strategy and append that to `s`, followed by ", ".
  - Perform the original step 4 on the children that are calculation nodes.

In reality you'd want to combine that with step 3, as the two steps have identical if conditions.

<!-- comment id=3595781091 author=AtkinsSJ created=2025-12-01T10:28:31Z url=https://github.com/w3c/csswg-drafts/issues/11783#issuecomment-3595781091 -->
## @AtkinsSJ — 2025-12-01

> 1. The `clamp()` function has a `<rounding-strategy>` as its first child. Passing that to "serialize the calculation tree" isn't valid, but ignoring it would miss it from the serialization.

I think I was wrong here, but it depends on how step 2 of "serialize a calculation tree" is supposed to be interpreted:

> 2. If root is a numeric value, or a non-[math function](https://drafts.csswg.org/css-values-4/#math-function), serialize root per the normal rules for it and return the result.

There are two ways of interpreting "non-math function":
1. A function that isn't a math function.
2. A value that isn't a math function.

To me, the wording sounds like 1, such that I didn't even think 2 could be a possibility until very recently. (And I'm [not alone](https://github.com/LadybirdBrowser/ladybird/pull/6707#discussion_r2572908677) in reading it this way.) Apparently 2 is intended, and that reading would solve my issue here (it'd just serialize the `<rounding-strategy>` as expected) and would also serialize the random-value-sharing parameter of `random()`.

I'd like to request that the wording in that step 2 be adjusted to be clearer. I think "If root is not a math function, ..." would be enough, as the "is a numeric value or" clause is redundant if I'm understanding correctly.

<!-- comment id=4748061940 author=Calme1709 created=2026-06-19T02:51:55Z url=https://github.com/w3c/csswg-drafts/issues/11783#issuecomment-4748061940 -->
## @Calme1709 — 2026-06-19

Adding to this - the same issue applies for `progress()`'s `no-clamp` argument as for `round()`'s `<rounding-strategy>`.

`no-clamp` is also not followed by a comma in the existing grammar.
