---
number: 4972
title: "Suggestion: Ability to have a relative position without creating a gap in the text."
state: "open"
labels: ["css-position-3"]
author: "HT-7"
created_at: "2020-04-19T21:55:32Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/4972"
comments: 7
has_resolution: false
synced_at: "2026-07-04T04:43:45Z"
---

Usually, `position:relative` makes the element create a gap while `position:absolute` does not.

I suggest the option to be able to relatively position something without creating a gap.

Suggestion for parameter name: `position: relative-nogap` or `position:relative-gapless`.

For older browsers, a fallback could be used, like `position: relative-gapless,relative`.

<!-- comment id=616234372 author=fantasai created=2020-04-19T22:18:52Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-616234372 -->
## @fantasai — 2020-04-19

Use `position: absolute` + margins? I'm not sure what you're trying to do.

<!-- comment id=616241082 author=HT-7 created=2020-04-19T23:10:38Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-616241082 -->
## @HT-7 — 2020-04-19

> Use `position: absolute` + margins? I'm not sure what you're trying to do.

The problem is that `position:absolute` and margins would only work on a fixed screen width and would not work with responsive web design.

I would like to have something `:after` an element without creating a gap like `position:relative` usually does.

But `position:absolute`, which creates no gap, causes the content of `:after` to be positioned relative to the entire page.

<!-- comment id=616713754 author=Loirooriol created=2020-04-20T17:51:56Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-616713754 -->
## @Loirooriol — 2020-04-20

> causes the content of :after to be positioned relative to the entire page.

Relative in which way? If all the inset properties are set to auto, the element should appear in its static position. Or do you mean that something like `width: 50%` now resolves using another containing block? Then set `position: relative` to the parent.

<!-- comment id=616807853 author=SebastianZ created=2020-04-20T21:03:48Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-616807853 -->
## @SebastianZ — 2020-04-20

I assume a small example could clarify the issue. @HT-7 Could you please create one and describe it, so people can reproduce the issue you are facing?

Sebastian

<!-- comment id=618134429 author=ByteEater-pl created=2020-04-23T02:08:49Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-618134429 -->
## @ByteEater-pl — 2020-04-23

I can't remember any use case but there have been times I wished for that too.

But I think it should be another property, decoupled from `position`, thus working with all of its values:
`reserve-space: always | never | auto`
with `auto` as the default, resulting in currently specified behaviour.

(And actually at times I'd have also used `reserve-space: never` even with `position: static`.)

<!-- comment id=618388405 author=Loirooriol created=2020-04-23T12:59:12Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-618388405 -->
## @Loirooriol — 2020-04-23

@ByteEater-pl But `reserve-space: never` would be like `position: absolute`?

It seems to me that the new functionality that your proposal would provide is `position: absolute; reserve-space: always`, i.e. being able to position the element like in absolute positioning, but still take some space. The problem is that it's not much well-defined, since abspos elements are out-of-flow and they don't really participate in the parent formatting context. For example, an abspos child of a grid container is not a grid item, so how exactly would you reserve the space?

<!-- comment id=618415738 author=ByteEater-pl created=2020-04-23T14:08:44Z url=https://github.com/w3c/csswg-drafts/issues/4972#issuecomment-618415738 -->
## @ByteEater-pl — 2020-04-23

> @ByteEater-pl But `reserve-space: never` would be like `position: absolute`?

More precisely, with `position: absolute` the `auto` (initial) value of `reserve-space` would indeed behave as `never`. And you're right, for absolute positioning (including `fixed`) the new possibility is to have `reserve-space: always`. I imagine it working for positioned elements by generating its boxes according to the positioning scheme and additionally a copy of its principal box (not sure about cases where there are additional boxes) as if it had `position: static` and `visibility: hidden`. (This is likely just a rough approximation to portray the idea. I realize that an actual intervening box would probably cause undesirable effects in addition to reserving space, e.g. prevent margin collapsing in some cases.)

And `reserve-space: never` might be approximated as `width: 0; height: 0; overflow: visible` (but of course then you give up on using these 3 properties with different values).
