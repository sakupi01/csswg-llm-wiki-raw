---
number: 12906
title: "[css-borders] [css-ui] Consider rounding (negative?) outline-offsets like border-widths"
state: "open"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Edits", "Needs Testcase (WPT)", "css-borders-4", "css-ui-4"]
author: "emilio"
created_at: "2025-10-07T11:04:14Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12906"
comments: 3
has_resolution: true
synced_at: "2026-07-04T04:45:46Z"
---

Doing 'inset' outlines is a pretty common thing to do. However doing it well is rather non-trivial.

The obvious way:

```css
input:focus-visible {
  outline: 2px solid blue;
  outline-offset: -2px;
}
```

Breaks with fractional scaling, because the negative offset doesn't get rounded like the width.

With `env(hairline)` as proposed in #3720 it becomes possible to do (assuming hairline == 1 devpx which isn't _quite_ guaranteed but should hold in today's screens), but slightly annoying:

```css
  outline-offset: calc(-1 * max(env(hairline), round(down, 2px, env(hairline)));
```

With `border-round()`, also discussed on that issue, this would become one of:

```css
  outline-offset: border-round(-2px);
  outline-offset: calc(-1 * border-round(2px));
```

However, in this particular case of negative outline offsets, it seems reasonable to make the "obvious" code work?

Should we round negative outline offsets (or outline offsets more generally, even positive) like borders?

<!-- comment id=3637879299 author=fantasai created=2025-12-10T16:16:50Z url=https://github.com/w3c/csswg-drafts/issues/12906#issuecomment-3637879299 -->
## @fantasai — 2025-12-10

This makes sense to me. Even for positive offsets, having the offset be consistent on all sides of the box and at least 1 device pixel when it is not zero is probably desirable.

<!-- comment id=3961494454 author=css-meeting-bot created=2026-02-25T19:25:23Z url=https://github.com/w3c/csswg-drafts/issues/12906#issuecomment-3961494454 resolution=true -->
## @css-meeting-bot — 2026-02-25

The CSS Working Group just discussed `[css-borders] [css-ui] Consider rounding (negative?) outline-offsets like border-widths`, and agreed to the following:

* `RESOLVED: Round outline-offset same as outline-width and border-width`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> emilio: Fairly common use case to have a negative outline-offset to go over the border.<br>
&lt;fantasai> emilio: given the way things work by default, border gets rounded differently, and outline doesn't cleanly fit<br>
&lt;fantasai> emilio: Feel it should be uncontroversial to round outline-offset the same way as borders<br>
&lt;fantasai> emilio: Firefox has been doing this for awhile to fix high-DPI issues<br>
&lt;fantasai> emilio: I think we should just do it everywhere<br>
&lt;fantasai> +1 from us<br>
&lt;fantasai> florian: I think for awhile we didn't specify outline at all because UI, but not doing this anymore.<br>
&lt;fantasai> astearns: Just outline-offset?<br>
&lt;fantasai> emilio: outline-width is already rounded like border-width<br>
&lt;lwarlow> +1<br>
&lt;fantasai> PROPOSED: Round outline-offset same as outline-width and border-width<br>
&lt;kbabbitt> sgtm<br>
&lt;florian> s/we didn't specify outline at all /we deliberately didn't specify outline much<br>
&lt;fantasai> RESOLVED: Round outline-offset same as outline-width and border-width<br>
&lt;lea> +1<br>
&lt;fantasai> weinig: I will note that there are some open issues wrt rounding, especially regarding zoom<br>
&lt;fantasai> weinig: So if we do this, would be good to get to some of those zoom-related outline and border questions sooner than later<br>
&lt;fantasai> astearns: issues?<br>
&lt;fantasai> weinig: Yeah, I'll post in IRC.<br>
&lt;fantasai> astearns: ok, let's leave space to add those to the logs. But we're done for today.<br>
&lt;fantasai> Meeting closed.<br>
&lt;weinig> its written in terms of border-width, but applies to outline-width and column-rule-width as well, https://github.com/w3c/csswg-drafts/issues/13350<br>
</details>

<!-- comment id=4262217234 author=sb3nder created=2026-04-16T17:43:36Z url=https://github.com/w3c/csswg-drafts/issues/12906#issuecomment-4262217234 -->
## @sb3nder — 2026-04-16

@tabatkins 
Does this resolution make negative offsets symmetric to positive ones?
For example, would the computed value be the result of something like:
`calc(sign(var(--offset)) * round(line-width, abs(var(--offset))))`
Just doing some research for MDN docs - thanks!
[EDIT] It does: https://wpt.fyi/results/css/css-borders/outline-offset-rounding.tentative.html
