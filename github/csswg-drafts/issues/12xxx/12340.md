---
number: 12340
title: "[css-values-5] allow attr() to be used for URLs"
state: "open"
labels: ["css-values-5"]
author: "miragecraft"
created_at: "2025-06-15T17:17:49Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12340"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:45:42Z"
---

I've been waiting for `attr()` to be made available outside of content, but was dismayed to discover you are [not allowed to use it for URLs](https://developer.mozilla.org/en-US/docs/Web/CSS/attr#limitations_and_security) because this is exactly what I wanted to use it for.

I believe this cripples `attr()`'s utility and I do not agree with the security threat justification.

First of all, the reasoning given is that it risks leaking data via dynamically constructing URLs, but CSS **does not allow** dynamic URLs anyhow, because you cannot use variables inside `url()`.

The "dynamic" example given in MDN is entirely static:

```html
<!-- This won't work! -->
<span data-icon="https://example.org/icons/question-mark.svg">help</span>
```
```css
span[data-icon] {
  background-image: url(attr(data-icon));
}
```
How is this a security risk if the attribute already include the entirety of the URL?

If attacks already have access to the HTML to create the entire URL, they don't need to use `attr()`.

Secondly, even if there is plans to allow dynamic URL construction in the future, you can apply ~~CORS~~ same-origin restrictions to the `attr()`-tainted value instead of disallowing its use entirely.

For the above reasons, I'm advocating for the removal or modification of this restriction as it stands.

<!-- comment id=2974669618 author=Crissov created=2025-06-15T20:07:41Z url=https://github.com/w3c/csswg-drafts/issues/12340#issuecomment-2974669618 -->
## @Crissov — 2025-06-15

Note that the restriction you mention is the major difference between `url()` and [`src()`](https://drafts.csswg.org/css-values-4/#funcdef-src), and the motivation to introduce the latter.

<!-- comment id=2974671120 author=miragecraft created=2025-06-15T20:11:21Z url=https://github.com/w3c/csswg-drafts/issues/12340#issuecomment-2974671120 -->
## @miragecraft — 2025-06-15

> Note that the restriction you mention is the major difference between `url()` and [`src()`](https://drafts.csswg.org/css-values-4/#funcdef-src), and the motivation to introduce the latter.

Hence my suggestion to apply CORS restriction to the `attr()`-tainted URL instead of disallowing its use.

<!-- comment id=2978270319 author=tabatkins created=2025-06-16T21:51:29Z url=https://github.com/w3c/csswg-drafts/issues/12340#issuecomment-2978270319 -->
## @tabatkins — 2025-06-16

As @Crissov says, `src()` lets you specify concatenation, which is the thing we're concerned about.

I don't understand how CORS would help us here.  The security threat isn't accessing resources on the page's server against its will, it's extracting data from the page (like nonces or user IDs stored in data attributes, or implicitly in the parameters of other URLs) and sending it to *other* (hostile) servers. The hostile server could set up whatever CORS allowances it needs.

I *do* want to figure out a way to specify that host-language-defined "URL attributes" (like `<img src>` or `<a href>`) could be used *directly* as a `<url>` via `attr()`, but not when combined with other stuff. That doesn't allow your example, tho, where the URL is specified in a data attribute. That'll require a more general allowlist feature that we've so far avoided specifying.

<!-- comment id=2978507872 author=miragecraft created=2025-06-16T23:59:29Z url=https://github.com/w3c/csswg-drafts/issues/12340#issuecomment-2978507872 -->
## @miragecraft — 2025-06-16

You're right, I read up on CORS and it blocks you from reading the response, but not block the request.

So CORS is the wrong technology to use here.

But we can check whether the constructed URL is same-origin, and block it entirely (from making the request) if it isn't, no?

<!-- comment id=4497101198 author=myfonj created=2026-05-20T10:00:33Z url=https://github.com/w3c/csswg-drafts/issues/12340#issuecomment-4497101198 -->
## @myfonj — 2026-05-20

### Allow re-use of already loaded resources? `background-image: src(attr(src))`

I understand the risk of fabricating URL from random fragments, even using URI-looking `data-*` attributes that have no guarantee to be actually used for loading anything during the page lifecycle. These all are quite clearly dangerous.

#### But what about strict re-use of resources that the page already "owns"?

If the restricting mechanism of "attr-tainting" kept the hard block for making any **new** request, but allowed pulling images from **the cache**, then the data exfiltration prevention would be preserved, and authors will gain pretty intuitive way of making extended image backdrops, putting video posters around videos etc, all without extra HTML polluting.

---

Realised while reading [Ana Tudor's post _Background extending out and under_](https://www.patreon.com/posts/158742602).
