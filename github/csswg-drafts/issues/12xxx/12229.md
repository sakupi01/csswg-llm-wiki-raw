---
number: 12229
title: "[css-sizing] How should auto-sizing of iframes work?"
state: "open"
labels: ["css-sizing-3"]
author: "chrishtr"
created_at: "2025-05-23T16:31:04Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12229"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:24Z"
---

In #1771 we resolved to add the ability for an `<iframe>` to opt into resizing to match its content document's sizing, similar to how other elements do so, via a `contain-intrinsic-size: from-element` opt-in CSS property on the `<iframe>` element and a similar opt-in from the iframe content document (either attribute or meta tag).

However, in that discussion it was proposed that the iframe contents document would explicitly request particular width and height dimensions via the attribute/meta tag. With that setup, the sizing of the iframe would not automatically match the natural sizing of the document contents, and would need to be manually updated by script when the natural sizing changes.

A number of use cases raised in #1771 and elsewhere would benefit from changing to automatic sizing based on the layout size of the iframe content document. Automatic sizing would be ideal if we can achieve it, because everything would just work and the author wouldn't have to write scripts to observe document layout.

Auto-sizing has several challenges:
* Infinite loops / hysteresis: how to avoid layout thrashing or infinite loops due to the dependency of the `<iframe>` on its contents, and the contents on the `<iframe>`. This mutual dependency could also cause an iframe with the same contents ending up with a different size depending on the sequences of layout after load.
* Performance: iframe documents that often change size could cause excessive layout work. Nested responsive iframes could induce exponential runtime costs.
* Definition: which values from the iframe content document layout should be used to size the `<iframe>`?

Note also that these challenges are not really unique to auto-sizing, since if an author pollyfills auto-sizing the same challenges will result.

Here are some possible approaches to the above problems:
* Infinite loops / hysteresis: always lay out an iframe content document with the same initial viewport size. (TODO: but which size should this be?)
* Performance: Only automatically perform one layout after the `load` event of the iframe. (Pro: simple, should work. Con: not responsive to content streaming in later, such as in comment widgets; also does not resize when the browser window resizes.)
* Performance: limit responsive iframe depth, or always lay out an iframe content document with the same initial viewport size.
* Definition: Use the layout height of the iframe document contents as input to the `<iframe>` sizing, but not the layout width of the iframe document. (Pro: content commonly fills horizontally to the space available and then wraps, and adjusts its height to contain all content.)

<!-- comment id=2905185658 author=jakearchibald created=2025-05-23T17:14:00Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-2905185658 -->
## @jakearchibald — 2025-05-23

A lot of the problems described above were the problems faced by container queries. It isn't exactly the same, but developers are familiar with the restrictions that CSS containers bring, so it would be good to lean into those patterns.

Solutions that only layout once, or on explicit developer request, do not feel like enough of a step forward to justify adding the feature.

<!-- comment id=2905556545 author=bfgeek created=2025-05-23T19:17:39Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-2905556545 -->
## @bfgeek — 2025-05-23

> A lot of the problems described above were the problems faced by container queries. It isn't exactly the same, but developers are familiar with the restrictions that CSS containers bring, so it would be good to lean into those patterns.

This is somewhat building on how container-queries work, but its fundamentally different.

E.g. with this feature its enforcing that you can only respond in one dimension (the height). However the difference is that the height of the iframe _cannot_ be known synchronously. It needs a IPC jump to a different process, hence the issues with loops/hysteresis, etc.

<!-- comment id=2905580534 author=jakearchibald created=2025-05-23T19:28:00Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-2905580534 -->
## @jakearchibald — 2025-05-23

It'd be good to define some of the loop cases and see if there are solutions. Eg make viewport units behave more like container units within the iframe.

If the feature signals layout really late (eg load event) and only does it once, it seems just as complex and less functional than the solutions out there today, so I don't think developers would use it.

<!-- comment id=2922683754 author=chrishtr created=2025-05-30T15:19:47Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-2922683754 -->
## @chrishtr — 2025-05-30

> A lot of the problems described above were the problems faced by container queries. It isn't exactly the same, but developers are familiar with the restrictions that CSS containers bring, so it would be good to lean into those patterns.

One big difference is that the "container" in this case can change its size in response to contained content. Container queries, OTOH, have forced `contain:size`.

<!-- comment id=2924407031 author=jakearchibald created=2025-05-31T05:51:28Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-2924407031 -->
## @jakearchibald — 2025-05-31

With container queries you can use `contain: inline-size`, which allows the container to change block size. I think that's the same pattern we're talking about here.

<!-- comment id=3491414636 author=yisibl created=2025-11-05T14:08:04Z url=https://github.com/w3c/csswg-drafts/issues/12229#issuecomment-3491414636 -->
## @yisibl — 2025-11-05

Currently, many pages dynamically load content via backend APIs, especially feed pages like `x.com` that scroll to load content, making their height virtually infinite.

Additionally, with the rise of GPTs, the height of these AI question-and-answer pages also increases dynamically.

At Alibaba, we have numerous AI pages embedded via iframes, many of which are in feed-based.
