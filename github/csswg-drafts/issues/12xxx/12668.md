---
number: 12668
title: "[css-values-5] `ident()` should be validated at parse time"
state: "closed"
labels: ["css-values-5"]
author: "cdoublev"
created_at: "2025-08-28T07:03:04Z"
closed_at: "2026-06-19T05:17:47Z"
url: "https://github.com/w3c/csswg-drafts/issues/12668"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:46Z"
---

Follow-up on https://github.com/w3c/csswg-drafts/issues/11663#issuecomment-3036261525.

As currently [specified](https://drafts.csswg.org/css-values-5/#ident):

  1. `ident(1)` is valid but produces an invalid `<ident>`
  2. `ident(initial)` is valid but produces an invalid `<custom-ident>`
  3. `ident(custom)` is valid but produces an invalid `<dashed-ident>`
  4. `ident(none)` is valid but produces an invalid `<keyframes-name>`

The result of `ident(1)` could be escaped but this does not work for 2-4, so it would be more consistent to always validate the first argument (at parse time).

I presume that 3-4 and similar cases should also be validated at parse time. `calc(-1)` is a valid `<number [0,∞]>` and is clamped at computed value time. I do not think anything similar can be done for `ident()`.

<!-- comment id=4744920251 author=weinig created=2026-06-18T18:20:23Z url=https://github.com/w3c/csswg-drafts/issues/12668#issuecomment-4744920251 -->
## @weinig — 2026-06-18

It seems like the resolution for https://github.com/w3c/csswg-drafts/issues/12206 answers the questions here and validity should be checked against the concatenation of the terms with all `<number>` components replaced with 0.

@tabatkins @cdoublev does that sound correct to you?

<!-- comment id=4748710905 author=cdoublev created=2026-06-19T05:17:47Z url=https://github.com/w3c/csswg-drafts/issues/12668#issuecomment-4748710905 -->
## @cdoublev — 2026-06-19

I missed #12206. Thanks. The resolution seems a bit abstract but the key point is that `ident()` must be validated at parse time, right? I will read the IRC log later.
