---
number: 12513
title: "[css-images-4] Add `light-dark-image()`, or generalize `light-dark()` for images too?"
state: "closed"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-color-5", "css-images-4"]
author: "tabatkins"
created_at: "2025-07-22T18:23:20Z"
closed_at: "2026-03-24T22:08:24Z"
url: "https://github.com/w3c/csswg-drafts/issues/12513"
comments: 68
has_resolution: true
synced_at: "2026-07-04T04:45:37Z"
---

The existing `light-dark()` function gives a very easy way to select different colors based on the used color scheme on an element. In #10577 we're discussing adding an `if()` test to generalize that, so you can vary *any* value based on the used color scheme (for example, `font-weight: if(color-scheme(dark): bold, else: normal);`). This is a little more verbose, but it's a rarer use-case, so that's all right.

In <https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-2887663097>, Emilio suggests that varying CSS images based on color scheme is a common enough use-case to be worth addressing as simply as colors. (Several other people have argued this as well in similar issues.) I agree; any background image I apply in a light-mode stylesheet almost certainly has to be changed in a dark mode stylesheet.

I see two possible ways to do this:

1. Just extend the grammar of `light-dark()`, to `light-dark( <color>#{2} | <image>#{2} )`. If you supply two colors, it returns a color; if you supply two images, it returns an image.
2. Add a `light-dark-image( <image>#{2} )` function, which is identical to `light-dark()` but always returns an image, instead of always returning a color.

I don't have a strong opinion in this regard. Folding them both together into a single function like in (1) is convenient, but starts implying that it might cover even more types, and we don't want to suggest that. Duplicating the functionality with a similarly-named function like in (2) is simple, but also a little bit more verbose.

<!-- comment id=3104506310 author=svgeesus created=2025-07-22T19:18:19Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3104506310 -->
## @svgeesus — 2025-07-22

> 2\. Add a `light-dark-image( <image>#{2} )` function, which is identical to `light-dark()` but always returns an image, instead of always returning a color.

I have a moderate preference for this option, keeping the types distinct.

Otherwise there is also a need to explain what happens with nested functions (like say relative colors, where the origin color is a `light-dark()` that returns an image)

<!-- comment id=3104641297 author=tabatkins created=2025-07-22T19:53:01Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3104641297 -->
## @tabatkins — 2025-07-22

>  (like say relative colors, where the origin color is a light-dark() that returns an image)

That would just be invalid, as a `light-dark()` containing images would resolve to an `<image>`, which isn't allowed in relative color syntax.

(But I do agree that I mildly prefer the separate name.)

<!-- comment id=3104722480 author=SebastianZ created=2025-07-22T20:24:09Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3104722480 -->
## @SebastianZ — 2025-07-22

As I noted in https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-2231779930, I'd go with option 1.

Giving it a separate name might confuse people, especially, because the new function's name pattern differs from the existing one. I.e. it's `light-dark()` and not `light-dark-color()`. This issue increases if we add a third variant at some point.
Polymorphism increases maintainability and requires less muscle memory.

Sebastian

<!-- comment id=3105016710 author=LeaVerou created=2025-07-22T22:34:01Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3105016710 -->
## @LeaVerou — 2025-07-22

If we add an `-image()` version, could we also add a `-calc()` version? I've often run into cases where I needed it for dimensions.

And if we go down that route, I wonder if we can just make the type a parameter, since there's precedent for that…

<!-- comment id=3107532554 author=SebastianZ created=2025-07-23T11:57:25Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3107532554 -->
## @SebastianZ — 2025-07-23

> And if we go down that route, I wonder if we can just make the type a parameter, since there's precedent for that…

I wonder if we actually need that. Depending on the given parameters and context in which the function is used its return type should be clear. And if there are cases, in which the function would be ambiguous, that would be candidate for explicit type conversion as discussed in #6408.

Sebastian

<!-- comment id=3109087702 author=LeaVerou created=2025-07-23T15:19:37Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109087702 -->
## @LeaVerou — 2025-07-23

> > And if we go down that route, I wonder if we can just make the type a parameter, since there's precedent for that…
> 
> I wonder if we actually need that. Depending on the given parameters and context in which the function is used its return type should be clear. 

Not really, there are cases where all three types are valid, e.g. `background`. Though we _could_ require that the type of both arguments is the same, then disambiguation can still work by simply looking at the type of the first argument, and making it a parse error if they are not the same (e.g. `background: light-dark(red, url('foo.png'));`)

There is no precedent for this in CSS, and I haven't thought it through very much, but it does seem like a pretty elegant solution. In fact, I now wish we went that way with the interpolation functions rather than introducing separate functions for every possible type…

<!-- comment id=3109172986 author=AtkinsSJ created=2025-07-23T15:41:12Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109172986 -->
## @AtkinsSJ — 2025-07-23

> There is no precedent for this in CSS

It's quite similar to how several of the math functions behave. It would make it a bit more complicated on the implementation side, but IMO it's nicer to have a small number of more versatile functions. I'm not much of a web dev these days but I'd expect that to be nicer for authors too.

<!-- comment id=3109226382 author=LeaVerou created=2025-07-23T15:56:33Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109226382 -->
## @LeaVerou — 2025-07-23

> > There is no precedent for this in CSS
> 
> It's quite similar to how several of the math functions behave. It would make it a bit more complicated on the implementation side, but IMO it's nicer to have a small number of more versatile functions. I'm not much of a web dev these days but I'd expect that to be nicer for authors too.

Absolutely, that's what the [PoC](https://www.w3.org/TR/design-principles/#priority-of-constituencies) is all about. Some complexity on the implementation side to avoid user-facing complexity is usually a desirable tradeoff (but of course it depends on the ratio: _how much_ implementation complexity are we adding vs _how much_ author complexity are we saving?).

In general, from what I've observed, authors _really_ struggle with anything typed in CSS, so the fewer such warts we can have, the better. We shouldn't just be reaching for `*-calc()` and `*-image()` etc to make our lives easier, but only after we've exhausted alternatives.

<!-- comment id=3109520686 author=tabatkins created=2025-07-23T17:33:19Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109520686 -->
## @tabatkins — 2025-07-23

Right, we *cannot* actually infer types in general. There's too much trivial overlap between types (`red` might be an animation name or a color, for example), and there's too much flexibility in grammars to reliably get hints from placement.

Anyway, I'm fine either way for the function here. If several people are leaning towards the "single function, takes two colors *or* two images", great, that's fine. Perfectly unambiguous, just adds a touch of complexity to the impl but nothing serious.

<!-- comment id=3109613217 author=LeaVerou created=2025-07-23T18:09:03Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109613217 -->
## @LeaVerou — 2025-07-23

@tabatkins 

> Right, we _cannot_ actually infer types in general. There's too much trivial overlap between types (`red` might be an animation name or a color, for example), and there's too much flexibility in grammars to reliably get hints from placement.

We can and do infer types **in context**. E.g. `red` in `background-color: red` is a color, not a keyword. As long as the type of `light-dark()` is inferred from the type of its first argument, I'm not sure how it's any different from inferring the type of its argument when used directly.

> Anyway, I'm fine either way for the function here. If several people are leaning towards the "single function, takes two colors _or_ two images", great, that's fine. Perfectly unambiguous, just adds a touch of complexity to the impl but nothing serious.

And two dimensions! Would that add too much complexity? If so, would restricting to `<number>` make it easier?

<!-- comment id=3109966313 author=tabatkins created=2025-07-23T20:01:36Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3109966313 -->
## @tabatkins — 2025-07-23

> We can and do infer types in context.

Yes, that's why I specifically said "in general" in the text you quoted. We do not always have appropriate context to be capable of resolving a value to a particular type. (At least, not without expensive/complicated backtracking that can, in the worst case, go exponential in cost, which is strictly unacceptable.)

> And two dimensions! Would that add too much complexity?

Not to the implementation, no. What it adds complexity to is *the mental model*. If `light-dark()` is *only* a color, and anything else is done with the generic `if()`, that's easy to understand. If `light-dark()` is a color *or* an image, that's not too bad to remember; it's only two things, and they're conceptually related anyway. If `light-dark()` is *three* things, especially if the third thing isn't really conceptually related, it gets harder. Four things makes it worse, etc. 

The more things it can do, the more complicated the boundary between "can use `light-dark()`" and "have to use `if()`" is. It becomes harder for authors to confidently write code; they either have to guess and check, or just give up and always use the generic `if()` because it always works (even with its drawbacks due to being an arb-sub function).

Dimensions have a further issue, which is that where dimensions are used, often there are keywords allowed as well. If `width: light-dark(500px, auto);` doesn't work, how do you explain that to users? If it does work, it means we have to keep more in mind in the future when adding to these categories; we have to make sure that either (a) we never add syntaxes that would make this type-detection ambiguous, or (b) we define that certain values are always detected as a certain type, and you can't use them as the other type. This gets worse as we add more types.

So, this is why I'll push back on making `light-dark()` too complicated. ^_^ "Color or image" is fine; going further probably isn't. We have a generic function proposed for this; it's a little more verbose but not terrible.

<!-- comment id=3110167480 author=LeaVerou created=2025-07-23T21:04:07Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3110167480 -->
## @LeaVerou — 2025-07-23

> > We can and do infer types in context.
> 
> Yes, that's why I specifically said "in general" in the text you quoted. We do not always have appropriate context to be capable of resolving a value to a particular type. (At least, not without expensive/complicated backtracking that can, in the worst case, go exponential in cost, which is strictly unacceptable.)

If we could have resolved the type of the first value, were it had been used by itself, then we can resolve the type of the whole thing. This assumes we define its return type as a passthrough "the return type is the same as the type of its **first** argument". We don't have any functions typed this way in CSS today, but I don't see why it wouldn't work (but as always, I could be missing it). Note that it's the **first** argument, not any argument, which I think prevents your exponential backtracking / combinatorial explosion concern?

Then you _never_ have to use `if()` for this, which also resolves your second point.

<!-- comment id=3112549605 author=Crissov created=2025-07-24T08:30:34Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3112549605 -->
## @Crissov — 2025-07-24

Hm, “always provide two parameters, second argument type has to be compatible with the first argument type” sounds doable at first, both mental and implementation wise, but if that meant that `light-dark(500px, auto)` works whereas `light-dark(auto, 500px)` does not then it really is not worth it because that is too surprising. 

<!-- comment id=3113699874 author=LeaVerou created=2025-07-24T14:30:01Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3113699874 -->
## @LeaVerou — 2025-07-24

@Crissov what would `auto` resolve to if specified directly in the same place instead of inside `light-dark()`? Any real examples?

<!-- comment id=3114351652 author=Crissov created=2025-07-24T17:56:37Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3114351652 -->
## @Crissov — 2025-07-24

Good question, I blindly copied from above. 

<!-- comment id=3249052098 author=flashymittens created=2025-09-03T12:30:41Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3249052098 -->
## @flashymittens — 2025-09-03

What about `light-dark([type], value_1, value_2)`? Where type is defined as `<color>` by default?

<!-- comment id=3254780551 author=tabatkins created=2025-09-04T17:35:00Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3254780551 -->
## @tabatkins — 2025-09-04

`light-dark(image, URL(), URL())` is not meaningfully different from `light-dark-image(URL(), URL())`, but it makes tooling a little more complicated.

<!-- comment id=3407562681 author=myfonj created=2025-10-15T17:35:46Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3407562681 -->
## @myfonj — 2025-10-15

Pardon for my ignorance, but what is the reason the type of the resulting value of the `light-dark()` has to be determinable (and determined) beforehand?

tl;dr: seconding Lea's remark

> In general, from what I've observed, authors really struggle with anything typed in CSS, so the fewer such warts we can have, the better.

Is the typing here even beneficial? Speaking for myself, my smooth brain expects exactly the seemingly simplest interpretation, where doing e.g.

```CSS
font-weight: light-dark(whatever, bolder);
```

would boil down to clearly invalid

```CSS
font-weight: whatever;
```

and do nothing in the "light mode", and to completely legitimate

```CSS
font-weight: bolder;
```

in the "dark mode". Nothing less, nothing more. Determining validity of the function alone doesn't make any sense in this mental model, since the concept is just a *"blind" syntactic sugar "macro"*.

In other words, it's the *current* state that seems like something quite difficult to explain:

```CSS
/* OK, green in the light mode */
body {
 --x: tan;
 color: light-dark(green, var(--x)); 
}
```

vs

```CSS
/* ERROR, invalid in either mode */
body {
 /* no --x OR --x not being interpretable as a `color` */
 color: light-dark(green, var(--x)); 
}
```

While it's perhaps nice that the latter is broken immediately, even in the "light mode", so I have a chance to catch it earlier, I'm not sure this benefit is worth the complexity. As I indicated, it appears to be way more cognitively (and presumably even computationally?) demanding, compared to the "syntactic sugar-y macro" mental concept. At this point, I have next to no clue how I would explain why `light-dark()` even cares about types of its arguments and why it becomes entirely invalid the moment one of its arguments (even the "unused" one) loses its proper type. It feels like overly strict optimisation or unexpectedly eager validation from the outsider's viewpoint.

<!-- comment id=3417506939 author=tabatkins created=2025-10-17T23:28:26Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3417506939 -->
## @tabatkins — 2025-10-17

Functions like `light-dark()` aren't "macro-like" - they're fully typed values, same as `1em`, `red`, or `rgb(...)` (specifically, they're a `<color>`). We reserve the "macro-like" behavior for the few functions that *must* act like that, like `var()`; otherwise, we want to know the types of values so we can correctly parse things immediately. (Note that `light-dark()` *can't be resolved* to one of its arguments at parse-time; we have to wait until we actually apply styles to the document, so we can know what the element's *used color scheme* is, as that can be affected by the 'color-scheme' property on it or an ancestor.)

Being a `<color>` vs being something else can definitely affect parsing - for example, when parsing the `background` shorthand, if the final value is a `light-dark()`, knowing that it's a `<color>` means it'll get expanded into the `background-color` longhand; if it was an image it would instead need to be the final value in the `background-image` longhand. If it could be *either*, we wouldn't know how to parse it! We'd have to make an arbitrary decision, or look into its arguments and guess.

<!-- comment id=3429403253 author=SebastianZ created=2025-10-21T20:14:33Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429403253 -->
## @SebastianZ — 2025-10-21

> ...or look into its arguments and guess.

That was my point in https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3107532554.

To summarize the options we have are:
| Opt. | Solution | Pros | Cons |
|---|--------|--------|--------|
| 1. | Make `light-dark()` return a different value based on its parameters. | "just works" ⇒ reduces cognitive load, extends existing function, consistent with other functions like `calc()` and `progress()` | implementation a bit more complicated, tooling like auto-completion a bit more complicated |
| 2. | Add a separate function `light-dark-image()` for images … | | |
| 2. a) | … and keep `light-dark(<color>, <color>)` for colors. | existing function untouched, explicit function for images | two functions doing similar things, inconsistent naming ⇒ increases cognitive load |
| 2. b) | … and also add a `light-dark-color()` function for consistency and mark `light-dark(<color>, <color>)` as a deprecated alias to it. | explicit functions for both colors and images, consistent naming | introduces a legacy alias |
| 3. | Introduce an explicit parameter for the type (with `<color>` as default). | extends existing function, makes type explicit | redundant type definition for images |
| 4. | Do not add a function for images | no conflicts, use cases are generally covered by `if()` | `if()` more verbose / no syntax sugar for common cases |

And to be clear, I have a somewhat strong dislike against option 2a due to it introducing an inconsistency in naming. My favorite is 1 (as it reduces cognitive load and is consistent with other functions like `calc()` or `progress()`). I'd be fine with option 3 and 4 and could live with 2b.

Given those four options, I think this is ready to be discussed on a call.

Sebastian

<!-- comment id=3429474969 author=LeaVerou created=2025-10-21T20:39:15Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429474969 -->
## @LeaVerou — 2025-10-21

Thanks for the summary @SebastianZ . It would be helpful to also outline the arguments for and against each.

One thing to note: it has been repeatedly mentioned that there is no real difference between `light-dark-image(...)` and `light-dark(image, ...)`. Philosophically this may be the case, but until we get something like #12806, it means that one of them can dynamically switch e.g. via a variable value, while the other one cannot.

My preference would also be to return a different type based on the parameters. Enabling existing primitives to _just work_ is usually much better than introducing additional syntax. If we want to provide a way to *coerce* to a given type, that can be done as a separate function.
In terms of API design, [forcing users to duplicate intent is an antipattern](https://lea.verou.me/blog/2025/user-effort/#signal-to-noise), and it's unfortunate that there is plenty of precedent for this in the language.

One consideration is whether the case of having different types is significant (where allowed), e.g. 

```css
background: light-dark(white, linear-gradient(gray, black));
```

It does complicate things a fair bit if so, but it *is* a natural thing to write, in places where either type is allowed.
There is a workaround however:
```css
background: light-dark(white, transparent) light-dark(none, linear-gradient(gray, black));
```
…so we can punt that and revisit later.

<!-- comment id=3429489682 author=bramus created=2025-10-21T20:44:02Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429489682 -->
## @bramus — 2025-10-21

> And to be clear, I have a somewhat strong dislike against option 2a due to it introducing an inconsistency in naming.

I brought this up in the call when we resolved on adding `light-dark()` and would have preferred `light-dark-color()` back then, as it would have allowed `light-dark()` to be used for multiple types of values. But that ship has sailed …

I think option 3 is the strategy I would prefer. It reminds me of `attr()` that can take a type and fall back to the default CSS string when omitted.

Also, FWIW: with custom functions nowadays it’s really easy to write your own `--light-dark()` that works with any type of value. See https://brm.us/--light-dark() for example.

<!-- comment id=3429696070 author=SebastianZ created=2025-10-21T21:52:17Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429696070 -->
## @SebastianZ — 2025-10-21

> Thanks for the summary [@SebastianZ](https://github.com/SebastianZ) . It would be helpful to also outline the arguments for and against each.

I've turned it into a table now and added the pros and cons.

Sebastian

<!-- comment id=3711614880 author=viktor-yakubiv created=2026-01-05T18:27:55Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3711614880 -->
## @viktor-yakubiv — 2026-01-05

I have been going through the `light-dark()` specification draft and wondering why it is limited to `<color>` values only if the styling preferences in light/dark color schemes go beyond colors. This curiosity led me to this issue, especially the following:

> I've often run into cases where I needed it for dimensions.

The dark color scheme (light text on a dark background) gives a more contrasting content-to-background feel, so one might want to decrease the `font-weight` or increase `letter-spacing` in the dark scheme. When done so, it feels unnatural to use `light-dark()` value for the `color` property, but reach for a `@media (prefers-color-scheme: dark)` (or suggested `if()`) for other properties.

The `light-dark()` function seems to be closer to the `var()` (and `calc()`) than to a color function. It feels natural to pass to the function any two CSS values and expect it to work. This is well demonstrated in [@myfonj code snippets](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3407562681).

---

> Being a `<color>` vs being something else can definitely affect parsing - for example, when parsing the `background` shorthand, if the final value is a `light-dark()`, knowing that it's a `<color>` means it'll get expanded into the `background-color` longhand; if it was an image it would instead need to be the final value in the `background-image` longhand. If it could be _either_, we wouldn't know how to parse it! We'd have to make an arbitrary decision, or look into its arguments and guess.

@tabatkins expressed a valid concern: knowing type in shorthand is important; but is it possible when `var()` can substitute any number of value components? I wonder how the following three snippets are different:

```css
.pane {
  background: light-dark(url('light-image') white, url('dark-image') black);
}
```

```css
.pane {
  --image: url('light-image');
  @media (prefers-color-scheme: dark) {
    --image: url('dark-image');
    /* --image: black; if so, background receives two colors */
  }
  background: var(--image) light-dark(white, black);
}
```

```css
.pane {
  --background: url('light-image') white;
  @media (prefers-color-scheme: dark) {
    --background: url('dark-image') black;
  }
  background: var(--background);
}
```

```css
.pane {
  --light: url('light-image') white;
  --dark: url('dark-image') black;
  background: light-dark(var(--light), var(--dark));

  /* what if one conditionally becomes valid?
  @media (prefers-color-scheme: dark) { --dark: black } */
}
```

---

Overall, the **option 1** from the [table](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429403253) seem to be the (most) CSS way to me: it preserves the feeling of the language and is (more) consistent with other constructs.

<!-- comment id=3712111015 author=emilio created=2026-01-05T21:15:00Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3712111015 -->
## @emilio — 2026-01-05

You could also support `light-dark(<image>, <image>)` as well fwiw, without going into full generality, if we know that's a common use case. The image case seems pretty clear to me, I'm less sure about `font-weight: light-dark(..)` etc.

<!-- comment id=3714052954 author=bramus created=2026-01-06T10:07:43Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3714052954 -->
## @bramus — 2026-01-06

> The image case seems pretty clear to me, I'm less sure about `font-weight: light-dark(..)` etc.

It has to do with the perception of the thickness of the font. In Dark Mode, you want to make it smaller so that it visually looks the same.

Here’s two articles about it:

- https://css-tricks.com/using-css-custom-properties-to-adjust-variable-font-weights-in-dark-mode/
- https://nerdy.dev/adjust-perceived-typepace-weight-for-dark-mode-without-layout-shift

This image from the first article nicely demonstrates it:

<img width="618" height="368" alt="Image" src="https://github.com/user-attachments/assets/4ca38ed1-af5b-462d-8e20-8e02fee733ed" />


> The top shows us some light text on a dark background.
> The middle panel shows what happens in dark mode without changing any font weight settings.
> And the bottom panel demonstrates dark mode text that we’ve thinned out a bit, to visually match the weight of its light counterpart.

<!-- comment id=3714603411 author=LeaVerou created=2026-01-06T12:56:25Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3714603411 -->
## @LeaVerou — 2026-01-06

I’ve also needed `mix-blend-mode: light-dark(multiply, screen)`.

<!-- comment id=3806070510 author=lukewarlow created=2026-01-27T16:07:18Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3806070510 -->
## @lukewarlow — 2026-01-27

Fwiw I very strongly prefer making "light-dark(image, image)" just work. Adding a new function for this feels very wonky and as a web developer my brain still says "this should just do the thing for all properties". The fact it's currently only colors disappoints me when I try to use it. So if we can expand it to cover images as another common case then that's a big win to me.

<!-- comment id=3807794058 author=tabatkins created=2026-01-27T22:15:38Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3807794058 -->
## @tabatkins — 2026-01-27

I'm fine with that, now that we've established the escape-valve (`if()`) for all the *other* types.

<!-- comment id=3998881483 author=djmarland created=2026-03-04T17:03:46Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3998881483 -->
## @djmarland — 2026-03-04

Images being supported should also mean that `image-set()` is supported? I don't know if that has any bearing on whether it should be in `light-dark()` or a new function

<!-- comment id=4004214916 author=keithamus created=2026-03-05T10:57:36Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4004214916 -->
## @keithamus — 2026-03-05

An off-the-cuff question: would `light-dark(<image>,<image>)` open up the possibility for `background: light-dark(<image>,<color>)`? Given `<image>` would include gradients (I presume), I could imagine unfortunate hacks around types with something like `background: light-dark(url(my-light-image.png), linear-gradient(black, black))` if we don't allow for the combinations.

<!-- comment id=4005013576 author=emilio created=2026-03-05T13:19:56Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4005013576 -->
## @emilio — 2026-03-05

@keithamus I dunno, I think that'd be rather weird to start allowing random colors. also you can use `linear-gradient(black)` fwiw.

For that specific thing, you could do:

```
background-image: light-dark(url(my-light-image.png), none);
background-color: light-dark(transparent, black);
```

Perhaps?

<!-- comment id=4006021611 author=viktor-yakubiv created=2026-03-05T15:48:46Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4006021611 -->
## @viktor-yakubiv — 2026-03-05

This is an interesting case. I see two possible ways:

1. resolve light-dark substitution first and then validate against the property grammar
2. consider light-dark a single grammatical chunk, and validate it first, than (if ok) continue to higher level grammar validation

I conclude, that currently `light-dark()` is designed for the second case: it is part of `<color>`, not a completely separate syntax.

In the first case, the following is valid:

```css
background: light-dark(transparent, linear-gradient(white, black));
```
Depending on the color-scheme context, the background shorthand resolves to either `<‘background-color’>` or `<‘background-image’>` inferring from the returned ~syntax~ type by the `light-dark()`.

In the second case, the previous is _invalid_ but the following is valid:

```css
background: light-dark(linear-gradient(white, whitesmoke), linear-gradient(gray, black));
```

This second case design enforces `light-dark()` (if go beyond the `<color>`), accept only parameters of the same grammar, i.e. creates a template `light-dark<T>(light: T, dark: T)`. If the first parameter syntax does not match the second parameter syntax, the result is rejected, no matter what. If generalize the `light-dark()` parameters further than `<color> | <image>`, this is an extra condition.

I believe, that resulting syntax can be (and better be) checked at the property level. It would then allow cases of sub-type substitution, such as following:

```css
--text: oklch(light-dark(0%, 100%) 0 none);
color: var(--text);
```

---

Some of the previous assumptions are not correct: template function in CSS does not imply a _condition_. It would look like following:

```
light-dark() = light-dark( [ <color> , <color> ] | [ <image> , <image> ] )
```

Looks quite simple on paper. If generalize the previous examples, we get the heavier syntax like following:

```
light-dark() = light-dark(
  [ <color>         , <color>         ] |
  [ <image>         , <image>         ] |
  [ <blend-mode>    , <blend-mode>    ] |
  [ <‘font-weight’> , <‘font-weight’> ] )
```

This can be resolved using a simple _loop_, checking if the both items of list inside matche one of the pre-defined syntaxes.

In any case, if the current `light-dark()` accepts something other than `<color>`, it cannot be safely treated as `<color>` and receives own special case (syntax, evaluation).

<!-- comment id=4014486764 author=tabatkins created=2026-03-06T22:24:41Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4014486764 -->
## @tabatkins — 2026-03-06

Yeah, we can't allow that; it would mean that `background: light-dark(url(my-light-image.png), black) white;` is valid or not based on color-scheme, and so would probably require special-purpose validation logic to reject it as invalid preemptively.

We have `linear-gradient(black)` to produce a solid-color image, and some day `image(black)` if anyone ever chooses to actually implement the `image()` function.

<!-- comment id=4040708183 author=mirisuzanne created=2026-03-11T17:02:47Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4040708183 -->
## @mirisuzanne — 2026-03-11

Am I wrong to remember at some point a `light-dark-value()` or similar being planned?

<!-- comment id=4040713109 author=css-meeting-bot created=2026-03-11T17:03:37Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4040713109 resolution=true -->
## @css-meeting-bot — 2026-03-11

The CSS Working Group just discussed ``[css-images-4] Add `light-dark-image()`, or generalize `light-dark()` for images too?``, and agreed to the following:

* `RESOLVED: Overload light-dark() to accept image pairs`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> TabAtkins: We already have light-dark() for colors, makes it very convenient to write colors inline without indirection<br>
&lt;fantasai> TabAtkins: Similar request for other types of values, but the most important one is images. Identical use case to colors.<br>
&lt;fantasai> TabAtkins: a lot of agreement in the thread that this is a reasonable request<br>
&lt;fantasai> TabAtkins: proposal is that we either augment light-dark() to allow pair of colors OR pair of images, and resolves accordingly<br>
&lt;emilio> q+<br>
&lt;fantasai> TabAtkins: or we add a new function, light-dark-image(), same as light-dark() but accepts images instead<br>
&lt;astearns> q+<br>
&lt;fantasai> TabAtkins: Thread leans towards augmenting light-dark()<br>
&lt;fantasai> TabAtkins: There's further questions about allowint more values than just colors or images<br>
&lt;fantasai> TabAtkins: I prefer to not do that right now<br>
&lt;fantasai> TabAtkins: Partly because it has some parsing complications<br>
&lt;fantasai> TabAtkins: and also, we do have the ability to do that with if()<br>
&lt;fantasai> TabAtkins: and the use cases are rarer<br>
&lt;astearns> ack emilio<br>
&lt;fantasai> TabAtkins: So proposing to allow light-dark() to accept pair of images<br>
&lt;fantasai> emilio: I agree with Tab's proposal<br>
&lt;ydaniv> +1 TabAtkins<br>
&lt;fantasai> emilio: Third alternative is to make it an arbitrary substitution thing, but it makes problems especially for images<br>
&lt;fantasai> emilio: image caches, etc.<br>
&lt;fantasai> emilio: So keeping it simple, and if you want something complicated use if()<br>
&lt;fantasai> emilio: I haven't heard requests for any requests for anything else from FF frontend folks<br>
&lt;fantasai> astearns: So you implemented light-dark(&lt;img>, &lt;img>)?<br>
&lt;fantasai> emilio: yeah<br>
&lt;smfr> q+<br>
&lt;dbaron> Is there any complexity here about url() versus image() ?<br>
&lt;fantasai> astearns: I don't have a strong opinion on which way to go syntactically<br>
&lt;fantasai> astearns: Thinking about other extensions, overloading seems better than creating a new function per type<br>
&lt;fantasai> TabAtkins: If we branch out into more thing, grammar issues become more difficult.<br>
&lt;dbaron> (had to go, past 1pm)<br>
&lt;fantasai> TabAtkins: colors and images are 100% distinct (and should always be), but not doing that rn<br>
&lt;fantasai> TabAtkins: so we can address in the future<br>
&lt;astearns> ack smfr<br>
&lt;fantasai> smfr: People have been talking about URL arguments. Can you nest these?<br>
&lt;fantasai> emilio: Yes<br>
&lt;fantasai> emilio: And you can have the color version inside your gradient<br>
&lt;fantasai> emilio: could even nest that inside the image light-dark<br>
&lt;fantasai> [seems weird but doable]<br>
&lt;fantasai> PROPOSED: Overload light-dark() to accept image pairs<br>
&lt;fantasai> RESOLVED: Overload light-dark() to accept image pairs<br>
</details>

<!-- comment id=4040743440 author=djmarland created=2026-03-11T17:08:31Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4040743440 -->
## @djmarland — 2026-03-11

Just to confirm: this would allow `image-set`?

```
background: light-dark(
    image-set(
        url('lightbg-1x.png') 1x,
        url('lightbg-2x.png') 2x
    ),
    image-set(
        url('darkbg-1x.png') 1x,
        url('darkbg-2x.png') 2x
    )
)
```

<!-- comment id=4041043826 author=tabatkins created=2026-03-11T17:56:14Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4041043826 -->
## @tabatkins — 2026-03-11

> Am I wrong to remember at some point a `light-dark-value()` or similar being planned?

It was discussed, but that predated us having `if()`. Currently the plan is to just let `if()` cover the more general use-case.



> Just to confirm: this would allow `image-set`?

Yes, it'll accept two `<color>` values in any syntax, or two `<image>` values in any syntax.

<!-- comment id=4041830817 author=svgeesus created=2026-03-11T19:58:33Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4041830817 -->
## @svgeesus — 2026-03-11

So concretely:

- CSS Color 5 already has a [color-only `light-dark()`](https://drafts.csswg.org/css-color-5/#light-dark)
- CSS Images 4 does not have an image-only `light-dark()` but from [this resolution](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4040713109), it will add it
- where does grammar go to say that you can have either two images or two colors, no other combination
- where does spec text go to say what happens if you try to mix them?

<!-- comment id=4049191500 author=tabatkins created=2026-03-12T18:50:54Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4049191500 -->
## @tabatkins — 2026-03-12

I think we should just let one or the other spec define the entire feature, and the other one will just reference it. Since Color already holds the definition, I'm fine with it staying there.

<!-- comment id=4049809022 author=svgeesus created=2026-03-12T20:27:47Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4049809022 -->
## @svgeesus — 2026-03-12

Sounds good.

<!-- comment id=4054781762 author=svgeesus created=2026-03-13T12:33:30Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4054781762 -->
## @svgeesus — 2026-03-13

@tabatkins okay how it this looking?

 - https://github.com/w3c/csswg-drafts/pull/13651

<!-- comment id=4065064037 author=cdoublev created=2026-03-16T05:02:43Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4065064037 -->
## @cdoublev — 2026-03-16

@svgeesus I think that the definition should also say that the function is invalid when it takes `<color>`s in contexts expecting an `<image>`, and the other way around, otherwise `color: light-dark(url(foo.webp), url(bar.webp))` would be valid at parse time, which I think is not intentional, and if I am not mistaken.

<!-- comment id=4085662455 author=svgeesus created=2026-03-18T21:23:37Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4085662455 -->
## @svgeesus — 2026-03-18

Yes, absolutely. I also prefer to have a linkable target for each of the two forms.

Currently we have

```css
light-dark() =  light-dark( [<color>, <color>] | [<image>, <image>])
```

This should be

```css
light-dark() =  <light-dark-color> | <light-dark-image>
<light-dark-color> = light-dark(<color>, <color>)
<light-dark-image> = light-dark(<image>, <image>) 
```

and then, in the [definition of the `<color>` type](https://drafts.csswg.org/css-color-5/#color-syntax) in CSS Color 5:

```css
<color> = <color-base> | currentColor | <system-color> | 
      <contrast-color()> | <device-cmyk()>  | <light-dark-color>
```

and in the [definition of the `<image>` type](https://drafts.csswg.org/css-images-4/#image-values) in CSS Images 4:

```css
<image> = <url> | <image()> | <image-set()> | <cross-fade()> | 
          <element()> | <gradient> | <light-dark-image>
```

<!-- comment id=4089117510 author=viktor-yakubiv created=2026-03-19T10:21:24Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4089117510 -->
## @viktor-yakubiv — 2026-03-19

Is circular grammar dependency for color acceptable here?

<!-- comment id=4089973119 author=Crissov created=2026-03-19T13:01:44Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4089973119 -->
## @Crissov — 2026-03-19

`image(light-dark(color(…), black))` – fun times ahead. 

Speaking of [`image()`](https://drafts.csswg.org/css-images-4/#image-notation), it feels like just extending `<image-tags>` to `[rtl|ltr] || [light | dark]` would also have been a viable option, which would have the additional benefit of pushing the adoption of this function.

PS: Okay, maybe that would not have been quite the same, because authors could only tag which kind of image this is, not provide an alternative resource for the other one:
* `image(light "day.png", light-dark(white, black))`
* `image(dark "night.png", light-dark(white, black))`
* ~~`image(light "day.png", dark "night.png", light-dark(white, black))`~~

PPS: That is unless either `image()` was changed to accept multiple (optionally tagged) URL resources or [`image-set()`](https://drafts.csswg.org/css-images-4/#typedef-image-set-option) got a new light/dark option alongside resolution and type:
* `image(image-set("day.png" light, "night.png" dark), light-dark(white, black)))`
* `image-set(image("day.png", white) light, image("night.png", black) dark)`
* `image(light "day.png", dark "night.png", light-dark(white, black))`
* `image(light "day.png", dark "night.png", light white, dark black)`
* `image(light "day.png" white, dark "night.png" black)`
* `light-dark(image("day.png", white), image("night.png", black))` – *adopted syntax*
* `light-dark(white, black) light-dark("day.png", "night.png")` – adopted syntax in some shorthand properties  like `background`

<!-- comment id=4089996649 author=yisibl created=2026-03-19T13:05:22Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4089996649 -->
## @yisibl — 2026-03-19

I'd like to know if it's possible to support mixed syntax like `light-dark(<color>, <image>)`. If not, I can imagine future CSS authors writing hacks like:

```css
background: light-dark(linear-gradient(red), url(foo.png))
```

<!-- comment id=4090038150 author=bramus created=2026-03-19T13:11:55Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4090038150 -->
## @bramus — 2026-03-19

> I'd like to know if it's possible to support mixed syntax like `light-dark(<color>, <image>)`.

No, as this would lead to parsing issues. We need to be very specific _where_ the `light-dark()` with colors and the `light-dark()` with images is allowed. See https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4085662455 for the proposed syntax details.

Practical example being `background: red light-dark(blue, url(dark.png));`. With a `dark` color-scheme that would be OK, but in a `light` color-scheme you’d end up with `background: red blue` which is invalid.

<!-- comment id=4090114790 author=yisibl created=2026-03-19T13:25:12Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4090114790 -->
## @yisibl — 2026-03-19

Additionally, I noticed that both @emilio and @LeaVerou mentioned that `light-dark()` supports the `none` keyword, but the current syntax does not include it.
- https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4005013576
  `background-image: light-dark(url(my-light-image.png), none);`
- https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-3429474969
  `background: light-dark(white, transparent) light-dark(none, linear-gradient(gray, black));`

Currently, `none` is valid in both Chrome and Firefox implementations.@fsoder raised this issue while reviewing the [Blink code](https://chromium-review.googlesource.com/c/chromium/src/+/7675477/7/third_party/blink/renderer/core/css/properties/css_parsing_utils.cc#3665), so I think it should be clarified.

<!-- comment id=4090157656 author=yisibl created=2026-03-19T13:31:32Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4090157656 -->
## @yisibl — 2026-03-19

> No, as this would lead to parsing issues. We need to be very specific _where_ the `light-dark()` with colors and the `light-dark()` with images is allowed. See [#12513 (comment)](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4085662455) for the proposed syntax details.
> 
> Practical example being `background: red light-dark(blue, url(dark.png));`. With a `dark` color-scheme that would be OK, but in a `light` color-scheme you’d end up with `background: red blue` which is invalid.

Thank you for your explanation. That’s a very clear explanation, and I think it’s worth including in the specification text.

<!-- comment id=4091181812 author=Crissov created=2026-03-19T15:50:59Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4091181812 -->
## @Crissov — 2026-03-19

@yisibl This would also be valid and more straightforward:
~~~~ css
background: light-dark(image(red), image("foo.png"));
~~~~

Given that `image()` may accept a single `<color>` argument but still always returns `<image>` itself, I think `light-dark()` could very well accept a mix of `<image>` and `<color>` but always return `<image>` then, basically auto-wrapping `image(<color>)` (similar to how `<string>` may effectively be auto-wrapped by `url()` or `src()` in some places).

~~~~ ebnf
<light-dark-image> = light-dark( [<image>, <image> | <color>] | [<color>, <image>] ) 
~~~~

<!-- comment id=4092448365 author=tabatkins created=2026-03-19T18:38:27Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4092448365 -->
## @tabatkins — 2026-03-19

> Additionally, I noticed that both @emilio and @LeaVerou mentioned that light-dark() supports the none keyword, but the current syntax does not include it.

It's certainly not supposed to. `none` isn't an image. It's very intentional that that's not part of the grammar. (But a transparent image does the same thing.)

That said, we could explicitly add `none` into the argument grammar for the image form, and have it resolve to a transparent image. That is, `light-dark( [ <image> | none ] , [ <image> | none ] )`

--------

@svgeesus Yeah that grammar approach looks good.

<!-- comment id=4093249444 author=svgeesus created=2026-03-19T20:57:51Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4093249444 -->
## @svgeesus — 2026-03-19

> Is circular grammar dependency for color acceptable here?

Yes, nesting is explicitly allowed.

<!-- comment id=4093399751 author=viktor-yakubiv created=2026-03-19T21:26:25Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4093399751 -->
## @viktor-yakubiv — 2026-03-19

@svgeesus thank you for clarifying. I was wondering how this does make sense... Technically, the grammar allows the following:

```
light-dark(light-dark(white, black), light-dark(black, white))
```

However, I see that [this](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4085662455) probably is the best way to express the grammar.

<!-- comment id=4093520167 author=LeaVerou created=2026-03-19T21:50:49Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4093520167 -->
## @LeaVerou — 2026-03-19

@viktor-yakubiv Literally every color manipulation function (e.g. `color-mix()`, `contrast-color()` etc) has a "circular grammar". It's not a circularity, it's how grammars work. Think of it as types, not variables.

<!-- comment id=4097510267 author=CGQAQ created=2026-03-20T11:55:40Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4097510267 -->
## @CGQAQ — 2026-03-20

Can we do nesting? like `light-dark(light-dark(url(a), url(b)), url(c))`

>   `<image> = <url> | <image()> | <image-set()> | <cross-fade()> | <element()> | <gradient> | <light-dark-image>`

So I saw `<image>` has `<light-dark-image>`, which means it can be nesting, but it doesn't seem to make any sense. because in the case of `light-dark(light-dark(url(a), url(b)), url(c))`, if it's light theme, we will see `url(a)`, if it's dark theme, we will see `url(c)`, either way, don't show `url(b)` at all.

<!-- comment id=4097661604 author=bramus created=2026-03-20T12:25:48Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4097661604 -->
## @bramus — 2026-03-20

> Can we do nesting? like `light-dark(light-dark(url(a), url(b)), url(c))`

The grammar allows using it to be used that way but like you say it’s not meaningful for authors to do it with `light-dark()` itself. 

This isn’t really an issue, because that is how grammars work — [see Lea’s reply](https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4093520167).

<!-- comment id=4112904959 author=svgeesus created=2026-03-23T18:43:36Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4112904959 -->
## @svgeesus — 2026-03-23

> Currently, `none` is valid in both Chrome and Firefox implementations.[@fsoder](https://github.com/fsoder) raised this issue while reviewing the [Blink code](https://chromium-review.googlesource.com/c/chromium/src/+/7675477/7/third_party/blink/renderer/core/css/properties/css_parsing_utils.cc#3665), so I think it should be clarified.

So, is the consensus to add a `none` option for the imge form of `light-dark()`?

<!-- comment id=4113020766 author=tabatkins created=2026-03-23T19:03:12Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4113020766 -->
## @tabatkins — 2026-03-23

Yeah, representing a transparent image with no natural sizes, identical to `linear-gradient(transparent)`.

<!-- comment id=4113634803 author=svgeesus created=2026-03-23T20:52:25Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4113634803 -->
## @svgeesus — 2026-03-23

The [recently-added WPT](https://github.com/web-platform-tests/wpt/blob/master/css/css-color/light-dark-image.html) for the image form tests `none`, at least in terms of parsing.

<!-- comment id=4113654690 author=emilio created=2026-03-23T20:55:54Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4113654690 -->
## @emilio — 2026-03-23

If we add `none`, any reason not to add it for all `<image>`s more generally?

<!-- comment id=4113997267 author=svgeesus created=2026-03-23T22:00:01Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4113997267 -->
## @svgeesus — 2026-03-23

> If we add `none`, any reason not to add it for all `<image>`s more generally?

That would seem to be the cleanest solution

<!-- comment id=4114524827 author=LeaVerou created=2026-03-24T00:14:17Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4114524827 -->
## @LeaVerou — 2026-03-24

The usual reason we can't have nice things: compat 😅
`none` is used all over the place for all sorts of things. E.g. `border-style: none` or `display: none` or `animation-name: none` are not images. 
In several places it can be used instead of a number or dimension, such as color components or `clamp()`.
That by itself is not a problem — we _can_ have the same tokens resolve to different types depending on the usage site. E.g. remember when the `100` in `font-weight: 100` was actually a keyword?
However, it makes it likely that there are places where it would create a disambiguation problem to define `none` as an image, i.e. usage sites where `none` is already accepted alongside `<image>` and they mean different things.

There are two components to this:
1. Is it compatible to add `none` to `<image>`?
2. IFF it _is_, do we want to do this? Could it back us into a corner for the future?

It would definitely be great for authors to have a nice initial value when they define an `<image>` custom property, whereas right now there is nothing. Either they need to specify their initial value using warts like `linear-gradient(transparent)` or add a separate keyword and handle it separately (`syntax: <image> | none;`).

It does seem that many CSS properties that accept images already accept `<image> | none`, which means any shorthands that include them are also safe:
- `background-image`
- `border-image-source`
- `list-style-image`
-	`mask-border-source`

Although there is the question of whether even in these properties, `none` produces any observable differences over the dimensionless transparent image we're discussing here. 

However, many other properties accept other values _as well_, and `none` has observable differences over `linear-gradient(transparent)`:
-	`content` is the biggest issue. `content: none` is very different than `content: linear-gradient(transparent)`.
-	`shape-outside` — `none | [ <basic-shape> || <shape-box> ] | <image>`
-	`shape-inside` — `auto | outside-shape | [ <basic-shape> || shape-box ] | <image> | display`
-	`mask-image` — via `<mask-reference> = none | <image> | <mask-source>`

Note that [`<paint>` ](https://drafts.csswg.org/fill-stroke-3/#paint)is defined as `<paint> = none | <image> | <svg-paint>` and used in:
•	`fill-image` — `<paint>#;` the spec text says these are image layers interpreted like background-image  ￼
•	`stroke-image` — `<paint>#;` likewise interpreted like background-image  ￼

My suggestion: Right now, while most places that accept `<image>` _also_ accept `none`, what that does varies. Let's define a new production that resolves to `<image> | none` (e.g. `<image-or-none>`, `<optional-image>`, TBB) and say that syntax hosts need to define what `none` does. Then, we can replace it in all these grammars, and define `light-dark()` to use that.

<!-- comment id=4116199037 author=Crissov created=2026-03-24T08:11:12Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4116199037 -->
## @Crissov — 2026-03-24

`image(none)` would be unambiguous and always an `<image>`, though.

<!-- comment id=4121126928 author=svgeesus created=2026-03-24T20:23:06Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4121126928 -->
## @svgeesus — 2026-03-24

> If we add `none`, any reason not to add it for all `<image>`s more generally?

Pull request to do that (and add the image form of light-dark, too)
 - https://github.com/w3c/csswg-drafts/pull/13713

<!-- comment id=4121365592 author=tabatkins created=2026-03-24T21:07:29Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4121365592 -->
## @tabatkins — 2026-03-24

Thanks for the compat analysis, Lea. I agree that it's likely unsafe to have a `none` keyword be generically an `<image>`, since we use `none` in plenty of other ways and those ways sometimes combine with images. It's the sort of thing that would be a constant pain point to remember to control for; *every other* image value is grammatically unambiguous in *any* position.

I don't think we need a special production just for this. `<image-or-none>` isn't any shorter or more comprehensible than `<image> | none`.

<!-- comment id=4133481168 author=lilles created=2026-03-26T10:34:57Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4133481168 -->
## @lilles — 2026-03-26

`light-dark()` is resolved at computed value time, right? So `light-dark(none, none)` won't round trip if it computes to `none`.

What if you have a registered custom property syntax `"none|<image>"`, where you want `none` to mean something else than an empty/missing image?


<!-- comment id=4133503817 author=emilio created=2026-03-26T10:37:49Z url=https://github.com/w3c/csswg-drafts/issues/12513#issuecomment-4133503817 -->
## @emilio — 2026-03-26

Yeah, I think we should probably just not support `none` for now / move that to a separate issue.
