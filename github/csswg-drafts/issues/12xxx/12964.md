---
number: 12964
title: "[css-grid-2] Aspect ratio and the row axis"
state: "open"
labels: ["Needs Testcase (WPT)", "css-grid-2"]
author: "alisonmaher"
created_at: "2025-10-16T22:06:05Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12964"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:29Z"
---

In the following test case:

```
<p>Test passes if there is a filled green square.</p>
<div style="display: inline-grid; background: green; aspect-ratio: 1/1; min-height: 60px; grid-template-rows: repeat(auto-fill, 50px);"></div>
```

All browsers appear to render a 60x60 green square, as opposed to 100x100. In order to hit the `min-height`, though, you'd expect to produce 2 50px rows.

<img width="240" height="103" alt="Image" src="https://github.com/user-attachments/assets/fd87bd24-09c8-4500-accb-c223aa09257b" />

In the column direction, however, this works as expected, and 2 50px columns are produced, resulting in a 100x100 square:
```
<p>Test passes if there is a filled green square.</p>
<div style="display: inline-grid; background: green; aspect-ratio: 1/1; min-height: 60px; grid-template-columns: repeat(auto-fill, 50px);"></div>
```

<img width="241" height="128" alt="Image" src="https://github.com/user-attachments/assets/ebc6ef2f-da26-415b-a7fc-7f25567e0a5b" />

Is this expected to only work in the column direction? (Same question applies for masonry)

<!-- comment id=3792975616 author=tabatkins created=2026-01-23T23:15:01Z url=https://github.com/w3c/csswg-drafts/issues/12964#issuecomment-3792975616 -->
## @tabatkins — 2026-01-23

This just looks like an impl bug. The spec is clear, and we should def be consistent between rows and columns. I suspect someone early on just wrote a buggy test, and everyone followed the test instead of the spec?

<!-- comment id=3793026486 author=Loirooriol created=2026-01-23T23:41:01Z url=https://github.com/w3c/csswg-drafts/issues/12964#issuecomment-3793026486 -->
## @Loirooriol — 2026-01-23

I don't think it's because of a buggy test.

It's because, in the 2nd case, when computing the min/max-content inline sizes you transfer size constraints from the block axis thru the aspect ratio. So (roughly speaking) `min-height: 60px` gets transferred into `min-width: 60px`. And this results in a width of 100px:

```html
<div style="display: inline-grid; background: green; height: 10px; min-width: 60px; grid-template-columns: repeat(auto-fill, 50px);"></div>
```

In the first case, though, you first compute the min/max-content inline sizes. And you don't want to perform layout when doing so, so you just transfer `min-height: 60px`, even if grid layout would end up producing a height of 100px. And this results in a width of 60px:

```html
<div style="display: inline-grid; background: green; height: 10px; min-width: 60px; grid-template-rows: repeat(auto-fill, 50px);"></div>
```
