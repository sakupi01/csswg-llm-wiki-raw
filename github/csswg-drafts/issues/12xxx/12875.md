---
number: 12875
title: "[css-values-5] support HTML syntax such as <html-integer> in typed attr()"
state: "open"
labels: ["css-values-5"]
author: "dbaron"
created_at: "2025-09-29T16:34:50Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12875"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:45:22Z"
---

The [typed `attr()` spec](https://drafts.csswg.org/css-values-5/#attr-notation) allows attributes in the markup to be parsed according to CSS parsing rules and used as the values of CSS properties.  However, attributes in the document might already be defined using a syntax that is somewhat different from the CSS parsing rules, such as HTML's [rules for parsing integers](https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#rules-for-parsing-integers) which ignore unexpected characters at the end of the integer.

While these differences are often not very important if somebody is writing a style sheet to be used on their own page, they can be more important in style sheets that are meant to be applied to pages written by others.  (One significant style sheet that falls in this category is the UA style sheet!)

It seems like we might want to add support for some common attribute syntaxes that are present in HTML so that they can be used as CSS property values based on HTML's parsing rules.  Probably the two most significant examples that come to mind would be (if I take the liberty of trying to name them at the same time) [`<html-integer>`](https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#rules-for-parsing-integers) and [`<html-nonnegative-integer>`](https://html.spec.whatwg.org/multipage/common-microsyntaxes.html#rules-for-parsing-non-negative-integers).

(I was thinking about this in the context of styling `<select>` elements based on their `size` attribute.  cc @josepharhar.)

<!-- comment id=3349365271 author=tabatkins created=2025-09-29T23:20:30Z url=https://github.com/w3c/csswg-drafts/issues/12875#issuecomment-3349365271 -->
## @tabatkins — 2025-09-29

Hm. I'm not necessarily opposed to this, but I question the need to expose these values publicly. Having some internal values that trigger the legacy HTML parsing, to enable turning some magic into UA stylesheet, sounds like it would satisfy the need just fine.

Having authors think about the difference between `number` and `html-integer` (which is solely some legacy details) just sounds bad.

<!-- comment id=3352588469 author=dbaron created=2025-09-30T14:52:18Z url=https://github.com/w3c/csswg-drafts/issues/12875#issuecomment-3352588469 -->
## @dbaron — 2025-09-30

Even though it sort of works, it seems weird to me to encourage developers (especially those building things that are more library-like) to style based on stricter parsing than the underlying language requires.

I wonder if an alternative is to be able to style based on a normalized reflection of the attributes rather than their raw values (probably tied to the [reflection rules](https://html.spec.whatwg.org/multipage/common-dom-interfaces.html#reflecting-content-attributes-in-idl-attributes) for the relevant IDL attribute).

<!-- comment id=3353874503 author=tabatkins created=2025-09-30T21:32:13Z url=https://github.com/w3c/csswg-drafts/issues/12875#issuecomment-3353874503 -->
## @tabatkins — 2025-09-30

I mean, HTML presumably would have *wanted* to have a nice, reasonable strict parsing algorithm if it were possible, but we've got too much legacy from before people considered that a nice-to-have feature. Surely nobody is writing `<select size=30px>` today and *expecting* it to work, or if they are, considering it a problem they'll refuse to fix when writing new CSS against it.

I wouldn't be opposed to attr() using the reflection (for built-in attributes with that behavior), I just lean toward it not being particularly necessary for authors. Attributes written like that are buggy; we probably could have branched to prevent them from working at all with the `<!doctype html>` switch if Hixie had valued that, but his style was definitely more towards paving every cowpath.

<!-- comment id=3353952059 author=josepharhar created=2025-09-30T22:09:35Z url=https://github.com/w3c/csswg-drafts/issues/12875#issuecomment-3353952059 -->
## @josepharhar — 2025-09-30

Here is where it could be used for customizable select: https://github.com/w3c/csswg-drafts/issues/12510#issuecomment-3314016143
