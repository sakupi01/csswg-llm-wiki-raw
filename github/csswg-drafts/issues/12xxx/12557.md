---
number: 12557
title: "[css-overflow] How should `text-overflow: <string>` handle forced line breaks?"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "Needs Testcase (WPT)", "css-overflow-4"]
author: "jja08111"
created_at: "2025-08-01T05:44:30Z"
closed_at: "2026-06-19T05:00:35Z"
url: "https://github.com/w3c/csswg-drafts/issues/12557"
comments: 7
has_resolution: true
synced_at: "2026-07-04T04:45:46Z"
---

### Summary

The specification for `text-overflow` does not appear to define the expected behavior when the value contains a forced line break character (e.g., \0A). This has led to differing behaviors between browser implementations, creating an interoperability issue.

### Details

During the implementation of `text-overflow` in Chromium, a question was raised about how to handle a string containing a forced line breaks character, for example, on a paragraph with `white-space: pre`.

There are currently different behaviors in browsers:

1. Chromium: In the [related implementation patch](https://chromium-review.googlesource.com/c/chromium/src/+/6773707), a string containing `\0A` results in the character being rendered as a missing glyph symbol (☒).
2. Firefox: The newline character is effectively removed from the string. For example, if the overflow string is `12 \0A 34`, it is rendered as if it were `12 34`.
3. Safari: String values not supported.

### Discussion & The Question for the CSSWG

This ambiguity was noted during the Chromium patch review. The behavior was not discussed in related issues https://github.com/w3c/csswg-drafts/issues/11660 or https://github.com/w3c/csswg-drafts/issues/11683. The current thinking is that this behavior is undefined because forced line breaks are typically handled during the layout phase, before the text shaper (which would handle the overflow string) is invoked.

This issue is filed to seek clarification from the CSSWG on the intended behavior. What should an implementation do when a forced line break character is present in the `text-overflow: <string>`?

Possible resolutions could be:

- The line break character should be stripped from the string (Firefox's behavior).
- The character should be rendered as a replacement or missing glyph character (Chromium's behavior).
- The character should be treated as another form of whitespace, like a space.
- The spec should define that including such control characters makes the value invalid.

We believe the specification should be updated to provide a clear directive for this case to ensure consistent rendering across all user agents.

<!-- comment id=3144227617 author=SebastianZ created=2025-08-01T11:19:56Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-3144227617 -->
## @SebastianZ — 2025-08-01

One solution might also be to _allow_ forced line breaks but clip the string to be one line high.

There's already a [note in the spec.](https://drafts.csswg.org/css-overflow-4/#:~:text=Setting%20the%20block%20overflow%20ellipsis) about avoiding layout reflows:

> Setting the block overflow ellipsis's line-height to 0 makes sure that inserting it cannot cause the line’s height to grow, which could cause further relayouts and potentially cycles. This is almost equivalent to inserting the block overflow ellipsis as a paint-time operation, except that it still participates in alignment and justification. The downside is that unusually tall / deep glyphs in the block overflow ellipsis may overflow.

So the ellipsis might be rendered as if `line-clamp: 1 '';` were applied to it. That would apply to the ellipses in both `text-overflow` and `block-ellipsis`.

Sebastian

<!-- comment id=3151275220 author=frivoal created=2025-08-04T15:37:58Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-3151275220 -->
## @frivoal — 2025-08-04

That note is about `block-ellipsis`. `text-overflow` has different mechanics.

<!-- comment id=3184486584 author=andreubotella created=2025-08-13T15:54:27Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-3184486584 -->
## @andreubotella — 2025-08-13

> That note is about `block-ellipsis`. `text-overflow` has different mechanics.

I don't think they're different enough. There's a lot of common behavior that's expressed in different words – for example, I think this `line-height: 0` behavior for `block-ellipsis` is included in the "Ellipsing only affects rendering and must not affect layout" spec text for `text-overflow`.

I think whatever solution we end up choosing for `text-overflow` should also apply to `block-ellipsis`; but I'm not sure that clamping the ellipsis text itself is a good idea.

I'd say that if the string text has a forced line break, this is almost certainly a bug by the web developer; but since the presence of a forced line break depends on `white-space-collapse`, this isn't something that can be detected at parse time. Given that, an option could be to fall back into behaving like `text-overflow: clip` in that case. But another option would be to treat the string as ending at the first forced break – essentially @SebastianZ's proposal of clipping it to be one line high, but without (re?)ellipsizing it.

<!-- comment id=3960338549 author=fantasai created=2026-02-25T16:06:07Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-3960338549 -->
## @fantasai — 2026-02-25

We could re-use the inlinification rules from Ruby: https://www.w3.org/TR/css-ruby/#box-fixup

<!-- comment id=3960400974 author=css-meeting-bot created=2026-02-25T16:16:27Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-3960400974 resolution=true -->
## @css-meeting-bot — 2026-02-25

The CSS Working Group just discussed ``[css-overflow] How should `text-overflow: <string>` handle forced line breaks?``, and agreed to the following:

* `RESOLVED: line breaks are suppressed as in ruby`

<details><summary>The full IRC log of that discussion</summary>
&lt;florian> q+<br>
&lt;ydaniv> andreubotella: related to line-clamp, what happens when you have a force line break, even if you have a span inside the paragraph, and the elipsis string still forces a line break<br>
&lt;ydaniv> ... currently Firefox seem to remove the new line from the string<br>
&lt;ydaniv> ... it's not clear what the behavior should be, and whether is should be common to text-overflow and elipsis<br>
&lt;ydaniv> ... for text-overflow I think it should be that elipsis can not occupy more than one string<br>
&lt;ydaniv> ... either trimming the new line or removing it<br>
&lt;dbaron> (maybe Andreu meant "one line" rather than "one string", I think?)<br>
&lt;ydaniv> ... for block elipsis, not so clear, but impl. wise would be nice to work the same way<br>
&lt;ydaniv> ... it's better to just deal with that line the same<br>
&lt;astearns> ack florian<br>
&lt;ydaniv> florian: I think problem is were inhering the behavior of white space from outside<br>
&lt;ydaniv> ... I also think that generally the whitespace processing is complecated enough, don't want to invent more<br>
&lt;ydaniv> ... if we set whitespace nowrap it supress the new line and that's fine<br>
&lt;ydaniv> ... perhaps in future we want a pseudo element that handles this, some day<br>
&lt;ydaniv> ... but for now new lines turn into spaces and not wrap<br>
&lt;ydaniv> ... I think that's simple, and would be my preference<br>
&lt;ydaniv> astearns: wondering whether changing the wrap value is an overkill<br>
&lt;ydaniv> florian: given we don't have a pseudo class/element to describe it, it's the same<br>
&lt;ydaniv> ... so far we don't have a way to address it directly and that's the behavior you get<br>
&lt;ydaniv> ... spending a lot of time to describe the behavior is not a good use of time<br>
&lt;ydaniv> astearns: if the mechanism is nowrap, may be other side effects we did not consider<br>
&lt;dbaron> (what we do seems like it could affect what happens to runs of multiple spaces, under some conditions)<br>
&lt;ydaniv> andreubotella: checking if we have chars with forced line breaks even with  nowrap...<br>
&lt;ydaniv> ... some are converted to whitespace at this points, not sure how all of these interact<br>
&lt;ydaniv> florian: some of these would demand a forced line break<br>
&lt;astearns> ack fantasai<br>
&lt;fantasai> https://www.w3.org/TR/css-ruby/#anon-gen-unbreak<br>
&lt;ydaniv> fantasai: perhaps copy the rules of surpressing line breaks in ruby<br>
&lt;ydaniv> astearns: shall we resolve on that?<br>
&lt;ydaniv> andreubotella: Sebastian proposed another thing that may be interesting, there are other possible solutions, like clipping first line, but probably would be better to supress the line break<br>
&lt;ydaniv> PROPOSED RESOLUTION: line breaks are converted to spaces as in ruby<br>
&lt;ydaniv> fantasai: not necessarily converted to whitespace, just suppressed, depending on context<br>
&lt;ydaniv> PROPOSED RESOLUTION: line breaks are suppressed as in ruby<br>
&lt;ydaniv> astearns: objections?<br>
&lt;ydaniv> florian: if you look at ruby it has a note that we're doing it since it's simple<br>
&lt;ydaniv> ... should we remove it?<br>
&lt;ydaniv> astearns: I'd prefer to leave it for now<br>
&lt;ydaniv> florian: ok<br>
&lt;ydaniv> andreubotella: we should probably link to that issue<br>
&lt;ydaniv> ... so that if there's an update<br>
&lt;ydaniv> florian: just a comment for now, no issue<br>
&lt;florian> https://drafts.csswg.org/css-ruby-1/#issue-f0987196<br>
&lt;ydaniv> astearns: if you feel strongly about it please open an issue<br>
&lt;ydaniv> RESOLVED: line breaks are suppressed as in ruby<br>
</details>

<!-- comment id=4623861903 author=andreubotella created=2026-06-04T15:55:13Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-4623861903 -->
## @andreubotella — 2026-06-04

Had we agreed that this resolution also applies to `block-ellipsis`, or only to `text-overflow`?

<!-- comment id=4704772938 author=frivoal created=2026-06-15T05:13:18Z url=https://github.com/w3c/csswg-drafts/issues/12557#issuecomment-4704772938 -->
## @frivoal — 2026-06-15

My understanding is that it applies to both.
