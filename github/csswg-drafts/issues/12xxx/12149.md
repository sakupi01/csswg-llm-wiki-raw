---
number: 12149
title: "[css-view-transitions-1] Removing the root element should probably skip the transition synchronously"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-view-transitions-1"]
author: "emilio"
created_at: "2025-05-02T14:14:54Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12149"
comments: 4
has_resolution: true
synced_at: "2026-07-04T04:45:18Z"
---

If during an animating view transition you replace the root element, then the originating element of the pseudo-element tree is disconnected, so you can't reasonably do stuff like computing styles with it or what not.

The spec doesn't mention what happens in this case. It probably should skip the transition?

Another alternative could be for the view transition pseudos to be reparented somehow (once / if there's a new root?), but that seems error prone and I don't think there's any use case for that.

Context for it is https://bugzilla.mozilla.org/show_bug.cgi?id=1963212 fwiw.

cc @vmpstr @noamr 

<!-- comment id=2851275233 author=noamr created=2025-05-05T14:55:34Z url=https://github.com/w3c/csswg-drafts/issues/12149#issuecomment-2851275233 -->
## @noamr — 2025-05-05

In this case, the transition would be skipped in the next frame because of https://www.w3.org/TR/css-view-transitions-1/#handle-transition-frame step 4. The transition is skipped when there is no active animation, which would be the case here.

<!-- comment id=2851757710 author=emilio created=2025-05-05T17:31:04Z url=https://github.com/w3c/csswg-drafts/issues/12149#issuecomment-2851757710 -->
## @emilio — 2025-05-05

@noamr but what if you add a new root element and somehow the styles so that there's an animation? Or is your interpretation that since you removed the parent of the view transition pseudo tree (and thus the view transition pseudo-tree itself) then there's no pseudo-tree and thus no animation?

<!-- comment id=2852115051 author=noamr created=2025-05-05T19:29:22Z url=https://github.com/w3c/csswg-drafts/issues/12149#issuecomment-2852115051 -->
## @noamr — 2025-05-05

> [@noamr](https://github.com/noamr) but what if you add a new root element and somehow the styles so that there's an animation? Or is your interpretation that since you removed the parent of the view transition pseudo tree (and thus the view transition pseudo-tree itself) then there's no pseudo-tree and thus no animation?

Yea, the pseudo-tree was attached to the old document element [here](https://www.w3.org/TR/css-view-transitions-1/#setup-transition-pseudo-elements) and the new one doesn't have a pseudo-tree and thus no animation, so the view transition would be skipped in the next frame. Is that sufficiently specified?

<!-- comment id=3200465102 author=css-meeting-bot created=2025-08-19T11:59:41Z url=https://github.com/w3c/csswg-drafts/issues/12149#issuecomment-3200465102 resolution=true -->
## @css-meeting-bot — 2025-08-19

The CSS Working Group just discussed `[css-view-transitions-1] Removing the root element should probably skip the transition synchronously`, and agreed to the following:

* `RESOLVED: Disconnecting the VT root should skip the VT sync with timing wrt promises TBD`

<details><summary>The full IRC log of that discussion</summary>
&lt;ntim> emilio: I'm not super convinced the current spec deals with it right<br>
&lt;ntim> emilio: the issue is that when you remove the root element, nothing happens sync at the doc level to skip the transition<br>
&lt;ntim> emilio: which means that depending on how you define the active-view-transition, you have a minor issue<br>
&lt;ntim> emilio: where you have an active VT but not a root element<br>
&lt;ntim> emilio: noamr claims that if we remove the root element, handle transition frame would skip the VT, but its not clear to me<br>
&lt;ntim> ...because it depends whether the active view transition is stashed on the root element or the document itself<br>
&lt;ntim> ...the spec says active VT of the document<br>
&lt;ntim> ...I don't think this is dealt with in the spec<br>
&lt;ntim> scribe+<br>
&lt;fantasai> i/OK, I'll post/scribe+ ntim/<br>
&lt;ntim> astearns: we have agreement?<br>
&lt;ntim> emilio: it's edge-casy but I found it during impl<br>
&lt;ntim> noamr: It ties into scoped VT where any element could be a root<br>
&lt;ntim> noamr: we could say removing the element pseudo-tree skips the VT<br>
&lt;ntim> emilio: i think it needs to be explicit since it's observable<br>
&lt;SebastianZ> q+<br>
&lt;ntim> emilio: especially with the new document.activeVT API<br>
&lt;ntim> emilio: we need to specify whether it's sync/asynchronous<br>
&lt;ntim> noamr: it requires a bit of design, but we can resolve on finding a design that skips the VT<br>
&lt;astearns> ack SebastianZ<br>
&lt;ntim> SebastianZ: what would happen if we skipped the VT async?<br>
&lt;ntim> emilio: depends, but you'd be able to observe it via document.activeVT<br>
&lt;ntim> SebastianZ: there would be a jank?<br>
&lt;ntim> emilio: not really, not observable via the rendering, but via the API<br>
&lt;ntim> emilio: the reason it makes sense it's sync, is because you have that edge case where we remove the root element, and I don't think it should keep the VT running<br>
&lt;flackr> s/and I don't think/ and add it again, then I don't think<br>
&lt;ntim> noamr: proposed resolution, disconnecting an element should skip the VT sync with timing wrt promises TBD<br>
&lt;ntim> VT root*<br>
&lt;ntim> RESOLVED: Disconnecting the VT root should skip the VT sync with timing wrt promises TBD<br>
</details>
