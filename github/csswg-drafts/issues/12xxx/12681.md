---
number: 12681
title: "[css-conditional][selectors] A way to set styles according to the used color scheme"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution"]
author: "JoshTumath"
created_at: "2025-08-29T11:51:38Z"
closed_at: "2025-09-24T22:24:44Z"
url: "https://github.com/w3c/csswg-drafts/issues/12681"
comments: 16
has_resolution: false
synced_at: "2026-07-04T04:45:22Z"
---

See also: #10577

## Problem

We can use `@media (prefers-color-scheme)` to set styles according to the user's preferred colour scheme. We can use the `light-dark()` colour function to set a colour value according to the [_used colour scheme_](https://drafts.csswg.org/css-color-adjust-1/#used-color-scheme). However, there is no way to set an entire rule of styles depending on the _used colour scheme_.

The author can use `<meta name="color-scheme">` and the `color-scheme` property to change the used colour scheme. As discussed in #10249, it is desirable for the author to be able to force the colour scheme to `only dark` or `only light` on certain pages of their website or certain portions of their page.

<details>
<summary>Examples of why an author would want to do that</summary>

* A page that play video content may use a dark colour scheme to make the video stand out better
* Pages for young children may use a light colour scheme, because user research shows young children have a worse experience on a dark colour scheme
* A portion of a page uses
* A WYSIWYG editor for content that appears in a dark colour scheme would need to present that content in the editor in a dark colour scheme as well
</details>

@tabatkins made the argument in #10249 that the fact that `@media (prefers-color-scheme)` doesn't use the _used colour scheme_ may be confusing to authors, or may even make the media query pointless for them.

So authors need a way to set styles for an element according to the _used colour scheme_.

## Possible solutions

In #10249, there were two ideas for how it could be done.

### 1. A new type of container query

Probably would be called `color-scheme()`.

```css
.header {
  background-image: url("light-bg.avif");

  @container color-scheme(dark) {
    background-image: url("dark-bg.avif");
  }
}
```

We could just use a style query for the `color-scheme` property, but that won't get the true _used colour scheme_ (i.e. it won't know if it has been set by `<meta name="color-scheme">`.)

### 2. A new pseudo-class

Probably would be called `:color-scheme()`.

```css
.header {
  background-image: url("light-bg.avif");

  &:color-scheme(dark) {
    background-image: url("dark-bg.avif");
  }
}
```

<!-- comment id=3236778706 author=JoshTumath created=2025-08-29T11:53:39Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3236778706 -->
## @JoshTumath — 2025-08-29

I don't have a strong opinion on which option I think is best, but I like the pseudo-class option because if its simplicity.

Because there was already strong support for a style query on the call a year ago https://github.com/w3c/csswg-drafts/issues/10249#issuecomment-2302515655, I will add this issue to the agenda.

<!-- comment id=3236905751 author=johannesodland created=2025-08-29T12:38:13Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3236905751 -->
## @johannesodland — 2025-08-29

Just repeating https://github.com/w3c/csswg-drafts/issues/10249#issuecomment-2306329038 here that there's a use case to provide matching image and video sources. 

To be able to match the used color-scheme on the page we would probably need a media feature for `used-color-scheme`. Pseudo-classes and container-queries can not solve this use-case.  

<!-- comment id=3236909533 author=lukewarlow created=2025-08-29T12:39:40Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3236909533 -->
## @lukewarlow — 2025-08-29

The pseudo class would trivially create cycles I think?

From @bramus on Bsky:

```css
:color-scheme(dark) {
  color-scheme: light;
}
```

For that reason some sort of container query feels better to me.

Alternatively/additionally if we could make light-dark() work for non colours that would solve basically all my use cases.

> To be able to match the used color-scheme on the page we would probably need a media feature for used-color-scheme. Pseudo-classes and container-queries can not solve this use-case.

There's previously been discussions about replacing the media attribute with some sort of "condition" one, if that allowed style queries I think a new style query would also solve that case?

<!-- comment id=3237005818 author=mayank99 created=2025-08-29T13:14:42Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237005818 -->
## @mayank99 — 2025-08-29

Would a container query prevent using this on the `<html>` element? It is very common to declare `background-color` and color-related custom properties on `<html>`.

<!-- comment id=3237110213 author=JoshTumath created=2025-08-29T13:49:28Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237110213 -->
## @JoshTumath — 2025-08-29

> The pseudo class would trivially create cycles I think?
> 
> From [@bramus](https://github.com/bramus) on Bsky:
> 
> :color-scheme(dark) {
>   color-scheme: light;
> }

Ah yes of course. Then that rules out a pseudo-class.

> Alternatively/additionally if we could make light-dark() work for non colours that would solve basically all my use cases.

That could be interesting! But I guess the `if()` function would achieve the same thing, if we resolve to adopt the `color-scheme()` query.

> > To be able to match the used color-scheme on the page we would probably need a media feature for used-color-scheme. Pseudo-classes and container-queries can not solve this use-case.
> 
> There's previously been discussions about replacing the media attribute with some sort of "condition" one, if that allowed style queries I think a new style query would also solve that case?

Do you have a link to where that's been discussed, @lukewarlow?

@johannesodland I don't think the solution for the `<picture>` source problem is that simple, because the HTML parser tries to fetch the image before CSS is parsed. So it wouldn't be possible to work out the _used colour scheme_ before the browser selects the picture source to fetch. However, if you're using `loading=lazy`, then - just like how we can use `sizes="auto"` on the `img` element - maybe the used colour scheme could be determined in a similar way?

I recommend you make a new issue for it on the [whatwg/html](https://github.com/whatwg/html/) repo.

> Would a container query prevent using this on the `<html>` element? It is very common to declare `background-color` and color-related custom properties on `<html>`.

Yes. But would you need to use it there, or would the media query be enough? Or could you apply the styles on the `body` element instead?

<!-- comment id=3237154817 author=lukewarlow created=2025-08-29T14:04:46Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237154817 -->
## @lukewarlow — 2025-08-29

> > There's previously been discussions about replacing the media attribute with some sort of "condition" one, if that allowed style queries I think a new style query would also solve that case?

> Do you have a link to where that's been discussed, @lukewarlow?

https://github.com/whatwg/html/issues/7540 - some of the later discussion here seemed to be going towards a condition attribute.

<!-- comment id=3237223426 author=o-t-w created=2025-08-29T14:26:11Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237223426 -->
## @o-t-w — 2025-08-29

I think it was resolved to allow `if` to get the used color scheme `if(color-scheme(dark): url(foo-dark.svg), else: url(foo-light-svg))`, but the proposal in this issue is still useful for setting a whole block of multiple properties rather than just one property value at a time. 
Related issue: https://github.com/w3c/csswg-drafts/issues/10577

<!-- comment id=3237285362 author=romainmenke created=2025-08-29T14:46:40Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237285362 -->
## @romainmenke — 2025-08-29

I think this is mostly a duplicate of https://github.com/w3c/csswg-drafts/issues/10577
The solution should cover all use cases described in both this and that issue.

So maybe add your use cases to https://github.com/w3c/csswg-drafts/issues/10577?

I also see some overlap with: https://github.com/w3c/csswg-drafts/issues/10509
That proposal would allow using the same conditions that exist for `if()`, in the same way, but as an at rule.

<!-- comment id=3237385270 author=JoshTumath created=2025-08-29T15:14:17Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237385270 -->
## @JoshTumath — 2025-08-29

> I think this is mostly a duplicate of [#10577](https://github.com/w3c/csswg-drafts/issues/10577) The solution should cover all use cases described in both this and that issue.
> 
> So maybe add your use cases to [#10577](https://github.com/w3c/csswg-drafts/issues/10577)?

Yes, possibly, depending on whether it's agreed that `if()` conditions should be in sync with `@container` conditions. If we agree they should be in sync upon resolution of that #10577, then this issue is implicitly resolved as well. Until that decision is made, I don't think they are duplicates.

<!-- comment id=3237708214 author=tabatkins created=2025-08-29T17:16:36Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3237708214 -->
## @tabatkins — 2025-08-29

Just noting here that this issue does not subsume #10249 (about making the MQ reflect the used color scheme), but is a useful addition on its own, for testing the used color scheme on a *subtree*.

I agree that a CQ for this is appropriate, testing a style queries container, called `color-scheme()`. This matches the `if()` proposal in <https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-3104198955>. It would have the standard difference with `if()`, in that as a CQ it can only test ancestors of an element, while `if()` tests on the element itself.

<!-- comment id=3239167681 author=LeaVerou created=2025-08-30T10:12:29Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3239167681 -->
## @LeaVerou — 2025-08-30

> I agree that a CQ for this is appropriate, testing a style queries container, called `color-scheme()`. This matches the `if()` proposal in [#10577 (comment)](https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-3104198955). It would have the standard difference with `if()`, in that as a CQ it can only test ancestors of an element, while `if()` tests on the element itself.

Do we actually need a new function? The `style()` syntax was always defined to take regular properties, and custom properties were the start. While it will be very hard to implement `style()` for any property, perhaps we don't have to do it all at once. It seems just as feasible to implement `style(color-scheme: dark)` as this proposed `color-scheme()` and doesn't add more API surface.

<!-- comment id=3239253033 author=romainmenke created=2025-08-30T13:06:31Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3239253033 -->
## @romainmenke — 2025-08-30

You'd want to query the used color scheme, not the value of the `color-scheme` property.

<!-- comment id=3239414661 author=JoshTumath created=2025-08-30T17:23:36Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3239414661 -->
## @JoshTumath — 2025-08-30

Yeah, so if, for example, the author set:

```css
:root {
  color-scheme: light dark;
}
```

and the user has enabled dark mode, `@container style(color-scheme: dark)` wouldn't match.

<!-- comment id=3329917887 author=lukewarlow created=2025-09-24T17:12:52Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3329917887 -->
## @lukewarlow — 2025-09-24

I think per https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-3329616811 this issue can be closed right?

<!-- comment id=3330208065 author=svgeesus created=2025-09-24T18:41:49Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3330208065 -->
## @svgeesus — 2025-09-24

I think so. @JoshTumath ?

<!-- comment id=3330903017 author=JoshTumath created=2025-09-24T22:24:44Z url=https://github.com/w3c/csswg-drafts/issues/12681#issuecomment-3330903017 -->
## @JoshTumath — 2025-09-24

> I think per [#10577 (comment)](https://github.com/w3c/csswg-drafts/issues/10577#issuecomment-3329616811) this issue can be closed right?

Yes that's correct! Woohoo!
