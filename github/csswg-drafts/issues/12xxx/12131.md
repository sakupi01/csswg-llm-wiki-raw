---
number: 12131
title: "[css-anchor-position] Simplifying math functions for `<percentage>` in `anchor()`?"
state: "open"
labels: ["Agenda+", "Commenter Response Pending", "css-anchor-position-1"]
author: "dshin-moz"
created_at: "2025-04-28T15:31:22Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12131"
comments: 5
has_resolution: false
synced_at: "2026-09-10T04:39:57Z"
---

`anchor()` allows specifying percentage values, which [interpolates between `start` and `end`](https://drafts.csswg.org/css-anchor-position-1/#valdef-anchor-percentage).

This seems like something that can be simplified, since I think the basis of that will always be positive, since it depends on the anchor element's size in that axis. OTOH, WPT test `anchor-parse-valid.html` expect the `min(50%, 100%)` value to round trip.

<!-- comment id=3684462176 author=tabatkins created=2025-12-22T22:55:58Z url=https://github.com/w3c/csswg-drafts/issues/12131#issuecomment-3684462176 -->
## @tabatkins — 2025-12-22

Hm, I see where you're getting that: [per spec (step 5)](https://drafts.csswg.org/css-values/#simplify-a-calculation-tree) we should simplify min()/max() if the arguments are numeric and we have enough information to compare them. Exactly what that means is a bit ill-defined; do we *generally* care about %s against known-non-negative basises?

<!-- comment id=3725037702 author=fantasai created=2026-01-08T18:03:49Z url=https://github.com/w3c/csswg-drafts/issues/12131#issuecomment-3725037702 -->
## @fantasai — 2026-01-08

When we resolve this, we'll need to follow up on https://github.com/web-platform-tests/wpt/pull/56876

<!-- comment id=4600367547 author=emilio created=2026-06-02T08:36:25Z url=https://github.com/w3c/csswg-drafts/issues/12131#issuecomment-4600367547 -->
## @emilio — 2026-06-02

Test-case that doesn't involve `anchor()`:

```js
let div = document.body.appendChild(document.createElement("div"));
div.style.display = "none"; // Get the computed, not resolved, value
div.style.width = "min(50%, 100%)"
console.log(getComputedStyle(div).width)
```

Blink simplifies here, but WebKit and Gecko don't.

<!-- comment id=4614777740 author=tabatkins created=2026-06-03T16:54:02Z url=https://github.com/w3c/csswg-drafts/issues/12131#issuecomment-4614777740 -->
## @tabatkins — 2026-06-03

Yeah , re-reading this, anchor() actually isn't an example of the question here - the % in anchor() *doesn't* resolve against any basis, it's left as-is, so it can be resolved immediately regardless.

But the 'width' example does illustrate it; contrast with the identical value used in 'background-position' when the background-size can be smaller or larger than the background positioning area.

<!-- comment id=5604794412 author=tabatkins created=2026-09-09T15:56:04Z url=https://github.com/w3c/csswg-drafts/issues/12131#issuecomment-5604794412 -->
## @tabatkins — 2026-09-09

Just to make things clear here, since it's been a while since this was discussed: back in 2020 [we resolved that min()/max() will simplify values that we know the relative sizes of](https://github.com/w3c/csswg-drafts/issues/4550#issuecomment-594970559) - that is, `min(10px, 20px)` will just immediately resolve to `10px`.

This issue is specifically about whether or not we do so for percentages, *when we know via context* that the % is resolved against a non-negative number. (In general, such as in background-size, you can't tell whether `10%`	is bigger or smaller than `20%`, as whether the basis is positive or negative inverts the answer.)
