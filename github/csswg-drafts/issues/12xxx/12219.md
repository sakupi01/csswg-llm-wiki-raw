---
number: 12219
title: "[css-values-5] Behavior of ident() in at-rule preludes"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-values-5"]
author: "andruud"
created_at: "2025-05-21T08:45:58Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12219"
comments: 13
has_resolution: false
synced_at: "2026-07-04T04:45:32Z"
---

The spec says:

> The [ident()](https://drafts.csswg.org/css-values-5/#funcdef-ident) function can be used as a value for any property or function argument that accepts a [`<custom-ident>`](https://drafts.csswg.org/css-values-4/#identifier-value).

Does this mean that it's _not_ valid in other contexts? For example: `@position-try ident("--p" 1) {}`. If it _is_ valid, what happens with e.g. `@position-try ident("--p" sign(16px - 1em)) {}`? Or `@position-try ident("--p" sibling-index()) {}`?

`@container ident("--p" sign(16px - 1em)) {}`, if valid, would need to resolve `em` (etc) against something that's _not_ the container's font-size (unlike `em` in the query itself), since the ident is the thing that determines _what_ to query.

<!-- comment id=2898729978 author=fantasai created=2025-05-21T17:33:49Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-2898729978 -->
## @fantasai — 2025-05-21

Seems reasonable to make this valid anywhere that accepts `<custom-ident>`

<!-- comment id=2900473233 author=andruud created=2025-05-22T09:10:47Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-2900473233 -->
## @andruud — 2025-05-22

In media queries, there is no element context, but we still allow `em` units that resolve against the initial `font-size`. In general, anything you could put into an `ident()` (relative units, `sign()`, `sibling-index()`, etc), you could also put into a media query, and it must have (or should have) some behavior already.

So how about (proposed resoluition): `ident()`is allowed _anywhere_ `<custom-ident>` is accepted. When used outside of an element context, the numeric parts behave as they would in a media query.

<!-- comment id=2901040151 author=andruud created=2025-05-22T12:24:46Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-2901040151 -->
## @andruud — 2025-05-22

Ah, no. It's too much pain for zero (?) benefit. We would then need to parse _~all_ rules (also style rules, since [some selectors](https://drafts.csswg.org/css-view-transitions-1/#named-view-transition-pseudo) can hold custom idents) into some not-fully-resolved form that needs to be (re)computed into a final rule when media values change. I don't see that actually happening.

Then I think we should just make `ident()` invalid without an element context. (Which may be what the spec is saying already.)

<!-- comment id=2902816112 author=bramus created=2025-05-22T23:03:33Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-2902816112 -->
## @bramus — 2025-05-22

The problem is not with `ident()` but with things like `sibling-index()` outside of element context, no? If they cannot produce a value, then I would assume everything that relies on it – e.g. the `ident()` in `@position-try ident("--p" sibling-index())` – to also fail.

<!-- comment id=2902914637 author=tabatkins created=2025-05-23T00:08:54Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-2902914637 -->
## @tabatkins — 2025-05-23

There's several examples beyond `sibling-index()` - anything that relies on an element to calculate something, like the example `ident("--p" sign(16px - 1em))` given in the initial post, which relies on an element context to determine the size of an `em`, so it knows to produce `--p0`, `--p1`, or `--p-1`.

<!-- comment id=3293446258 author=LeaVerou created=2025-09-15T18:36:31Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3293446258 -->
## @LeaVerou — 2025-09-15

What about `@apply`? `@apply ident(var(--foo))` would be *incredibly* useful. See also #10006.

<!-- comment id=3297363555 author=andruud created=2025-09-16T10:06:23Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3297363555 -->
## @andruud — 2025-09-16

You mean `env(--foo)`, right? (I.e. `--foo` references a mixin parameter or local.)

I think we're aiming to make that work, but you don't need `ident()` for that. `@apply env(--foo)` will do.

<!-- comment id=3297790580 author=LeaVerou created=2025-09-16T10:59:58Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3297790580 -->
## @LeaVerou — 2025-09-16

> You mean `env(--foo)`, right? (I.e. `--foo` references a mixin parameter or local.)
> 
> I think we're aiming to make that work, but you don't need `ident()` for that. `@apply env(--foo)` will do.

Ah yes, `ident()` wouldn't be needed if you're not combining strings or idents. I was referring to a local variable that contains the name of the mixin. Question still stands, but it's not relevant to this issue.

<!-- comment id=3807555275 author=LeaVerou created=2026-01-27T21:11:44Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3807555275 -->
## @LeaVerou — 2026-01-27

In the interest of making progress, can we resolve that `ident()` is invalid anywhere without an element context for now? We can always expand it later.

<!-- comment id=3821023951 author=tabatkins created=2026-01-29T23:58:40Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3821023951 -->
## @tabatkins — 2026-01-29

Agree that requiring an element context is fine. There is *literally no use* for it unless, like, you're combining an env() with something. Pretty niche for now.

<!-- comment id=3821046382 author=LeaVerou created=2026-01-30T00:07:18Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3821046382 -->
## @LeaVerou — 2026-01-30

@astearns since we didn't get to this in the meeting, I wonder if we could do an async resolution. I don't expect it to be particularly controversial.

<!-- comment id=3874079303 author=astearns created=2026-02-09T21:56:32Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3874079303 -->
## @astearns — 2026-02-09

The CSSWG will automatically accept this resolution one week from now if no objections are raised here. Anyone can add an emoji to this comment to express support. If you do not support this resolution, please add a new comment.

Proposed Resolution: `ident()` is only valid within an element context

<!-- comment id=3910348143 author=astearns created=2026-02-16T20:09:59Z url=https://github.com/w3c/csswg-drafts/issues/12219#issuecomment-3910348143 -->
## @astearns — 2026-02-16

RESOLVED: `ident()` is only valid within an element context
