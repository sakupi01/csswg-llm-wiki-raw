---
number: 12903
title: "Should common @when and if() functions be the same?"
state: "open"
labels: ["css-conditional-5", "css-values-5"]
author: "dbaron"
created_at: "2025-10-06T18:16:35Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12903"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:37Z"
---

The [`@when` rule](https://drafts.csswg.org/css-conditional-5/#at-ruledef-when) defines `media()` and `supports()` functions.  The [`if()` function](https://drafts.csswg.org/css-values-5/#if-notation) defines `supports()`, `media()` and `style()` functions.

I understand (I think) that `style()` is only present for `if()` because if is in a value context, so style queries are possible, whereas `@when` is in a rule context.  (But see [container queries](https://drafts.csswg.org/css-conditional-5/#container-queries)!  Though I think those impose additional constraints which may be a good reason to keep them out of `@when`.)

However, I'm curious why there are differences in the definitions of the `supports()` and `media()` functions between these contexts.  I'd initially expect them to be the same, although maybe there are good reasons for them to be different.  (If so, it would be good to document them.)  Should these functions basically be the same?  Should they even have a common definition (which other types of conditions may add to in the future)?

(It's not clear to me that any of the current differences are substantive... but they're definitely defined in different ways right now.)

<!-- comment id=3373524854 author=Loirooriol created=2025-10-06T19:21:48Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3373524854 -->
## @Loirooriol — 2025-10-06

For a more concrete question, `@when media(width > 10em)` treats `em` as the initial value since it has no context.
Should `if(media(width > 10em): /* ... */)` do the same for consistency, or should it use the context to resolve `em`?

<!-- comment id=3375022186 author=cdoublev created=2025-10-07T03:22:50Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3375022186 -->
## @cdoublev — 2025-10-07

[`@import`](https://drafts.csswg.org/css-cascade-5/#typedef-import-conditions) also has its own `supports()`.

|   Context | `supports()` argument
| --------- | ------------
| `@import` | `<declaration> \| <supports-condition>`
|   `@when` | `<declaration>`
|   `if()`  | `<ident> : <declaration-value> \| <supports-condition>`

|   Context | `media()` argument
| --------- | ---------
|   `@when` | `<mf-plain> \| <mf-boolean> \| <mf-range>`
|   `if()`  | `<media-feature> \| <media-condition>`

An argument "in parens" accepts almost any value, thanks to `<general-enclosed>`. But `<declaration>`, `<media-feature>`, `<mf-*>`, do not. However, `<declaration>` must be parsed as if it was specified in parens in `@import`, so `@import 'foo.css' supports(garbage);` is still valid. (edit) `<declaration>` presumably represents a valid declaration so it's invalid, but I think the intent of this requirement was to parse any unwrapped value as it if it was wrapped in () block, so that it matches `<general-enclosed>` and does not make `@import` invalid, which does not make a difference though, since its resource will not be fetched in either case.

I thought these differences were editorial.

<!-- comment id=3376080738 author=AtkinsSJ created=2025-10-07T09:57:10Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3376080738 -->
## @AtkinsSJ — 2025-10-07

Unifying these somehow would be nice! It'd simplify implementation, but also I think it's less confusing to authors if the same condition is valid wherever they put it.

> For a more concrete question, `@when media(width > 10em)` treats `em` as the initial value since it has no context.
> Should `if(media(width > 10em): /* ... */)` do the same for consistency, or should it use the context to resolve `em`?

My instinct would be no, it should be whatever `em` is in that context. That seems more useful I think? But it'd be worth seeing what's more intuitive to authors.

<!-- comment id=3376521665 author=bigandy created=2025-10-07T11:45:00Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3376521665 -->
## @bigandy — 2025-10-07

if `@when` includes style queries in the same way that if() does then that would be very useful e.g. within `@mixin` context. 

<!-- comment id=3384028210 author=otomad created=2025-10-09T04:01:04Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3384028210 -->
## @otomad — 2025-10-09

Actually I hope rename `@when` to `@if`.

<!-- comment id=3385286762 author=SebastianZ created=2025-10-09T10:48:03Z url=https://github.com/w3c/csswg-drafts/issues/12903#issuecomment-3385286762 -->
## @SebastianZ — 2025-10-09

> Actually I hope rename `@when` to `@if`.

That's a different discussion, see #6684.

Sebastian
