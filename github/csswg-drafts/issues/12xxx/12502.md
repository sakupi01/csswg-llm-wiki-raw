---
number: 12502
title: "[selectors] Declarative custom `:state()` states"
state: "open"
labels: ["Agenda+ F2F", "selectors-5"]
author: "LeaVerou"
created_at: "2025-07-18T20:51:54Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12502"
comments: 22
has_resolution: false
synced_at: "2026-07-23T00:54:54Z"
---

The topic of allowing authors custom selectors has come up periodically, but we have no concrete proposal yet.

For custom elements, JS can define arbitrary states that can be matched through the [`:state()` pseudo-class](https://html.spec.whatwg.org/multipage/semantics-other.html#selector-custom). I just [proposed](https://github.com/whatwg/html/issues/11466) expanding `:state()` to regular elements as well (through a similar JS API).

However, being able to assign states declaratively through CSS is strictly more powerful and feeds multiple birds with one scone.

## Custom element use cases

### Simplification of custom state management code

Currently, custom states must be applied and removed imperatively, with all the DX issues that this implies.
However, many (possibly even most) custom states are simply an abstraction over existing light and shadow DOM selectors. 

This effectively forces authors to re-implement the UA's selector invalidation, and (especially in the absence of primitives to monitor selector invalidation) invites errors.

<details><summary>Example</summary>

```js
class MyCheckbox extends HTMLElement {
	#checkbox;
	constructor() {
		super();

		this.attachShadow({mode: "open"});
		this.#checkbox = Object.assign(document.createElement("input"), {type: "checkbox", id: "checkbox"});
		this.shadowRoot.append(this.#checkbox);
		
		this.addEventListener("input", ({target}) => {
			if (target === this.#checkbox) this.#toggleState("checked", this.#checkbox.checked);
		});
		
	}
	connectedCallback () {
		this.#toggleState("checked", this.#checkbox.checked);
		this.#toggleState("disabled", this.#checkbox.disabled);
	}

	attributeChangedCallback (name, value, oldValue) {
		this.#checkbox.setAttribute(name, value);

		// Oops, did not account for being inside a disabled form/fieldset!
		if (name === "disabled") this.#toggleState("disabled", this.#checkbox.disabled);
		else if (name === "checked") this.#toggleState("disabled", this.#checkbox.checked);
	}

	#toggleState (name, force = this._internals.states.has(name)) {
		if (force) this._internals.states.add(name);
		else this._internals.states.delete(name);
	}

	get checked () { return this.#checkbox.checked }
	set checked (v) { 
		this.#checkbox.checked = v; 
		this.#toggleState("checked", this.#checkbox.checked); 
	}

	get disabled () { return this.#checkbox.disabled }
	set disabled (v) {
		this.#checkbox.checked = v; 
		this.#toggleState("disabled", this.#checkbox.disabled); 
	}
}
```

Most of this code is mapping `:checked` and `:disabled` on the shadow DOM element to `:state(checked)` and `:state(disabled)` on the host, in a buggier way than if author intent could be expressed clearly, e.g. through something like:

```css
@state checked {
	matches: :host:has(#checkbox:checked)
}

@state disabled {
	matches: :host:has(#checkbox:disabled)
}
```

Then the JS collapses down to:


```js
class MyCheckbox extends HTMLElement {
	#checkbox;
	constructor() {
		super();

		this.attachShadow({mode: "open"});
		this.#checkbox = Object.assign(document.createElement("input"), {type: "checkbox", id: "checkbox"});
		this.shadowRoot.append(this.#checkbox);
	}

	attributeChangedCallback (name, value, oldValue) {
		this.#checkbox.setAttribute(name, value);
	}

	get checked () { return this.#checkbox.checked }
	set checked (v) { 
		this.#checkbox.checked = v; 
	}

	get disabled () { return this.#checkbox.disabled }
	set disabled (v) {
		this.#checkbox.checked = v; 
	}
}
```

</details> 

### Export states and other pseudo-classes from descendants

Currently, there is no primitive for exporting states from a nested component to the outside world (akin to exporting parts). This would enable that, often with remapping (just like `exportparts`!).

### Custom state pre-rendering

Currently, to prerender (SSR) custom elements, states must be be converted to attributes, then converted back when hydrating. CSS must be written to cater to both, like so:

```css
:host(:state(foo), [ssr-state-foo]) {
	/* ... */
}
```

If consumers need to handle the state, this also becomes a leaky abstraction:

```css
my-checkbox:is(:state(checked), [ssr-state-checked]) {
	/* ... */
}
```

Declarative custom states would improve the situation tenfold: 
1. For states that can be entirely described by CSS selectors, *all* JS state management code condenses down to 2-3 lines for the mapping and works automatically when prerendering.
2. For states that still need to be serialized into an attribute, this helps contain the mapping of attribute → state in one place while everything else (component CSS, consumer CSS, etc) continues to handle the state, not only improving maintainability but also preventing the abstraction from leaking.

### It enables custom elements to add state to slotted children

Authors are often forced to create a whole new element simply to track certain types of states on slotted children or deeper descendants.

For example:
- An accordion component may want to take `<details>` as children and set a `:state(active)` and `:state(last-active)` states
- A tabs component may want to accept any container as the tab-panel but be able to set a `:state(active)`
- A [`<random-content>`](https://webawesome.com/docs/components/random-content/) component may want to set a `:state(selected)` pseudo-class to the child currently being shown
- A `<filterable-table>` component that takes a `<table>` may want to set `:state(shown)` and `:state(filtered)` to corresponding rows while filtering.
- and so on

## Non-WC use cases

Reading the above, it sounds like this should be a WC-specific feature, however there are a ton of use cases around abstracting DOM querying that have nothing to do with custom elements.

### It provides a very decent workaround for custom pseudo-classes

If you ignore the name `state` for a second, this basically allows custom pseudo-classes with practically no downsides compared to a first-class solution designed specifically for this use case.

<details><summary>Example</summary>

E.g. suppose we didn't have `:heading`, it could be implemented like:

```css
/* Now :state(heading) matches every heading */
@state heading {
	matches: h1, h2, h3, h4, h5, h6;
}
```

This could also be used as a ponyfill for `:heading`:

```css
@state heading {
	matches: :heading;
}

@supports selector(:heading) {
	@state heading {
		matches: h1, h2, h3, h4, h5, h6;
	}
}
```

Or, maybe a project wants to include `[role=heading]` to what they target. They can build a higher level abstraction _over_ `:heading`:

```css
/* Now :state(heading) matches every heading */
@state heading {
	matches: :heading, [role=heading];
}
```

</details>

Similarly, it allows component consumers to alias component states into project-specific conventions so that swapping one component for another has less of a ripple effect on the codebase.

### It allows decoupled composability

This keeps cropping up from time to time, e.g. with a quick search:
- https://github.com/w3c/csswg-drafts/issues/1855
- https://github.com/w3c/csswg-drafts/issues/10222

This would allow host environments to use `:state()` as an extension hook that third-party code (CSS libraries, frameworks, etc) can extend. E.g.:

```css
button, :state(button) {
	/* Page’s button styles */
}
```

Now a web component could opt individual buttons into the default styling by adding the corresponding part to the set of mappings:

```css
@state button {
	matches: button[part=button]
}
```

The host page doesn't need to know what elements are hooking into each state. The shadow DOM elements hooking into the state don't need to know what styling is applied, or what other elements are using it. Everything is nicely decoupled and changing the part name breaks the mapping, if that's desired.

This can go both ways. A CSS library could also provide states that the host environment then extends to apply to their own situations.
For example, the same type of styling is often used for navigation menus, as well as horizontal tab strips.

<img width="778" height="160" alt="Image" src="https://github.com/user-attachments/assets/a266b879-eca6-4e8c-ae47-7127b2fb3c10" />

A CSS library could define styles like:

```css
:state(tabstrip) {
	display: flex;
	/* ... */
}

:state(tabstrip) > *, :state(tabstrip-item) {
	flex: 1;
	/* ... */
}
```

Then, a page can apply this to its navigation menu, a tabstrip component can apply it as well etc.
Yes, mixins will also solve this, but the ergonomics are different (better or worse, depending on the use case)

The benefit of this over other approaches is the **loose coupling** it enables.
The mapping is completely separate from either side, and neither side needs to know about the other.

## Design discussion

### Multiple `.states`/`@state` rules must compose

This is a semi-hard requirement, otherwise the feature will cater to _significantly_ fewer cases.
Each `@state` rule must **extend** the set of selectors mapped to that state, not replace it. There is no primitive for declaratively *removing* a state, that should be encoded in the mapped selector (e.g. via `:not()` or scoping criteria appropriately).

Instead of `@state` rules with the same id overwriting each other, they could be composed, providing another way to satisfy some of the #10222 use cases. Yes, it would require a double handshake, but perhaps authors can reach a convention for certain states, especially if there is only one mechanism for both CSS and WCs.

As a side effect, this means that a state added via CSS cannot be removed via JS until the selector stops matching (or the rule is removed via the CSSOM). I think this is fine and how other platform primitives work where state can be set via either CSS or DOM APIs.

### Cycle detection

Since not all states are set via CSS, states must be allowed as part of the mapping. But then, how to handle cycles?

```css
@state a {
	matches: :state(b), c, d;
}

@state b {
	matches: :state(a), c, e;
}
```

What happens here?

Observations:
- The code above *is* actually meaningful, and most likely meant to match `c, d, e`.
- Ideally any cycle detection/resolution strategy should be applied separately for each complex selector in the selector list instead of dropping the entire state.

Options, in order of preference:
1. Cycle detection + bounded expansion gets us as close as possible to the user intent. Meaning, in the example above `@state a` is expanded to `matches: :state(a), c, e, c, d`, and then `:state(a)` is removed from the set.
2. Cycle detection where the entire cycle is removed from matching. in the example above, `:state(a)` still matches `c, d` and `:state(b)` still matches `c, e`. 
3. Only allow JS-originating `:state()` in `matches`. Poor long-term solution, but could be an acceptable MVP.
4. Simply disallow `:state()` in `matches` altogether. Even poorer long-term solution, but could be an acceptable MVP.

### Shadow DOM boundaries

For many of the examples above, shadow DOM CSS must be able to expose states on the host and parts that are accessible from the outside. But we cannot simply define `@state` to operate in light DOM, since it can be useful for managing shadow DOM states, though that's not a very pressing need.

Additionally, while `:host` is unambiguous, `::part()` is not. The way `::part()` was used in the examples above, it was referring to parts *within* the shadow tree, but `::part()` in shadow CSS would refer to parts of elements *inside* that shadow tree.

Perhaps the algorithm is that states are associated with the element, and anything that has access to the element, with whatever means, has access to its states. So each pair of `:host`/`foo-bar`, `[part=foo]`/`::part(foo)`, `slot[name=foo]::slotted(*)`/`[slot=foo]`  would have access to the same states.

This is probably simplest, but ideally down the line should have an escape hatch for encapsulation. Possibly a descriptor in `@state`.

### What about pseudo-elements?

Simplest for the MVP is to just ignore pseudo-elements altogether, keeping on with the model of `:is()`.

But there are many legit use cases around pseudo-elements. Obviously, `:state()` being a pseudo-class, it acts as a filter and cannot *add* elements to the matching, but it's somewhat less defensible when the base selector already applies more broadly, e.g.:

```css
@state ba {
	matches: ::before, ::after;
}

/* Does this add "test" at the start and end of every div? */
div:state(ba) {
	content: "test";
}

/* What about this? */
div::before:state(ba) {
	content: "before"
}
```

I think for both of these  it would be _very_ weird if they worked.

That said, I think there are use cases for a declarative version of `::part()` as well, which would also allow authors to export pseudo-elements like `::backdrop` or `::checkmark`, a current pain point with web components encapsulating complex native elements.

## Specificity

In L1, **specificity** would always be `0, 1, 0` or whatever the equivalent `:is()` would have been. 
In L2, a `specificity` descriptor could be used to override. 

## Prior art

- Looks like @romainmenke also thought these should be the same: #8094
- #4805 seems relevant too

## Other related issues

- https://github.com/w3c/csswg-drafts/issues/13579

<!-- comment id=3090770151 author=bkardell created=2025-07-18T21:32:55Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3090770151 -->
## @bkardell — 2025-07-18

Previously these two conversations seemed to get mixed up - about custom state and custom selectors (@tabatkins had a draft started at the end of 2013 - you can still see it at https://web.archive.org/web/20140320013932/https://tabatkins.github.io/specs/css-aliases/#custom-selectors)... I'm wondering if it is getting mixed up again, or if it is intentional...  Is there a way that you understand headings (in your example) as state (like hover or focus)?  I suppose if you squint at it hard enough you could argue the used role is a state, but you can't tell that from selectors really, right?  Is it just leaning on old comments that said they were very similar?

<!-- comment id=3091274515 author=LeaVerou created=2025-07-18T23:54:02Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091274515 -->
## @LeaVerou — 2025-07-18

> Previously these two conversations seemed to get mixed up - about custom state and custom selectors ([@tabatkins](https://github.com/tabatkins) had a draft started at the end of 2013 - you can still see it at [web.archive.org/web/20140320013932/https://tabatkins.github.io/specs/css-aliases#custom-selectors](https://web.archive.org/web/20140320013932/https://tabatkins.github.io/specs/css-aliases/#custom-selectors))... I'm wondering if it is getting mixed up again, or if it is intentional... Is there a way that you understand headings (in your example) as state (like hover or focus)? I suppose if you squint at it hard enough you could argue the used role is a state, but you can't tell that from selectors really, right? Is it just leaning on old comments that said they were very similar?

Syntactically there is no distinction between state and identity, including in native CSS pseudo-classes (e.g. see #10296). You're understandably thrown a bit by the word "state" which I agree could be more general, but `:state(heading)` works fine by itself, it doesn't _need_ something before the `:` (sorry if I'm stating the obvious, but @westbrook said he hadn't realized before).

<!-- comment id=3091316126 author=knowler created=2025-07-19T00:42:07Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091316126 -->
## @knowler — 2025-07-19

Should setting state be a concern of CSS? I am very interested in a declarative way of doing this for custom elements, but [I had imagined that being a part of Declarative Custom Elements and not CSS](https://knowler.dev/blog/declarative-custom-elements-should-take-inspiration-from-css), since state is usually related to semantics (e.g. I would reflect `this.#internals.ariaExpanded = true` as `:state(expanded)`).

I’m definitely a fan of having custom selectors in some way though. Maybe something in the direction of `:alias()`/`@alias` could work?

Initially, I wondered if CSS Shadow Parts might be more appropriate to expand to the light DOM and make declarative (via CSS), but I imagine there would be issues complex selectors as `::part()` is more limited—a pseudo-class seems more appropriate.

<!-- comment id=3091438399 author=seanmay created=2025-07-19T02:50:16Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091438399 -->
## @seanmay — 2025-07-19

> Should setting state be a concern of CSS?

I don't believe that this is a matter of setting state, any more than the following are setting state:

```ts
// .ts
type Heading =
  | H1 | H2 | H3
  | H4 | H5 | H6
  | ({ role: "heading" } & {});

//  .js
const attributeMatches = (attr, value) => (el) =>
  el.getAttribute(attr) === value;

const isHeading = el => ([
  isH1, isH2, isH3,
  isH4, isH5, isH6,
  attributeMatches("role", "heading")
].some((test) => test(el))); 
```
Neither the `Heading` type, nor the `isHeading` function are setting state.

If I am reading the proposal correctly (and please correct me if I am wrong, @LeaVerou) the term "state" here is much more like a reflection of "state of being", rather than "I am changing the value of the text in the div, and deselecting the user's form input".

Perhaps the word is too overloaded to use. There are a lot of overloaded terms as you adopt more languages.
I would imagine a `:matches(any-heading)` would be idiomatic... except that `:matches` is the former name of `:is` and can be semantically (linguistically, not programmatically) confused with the same. But the hangup here is the English word, not the behaviour, to the best of my understanding.

<!-- comment id=3091450630 author=seanmay created=2025-07-19T03:06:36Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091450630 -->
## @seanmay — 2025-07-19

A comment directed to the proposal, itself:

Would there be any means of nesting queries (media / container / style) inside of these declarations, or would it make resolving the tree too unwieldy?

eg: 
```css
@state an-important-one {
  @media(20ch <= width < 80ch) {
   matches: .important, .notification, .warning;
  }
  @container(80ch <=  width) {
    matches: .emergency;
  }
}

:state(an-important-one) {
  color: var(--theme-fg-ohnoes);
  background-color: var(--theme-bg-ohnoes);
} 
```

*edits: fixing a typo and some syntax bugs that came from trying to type a sensible (if not 100% realistic) example, on a phone, with Gemini ruining code with each and every symbol typed, in the wee hours of the morning*

<!-- comment id=3091469730 author=knowler created=2025-07-19T03:37:50Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091469730 -->
## @knowler — 2025-07-19

@seanmay I use the _existing_ [CSS Custom State](https://developer.mozilla.org/en-US/docs/Web/CSS/:state) to create CSS APIs for custom elements that are akin to `:checked`, `:open`, `:popover-open`, `:focus`, `:hover`, `:active`, `:playing`, `:invalid`, `:required`, `:disabled` for built-in elements. In any of those cases, having something within CSS override them would be inappropriate and it would be for custom elements as well, which is why I’m suggesting that _setting the state_ might be outside of the purview of CSS. That’s not to say one can’t use CSS Custom State to create selectors for custom elements that’d be like the built-in `:any-link` or `:heading`, but to me it seems a little misplaced to do so using something for _state._

- - -

Imagine I have an existing custom element that has shadow styles for `:state(open)` which it sets when `ariaExpanded` has been set for its `ElementInternals`. As the custom element author, I’m deciding when that state gets added. If document authors can set states, then they could trigger those styles when they shouldn’t apply. I’m not sure if this is covered by the “Detailed discussion” of the proposal.

<details><summary>Code example of the above</summary>

```html
<!-- CE definition that’s controlling its state -->
<script>
customElements.define("my-element", class extends HTMLElement {
  #internals = this.attachInternals();

  get open() {
    return this.#internals.states.has("open");
  }

  set open(value) {
    if (value === true) {
      this.#internals.ariaExpanded = "true";
      this.#internals.states.add("open");
    } else if (value === false) {
      this.#internals.ariaExpanded = "false";
      this.#internals.states.delete("open");
    }
  }

  constructor() {
    super();
    // Ignore the fact that there’s no ARIA role… trying to keep this focused.

    // Default expanded state is false, so `open` does not apply
    this.#internals.ariaExpanded = "false";

    this.attachShadow({ mode: "open" });

    this.shadowRoot.innerHTML = `<slot></slot><style>
      :host(:state(open)) {
        /* Open styles */
      }
    </style>`;

    this.addEventListener("click", () => {
      this.open = !this.open;
    });
  }
});
</script>

<style>
/* CE consumer forces the state of a custom element (potentially breaking it beyond just styling) */
@state open {
  matches: my-element;
}
</style>

<my-element>Open styles apply when they shouldn’t</my-element>
```

</details>

<!-- comment id=3091515815 author=seanmay created=2025-07-19T04:19:36Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3091515815 -->
## @seanmay — 2025-07-19

@knowler 

Thank you for the example.

```html
<style>
/* CE consumer forces the state of a custom element (potentially breaking it beyond just styling) */
@state open {
  matches: my-element;
}
</style>
```

I understand more of where you are coming from, now, but on rereading, I do not think the comment in the referenced block would be true.

I think that it might be possible for the consumer of the element to show it in an open state, via CSS, if a `my-element::open` selector in a style tag is wholly sufficient to display your content, from userland.
However, I don't see where the proposal states that the CSS selectors, used in the CSS files (or style tags), would be reflected back into the HTML or JS.

The closest I can find to that reading is this:

> States added via @state combine with ElementInternals.states (and the proposed Element.states)
> The resulting states are a union. This means, if a component removes a state via JS but the same state is in effect through an @state rule, it still applies.

However, my interpretation of that is: "For the purpose of styling elements, within the styling rules, if the CSS-declared selectors still apply, despite the JS selectors being removed ..." rather than a reading of "For the purpose of DOM node state, and JS reflection of DOM node state, if the CSS ......."

Again, I could be wrong. I think we are both making assumptions in two different directions, relating to missing context, in this area of the proposal.

Note that I am not suggesting that this is your argument, at all, but for cases of "that selector, with a `display: block` on the host element, will break my component", it does mention that it would be tree-scoped, but could be extended later. And to that, I might say that having `doesMyComputedStyleContainDisplayBlock(root)` as a programmatic JS state, might be detrimental, in general. And also, if `my-element::open { display: block; }` is enough to mess with the component, just by virtue of the computed style (and not JS reflection, except through checking the computed style), then the end user could equally mess with the component with an `!important` or with any specificity greater than `(0,0,1)`.

<!-- comment id=3092444712 author=Loirooriol created=2025-07-19T16:38:15Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3092444712 -->
## @Loirooriol — 2025-07-19

Presumably this would need some restrictions to avoid cycles:

```css
@state heading {
	matches: :state(heading);
}
```

<!-- comment id=3094834080 author=LeaVerou created=2025-07-20T22:33:28Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3094834080 -->
## @LeaVerou — 2025-07-20

Overall, it looks like many people are thrown by the word "state" in `:state()`, since it's currently mainly used for transient states. The way I saw it, `:state()` is just the API we ended up on for doing custom pseudo-classes (instead of `:--foo`). It was called "state" because often pseudo-classes represent transient state, but they don't have to, `:state(button)` is a perfectly valid selector.

I was thinking that `:state()` would be paving the cowpaths, but if so many people are thrown by it, then it's clearly not a good option. Perhaps we need to revisit actual custom pseudo-classes with a similar syntax as what is proposed here. However, we do need WCs to have a composable way for hooking into them. We want authors to be able to create e.g.:

```css
@pseudo-class button {
	matches: button, .button;
}
```

And then we want a way for a WC to say "add me to whatever `:--button` matches. Perhaps something like [`@light { ... }`](https://github.com/w3c/csswg-drafts/issues/10941) could address that too, allowing the WC to just do:

```css
@pseudo-class button {
	matches: foo-button;
}
```

Alternatively, something like `:alias()` could work, with `:alias(foo)` automatically targeting `:state(foo)` (being an alias itself, which is delightfully meta 😅)

@seanmay:
> Would there be any means of nesting queries (media / container / style) inside of these declarations, or would it make resolving the tree too unwieldy?

Oooh that's a very interesting idea. I was thinking of it as a simpler at-rule that only takes descriptors and was thinking we could use a similar design for custom media queries (which is also a goal), but I quite like this as a way to feed two birds with one scone! This is brilliant actually, it also mostly solves #6247 and a bunch of other use cases. I’m not sure we have precedent of at-rules that can nest conditional rules but not regular rules, @tabatkins would know.

> [@seanmay](https://github.com/seanmay) I use the _existing_ [CSS Custom State](https://developer.mozilla.org/en-US/docs/Web/CSS/:state) to create CSS APIs for custom elements that are akin to `:checked`, `:open`, `:popover-open`, `:focus`, `:hover`, `:active`, `:playing`, `:invalid`, `:required`, `:disabled` for built-in elements. In any of those cases, having something within CSS override them would be inappropriate and it would be for custom elements as well, which is why I’m suggesting that _setting the state_ might be outside of the purview of CSS. That’s not to say one can’t use CSS Custom State to create selectors for custom elements that’d be like the built-in `:any-link` or `:heading`, but to me it seems a little misplaced to do so using something for _state._
> Imagine I have an existing custom element that has shadow styles for `:state(open)` which it sets when `ariaExpanded` has been set for its `ElementInternals`. As the custom element author, I’m deciding when that state gets added. If document authors can set states, then they could trigger those styles when they shouldn’t apply.

I don't understand what issue you're describing. Is the worry that authors may create a 

```css
@state focus {
	/* ... */
}
```

that would override your component's? 
If so, note that (as @seanmay explained) in the proposal these states **cascade**, so no CSS can ever *remove* a state set via either another CSS rule, or JS, it can only add states. So yes, in case of naming conflicts, _other_ elements may also have that state (or your WC, if it matches any author-defined selector *as well*), but I can't think of a case where that would be an issue?


@Loirooriol 
> Presumably this would need some restrictions to avoid cycles:
> 
> @state heading {
> 	matches: :state(heading);
> }

Correct. Perhaps a L1 could entirely forbid `:state()` from these selectors, then we could introduce proper cycle detection at a later stage.

<!-- comment id=3094906027 author=seanmay created=2025-07-21T00:16:21Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3094906027 -->
## @seanmay — 2025-07-21

> I was thinking that `:state()` would be paving the cowpaths, but if so many people are thrown by it, then it's clearly not a good option.

I have been waiting a long time for a proposal like this to land. I think the challenge a lot of people have with "state" is the connotation in the overlap with other languages. I believe that some people are reading it as the setting of state on document nodes, rather than the setting of styles, based on the state of document nodes (example of that, below). I think even an `:is-state(x)`, `:has-state(x)`, or `:matches-state(x)`, even if just parsed and read as an alias for `:state(x)`, would address some of the selector-based portion of it. That, and/or an education campaign. I'm partial to `:matches(x)`, but you can call it literally anything that won't trigger a visceral reaction, and I will be completely on board.


> > Would there be any means of nesting queries (media / container / style) inside of these declarations, or would it make resolving the tree too unwieldy?
> 
> ... I was thinking of it as a simpler at-rule that only takes descriptors and was thinking we could use a similar design for custom media queries (which is also a goal) ... it also mostly solves [#6247](https://github.com/w3c/csswg-drafts/issues/6247) and a bunch of other use cases. I’m not sure we have precedent of at-rules that can nest conditional rules but not regular rules, [@tabatkins](https://github.com/tabatkins) would know.

If it's unprecedented and unlikely to budge, soon, I am more than happy to live with the L1 you initially put forth. That on its own would be a great start (nomenclature misunderstandings aside). But I look at the conditional assignment of the matching selectors, even if it was done through different syntax, or if you could only set an internal variable, and assign it to the matches at the bottom... whatever mechanism works efficiently, and in a simple to teach/maintain/etc way, and I look back at the 2010s and responsive web monstrosities that could have been turned into hundreds of CSS lines, instead of thousands, if this alias, query-nesting, and `:has` had been around at the time. With all of that said, and with things like `if` and functions being worked on, I am really eager to hear reasons that the nesting couldn't / shouldn't be considered and / or included.

> I don't understand what issue you're describing. Is the worry that authors may create a
> 
> @state focus {
> 	/* ... */
> }
> that would override your component's? If so, note that (as [@seanmay](https://github.com/seanmay) explained) in the proposal these states **cascade**, so no CSS can ever _remove_ a state set via either another CSS rule, or JS, it can only add states. So yes, in case of naming conflicts, _other_ elements may also have that state (or your WC, if it matches any author-defined selector _as well_), but I can't think of a case where that would be an issue?

I can't speak for others, of course, but given the misunderstandings around the terminology, and the questions around state-setting, I think some people are taking it to mean that these selectors would actively be changing the state of DOM nodes, which would be reflected in the behavior of their JS.

Forgetting WebComponents (not a dismissal, this will cover either case), and just assuming Element State already existed, the smallest example I can think of, that might reproduce the concern I believe people are having, might be something like this:

```html
<!-- Benevolent CSS, HTML, and JS, below -->
<link rel="stylesheet" href="form-style.css">
<form >
  <!-- Form content -->
  <input type="text" ... >
</form>
<script type="module" src="form-behaviour.js"></script>

<!-- Malevolent State-setting CSS, here -->
<style>
/*
  by declaring the below state, I make it impossible to submit forms or modify inputs,
  via user-action, or JS, because the spec suggests that I am *adding* states in a cascade,
  where the CSS matches apply, even if DOM and JS states no longer do...
  ergo, all forms, inputs, buttons, and textareas will be :disabled if they ever have focus
*/
@state disabled {
  matches: form:focus, form:focus-within, input:focus, textarea:focus, button:focus;
}
</style>
```

I'm not suggesting this is the exact thought process of any given individual, but I think I understand where some of the alternative readings are coming from, and I think this highlights where the logic would conclude, if it were to work in the way this alternate reading might suggest.

<!-- comment id=3095927993 author=Crissov created=2025-07-21T09:37:38Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3095927993 -->
## @Crissov — 2025-07-21

Honestly, it feels like a lot of the purposed use cases could be better handled by `:role()`. #3596

The _heading_ example in particular is moot anyway, because `:heading` has been accepted recently. #11836

<!-- comment id=3096795206 author=LeaVerou created=2025-07-21T13:27:55Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3096795206 -->
## @LeaVerou — 2025-07-21

I didn’t spend much time listing use cases because I know this is something we were already interested in doing, the question was just what is the right API/syntax, but they are absolutely not covered by `:role()`. Those are just easier to use as examples. 

<!-- comment id=3097047503 author=knowler created=2025-07-21T14:35:24Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3097047503 -->
## @knowler — 2025-07-21

@LeaVerou My concern is that this seems to break encapsulation for custom element authors. The rule of not being able to remove a state isn’t enough and actually creates a potential mismatch between a custom element’s own (shadow) styles and its own (internal) state (e.g. `:host(:state(on))` is no longer reliable). The lack of a state can be just as meaningful as the presence of a state, so not being able to unset a state is problematic. As a custom element author, I want the states I set to be reliable for my styling purposes (and for my consumers) because they accurately reflect the semantic state I’m conveying.

<!-- comment id=3097979149 author=knowler created=2025-07-21T18:45:48Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3097979149 -->
## @knowler — 2025-07-21

Something that I do really like about this proposal is how it does really smooth over selectors and create a consistent API:

```css
@state checkbox {
	matches: input[type="checkbox"]:not([switch]);
}

@state checkbox {
	matches: foo-checkbox;
}

@state checked {
	matches: :checked;
}

/* I don’t like this selector, but I like that it creates a consistent API */
:state(checkbox):state(checked) {
}
```

I wonder if we could achieve something to that effect with just an `@alias` at-rule and `:alias()` functional pseudo-class that would act as a space for completely custom selectors for the context (i.e. regular selectors will not work).

```css
/* This is a type selector alias */
@alias checkbox {
	matches: input[type="checkbox"];
}

/* Like the OG proposal, further declarations would add to the list of matches. */
@alias checkbox {
	matches: foo-checkbox;
}

/* This is a pseudo-class alias (I imagine there could be issues with this syntax) */
@alias :checked {
	matches: :checked, :state(checked);
}

/* within `:alias()` is a completely custom selector */
:alias(checkbox:checked) {
	
}

/* This could be mixed with built-in selectors */
:alias(checkbox:checked):focus-visible {
	
}

/* This does not match `input:disabled` because `:alias()` only knows about aliases */
:alias(input:disabled) {
	
}
```

<!-- comment id=3101539145 author=Crissov created=2025-07-22T08:03:44Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3101539145 -->
## @Crissov — 2025-07-22

Selectors within property (or descriptor) values just look wrong. 

Many but not all at-rules agree to the pattern `@rule identifier {descriptor: value;}`. 
I wish this one would use one of the other approaches, e.g.:
- `@state checkbox: input[type="checkbox"];` or
- `@state input[type="checkbox"] {name: checkbox;}`.

I know that _theoretical purity_ is low on the list of priorities for syntax decisions. 

<!-- comment id=3102267717 author=AtkinsSJ created=2025-07-22T11:11:18Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3102267717 -->
## @AtkinsSJ — 2025-07-22

For future-proofing, maybe something like:

```css
@whatever checkbox {
	matches: selector(input[type="checkbox"]:not([switch]));
}
```

A `selector()` seems more future-proof to me in case other ways of matching are invented. (And also because selectors use `:` which could be problematic for parsing otherwise, I can't remember if that's the case or not.)

And using a block-rule with a descriptor is also more future-proof in case there are other ways of affecting how the custom matching works. Though if there aren't any conceivable ways you'd want to do that, then not having it is fine.

<!-- comment id=3109811916 author=benface created=2025-07-23T19:07:36Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3109811916 -->
## @benface — 2025-07-23

I don't have much to add to this conversation, other than I would really love something like this to improve the readability and file size of my generated CSS. Some examples of "custom states" from an actual production codebase:

```ts
const openSelector = `:is([data-gds-exposed-open=true], :not([data-gds-exposed-open]):where([open], :popover-open, :open, [aria-expanded=true]))`

// Should use `:blank` instead of `:placeholder-shown`, but no browser supports it
const blankSelector = `:is([data-gds-exposed-blank=true], :not([data-gds-exposed-blank]):where(input:placeholder-shown, textarea:placeholder-shown, :not(input, textarea)[data-placeholder-shown]))`

const getCheckedStateSelector = (checked: boolean | 'indeterminate') =>
  `:is([data-gds-exposed-checked=${checked === 'indeterminate' ? 'indeterminate' : checked}], :not([data-gds-exposed-checked]):where(${checked !== 'indeterminate' ? `${checked ? ':checked' : ':not(:checked)'}:not([aria-checked], [aria-selected]), ` : ''}[aria-checked=${checked === 'indeterminate' ? 'mixed' : checked}]${checked !== 'indeterminate' ? `, [aria-selected=${checked}]` : ''}))`

const getDisabledStateSelector = (disabled: boolean | 'read-only') =>
  `:is([data-gds-exposed-disabled=${disabled === 'read-only' ? 'read-only' : disabled}], :not([data-gds-exposed-disabled]):where(${disabled === true ? ':is' : ':not'}(:disabled, [aria-disabled=true])${disabled !== true ? `${disabled === 'read-only' ? ':is' : ':not'}([aria-readonly=true])` : ''}))`

const getClickableSelector = (selector?: string) =>
  `:is(:is(a[href], [role='link'], button, [role='button'], [role^='menuitem'], [role='option'], [role='tab'])${selector ?? ''}, :where(label:has(:is(input[type='checkbox'], input[type='radio'])${selector ?? ''})))`
```

These are then passed to Tailwind CSS as custom variants, which means they are repeated over and over in the CSS.

<!-- comment id=3114172569 author=tabatkins created=2025-07-24T16:52:35Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3114172569 -->
## @tabatkins — 2025-07-24

@bkardell 
> you can still see it at [web.archive.org/web/20140320013932/https://tabatkins.github.io/specs/css-aliases#custom-selectors](https://web.archive.org/web/20140320013932/https://tabatkins.github.io/specs/css-aliases/#custom-selectors)).

Out of curiosity, why the archive.org link? The draft is still up, you can just go see it at <https://tabatkins.github.io/specs/css-aliases/#custom-selectors>.

@LeaVerou 

I get the argument you're making about having less distinct syntaxes for this sort of custom selector, but I agree with @knowler in <https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3097047503>: this conflates "information from inside a custom element" with other things, in a way that I think can be confusing.

Part of the reason we added `:state()` in the first place was to let custom elements avoid messing with the class attribute on the host element, as we wanted to leave that for the outer page to use instead. We both didn't want custom elements to accidentally clobber the page's classes thru careless mutation, and also didn't want to force a custom element to use ugly uniquified names just to avoid accidentally matching a class the outer page is using for other purposes and getting wrongly styled. Your suggestion to use :state() here would bring back that latter concern - if your page is already using `:state(heading)` as a way to select h1/etc, and you start using a new custom element that happens to use a heading state as well, you'll automatically match that custom element as well, with no way to avoid that. You'd have to either change your existing alias name across your stylesheets, or edit the component to use a different state name.

I think we should stick with just doing custom pseudo-classes a la my personal proposal. If you do want your `:--heading` pseudo to also match `:state(heading)`, you can easily do that by just adding `:state(heading)` to the selector it's defined with:

```css
@custom-selector :--heading h1, h2 h3, h4, h5, h6, [role=heading], :state(heading);
```

Or even further restrict it to just the particular custom element name you know you want. But if you don't, you can just not do that.

<!-- comment id=3150951762 author=bkardell created=2025-08-04T14:25:30Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-3150951762 -->
## @bkardell — 2025-08-04

> Out of curiosity, why the archive.org link? 

I just had it and it has a date that is from it's origins.. no special reason really.

<!-- comment id=4558705527 author=svgeesus created=2026-05-27T21:11:36Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-4558705527 -->
## @svgeesus — 2026-05-27

Selectors 4 is being stabilized to move to Recommendation. Proposed new features are being re-tagged for Selectors-5.

<!-- comment id=4878221835 author=LeaVerou created=2026-07-03T17:07:06Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-4878221835 -->
## @LeaVerou — 2026-07-03

I just rewrote the proposal, and added a lot of use cases that have come up meanwhile, including many *around* web components, such as pre-rendering/SSR custom states, simplifying custom state management, and many more, plus more examples.
I also fleshed out the design discussion a lot more. I'm tagging it Agenda+ F2F so it gets some broader feedback.

As for the arguments that have been brought up by @knowler and @tabatkins . 

### We should instead introduce a custom pseudo-class primitive

I agree that the name `state` is suboptimal for some of the use cases. But is naming enough to invent a whole new primitive, with its own separate JS API etc? Precedent generally says no.

Also, if we introduce a new custom pseudo-class primitive, as a web component author, I'd want to tap into that! Why expose `:state(selected)` in my API when I can expose `:--selected`?!

So what I can see happening is a weird fragmentation when component authors end up using _both_ to express the same conceptual state: one for compat and one for ergonomics.

I think it's much better to just make `:--foo` an automatic alias of `:state(foo)`. If we later introduce parameters, `:--foo(bar)` would be an alias of `:state(foo, bar)`.

### This breaks encapsulation

The outside world can already clobber everything in your web component:
- They can set attributes and override your nice ARIA
- They can apply all sorts of CSS to the host and its parts (which are usually >80% of shadow DOM elements anyway), whether a certain state applies or not and wreak havoc and you can't do anything because even their `*` has higher priority than any CSS in your shadow DOM
- They can even reach inside your shadow DOM and have a party (unless you're in the <10% of components that use closed shadow roots)

And custom states is where we draw the line?? 😅
 
IMO the goal is to prevent **accidental breakage**. 
For intentional breakage the ship was sinking before it even left the harbor. 😅
I'm not sure how that would happen with this proposal, how would someone *accidentally* assign a state to your component that would break it? And what were they _actually_ trying to do? What are they going to now do _instead_?
Even if they did that to intentionally force it to e.g. open when it otherwise would be closed, it *still* gives the component author more control than if the consumer was trying to do the same thing with CSS overrides. It's not like this pseudo-class grants them access to any additional elements they wouldn't otherwise have access to.

<!-- comment id=5018590336 author=knowler created=2026-07-20T03:48:46Z url=https://github.com/w3c/csswg-drafts/issues/12502#issuecomment-5018590336 -->
## @knowler — 2026-07-20

I still need to find the time to adequately re-read the updated proposal before I share some fuller thoughts, but I’ve become less concerned about the “state” naming and I agree that having one API makes sense.

**Some more use cases**

- I also have frequently found cases where as a custom element author, I want to set a custom state on a shadow part (e.g. `some-element::part(part):state(part-state)`) rather than making that shadow part a custom element with its own internals to do so.
- I would also love to set state on a `::slotted()` element.
