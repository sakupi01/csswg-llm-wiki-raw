---
number: 12953
title: "[css-view-transitions-2][scoped] Allow starting a scoped view transition on a shadow root"
state: "open"
labels: ["Agenda+", "css-view-transitions-2"]
author: "noamr"
created_at: "2025-10-15T19:02:42Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12953"
comments: 9
has_resolution: false
synced_at: "2026-07-05T02:08:56Z"
---

Currently transitions can only be started on an element/document.
Starting a transition on the element as the scope, would only include same-tree descendants of that element when finding participants.
Considering adding `shadowRoot.startViewTransition`, which would pick elements and create pseudo elements inside the shadow scope.


> How does this work with Shadow DOM? If I'm manipulating multiple elements that are directly the children of a shadow root, what do I call `startViewTransition()` on? 

 _Originally posted by @EisenbergEffect in [#9890](https://github.com/w3c/csswg-drafts/issues/9890#issuecomment-3399387164)_

<!-- comment id=3407951937 author=skobes-chromium created=2025-10-15T19:26:40Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-3407951937 -->
## @skobes-chromium — 2025-10-15

This seems good from an ergonomic perspective, but it does raise the question of which element serves as the "originating element" for the `::view-transition*` pseudos. Document transitions hang them on the `<html>` element but the shadow root has no equivalent of that. If we put it on the shadow host then it interferes with calling `shadowHost.startViewTransition()`.

<!-- comment id=3407998117 author=noamr created=2025-10-15T19:43:16Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-3407998117 -->
## @noamr — 2025-10-15

> This seems good from an ergonomic perspective, but it does raise the question of which element serves as the "originating element" for the `::view-transition*` pseudos. Document transitions hang them on the `<html>` element but the shadow root has no equivalent of that. If we put it on the shadow host then it interferes with calling `shadowHost.startViewTransition()`.

It's a good question. Styling them should probably be done via `:host::view-transition*` inside the shadow-scoped style. Their "ultimate originating element" is likely the actual host, but this raises questions about how this works when the element has both an external and an internal view transition at the same time. More details would need to be looked at...

<!-- comment id=4798896126 author=jpzwarte created=2026-06-25T11:50:33Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4798896126 -->
## @jpzwarte — 2026-06-25

I just ran into this. Workaround seems to be to put all the `view-transition-name` declarations inside `::part(...)` selectors, so they're not actually in the shadow root scope.

<!-- comment id=4846554970 author=noamr created=2026-06-30T18:06:09Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4846554970 -->
## @noamr — 2026-06-30

> I just ran into this. Workaround seems to be to put all the `view-transition-name` declarations inside `::part(...)` selectors, so they're not actually in the shadow root scope.

Or to have an element within the scope 

<!-- comment id=4854837372 author=noamr created=2026-07-01T12:32:33Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4854837372 -->
## @noamr — 2026-07-01

I was thinking about this further, and a cleaner and more flexible approach would be to pass in a list of shadow roots to the `startViewTransition` options:

```js
documentOrElement.startViewTransition({
  async update() { ... },
  shadowRoots: [thisElement.shadowRoot, someOtherElement.shadowRoot]
});
```

So the transition is still on an element and everything is dandy, but it can collect names from any descendant shadow root.

<!-- comment id=4854843077 author=noamr created=2026-07-01T12:33:04Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4854843077 -->
## @noamr — 2026-07-01

@bramus @vmpstr @nt1m @emilio any thoughts?

<!-- comment id=4874253996 author=jpzwarte created=2026-07-03T08:21:00Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4874253996 -->
## @jpzwarte — 2026-07-03

I have a pretty specific scenario: design system web components such as `<page-header>` that has view transitions that are linked to the scroll timeline (via https://chrome.dev/view-transitions-toolkit/scroll-driven-view-transition/). Since element view transitions are not baseline yet, i use document view transitions. The reason i'm mentioning this is that i can't foresee what the impact is of @noamr API suggestion. Ideally these kind of scroll-linked-view-transitions in shadow DOMs should "just work"? Is that reasonable/doable?

<!-- comment id=4874313964 author=noamr created=2026-07-03T08:29:29Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4874313964 -->
## @noamr — 2026-07-03

> I have a pretty specific scenario: design system web components such as `<page-header>` that has view transitions that are linked to the scroll timeline (via https://chrome.dev/view-transitions-toolkit/scroll-driven-view-transition/). Since element view transitions are not baseline yet, i use document view transitions. The reason i'm mentioning this is that i can't foresee what the impact is of @noamr API suggestion. Ideally these kind of scroll-linked-view-transitions in shadow DOMs should "just work"? Is that reasonable/doable?

Hopefully element scoped view transitions would be baseline at some point... Either way, yea with this you should be able to set view transition names inside a shadow root that link to the scroll timeline. You can also do this today with parts.

<!-- comment id=4881348215 author=emilio created=2026-07-04T08:46:50Z url=https://github.com/w3c/csswg-drafts/issues/12953#issuecomment-4881348215 -->
## @emilio — 2026-07-04

I think the transition should probably still be in the host, yeah, or otherwise should behave the same rendering-wise as if you called it on the host... But at that point having it on the shadow tree seems a bit overkill and prevents from transitioning slotted content along with the shadow contents, right?

I guess another option would be to collect names in the flat tree but keep their scoped-ness right? so view-transition-name: foo inside and outside the shadow tree could be two different names? But I think that could be a bit awkward / be undesired in some cases.

So the "give me a set of shadow trees which are ok to pull names from" seems possibly fine, yeah.
