---
number: 12978
title: "Proposal: a way to _not_ reverse an animation's timing function when it plays in `reverse` or `alternate` direction"
state: "open"
labels: ["Needs Thought", "css-animations-2"]
author: "benface"
created_at: "2025-10-19T01:46:25Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12978"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:45:44Z"
---

`animation-direction: alternate` is a nice way to write less keyframes when the second half of the animation is essentially a mirror of the first half, but it doesn't always mean we want the timing function to reverse in the second part. I wonder if we could add a flag to [the upcoming `animation-easing` property](https://github.com/w3c/csswg-drafts/issues/6982#issuecomment-2378038074) to make that customizable? 

<!-- comment id=3503254444 author=ydaniv created=2025-11-07T15:39:04Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-3503254444 -->
## @ydaniv — 2025-11-07

Yeah, this is something that's common with animations that use asymmetrical easings, especially when using `linear()`, like "bounce" easings. You want the bouncing to keep the "forward" easing also when reversing the animation, regardless of direction.

<!-- comment id=4060132335 author=ydaniv created=2026-03-14T09:14:39Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4060132335 -->
## @ydaniv — 2026-03-14

I see 3 options here:

1. Add a modifier to `animation-easing` that controls how the easing is done according to direction
2. Add a new animation longhand `animation-easing-direction`
3. Make `animation-easing` a shorthand, with `-function` and `-direction` longhands

Values can be `normal` and `flip` (or `relative`?).
Open to bikeshedding, of course.

I think I have slight preference to option 1 since it sounds the most simple.

<!-- comment id=4429071523 author=bramus created=2026-05-12T09:20:37Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4429071523 -->
## @bramus — 2026-05-12

+1 to this request.

Instead of tacking it onto `animation-easing`, why not an extra keyword on the `animation-direction`? The concept of not reversing the timing functions does seem very much linked to the `animation-direction`. And it also wouldn’t require implementers to implement the `animation-easing` functionality first.

Something like `animation-direction: alternate keyword-that-asks-to-not-flip-the-timing-functions`.

Or maybe a new keyword  for `animation-direction` like `flip` (and `flip-reverse`).

<!-- comment id=4443032120 author=bramus created=2026-05-13T16:13:25Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4443032120 -->
## @bramus — 2026-05-13

For completeness: here’s [a demo](https://codepen.io/bramus/pen/pvNbLKw) that has two animations:

- The first one (on the left) uses `animation-direction: alternate;`, in which the timing reverses. You can see that the bouncing back timing is _visually_ not correct (because it got reversed).
- The second one (to the right) is a manually fixed version that stretched out the keyframes (and the duration) to get the effect I would want  `animation-direction: flip;` to do out of the box (using the orig keyframes)

<!-- comment id=4458637454 author=ydaniv created=2026-05-15T09:30:15Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4458637454 -->
## @ydaniv — 2026-05-15

> Instead of tacking it onto `animation-easing`, why not an extra keyword on the `animation-direction`? The concept of not reversing the timing functions does seem very much linked to the `animation-direction`.

It's a bit of both, yeah, but IMO it's more a property of the easing than the direction.

Let's consider an animation that has a single iteration, so `animation-direction` is not set, it has the initial value of `normal`.
Now you want to trigger it forwards/backwards, either via WAAPI or `animation-trigger`, but you want the easing to flip when reversed.

Let's consider the two options:

1. When set on the **easing**:

```css
.animated {
  animation: move 1s both;
  animation-easing: ease-out flip;
  animation-trigger: --click play-forwards play-backwards;
}
```

2. When set on the **direction**:

```css
.animated {
  animation: move 1s both;
  animation-easing: ease-out;
  animation-direction: flip;
  animation-trigger: --click play-forwards play-backwards;
}
```

IMO the first one is clearer.

<!-- comment id=4556282829 author=css-meeting-bot created=2026-05-27T16:00:55Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4556282829 -->
## @css-meeting-bot — 2026-05-27

The CSS Working Group just discussed ``Proposal: a way to _not_ reverse an animation's timing function when it plays in `reverse` or `alternate` direction``.

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> ydaniv: there's a request to be able to control the way the iteration easing behaves when alternating direction<br>
&lt;emilio> ... more useful when the easing function isn't symmetrical<br>
&lt;emilio> ... it happens quite often, e.g. if you want ease-out on both directions when alternating<br>
&lt;emilio> ... this is even more useful with triggers or even with just triggering animations from JS<br>
&lt;flackr> q+<br>
&lt;emilio> ... when you're toggling an element in or out or from different states, you don't want to tweak the easing function for each case to preserve the effect<br>
&lt;emilio> ... we have an agreement that this is useful<br>
&lt;bramus> q+<br>
&lt;emilio> ... now the question is, if we want to have it, what'd be the syntax<br>
&lt;emilio> ... so there's the proposal for extending animation-direction or animation easing<br>
&lt;emilio> ... I propose modifier in animation easing<br>
&lt;astearns> ack flackr<br>
&lt;emilio> flackr: so an animation has two easings at any time, the animation-wide easing and the keyframe easing<br>
&lt;emilio> ... presumably this'd change both of them?<br>
&lt;emilio> ... or we want a property per easing?<br>
&lt;emilio> ydaniv: I think this is more useful for the iteration wise easing, keyframe-wise it breaks if you try to over-think it<br>
&lt;emilio> ... not sure if there's a use case for the per-keyframe<br>
&lt;emilio> flackr: most pure CSS animations use the keyframe one<br>
&lt;emilio> ydaniv: so I propose adding it to the new animation-easing property<br>
&lt;emilio> flackr: maybe? One syntax proposal is maybe `animation-easing-direction: normal | forwards`, where normal follows animation playback direction<br>
&lt;emilio> ... this would also apply any time you set playbackRate &lt; 0<br>
&lt;emilio> ... you'd also flip this<br>
&lt;emilio> ... so you'd see an immediate update of the effect to get some new value<br>
&lt;bramus> emilio: you are describing current behavior?<br>
&lt;bramus> flackr: no. with this change, if you change animation-direction midway through, you’d see a jump<br>
&lt;emilio> ydaniv: yeah that's a problem<br>
&lt;emilio> flackr: I don't know, is it?<br>
&lt;emilio> ydaniv: yeah because you may want to have animations with just from/to<br>
&lt;emilio> ... and you want them to flip half way<br>
&lt;emilio> ... if you are opening / closing a menu<br>
&lt;emilio> ... this would be a classic case where you'd want the menu to open and bounce inside<br>
&lt;emilio> ... bounce completely flips<br>
&lt;bramus> Bounce example: https://codepen.io/bramus/pen/pvNbLKw<br>
&lt;emilio> ... but if it's mid-way you still want the reverse<br>
&lt;emilio> ... you don't want it to jump if you reverse in the middle of the animation<br>
&lt;emilio> flackr: so maybe the effect isn't so much having an effect not follow the direction, but having an alternate version that flips the easing<br>
&lt;emilio> ... it seems we might need to think a bit more about these cases<br>
&lt;astearns> ack bramus<br>
&lt;emilio> bramus: one of the things I mentioned in the issue is that it'd be interesting to not hang it off animation-easing because that still needs to be implementing<br>
&lt;emilio> ... maybe it should be something on the animation-direction<br>
&lt;emilio> ... an extra keyword for alternate or something<br>
&lt;emilio> ... or maybe a new keyword for animation-direction, where you don't flip the timing-function<br>
&lt;emilio> ... being well aware that a good keyword for it would be difficult<br>
&lt;ydaniv> s/completely flips/completely breaks/<br>
&lt;emilio> astearns: there are some difficulties, any arguments for not handling this at all<br>
&lt;emilio> bramus: you mean just leave as-is?<br>
&lt;emilio> astearns: asking if anyone thinks we should leave it as-is<br>
&lt;emilio> q+<br>
&lt;emilio> ... basically is there a good reason not to do this<br>
&lt;emilio> bramus: I think it's a problem worth solving<br>
&lt;emilio> ... see codepen linked above<br>
&lt;ydaniv> q+<br>
&lt;emilio> ... ball bouncing inside the rectangle is totally broken, the "fixed" version is manually fixed-up, and I think it'd be nice to make easy<br>
&lt;emilio> astearns: playing devil's advocate you _can_ fix the effect<br>
&lt;emilio> flackr: another thing would be, you could have an easy way to reverse your easing function<br>
&lt;emilio> bramus: but now it does reverse it by default<br>
&lt;emilio> flackr: but you could fix it by adding a keyword to flip the easing on the reverse direction<br>
&lt;emilio> bramus: right, and it ends up not flipped because negatives<br>
&lt;bramus> s/negatives/negative playback rate/<br>
&lt;emilio> flackr: I think we can fix it multiple ways, I lean about tweaking how alternate works because that doesn't have those edge cases when stopping mid-way<br>
&lt;emilio> ... an alternative would be having an easy syntactic way to reverse the easing function and then authors would be able to do that<br>
&lt;emilio> bramus: but then it wouldn't happen on the way back automatically<br>
&lt;emilio> flackr: yeah you might need the reverse thing when playing an animation backwards explicitly<br>
&lt;emilio> ... maybe we need both?<br>
&lt;emilio> bramus: maybe a flip-timing keyword?<br>
&lt;astearns> ack emilio<br>
&lt;bramus> emilio: agree with flackr that it seems like changing alternate is less confusing (less side-effects0 and seems like a good start<br>
&lt;bramus> … if we only do reversal thing — as an author you are reversing the easing … fun, it works, but feels a bit off<br>
&lt;bramus> … would lean to fixing on the alternate, then if needed you can reverse the easing if you need to<br>
&lt;bramus> … the alternate thing is what most use-cases care about<br>
&lt;bramus> flackr: great place to start, doesn’t have side-effects<br>
&lt;emilio> bramus: wouldn't it break a bunch of sites?<br>
&lt;bramus> bramus: wouldn’t that break?<br>
&lt;emilio> flackr: not breaking the existing alternate, just an opt-in / replacement for it<br>
&lt;emilio> bramus: which is what I proposed, flip / flip-reverse which would do alternate but with the flipped timing<br>
&lt;emilio> ack bramus<br>
&lt;astearns> ack ydaniv<br>
&lt;emilio> ydaniv: so we're solving it first on the direction to avoid the discontinuity on the flipping mid-way case<br>
&lt;emilio> ... right?<br>
&lt;emilio> ... so we only flip if you actually flipped the end / the beginning<br>
&lt;emilio> flackr: not exactly<br>
&lt;emilio> ... when you have animation-direction alternate every cycle you're playing in reverse<br>
&lt;emilio> ... you'd additionally flip the easing<br>
&lt;emilio> ydaniv: ah so only when you have multiple iterations<br>
&lt;emilio> ... I think we still need to fix the single iteration case where you manually flip the animation<br>
&lt;emilio> ... that'd still be open<br>
&lt;emilio> flackr: yes<br>
&lt;emilio> ydaniv: we need to solve this separately<br>
&lt;emilio> flackr: ideally with a way that doesn't cause a jump of the current value<br>
&lt;emilio> astearns: we're at time, should we take it back to the issue?<br>
&lt;emilio> &lt;br size=2min><br>
</details>

<!-- comment id=4556295072 author=bramus created=2026-05-27T16:02:19Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4556295072 -->
## @bramus — 2026-05-27

To also tackle the single iteration situation, the extra keyword route onto `animation-direction` would fix both:

```css
animation-direction: alternate flip-timing;
animation-direction: reverse flip-timing;
```

<!-- comment id=4582984151 author=ydaniv created=2026-05-30T13:40:05Z url=https://github.com/w3c/csswg-drafts/issues/12978#issuecomment-4582984151 -->
## @ydaniv — 2026-05-30

> To also tackle the single iteration situation, the extra keyword route onto animation-direction would fix both:
>
> ```css
> animation-direction: alternate flip-timing;
> animation-direction: reverse flip-timing;
> ```

I'm not that really works in this scenario.
The single iteration case is mostly observed when reversing an animation via WAAPI:

```js
const animation = element.getAnimations()[0];
let firstTime = true;

const button = document.getElementById('toggle');
button.addEventListener('click', () => {
  if (firstTime) {
    animation.play();
  } else {
    animation.reverse();
  }
});
```

Or via `animation-trigger`:

```css
.animated {
  animation: slide 0.3s var(--bounce-easing) both;
  timeline-trigger: --tr view() cover 20% 80%;
  animation-trigger: --tr play-forwards play-backwards;
}
```

So neither actually set the `animation-direction` property, the `-direction` remains `normal`.
What you want is to be able to say that when played in reverse the timing if flipped. That's I thought of sticking this property on the `-easing` property.

------

Regarding the discontinuity issue. Since the easing function is `f(t) -> p`, where `t` is `currentTime` and `p` is the effect progress, we want to maintain same progress - so we don't get a visual jump - and therefor we have to _NOT_ maintain `currentTime`.
So I think we want to be able to do something along the following procedure:

1. Flip the easing function
2. Get the inverse function of the flipped easing function `f^-1(p) -> t` (let's assume the function is invertible for now)
3. Use the inverse function with the current progress to get a new `currentTime`
4. Set the new `currentTime` to the animation and play it from there


So first question is: is getting the inverse function of the easing function something that's feasible?
Another question is what to do with functions where are not invertible, e.g. bounce.
Ideally we'd want to use the estimated "nearest" time with same progress, again, not sure from implementation POV if feasiable.
