---
number: 12240
title: "[css-overflow-5]: calling focus on the carousel / scroll container"
state: "open"
labels: ["a11y-tracker", "css-overflow-5"]
author: "smhigley"
created_at: "2025-05-28T01:35:22Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12240"
comments: 9
has_resolution: false
synced_at: "2026-09-24T04:47:17Z"
---

## Current behavior

Adding `::scroll-marker-group` and scroll markers to an element appears to make it focusable but not in the tab order, similar to adding `tabindex="-1"` to the element.

Saving a reference to the scroll container and calling `element.focus()` moves focus to the scroll container itself, not to either the inner scroll buttons or scroll marker group. This can be verified either by running a screen reader or inspecting the related FocusEvent's `event.target` (which currently points to pseudo-elements when you tab to them, but points to the scroll container if you programmatically call focus).

## Problem

There are two related issues around handling `.focus()` calls, one with the current behavior, and the other with the API in general.

### 1. Current behavior

It seems pretty clear to me that making an element into a scroll container does not imply an authorial intent for that element to itself be interactive or exposed to assistive tech users. Instead, the scroll buttons, markers, and scrolling content are the semantically relevant controls. In this basic case, I don't think the scroll container element should become focusable just because of `::scroll-marker-group` -- it likely does not have (and often _should_ not have) an interactive role and accessible name that would support it being focusable and accessible.

By default, it seems like the best course of action would be to either set focus to the first internal focusable element (which is currently a scroll button if it exists, and the scroll markers if it doesn't), or always set it to the scroll markers. I'm curious on other folks' thoughts on which it should be, since generally I would lean towards moving focus to the scroll markers, but having the scroll buttons before them in the tab order complicates that.

I personally think authors need the ability to call focus on the prev/next scroll buttons and scroll marker group separately, so perhaps figuring out how to expose that would solve this question (and the next) as well.

### 2. Resolving `.focus()` on focusable scroll containers

This problem extends beyond resolving the current issue with focusing what are otherwise unfocusable scroll containers. If an author has intentionally created a focusable scroll container, then calling `scrollContainerElement.focus()` would need to send focus directly to the element. Authors would still need a way to send focus to the scroll marker/button controls though, separately from the container element.

Since pseudo-elements are not currently accessible in JS, and also are type Element and do not support the `.focus()` method, it probably needs someone more creative than I am right now to figure out how this would be solved 😅

(As a brief top-of-mind example on why this would be necessary -- consider an overflow container with `tabindex="-1"` to handle fallback focus if all content is deleted and there is no longer overflow. However, when sending focus to the region while it is interactive, the author would want to move focus directly to the scroll buttons/markers. Or a scenario where the author has made custom keyboard commands to jump to a specific slide, and needs to manage focus when doing so.)

<!-- comment id=2967773766 author=smhigley created=2025-06-12T18:11:23Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-2967773766 -->
## @smhigley — 2025-06-12

@flackr (well I now feel awkward trying to write 6 different comments tagging you in with slightly different wording)

<!-- comment id=3282135833 author=flackr created=2025-09-11T18:15:32Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3282135833 -->
## @flackr — 2025-09-11

> It seems pretty clear to me that making an element into a scroll container does not imply an authorial intent for that element to itself be interactive or exposed to assistive tech users.

This isn't related to `scroll-marker-group`, but rather just making it a scroll container, see https://chromestatus.com/feature/5231964663578624. I.e. just having a scroll container makes it focusable https://jsbin.com/yufibuw/edit?html,css,js,output

@mfreed7 you're listed an owner on this feature and maybe have more context about where this was discussed.

<!-- comment id=3282160016 author=smhigley created=2025-09-11T18:23:49Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3282160016 -->
## @smhigley — 2025-09-11

@flackr I'm curious if you think the `scroll-marker-group` behavior should also trigger focusability in the same way that regular scroll should. The reason for it happening with a traditional scroll overflow element is so keyboard users can scroll, but that doesn't really apply to scroll marker containers. Mouse scrolling doesn't do anything, and keyboard arrows don't scroll in the traditional way.

That said, the primary issue is still figuring out how authors can call focus directly on the scroll markers, separately from the container element.

<!-- comment id=3282177985 author=flackr created=2025-09-11T18:29:24Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3282177985 -->
## @flackr — 2025-09-11

> but that doesn't really apply to scroll marker containers. Mouse scrolling doesn't do anything, and keyboard arrows don't scroll in the traditional way.

But it is just a scrolling element.

I'm confused, adding scroll-marker-group does not affect the focusability of an element as far as I know.

<!-- comment id=3282184968 author=flackr created=2025-09-11T18:31:55Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3282184968 -->
## @flackr — 2025-09-11

> That said, the primary issue is still figuring out how authors can call focus directly on the scroll markers, separately from the container element.

@danielsakhapov (https://github.com/w3c/csswg-drafts/issues/12163) and @naomr (https://github.com/w3c/csswg-drafts/issues/12575) have both proposed ways that we might want to handle events on pseudo-elements. Either of these strategies could lend themselves to allowing setting focus, e.g.
* If we have a CSSPseudoElement, you can call focus on it.
* We could have a pseudoElement target string on the focus method.
* If there is a DOM element proxy for it you could call focus on this.

<!-- comment id=3293294924 author=mfreed7 created=2025-09-15T17:49:50Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3293294924 -->
## @mfreed7 — 2025-09-15

> This isn't related to `scroll-marker-group`, but rather just making it a scroll container, see https://chromestatus.com/feature/5231964663578624. I.e. just having a scroll container makes it focusable https://jsbin.com/yufibuw/edit?html,css,js,output
> 
> [@mfreed7](https://github.com/mfreed7) you're listed an owner on this feature and maybe have more context about where this was discussed.

Right - starting with Chrom(ium) 132, scroll containers are focusable, and they become keyboard focusable (part of the sequential focus navigation order) if they do not contain any other focusable descendants.



> [@flackr](https://github.com/flackr) I'm curious if you think the `scroll-marker-group` behavior should also trigger focusability in the same way that regular scroll should. The reason for it happening with a traditional scroll overflow element is so keyboard users can scroll, but that doesn't really apply to scroll marker containers. Mouse scrolling doesn't do anything, and keyboard arrows don't scroll in the traditional way.

This is an interesting point. Do you think we should modify the above heuristic in some way to avoid that treatment for scroll marker containers? That would (I think?) be relatively easy to do, if it's the correct thing to do.

<!-- comment id=3308726442 author=smhigley created=2025-09-18T17:34:04Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-3308726442 -->
## @smhigley — 2025-09-18

I think my initial confusion around it triggering focusability was caused by a personal issue with janky horizontal scroll behavior when I was testing, so I thought it just didn't support mouse/keyboard scroll 😅. Given it does, that seems OK.

The central issue is still the authoring need to be able to call focus on the container separately from the scroll markers, together with the following:
- When focus is on the scroll markers, it should be possible to find the current and next/previousfocusable element through using `document.activeElement`
- When looking for the next/previous focusable element starting from either something within the carousel or after it, it should be possible to query & find the scroll markers separately from the container (also based on where they are in the DOM in relation to other focusable elements, which is different from where the container is)

From the perspective of a library maintainer, those should ideally be possible for every possible way to author a carousel, since the team writing focus management code is often not the team writing the carousel code.

<!-- comment id=5054246826 author=smhigley created=2026-07-23T04:11:53Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-5054246826 -->
## @smhigley — 2026-07-23

I thought it might be useful to add a concrete example of where this might be needed:

Sometimes a web app has multiple discrete sections (e.g. an editing surface, a toolbar, and a sidepane) where each section has its own tab loop, with F6 (or ctrl + F6) moving focus between sections. When authoring both the tab loop and the F6 focus target, it would be important to be able to query focusables and set focus on the most relevant focus target depending on the forwards/backwards direction.

Similarly, there would be a need to manage tabindex / tabbability for both the scroll container and the scroll markers separately.

<!-- comment id=5717307987 author=css-meeting-bot created=2026-09-17T15:52:49Z url=https://github.com/w3c/csswg-drafts/issues/12240#issuecomment-5717307987 -->
## @css-meeting-bot — 2026-09-17

The CSS Working Group just discussed `[css-overflow-5]: calling focus on the carousel / scroll container`.

<details><summary>The full IRC log of that discussion</summary>
&lt;keithamus> sarah: overview with 12240 - in adding a bunch of interactive elements in CSS, one is not able to query them, generally, e.g. qSA, focusable qSA for all focusable elements - every JS lib that deals with focus back to dojo or pre-jquery, and up until present, it's very common for every mature script system to do this. Very common in JS libs, some<br>
&lt;keithamus> examples if you're in a custom focus management - moving focus back and forth between large areas e.g. to recover focus or move high level sections or arrowing between items in a chat list, drilling in, drilling out. It's common to look for any focusable item within a range to move focus between them. Cannot query for them<br>
&lt;keithamus> sarah: scroll markers vs scroll container, also itself focusable. Or an aside which could be focusable. Possible you need to differentiate between what has focus and what you need to call focus on<br>
&lt;flackr> q+<br>
&lt;astearns> ack vmpstr<br>
&lt;keithamus> sarah: hit to DX where you need to find out what focus is set on and where it moves to, especially as document.activeElement might not be the element that's getting announced.<br>
&lt;astearns> ack flackr<br>
&lt;keithamus> flackr: there are a lot of different points raised. Reverse order - the first is more complicated.<br>
&lt;keithamus> flackr: activeElement not being the element that is itself focusable, which is what you get with shadowdom, this being a new API it would be possible with a pseudo interface that it could return some pseudo which is the focusable thing<br>
&lt;keithamus> flackr: given it would only occur on sites which have adopted the new APIs it wouldnt break old sites, but it does have complications, change at a distance with what is consuming the element<br>
&lt;masonf> differences between browsers about what is focusab.e...<br>
&lt;vmpstr> +1<br>
&lt;keithamus> flackr: querying focusable elements; I have not seen qSA that can accurately handle the complex set of situations which are focusable, e.g. a parent element inerting its children. I've seen libs query for tabindex but not heirarchical problems which can occur, or things which arent displayed. I think it's possible for browser to expose e.g. what<br>
&lt;keithamus> can be focused or what is next to be focused, which can be consumed as a more reliable mechanism for finding this. Things selectable via qSA are not strictly focusable.<br>
&lt;sarah> q+<br>
&lt;keithamus> flackr: on separately calling focus, we've talked about ways to focus the element, so if activeElement returned a Pseudo we could have API for that, or params to focus method, e.g. how you animate pseudos<br>
&lt;keithamus> flackr: happy to explore and pursue whichever makes most ergonomic sense. Appreciate into insight what would help there.<br>
&lt;keithamus> astearns: we've been working on pseudo interface for event listeners, adding more API to that might make more sense.<br>
&lt;astearns> ack sarah<br>
&lt;vmpstr> (checkVisibility?)<br>
&lt;keithamus> sarah: how you filter for inert or hidden, there is an API for that - the visibility APIs, which works fine and is part of qSA - you filter by what is still visible or not inert. checkvisibility.<br>
&lt;RRSAgent> I have made the request to generate https://www.w3.org/2026/09/17-css-minutes.html fantasai<br>
&lt;keithamus> sarah: being able to call focus() on a pseudo would probably be a requirement for this, but the problem with being able to query for them, if you need to query for them, check its style etc, that's not a workable way to find focusable elements<br>
&lt;masonf> q+<br>
&lt;keithamus> flackr: no the proposal would be to e.g. have a function on element to give you a list of descendant focusable elements<br>
&lt;astearns> q+<br>
&lt;keithamus> sarah: focusnext would be useful but not entirely, e.g. jumping focus section to section, or a focusable right here use cases.<br>
&lt;keithamus> sarah: under impression that having a general way to query for foucables is not really workable?<br>
&lt;astearns> ack masonf<br>
&lt;keithamus> masonf: was on the queue to say that. The bar we should set for ourselves is that it's possible but not easy. If you've taken over focus management from the browser you've taken on a huge job. Even between browsers it's different, or e.g. scrollers can or cannot be focusable depending on what's inside.<br>
&lt;keithamus> masonf: trying to make something where you have a qSA to find focusable things is not going to fly<br>
&lt;vmpstr> q+<br>
&lt;keithamus> masonf: adding ability to call focus on pseudos is great, not possible right now<br>
&lt;sarah> q+<br>
&lt;keithamus> astearns: likely pitfalls with either approach - if we make it so qSA returns elements and pseudos will break a lot unless it's opt in, but then that gets clunky itself. Having specific focus API runs risk of having these bespoke APIs for every purpose, which is clutter in of itself. Might need both though, but pitfalls to work around<br>
&lt;astearns> ack astearns<br>
&lt;astearns> ack vmpstr<br>
&lt;keithamus> vmpstr: if getting list of all focusable elements in subtree is hard, then getting a "what is next focusable element" might be enough. Wanted to note, we mention check visibility, we added this because it was hard to check without doing rendering work.<br>
&lt;masonf> q+<br>
&lt;keithamus> vmpstr: example of a feature we added for a particular case to allow us to proceed with web platform. Focus isn't something we've addressed. Without getting into whats possible and whats not, surely _something is_.<br>
&lt;keithamus> vmpstr: allow more advanced focus management than whats possible today<br>
&lt;astearns> ack sarah<br>
&lt;chrishtr> q+<br>
&lt;keithamus> sarah: I'd want to know what that would be. I've not heard a proposal to address what's common in focus management systems. If you havent worked on keyboard management its often ... how common this is.<br>
&lt;keithamus> sarah: having a focusable qSA is just a given in any project i've worked on beyond simple demo site. Very common developer part of the web for decades. Breaking that - or suddenly saying "there's not that" is going to cause a lot of pain, primarily around a11y behaviours. If theres a way to do that, wonderful. My understanding is that returning a<br>
&lt;keithamus> list is a no go for several reasons, not just compat.<br>
&lt;keithamus> sarah: having it would address that concern of mine entirely.<br>
&lt;keithamus> q+<br>
&lt;astearns> ack masonf<br>
&lt;keithamus> masonf: totally acknowledge focus management has been around for a long time, I'd rather us fix the problems though so that the painful work is in the browser.<br>
&lt;keithamus> masonf: at least in chromium when you hit tab it walks the dom to try and find the next thing. We can expose that - "get next focusable". It could also return pseudos without breaking anything<br>
&lt;chrishtr> Proposed resolution: write up proposal for extending activeElement to include pseudo elements, add a focus property to CSSPseudoElement, and add a next and previous focusable element/pseudo-element property to elements and pseudo elements. Put it in &lt;this spec (which one?)'s editor's draft><br>
&lt;keithamus> masonf: I would not want to expand that into "give me all of them", dom tree walk, perf bad.<br>
&lt;sarah> q+<br>
&lt;keithamus> chrishtr: whoever writes the proposal could come back to the proposal<br>
&lt;masonf> s/focus property/focus method<br>
&lt;keithamus> chrishtr: activeELement - the currently focused element, would need something like activePseudo or something, to let you know if it's pseudo was active.<br>
&lt;fantasai> scribe+<br>
&lt;astearns> ack chrishtr<br>
&lt;astearns> ack keithamus<br>
&lt;fantasai> keithamus: The focus stuff, I agree with Mason. Focus management is very complex, and it depends on things -- not just visibility, but what is currently focused.<br>
&lt;fantasai> keithamus: That's not something userland code wants<br>
&lt;fantasai> keithamus: Userland code probably wants "I want all the focusable elements", that's a new concept<br>
&lt;fantasai> keithamus: We can't do that<br>
&lt;fantasai> keithamus: So there's a missing concept in the browser, that I don't think we can introduce based on current semantics, that individual sites might apply<br>
&lt;fantasai> keithamus: E.g. site might say "We might not use X component". Sometimes authors opt out of using popovers because they want to manage focus themselves.<br>
&lt;fantasai> keithamus: Even if it's all software, I don't know that we can solve it given the constraints on each side.<br>
&lt;astearns> ack sarah<br>
&lt;fantasai> sarah: The three points in the proposed resolution don't actually address the concerns<br>
&lt;fantasai> sarah: I do remember evaluating when focusNext/focusPrevious proposal was presented, if that would address concerns<br>
&lt;keithamus> sarah: 3 points in proposed resolution dont address what focus management actually does. focusNext focusPrevious was introduced in openui, I dont think it addresses our focus needs fully, e.g. it doesn't solve our use cases whenr I audited.<br>
&lt;fantasai> sarah: and I remember going through the instances, and finding that it didn't solve all the problems<br>
&lt;vmpstr> q+<br>
&lt;keithamus> sarah: can check and add more details in issue<br>
&lt;keithamus> sarah: also in proposed resolution I'd like to see impact on DX that this introduces<br>
&lt;masonf> I'd love to see how. `function getAllFocusables() {while(something) {focus_list.push(current=getNextFocusableAfter(current))}}` should enumerate them all.<br>
&lt;keithamus> sarah: one big reason to do this in CSS is DX - negative impacts in DX elsewhere or when it comes to real world systems, it should be weighed against this.<br>
&lt;keithamus> astearns: action on sarah to come back with more analsys then, especially on proposed resolution and how it does/doesn't solve issues.<br>
&lt;keithamus> q+<br>
&lt;astearns> ack vmpstr<br>
&lt;sarah> q+<br>
&lt;keithamus> vmpstr: 2 things: I support doing more research, so roughly what chrishtr proposed. Second: I want to clarify, when we say focusnext/focusprevious does not address all the issues, I believe its a step in the right direction, we should take that step, even if it doesnt address all of the issues. Or is there a feeling that it's not?<br>
&lt;astearns> ack sarah<br>
&lt;keithamus> sarah: I do think focusnext/focusprevious is a great suggestion, it would make things easier, my worry is that inroducing css carousels would fundementally break focus management that we wouldn't be able to repair.<br>
&lt;masonf> q+<br>
&lt;keithamus> astearns: useful but not sufficient<br>
&lt;keithamus> sarah: yes<br>
&lt;masonf> q-<br>
&lt;astearns> ack keithamus<br>
&lt;chrishtr> q+<br>
&lt;vmpstr> q+<br>
&lt;TabAtkins> Agreed, having access to the sequential focus order is pretty important imo<br>
&lt;sarah> q+<br>
&lt;keithamus> keithamus: I think one use case is focusing a region, such as programatically setting SFNSP - we had a proposal for this a few years ago, but this is an example of use cases focusnext/focusprevious don't address.<br>
&lt;keithamus> chrishtr: API to call on element to find next focusable element in subtree and focus it, sounds useful. I didn't hear anyone say the three things suggested on resolving on are not good, so that's great.<br>
&lt;vmpstr> q-<br>
&lt;keithamus> chrishtr: even if we dont have a resolution we have all 3 we can pursue on<br>
&lt;keithamus> astearns: premature to resolve on any of them yet<br>
&lt;astearns> ack chrishtr<br>
&lt;keithamus> chrishtr: yes I want to resolve that we should explore those.<br>
&lt;keithamus> sarah: one example is the need to query whether an are has focusable or not, without calling focus.<br>
&lt;keithamus> masonf: you could do that right? Call it and see if it skips the tree<br>
&lt;keithamus> flackr: querying and setting right?<br>
&lt;keithamus> astearns: both<br>
&lt;TabAtkins> the acting is just calling `.focus()` on the thing you queries<br>
&lt;keithamus> flackr: with an ability to query you can always call it<br>
&lt;keithamus> masonf: to be crisp: assuming the current focussed element, find the next element that might be focused if you hit the tab key.<br>
&lt;keithamus> astearns: vmpstr will open an issue and we can go into this there.<br>
&lt;keithamus> astearns: we will bring this up again in two weeks<br>
&lt;keithamus> masonf: all 4 remain on agenda, this one can bubble to top<br>
&lt;keithamus> astearns: thanks everyone, done for today<br>
</details>
