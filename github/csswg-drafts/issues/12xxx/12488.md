---
number: 12488
title: "[css-values] Provide functions for resolving relative units based on type"
state: "open"
labels: ["Needs Design / Proposal", "Needs Edits", "css-values-5", "css-variables-2"]
author: "mirisuzanne"
created_at: "2025-07-16T20:44:28Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12488"
comments: 15
has_resolution: true
synced_at: "2026-07-04T04:45:42Z"
---

Registered and un-registered custom properties with relative units behave differently in the cascade. When a property is not registered, it will inherit before resolving (`2em` inherits as `2em` and resolves based on the element where applied) – but a registered `<length>` property will resolve `2em` into a pixel value that inherits. The value is not re-calculated at the point where the property is applied.

Both behaviors can be useful, and are becoming even more relevant as we think about the behavior of [mixins and functions](https://github.com/w3c/csswg-drafts/issues/9350). Various articles have been written about how to 'capture' a value -- @kizu has covered both the [existing and `@function`-based approaches](https://blog.kizu.dev/functional-capturing/), and links to several other relevant posts. The functional approach is even used in an [example of custom mixins](https://drafts.csswg.org/css-mixins/#example-399711fd) where authors want the units passed into a parameter to resolve once for a given call of the mixin.

```css
@function --as-length(--arg <length>) { result: var(--arg); }
```

It seems to me that there's enough desire for this behavior, it probably should not rely on every project duplicating the same custom function. Maybe we need a built-in function (or functions) for this?

Maybe tangential, but…

- I believe there was also discussion of the `style()` function used in both `if()` and `@container`, with a proposal for something like `style(--x as type(length) > --y)`. I can't find that conversation off-hand.
- There are also use-cases for 'type casting'. Length-to-number casting can be achieved in other ways like dividing-by-units (if browsers implement) or the new `progress()` function. But it's still difficult to take a number (for example) and cast it as a string that can be used for generated content.

<!-- comment id=3374904152 author=mirisuzanne created=2025-10-07T02:05:54Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3374904152 -->
## @mirisuzanne — 2025-10-07

Agenda+ to get initial thoughts on this, and see if there's interest.

<!-- comment id=3806891222 author=mirisuzanne created=2026-01-27T18:47:09Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3806891222 -->
## @mirisuzanne — 2026-01-27

This concern about user control of how things inherit and resolve has some overlap with issues like https://github.com/w3c/csswg-drafts/issues/13188 and https://github.com/w3c/csswg-drafts/issues/2749

<!-- comment id=3806897048 author=Loirooriol created=2026-01-27T18:48:23Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3806897048 -->
## @Loirooriol — 2026-01-27

@mirisuzanne You linked the same issue twice

<!-- comment id=3806947617 author=mirisuzanne created=2026-01-27T18:57:35Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3806947617 -->
## @mirisuzanne — 2026-01-27

@Loirooriol fixed, thanks

<!-- comment id=3807146514 author=css-meeting-bot created=2026-01-27T19:38:14Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3807146514 resolution=true -->
## @css-meeting-bot — 2026-01-27

The CSS Working Group just discussed `[css-values] Provide functions for resolving relative units based on type`, and agreed to the following:

* `RESOLVED:  add value/type conversion functions to css-values-5`

<details><summary>The full IRC log of that discussion</summary>
&lt;dholbert> miriam: this is sort of me seeing stuff happpening in the ecosystem and wondering if we want to address it more directly<br>
&lt;dholbert> miriam: people have often been confused with variables and inheritance, what inherits and what resolves after inheritance<br>
&lt;dholbert> miriam: not many people have been using registration until recently<br>
&lt;dholbert> so the beahvior was that you inherit an unresolved token, and it turns into a type when you apply the variable<br>
&lt;dholbert> and then you get whatever type result, and that inherits from there. but the variable itself was an abstract thing that would resolve later<br>
&lt;dholbert> once people started playing with registering properties, that changed. a calculation gets resolved down to a length, and that's what inherirts<br>
&lt;kizu> q+<br>
&lt;dholbert> whether you want resolution early or late doesn't always align with whether it makes sense to register the property for other reasons (e.g. to set an initial value)<br>
&lt;dholbert> if you give the property a type by registering it, you have to give it an initial value<br>
&lt;dholbert> then you can never use fallbacks after the property is registered<br>
&lt;oriol> q+<br>
&lt;emilio> q+<br>
&lt;dholbert> it's useful both to capture values and have the resolved value inherit<br>
&lt;dholbert> I've linked to feature requests where people have asked for ways to inherit a calculation or expression rather than a resolved value<br>
&lt;dholbert> miriam: there have been solutions posted, with how to use custom functions [...]<br>
&lt;dholbert> this is becoming more exposed to authors as we add functions and mix-ins<br>
&lt;dholbert> we have these various behaviors but not ways of tying into them.<br>
&lt;dholbert> there are also requests for type-casting, e.g. taking a number and treating it as a string for generated content. Or the reverse<br>
&lt;TabAtkins> q+<br>
&lt;dholbert> miriam: so I wonder if we need some built-in ways for working with types in CSS, so authors don't have to reach for these workarounds<br>
&lt;astearns> ack kizu<br>
&lt;dholbert> kizu: there are some workarounds for some of the types<br>
&lt;dholbert> kizu: if your'e using currentColor inside relative color syntax or color-mix or as-is, your'e not able to get the resolved final value of the color calcluation and use it in style queries or if computations<br>
&lt;dholbert> kizu: becaues it will only be available at used value time<br>
&lt;dholbert> kizu: if authors had a way to say they want to resolve it up-front, that'd be very useful<br>
&lt;dholbert> kizu: but generally, big +1 to have this built-in<br>
&lt;astearns> ack oriol<br>
&lt;emilio> I don't think we want to resolve currentColor, that'd be really weird fwiw<br>
&lt;dholbert> oriol: I agree with trying to find solutions<br>
&lt;dholbert> oriol: this isn't specific to registered property types<br>
&lt;dholbert> oriol: one of the issues that miriam linked -- what was desired there, wasn't using registered properties at all. instead it was a custom property whose value was a var() expression pointing to another variable<br>
&lt;dholbert> oriol: and then in descendants, they could change the value of the referenced variable<br>
&lt;dholbert> oriol: another example about 'em' units, not absolutizing them at computed value time<br>
&lt;dholbert> oriol: it would be good to find a solution that addresses as many of these use cases as possible<br>
&lt;astearns> ack emilio<br>
&lt;dholbert> emilio: 2 things here... one, "i want to keep the unresolved stuff" - that's different and worth dealing with separately<br>
&lt;dholbert> emilio: "i want this variable, but compute it to a length" - that seems reasonable<br>
&lt;bramus> q+<br>
&lt;dholbert> emilio: we maybe could reuse some of the type syntax that we have for attr. But "i want this attr as a length" gives you the specified length, not the computed length<br>
&lt;lea> depending on how we design this feature, we could even do things like `background: url(foo.svg) no-repeat bottom right color(revert-rule)` for the earlier issue<br>
&lt;dholbert> emilio: currentColor is its own special case because it's part of the computed color. So resolving it would be weird, since nothing resolves it, except maybe 'color' property<br>
&lt;lea> q?<br>
&lt;lea> q+<br>
&lt;dholbert> emilio: I'd propose we add a type syntax within 'var'<br>
&lt;dholbert> emilio: should have same behavior as attr(), to trigger the same computation. Parse as a length, give you the 'em' size for example<br>
&lt;TabAtkins> parse() and compute()<br>
&lt;TabAtkins> with identical args<br>
&lt;dholbert> emilio: maybe we need 'resolve(type(...))' or 'compute(type(...))'<br>
&lt;dholbert> emilio: keeping unresolved variables around is a bit weirder -- what does it mean for a computed value to have a var reference<br>
&lt;dholbert> miriam: in some ways that's what custom functions do. you have an argument, and the argument sticks around. it's sort of a variable reference that can be plugged in<br>
&lt;dholbert> TabAtkins: doesn't stick around past computed-value though. it substitutes at normal var substition time, because that's how we handle functions<br>
&lt;kizu> q+<br>
&lt;dholbert> emilio: something consistent with attr() would be nice. Nice to figure out how to handle unresolved references, but that's very weird<br>
&lt;emilio> s/computed(type(...))/computed(&lt;type>)//<br>
&lt;emilio> s/computed(type(...))/computed(&lt;type>)/<br>
&lt;dholbert> miriam: there's also a type function used in defining parameters for mixins &amp; functions... can we use that<br>
&lt;dholbert> emilio: I would assume that keeps the specified thing<br>
&lt;dholbert> TabAtkins: same grammar space, type functions are identical<br>
&lt;dholbert> TabAtkins: attr does some additional parsing<br>
&lt;dholbert> TabAtkins: as long as you're sticking to the type function, it's the same between the two<br>
&lt;astearns> ack TabAtkins<br>
&lt;dholbert> TabAtkins: generally I support this. people are already working around this. I have an example in the spec, with a hand-written link converter function<br>
&lt;dholbert> TabAtkins: silly to have folks rewriting this over and over, even if it's trivial<br>
&lt;astearns> s/link/length/<br>
&lt;dholbert> TabAtkins: I'm ok with having 2 versions as emilio says<br>
&lt;dholbert> TabAtkins: I'm ok if there's a parse-only version like attr(), but I mostly want to be sure we have the useful one<br>
&lt;dholbert> emilio: it would be weird if [...]<br>
&lt;astearns> ack bramus<br>
&lt;emilio> var(--foo type(&lt;length>)) resolved em to pixels, but attr(foo type(&lt;length>)) didn't<br>
&lt;dholbert> bramus: +1 to the use-cases. typecasting in general has come up within the community a lot<br>
&lt;dholbert> bramus: people are already registering custom properties with a certain type, and then they compute before they inherit... maybe we add a descriptor to custom properties that say "don't compute this before inheritance"?<br>
&lt;dholbert> bramus: e.g. register property as a length, give it a value 2em, and if this flag is set, you'd always inherit "2em"<br>
&lt;dholbert> bramus: so it would be recognized as a type<br>
&lt;dholbert> bramus: and would reject "blue"<br>
&lt;dholbert> emilio: that would be the attr() behavior<br>
&lt;dholbert> emilio: that's what the type() function would do<br>
&lt;miriam> This issue proposes something like @bramus is suggesting: https://github.com/w3c/csswg-drafts/issues/13188 `inherits: declaration`<br>
&lt;astearns> ack lea<br>
&lt;lea> scribe+<br>
&lt;TabAtkins> Oh I absolutely was not wanting to tie it to var(), where did this idea come from?<br>
&lt;TabAtkins> I presumed it was just emilio thinking out loud<br>
&lt;lea> lea: General support for the use cases too. Idea: rather than tying it to `var()` which limits it (e.g. now we need a new one for `inherit()`), what if we decouple it from the actual value? E.g. `as(var(--foo) type(&lt;color>))`or something. It would be a little more verbose but much more flexible. E.g. what we were discussing before would become a single declaration: `background: url(foo.svg) no-repeat bottom right as(revert-rule type(&lt;color>))`<br>
&lt;astearns> ack kizu<br>
&lt;dholbert> TabAtkins: that's a huge scope expansion, I don't want to mess around with that as part of this issue. Can discuss after<br>
&lt;dholbert> kizu: I remember use-case for computing currentColor. Imagine we have a container<br>
&lt;dholbert> kizu: we want to capture its currentColor, and then override it on some inner element, and then restore it on a deeper inner element<br>
&lt;emilio> q+<br>
&lt;dholbert> kizu: right now we can't do that.<br>
&lt;lea> would `inherit(color)` help with this?<br>
&lt;dholbert> kizu: you can't access currentColor from ancestor<br>
&lt;dholbert> kizu: if we were to implement this as a native feature that computes it, we could do this<br>
&lt;lea> q?<br>
&lt;astearns> ack emilio<br>
&lt;dholbert> emilio: there are strong reasons to postpone calculating currentColor<br>
&lt;dholbert> emilio: it would introduce a lot of dependencies between stuff<br>
&lt;dholbert> emilio: color right now doesn't introduce any dependency on anything else, because currentColor is deferred<br>
&lt;dholbert> emilio: if it wasn't, then every property that takes a color would be cyclic<br>
&lt;lea> q+<br>
&lt;dholbert> kizu: can we capture the inherited color? not the current element's color, but the inherited one<br>
&lt;dholbert> emilio: that's more feasible but orthogonal to this issue<br>
&lt;astearns> ack lea<br>
&lt;dholbert> lea: there's the inherit() function, with an intent-to-prototype in chromium maybe<br>
&lt;dholbert> lea: would inherit(color) solve this problem, once that's landed?<br>
&lt;dholbert> emilio: as-written, that would return `currentColor` keyword, since that's its own computed value<br>
&lt;dholbert> astearns: sounds like handling currentColor is a separate issue<br>
&lt;dholbert> emilio: yeah<br>
&lt;dholbert> astearns: but I'm hearing a lot of support for handling types like this.<br>
&lt;dholbert> astearns: do we need a resolution?<br>
&lt;dholbert> astearns: or should we think more in the issue?<br>
&lt;dholbert> TabAtkins: resolution should be good. We have a proposal<br>
&lt;dholbert> TabAtkins: additional features can be layered on top<br>
&lt;dholbert> TabAtkins: we have a straightforward use-case to solve, and a concrete proposal<br>
&lt;dholbert> TabAtkins: proposed resolution: add value/type conversion functions to css-values-5<br>
&lt;dholbert> fantasai: do we have a syntax for it?<br>
&lt;dholbert> TabAtkins: no strict proposed syntax; we can put something in the spec and see what people think<br>
&lt;dholbert> RESOLVED:  add value/type conversion functions to css-values-5<br>
</details>

<!-- comment id=3809417913 author=Crissov created=2026-01-28T07:14:39Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3809417913 -->
## @Crissov — 2026-01-28

Am I understanding correctly that this resolution will affect, i.e. solve, issues like #1026 (re counter values) as well? Would authors be able to convert named strings into numeric values?

<!-- comment id=3812887391 author=mirisuzanne created=2026-01-28T17:55:12Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-3812887391 -->
## @mirisuzanne — 2026-01-28

That's my understanding as well, yes. @tabatkins had more specific thoughts about re-using existing syntax, so they might be able to give a more confident answer on the details.

<!-- comment id=4195454483 author=tabatkins created=2026-04-06T22:56:35Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4195454483 -->
## @tabatkins — 2026-04-06

Yeah, the proposal in the discussion vaguely landed on `parse()` and `compute()`, for parsing strings and tokens, respectively.

I'm thinking:

* `parse()` would take a string and an [`<attr-type>`](https://drafts.csswg.org/css-values-5/#typedef-attr-type), and parse it like attr(). Probably take a fallback too, in case parsing fails.
* `compute()` would take a [`<css-type>`](https://drafts.csswg.org/css-mixins/#typedef-css-type) and some tokens, and parse it like a custom property registration. Also taking a fallback, in case parsing fails.

That is, `parse(<length> "5em")` would act identically to `attr(foo <length>)` (with `foo="5em"`), and `compute(<length> 5em)` would act identically to `--as-length(5em)` (with `@function --as-length(--x <length>){ result: var(--x); }`)

So `parse(number counter(...))` would indeed convert a counter value into an integer.

<!-- comment id=4398761059 author=JaneOri created=2026-05-07T16:00:41Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4398761059 -->
## @JaneOri — 2026-05-07

Could we please allow the following to work in spec?

```css
--v12: "12";
--v0: "0";
--px: "px";
--deg: "deg";
--dim: var(--px);

--len120px: parse(<dimension> var(--v12) var(--v0) var(--dim));
// Keyword `auto` instead of <dimension> would also be great 
 --num12: parse(<number> var(--v0) var(--v12));
 --num12andhalf: parse(<number> var(--v0) var(--v12) "." counter(5));
```

Edit:
It might even be a great avenue into supporting conversion of binary and hex strings into numbers 

<!-- comment id=4399340448 author=mirisuzanne created=2026-05-07T17:13:11Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4399340448 -->
## @mirisuzanne — 2026-05-07

@JaneOri Can you explain what this code is intended to do? You're trying to concatenate stings and then parse the result as a number or length?

<!-- comment id=4399395974 author=JaneOri created=2026-05-07T17:19:15Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4399395974 -->
## @JaneOri — 2026-05-07

Yes, sorry, I'd like to parse the concatenated string rather than only a single token.
`<string>+ computed to <string> then parsed to <x>`

<!-- comment id=4401317646 author=tabatkins created=2026-05-07T21:29:21Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4401317646 -->
## @tabatkins — 2026-05-07

`parse(<dimension> string(var(--v12) var(--v0) var(--dim)))` should definitely work, yes. I don't want to spread implicit concatenation any further than it needs to be, but you can certainly concat strings with `string()`.

<!-- comment id=4401971815 author=JaneOri created=2026-05-07T23:46:44Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4401971815 -->
## @JaneOri — 2026-05-07

Awesome! Where can I find information about `string()`? that one's new to me but it sounds exactly what I was hoping for

And we're on board with adding an author-exposed `<dimension>` casting?

The dimension type doesn't currently work in @ property `syntax` nor in @ function typing.

I am fairly sure I've seen specs defining a specific subset of types that _are_ allowed for those two scenarios (and that it also included dimensions not yet implemented for syntax like `<frequency>`) so I imagine these would all be on the same page for what types we can use?

<!-- comment id=4408067399 author=Crissov created=2026-05-08T16:34:53Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4408067399 -->
## @Crissov — 2026-05-08

[`string()`](https://drafts.csswg.org/css-content-3/#string-function) does exist, but it does not do concatenation (yet), as settled on in #542. 

<!-- comment id=4557155888 author=kbabbitt created=2026-05-27T17:53:46Z url=https://github.com/w3c/csswg-drafts/issues/12488#issuecomment-4557155888 -->
## @kbabbitt — 2026-05-27

I ran into another use case for this while looking further into text animations. I wanted to do something like:

```css
.paragraph {
  counter-reset: word-counter;
}
@starting-style {
  .word {
    opacity: 0;
  }
}
.word {
  counter-increment: word-counter;
  opacity: 1;
  transition: opacity 0.6s;
  transition-delay: calc(0.1s * parse(<number> counter(word-counter)));
}
```
