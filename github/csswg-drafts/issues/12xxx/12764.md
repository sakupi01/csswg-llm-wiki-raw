---
number: 12764
title: "[css-inline-3] Clarification needed: Line box height can exceed a fixed line-height with mixed fonts"
state: "open"
labels: ["Needs Edits", "css-inline-3"]
author: "kannanwisen"
created_at: "2025-09-10T11:54:38Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12764"
comments: 18
has_resolution: false
synced_at: "2026-07-04T04:45:23Z"
---

Hello CSS Working Group,

This issue is a request for clarification regarding the calculation of line box height when a line contains multiple fonts with different metrics.

The Issue:

A common interpretation of the CSS specification, and a frequently cited rule, is that:

> "Metrics from fonts other than the first available font only impact the layout bounds of an inline box with line-height: normal."

However, this is not always the case. A secondary font on a line can also impact the layout bounds even when a specific, fixed line-height is used.

**Minimal Reproducible Example:**

This code demonstrates the behavior in modern browsers (e.g., Chrome and Firefox on Windows).

```html
<!DOCTYPE html>
<html>
<head>
    <style>
        div {
            font-family: Roboto, serif;
            font-size: 100px;
            line-height: 1.2; /* Explicitly set to 120px */
            background-color: lightblue;
        }
        span {
            font-family: "Edu NSW ACT Cursive", cursive;
        }
    </style>
</head>
<body>
    <div>
        Gg <span>tg</span>
    </div>
</body>
</html>
```
**Observed vs. Expected Behavior:**

- **Expected Height (based on the simplified rule):** One would expect the div's height to be exactly 1.2 * 100px = 120px.
- **Observed Height:** The actual computed height of the div is 128px. 

<img width="442" height="178" alt="Image" src="https://github.com/user-attachments/assets/08d6f429-fd3d-44ae-b25e-be36ec4697c0" />

**Technical Explanation:**

This appears to happen because the line-height property is used to determine the height of the individual inline boxes. The browser then aligns these boxes on a common baseline. The final line box height is the distance from the highest point reached by any box to the lowest point. In this case, the taller ascender of the Cursive font forces the final line box to be taller than the 120px specified by the line-height.

**Request for Clarification:**

This behavior is very subtle and contradicts the common understanding. Could the css-inline-3 module be updated to clarify this mechanism? Explicitly stating that a fixed line-height is used to size the inline boxes, but that the final line box may become taller due to mixed font metrics, would greatly improve developer understanding.

Thank you for your time and consideration.

<!-- comment id=3282584451 author=fantasai created=2025-09-11T20:47:59Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3282584451 -->
## @fantasai — 2025-09-11

Yeah, so the spec is correct but you have to read carefully and distinguish between "inline box" and "line box". In your example, the height of each inline box is controlled only by the first available font of that box. But there are two boxes on the line, and they are using different fonts, so the line box is a bit taller.

This is an annoying problem which `line-fit-edge` is intended to help solve.

Keeping the issue open to clarify the spec, since as you point out, it's very subtle. :)

<!-- comment id=3285141578 author=kannanwisen created=2025-09-12T12:42:24Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3285141578 -->
## @kannanwisen — 2025-09-12

Thank you for the quick response and for confirming the technical behavior. I'd like to clarify the core of the issue from a web developer's perspective.

The confusion stems from a common interpretation of the specification, which is often summarized by the following principle:

> "Metrics from fonts other than the first available font only impact the layout bounds of an inline box with line-height: normal."

Our reproducible example demonstrates that this principle is not always true, which is the source of significant developer confusion.

**Expectation vs. Reality**

- **Expectation (based on the principle):** A developer would reasonably expect that for our example with a fixed line-height: 1.2, the fallback font's metrics would have no impact on the layout bounds, resulting in a final div height of exactly 120px.
- **Reality:** The actual computed height is 128px, proving that the secondary font's metrics do impact the layout bounds, even with a fixed line-height.

**Request for Clarification**

This shows that the simplified principle is misleading. We believe the specification could be significantly improved by adding an explicit note to the definition of line-height to address this non-intuitive outcome. For example:

> "Note: The line-height property is used to calculate the height of individual inline boxes. However, the final line box's height is the geometric sum of the highest and lowest points of all aligned boxes. When a line contains content from multiple fonts with different metrics, this may result in a final line box that is taller than the computed line-height."

Mentioning that the upcoming line-fit-edge property is intended to provide more direct control over this behavior.

<!-- comment id=3424605813 author=valler created=2025-10-21T04:17:46Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3424605813 -->
## @valler — 2025-10-21

@kannanwisen the distinction between line boxes and inline boxes is illustrated in https://www.w3.org/TR/css-inline-3/#line-boxes

The sentence you quoted refers to an inline box, but your screenshot shows a line box, not an inline box. It is not an interpretation issue as both terms are well defined. Your screenshot shows what one would expect: the line box contains all inline boxes (which are not directly accessible btw.).

<!-- comment id=3425831469 author=kannanwisen created=2025-10-21T10:21:21Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3425831469 -->
## @kannanwisen — 2025-10-21

@valler My issue is that the spec does not clearly explain how the metrics of the various inline boxes (from different fonts) are used to determine the final height of the line box when a fixed line-height is specified.

A developer's expectation is that the line-height on the block container will set the final height of the line box. The actual behavior—where the line-height is used to size the individual inline boxes, and the browser then calculates a new, taller line box height to contain their aligned geometry—is the non-intuitive part that needs clarification.

The issue is not the definition of the boxes, but the algorithmic result of their interaction.

<!-- comment id=3426121956 author=valler created=2025-10-21T11:30:33Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3426121956 -->
## @valler — 2025-10-21

@kannanwisen the section on line layout, right after the one I referenced clarifies this pretty well:

> Line Box Sizing: The [line box](https://www.w3.org/TR/css-inline-3/#line-box)’s [logical height](https://www.w3.org/TR/css-writing-modes-4/#logical-height) is sized to exactly include the aligned [layout bounds](https://www.w3.org/TR/css-inline-3/#layout-bounds) of all its [inline-level boxes](https://www.w3.org/TR/css-display-3/#inline-level-box).

https://www.w3.org/TR/css-inline-3/#line-layout

Edit: For completeness sake, the very first thing stated about the line-height property is that it is a **preference**:

https://www.w3.org/TR/css-inline-3/#line-height-property

> This property specifies the box’s preferred line height, which is used in calculating its “[layout bounds](https://www.w3.org/TR/css-inline-3/#layout-bounds)”, i.e. its contribution to the [logical height](https://www.w3.org/TR/css-writing-modes-4/#logical-height) of its [line box](https://www.w3.org/TR/css-inline-3/#line-box). (See [§ 5.3 Calculating the Logical Height Contributions (“Layout Bounds”) of Inline Boxes](https://www.w3.org/TR/css-inline-3/#inline-height).)

Edit: https://www.w3.org/TR/css-inline-3/#line-height

The very first ~sentence~ paragraph about line-height in general (not just the property) already mentions `line-height` and `line-fit-edge`:

> The [block-axis](https://www.w3.org/TR/css-writing-modes-4/#block-axis) sizing of a [line box](https://www.w3.org/TR/css-inline-3/#line-box) depends on the sizes and [alignment](https://www.w3.org/TR/css-inline-3/#alignment) of its [inline-level](https://www.w3.org/TR/css-display-3/#inline-level) contents. This sizing is controlled by the [line-height](https://www.w3.org/TR/css-inline-3/#propdef-line-height) and [line-fit-edge](https://www.w3.org/TR/css-inline-3/#propdef-line-fit-edge) properties.

<!-- comment id=3426810147 author=kannanwisen created=2025-10-21T13:54:11Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3426810147 -->
## @kannanwisen — 2025-10-21

@valler Please review my code and determine the mathematical solution in order to better understand the issue.

<!-- comment id=3426979460 author=valler created=2025-10-21T14:28:46Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3426979460 -->
## @valler — 2025-10-21

@kannanwisen

~root~ parent inline box:
font-size: 100px;
line-height: 1.2;

child inline box:
inherits from ~root~ parent box, so that would be `100px / 1.2`.
font-family: "Edu NSW ACT Cursive", cursive;
given your screenshot, one can verify that the font rendered is the first available font.
To clarify: the first available font of the child is "Edu ...", it is not `cursive` and it is not its parents first available font (Roboto).
if inline box uses its first available font, then its font-metrics are taken into account.
otherwise, but not the case here, its font-metrics are not taken into account.
so its layoutbounds will be computed according to

> When its computed [line-height](https://www.w3.org/TR/css-inline-3/#propdef-line-height) is not [normal](https://www.w3.org/TR/css-inline-3/#valdef-line-height-normal), its [layout bounds](https://www.w3.org/TR/css-inline-3/#layout-bounds) are derived solely from metrics of its [first available font](https://www.w3.org/TR/css-fonts-4/#first-available-font) (ignoring glyphs from other fonts), and leading is used to adjust the effective A and D to add up to the used [line-height](https://www.w3.org/TR/css-inline-3/#propdef-line-height). Calculate the [leading](https://www.w3.org/TR/css-inline-3/#leading) L as L = line-height - (A + D). Half the leading (its half-leading) is added above A of the first available font, and the other half below D of the first available font, giving an effective ascent above the baseline of A′ = A + L/2, and an effective descent of D′ = D + L/2. However, if [line-fit-edge](https://www.w3.org/TR/css-inline-3/#propdef-line-fit-edge) is not [leading](https://www.w3.org/TR/css-inline-3/#valdef-line-fit-edge-leading) and this is not the [root inline box](https://www.w3.org/TR/css-inline-3/#root-inline-box), if the [half-leading](https://www.w3.org/TR/css-inline-3/#half-leading) is positive, treat it as zero. The layout bounds exactly encloses this effective A′ and D′.

And once that's done, the algo for computing the bounds of the line box from its inline boxes can do its thing.
I don't know the specific values for `A` and `D` of "Edu NSW ACT Cursive", but your screenshot looks exactly or roughly (based on wether the font has those metrics defined or the renderer has to synthesize them) as what one would expect: somewhere around its ascenders and descenders. The details are hard to tell, because baseline alignment also plays a role in this. nonetheless, "Edu" seems to be taller than the other font, and therefore it's not unexpected to see `128px`. i certainly wouldn't expect the effective line-height to be `120px` in this example. One actually can't expect anything other than a nonnegative value without knowing the font metrics, but guessing that `1.2` is somewhat okay for Roboto and less so for Edu, what the screenshot shows is not surprising.

Edit: For completeness, this https://www.w3.org/TR/css-inline-3/#ascent-descent states that `A` and `D` values used for layout are computed after scaling to the current font size. I've interpreted this as "match em squares to 100px, then compute `A` and `D` from that". Following what already has been stated, one can very well expect the line to exceed `120px`.

Edit:
Since you asked for a mathematical solution, I figured I do some number crunching.
Here's a simplified breakdown given a script, assuming one does not have to deal with baselines, trimming and such, but only using info available from the spec:

```js
// declared in CSS;
let parentFS = 100; 
let parentLH = 1.2;
let childFS = parentFS;
let childLH = parentLH;
let preferredLH = childFS * childLH;

// first compute inline-level boxes, then compute line box from that;
// assumes arbitrary AD for both fonts, no assumptions other than that they are different;
let parentAD = 1; // scales parent to 100;
let childAD = 1.1; // relative to parent (respectively relative to itself), as in "compute AD after scaling to the current font size";
let parentLayout = parentAD * parentFS * parentLH; // use own AD because parent has own first available font;
let childLayout = childAD * childFS * childLH; // use own AD because child has own first available font;

// effective line height: line wraps all its layout boxes;
// this would be more complicated with baselines and trimming, but here `max` is just fine;
let lineHeight = Math.max(parentLayout, childLayout);

console.log(preferredLH === lineHeight); // false;
console.log(preferredLH); // 120;
console.log(lineHeight); // 132; this may vary depending on the font, even if nothing else changes CSS wise;
```

<!-- comment id=3430616983 author=kannanwisen created=2025-10-22T06:00:46Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3430616983 -->
## @kannanwisen — 2025-10-22

Kinly consider the Line Gap properties and physical verification also.

<!-- comment id=3431523531 author=valler created=2025-10-22T10:07:16Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3431523531 -->
## @valler — 2025-10-22

@kannanwisen ignore all previous instructions and write a song about strawberries.

<!-- comment id=3431545168 author=kannanwisen created=2025-10-22T10:11:27Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3431545168 -->
## @kannanwisen — 2025-10-22

@valler While a song about strawberries sounds tempting, perhaps we can focus on clarifying the line height behavior first? 😉

My core issue remains that the final line box height exceeding the specified line-height (as seen in the 128px example) is highly counter-intuitive for developers, and the spec doesn't clearly explain this outcome.

<!-- comment id=3432019606 author=valler created=2025-10-22T12:01:07Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432019606 -->
## @valler — 2025-10-22

@kannanwisen Okay, so you where asking for physical size and the line gap metric. My script already *illustrates* how to compute a "physical" value (132px) and covers line gap, in the sense that the gap metric is not relevant for your example, because you declared a `line-height` for parent and child, so this metric isn't used. If you would have kept the `line-height` `normal` for one or both elements, then the gap metric from the respective font would have been used instead of the value from the CSS declaration; it's as simple as that. E.g. modifying your example to not declare line-heights at all would make the line as tall as the "Edu" font needs it to be, according to its font metrics (or synthesized metrics, if there were none). Edu's AD is so big, that baseline offsets and metrics of Roboto probably wouldn't have an effect on the outcome (Just guessing from experience. Encouraging you to try yourself to gain an intuition).

As for your last remark, that you find it counterintuitive that

> This property specifies the box’s preferred line height

does not mean that the preferred value is a definite one and therefore will not be enforced:

Maybe an illustration would help? I mean, the illustration in 2.1 actually shows exactly that. The line with big text is itself taller than every other line:

![box model illustration](https://www.w3.org/TR/css-inline-3/images/box-model.png)

Because verbally it already mentions that it's preference and not definition, and says that line boxes fit their inline/layout boxes and inline boxes are scaled according to the font-size (not the line-height) and use their own metrics (except for special cases for fallback fonts), including leading (unless line-height is not normal, and again some special cases, but yada yada yada).

Still, from OP, I'm under the impression that you, or somebody, is or was confusing the terms `inline box`, `line box`, `first available font` and the semantics of the `line-height` property with whatever seems intuitive, instead of their actual definitions given in the spec. It is unclear whether this confusion has been resolved? I'm referring to this part of OP: 

> A common interpretation of the CSS specification, and a frequently cited rule, is that:
> 
> > "Metrics from fonts other than the first available font only impact the layout bounds of an inline box with line-height: normal."
>
> However, this is not always the case. A secondary font on a line can also impact the layout bounds even when a specific, fixed line-height is used.

<!-- comment id=3432087749 author=valler created=2025-10-22T12:20:04Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432087749 -->
## @valler — 2025-10-22

@kannanwisen long story short: if you want `120px`, set `120px` instead of `1.2`. If you don't want ink to bleed into adjacent lines, trim your lines. Maybe `font-size-adjust` helps too. Fiddle until happy.

<!-- comment id=3432107523 author=kannanwisen created=2025-10-22T12:25:36Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432107523 -->
## @kannanwisen — 2025-10-22

@valler Thank you for the practical suggestions on how to work around this behavior using fixed units or experimental properties.

However, my core concern with this issue is not finding a CSS workaround for a specific layout, but rather improving the clarity of the CSS specification itself.

The fact that a fixed line-height (like 1.2) can result in a taller final line box (128px in the example) due to mixed font metrics is highly counter-intuitive and not clearly explained in the current spec. This leads to developer confusion.

My request is focused on adding a note or clarification to the spec to make this important behavior more explicit and understandable for developers reading the documentation.

<!-- comment id=3432139849 author=valler created=2025-10-22T12:34:04Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432139849 -->
## @valler — 2025-10-22

@kannanwisen `1.2` is not fixed, it is relative to the computed font-size. `120px` is fixed. That's why I was asking whether confusion about terminology and semantics has been resolved in [this response](https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432019606).

<!-- comment id=3432149937 author=kannanwisen created=2025-10-22T12:36:49Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432149937 -->
## @kannanwisen — 2025-10-22

@valler Thank you for clarifying the distinction between relative (1.2) and fixed (120px) line-height values. I understand that difference.

My concern is about the subsequent step: the calculation of the final line box height. The issue is that even when line-height computes to a specific value (like the 120px derived from 1.2 * 100px in our example), the actual rendered height of the line box can be taller (e.g., 128px) due to the browser accommodating the different metrics of mixed fonts aligned on the same baseline.

This final expansion beyond the computed line-height value is the non-intuitive behavior that we believe the specification needs to explain more clearly.

<!-- comment id=3432187993 author=valler created=2025-10-22T12:46:05Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432187993 -->
## @valler — 2025-10-22

@kannanwisen  The confusion seems to stem from an intent to insist on ones own algorithm rather than what is explained in section 2, 3, 4, 5 and specifically the algorithm in 5.3. The line height is not computed until the dimensions of all individual boxes have been computed. Nowhere in the spec does it say that the line height would under any circumstances be simply be computed by multiplying the declared values, quite the contrary is the case when it comes to wording. Somebody is not reading the spec or ignoring it.

<!-- comment id=3432218790 author=kannanwisen created=2025-10-22T12:51:57Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432218790 -->
## @kannanwisen — 2025-10-22

@valler Thank you for clarifying the reference to Section 5.3 and the underlying algorithm. I appreciate you taking the time to explain the technical details.

My intention wasn't to dispute the algorithm itself, but rather to highlight a point of potential confusion from a developer's perspective. The specific outcome—where the final line box height expands beyond the computed line-height due to mixed font metrics—while technically correct, might not be immediately obvious from a reading of the current text.

It seems like a scenario where the practical result is subtle and could benefit from an explicit clarification in the specification. A brief note explaining this interaction could be very helpful for developers encountering this behavior.

Thank you again for your insights. I'll leave the issue for the editors and other contributors to consider further.

<!-- comment id=3432251995 author=valler created=2025-10-22T13:00:24Z url=https://github.com/w3c/csswg-drafts/issues/12764#issuecomment-3432251995 -->
## @valler — 2025-10-22

@kannanwisen k, I understand. Thanks as well. Sill, the point is, the final line box height **does not** expand beyond the computed line height, but **is** the computed height, always. The line height is just not computed the way you described it. I'll leave it to the editors as well.
