---
number: 12609
title: "[css-grid-3][Masonry] fit-content track size"
state: "closed"
labels: ["css-grid-3", "topic: masonry"]
author: "alisonmaher"
created_at: "2025-08-14T22:10:44Z"
closed_at: "2025-08-14T23:05:55Z"
url: "https://github.com/w3c/csswg-drafts/issues/12609"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:18Z"
---

In Grid, we don't currently support a `fit-content` track size, only [`fit-content()`](https://www.w3.org/TR/css-grid-2/#funcdef-grid-template-columns-fit-content). I couldn't find why in the spec - is this because of the special `minmax()` definition/restriction in grid? If so, depending on how https://github.com/w3c/csswg-drafts/issues/12573 gets resolved, should we consider adding support for `fit-content`, as well as [auto repeats](https://github.com/w3c/csswg-drafts/issues/10915) that take `fit-content`?

<!-- comment id=3190084143 author=Loirooriol created=2025-08-14T22:51:02Z url=https://github.com/w3c/csswg-drafts/issues/12609#issuecomment-3190084143 -->
## @Loirooriol — 2025-08-14

My guess is:
 - A `min-content` track size uses the min-content contribution of items
 - A `max-content` track size uses the max-content contribution of items
 - So to be consistent, a `fit-content` track size would use the fit-content contribution of items, but there is no such thing.

What is your idea, treating a `fit-content` track size as `min-content` when sizing the grid under a min-content constraint, treat it as `max-content` when sizing under a max-content constraint, and otherwise use the shrink-to-fit formula like `clamp(min-content contribution, available grid space, max-content contribution)`?

That's the only reasonable thing I can think of, but not sure how useful it would be to use the available grid space when there are multiple tracks. Of course we can't use the grid area size, since that's the thing that is being calculated.

Also note that adding a `fit-content` track size that relates to the `fit-content` box size would probably make people think that the `fit-content()` track size relates to the `fit-content()` box size, but that's not really the case (probably it wasn't a good idea to use the same name).

<!-- comment id=3190125462 author=alisonmaher created=2025-08-14T23:05:48Z url=https://github.com/w3c/csswg-drafts/issues/12609#issuecomment-3190125462 -->
## @alisonmaher — 2025-08-14

Thanks - yeah, I found it surprising that we supported one and not both, but based on your explanation, that makes more sense now. I'll close this issue out since there likely isn't a good use case for adding it
