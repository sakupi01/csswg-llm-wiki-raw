---
number: 12151
title: "[css-values] Spec does not reject duplicate `<request-url-modifier>`s, but WPT does"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "topic: urls"]
author: "AtkinsSJ"
created_at: "2025-05-02T15:47:00Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12151"
comments: 21
has_resolution: false
synced_at: "2026-07-04T04:45:16Z"
---

[This test](https://wpt.live/css/css-values/urls/url-request-modifiers-invalid.sub.html) requires that a `url()` with more than one of the same `<request-url-modifier>` be treated as invalid, but I can't find anything in the spec that says this is invalid. ~~The 3 major engines all pass these tests, so evidently that's the desired behaviour, but it'd be good to specify it.~~ _Correction: The 3 major engines pass that test because they don't support url-modifiers *at all*. So it's not established whether the test behaviour is what's wanted._ 😅

The [`<url>`](https://drafts.csswg.org/css-values-4/#url-value) and [`<url-modifier>`](https://drafts.csswg.org/css-values-4/#url-modifiers) definitions in CSS Values 4 do not place any requirements on repeated modifiers.

Likewise, the definitions in CSS Values 5 of [`<request-url-modifier>`s](https://drafts.csswg.org/css-values-5/#request-url-modifiers) do not say anything about more than one of a given type appearing in a `url()`.

cc: @weinig as you wrote that particular test.

<!-- comment id=2847589806 author=AtkinsSJ created=2025-05-02T16:05:21Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2847589806 -->
## @AtkinsSJ — 2025-05-02

The tests also expect the modifiers to be sorted by name alphabetically, which needs mentioning somewhere in "serialize a URL", see #12057.

<!-- comment id=2878235884 author=weinig created=2025-05-14T00:10:58Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2878235884 -->
## @weinig — 2025-05-14

> [This test](https://wpt.live/css/css-values/urls/url-request-modifiers-invalid.sub.html) requires that a url() with more than one of the same <request-url-modifier> be treated as invalid, but I can't find anything in the spec that says this is invalid

You are right, the grammar currently allows duplicate modifiers due to the use of `<url-modifier>*` `url( <string> <url-modifier>* )`

I don't think duplicate modifiers make sense, so I would propose we change grammar 

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers> ) | <url-token>
<src()> = src( <string> <url-modifiers> )

<url-modifiers> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
<crossorigin-modifier> = crossorigin(anonymous | use-credentials)
<integrity-modifier> = integrity(<string>)
<referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
```

With this construction, the serialization would fall out of the normal serialize in grammar order assumptions.

@noamr @tabatkins, any thoughts on that change?

<!-- comment id=2879082913 author=noamr created=2025-05-14T07:44:00Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2879082913 -->
## @noamr — 2025-05-14

> > [This test](https://wpt.live/css/css-values/urls/url-request-modifiers-invalid.sub.html) requires that a url() with more than one of the same  be treated as invalid, but I can't find anything in the spec that says this is invalid
> 
> You are right, the grammar currently allows duplicate modifiers due to the use of `<url-modifier>*` `url( <string> <url-modifier>* )`
> 
> I don't think duplicate modifiers make sense, so I would propose we change grammar
> 
> ```
> <url> = <url()> | <src()>
> 
> <url()> = url( <string> <url-modifiers> ) | <url-token>
> <src()> = src( <string> <url-modifiers> )
> 
> <url-modifiers> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
> <crossorigin-modifier> = crossorigin(anonymous | use-credentials)
> <integrity-modifier> = integrity(<string>)
> <referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
> ```
> 
> With this construction, the serialization would fall out of the normal serialize in grammar order assumptions.
> 
> [@noamr](https://github.com/noamr) [@tabatkins](https://github.com/tabatkins), any thoughts on that change?

Works for me!

<!-- comment id=2879173986 author=cdoublev created=2025-05-14T08:00:15Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2879173986 -->
## @cdoublev — 2025-05-14

Please take this [resolution](https://github.com/w3c/csswg-drafts/issues/1603#issuecomment-467221081) into consideration to change the value definition:

  > Unknown `<url-modifier>`s get silently ignored (rather than invalidating the function).

<!-- comment id=2879226644 author=noamr created=2025-05-14T08:13:24Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2879226644 -->
## @noamr — 2025-05-14

@weinig do you want to take a crack at a spec PR? Otherwise I need to get a few things off my backlog before I can attend to doing this myself.

<!-- comment id=2880790247 author=weinig created=2025-05-14T16:06:17Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2880790247 -->
## @weinig — 2025-05-14

@noamr, happy to. 

I realize there is one little issue with what I proposed, it should actually have a `?` in url() and src() and should be:

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )

<url-modifiers> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
<crossorigin-modifier> = crossorigin(anonymous | use-credentials)
<integrity-modifier> = integrity(<string>)
<referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
```

The only question I have is which spec to make the changes in since this would change the CSS Value 4 definition of <url> as well as the CSS Value 5 definitions of the modifiers.

I think probably the least invasive change would be to update both CSS Value 4 to:

 ```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )
```

and leave <url-modifiers> without any definitions.

And then update CSS Value 5 with the new <url-modifiers> changes.

Does that sound good?

<!-- comment id=2880832119 author=noamr created=2025-05-14T16:21:43Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2880832119 -->
## @noamr — 2025-05-14

SGTM, also you can update both specs using the same PR. 
Let's open this for an async resolution first?

<!-- comment id=2880942913 author=weinig created=2025-05-14T17:03:23Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2880942913 -->
## @weinig — 2025-05-14

Ok, what needs to be done to open for async resolution?

<!-- comment id=2882053152 author=cdoublev created=2025-05-15T02:45:34Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2882053152 -->
## @cdoublev — 2025-05-15

  > Please take this [resolution](https://github.com/w3c/csswg-drafts/issues/1603#issuecomment-467221081) into consideration to change the value definition:
  > 
  > > Unknown `<url-modifier>`s get silently ignored (rather than invalidating the function).

```
<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )

<url-modifiers> = <request-url-modifier> || [ <ident> | <function-token> <any-value>? ]+
<request-url-modifier> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
```

<!-- comment id=2892019924 author=astearns created=2025-05-19T19:09:39Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2892019924 -->
## @astearns — 2025-05-19

@weinig could you summarize the intended resolution?

<!-- comment id=2932205741 author=astearns created=2025-06-02T19:43:39Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2932205741 -->
## @astearns — 2025-06-02

As I’m not sure what to propose here, please re-add a tag when things are ready to discuss on a call or there is a proposed async resolution.

<!-- comment id=2963404664 author=weinig created=2025-06-11T16:09:04Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2963404664 -->
## @weinig — 2025-06-11

Intended resolution:

Update grammar for `<url>` to disallow duplicate `<request-url-modifier>` productions by changing the grammar from:

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifier>* ) | <url-token>
<src()> = src( <string> <url-modifier>* )
```

to

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )
```

and adding the definition of `<url-modifiers>` as:

 ```
<url-modifiers> = <request-url-modifier> || [ <ident> | <function-token> <any-value>? ]+
<request-url-modifier> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
<crossorigin-modifier> = crossorigin(anonymous | use-credentials)
<integrity-modifier> = integrity(<string>)
<referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
```

<!-- comment id=2998309703 author=astearns created=2025-06-23T23:49:58Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2998309703 -->
## @astearns — 2025-06-23

The CSSWG will automatically accept this resolution one week from now if no objections are raised here. Anyone can add an emoji to this comment to express support. If you do not support this resolution, please add a new comment.

Proposed Resolution: Update grammar for `<url>` to disallow duplicate `<request-url-modifier>` productions

<!-- comment id=2998903057 author=cdoublev created=2025-06-24T05:47:33Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-2998903057 -->
## @cdoublev — 2025-06-24

Sorry I realize this late but `url(img.avif foo crossorigin(anonymous) bar())` would be entirely invalid with the proposed syntax.

So repeating unkown modifiers should be allowed for future additions to known modifiers, but known modifiers should (presumably) not be allowed to repeat... 

Hard-coding both requirements could be translated with `[ <ident> | <function-token> <any-value>? ) ]+ || <request-url-modifier> || [ <ident> | <function-token> <any-value>? ) ]+`. Obviously, if other CSS types of known modifiers should be added, this would become unreadable.

Now with some hindsight...

  > I don't think duplicate modifiers make sense

Many CSS values can repeat in a stylesheet, with the last occurrence taking precedence. The only other place I can think of where repeating a value is forbidden (in prose) is `@function` parameter list. If url modifiers cannot repeat, this seems simpler to specify in prose, imo.

I do not see how serializing modifiers in alphabetic order is useful but I have no strong opinion on whether it should not be enforced.

<!-- comment id=3005342031 author=weinig created=2025-06-25T16:08:07Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3005342031 -->
## @weinig — 2025-06-25

> Many CSS values can repeat in a stylesheet, with the last occurrence taking precedence. The only other place I can think of where repeating a value is forbidden (in prose) is @function parameter list. If url modifiers cannot repeat, this seems simpler to specify in prose, imo.

Interesting, but I don't really see it that way. I would argue just the opposite, as for most values, a part of a CSS value can't be repeated. You can't say `color(srgb srgb 1 0 1)`. That's what the grammars enforce.

I do see the issue you bring up with ordering however and I think your new grammar is good. In general, I think as little (ideally nothing) as possible should be in prose, as that makes implementations and testing more challenging.

--

Updated resolution:

Update grammar for `<url>` to disallow duplicate `<request-url-modifier>` productions by changing the grammar from:

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifier>* ) | <url-token>
<src()> = src( <string> <url-modifier>* )
```

to

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )
```

and adding the definition of `<url-modifiers>` as:

 ```
<unknown-modifiers> = [ <ident> | <function-token> <any-value>? ) ]+
<url-modifiers> = <unknown-modifiers> || <request-url-modifier> || <unknown-modifiers>
<request-url-modifier> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
<crossorigin-modifier> = crossorigin(anonymous | use-credentials)
<integrity-modifier> = integrity(<string>)
<referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
```

Additionally, some prose is needed to state:

"`<unknown-modifiers>` matches anything except values matched by `<request-url-modifier>`".

<!-- comment id=3007066310 author=cdoublev created=2025-06-26T05:05:51Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3007066310 -->
## @cdoublev — 2025-06-26

  > I would argue just the opposite, as for most values, a part of a CSS value can't be repeated. You can't say `color(srgb srgb 1 0 1)`. That's what the grammars enforce.

Sorry, my point was not very clear but it was about values that the grammar allows to repeat, representing resources, instructions, function arguments, etc... where the last one wins because it naturally overrides the previous ones, like `@property` or `@counter-style` for rules, obviously property/descriptor declarations, but also [`counter-*`](https://drafts.csswg.org/css-lists-3/#propdef-counter-reset) value with the same `<counter-name>`, [`link-parameters`](https://drafts.csswg.org/css-link-params-1/#propdef-link-parameters) value with the same `<link-param>`, [`param()`](https://drafts.csswg.org/css-link-params-1/#svg-parameter-fragment-identifier) arguments with the same `<custom-property-name>`, etc.

<!-- comment id=3007472112 author=AtkinsSJ created=2025-06-26T07:27:05Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3007472112 -->
## @AtkinsSJ — 2025-06-26

I can see repetition (with later modifiers overriding earlier ones) being useful sometimes. For example, having a custom property with some default modifiers, and then being able to modify just the one you want:

```css
:root {
  --default-modifiers: foo(1) bar(2) baz(3);
}

.thing {
  background-image: src("image.png" var(--default-modifiers) bar(500));
}
```

I don't know enough about CSP to know if this would be useful in practice, but at least conceptually it seems useful to avoid repetition.

<!-- comment id=3009005578 author=weinig created=2025-06-26T16:06:16Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3009005578 -->
## @weinig — 2025-06-26

Given there is enough discussion here, I want to simplify the prosed resolution to just the content for this issue. So, here it is again, but without the unknown modifiers bit.

I'm not convinced allowing duplicate modifiers makes sense and that these modifiers fall into the same class as the ones @cdoublev mentions "representing resources, instructions, function arguments, etc". 

--

Updated resolution:

Update grammar for `<url>` to disallow duplicate `<request-url-modifier>` productions by changing the grammar from:

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifier>* ) | <url-token>
<src()> = src( <string> <url-modifier>* )
```

to

```
<url> = <url()> | <src()>

<url()> = url( <string> <url-modifiers>? ) | <url-token>
<src()> = src( <string> <url-modifiers>? )
```

and adding the definition of `<url-modifiers>` as:

 ```
<url-modifiers> = <request-url-modifier>
<request-url-modifier> = <crossorigin-modifier> || <integrity-modifier> || <referrerpolicy-modifier>
<crossorigin-modifier> = crossorigin(anonymous | use-credentials)
<integrity-modifier> = integrity(<string>)
<referrerpolicy-modifier> = referrerpolicy(no-referrer | no-referrer-when-downgrade | same-origin | origin | strict-origin | origin-when-cross-origin | strict-origin-when-cross-origin | unsafe-url)
```

<!-- comment id=3011323933 author=cdoublev created=2025-06-27T03:26:04Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3011323933 -->
## @cdoublev — 2025-06-27

Fwiw, I have no objection to this resolution, nor a strong opinion on whether a url modifier can repeat. I guess how to allow matching unknown modifiers will be figured out in #1603 but in the meantime, can you please clarify if you plan to remove this in [CSS Values 4](https://drafts.csswg.org/css-values-4/#url-modifiers):

  > A `<url-modifier>` is either an `<ident>` or a [functional notation](https://drafts.csswg.org/css-values-4/#functional-notation).

<!-- comment id=3013392345 author=weinig created=2025-06-27T15:05:20Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3013392345 -->
## @weinig — 2025-06-27

> in the meantime, can you please clarify if you plan to remove this in [CSS Values 4](https://drafts.csswg.org/css-values-4/#url-modifiers):
> 
> > A `<url-modifier>` is either an `<ident>` or a [functional notation](https://drafts.csswg.org/css-values-4/#functional-notation).

Ah, yes, that will be removed.

<!-- comment id=3026150859 author=astearns created=2025-07-02T02:38:27Z url=https://github.com/w3c/csswg-drafts/issues/12151#issuecomment-3026150859 -->
## @astearns — 2025-07-02

RESOLVED: Update grammar for `<url>` to disallow duplicate `<request-url-modifier>` productions 
