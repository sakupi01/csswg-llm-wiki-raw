---
number: 12363
title: "[css-view-transitions-2] Animating view-transition-group-children captures"
state: "open"
labels: ["Needs Edits", "Needs Testcase (WPT)", "css-view-transitions-2"]
author: "vmpstr"
created_at: "2025-06-18T15:08:21Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12363"
comments: 5
has_resolution: true
synced_at: "2026-07-04T04:45:17Z"
---

In https://github.com/w3c/csswg-drafts/issues/11926, we've agreed to add a new view transition pseudo element: view-transition-group-children which copies the border-width property. In #12357 I'm also proposing that we capture `border-radius` and `corner-shape`. 

Currently we capture these from the "old" element. If the "new" element differs from that, then it can look a bit awkward

In Chrome Canary with experimental web platform features enabled:
https://codepen.io/bramus/full/YPXOXra

Thanks to @bramus for the demo

The proposal here is to follow the pattern we use for group captures: store the "old" element values in a `from` keyframe, and always update computed style to the "new" values. Also add a keyframe animation on the group-children pseudo that uses the from keyframe. 

/cc @noamr

<!-- comment id=2987420609 author=bramus created=2025-06-19T09:34:57Z url=https://github.com/w3c/csswg-drafts/issues/12363#issuecomment-2987420609 -->
## @bramus — 2025-06-19

To those not entirely following let me walk you through the demo.

The current default behavior – which does not animate the border – is this:

https://github.com/user-attachments/assets/23fbe6a1-b8e7-4714-9492-1424f649d5ca

If you look closely the nested albums bleed over the border when the border becomes bigger, or they get clipped a bit when the border becomes smaller.

When you tick the second checkbox on the demo, the `border-width` on the `::view-transition-group-children` pseudo will animate. It’s a slightly better experience, but as you can see it’s still not perfect:

https://github.com/user-attachments/assets/d75c4bd2-1210-4a11-ad43-c5cc2041cd3f

The best experience comes when ticking the third checkbox as well, which animates the original element by only showing the new snapshot and using a transition on the border – a technique described in https://brm.us/view-transitions-border-radius-revisited

https://github.com/user-attachments/assets/4d1aa57b-9d47-451b-97ef-491bfdac3594

With both workaround checkboxes checked:

- The wrapper itself animates
- The position of the nested contents also animate (and get clipped) as you’d expect.

---

I’d like to assume that the last recording is what authors want. We could get partially give authors that by animating the border (and other decorations) on the `::view-transition-group-children` pseudo, but it’d still require authors to duplicate that animation/transition on the actual element itself so that they don’t get the cross-fade.

That last part is definitely doable in CSS (hide the old snapshot + don’t animate the new + transition the props on the OG element *), but it would require a manual intervention by the author.

Maybe we can do this by default, expecting authors to fill in the extra gap (because they opted-in to this by using groups)? Or maybe authors can opt-in to a more advanced thing in which we do the transition for them (by the UA creating [an animation on the original group](ttps://www.bram.us/2025/03/11/view-transitions-border-radius/#dealing-with-changing-borders) for them?)

_TL;DR: I do think we’d want to animate the props on the new pseudo, but perhaps we should also give authors an option to animate the actual OG element underneath (which would only work if it’s the same element)_

<!-- comment id=2987449056 author=bramus created=2025-06-19T09:44:39Z url=https://github.com/w3c/csswg-drafts/issues/12363#issuecomment-2987449056 -->
## @bramus — 2025-06-19

> Or maybe authors can opt-in to a more advanced thing in which we do the transition for them (by the UA creating an animation on the original group for them?)

This has potential overlap with this suggestion: https://github.com/w3c/csswg-drafts/issues/10930#issuecomment-2981373113 _(last paragraph)_

<!-- comment id=2988414304 author=noamr created=2025-06-19T14:58:03Z url=https://github.com/w3c/csswg-drafts/issues/12363#issuecomment-2988414304 -->
## @noamr — 2025-06-19

Wouldn't this create a situation where the nested groups would move in an animated way, but the content captured into the group's old/new pseudos would crossfade?

<!-- comment id=2988509913 author=vmpstr created=2025-06-19T15:33:24Z url=https://github.com/w3c/csswg-drafts/issues/12363#issuecomment-2988509913 -->
## @vmpstr — 2025-06-19

> Wouldn't this create a situation where the nested groups would move in an animated way, but the content captured into the group's old/new pseudos would crossfade?

Yeah, that's the second video in @bramus's comment. It's better than the first where there's clear clipping problems, but it's not as good as the third options of the full workaround. Given that we're already capturing borders in standard vt and crossfading them, I don't think changing that is in scope of this particular proposal, but moving from static old-value only towards animating seems like a step in the right direction.

<!-- comment id=3109400630 author=css-meeting-bot created=2025-07-23T16:49:20Z url=https://github.com/w3c/csswg-drafts/issues/12363#issuecomment-3109400630 resolution=true -->
## @css-meeting-bot — 2025-07-23

The CSS Working Group just discussed `[css-view-transitions-2] Animating view-transition-group-children captures`, and agreed to the following:

* `RESOLVED: animate the v-t-g-c border properties in the same way we animate other captured props`

<details><summary>The full IRC log of that discussion</summary>
&lt;TabAtkins> vmpstr: so we recently resolved to copy over border-radius and corner-shape<br>
&lt;TabAtkins> vmpstr: border-width is currently copied as a static value form the "old" element<br>
&lt;TabAtkins> vmpstr: for other props, we set up a from keyframe with the old values, and reflect the new values in the element's style, so you have an animation that runs from the old to the new, if they change between the two states<br>
&lt;TabAtkins> vmpstr: proposal is to do the same with the border properties on the group-children<br>
&lt;TabAtkins> vmpstr: want to point out, this doesn't perfectly match the visuals the user will see. the old capture cross-fades in to the new one.<br>
&lt;TabAtkins> vmpstr: so if you have a different width border you'll see a crossfade of the two borders. but the clip will animate, so if children are clipped it'll animate. it's close but not perfect, but still an improvement<br>
&lt;TabAtkins> astearns: do you have a conception of the ideal future state, and is this a step towards it?<br>
&lt;TabAtkins> vmpstr: there is... the ideal state a thing we called layered capture, where the new pseudos would only capture the contents, not the box decorations, and the box decos would animate on the pseudos instead. so this is a step in that direction.<br>
&lt;TabAtkins> vmpstr: we've tried to impl that model, it has a lot of complications. but it probably is ideal<br>
&lt;TabAtkins> astearns: summarize?<br>
&lt;TabAtkins> vmpstr: animate the v-t-g-c border properties in the same way we animate other captured props<br>
&lt;TabAtkins> astearns: concerns? anyone who wants more time?<br>
&lt;TabAtkins> ntim: is this only for the new group-children, or for all captures?<br>
&lt;TabAtkins> vmpstr: just for the new element<br>
&lt;TabAtkins> ntim: only for nested, or for all?<br>
&lt;TabAtkins> vmpstr: this element is only added for nested<br>
&lt;TabAtkins> ntim: sounds good<br>
&lt;TabAtkins> astearns: objections?<br>
&lt;TabAtkins> RESOLVED: animate the v-t-g-c border properties in the same way we animate other captured props<br>
</details>
