---
number: 12040
title: "[cssom-view] `getClientRects()` should return a flattened list of child rects for `display: contents`"
state: "open"
labels: ["cssom-view-1"]
author: "LeaVerou"
created_at: "2025-04-02T16:22:46Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12040"
comments: 8
has_resolution: false
synced_at: "2026-07-04T04:45:17Z"
---

Spec: https://drafts.csswg.org/cssom-view/#dom-element-getclientrects
Testcase: https://codepen.io/leaverou/pen/GgRPVgg

Currently, for `display: contents` elements, `getClientRects()` returns no rects, so `getBoundingClientRect()` returns an empty bounding box. This breaks literally every library or component doing anything related to positioning (e.g. popups, popovers, etc), as they typically rely on `getBoundingClientRect()`, which in turn is computed as a union of `getClientRects()`. It even breaks the UA devtools! 

This is not just an inconvenience, I think it's also not architecturally sound: for everything else (e.g. flexbox, grid, etc), `display: contents` basically "offloads" layout to the element's children, so it seems reasonable `getClientRects()` should do the same and return a flattened list of the rects of its children.

One concern at this point is web compat, but @tabatkins thought because it's limited to `display: contents` it may be okay.

<!-- comment id=2773195887 author=schenney-chromium created=2025-04-02T17:01:21Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2773195887 -->
## @schenney-chromium — 2025-04-02

From a compat perspective, why would you be relying on this behavior, other than to check for this situation in practice so you can work around it.

<!-- comment id=2773218827 author=tabatkins created=2025-04-02T17:12:03Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2773218827 -->
## @tabatkins — 2025-04-02

Agreed that we should fix this, and that compat is unlikely to be a problem.

<!-- comment id=2780702499 author=RobMayer created=2025-04-05T12:42:06Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2780702499 -->
## @RobMayer — 2025-04-05

Forgive what must be a naive question, but this - in theory - would allow for, say, an element to position-anchor to something with display: contents and it would utilize the bounding box of that elements collective children as its positional reference, yes?

if so - heck yeah batman!, that'd be awesome. In the last two weeks I've hit about a half dozen scenarios where this would be a blessing.

Edit: I think I might have found the [disappointing] answer - according to specs display: contents still doesn't generate a 'principle box', and therefor wouldn't be a valid 'anchor-box'. Would this still hold true despite this change? and if not, does the underlying intent of changing the return of getBoundingClientRect() venn with how position-anchor ideally ought to work?

<!-- comment id=2780887981 author=bfgeek created=2025-04-05T16:15:29Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2780887981 -->
## @bfgeek — 2025-04-05

> Would this still hold true despite this change?

It would. `display:contents` elements are completely transparent to layout, we can't use them for anchor positioning because they don't exist.

<!-- comment id=2780920167 author=RobMayer created=2025-04-05T16:41:43Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2780920167 -->
## @RobMayer — 2025-04-05

This might be going out of bounds of scope on this particular topic, and if it is, I'm happy to raise this as a thing elsewhere.

> It would. display:contents elements are completely transparent to layout, we can't use them for anchor positioning because they don't exist.

I guess I'm wondering if this ought to be some sort of exception. If one of the reasons behind having `display: contents` give a non-zero bounding box is to let popover component authors attach their popovers to that element despite it not being part of layout, then I'd posit that we ought to be able to do the same with browser-native popover/anchor.

<!-- comment id=2781506232 author=LeaVerou created=2025-04-06T16:43:35Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-2781506232 -->
## @LeaVerou — 2025-04-06

I agree that we should fix this and that compat is not a problem, I was just mentioning it in case, as some teams are more risk averse than others.

Yes, anchor positioning also working would be great. Not sure if this would be sufficient for it, I suspect it would need a separate exception, but I cannot imagine any cases where the current behavior would be desirable. 

As for use cases, the one I’ve run into is web components wrapping natives to get the UA a11y and behavior but wanting to still make them stylable from the outside with regular CSS properties and without parts. Eg a <foo-button> wrapping a native <button> but still wanting properties on the host to work, and style the underlying button. Applying display: contents on the host and inheriting liberally on the shadow button seems to be the only way to do this, not just currently, but in the forseeable future (in some cases even if extending builtins were a possibility). 

<!-- comment id=3105579900 author=Loirooriol created=2025-07-23T03:52:34Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-3105579900 -->
## @Loirooriol — 2025-07-23

I don't think I agree with the proposal.

First, an element with `display: contents` generates no box of its own, so `getClientRects()` being empty seems reasonable to me.

Then, if `getClientRects()` redirects to children, it can create a disparity with other APIs. Should `ResizeObserver` also return/track the list of sizes of the children? `clientWidth`? Etc.

Also, what children would be reported, exactly?
 - Text nodes have no `getClientRects()`, are they included?
 - Pseudo-elements have no `getClientRects()`, are they included? Which ones?
 - Would this choose children in the flat tree? Even if that would expose number of contents of a closed shadow host?
 - Should absolutely positioned children be included? I can imagine authors not wanting them since they are out-of-flow

It just seems an inconsistent can of worms to me. I think it's better to defer this to authors if they want to do something special if `getComputedStyle(el).display === "contents"`. 

<!-- comment id=3184600188 author=css-meeting-bot created=2025-08-13T16:26:05Z url=https://github.com/w3c/csswg-drafts/issues/12040#issuecomment-3184600188 -->
## @css-meeting-bot — 2025-08-13

The CSS Working Group just discussed ``[cssom-view] `getClientRects()` should return a flattened list of child rects for `display: contents` ``.

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> florian: if you ask getClientRect on display: content, it returns nothng<br>
&lt;JoshT> ... if you return the pieces of it, that would be more useful<br>
&lt;oriol> q+<br>
&lt;JoshT> ... this is likely to be a breaking change. are we OK with breaking compat?<br>
&lt;astearns> ack oriol<br>
&lt;bkardell> it would be good to answer Oriol's questions in the issue<br>
&lt;JoshT> oriol: I'm not opposing. but was explaining in last comment, not sure if we should wait for Lea to defend proposal, but I can explain my reasoning<br>
&lt;JoshT> astearns: is your concern about the way it is to be resolved or the use case in general?<br>
&lt;JoshT> oriol: there are specific values where they can all be valid and it's not clear how we would do it regardless of the method. some authors would want a different behaviour<br>
&lt;JoshT> ... like should we include abspos children that are out of flow<br>
&lt;JoshT> ... should we include text children. they don't have clientRects<br>
&lt;emilio> +1 oriol<br>
&lt;JoshT> ... there is a proposal to support it for pseudo elements. would they be included?<br>
&lt;JoshT> ... in general, I think it seems useful, but a can of worms!<br>
&lt;emilio> q+<br>
&lt;JoshT> ... I think it's better if authors implement exactly what they want in JS<br>
&lt;JoshT> ... if the element has display contents, then I think producing an empty value is reasonable because it has no box of its own<br>
&lt;astearns> ack emilio<br>
&lt;JoshT> emilio: +1 to oriol. we already have some APIs for selection that behave like this<br>
&lt;JoshT> ... range.getClientRect is a thing<br>
&lt;JoshT> ... that pretty much does what Lea wants I think<br>
&lt;JoshT> ... for display: contents, it doesn't produce a box so it should return empty<br>
&lt;lea> q?<br>
&lt;JoshT> ... I don't think this would be web compat<br>
&lt;JoshT> lea: what's the alternative proposal, I've just joined<br>
&lt;JoshT> astearns: for authors using JS to work out which children they want to get the rect for<br>
&lt;JoshT> lea: anchor positioning?<br>
&lt;JoshT> ... it's been a while, but this came up with certain components that need to capture native elements<br>
&lt;JoshT> ... a solution for some things is to wrap a native element<br>
&lt;JoshT> ... there are two ways to style. either not style the button in the shadow dom and consumers use custom properties for basic styling<br>
&lt;JoshT> ... Or to display: contents on the host and then authors can style the button, for example<br>
&lt;JoshT> ... authors can apply bg and borders and whatever<br>
&lt;emilio> The alternative is implement what you want. which might be `let r = new Range(); r.selectNodeContents(thing); r.getClientRects();`<br>
&lt;JoshT> ... but makes it difficult to get getClientRect to work to return a union of the children<br>
&lt;JoshT> ... that might be more what authors expect<br>
&lt;JoshT> ... but what happens today if you use anchor pos and you have getClientRect<br>
&lt;JoshT> emilio: it doesn't work<br>
&lt;JoshT> lea: and it's also defined in terms of client rect. so I'm not married to fixing getClientRect<br>
&lt;JoshT> ... but if anchor positioning doesn't work here, that's a broader problem<br>
&lt;emilio> q+<br>
&lt;JoshT> TabAtkins: the element wouldn't exist here as far as the layout tree is concerned<br>
&lt;JoshT> lea: if there was a declarative way to declare which elements make the box might work. that would help with this use case<br>
&lt;JoshT> emilio: I understand the use case of wrapping a single element and wanting it to behave as the element<br>
&lt;JoshT> ... for that, all these questions have answers<br>
&lt;lea> q+<br>
&lt;astearns> ack emilio<br>
&lt;JoshT> ... I don't think we can make this generally work without answering a lot of really hard questions around chaining behaviour of these APIs<br>
&lt;JoshT> ... maybe the answer is you don't style the component itsself and you style the parts<br>
&lt;JoshT> lea: yeah that's the other alternative<br>
&lt;astearns> ack lea<br>
&lt;JoshT> emilio: that's what works now then. but I agree it would be useful for the specific use case you outlined<br>
&lt;JoshT> lea: this makes it confusing for users using parts. there's certain things you can't do. now you have to learn a new thing: the obvious thing doesn't work<br>
&lt;JoshT> ... this feels like a theoretical purity argument<br>
&lt;JoshT> ... it doesn't generate a box on its own but the descendents do<br>
&lt;florian> q?<br>
&lt;florian> q+<br>
&lt;JoshT> ... but I'm not sure there are any use cases where you would want an empty rect<br>
&lt;JoshT> emilio: could be places where intersection observers accidentally fire if we change this<br>
&lt;JoshT> ... I understand it would be nice if things were magic and transparent. but if you have to reach into the shadow dom and style that then ???<br>
&lt;JoshT> lea: I agree it's weird but the web platform doesn't supply another way to do this<br>
&lt;florian> q-<br>
&lt;JoshT> emilio: maybe we should extend built ins. but that's not related to display: contents<br>
&lt;bkardell> there is a separate issue on what Emilio just mentioned<br>
&lt;JoshT> astearns: we should take this back to the issue, but should have a new issue for display: contents on anchor positioning<br>
&lt;JoshT> TabAtkins: maybe we could but I'd like it to be consistent with something else on the web platform<br>
&lt;JoshT> lea: we could add a dom method that gets you the union of all these<br>
&lt;JoshT> emilio: the range API already exposes this. I don't think it's great, but it's already exposed<br>
&lt;JoshT> lea: are you suggested this is precident we could look at or we could already use range?<br>
&lt;JoshT> emilio: I don't think it would be what you want generally because it only works on a flat tree, but for most use cases it should work<br>
&lt;JoshT> ... as oriol said, there are many bounding boxes that could work here<br>
&lt;JoshT> ... so I don't see this working in a general way that is useful enough but maybe that's me<br>
&lt;JoshT> lea: I agree but think anything we do is more useful than the current situation<br>
&lt;JoshT> astearns: let's take it back to the issue<br>
</details>
