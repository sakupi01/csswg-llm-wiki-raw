---
number: 12598
title: "[css-shapes-1] Proposal: Option for the radii of `round` on `<basic-shape-rect>` to resolve percentages against the rectangle itself instead of the reference box"
state: "open"
labels: ["css-shapes-1", "css-shapes-2"]
author: "bernhardf-ro"
created_at: "2025-08-13T08:21:18Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12598"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:45:27Z"
---

The border radii specified after the `round` identifier of `<basic-shape-rect>` functions resolve percentage values against the reference box.

However, in many cases authors prefer radius percentages to be resolved against the rectangle itself, i.e. the dimensions computed from the `<basic-shape-rect>` function.

We propose an additional or alternative identifier that changes how those percentages are resolved.

We are not yet sure about good names, so we will go with `self` and `round-self` for the following examples.

For example, the following would create border radii that are always about a third of the related side of the rectangle, leaving the remaining middle third of each side straight, regardless of the `font-size`, and with it the resolved `em` values:
```css
xywh(10mm 5mm 4em 3em round self 33%) /* additional identifier */
xywh(10mm 5mm 4em 3em round-self 33%) /* alternative identifier */
```

Of course `em` values are not a common case. But even with absolute lengths, such percentages are easier to read and understand for authors and maintainers.

<!-- comment id=3582422230 author=css-meeting-bot created=2025-11-26T17:24:40Z url=https://github.com/w3c/csswg-drafts/issues/12598#issuecomment-3582422230 -->
## @css-meeting-bot — 2025-11-26

The CSS Working Group just discussed ``[css-shapes-1] Proposal: Option for the radii of `round` on `<basic-shape-rect>` to resolve percentages against the rectangle itself instead of the reference box``.

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> TabAtkins: request from a user to allow % in border-radius of basic shapes<br>
&lt;JoshT> ... to calc % off of the rectangle you specify<br>
&lt;JoshT> ... right now they resolve off the size of the element that's being styled<br>
&lt;JoshT> ... this is a good behavuour but means % border-radius do not act like how they do on a element<br>
&lt;JoshT> ... so you can't set 25% and get a rectangle with straight segments<br>
&lt;noamr> q+<br>
&lt;JoshT> ... this might be longer than you expect and give you a full ellipse<br>
&lt;JoshT> ... so request to make it relative to the size of the defined rectangle<br>
&lt;JoshT> ... they suggest putting a self suffix on the round keyword<br>
&lt;emilio> q+<br>
&lt;JoshT> astearns: any other keywords?<br>
&lt;astearns> ack noamr<br>
&lt;JoshT> TabAtkins: we have the round keyword, and presumsbly other variants<br>
&lt;JoshT> noamr: this is a problem when % can come from calc and vars from outside<br>
&lt;JoshT> ... all of those things end up being x% smooshed together from various sources<br>
&lt;JoshT> ... we have problem with arc with direction agnostic radius<br>
&lt;JoshT> ... you can't serialise back to the computed value<br>
&lt;JoshT> ... this has come with shape fn as well<br>
&lt;JoshT> ... anything with % relative to the shape rather than the box<br>
&lt;JoshT> ... now it's a length % from something else<br>
&lt;JoshT> ... so we need to consider that<br>
&lt;JoshT> TabAtkins: I don't understand the problem<br>
&lt;JoshT> ... it's a full switch with two types of percentages<br>
&lt;JoshT> ... it shouldn't change the timing of how we interpret them<br>
&lt;JoshT> ... you interpret as soon as you know how to interpret the sides<br>
&lt;JoshT> noamr: if you have calc with % from everywhere, you say they refer to the rect() rather than to the element; that would be confusing<br>
&lt;JoshT> ... if you pass. 50% from somewhere, you change the meaning<br>
&lt;JoshT> ... in the past it might have meant something else, could be confusing to change the meaning of %<br>
&lt;JoshT> TabAtkins: you can already change the meaning at the point of use. you can change ??? as well<br>
&lt;weinig> q+<br>
&lt;JoshT> astearns: no different to using a % in two different properties that eval % in different ways<br>
&lt;JoshT> ... we might need this kind of switch in other fns<br>
&lt;weinig> q-<br>
&lt;JoshT> noamr: it came up with other fns. usually the reference box. not just this issue but it's related<br>
&lt;JoshT> astearns: if there are other places where we have switches, it would be good to do it consistently<br>
&lt;JoshT> ... -self is good<br>
&lt;astearns> ack emilio<br>
&lt;JoshT> emilio: I wasn't sure what was the issue with the independent computed value<br>
&lt;JoshT> ... it's weird. part of the issue is you can specify them in the input fn<br>
&lt;JoshT> ... if we want to do this, I'm not objecting, the weird thing is making only one of the % relative to the inset modfied box<br>
&lt;JoshT> ... that makes it very round specific<br>
&lt;JoshT> TabAtkins: use case is to make it act like border-radius<br>
&lt;JoshT> emilio: how often does it come up?<br>
&lt;JoshT> ... you can work around it with calc, but repeating the insets<br>
&lt;JoshT> ... it gets weird<br>
&lt;JoshT> ... but I think you can do it<br>
&lt;JoshT> ... weird that % in the same function is relative to something else<br>
&lt;TabAtkins> instead of 10%, you'd write calc((100% - TOP - BOTTOM) * .1), pretty non-trivial<br>
&lt;JoshT> astearns: given there's a workaround, maybe take back to the issue and do more investigation about the rest of the basic shape functions to see where else we want to do it<br>
&lt;JoshT> emilio: I think it's specific to round<br>
&lt;JoshT> ... I won't object to doing round-self<br>
&lt;JoshT> ... but I don't think it generalises well to other shape fns<br>
&lt;noamr> q+<br>
&lt;JoshT> astearns: I'd like to get opinion to the issue author to see if the workaround meets their needs or if it's non-trivial<br>
&lt;astearns> ack noamr<br>
&lt;JoshT> noamr: also applies when you have curves<br>
&lt;JoshT> ... and you want the points to be % of the rectangle<br>
&lt;JoshT> ... no matter how big the rect is, it's similar to this rounding thing<br>
&lt;JoshT> astearns: I wonder if there's a simpler design that is a self-size function where you tag a %<br>
&lt;JoshT> ... and it could make it clear that that's a special percentage. could be added to other functions<br>
&lt;JoshT> ... so take back to the issue saying we're not against it, but we want more investigation<br>
</details>

<!-- comment id=3605797041 author=bernhardf-ro created=2025-12-03T09:07:23Z url=https://github.com/w3c/csswg-drafts/issues/12598#issuecomment-3605797041 -->
## @bernhardf-ro — 2025-12-03

**Workaround / Benefit**

While using calc as a workaround does technically work, it results in code that is not very easy to read. For comparison:
```
xywh(10mm 5mm 4em 3em round-self 33%)
xywh(10mm 5mm 4em 3em round calc(4em * 0.33) / calc(3em * 0.33))

rect(10mm 5mm 4em 3em round-self 33%)
rect(10mm 5mm 4em 3em round calc((5mm - 3em) * 0.33) / calc((4em - 10mm) * 0.33))

inset(10mm 5mm 4em 3em round-self 33%)
inset(10mm 5mm 4em 3em round calc((100% - 5mm - 3em) * 0.33) / calc((100% - 10mm - 4em) * 0.33))
```
We consider the difference in maintainability significant. And these are only cases with a single value for `round-self`. While less common, cases with multiple values make the calc workaround quite long and complex.

So we think there would actually be a relevant benefit to `round-self`.


**Resolving against different values**

We don't think that parameters of the same function being resolved against different values is a significant problem. Technically that is already happening as basic-shape-rect function parameters use width and height in turn.

But if that is considered a major issue in this design, we'd propose to turn `round-self` from a delimiting ident into a function:
```
xywh(10mm 5mm 4em 3em round-self(33%))
```
With 20/20 hindsight we'd say that ideally `round` could also have been a function. Then its parameters would already be separated form the ones of the basic-shape-rect function. `round-self` could then just be added as an alternative.

However, even with `round` already established as a delmiting ident, `round-self` could still be introduced as a function, just to separate its parameters from the ones of the basic-shape-rect function.

I'd like to stress that this is not our preferred approach, but if it eliminates a major concern we'd be fine with it.
