---
number: 12255
title: "[css-text-4] Add an optional second value to `hyphenate-character` to render the unchosen `&shy;`"
state: "open"
labels: ["css-text-4"]
author: "jfrech"
created_at: "2025-05-31T19:15:52Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/12255"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:22Z"
---

### Status quo
Currently, `hyphenate-character`'s values are defined as (cf. https://www.w3.org/TR/css-text-4/#hyphenate-character [2025-05-31])

````
hyphenate-character = auto | <string>
````

with the semantics that when a `&shy;` word-break opportunity is taken, the `&shy;` character is rendered as the given string (which has an `auto` default). When a `&shy;` word-break opportunity isn't taken, the `&shy;` character isn't rendered.

### Proposal
I propose adding a second, optional argument to `hyphenate-character` with default value `""` that specifies the rendering of a `&shy;` character whose word-break opportunity isn't taken:

````
hyphenate-character = auto | <string> | <string> <string>
````

### Motivation
Hyphenation rules in modern English (e.g. `en-GB`) and German after the spelling reform (e.g. `de-DE`) fit `hyphenate-character`'s model well: when you can break, break and add a hyphen, else don't break and leave the word alone.

However, when typesetting German preceding the spelling reform (language code `de-DE-1901`), hyphenation *may add and remove letters*:

````text
((de-DE-1901))

Nach Tagen der Suche konnte sie eine Pampelmuse entdecken, und begann, ein Stilleben anzufertigen.
                                                     ^^ "ck" -> "k-k"         ^^ "ll" -> "ll-l"
    
Nach Tagen der Suche konnte
sie eine Pampelmuse entdek-
ken, und begann, ein Still-
leben anzufertigen.
````

*Adding* letters `"ll" -> "ll-l"` when breaking can be achieved with contemporary CSS:

````html
<p lang="de-DE-1901">Nach Tagen der Suche konnte sie eine Pampelmuse entdecken, und begann, ein
<span style="hyphenate-character: 'l-';">Stil&shy;le</span>&shy;ben
anzufertigen.</p>
````

But *changing* letters `"ck" -> "k-k"` when breaking, to my knowledge, cannot be achieved in pure CSS (I have written an ECMAScript resize observer which changes letters' `display: none` styling, though it possibly falls into a never-ending loop when the font rendering gives different characters too dissimilar widths).  
With an unused-`&shy;` text parameter to `hyphenate-character`, the break opportunity could be aptly described:

````html
<p lang="de-DE-1901">Nach Tagen der Suche konnte sie eine Pampelmuse
ent&shy;<span style="hyphenate-character: 'k-' 'c';">de<span
  style="font-size: 0px; float: inline-end;">c</span>&shy;ken</span>,
und begann, ein Stilleben anzufertigen.</p>
````

### Raison d'être
I think adding an as-described non-taken `&shy;` rendering argument to `hyphenate-character` opens up the possibility for correctly breaking justified `de-DE-1901` text, a feature CSS is currently lacking, through a generic feature without introducing `de-DE-1901`-specific styling rules.

Pre-orthography-reform German has its place on the Web: when citing older texts in a justified layout, incorrectly breaking invalidates the citation and not breaking aesthetically hurts the text. Furthermore, active publications exist that choose to write in `de-DE-1901` (e.g. Lettre International (cf. https://www.lettre.de/ [2025-05-31]).

<!-- comment id=2939207808 author=frivoal created=2025-06-04T09:00:13Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-2939207808 -->
## @frivoal — 2025-06-04

I think the rendering when not broken needs to be included in the text of the actual element, otherwise the actual text would failed to be rendered as expected in browsers that don't support the feature (initially all of them), or in contexts where css isn't applied (reader mode, plain-text copy&paste, search…). That said, the suggested feature could plausibly be realized via CSS by listing not what is added, but what is replaced.

<!-- comment id=2972519263 author=jfrech created=2025-06-14T09:13:14Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-2972519263 -->
## @jfrech — 2025-06-14

@frivoal Fully agree, this missing backwards-compatibility was a sizable blemish in my eyes, too.

I now came across a {CSS/text selection} interaction with which to remedy it (which might be too much of a hack, ideally the shy in "entdec&shy;ken" would vanish, too); I have edited the body accordingly.

<!-- comment id=2974305216 author=jfkthame created=2025-06-15T17:18:41Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-2974305216 -->
## @jfkthame — 2025-06-15

> I now came across a {CSS/text selection} interaction with which to remedy it (which might be too much of a hack, ideally the shy in "entdec­ken" would vanish, too); I have edited the body accordingly.

I'm curious about the use of `float: inline-end` there -- is that necessary? It seems likely to disrupt things like copy/paste and find-in-text operations.

<!-- comment id=3075734503 author=jfrech created=2025-07-15T21:23:37Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-3075734503 -->
## @jfrech — 2025-07-15

@jfkthame It's a hack *to disrupt copy&amp;paste*: when displaying "...k-k...", I want the user agent to copy and find-in-text "...ck...". This is the crux of de-DE-1901 word wrapping: the letters that are produced on either side of the hyphen *are not* those of the word's unhyphenated orthography.

<!-- comment id=3075821069 author=jfkthame created=2025-07-15T21:42:36Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-3075821069 -->
## @jfkthame — 2025-07-15

I think I still don't entirely understand. If I try your example:
```
<p lang="de-DE-1901">Nach Tagen der Suche konnte sie eine Pampelmuse
 ent&shy;<span style="hyphenate-character: 'k-' 'c';">de<span
 style="font-size: 0px; float: inline-end;">c</span>&shy;ken</span>,
 und begann, ein Stilleben anzufertigen.</p>
```
in a current browser (i.e. one that doesn't support this suggested feature), and the `&shy;` in `entdecken` is **not** used as a breakpoint, the word renders as "entdeken" -- there's no visible "c". Isn't that wrong? So this does not seem to be backward-compatible.

<!-- comment id=3364069135 author=jfrech created=2025-10-03T03:19:41Z url=https://github.com/w3c/csswg-drafts/issues/12255#issuecomment-3364069135 -->
## @jfrech — 2025-10-03

@jfkthame It is fully backward-compatible; due to the fact that `de-DE-1901` *does interchange a character* (`entdecken` gets hyphenated into `entdek-ken`), I hid the `c` in `entdecken` so that it only appears inside selections but doesn't get rendered. The HTML snippet assumes the proposed feature present and thus doesn't render as envisaged, yet this isn't in opposition to the proposal being CSS-feature-monotonic.
