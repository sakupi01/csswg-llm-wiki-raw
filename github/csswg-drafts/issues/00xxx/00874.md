---
number: 874
title: "[css-content] `none` and `normal` values (for `content` property) are never linkified"
state: "closed"
labels: ["css-content-3"]
author: "dholbert"
created_at: "2017-01-09T18:46:17Z"
closed_at: "2019-04-09T22:26:27Z"
url: "https://github.com/w3c/csswg-drafts/issues/874"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:38:06Z"
---

In the css-content spec, the `none` and `normal` values (for the `content` property) are never linkified, but probably should be. I suspect there's some bug in the markup that prevents the correct link target from being inferred/discovered when bikeshed is generating HTML.

Other values of the same property (like e.g. the `contents` value) *are* successfully linkified.

Sample sections of the spec where this is noticeable:
 * In the definition of the `normal` value, two other values are mentioned -- `contents` and `none`. The former is linkified, the latter is not. https://drafts.csswg.org/css-content-3/#valdef-content-normal
 * In the definition of the `contents` value, all three values (`none`, `normal` and `contents`) are mentioned. Again, only `contents` is linkified. https://drafts.csswg.org/css-content-3/#valdef-content-contents

I don't know bikeshed markup very well, but I'm guessing that perhaps the issue is that there's no `dfn-for="content"` attribute, on the `<dl>` parent of the none & normal `<dfn>` tags...? (At least, the correctly-working `contents` value does have a `dfn-for` attribute on its `<dl>` parent in the bikeshed markup.)

CC @tabatkins

<!-- comment id=271413493 author=dauwhe created=2017-01-09T21:30:28Z url=https://github.com/w3c/csswg-drafts/issues/874#issuecomment-271413493 -->
## @dauwhe — 2017-01-09

I've been working on this. I think it's a bit better, but there's more to do. This is one of the more interesting specs when it comes to complex definitions. I'll try to get some markup advice at the F2F.

<!-- comment id=481164925 author=xfq created=2019-04-09T08:57:25Z url=https://github.com/w3c/csswg-drafts/issues/874#issuecomment-481164925 -->
## @xfq — 2019-04-09

All links to `none` (for `content` property) I found have been linkified, and the only `normal` that's not linkified is the second `normal` in the definition of the `contents` value, which isn't bad, because there's already a link to the definition of `normal` in that paragraph. So I guess this issue can be closed?

<!-- comment id=481462467 author=dholbert created=2019-04-09T22:23:12Z url=https://github.com/w3c/csswg-drafts/issues/874#issuecomment-481462467 -->
## @dholbert — 2019-04-09

I just noticed that my https://drafts.csswg.org/css-content/ URLs from comment 0 are now pointing at `css-content-4`.  Back when I filed this, the URL pointed to `css-content-3`, which now lives at https://drafts.csswg.org/css-content-3/ (which is where the `content` property and its `none`/`normal` values are defined).

Anyway: I've edited the original comment here to use links with the version number.  And yes, I agree with @xfq - it looks like this has been addressed (mostly if not entirely).

<!-- comment id=481479166 author=xfq created=2019-04-09T23:39:23Z url=https://github.com/w3c/csswg-drafts/issues/874#issuecomment-481479166 -->
## @xfq — 2019-04-09

Hmm, I think css-content doesn't have L4 yet?

<!-- comment id=481506684 author=dholbert created=2019-04-10T02:15:23Z url=https://github.com/w3c/csswg-drafts/issues/874#issuecomment-481506684 -->
## @dholbert — 2019-04-10

Oh, sorry, you're right - I had just noticed that https://drafts.csswg.org/css-content/#normal (from my un-edited initial comment) was a broken anchor, and I assumed it was from a level increase and I didn't actually look too closely. :)  (And then I later updated the anchor target, after updating the link to include the level number.)

Anyway - the updated links with `-3` are still valid, and this is still fixed, so all is well.
