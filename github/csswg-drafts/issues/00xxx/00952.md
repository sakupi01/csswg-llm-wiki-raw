---
number: 952
title: "[mediaqueries] Should a top-level not be evaluated as a <media-not> if possible?"
state: "closed"
labels: ["Closed Rejected as Invalid", "Testing Unnecessary", "Tracked in DoC", "mediaqueries-4"]
author: "grorg"
created_at: "2017-01-18T00:29:59Z"
closed_at: "2017-01-23T21:08:41Z"
url: "https://github.com/w3c/csswg-drafts/issues/952"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:43:48Z"
---

Suppose we have:

```
div {
  width: 100px;
  height: 100px;
  background-color: red;
}

@media not (min-width: 600px) {
  div {
    background-color: blue;
  }
}
```

Then the `@media` rule follows

[[[
2.2.1. Negating a Media Query: the not keyword

An individual media query can have its result negated by prefixing it with the keyword not. If the media query would normally evaluate to true, prefixing it with not makes it evaluate to false, and vice versa.
]]]

And it gives an example.

However, I don't this this rule applies in this case. Check out Section 3. Syntax.

```
<media-query> = <media-condition>
             | [ not | only ]? <media-type> [ and <media-condition-without-or> ]?
```

In our example, it matches `<media-condition>`, rather than "not `<media-type>`".

```
<media-condition> = <media-not> | <media-and> | <media-or> | <media-in-parens>
```

Cool. It looks like we are a `<media-not>`.

```
<media-not> = not <media-in-parens>
<media-in-parens> = ( <media-condition> ) | <media-feature> | <general-enclosed>
```

So this is a media-not, that has a media-in-parens that is a media-feature.

However, none of the browsers implement this. I expect because they treat the "not" as the prefix to a `<media-type>`, and then decide that the type is invalid.

<!-- comment id=273346165 author=grorg created=2017-01-18T00:32:50Z url=https://github.com/w3c/csswg-drafts/issues/952#issuecomment-273346165 -->
## @grorg — 2017-01-18

WebKit bug: https://bugs.webkit.org/show_bug.cgi?id=167031

It seems a bit silly that you can't write something like this:

```
@media not (prefers-reduced-motion) {}
```

<!-- comment id=273346353 author=grorg created=2017-01-18T00:34:00Z url=https://github.com/w3c/csswg-drafts/issues/952#issuecomment-273346353 -->
## @grorg — 2017-01-18

Check out the Simple Test on the webkit bug.

<!-- comment id=273478446 author=frivoal created=2017-01-18T13:44:31Z url=https://github.com/w3c/csswg-drafts/issues/952#issuecomment-273478446 -->
## @frivoal — 2017-01-18

> However, none of the browsers implement this. I expect because they treat the "not" as the prefix to a <media-type>, and then decide that the type is invalid.

Well, I'd phrase this differently. none of the browsers implement this because what they implement is Media queries level 3, which had a much simpler grammar without the full boolean logic:
https://drafts.csswg.org/mediaqueries-3/#syntax

> It seems a bit silly that you can't write something like this:
> `@media not (prefers-reduced-motion) {}`

Yes, it would be great if we could do that, which is why the spec says you can. But this ability to do generic logic with arbitrary and/or/not and not having to involve media types is new in Media queries 4.

As far as I can tell, the action item is on you (and other vendors) to actually implement this new generalized syntax. You may be able to reuse a good chunk of what you already implemented for `@supports`, since that's very similar.

<!-- comment id=274205244 author=grorg created=2017-01-20T23:15:44Z url=https://github.com/w3c/csswg-drafts/issues/952#issuecomment-274205244 -->
## @grorg — 2017-01-20

Cool. In which case I'll implement this 😄 

<!-- comment id=274224718 author=frivoal created=2017-01-21T02:03:48Z url=https://github.com/w3c/csswg-drafts/issues/952#issuecomment-274224718 -->
## @frivoal — 2017-01-21

Awesome.
