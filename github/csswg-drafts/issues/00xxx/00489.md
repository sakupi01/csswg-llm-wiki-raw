---
number: 489
title: "[css-values] Parsing \"<integer> | <length>\" or \"<number> | <length>\""
state: "closed"
labels: ["css-values-3"]
author: "upsuper"
created_at: "2016-09-15T10:09:09Z"
closed_at: "2017-06-09T17:40:21Z"
url: "https://github.com/w3c/csswg-drafts/issues/489"
comments: 8
has_resolution: true
synced_at: "2026-07-04T04:44:17Z"
---

From #460 I noticed that `tab-size`'s syntax is `<integer> | <length>`, and I was confused on how `0` should be handled in this case, since `0` is valid as both `<integer>` and `<length>`. It seems the same issue applies to `<number>`, which `line-height` uses.

I do not find the answer whether for whether `0` should be parsed as `<integer>` or `<length>` in this case from css-values spec. Am I missing something, or probably the spec should say something about this case?

cc @fantasai @tabatkins 

<!-- comment id=247399356 author=tabatkins created=2016-09-15T17:45:05Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-247399356 -->
## @tabatkins — 2016-09-15

It doesn't matter which it parses as; both end up with an identical effect and serialize the same way.

Tho, hm, I guess the Typed OM would want to make a distinction.  I'm inclined to have it parse as integer/number, and would be happy to clarify that as a general rule in V&U.

<!-- comment id=247501825 author=upsuper created=2016-09-16T02:03:09Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-247501825 -->
## @upsuper — 2016-09-16

It seems to me zero `<length>` is [serialized](https://drafts.csswg.org/cssom/#serializing-css-values) as `0px`, no?

<!-- comment id=247627312 author=wargrey created=2016-09-16T15:15:28Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-247627312 -->
## @wargrey — 2016-09-16

I define a CSS:Zero for 0 as a subtype of CSS:Integer in my engine which implemented in a typed language(There also is a CSS:Flzero for 0.0). Then I can easily handle these two special number types when a dimension type is required.

<!-- comment id=301954375 author=fantasai created=2017-05-17T00:50:46Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-301954375 -->
## @fantasai — 2017-05-17

Agenda+ to ask if we should add a general rule to V&U that these ambiguous cases parse as `<number>`/`<integer>`

<!-- comment id=303781626 author=css-meeting-bot created=2017-05-24T16:41:20Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-303781626 resolution=true -->
## @css-meeting-bot — 2017-05-24

The CSS Working Group just discussed `CSS Values and Units: ambiguous zero`, and agreed to the following resolutions:

* `RESOLVED: Unitless zeros are parsed as numbers if it is ambiguous whether they are a <length> or a <number>`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> Topic: CSS Values and Units: ambiguous zero<br>
&lt;fantasai> github topic: https://github.com/w3c/csswg-drafts/issues/489<br>
&lt;Rossen_> Github issue: https://github.com/w3c/csswg-drafts/issues/489<br>
&lt;myles> fantasai: we have a situation where you can have &lt;interger> or &lt;length> (or &lt;nubmer> &amp; &lt;length> &amp; it's not clear what happens with unitless zero.<br>
&lt;myles> fantasai: tab-size &amp; line-height. which of these values does the 0 parse to?<br>
&lt;myles> fantasai: currently there is no behavior difference for these properties, but in the fiture it might matter<br>
&lt;myles> Rossen: so where would it matter?<br>
&lt;fantasai> cases are like &lt;integer> | &lt;length><br>
&lt;myles> Rossen: in calc(), parsing would fail if you do it wrong<br>
&lt;fantasai> properties affected include tab-size and line-height<br>
&lt;myles> Rossen: in another expression, in grid gaps, or something else, it will always be interpreted as a &lt;length><br>
&lt;myles> fantasai: xidorn says that 0 lengths get serialized as "0px"<br>
&lt;myles> Rossen: the computed style?<br>
&lt;myles> fantasai: i dunno<br>
&lt;myles> fantasai: TabAtkins says that the OM might want to make a distinction<br>
&lt;fantasai> s/OM/Typed OM/<br>
&lt;myles> fantasai: we could make a rule "if you're in an imbiguous situation, it parses as a number"<br>
&lt;fantasai> s/imbig/ambig/<br>
&lt;myles> Rossen: yes, and later in the cascade, you could typecast the number to an internal unit type, imlementation-specific, and the implementation could handle it<br>
&lt;myles> Rossen: the question is: what happens when you serialize it back out? Do you return the number or a real reallyrealies length<br>
&lt;myles> Rossen: for us, if we have to tag along an additional information about what i just described, it's going to be difficult<br>
&lt;myles> Rossen: not impossible though.<br>
&lt;myles> Rossen: doesn't buy authors much.<br>
&lt;myles> Rossen: (unless we have a specific example)<br>
&lt;myles> fantasai: we might not fix it, or we can say it parses as an integer<br>
&lt;myles> fantasai: those are the two options<br>
&lt;myles> fantasai: dbaron?<br>
&lt;myles> fantasai: &lt;restates the question><br>
&lt;myles> dbaron: i thought it would be anumber<br>
&lt;myles> fantasai: spec doesn't say that<br>
&lt;myles> fantasai: can we resolve?<br>
&lt;myles> Rossen: i have no problem keeping it as a number<br>
&lt;myles> Rossen: objections?<br>
&lt;myles> Rossen: to specify it as a number if it isn't already<br>
&lt;myles> RESOLVED: Unitless zeros are parsed as numbers if it is ambiguous whether they are a &lt;length> or a &lt;number><br>
</details>

<!-- comment id=1216397767 author=cdoublev created=2022-08-16T09:35:07Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-1216397767 -->
## @cdoublev — 2022-08-16

I think `<length> && <number>` cannot match `0 1` if you do not give priority to `<length>` for matching `0`.

I noticed the ambiguity with `border-image-outset`, defined with `[ <length [0,∞]> | <number [0,∞]> ]{1,4}`. Its initial value is `0` but imo it should be `0px`, because if you set `border` to `1px` then `border-image-outset` to `0px`, `border` cannot serialize to `1px` because its reset-only sub-property `border-image-outset` is not declared with its initial value.

<!-- comment id=1216750258 author=dbaron created=2022-08-16T14:53:31Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-1216750258 -->
## @dbaron — 2022-08-16

I think the discussion the working group had related to the `|` combinator, which accepts exactly one of the alternatives.  I don't think it's relevant to the `&&` combinator or to the all-values-present case of the `||` combinator (since these combinators are [not dependent on order of the grammar](https://drafts.csswg.org/css-values-4/#component-combinators), from which you can infer that the parse-time handling of them is not greedy).

Or, to put it another way, when parsing `<length> && <number>` or `<length> || <number>` none of the values `0 1`, `1px 0`, `0 1px`, or `1 0` is ambiguous.  The first two have the `<length>` before the `<number>`, and the latter two have the `<number>` before the `<length>`.

However, the cases that are ambiguous, and thus affected by the resolution above, are parsing `0` as `<length> | <number>` or as `<length> || <number>`; the resolution above says it is parsed as a `<number>`.

<!-- comment id=1217078272 author=cdoublev created=2022-08-16T19:34:26Z url=https://github.com/w3c/csswg-drafts/issues/489#issuecomment-1217078272 -->
## @cdoublev — 2022-08-16

Oh, you are right, backtracking should handle it. Thanks for the reply. 

CSS Value is not 100% precise on this custom priority.

> However, if a 0 could be parsed as either a `<number>` or a `<length>` in a property (such as line-height), it must parse as a `<number>`.

`|` combinator is not specified, and it can apply to `<integer> | <length>`, `<number> | <length-percentage>`, etc. Do you think it is worth opening a new issue?
