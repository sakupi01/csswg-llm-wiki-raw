---
number: 984
title: "[mediaqueries-4] min-width / max-width / min-height / max-height and fractional viewport dimensions"
state: "closed"
labels: ["Closed Accepted as Editorial", "Testing Unnecessary", "Tracked in DoC", "mediaqueries-4"]
author: "patrickhlauke"
created_at: "2017-01-25T11:27:34Z"
closed_at: "2017-03-07T19:39:23Z"
url: "https://github.com/w3c/csswg-drafts/issues/984"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:43:48Z"
---

Currently, `min-` and `max-` prefixes are equivalent to `>=` and `<=` (see https://drafts.csswg.org/mediaqueries-4/#mq-min-max)

This is fine under the assumption that the width/height of the viewport will always be integers, but will fall apart in the case of fractional dimensions (admittedly an edge case?) - see for instance https://github.com/twbs/bootstrap/issues/21552

Wondering if it would be possible to change these so that one of them is "including", while the other is "excluding". For instance, changing the definition to make `min-` equivalent to `>=` but `max-` equivalent to `<` only?

<!-- comment id=275086620 author=patrickhlauke created=2017-01-25T11:38:43Z url=https://github.com/w3c/csswg-drafts/issues/984#issuecomment-275086620 -->
## @patrickhlauke — 2017-01-25

alternatively, for backwards-compatibility with existing content, perhaps having a new modifier for the value part to indicate it's "excluding", thus allowing authors to choose which one they want to make "excluding" (assuming browsers would then infer the rest from context, i.e. if it's used for a `min-` then the value with modifier results in a `>` only, and in case of `max-` with modifier value a `<` only).

Spitballing, based on mathematical notation for intervals, maybe the `]` and `[` characters (where `[` could be assumed as the default for backwards compat)? Allowing for notation such as `min-width: ]320px` / `max-width: [320px` perhaps?

<!-- comment id=277874948 author=Marat-Tanalin created=2017-02-07T01:43:12Z url=https://github.com/w3c/csswg-drafts/issues/984#issuecomment-277874948 -->
## @Marat-Tanalin — 2017-02-07

The `@media (width < 600px)` syntax stability is [confirmed](https://lists.w3.org/Archives/Public/www-style/2017Feb/0036.html) by Tab Atkins in a [discussion](https://lists.w3.org/Archives/Public/www-style/2017Feb/0034.html) started recently at www-style@w3.org.

I’ve filed a [feature request](https://bugzilla.mozilla.org/show_bug.cgi?id=1337174) for Firefox.

<!-- comment id=277949205 author=patrickhlauke created=2017-02-07T09:45:47Z url=https://github.com/w3c/csswg-drafts/issues/984#issuecomment-277949205 -->
## @patrickhlauke — 2017-02-07

As https://drafts.csswg.org/mediaqueries/#mq-range-context would cover the problem of fractional viewport dimensions, is it worth adding a note (or series of notes) in the spec to point out that:

- `min-`/`max-` queries for `width`/`height` can cause problems (or gaps) with fractional viewport dimensions, and that this problem is better solved with the explicit use of `>`/`<=` etc?
- in the range context section, have a matching note reiterating this

If yes, happy to have a stab at this, just let me know @frivoal @tabatkins 
