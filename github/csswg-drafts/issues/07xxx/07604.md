---
number: 7604
title: "[css-animation-1] Limit keyframe rule to `@keyframes` context"
state: "closed"
labels: []
author: "cdoublev"
created_at: "2022-08-14T13:28:43Z"
closed_at: "2022-08-23T11:23:37Z"
url: "https://github.com/w3c/csswg-drafts/issues/7604"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:17Z"
---

This seems to be a minor problem but... I cannot find some definition that prevents this:

```css
@media all {
  from { translate: 0px; } /* keyframe rule */
}
```

In CSS Syntax 3, [`<stylesheet>`](https://drafts.csswg.org/css-syntax-3/#typedef-stylesheet) (the block value type of `@media`) is defined as:

> the `<stylesheet>` production represents a list of rules. It is identical to `<rule-list>`, except that blocks using it default to accepting all **rules that aren’t otherwise limited to a particular context**.

> For rules that use `<stylesheet>`, all rules are allowed by default, but **the spec for the rule may define what types of rules are invalid inside the rule**.

CSS Animation 1 does not limit `<keyframe-block>` (keyframe rule) to `@keyframes` and CSS Conditional 3 does not prevent `@media` from containing `<keyframe-block>`.

If I'm not mistaken, the issue is also valid for `@supports` and/or margin rules. 

<!-- comment id=1223929415 author=cdoublev created=2022-08-23T11:23:36Z url=https://github.com/w3c/csswg-drafts/issues/7604#issuecomment-1223929415 -->
## @cdoublev — 2022-08-23

I guess the following definition, quoted from CSS Conditionals 3, prevents `@media` from containing a keyframe rule:

> All conditional group rules are defined to take a `<stylesheet>` in their block, which means they can accept any rule that is normally allowed at the top-level of a stylesheet, and not otherwise restricted.
