---
number: 7040
title: "[syntax-3][cssom-1][selectors-3][mediaqueries-4][cascade-3][css-counter-styles-3] Improve the procedures used for parsing a CSS input"
state: "open"
labels: ["css-conditional-5", "css-page-3", "css-syntax-3", "cssom-1"]
author: "cdoublev"
created_at: "2022-02-12T11:09:00Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/7040"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:44:43Z"
---

As a follow up on [the second part of this comment](https://github.com/w3c/csswg-drafts/issues/4927#issuecomment-1012127022), which was about replacing the steps *parse a list of component values* and *match the result against the corresponding grammar*, by *parse something according to a CSS grammar*, I create this (list of) issue(s) (and suggestions) about the parsing procedures used across Syntax, CSSOM, Selectors, Media Queries, etc.

* [x] **fixed in https://github.com/w3c/csswg-drafts/commit/10d1cea64c889ead72aaa959859529e56ae5f252** - [CSS Syntax](https://drafts.csswg.org/css-syntax-3/#declaration-rule-list) is missing `<style-block>` in the section title *8.1. Defining Block Contents: the `<declaration-list>`, `<rule-list>`, and `<stylesheet>` productions*.
* [x] **fixed in https://github.com/w3c/csswg-drafts/commit/6ab5888d05ef3f445e26a1ca81081a7b912016ed** - [CSS Syntax](https://drafts.csswg.org/css-syntax-3/#qualified-rule) associates "style block" to *parse a list of declarations* instead of [parse a `<style-block>`](https://drafts.csswg.org/css-syntax-3/#typedef-style-block).
* [x] **fixed in https://github.com/w3c/csswg-drafts/commit/7d4d5b02b71b81bd79cdfdf2a3f6cc919b52e286** - [CSS Counter Styles - (set) CSSCounterStyleRule.`<descriptor>`](https://drafts.csswg.org/css-counter-styles/#the-csscounterstylerule-interface) should use *parse something according to a CSS grammar* using the descriptor’s grammar instead of *parse a list of component values*, otherwise the component values will not be parsed against the grammar of the corresponding descriptor (only the basic syntax will be validated).
* [ ] [CSS Syntax](https://drafts.csswg.org/css-syntax-3/#parser-entry-points) defines *parse a list of component values* as the entry point to parse a *stand-alone selector*, but [*parse a selector*](https://drafts.csswg.org/selectors-4/#parse-a-selector) should be used.
* [ ] [CSS Syntax](https://drafts.csswg.org/css-syntax-3/#parser-entry-points) defines *parse a list of component values* as the entry point to parse `Element.media`, but [*parse a media query list*](https://drafts.csswg.org/cssom/#parse-a-media-query-list) should be used.
* [ ] [CSS Conditional](https://drafts.csswg.org/css-conditional-3/#support-definition) defines that *a CSS processor is considered to support a declaration (consisting of a property and value) if it accepts that declaration*: it would be helpfull to refer to *parse something according to a CSS grammar using the grammar of the property*, or to [parse a CSS value](https://drafts.csswg.org/cssom/#parse-a-css-value).
* [ ] [CSSOM](https://drafts.csswg.org/cssom/#parse-a-css-value) defines *parse a CSS value* for parsing a property declaration value: *parse a CSS property value* seems a more correct name because the procedure receives a `property` and what is a CSS value is not defined anywhere: at least, it can also be a CSS descriptor value.
* [ ] [CSSOM](https://drafts.csswg.org/cssom/#parse-a-css-value) defines *parse a CSS value* with *parse a list of component values* composed with *match [the result] against the grammar for the property `property` in the CSS specification* but it could refer to *parse something according to a CSS grammar* using the property’s grammar instead.
* [x] **fixed in https://github.com/w3c/csswg-drafts/pull/9831** - [CSSOM](https://drafts.csswg.org/cssom/#parse-a-css-declaration-block) defines *parse a CSS declaration block* with *let parsed declaration be the result of parsing declaration according to the appropriate CSS specifications, dropping parts that are said to be ignored*, but this step could be replaced by *parse something according to a CSS grammar* with the property’s grammar, or by *parse a CSS value* with the property and its value.
* [x] **invalid: it can also be used for other rules than style rules** - *Parse a CSS declaration block* could be named *parse a CSS style block contents* because a "block" implies the presence of its associated tokens whereas this procedure only takes the contents of the block, which also accepts (but ignores) nested rules.
* [ ] [CSS Page](https://drafts.csswg.org/css-page-3/#syntax-page-selector) defines that *`@page` rules are [parsed](https://drafts.csswg.org/css-syntax-3/#css-parse-something-according-to-a-css-grammar)* with *parse something according to a CSS grammar* but this algorithm does not expect a high level rule object resulting from consuming stylesheet's contents
* [x] **fixed in https://github.com/w3c/csswg-drafts/commit/6ab5888d05ef3f445e26a1ca81081a7b912016ed** - [CSS Cascade](https://drafts.csswg.org/css-cascade-4/#fetch-an-import) defines *fetch an `@import`* (removed in Cascade 5) with a step that includes [parse a stylesheet](https://drafts.csswg.org/css-syntax-3/#parse-a-stylesheet) ~~but it would be helpfull to note that only the basic syntax will be validated, or to define that [parse a CSS style sheet](https://drafts.csswg.org/css-syntax-3/#css-stylesheets) should be used, otherwise the list of rules will not be parsed against the context rules or the corresponding grammar for the rule (only the basic syntax will be validated and the nested rule's contents will be left unparsed as a list of component values).~~
* [ ] [CSS Syntax does not define](https://drafts.csswg.org/css-syntax-3/#parse-a-css-stylesheet) where/when *parse a CSS stylesheet* should run (dedicated issue: [#2997](https://github.com/whatwg/html/issues/2997)) and its return value.
* [x] **invalid: see #9173** - [CSS Media Queries](https://drafts.csswg.org/mediaqueries/#typedef-media-query-list) defines the procedure to parse `<media-query-list>` with *parse a comma-separated list of component values* composed with *parse something according to a CSS grammar* using `<media-query>`, but it should use *parse a comma-separated list according to a CSS grammar* using `<media-query>` instead, therefore parsing a whitespace as input (eg. from `Element.media`) to an empty list instead of `not all` ([as defined in *parse a media query or a media query list*](https://drafts.csswg.org/cssom/#parsing-media-queries)), and it should define this production specific rule: an empty list should default to `all` and an invalid `<media-query>` should default to `not all`.

<!-- comment id=1041112765 author=cdoublev created=2022-02-16T05:06:07Z url=https://github.com/w3c/csswg-drafts/issues/7040#issuecomment-1041112765 -->
## @cdoublev — 2022-02-16

I improved the sorting in my previous comment to put the less important items first. I removed two items that already have a dedicated issue and didn't provide additional details, and added one about parsing `@page`.

<!-- comment id=1563578139 author=tabatkins created=2023-05-25T22:18:48Z url=https://github.com/w3c/csswg-drafts/issues/7040#issuecomment-1563578139 -->
## @tabatkins — 2023-05-25

Fixed the Counter Styles issue. (and marked it as completed in your list) I consider the change editorial; I was just using bespoke language for the operation rather than invoking the algorithm designed for this.
