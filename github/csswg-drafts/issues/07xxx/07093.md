---
number: 7093
title: "[css-fonts-4] Color fonts are not enabled by default"
state: "closed"
labels: ["css-fonts-4"]
author: "litherum"
created_at: "2022-02-28T23:57:58Z"
closed_at: "2022-03-08T02:59:39Z"
url: "https://github.com/w3c/csswg-drafts/issues/7093"
comments: 5
has_resolution: false
synced_at: "2026-07-04T04:44:11Z"
---

`font-palette` indicates that its initial value is `normal`. The prose for `normal` [says](https://drafts.csswg.org/css-fonts-4/#valdef-font-palette-normal):

> User-Agents attempt to treat the color font as closely as possible to a non-color font.

This is kind of unfortunate, as it means that color fonts, by default, will be rendered 'as closely as possible to a non-color font.'

In WebKit, we'll just use the 0th palette by default. I don't know what other browsers do (@drott) but if they do the same then we probably should adjust the description of `normal`.

<!-- comment id=1055706417 author=svgeesus created=2022-03-01T17:57:44Z url=https://github.com/w3c/csswg-drafts/issues/7093#issuecomment-1055706417 -->
## @svgeesus — 2022-03-01

I agree, the initial value should probably be `auto` which should mean the UA decides whether to display as color or monochrome based on whatever heuristics they choose. The language about "best legibility" should probably stay.

Implies that we do need another value which means `monochrome`. And `normal` does not really convey that. 

<!-- comment id=1055779735 author=drott created=2022-03-01T19:24:27Z url=https://github.com/w3c/csswg-drafts/issues/7093#issuecomment-1055779735 -->
## @drott — 2022-03-01

We generally prioritise color tables (`COLR` (v0 and v1), `SBIX`, `CBDT/CBLC`) over contour tables (`glyf`, `CFF2`) when present. Thus, if COLR is present and valid, we do not "render it as close as possible to a non-color font" even if it would have `glyf` glyphs for the same glyph ids. We do use the default palette at index 0 as well, same as WebKit.

In fact, I find the whole paragraph a bit odd. Not sure what its origins are. I haven't seen any implementation synthesising a custom palette for the default rendering.

> User-Agents attempt to treat the [color](https://drafts.csswg.org/css-color-4/#propdef-color) font as closely as possible to a non-color font. In particular, user-agents use a palette which will yield the best default result for reading. User-agents may take the computed value of the color property into consideration when making this decision. User agents may also construct and use a palette not defined in the font."

I am in favour of just replacing the description for `normal` with something like:

User-Agents display the color font with the default palette or default glyph colorisation. User-agents should/must (WDYT?) take the computed value of the color property into consideration when a color font format requires use of the foreground color (for example: color index 0xFFFF in COLR). 

Then add a note: "For COLR/CPAL this usually means rendering the font with the palette at index 0."

Regarding:

>  User agents may also construct and use a palette not defined in the font.

Yes, that's what we do when a `@font-palette-values` provide custom palette overrides, but I don't think this sentence needs to be in the spec text for `normal`.

<!-- comment id=1055865595 author=litherum created=2022-03-01T21:16:15Z url=https://github.com/w3c/csswg-drafts/issues/7093#issuecomment-1055865595 -->
## @litherum — 2022-03-01

+1 to @drott.

I'm not sure how one would implement a potential `monochrome` value on Core Text.

<!-- comment id=1056615653 author=ByteEater-pl created=2022-03-02T08:59:37Z url=https://github.com/w3c/csswg-drafts/issues/7093#issuecomment-1056615653 -->
## @ByteEater-pl — 2022-03-02

> User-Agents attempt to treat the color font as closely as possible to a non-color font.

That (and some other text there) doesn't look very prescriptive (normative), rather descriptive. Could use some editorial massaging.

<!-- comment id=1061101964 author=litherum created=2022-03-07T20:24:52Z url=https://github.com/w3c/csswg-drafts/issues/7093#issuecomment-1061101964 -->
## @litherum — 2022-03-07

Pull request: https://github.com/w3c/csswg-drafts/pull/7110
