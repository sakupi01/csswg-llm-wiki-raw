---
number: 7417
title: "[css-counter-styles-3] Invalid `initial` of `negative`, `prefix`, `suffix`"
state: "closed"
labels: ["Closed Accepted as Editorial", "Commenter Satisfied", "css-counter-styles-3"]
author: "cdoublev"
created_at: "2022-06-27T07:42:05Z"
closed_at: "2023-05-25T22:12:33Z"
url: "https://github.com/w3c/csswg-drafts/issues/7417"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:30Z"
---

In the context of automatic parsing of a CSS value against a value definition, the `initial` field of the [`negative` descriptor](https://drafts.csswg.org/css-counter-styles-3/#descdef-counter-style-negative) should be defined with a value that can be parsed against the `value` field. But the `initial` field is defined with *"\2D" ("-" hyphen-minus)*.

I suggest using `"\2D"` or just `"-"` (I believe [tokenization](https://drafts.csswg.org/css-syntax-3/#consume-string-token) will replace `\\2d` by `-`) and to define in prose that it corresponds to *("-" hyphen-minus)*, or to use some kind of markup (containing *("-" hyphen-minus)*) that provides additional details that `w3c/reffy` can read to separate it from the `initial` value.

FYI, @tidoust.

**EDIT:** the same issue applies to `prefix`, defined with *"" (the empty string)*, and `suffix`, defined with *"\2E\20" ("." full stop followed by a space)*.

<!-- comment id=1563569888 author=tabatkins created=2023-05-25T22:12:33Z url=https://github.com/w3c/csswg-drafts/issues/7417#issuecomment-1563569888 -->
## @tabatkins — 2023-05-25

Done. This is editorial so no WG confirmation needed. I also just switched to writing them with the actual characters rather than the escapes, they're all ASCII so I'm not sure why I was being weird about it.

Assuming commenter satisfied, since I did exactly what was asked. ^_^
