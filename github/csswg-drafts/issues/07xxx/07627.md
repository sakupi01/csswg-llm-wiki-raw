---
number: 7627
title: "[scroll-animations-1] `scroll-timeline` and `view-timeline` shorthand syntax"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "Closed Accepted by Editor Discretion", "scroll-animations-1"]
author: "bramus"
created_at: "2022-08-19T10:14:18Z"
closed_at: "2022-12-13T22:40:08Z"
url: "https://github.com/w3c/csswg-drafts/issues/7627"
comments: 8
has_resolution: true
synced_at: "2026-07-04T04:44:22Z"
---

The syntax for [the `scroll-timeline` shorthand](https://drafts.csswg.org/scroll-animations-1/#scroll-timeline-shorthand) is defined as such:

```css
scroll-timeline: <'scroll-timeline-axis'> || <'scroll-timeline-name'>
```

The syntax for [the `view-timeline` shorthand](https://drafts.csswg.org/scroll-animations-1/#view-timeline-shorthand) is defined as such:

```css
view-timeline: [<'view-timeline-name'> || <'view-timeline-axis'>]#
```

It’s odd that the `scroll-timeline` has the name mentioned first, while in `view-timeline` it is the axis. Even though the order of both does not matter – due to `||` – I would like to see these listed in the same order for consistency reasons.

---

Going further, maybe we should disallow reordering the values? There is precedent to have the `*-name` first: [the `container` shorthand](https://www.w3.org/TR/css-contain-3/#propdef-container) has the `container-name` first and then the `container-type`.

```css
container: <'container-name'> [ / <'container-type'> ]?
```

Maybe we can update the `scroll-timeline` and `view-timeline` shorthands to follow the form of the `container` shorthand? Like so:

```css
scroll-timeline: <'scroll-timeline-name'> [ / <'scroll-timeline-axis'> ]?
```
```css
view-timeline: [<'view-timeline-name'> [ / <'view-timeline-axis'> ]?]#
```

<!-- comment id=1237917878 author=andruud created=2022-09-06T09:44:10Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1237917878 -->
## @andruud — 2022-09-06

Yeah, it's weird that the order is reversed. Is that intentional @fantasai?

Especially since we don't disallow block/inline/etc from &lt;custom-ident>, so:

- `view-timeline:block` creates a timeline named `block`, with the default axis.
- `scroll-timeline:block` creates no timeline, since it has no name.

See also https://github.com/w3c/csswg-drafts/pull/7694 (which doesn't address the ordering, but it's related).

<!-- comment id=1239776904 author=fantasai created=2022-09-07T19:13:51Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1239776904 -->
## @fantasai — 2022-09-07

Pretty sure that's not intentional, I agree we should fix it to be consistent.

<!-- comment id=1239786388 author=fantasai created=2022-09-07T19:25:30Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1239786388 -->
## @fantasai — 2022-09-07

@bramus Flipped ordering of scroll-timeline as you suggested. I agree we should discuss fixing the ordering, since reorderability is creating an unfortunate parsing ambiguity here. :(

<!-- comment id=1280553623 author=cdoublev created=2022-10-17T09:24:28Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1280553623 -->
## @cdoublev — 2022-10-17

> Going further, maybe we should disallow reordering the values? There is precedent to have the `*-name` first: the `container` shorthand has the `container-name` first and then the container-type.

`animation` has `animation-name` last (conflict with `animation-fill-mode` for `none`).

---

> See also #7694 (which doesn't address the ordering, but it's related).

I presume that the topic related to this PR is about whether `none` can repeat. Some context: `none` can repeat in `animation` for `animation-name` (but the `<custom-ident>` for `@keyframes` prelude excludes the `none` keyword) but it cannot in `transition` for `transition-property` (there is a minor [issue](https://github.com/w3c/csswg-drafts/issues/2031) related to how this is enforced).
  
The current `view-timeline-name` syntax does not allow repeating `none` but the current `view-timeline` syntax does. There is also: 

  > If `view-timeline-name` has more names than `view-timeline-axis` has specified axes, the excess timelines use the last `view-timeline-axis` value. If `view-timeline-name` has fewer names than view-timeline-axis has specified axes, the used view-timeline-axis list is truncated.

If this difference is unintentional and `none` should not repeat in `view-timeline` AND `view-timeline-name`, it seems simpler and clearer to enforce it in prose rather than in the syntax (cf. my suggestion in the linked issue related to `transition`).

<!-- comment id=1280750888 author=cdoublev created=2022-10-17T12:04:22Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1280750888 -->
## @cdoublev — 2022-10-17

After reading [this thread](https://lists.w3.org/Archives/Public/www-style/2012Sep/0393.html), I wonder if `none` could not be further restricted to be used alone in `transition` and `view-timeline`.

> First, animation-name: none is basically a special value for
disabling an animation or set of animations; there's really no
reason to use it with a list, only on its own, since when
animation-name is none all the other properties are irrelevant.

Would there be a reason to use `transition: none 1s;` or `view-timeline: none inline`?

`animation: none 1s` would still have to be valid because `none` must be assigned to `animation-fill-mode`.

<!-- comment id=1341707334 author=fantasai created=2022-12-07T22:57:17Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1341707334 -->
## @fantasai — 2022-12-07

Agenda+ to discuss if we want to address the parsing ambiguity by fixing the order of arguments, or something else.

The proposal is to have:
```
scroll/animation-timeline: <name> <axis>?
```
rather than
```
scroll/animation-timeline: <name> || <axis>
```

<!-- comment id=1341797323 author=css-meeting-bot created=2022-12-08T00:26:48Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1341797323 resolution=true -->
## @css-meeting-bot — 2022-12-08

The CSS Working Group just discussed ``[scroll-animations-1] `scroll-timeline` and `view-timeline` shorthand syntax``, and agreed to the following:

* `RESOLVED: Have parsing with fixed order of name and axis`

<details><summary>The full IRC log of that discussion</summary>
&lt;dael> Rossen_: Who can summerize?<br>
&lt;fantasai> -> https://github.com/w3c/csswg-drafts/issues/7627<br>
&lt;dael> bramus: I can. In the spec there's a difference between scroll and view timeline argument order.<br>
&lt;dael> bramus: Proposal was to have them use same syntax<br>
&lt;fantasai> scroll/animation-timeline: &lt;name> || &lt;axis><br>
&lt;fantasai> scroll/animation-timeline: &lt;name> &lt;axis>?<br>
&lt;dael> fantasai: Other point you raised is timeline name and axis are in same namespace. I put this on agenda to ask WG if we shoudl require an order and require name always? We could change the current syntax<br>
&lt;bramus> q+<br>
&lt;dael> fantasai: Other ways to disambig but asking if this is what we want to do or something different<br>
&lt;Rossen_> ack bramus<br>
&lt;dael> bramus: One question on prop new is there is no slash separator but container queries requires one after the name. Seems like a difference<br>
&lt;dael> miriam: Likely b/c you can have multiple names<br>
&lt;bramus> q-<br>
&lt;dael> bramus: Don't think we want to allow multiple names on scroll timeline, do we?<br>
&lt;dael> fantasai: Do it with a comma sep syntax and axises are listed with name. Container property doesn't have that; doesn't tie two properties with lists. Only one type of contianment<br>
&lt;dael> fantasai: Here we have a list of timelines that have their own properties<br>
&lt;dael> flackr: I'm in favor of fixing the order<br>
&lt;bramus> +1<br>
&lt;dael> Rossen_: Any opposing opinions?<br>
&lt;dael> Rossen_: Prop: Have parsing with fixed order of name and axis<br>
&lt;dael> Rossen_: Objections?<br>
&lt;dael> RESOLVED: Have parsing with fixed order of name and axis<br>
&lt;dael> Rossen_: fantasai anything else on this?<br>
&lt;dael> fantasai: nope<br>
</details>

**[Official Minutes](https://lists.w3.org/Archives/Public/www-style/2022Dec/0007.html)**

<!-- comment id=1349931326 author=fantasai created=2022-12-13T22:40:08Z url=https://github.com/w3c/csswg-drafts/issues/7627#issuecomment-1349931326 -->
## @fantasai — 2022-12-13

Fixed in https://github.com/w3c/csswg-drafts/commit/c57ee4f2be513edcb89974c0f861578d7b13c551
