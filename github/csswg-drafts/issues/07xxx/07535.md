---
number: 7535
title: "[css-cascade] New syntax for making an entire layer important"
state: "open"
labels: ["css-cascade-5"]
author: "simevidas"
created_at: "2022-07-25T22:41:53Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/7535"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:44:39Z"
---

I have some user styles (Stylus extension) that I’m enforcing on a website. For example:

```css
.sticky-header {
  position: static !important;
}

.video-popup {
  display: none !important;
}
```

Instead of writing `!important` in every declaration, I would prefer if I could just put my styles inside an important layer:

```css
@layer stylus-extension !important {
  .sticky-header 
     position: static;
  }

  .video-popup {
    display: none;
  }
}
```

The longer the user styles are, the worse the `!important` repetition becomes. This repetition could be avoided with a new syntax.

<!-- comment id=1670605828 author=yisibl created=2023-08-09T03:40:43Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1670605828 -->
## @yisibl — 2023-08-09

So similarly, would `!important` in other at-rule work? e.g.

```css
@keyframes foo !important { }
```

<!-- comment id=1671507627 author=simevidas created=2023-08-09T14:33:55Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1671507627 -->
## @simevidas — 2023-08-09

I don’t think `!important` is valid in animation declarations.

<!-- comment id=1671548993 author=Loirooriol created=2023-08-09T14:47:59Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1671548993 -->
## @Loirooriol — 2023-08-09

https://drafts.csswg.org/css-animations-1/#keyframes

> None of the properties interact with the cascade (so using `!important` on them is invalid and will cause the property to be ignored)

<!-- comment id=1671605980 author=yisibl created=2023-08-09T15:15:29Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1671605980 -->
## @yisibl — 2023-08-09

I mean, maybe we need to support `!important` in `@keyframes` as well.

My coworker ran into this scenario today. A third-party library defines the `@keyframes rote {}` rule, and it just so happens that we have `@keyframes` with the same name on our page, and the library renders asynchronously via JS, which results in broken animations on our page, and there's no good way to override the library's `@keyframes` in CSS.

Alternatively, we could override the triple styles in a similar way, as proposed in this issue:

```css
@layer foo !important {
  @keyframes rote {}
}
```

<!-- comment id=1671992757 author=simevidas created=2023-08-09T19:08:53Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1671992757 -->
## @simevidas — 2023-08-09

I don’t think `!important` is the correct solution for name collisions. The purpose of `!important` is to increase a declaration’s priority in the cascade. Animation declarations cannot be `!important`.

<!-- comment id=1850925063 author=mirisuzanne created=2023-12-11T21:34:42Z url=https://github.com/w3c/csswg-drafts/issues/7535#issuecomment-1850925063 -->
## @mirisuzanne — 2023-12-11

I the `@keyframes` issue can already be resolved using layers, without any need for importance. Naming conflicts between at-rules are already resolved using layer order.  Importance doesn't add much to that use-case.

For the OP use-case (injected user-styles not in the user stylesheet), that is the same use-case that lead to reopening #6323. We may only need one of these features, and not the other?

Beyond this specific use-case, I think it's generally better to keep styles grouped by their purpose rather than their importance. E.g. we should be putting `!important` on specific properties in the `component` layer block, rather than creating a new `component !important` block. Are there other use-cases where an entire group of styles really should be easily marked as important?
