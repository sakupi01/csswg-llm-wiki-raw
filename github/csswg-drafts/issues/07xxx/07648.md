---
number: 7648
title: "[css-images] Mesh gradient / Freeform gradient / 2D gradient"
state: "open"
labels: ["Agenda+", "css-images-5"]
author: "JoshTumath"
created_at: "2022-08-24T23:51:46Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/7648"
comments: 76
has_resolution: false
synced_at: "2026-09-25T16:48:55Z"
---

**EDIT: For those catching up on the issue, please read the Explainer doc.**

[**You can read the 2D Gradients Explainer here.**](https://github.com/JoshTumath/Explainers/blob/main/css-2d-gradients/explainer.md)

---

**Mesh gradients** and **freeform gradients** are becoming a popular design feature, and it would be useful to be able to generate mesh gradient images dynamically with a function in CSS. The nomenclature of 'mesh' vs 'freeform' seems to get conflated, but they are different things in tools like Adobe Illustrator.

Here's an example of a complex mesh gradient from our branding kit at the BBC:

![](https://user-images.githubusercontent.com/1760227/186539545-2692dde8-9f09-45b2-bde4-042352f366d9.jpg)

## Why do we need a new feature?

[Tools such as CSS Hero try to replicate mesh gradients](https://csshero.org/mesher/), but they only 'fake' it by layering radial gradients. You wouldn't be able to make a complex mesh like the one above using radial gradients.

## How they work

### Creating the gradients

**Freeform gradients** are generated by specifying any number of nodes at arbitrary coordinates on a canvas. Each node is given a colour.

**Mesh gradients** seem to be more complex, and work using a 2D grid of nodes. The grid lines can be bent using bezier curves.

It's easier to understand how they work by playing around with them yourself. Here are some tools and videos to explain them better:

* [Freeform gradient tool that generates them in a HTML canvas](https://meshgradient.com/)
* [YouTube video that explains how both mesh and freeform gradients are generated in Adobe Illustrator](https://www.youtube.com/watch?v=8hJlffeGkmo)

### How they are created

See https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2157943896

## Possible syntax

For creating a freeform gradient, the gradient function could have a comma-separated list of coloured coordinates/nodes. I'm suggesting using `<length-percentage>` units for consistency with other gradient function syntaxes.

```css
background-image: freeform-gradient(red 2% 2%, gold 60% 40%, yellow 90% 100%);
```

The syntax definition would be the following: (Thanks @SebastianZ)

```
freeform-gradient([<color> <length-percentage>{2}]#{2,})
```

I don't have a proposal for a mesh gradient syntax because they're more complex. EDIT: See discussion in the comments about how mesh gradients might be better represented using SVG markup.

<!-- comment id=1226959721 author=faceless2 created=2022-08-25T08:40:12Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1226959721 -->
## @faceless2 — 2022-08-25

There was a `<meshgradient>` element suggested for SVG2, although it's been withdrawn. It remains in the svg-next proposals,  see https://svgwg.org/svg-next/pservers.html#MeshGradients. It was originally proposed by the Inkscape team.

We've implemented it, it's complex and trying to represent it as a function would be horrendous. If it ever makes it back into SVG it will be usable as a `background-image` and I would suggest that's probably the best approach.

For future reference, these are also called "Coons Patch" (or "tensor-product patch" mesh gradients, for the bicubic interpolation mode) and the maths is all defined in section 8.7.4.5.6 of the PDF spec, older versions of which are still publicly available. The SVG model is almost identical.

<!-- comment id=1227036064 author=JoshTumath created=2022-08-25T09:48:40Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1227036064 -->
## @JoshTumath — 2022-08-25

Thanks @faceless2 for that background on the SVGWG's proposal. It sounds like the amount of data you have to specify to create a Coons Patch mesh gradient is quite complex, so using SVG images rather than a function in CSS to make one sounds like a better approach.

As for what Adobe calls 'freeform gradients', the way they are specified seems much simpler. I would be interested to see if there is a published spec or mathematical model for them.

<!-- comment id=1227734141 author=SebastianZ created=2022-08-25T20:33:26Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1227734141 -->
## @SebastianZ — 2022-08-25

> We've implemented it, it's complex and trying to represent it as a function would be horrendous.

@JoshTumath already provided an example for a more simplistic approach.

Written in CSS syntax, it would be

```
mesh-gradient([<length-percentage>{2} <color>]#{2,})
```

I assume that would restrict the interpolation between the color stops to be linear, though that probably already covers a lot of use cases.
And the syntax could still be extended in the future to allow Coons Patches if required.

As I understand it, Adobe's freeform gradients are restricted to four colors, which probably also cover many use cases but it's a big restriction if you want to create arbitrary gradients.

Sebastian

<!-- comment id=1228363728 author=JoshTumath created=2022-08-26T11:10:18Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1228363728 -->
## @JoshTumath — 2022-08-26

> As I understand it, Adobe's freeform gradients are restricted to four colors, which probably also cover many use cases but it's a big restriction if you want to create arbitrary gradients.

If that was true in the past, it looks as though that restriction doesn't exist in Illustrator any more. A designer in my team, Ollie Buchanan, just created this example that has 7 points:

![image (4)](https://user-images.githubusercontent.com/1760227/186891192-25a3119e-bb61-47d4-abbd-f2be11234652.png)

<!-- comment id=1228519542 author=faceless2 created=2022-08-26T13:53:37Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1228519542 -->
## @faceless2 — 2022-08-26

> @JoshTumath already provided an example for a more simplistic approach.
> I assume that would restrict the interpolation between the color stops to be linear, though that probably already covers a lot of use cases.
> And the syntax could still be extended in the future to allow Coons Patches if required.

If we're going to define a 2D gradient syntax I would rather we do it once, in such a way that it covers the options. Make the simple things easy and the difficult things possible.

So, fond as I am of CSS, I'm not sure it's the right place for this. There are all sorts of interesting uses for 2D gradients and the end result of all of them is going to be a very large function with hundreds of arguments. CSS tooling isn't designed for these - they're going to be impossible to prototype, edit or understand by hand.

If a concept requires structure to usefully represent it then we should start with a structured container, like XML (in the form of SVG), rather than trying to force it into a function where it will be represented as a very long list of numbers.

<!-- comment id=1229284742 author=bradkemper created=2022-08-27T22:56:06Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-1229284742 -->
## @bradkemper — 2022-08-27

The simple, free form version in the syntax suggested seems reasonable to me. 

<!-- comment id=2157943896 author=svgeesus created=2024-06-10T10:16:05Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2157943896 -->
## @svgeesus — 2024-06-10

Freeform meshes take a set of points and then perform a [Delauney trianglation](https://en.wikipedia.org/wiki/Delaunay_triangulation) to produce a mesh. They then use [bilinear interpolation](https://en.wikipedia.org/wiki/Bilinear_interpolation) to generate the intermediate colors.

Thus, our current linear gradients are **1D linear gradients**, and freeform are **2D linear gradients**.

See also 

 - https://people.eecs.berkeley.edu/~jrs/meshpapers/delnotes.pdf
 - https://www.jstor.org/stable/43693155

<!-- comment id=2157972915 author=svgeesus created=2024-06-10T10:28:00Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2157972915 -->
## @svgeesus — 2024-06-10

Skia supports mesh gradients and GLSL also.

 - https://meshgradient.com/

<!-- comment id=2158233133 author=LeaVerou created=2024-06-10T12:37:10Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2158233133 -->
## @LeaVerou — 2024-06-10

+1. I think this syntax works, covers a lot of mesh gradient use cases, and is much easier to implement than mesh gradients. 

Notably, it enables this use case which is currently impossible (any color pickers on the Web fake it by overlaying semi-transparent gradients or use WebGL):

<img width="646" alt="image" src="https://github.com/w3c/csswg-drafts/assets/175836/e4f83329-cc68-4c6f-9f8f-8310e7c59cda">

Based on recent resolutions and syntax, I’d adjust the grammar to:

```
freeform-gradient( [<color-interpolation-method>]?, [ <color> <bg-position> ]#{1,} )
```

Changes:
- We want `<color-interpolation-method>` on every gradient
- We [recently resolved](https://github.com/w3c/csswg-drafts/issues/10092#issuecomment-2145860054) to allow gradients with 1 color stop and no positions
- Using `<bg-position>` also allows keywords, with established rules about defaulting.

<!-- comment id=2158929389 author=ydaniv created=2024-06-10T17:31:48Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2158929389 -->
## @ydaniv — 2024-06-10

Wix also has this tool, kinda like CSS Hero's Mesher, that overlays radial gradients - called Fluid Gradients:

<img width="1321" alt="image" src="https://github.com/w3c/csswg-drafts/assets/116658/d60346c9-07e5-437c-8b8c-3db521e71a2d">

It's like ~3 years old now, though our intent was actually faking mesh gradients.

I also tried playing with voronois on canvas and faking the bilinear interpolation by adding blur on top of it:
https://codepen.io/ydaniv/pen/VwMrKme?editors=0010

I think it's somewhat close to the syntax above, generating points, triangulate using Delanuay, interpolate.
Big +1!

<!-- comment id=2161289085 author=neneodonkor created=2024-06-11T17:37:10Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2161289085 -->
## @neneodonkor — 2024-06-11

This will be a cool and needful feature. 

<!-- comment id=2161686417 author=SebastianZ created=2024-06-11T22:10:02Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2161686417 -->
## @SebastianZ — 2024-06-11

Given the very positive feedback from outside and inside the working group and @LeaVerou's [concrete proposal](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2158233133), I think it's ready to be discussed on a call.

Sebastian

<!-- comment id=2162006758 author=bernaferrari created=2024-06-12T02:54:02Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2162006758 -->
## @bernaferrari — 2024-06-12

Apple has now implemented Mesh Gradient in SwiftUI: https://developer.apple.com/documentation/SwiftUI/MeshGradient

<!-- comment id=2163141171 author=xmacinfo created=2024-06-12T14:20:21Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2163141171 -->
## @xmacinfo — 2024-06-12

Good use cases for mesh gradients are heatmaps displayed as overlays on web pages.

I do not have any example screen capture, but searching for “heatmap” Google should return a lot of examples.

<!-- comment id=2163539435 author=JoshTumath created=2024-06-12T17:14:02Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2163539435 -->
## @JoshTumath — 2024-06-12

> Apple has now implemented Mesh Gradient in SwiftUI: https://developer.apple.com/documentation/SwiftUI/MeshGradient

Thank you. And this seems like a true 'mesh gradient' like in Adobe Illustrator.

On a side note, earlier in the issue, we discounted mesh gradients as being too complex to implement in CSS and maybe more suited for future versions of SVG (which are unlikely to happen, given browser vendors' current stance on SVGs 😞). But I'd be interested to see if anyone would be willing to attempt a syntax for mesh gradients - maybe using the `<cubic-bezier-easing-function>` type.

It could be similar to the SwiftUI function, where the first argument is a matrix specifying the grid and the second is a corresponding matrix of colours.

<!-- comment id=2163568107 author=JoshTumath created=2024-06-12T17:30:19Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2163568107 -->
## @JoshTumath — 2024-06-12

I published [a blog post](https://www.joshtumath.uk/posts/2024-06-11-mesh-gradients-in-css/) about this yesterday. I'd thought of some questions around the details of how a freeform gradient function might work:

#### The name

Is the name right? Is 'freeform gradient' just an Adobe term?

#### Spread

Adobe Illustrator supports increasing the spread of the colour stops (i.e. how dominant the colour is). How would we specify that? Could each colour stop have an optional `<percentage>` or `<length-percentage>` value for specifying this?

```css
background-image: freeform-gradient(
  in oklab,
  red top left / 2%,
  gold 60% 40% / 4px,
  yellow bottom right %5
);
```

#### Specifying lines

Adobe Illustrator supports setting lines as well as points as colour stops. Is that something we would want to support in CSS, too? These would be some kind of linear path bezier path.

I know we have the `<cubic-bezier-easing-function>`, but that only accepts four arguments and doesn't allow you to create a continuous line with many bends on a set of coordinates. I imagine a new kind of positioning function might be needed. Maybe something like the `path()` function used in CSS Shapes?

#### Animation

We often see apps and websites use animated mesh gradients. I'd imagine this is computationally expensive to do unless it's pre-rendered.

I suppose, with this proposal, you could animate a freeform gradient by registering CSS custom properties using `@property` and animating the value changing.

<!-- comment id=2166181480 author=LeaVerou created=2024-06-13T16:36:01Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2166181480 -->
## @LeaVerou — 2024-06-13

Wrt implementability, we need to investigate whether the graphics libraries used by browsers today have a primitive that makes this easy. E.g. can mesh gradients emulate freeform gradients? What about the libraries beyond Swift UI?

<!-- comment id=2169021061 author=chriskirknielsen created=2024-06-15T01:19:13Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2169021061 -->
## @chriskirknielsen — 2024-06-15

I love this proposal, it would be a nice addition! As far as naming goes, I had `cloud-gradient` in mind (though I like `freeform` and `mesh`, too), as you're kind of painting soft blobs that look like clouds.

And to @JoshTumath's point, if spread were an option, we could end up creating really interesting and organic backgrounds (see [Stripe's homepage](https://stripe.com/) for inspiration), maybe even end up painting something that looks like cells/scales, a bit Voronoi noise-like, perhaps?

<!-- comment id=2173861488 author=AmeliaBR created=2024-06-17T16:37:13Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2173861488 -->
## @AmeliaBR — 2024-06-17

Thanks for proposing this up @JoshTumath, and for your [summary article](https://www.joshtumath.uk/posts/2024-06-11-mesh-gradients-in-css/) bringing more attention. 

As others have mentioned, a mesh gradient proposal was [previously added to SVG](https://www.w3.org/TR/2016/CR-SVG2-20160915/pservers.html#MeshGradients) & was implemented in Inkscape. I have never liked that syntax. It is both far too verbose to write by hand and (IMO) simplified in ways that obscure the logical structure. I like the proposed array of 2D colour points much better.

But, the SVG proposal is a direct representation of the model used in PDF and Illustrator & implemented in many rendering engines.  Therefore, for ease of implementation, any CSS 2D gradient should be rendered in terms of an equivalent Coons-patch mesh gradient.

I assume that is true for the Adobe Illustrator "freeform" gradient. It would be helpful if someone from Adobe could confirm that & describe how the points are converted into a mesh. @svgeesus mentioned that it used Delauney triangulation, but are those triangles then somehow arranged into rows and columns? Does the "spread" factor affect the triangulation, or is it used like a colour hint to adjust the colour interpolation along the triangle edges, according to the relative weights of the spread factors at each vertex?

Before settling on a syntax, it is also worth considering the full potential of mesh gradients, so that CSS can be intentional about which subset it supports and why. Josh's blog post mentions that in addition to simple points, Adobe's "freeform" tool supports colour stops defined as curves or lines of solid colour. Should that also be supported?

The scope of the feature also influences the name. We shouldn't borrow Adobe's name if we're not going to fully represent their feature (and their feature set might change). If we might in future add a more mesh-like mesh gradient, we shouldn't use that name for the simple version. For the array of 2D colour stop points, I'd prefer a descriptive name like `points-gradient()` or `scatterplot-gradient()`. But if some of the colour stops are curves, then a more generic name might be better.

So, let's consider the potential scope. To quickly summarize how these Coons-patch mesh gradients work:

- Gradient colour stops are positioned at the intersections of set of row and column lines. 
- Each line can actually be a curve, or angled in any direction, so they might not end up being vertical and horizontal, but the row and column relationships still define how colours are interpolated along the line and in between them.
- In between the lines, there is very complex but clearly defined math for how the colours fill in that grid cell, which is called a patch.
- The patches (grid cells) can be all sorts of shapes, so long as each can be described by a boundary of four Bézier path components (straight lines or single curve commands) connecting four colour stop positions.
- Triangular patches can be created by making one of the four colour stops overlap with another, with a zero-length path in between.
- Radial and conic-gradient effects can be created by bending either the row or column lines around into a loop where the end points overlap the starting points.

The markup structure from the SVG proposal is inspired by HTML table markup: It defines rows of cells (patches). The columns are implicit, based on the number of patches per row. Each patch logically has four stop points, but some of them are implicitly borrowed from the previous column in the row or row in the column. Each stop point also defines the curve of a segment of one of the row/column lines reaching to the next point. Figure from the spec shows the order in which each patch edge is defined, skipping the implicit ones:

![A square-ish shape with wavy edges. It is bright red in the centre, with the red extending to the middle of the four wavy sides. The four corners are light blue. The rest is filled in with smoothly interpolated shades of purple. Arrows have been overlaid on the four quadrants of the shape: the top-left corner has an arrow doing a squarish loop from top left of the full shape to top middle, centre, left middle, then back to top left. The top-right and bottom-left arrows only do three-quarters of a loop to avoid overlapping the previous loop. The bottom-right arrow only covers two edges of that quadrant.](https://github.com/w3c/csswg-drafts/assets/9876129/7ec03efe-4f57-4451-8dcc-ac95312fbf37)

<details>
<summary>Markup for that gradient</summary>
<pre><code>&lt;meshgradient x="50" y="50" id="example"&gt; &lt;!-- x, y used for initial point in first patch. --&gt;
  &lt;meshrow&gt; &lt;!-- No attributes, used only to define begin/end of row. --&gt;
	&lt;meshpatch&gt;
	  &lt;stop path="c  25,-25  75, 25  100,0" stop-color="lightblue" /&gt;
	  &lt;stop path="c  25, 25 -25, 75  0,100" stop-color="purple" /&gt;
	  &lt;stop path="c -25, 25 -75,-25 -100,0" stop-color="red" /&gt;
	  &lt;stop path="c -25,-25, 25,-75"        stop-color="purple" /&gt; &lt;!-- Last point not needed (closed path). --&gt;
	&lt;/meshpatch&gt;
	&lt;meshpatch&gt;
	  &lt;stop path="c  25,-25  75, 25  100,0" /&gt; &lt;!-- stop-color from previous patch. --&gt;
	  &lt;stop path="c  25, 25 -25, 75  0,100" stop-color="lightblue" /&gt;
	  &lt;stop path="c -25, 25 -75,-25"        stop-color="purple" /&gt; &lt;!-- Last point not needed (closed path). --&gt;
	  &lt;!-- Last path (left side) taken from right side of previous path (with points reversed). --&gt;
	&lt;/meshpatch&gt;
  &lt;/meshrow&gt;
  &lt;meshrow&gt; &lt;!-- New row. --&gt;
	&lt;meshpatch&gt;
	  &lt;!-- First path (top side) taken from bottom path of patch above. --&gt;
	  &lt;stop path="c  25, 25 -25, 75  0,100" /&gt; &lt;!-- stop-color from patch above. --&gt;
	  &lt;stop path="c -25, 25 -75,-25 -100,0" stop-color="purple" /&gt;
	  &lt;stop path="c -25,-25, 25,-75"        stop-color="lightblue" /&gt; &lt;!-- Last point not needed (closed path). --&gt;
	&lt;/meshpatch&gt;
	&lt;meshpatch&gt;
	  &lt;!-- First path (top side) taken from bottom path of patch above (with points reversed). --&gt;
	  &lt;stop path="c  25, 25 -25, 75  0,100" /&gt; &lt;!-- stop-color from patch above. --&gt;
	  &lt;stop path="c -25, 25 -75,-25"        stop-color="lightblue" /&gt; &lt;!-- Last point not needed (closed path). --&gt;
	  &lt;!-- Last path (left side) taken from right side of previous path (with points reversed). --&gt;
	&lt;/meshpatch&gt;
  &lt;/meshrow&gt;
&lt;/meshgradient&gt;
  </code></pre>
</details>

I don't like that the mesh curves are defined piece-meal for each stop. I don't like that the number of stops and curves per patch varies according to which row and column it is in. I don't like that the point defined within the stop geometry (the end-point of the curve segment) is *not* the point that receives the colour defined by that stop. I don't like that if you want a more complex curve between stops (more than a single path segment) you need to split a row or column in two and figure out the in-between colour not just for that segment, but for all the others. I don't like that it doesn't include colour-hints or interpolation colour spaces to adjust the colour change along each curve segment (those could probably be added, but might require changes to rendering engines). I don't like that if you want to create a radial or conic effect, you may nonetheless need to duplicate stops and segments, with the segment curve reversed.

As I mentioned at the top: I really don't like this syntax! It's only really suitable as output from other software.

But I do like all the results you can get from editing the mesh-line structure in a visual editor! Here are some examples I created in Inkscape for a future-focused sidebar in the book [_Using SVG with CSS3 and HTML5_](https://oreillymedia.github.io/Using_SVG/ch12-fill-files/):
![Two shapes. The first is a dark blue circle surrounded by a thick ring of swirling glossy light blue colour, creating an effect something like an eye. The colour changes in the swirling ring are at times soft, at times sharp along a curved ridge. The second shape is a five-point star where each point has a symmetrical progression of colours from the centre to the tip, with crisp angles matching the ins and outs of the star points.](https://github.com/w3c/csswg-drafts/assets/9876129/db5e1a12-7cc6-44e6-8981-28c868aa0bbd)

<!-- comment id=2173983949 author=bernaferrari created=2024-06-17T17:46:24Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2173983949 -->
## @bernaferrari — 2024-06-17

@AmeliaBR how much do you like the Apple syntax? https://developer.apple.com/documentation/SwiftUI/MeshGradient

<!-- comment id=2174118904 author=tabatkins created=2024-06-17T18:30:37Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174118904 -->
## @tabatkins — 2024-06-17

The Apple, syntax, fwiw, has a few forms:

* a list of points, associated with colors: the grid is straight-edged polygons, connecting the points.
* a list of "Bezier points", associated with colors: the grid is connecting the points with beziers; each "point" also contains (up to) 4 control points for the four beziers anchored at this point. (The *other* control point for each bezier is attached to the other "Bezier point".

Both syntaxes specify the width and the height of the grid points array, for interpreting the list into 2d. They allow adding a bg color which is defined as giving the color for any points outside the mesh, and a colorspace for interpolating the colors inside the mesh.

There's also a "smoothsColors" bool, which says it controls "Whether cubic (smooth) interpolation should be used for the colors in the mesh (rather than only for the shape of the mesh).". That appears to suggest that it interpolates the interior grid points via bicubic interpolation (to determine what points on the surrounding beziers contribute to that point, and to what amount), and can optionally do bicubic interpolation on the colors as well (rather than linear interpolation in the provided colorspace).

------------

This syntax solves the most basic problem of the SVG syntax, which is differently-shaped arguments for different points of the grid - rather than define each patch one by one (requiring 2-4 edges), you define the points one by one and the beziers defining each patch are half-defined by each point.

It doesn't solve the "I want to split one patch edge, now I have to manually split *all* the edges in this row/col and manually interpolate their colors" issue, tho. But that's only important if we really want to make this meaningfully human-authorable, and I'm not sure to what extent this even *can* be.

<!-- comment id=2174600950 author=AmeliaBR created=2024-06-17T23:15:04Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174600950 -->
## @AmeliaBR — 2024-06-17

@bernaferrari asked:
> how much do you like the Apple syntax? 

I agree with @tabatkins (and thanks for the summary, Tab): the Swift UI mesh gradient definition solves many of my complaints with the SVG syntax, although there remain some unavoidable complexities from the mesh-gradient structure:

- The grid is defined by the network of points (colour stop positions), not the patches between them.
- The curve control points (or none for straight lines) are clearly associated with each point.
- The order of points and how they relate to rows/columns of the grid is consistent.
- There's no worry about implicit points or curves: control points for curves that don't connect anywhere can be specified and ignored.
- The geometry and colours are given as separate objects, so that the same geometry (stored in a variable) can be re-used with a different colour set.
- But: it still requires an explicit row & column grid to be defined, and is therefore more complicated to author and adjust than the freeform scatterplot of colour points.

I don't like the practice of specifying a 2D structure as a 1D array with width & height attributes to parse it in a grid, but that's common in graphics programming & CSS has a lot more syntactic options to more clearly separate rows of values.

As a potential [CSS value-definition syntax](https://drafts.csswg.org/css-values-4/#value-defs) version of that, we'd be looking at something like:

```
mesh-gradient( [<color-interpolation-method> || <mesh-gradient-method>, ]?
                         mesh-points(<mesh-point-grid>) || mesh-colors(<mesh-color-grid>)  || <base-color> )

<mesh-gradient-method> = bilinear | bicubic

<mesh-color-grid> = <mesh-color>* [ / <mesh-color>* ]* 

<mesh-color> = <color> | auto 

<base-color> = <color>

<mesh-point-grid> = <mesh-point>* [ / <mesh-point>* ]* 

<mesh-point> = [ <position> || auto ] ( <mesh-control-point>#{1,4} )?

<mesh-control-point> = [<length-percentage> <length-percentage>]  | <angle>? && <length-percentage> 
```

The mesh-gradient-method would be bilinear by default, because I think that's simpler to render and I believe it's the default for PDF. (Although I don't really know the difference, so I defer to those who do!)

The four control points in the mesh-point data structure would be ordered & expanded in a TRBL way. Each control point is defined either as relative lengths in the X and Y axis from the point with percentages relative to the distance in that axis to the next point, or (optionally, my proposal) as an angle (defaulting to straight up, right, down, left as appropriate) and a length with percentages relative to the straight-line distance to the adjacent point on that edge. If omitted (or 0 length), the control point would be the point itself, creating a straight-line approach to that point.

(I'm assuming that if CSS adds a logical-directions mode to gradients in general, it would apply as a single keyword that then mirror-flips all geometry as necessary to convert top/bottom to block-start/block-end and right-left to inline-start/inline-end. In other words, I'm not worrying about logical directions at the micro-syntax level.)

Auto positions, or those that are omitted (that is, there aren't enough points in any row/column, or the whole position grid is skipped), would be automatically distributed similar to linear gradients: a first/last value in either axis defaulting to 0 or 100%, other stops without positions would be evenly spaced on each axis. (Which means it might be useful to extend the position syntax to support `auto` in one axis but not the other, but I'm not going to define the Value syntax for that.)

The base color fills in the space outside the mesh geometry (it's called background color in the Swift API, but I didn't want to confuse with CSS background layers: this is a color that is part of the gradient image). It defaults to transparent. Auto colors use the base color for the corner-points of the mesh, or are automatically interpolated from adjacent color points otherwise (exact algorithm to be defined, but the idea is that it would make it much easier to insert an extra row/column into the grid and only specify colours where you want to tweak them).

If colors are omitted (not enough values in each row/column to match the grid points) they would be auto. If you omitted the entire color grid, you'd just get a solid color gradient in the base color.

The only thing missing is a way to snap adjacent color points together to get a sharp color-change stripe. In other CSS gradients, you can do this by setting the position to 0 and letting the fix-up algorithm make the length/angle position match the previous point, since it can't be any less. For a mesh grid, I'm not sure whether that fix-up applies. Maybe there could be another position keyword, similar to auto, but to snap to the previous point in a row/column instead of to distribute points evenly?

That syntax definition is a lot. What would it look like in actual CSS code?

A simple 2D gradient like in @LeaVerou's colour picker example — where you define four corner colors and let the color interpolation space do the rest — would be defined like:

```css
mesh-gradient(oklab, mesh-colors(lime, red / cyan, magenta) );
/* except you'd probably define the corner colors in oklab() functions */
```

The 4-patch / 9-point wavy gradient example from the SVG spec (for which I gave the proposed SVG markup, above) could be defined like this, making using of a variable for the repeated control point geometry of the repetitive wave structure:

```css
--wavy: (-25% -25%, 25% -25%, 25% 25%, -25% 25%) ;
background-image: 
  mesh-gradient(
    mesh-points(
      50px  50px var(--wavy), 150px  50px var(--wavy), 250px  50px var(--wavy) /
      50px 150px var(--wavy), 150px 150px var(--wavy), 250px 150px var(--wavy) /
      50px 250px var(--wavy), 150px 250px var(--wavy), 250px 250px var(--wavy)
    )
    mesh-colors (
      lightBlue, purple, lightBlue /
      purple, red, purple /
      lightBlue, purple, lightBlue 
    )
  );
```

As far as CSS function values go, there's a lot of parts there! But I'd still say it's much more readable (and editable) than the proposed SVG version, and in 1/3 of the code (even if the repetitive geometry helped a lot for this example). I especially like how the colours can easily be read as a grid to get an idea of what it might look like.

The absolute dimensions for the points in this example aren't very CSS-y, but they could easily be replaced by percentages, side keywords, or calc expressions to make them offsets from the sides of the box. Any mix of relative, absolute, and percentage lengths can be resolved down to absolute lengths and positions at used-value time, which would then get sent to a rendering engine that's built for PDF-style mesh gradients.

**If CSS were to add *both* something like this detailed mesh-gradient syntax *and* a simpler freeform/scatterplot syntax, I think it would meet the objective to "make easy things easy and make difficult things possible."**  (But we still need a clear definition of exactly what Adobe Illustrator is doing with those scatterplot points & whether they can be rendered as an equivalent mesh gradient.)

And then SVG would just have to [support CSS gradients as fill / stroke](https://drafts.fxtf.org/fill-stroke/), and it would get them too! 😜

<!-- comment id=2174648497 author=AmeliaBR created=2024-06-17T23:57:27Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174648497 -->
## @AmeliaBR — 2024-06-17

For the CSS WG meeting, if it is decided to pursue this further, people should be tasked to collect the following information:

- What is the best open specification for mesh gradient rendering that is consistent with PDF & other common rendering environments?
- For the Adobe Illustrator team: How exactly are the "freeform" gradients rendered, and is this defined somewhere that can be incorporated freely into CSS?
- For the browser teams & other rendering engines: Do you already have an implementation of mesh gradients for PDF in the browser? Or is one available in the underlying graphics rendering engines you use? What features does it support (bicubic interpolation, multiple colour spaces, etc)?

Basically, the question is: If CSS defines a syntax for mesh gradients, will the CSS layout & rendering engine be able to call on the underlying graphics library to render them with existing code, after converting the CSS syntax into a mesh of absolutely-positioned grid and control points?

<!-- comment id=2174656964 author=bernaferrari created=2024-06-18T00:05:08Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174656964 -->
## @bernaferrari — 2024-06-18

Additional point: mesh is probably (even if slightly) computationally heavy. I saw people on Apple devices using a blur hash transition to the mesh gradient. A fallback API for the blur hash would potentially be useful. https://x.com/DLX/status/1801489961033552336

<!-- comment id=2174736303 author=AmeliaBR created=2024-06-18T01:29:41Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174736303 -->
## @AmeliaBR — 2024-06-18

> A fallback API for the blur hash would potentially be useful

Regardless of whether or not it is useful as a pre-render fallback for mesh gradients, the BlurHash algorithm is used on the web to create placeholders for lazy-loading images. It could be helpful to define it directly in CSS & save a bunch of JavaScript/canvas code.

The BlurHash technique creates a visual effect similar to a 2D gradient, so it seems reasonable to discuss it here. (It is actually a compressed string notation for a very small pixel-grid image, which is then drawn scaled up, letting the rendering environment's image-scaling algorithms blur the pixels together to create a gradient-like effect.) 

See the [BlurHash algorithm](https://github.com/woltapp/blurhash/blob/master/Algorithm.md). That GitHub repo is MIT licensed & has rendering implementations in many languages, so hopefully it would be OK for incorporation into CSS (but I am not a lawyer).

<!-- comment id=2174878571 author=bernaferrari created=2024-06-18T03:09:05Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174878571 -->
## @bernaferrari — 2024-06-18

Technically if Mesh Gradient is implemented in a "fast enough" way (even if at loss of some quality), it would be possible to implement a BlurHash fallback in CSS for images. Would be super super cool. 

<!-- comment id=2176388135 author=JoshTumath created=2024-06-18T15:29:29Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2176388135 -->
## @JoshTumath — 2024-06-18

> For the CSS WG meeting, if it is decided to pursue this further, people should be tasked to collect the following information:
> 
> * For the Adobe Illustrator team: How exactly are the "freeform" gradients rendered, and is this defined somewhere that can be incorporated freely into CSS?

CC @dirkschulze are you able to help us find out how freeform gradients in Illustrator are rendered? Is it still ultimately a mesh gradient or something else entirely?

<!-- comment id=2181135497 author=JoshTumath created=2024-06-20T16:51:18Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2181135497 -->
## @JoshTumath — 2024-06-20

For reference, here is Apple's WWDC talk on SwiftUI's new mesh gradients API.
https://www.youtube.com/watch?v=alhFwkbsxrs&t=492s

<!-- comment id=2340792500 author=drott created=2024-09-10T13:29:50Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2340792500 -->
## @drott — 2024-09-10

Printing might be worth considering: I consider it useful to opt for a gradient algorithm (freeform or mesh) that can be expressed or transformed into primitives of the PDF standard. Otherwise printing mesh gradients becomes lossy, and would have to be broken down into color shape segments (with visible boundaries) or rendered into a bitmap before exporting to PDF or other printing intermediate formats.

<!-- comment id=2345212718 author=smfr created=2024-09-12T03:50:12Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2345212718 -->
## @smfr — 2024-09-12

Presumably we'd want the same [color interpolation rules](https://www.w3.org/TR/css-color-4/#interpolation-space) for mesh gradients as for linear/conic/radial gradients? That makes it harder to use a built-in, system-provided implementation.

<!-- comment id=2402827482 author=css-meeting-bot created=2024-10-09T16:49:35Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2402827482 -->
## @css-meeting-bot — 2024-10-09

The CSS Working Group just discussed `[css-images] Mesh gradient / Freeform gradient / 2D gradient`, and agreed to the following:

* `SUMMARY: We need info on PDF compatility and color interpolation abilities of the options, so we know what is possible to render to screen and print.`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> ChrisL: We need more information here.<br>
&lt;fantasai> ChrisL: 1. PDF compatibility, need to be able to print<br>
&lt;fantasai> ChrisL: 2. Color interpolation possibilities<br>
&lt;fantasai> ChrisL: We need to know that what we produce can be rendered in screen and print<br>
&lt;fantasai> ChrisL: There's discussion of syntax, and no agreement on which we would go for (or both or combine)<br>
&lt;fantasai> ChrisL: Not ready to discuss here, but do need info.<br>
&lt;fantasai> PROPOSED: We need info on PDF compatility and color interpolation abilities of the options, so we know what is possible to render to screen and print.<br>
&lt;fantasai> SUMMARY: We need info on PDF compatility and color interpolation abilities of the options, so we know what is possible to render to screen and print.<br>
</details>

<!-- comment id=2467223278 author=nt1m created=2024-11-11T04:44:21Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2467223278 -->
## @nt1m — 2024-11-11

Came across this tool the other day in case that inspires anyone syntax: https://photogradient.com

<!-- comment id=3993886358 author=brandonmcconnell created=2026-03-03T22:11:18Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-3993886358 -->
## @brandonmcconnell — 2026-03-03

is anyone working on the requirements set by CSSWG?
> * `SUMMARY: We need info on PDF compatility and color interpolation abilities of the options, so we know what is possible to render to screen and print.`

<!-- comment id=3993950969 author=svgeesus created=2026-03-03T22:26:04Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-3993950969 -->
## @svgeesus — 2026-03-03

This sounds like something Adobe could help with. @astearns could you investigate? Perhaps Leonard Rosenthol would be the person to ask?

<!-- comment id=4001910514 author=lrosenthol created=2026-03-05T03:41:04Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4001910514 -->
## @lrosenthol — 2026-03-05

@svgeesus Happy to jump in and help where I can.  @faceless2 is also a good resource here as well, and we can sync up on this topic

<!-- comment id=4793890103 author=bramus created=2026-06-24T21:37:57Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4793890103 -->
## @bramus — 2026-06-24

> is anyone working on the requirements set by CSSWG?
> 
> > * `SUMMARY: We need info on PDF compatility and color interpolation abilities of the options, so we know what is possible to render to screen and print.`

I believe it is described in section 8.7.4 from https://opensource.adobe.com/dc-acrobat-sdk-docs/pdfstandards/PDF32000_2008.pdf

Asking Gemini to interpret things, it told me:

> Most vector design software (like Adobe Illustrator or Inkscape) exports 2D mesh gradients to PDF using Type 6 (Coons patches).

This “Coons patch mesh” defines curved surfaces using 12 control points and 4 corner colors.

<details>
<summary>Full Response</summary>

In the Portable Document Format (PDF) specification, 2D mesh gradients are coded as specialized Shading Patterns (Pattern Type 2). Because mesh gradients require a massive amount of geometric and color data, PDF does not write them out as plain text coordinates. Instead, they are encoded into highly compressed binary data streams, which are then interpreted using a dictionary of bit-mapping rules.

The ISO 32000 PDF specification defines seven shading types, four of which are dedicated to mesh geometries:  

Shading Type | Name | Mechanism
-- | -- | --
Type 4 | Free-form Gouraud triangle mesh | Vertices are placed freely to form triangles; colors interpolate linearly.
Type 5 | Lattice-form Gouraud triangle mesh | Vertices form a grid; triangles are implicitly formed between adjacent rows.
Type 6 | Coons patch mesh | Defines curved surfaces using 12 control points and 4 corner colors.
Type 7 | Tensor-product patch mesh | Uses 16 control points to allow internal warping of the patch.

Most vector design software (like Adobe Illustrator or Inkscape) exports 2D mesh gradients to PDF using Type 6 (Coons patches).

</details>

https://opensource.adobe.com/dc-acrobat-sdk-docs/pdfstandards/PDF32000_2008.pdf#page=202 has more details on this Coons Patch (as well as some visuals and formulas).

---

Asking Gemini for the interpolation specifically, it told me:

> The actual mathematical method used to calculate the gradients across a Coons mesh (Type 6 Shading) is bilinear interpolation for the colors, paired with a specialized surface blending formula for the geometry.

<details>
<summary>Full Response</summary>

The actual mathematical method used to calculate the gradients across a Coons mesh (Type 6 Shading) is **bilinear interpolation** for the colors, paired with a specialized **surface blending formula** for the geometry.

Here is exactly how the PDF renderer handles it:

## 1. Color Interpolation (Bilinear)

In a Coons patch, colors are strictly defined at the four corner vertices of the patch. The PDF renderer assigns a parametric grid to the inside of the patch, using internal coordinates $u$ and $v$, where both range from $0$ to $1$.

To find the exact color at any point $(u, v)$ inside the patch, the renderer applies a standard bilinear interpolation across each color channel defined by your `ColorSpace`. If the four corner colors are $C_{00}, C_{10}, C_{01},$ and $C_{11}$, the color equation is:

$$C(u,v) = (1-u)(1-v)C_{00} + u(1-v)C_{10} + (1-u)vC_{01} + uvC_{11}$$

The renderer runs this equation independently for every channel. For an RGB `ColorSpace`, it runs three times (once each for Red, Green, and Blue) to calculate the final pixel color.

## 2. Geometric Interpolation (Coons Blending)

While the colors interpolate in a straightforward straight-line (linear) manner, the *physical shape* of a Coons patch relies on cubic curves.

A Type 6 patch is defined by 12 control points that create four intersecting cubic Bézier curves (the borders of the patch). To draw the mesh mathematically, the PDF uses the **Coons patch equation** to calculate where the internal pixels physically sit. It blends the opposite boundary curves using linear interpolation, but then subtracts a "bilinear surface" of the four corners to prevent the math from counting the corner data twice.

$$S(u,v) = S_C(u,v) + S_D(u,v) - S_B(u,v)$$

* **$S_C(u,v)$**: Interpolation between the top and bottom Bézier curves.
* **$S_D(u,v)$**: Interpolation between the left and right Bézier curves.
* **$S_B(u,v)$**: The bilinear interpolation of the four fixed corner points.

### Why the Distinction Matters

Because the geometry is governed by curved Bézier mathematics but the color is strictly bilinear, you can sometimes get unexpected visual results if you aggressively warp the control points of a Coons mesh. The shape will curve beautifully, but the color gradient will still transition "in a straight line" relative to the warped parametric grid, which is why designers often have to add more mesh grid-lines to create complex, flowing color contours.
</details>

---

Looking for some examples, I found the [this PHP TCPDF library PDF example](https://tcpdf.org/examples/E034_color_gradient_mesh/) which creates three of those patch meshes.

<details>
  <summary>Example 1</summary>

<img width="335" height="334" alt="Image" src="https://github.com/user-attachments/assets/e460b7fa-b392-4d92-91ff-5e1552a0264c" />

```php
$coonspatchmesh1 = $pdf->graph->getCoonsPatchMeshWithCoords(20, 180, 80, 80, 'yellow', 'blue', 'green', 'red');
```
</details>

<details>
  <summary>Example 2</summary>

<img width="343" height="342" alt="Image" src="https://github.com/user-attachments/assets/3618ae1c-b294-433c-b317-01a0e5db67ba" />

```php
// set the coordinates for the cubic Bézier points x1,y1 ... x12, y12 of the patch
$coords = [
    0.00,
    0.00,
    0.33,
    0.20,
    //lower left
    0.67,
    0.00,
    1.00,
    0.00,
    0.80,
    0.33,
    //lower right
    0.80,
    0.67,
    1.00,
    1.00,
    0.67,
    0.80,
    //upper right
    0.33,
    1.00,
    0.00,
    1.00,
    0.20,
    0.67,
    //upper left
    0.00,
    0.33,
]; //lower left

// paint a coons patch gradient with the above coordinates
$coonspatchmesh2 = $pdf->graph->getCoonsPatchMeshWithCoords(
    110,
    180,
    80,
    80,
    'yellow',
    'blue',
    'green',
    'red',
    $coords,
    0,
    1,
);
```
</details>

<details>
  <summary>Example 3</summary>

<img width="473" height="673" alt="Image" src="https://github.com/user-attachments/assets/fd4f56d2-43d4-40f5-8088-b8ed93a5d659" />

```php
// first patch: f = 0
$patch_array[0]['f'] = 0;
$patch_array[0]['points'] = [
    0.00,
    0.00,
    0.33,
    0.00,
    0.67,
    0.00,
    1.00,
    0.00,
    1.00,
    0.33,
    0.8,
    0.67,
    1.00,
    1.00,
    0.67,
    0.8,
    0.33,
    1.80,
    0.00,
    1.00,
    0.00,
    0.67,
    0.00,
    0.33,
];
$patch_array[0]['colors'][0] = [
    'red' => 1,
    'green' => 1,
    'blue' => 0,
    'alpha' => 1,
];
$patch_array[0]['colors'][1] = [
    'red' => 1,
    'green' => 1,
    'blue' => 1,
    'alpha' => 1,
];
$patch_array[0]['colors'][2] = [
    'red' => 0,
    'green' => 1,
    'blue' => 0,
    'alpha' => 1,
];
$patch_array[0]['colors'][3] = [
    'red' => 1,
    'green' => 0,
    'blue' => 0,
    'alpha' => 1,
];

// second patch - above the other: f = 2
$patch_array[1]['f'] = 2;
$patch_array[1]['points'] = [
    0.00,
    1.33,
    0.00,
    1.67,
    0.00,
    2.00,
    0.33,
    2.00,
    0.67,
    2.00,
    1.00,
    2.00,
    1.00,
    1.67,
    1.5,
    1.33,
];
$patch_array[1]['colors'][0] = [
    'red' => 0,
    'green' => 0,
    'blue' => 0,
    'alpha' => 1,
];
$patch_array[1]['colors'][1] = [
    'red' => 1,
    'green' => 0,
    'blue' => 1,
    'alpha' => 1,
];

// third patch - right of the above: f = 3
$patch_array[2]['f'] = 3;
$patch_array[2]['points'] = [
    1.33,
    0.80,
    1.67,
    1.50,
    2.00,
    1.00,
    2.00,
    1.33,
    2.00,
    1.67,
    2.00,
    2.00,
    1.67,
    2.00,
    1.33,
    2.00,
];
$patch_array[2]['colors'][0] = [
    'red' => 0,
    'green' => 1,
    'blue' => 1,
    'alpha' => 1,
];
$patch_array[2]['colors'][1] = [
    'red' => 0,
    'green' => 0,
    'blue' => 0,
    'alpha' => 1,
];

// fourth patch - below the above, which means left(?) of the above: f = 1
$patch_array[3]['f'] = 1;
$patch_array[3]['points'] = [
    2.00,
    0.67,
    2.00,
    0.33,
    2.00,
    0.00,
    1.67,
    0.00,
    1.33,
    0.00,
    1.00,
    0.00,
    1.00,
    0.33,
    0.8,
    0.67,
];
$patch_array[3]['colors'][0] = [
    'red' => 0,
    'green' => 0,
    'blue' => 0,
    'alpha' => 1,
];
$patch_array[3]['colors'][1] = [
    'red' => 0,
    'green' => 0,
    'blue' => 1,
    'alpha' => 1,
];

$coonspatchmesh3 = $pdf->graph->getCoonsPatchMesh(0, 0, 210, 297, $patch_array, 0, 2);
```
</details>

The library offer two different functions:

- https://tcpdf.org/docs/srcdoc/tc-lib-pdf-graph/classes-Com-Tecnick-Pdf-Graph-Gradient/#method_getCoonsPatchMesh
- https://tcpdf.org/docs/srcdoc/tc-lib-pdf-graph/classes-Com-Tecnick-Pdf-Graph-Gradient/#method_getCoonsPatchMeshWithCoords

---

UPDATE: Based on all this, I had Gemini generate a little Coons Patch Mesh Editor: https://codepen.io/bramus/full/zxNzwQG

<img width="1555" height="1021" alt="Image" src="https://github.com/user-attachments/assets/8281b72e-1a39-4dd2-9dcf-9ebc161cae02" />

When you have a 2D mesh gradient with multiple points, it’s a matter of tiling patches next to (and underneath) each other, in which the points at the edges are shared with the direct sibling in that specific direction. See this 2nd prototype: https://codepen.io/bramus/full/emgRRNm

<!-- comment id=4802925863 author=JoshTumath created=2026-06-25T18:28:34Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4802925863 -->
## @JoshTumath — 2026-06-25

Thanks for looking into this, @bramus.

I want to try to summarise my understanding of this issue and where we are currently blocked:

1. We do have a syntax proposal for [point-based freeform gradients](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2158233133) and for [mesh gradients](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174600950). We don't have a proposal for line-based freeform gradients, and we don't know if we should have a syntax to support that.
2. We do know how mesh gradients are implemented using coons patches and how they are defined in PDFs. We have an idea how freeform gradients are implemented, but we're not certain, and need clarification from folks at Adobe.
3. We don't know how mesh gradients or freeform gradients can be implemented in a way that is compatible with [CSS colour interpolation](https://www.w3.org/TR/css-color-4/#interpolation), and that needs investigating.

Is that right?

@lrosenthol and @faceless2, are you both still able to help with investigating items 2 and 3? If a freeform gradient in Adobe Illustrator is exported to a PDF, how is it implemented in the PDF (or is it just rasterised)?

---

Bramus has inspired me to ask Gen AI about how Freeform Gradients work in Adobe Illustrator; whether it's compatible with Coons Patches. According Copilot, they likely are not and use a different implementation (which may involve Adobe patents). Also, the way it's implemented is different depending on whether it's using points or lines.

<!-- comment id=4803871923 author=faceless2 created=2026-06-25T20:27:57Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4803871923 -->
## @faceless2 — 2026-06-25

Yes absolutely. Great demo by the way.

* I can't help with the freeform syntax - I know nothing of the secret sauce in Illustrator. But I certainly think it's the ideal syntax.
* The mesh syntax looks like a reformatting of the SVG syntax, in which case it will work when translated to PDF.

The hard part of this (which applies to both bicubic interpolation and to the freeform syntax) is going to be _**G¹ continuity**_ at the edge of each patch - ensuring each patch folows nicely into the next one with no sharp lines. I don't know how to solve this - it's a hard problem, too hard for me.

For point 3,  how this works with CSS color interpolation: conceptually for each point within the mesh you're calculating what proportion of the color at each corner goes into the point as the first step. Once you end up with those values (eg 10% of top-left color, 40% of top-right, 22% of bottom-left, 28% bottom-right), that's when the CSS color interpolation rules come into play, succesively interpolating between two points to give the final color. I feel like I've just told you something you already know, so maybe I've misunderstood the question.

Can this approach be translated to PDF? Yes, easily - the interpolation rules between any two colors are determined by a function, and that function can be anything you like. So you can simulate (eg) an HSL gradient in PDF just by specifying the two end points as RGB, and the function to get between them with a big lookup table  that emulates HSL. That same process works for mesh gradients too.

<!-- comment id=4804703805 author=JoshTumath created=2026-06-25T22:27:59Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4804703805 -->
## @JoshTumath — 2026-06-25

Thanks very much, @faceless2.

> I feel like I've just told you something you already know, so maybe I've misunderstood the question.

No, don't worry, that was very helpful - for me at least! I think we just want to understand viability at this stage and I'm not hearing any concerns from you.

---

Based on this information, it sounds to me like **we can pursue the 'traditional' mesh gradients approach** as we know we can answer questions around PDF compatibility and colour interpolation, so I'll add the issue back on the agenda. See @AmeliaBR's [mesh-gradient function syntax proposal.](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174600950) From an author usability perspective, it seems very short for simple use cases, but can scale up to support very complex meshes. Authors would probably need tooling to help them craft more complex meshes.

However, I think everyone who has commented on this issue agrees that the Adobe Illustrator-style 'freeform gradients' approach is still desirable, as it's more intuitive and easier for authors to understand - and AFAICT can do things that mesh gradients can't do. I'm still really keen for us to be able to support them, but I don't think we can without a better understanding of how they are calculated and how they will serialise. Can I encourage Adobe folks to reach out internally to the Adobe Creative Cloud teams to see if they can share how freeform gradients work and how they export to PDFs? Also, we might not need to do it the same way as Adobe - does anyone know of another way that we could achieve 'freeform gradients'?

<!-- comment id=4807416203 author=faceless2 created=2026-06-26T07:46:05Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4807416203 -->
## @faceless2 — 2026-06-26

Two further thoughts on this topic
* I talked about "successively interpolating between two points" - essentially Gouraud shading. For interpolation methods like sRGB it doesn't matter which order you interpolate - if a triangle has points ABC, then interpolating **(AB)C** gives the same result as **A(BC)**. Does this hold true for all interpolation methods, in particular things like hue? I _think_ it does.
* Freeform gradients are essentially a curve fitting algorithm. We know how to fit one curve to a set of points already, with a Catmull-Rom spline. Doing this to a random set of points, maybe something like this?
  * assign each point to a point on a grid.
  * for each grid-line, determine the Catmull-Rom spline that connects each point.
  * convert the Catmull-Rom splines to the equivalent Bezier spline.
  * if the grid was irregular - if not every rectangle was the same size - then segment the splines to make it regular.
  * result: a regular grid connected by bezier splines = a Coons Patch mesh.

The first step is the one I'm less sure on, but there appear to be algorithms available. No doubt converting a freeform set of points to a Coons Patch is a well-understood problem in some circles - maybe the Blender team have some ideas. But in the absence of anything better it might be interesting to give that one to the Gen AI and see what the demo looks like.

<!-- comment id=4848574189 author=bramus created=2026-06-30T22:56:05Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-4848574189 -->
## @bramus — 2026-06-30

I continued my experiment with a online visualizer/editor and here is the result: https://genuine-waterfall-crane.codepen.app/

<img width="1658" height="1119" alt="Image" src="https://github.com/user-attachments/assets/454f6bb4-0d61-4909-b8b9-4599102932e9" />

_(The editor is showing [this gradient](https://gist.github.com/bramus/0a1fe193643c40c91f813c8106daa17e). You can download that `.json` file and drop it onto the visualizer)_

This thing can create 2D Mesh Gradients using either plain locations (points) or using Bezier points. The rendering is done using Coons Patches, so it _should_ be compatible with PDF — I’ve tried some [SwiftUI examples](https://nilcoalescing.com/blog/MeshGradientsInSwiftUI/), and it seems to be doing things quite similarly.

You can export the created gradients to JSON, SwiftUI, and the [proposed CSS syntax](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174600950).

<!-- comment id=5044974370 author=phantomxx29474 created=2026-07-22T10:58:29Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5044974370 -->
## @phantomxx29474 — 2026-07-22

Interesting, I like it.

<!-- comment id=5143572276 author=svgeesus created=2026-07-31T13:47:37Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5143572276 -->
## @svgeesus — 2026-07-31

For comparison,  [Mesh Gradients in SVG](http://tavmjong.free.fr/blog/?p=316) and [Inkscape documentation of their implementation of SVG2 mesh gradients](https://wiki.inkscape.org/wiki/Mesh_Gradients). These were prototyping the proposed SVG syntax, about a hundred years ago, but the technical aspects are still relevant.

<!-- comment id=5174438968 author=smfr created=2026-08-04T03:59:15Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5174438968 -->
## @smfr — 2026-08-04

Here's a [link to the SwiftUI mesh gradient API](https://developer.apple.com/documentation/swiftui/meshgradient/), which seems like it would map nicely to CSS.

<!-- comment id=5176689428 author=tabatkins created=2026-08-04T08:48:45Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5176689428 -->
## @tabatkins — 2026-08-04

Just as a reminder, [I summarized the SwiftUI approach](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174118904) earlier in the thread, and the immediately following comment from @AmeliaBR gives a concrete syntax proposal for it.

<!-- comment id=5176719226 author=bramus created=2026-08-04T08:51:57Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5176719226 -->
## @bramus — 2026-08-04

> Just as a reminder, [I summarized the SwiftUI approach](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174118904) earlier in the thread, and the immediately following comment from [@AmeliaBR](https://github.com/AmeliaBR) gives a concrete syntax proposal for it.

Yep, and the prototype at https://genuine-waterfall-crane.codepen.app/ can export the gradient to both SwiftUI code as well as the proposed CSS syntax.

<!-- comment id=5239916365 author=drott created=2026-08-10T12:10:20Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5239916365 -->
## @drott — 2026-08-10

> Yep, and the prototype at https://genuine-waterfall-crane.codepen.app/ can export the gradient to both SwiftUI code as well as the proposed CSS syntax.

@bramus would it be possible to add a PDF export to the tool, too? That might help confirming we have PDF compatibility? Viewer compatibility might vary, so if you'd add that, maybe test with Adobe's impl, with Mac Preview, Firefox and PDFium? 

<!-- comment id=5240523686 author=bramus created=2026-08-10T12:50:59Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5240523686 -->
## @bramus — 2026-08-10

Done.

To save you some trouble comparing things, here is [this mesh gradient](https://gist.github.com/bramus/0a1fe193643c40c91f813c8106daa17e) and its various renderings:

| The Editor | Editor Preview (PDF.js) | Chrome | Firefox (PDF.js) | macos Preview |
|-|-|-|-|-|
| <img width="1492" height="989" alt="Image" src="https://github.com/user-attachments/assets/2f7e0229-964f-48b4-add9-cdc489e7d8f8" /> | <img width="1520" height="1083" alt="Image" src="https://github.com/user-attachments/assets/d172729d-dc21-429b-99a0-51009d32add5" /> | <img width="1492" height="989" alt="Image" src="https://github.com/user-attachments/assets/e5d647d5-a48d-4fe1-9cb2-6fcc3e9cfece" /> | <img width="1425" height="1070" alt="Image" src="https://github.com/user-attachments/assets/e3aba7be-6a46-4587-81f1-6d3a45b1d0d7" /> | <img width="1129" height="988" alt="Image" src="https://github.com/user-attachments/assets/945d3f28-5053-4678-93d4-22f9c748c1fa" /> |

_(Edit: Corrected the “Editor Preview (PDF.js)” image. Before it showed a distorted export)_

<!-- comment id=5243184646 author=tabatkins created=2026-08-10T16:41:43Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5243184646 -->
## @tabatkins — 2026-08-10

That's surprisingly terrible in every single implementation! Is it any better in an actual SwiftUI app?

<!-- comment id=5243846049 author=bramus created=2026-08-10T17:45:18Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5243846049 -->
## @bramus — 2026-08-10

Here is SwiftUI Playground:

<img width="1180" height="820" alt="Image" src="https://github.com/user-attachments/assets/6fa8a061-7e8e-43fe-a578-645db1d6218a" />

<!-- comment id=5244812056 author=tabatkins created=2026-08-10T19:15:03Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5244812056 -->
## @tabatkins — 2026-08-10

Okay, so better than all the PDF renders but still a little weird. (The way it shows white in the top-right is, I think, expected beahvior from having the edges not straight, but the bizarre little yellow triangle seems unexpected.)

What this is telling me is that any arguments based on the existence of widespread implementations of mesh gradients are, basically, bunk. Existing mesh gradient impls are pretty uniformly garbage and can't be depended on for anything non-trivial; SwiftUI is the best of them but still has a few slightly odd behaviors in extreme cases. PDF is just a complete lost cause; in practice it does not have mesh gradients at all, since every impl is unusable garbage even in portions of the image that shouldn't be extreme cases.

This doesn't mean we shouldn't use this particular mesh gradient concept, but it does open up the possibility space significantly. If we instead wanted to go for a freeform "just plop some colors down in particular spots and interpolate in 2d between them via triangulation", like in the OP, that's a lot more reasonable now, given the effective lack of in the wild implementations of *anything* so far.

<!-- comment id=5245123013 author=tabatkins created=2026-08-10T19:43:00Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5245123013 -->
## @tabatkins — 2026-08-10

So, in particular, I *do* lean a lot more in the direction of freeform-gradient() now, because the authoring experience is so much simpler. Specify a color and a location, identical to all the other gradients except the location is 2d, and that's it. Easy to explain, easy to understand, easy to edit and tweak. Are there strong use-case reasons to prefer the significantly more complex mesh gradient syntax, then? Any sorts of images that can be done in one and not the other, that are convincing enough to make us prefer a given solution?

Hm, lemme go rewatch the [Youtube video linked in the OP that's about exactly that comparison](https://www.youtube.com/watch?v=8hJlffeGkmo). Okay, having watched that, it doesn't look there's a really meaningful difference in their abilities. Of course either one can generate gradients that are hard to replicate *precisely* in the other, but both can make very similar-looking things. Especially when free-form has the curved line ability (probably represented in the syntax by just allowing a stop to specify multiple positions), it looks like freeform is more or less just as powerful as mesh but drastically easier to write and understand.

I think mesh gradients are reasonable when used with an authoring tool that can present the bezier handles reasonably, but are dramatically worse as a hand-authoring format. I want to support simple usages of this kind of 2d gradient, so I'm inclined to support freeform as the basic 2d gradient to use. We can talk about whether to support mesh gradients *as well*, with the assumption that they'll be a tool output format instead of a human-usable thing, but freeform as the basic thing seems better to me.

<!-- comment id=5245288807 author=SebastianZ created=2026-08-10T19:58:52Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5245288807 -->
## @SebastianZ — 2026-08-10

For what it's worth, I've tested this in four different Windows apps. Only two of them (of course including Adobe Acrobat Reader) render the example gradient as expected, while the other two have one rendering issue, presumably due to using the same rendering engine.

| Acrobat Reader | PDF24 | PDF X | PDF Reader |
|-|-|-|-|
| <img width="2560" height="1528" alt="Image" src="https://github.com/user-attachments/assets/3646d4bd-8a18-46aa-b9cb-536f60633d47" /> | <img width="2560" height="1528" alt="Image" src="https://github.com/user-attachments/assets/3720af7a-41ff-47b7-861d-096383838606" /> | <img width="2560" height="1528" alt="Image" src="https://github.com/user-attachments/assets/ebc59551-ddf5-4c79-b22c-892e22ae2146" /> | <img width="2560" height="1528" alt="Image" src="https://github.com/user-attachments/assets/d2e41002-64de-4ccf-b8da-564d646f6260" /> |

Sebastian

<!-- comment id=5245801349 author=bramus created=2026-08-10T20:49:13Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5245801349 -->
## @bramus — 2026-08-10

> Okay, so better than all the PDF renders but still a little weird. (The way it shows white in the top-right is, I think, expected beahvior from having the edges not straight, but the bizarre little yellow triangle seems unexpected.)

The observation about top-right corner is correct. I have updated https://genuine-waterfall-crane.codepen.app/ so that it now also cuts off any pixels that bleed out of the resulting shape.
 
> SwiftUI is the best of them but still has a few slightly odd behaviors in extreme cases.

Looking at the results, I had a hunch the rendering of the app I built was off because all implementations show the yellow fold going above instead of underneath. I nudged AI Studio to make sure the rendering of the Coons Patches is in the same order as whatever PDF does. The rendering of the app is now updated and … matches SwiftUI:

<img width="1491" height="1023" alt="Image" src="https://github.com/user-attachments/assets/7b96725f-4778-411d-ae06-f8dbbe58054b" />

Perhaps with some more nudging it can get to the PDF result _(Acrobat Reader seems like the one to get to)_

> What this is telling me is that any arguments based on the existence of widespread implementations of mesh gradients are, basically, bunk.

LOL, I was thinking the exact opposite after doing these tests: while most implementations need some (or a lot of) work to get the rendering 100% correct, it’s already looking somewhat OK. CSS could lead the way here, showing how it’s done, knowing that things can be made interoperable over time.

If, for example, `PDF.js` didn’t straighten out their curves and if Chrome update the grid density for the interpolation (to make the cells less obvious) things would already be greatly improved.

_(Also, to be fair: the used gradient is a pretty complicated one to render)_

<!-- comment id=5247646667 author=tabatkins created=2026-08-11T00:32:25Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5247646667 -->
## @tabatkins — 2026-08-11

> LOL, I was thinking the exact opposite after doing these tests: while most implementations need some (or a lot of) work to get the rendering 100% correct, it’s already looking somewhat OK.

You think those PDF results look somewhat OK? I wouldn't consider *any* of those to be of sufficiently quality to even consider using. They all have ugly, obvious rendering artifacts that make them completely unusable imo, even in the areas that shouldn't be particularly complex. (I can *understand* weirdness in spots that overlap or have extreme curves, but they all screw up in places with relatively simple curves, too.)

Like, sure, all of those *could* probably be fixed, but they *haven't* been fixed despite, presumably, being in the implementations for some time. That suggests they're low-priority for the implementations, which suggests they're not used much. (Tho, of course, with the quality that bad, it's *no wonder* they're not used much!)

<!-- comment id=5248248408 author=JoshTumath created=2026-08-11T02:14:45Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5248248408 -->
## @JoshTumath — 2026-08-11

> What this is telling me is that any arguments based on the existence of widespread implementations of mesh gradients are, basically, bunk. Existing mesh gradient impls are pretty uniformly garbage and can't be depended on for anything non-trivial; SwiftUI is the best of them but still has a few slightly odd behaviors in extreme cases. PDF is just a complete lost cause; in practice it does not have mesh gradients at all, since every impl is unusable garbage even in portions of the image that shouldn't be extreme cases.

We can see that Bramus's example is an extreme case because the top right node's bezier control point is so long that it passes through to another grid column. Based on the video I linked to in the original post, that would be considered bad practice anyway. I think when that happens, it's known as 'knotting'. There are probably other best practices, like making sure the edge nodes of the mesh gradient are touching the edge of the shape. 

<img width="2118" height="1500" alt="" src="https://github.com/user-attachments/assets/175badb9-8112-4f59-b8e6-50ee17e91c3a" />

> Are there strong use-case reasons to prefer the significantly more complex mesh gradient syntax, then? Any sorts of images that can be done in one and not the other, that are convincing enough to make us prefer a given solution?

What prompted me to propose mesh gradients in the first place was after the BBC's rebrand in 2022 which included a lot of mesh gradients. I wanted to create lots of content using mesh gradients, without having to create lots of background images to render them at different dimensions. From what I've learnt about mesh gradients vs freeform gradients over the last four years, freeform gradients will not let us replicate our brand (but we could probably attempt something that looks similar).

So I thought maybe I could try to replicate the BBC branding mesh gradient shown in my OP. While I did prove that I'm a terrible designer, I sort-of managed to do it.

[mesh-gradient.pdf](https://github.com/user-attachments/files/30918663/mesh-gradient.pdf)

Here's a screenshot of it in Edge's PDF viewer.

<img width="500" height="500" alt="" src="https://github.com/user-attachments/assets/b7e212be-0002-49aa-966d-b278bb6ef3a5" />

When I'm in work tomorrow, I'll see if I can ask one of our designers to open up the original Illustrator file for the BBC brand mesh gradient in Illustrator and try to replicate it better in Bramus's tool. And maybe we can attempt to replicating it using freeform gradients and see how close we can get, if that's useful?

That said, I strongly agree that freeform gradients offer a better API for authors and would be a better feature for the WG pursue first. It's probably not going to be possible for us to come up with a simple API for mesh gradients that can be typed by hand. Tooling is definitely needed.

<!-- comment id=5248267962 author=JoshTumath created=2026-08-11T02:17:50Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5248267962 -->
## @JoshTumath — 2026-08-11

If we pursue freeform gradients, are we blocked by a requirement to render them in PDFs? Or could we simply have UAs export a rasterised version?

<!-- comment id=5248354344 author=tabatkins created=2026-08-11T02:34:11Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5248354344 -->
## @tabatkins — 2026-08-11

Considering all the things that *aren't* native gradients that browser-produced PDFs have to render (like, you know, raster images), yeah, generating a rasterized version should be just fine. (And in any case I'd encourage browsers to do so for mesh gradients, too, given the rendering quality in PDF viewers.)

> We can see that Bramus's example is an extreme case because the top right node's bezier control point is so long that it passes through to another grid column.

Yes, I know the top right corner is an extreme case, and that's not what I'm particularly worried about. (Tho we *should* expect consistency in rendering.) The problems with the various PDF renders apply *everywhere else*, too.

(Btw, @bramus, for some reason the images in <https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5240523686> are no longer viewable on their own. Would you mind editing the comment to display them inline, rather than tiny in a table, so it's more obvious what they're doing wrong?)

<!-- comment id=5251415247 author=bramus created=2026-08-11T09:33:00Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5251415247 -->
## @bramus — 2026-08-11

> > We can see that Bramus's example is an extreme case because the top right node's bezier control point is so long that it passes through to another grid column.
> 
> Yes, I know the top right corner is an extreme case, and that's not what I'm particularly worried about. (Tho we _should_ expect consistency in rendering.) The problems with the various PDF renders apply _everywhere else_, too.

~~I can fix the top right corner’s rendering in the app _(it has to do with the order of rendering the patches: top to bottom vs bottom to top)_ …~~

<img width="1427" height="804" alt="Image" src="https://github.com/user-attachments/assets/123f1d85-5044-4b63-bdb6-08577575fa14" />

~~… but then that breaks the rendering of the center patch (which is also one of the patches that _wildly_ varies per PDF renderer).~~

**EDIT/UPDATE:** I was eventually able to work around this limitation ([see this update further down](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5253522743)). The app can now render both the top-right and center patches correctly by detecting wether a surface folds over itself or not.

After a long chat with Gemini it seems that PDF renderers are doing proprietary stuff do determine the rendering order of the patches (something about “Adaptive Subdivision tessellation” vs. “Fixed Grid tessellation”).

<details>
<summary>Excerpt from what Gemini told me <em>(AI answer; Add grains of salt where needed)</em>:</summary>

> Apple's CoreGraphics engine does not evaluate Coons patches using a fixed grid loop (like our WebGL for y in 0..30 loop). Instead, it uses an algorithm called Adaptive Subdivision.
> 
> - It looks at a patch and asks: "Is this curved?"
> - If yes, it splits the patch into 4 smaller quadrants recursively until the resulting micro-quads are "flat enough".
> - It then draws those micro-quads in the order of the resulting recursive QuadTree.
> 
> When a mesh heavily folds over itself (self-intersects), the Painter's Algorithm "Z-order" (what overlaps what) is entirely determined by the order these triangles are emitted.
> 
> - In your Center patch, the top edge dips heavily downwards. Apple's adaptive subdivision happens to traverse the tree in a way that draws this dipped edge early, hiding it under the middle (which acts like our `y++`).
> - In your Top-Right patch, the top edge sweeps heavily leftwards. The curvature causes the adaptive tree to traverse differently, resulting in that edge being drawn late, making it lay over the middle (which acts like our `y--`).
> 
> Because our loop direction is rigidly fixed to `y++` (Top-to-Bottom) and `x++` (Left-to-Right), we will perfectly match PDF for patches where Apple's dynamic tree happens to draw top-to-bottom, but we will look inverted on patches where the geometry's curvature caused Apple's tree to evaluate bottom-up.
> 
> There is unfortunately no single for loop direction (`y++` or `y--`, `x++` or `x--`) that can perfectly mimic CoreGraphics across all wildly folding geometry simultaneously. The current `y++` (Top-to-Bottom) and `x++` (Left-to-Right) traversal you restored is the absolute most mathematically correct standard for a parameterization sweep. The overlapping artifacts you see on extreme self-intersecting folds are just an inherent fingerprint of the specific rendering engine (WebGL fixed grid vs. Apple CoreGraphics adaptive quadtree).
</details>

So there seems to be no interop amongst PDF renderers for handling these complex cases that use bézier points.

However, we could still choose to have a syntax that is compatible _in spirit_ with PDF — and also compatible with how Swift, PostScript, SVG 2 (if `<meshgradient>` actually becomes a thing), and Cairo approach these — while exporting them as rasterized images. That way the concepts folks need to learn are portable across languages … which, to me, seems like a good thing.

_(FWIW: When using regular points the rendering of the 2D Mesh is pretty much consistent across all PDF renderers I tried)_

---

> (Btw, [@bramus](https://github.com/bramus), for some reason the images in [#7648 (comment)](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5240523686) are no longer viewable on their own. Would you mind editing the comment to display them inline, rather than tiny in a table, so it's more obvious what they're doing wrong?)

I think this is a GitHub issue when leaving the tab open for too long (and the JWT token expiring). If you refresh, all should be good.

<!-- comment id=5252869736 author=drott created=2026-08-11T12:01:15Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5252869736 -->
## @drott — 2026-08-11

@bramuys wrote: 

> However, we could still choose to have a syntax that is compatible in spirit with PDF — and also compatible with how Swift, PostScript, SVG 2 (if <meshgradient> actually becomes a thing), and Cairo approach these — while exporting them as rasterized images. That way the concepts folks need to learn are portable across languages … which, to me, seems like a good thing.

+1. 

@JoshTumath wrote: 

> If we pursue freeform gradients, are we blocked by a requirement to render them in PDFs? Or could we simply have UAs export a rasterised version?

@JoshTumath , @tabatkins some thoughts on rasterized output and the rendering flaws:

There are multiple open source implementations here, pdf.js and PDFium that can be improved and fixed. Using a gradient concept that allows PDF encoding at least creates an avenue for generating efficient PDFs for such type of gradients in the future, even though we might initially need to use rasterized output until the renderers are fixed.

If we specify something that cannot be reasonably transferred into PDF format other than through rasterization, we would be stuck on generating inefficient PDFs. Imagine a PDF where a whole page background is such a gradient - that would always require generating really large PDFs containing for example a 300dpi rasterized background of the whole page.

I am also speaking from the perspective of adopting the outcome of this issue potentially to COLRv2, a successor to the COLRv1 color format. For that to work, we would also need a mathematical/vector encoding in PDF to avoid having to rasterize every letter. 

<!-- comment id=5253522743 author=bramus created=2026-08-11T13:02:13Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5253522743 -->
## @bramus — 2026-08-11

OK, I think I figured it out: in https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5251415247 I noted that I could adjust the rendering of my app to fix the top-right patch but that it would mess up the center patch.

I pushed AI Studio a bit further and extended it by detecting whether a surface folds over itself or not, and, based on that, loop over the grid cells in a different order.

The result is that the rendering for [this gradient](https://gist.github.com/bramus/0a1fe193643c40c91f813c8106daa17e) is now this:

<img width="1840" height="1196" alt="Image" src="https://github.com/user-attachments/assets/70d99b83-28ea-4d99-be92-116965963efe" />

… and this closely matches what Acrobat Reader and PDF24 do (see screenshots by @SebastianZ [here](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5245288807)), so that’s a good start :)

Looking at the other screenshots _(also see [mine here](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5240523686))_, I think PDF X, PDF Reader, PDFium (Chrome), and SwiftUI can easily be fixed by also incorporating this folding detection logic (the rest of their rendering seems fine). PDF.js then only needs to stop straightening out its curves, and finally there is macOS Preview that is left but that one seems a bit more complicated (as it also does something weird in the top-right patch)

<!-- comment id=5256633813 author=bramus created=2026-08-11T17:31:35Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5256633813 -->
## @bramus — 2026-08-11

Skia also seems to render things correctly (modulo a few white pixel glitches): https://fiddle.skia.org/c/7809027f37cf1412bdf76b8a93a5cd79 (scroll past the code to see the resulting visual).

The exported PDF from that site results in a blank PDF though. When inspecting said PDF, it looks like the gradient is not present at all _(there should be a `<< /ShadingType 6 /ColorSpace /DeviceRGB /…` present)_, so I guess it’s more of an issue with the site and its export functionality rather than with Skia itself.

<!-- comment id=5260882754 author=tabatkins created=2026-08-12T01:07:31Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5260882754 -->
## @tabatkins — 2026-08-12

> SVG 2 (if `<meshgradient>` actually becomes a thing), 

At least for SVG2, the tech goes either way. If *we* settled on one particular 2d gradient tech, they'd copy; if they settled on one, we'd copy.

> quality of existing PDF renderers

It's good to hear that the basic "no beziers or misorderings, just colors on a grid" works well across impls. Is that simple use-case common enough to be worth reifying in the spec, tho? Or is that simple case *even better* addressed by freeform?

(Note: I'm not opposed to doing *both* if it's sufficiently valuable! I'm just fairly concerned, for both usability and quality reasons, about doing *Just* mesh at this point.)

<!-- comment id=5284706747 author=bramus created=2026-08-13T18:19:59Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5284706747 -->
## @bramus — 2026-08-13

> > quality of existing PDF renderers
> 
> It's good to hear that the basic "no beziers or misorderings, just colors on a grid" works well across impls. Is that simple use-case common enough to be worth reifying in the spec, tho? Or is that simple case _even better_ addressed by freeform?
> 
> (Note: I'm not opposed to doing _both_ if it's sufficiently valuable! I'm just fairly concerned, for both usability and quality reasons, about doing _Just_ mesh at this point.)

I’ll happily admit that I’ve been over-indexing on mesh gradients here. In the OP @JoshTumath linked to [this YouTube video](https://www.youtube.com/watch?v=8hJlffeGkmo) and both types have a different output/behavior, so I think that, depending on the use-case, an author would go for the one or the other.

Both are not interoperable as the freeform uses an arbitrary number of points that can be randomly placed, whereas the points for the mesh gradient are determined by the number of lines and rows, so we’d need something different for both.

I did some research into how various platforms and applications export/render Freeform Gradients and there seems to be no real consensus there … a lot of languages (SwiftUI, Skia, Cairo, SVG, CSS) fake it by simply drawing multiple circular gradients. The results are visually bayd (the typical dip to grey when things start blending is everywhere) and are not interoperable — which wings back to the original request in this issue

Adobe Illustrator has the best visual result (see Josh’s linked to video) and to make their sure their freeform gradients are PDF compatible, they convert that gradient to a Type 7 “Tensor-product patch” in a PDF — the [“ECI Altona Test Suite 2.0 Technical 2”](https://eci.org/lib/exe/eci_altona-test-suite-v2_technical2_documentation_eng-4.pdf) even calls this the “Illustrator gradient mesh” 😅 . While Illustrator’s approach works, the downside is that its results in a much larger byte stream.

To try things out myself, I extended https://genuine-waterfall-crane.codepen.app to also support Freeform Gradients (rendered using WebGL). When exporting a freeform gradient to PDF, the app takes the same approach as Illustrator does and samples the it very precisely, to then convert it to a PDF-compatible Type 7 ShadingType. _(Unfortunately I don’t have Illustrator to have it generate a PDF for me to inspect)_

As mentioned the result is quite large: the simple gradient the app shows by default results in byte stream of 138601 bytes. For comparison: [the mesh gradient I’ve been using in this thread](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5253522743) is represented by a short bytestream of only 1387 bytes.

In summary, based on my research and what [the tool](https://genuine-waterfall-crane.codepen.app) is producing, I think that the path to having Mesh Gradients in a compatible way is clearly lit (although some PDF renderers currently have some render bugs), while the road to compatible freeform gradients is less clear.

<!-- comment id=5295085417 author=JoshTumath created=2026-08-14T15:27:12Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5295085417 -->
## @JoshTumath — 2026-08-14

Amazing work again, @bramus !! Thank you for looking into this. So now we know that freeform gradients are compatible with PDFs after all.

Lea's proposed syntax for freeform gradients was:

```
freeform-gradient( [<color-interpolation-method>]?, [ <color> <bg-position> ]#{1,} )
```

But your tool introduces a spread value, so maybe we need to extend the proposed syntax?

```
freeform-gradient( [<color-interpolation-method>]?, [ <color> <bg-position> <length-percentage> ]#{1,} )
```

However, the spread feature in your tool seems to work differently to Illustrator. In yours, you specify a specific radius(?) of the colour stops. But in Illustrator, the spread seems to be a percentage. But that percentage doesn't seem to translate to a length; it seems to be more of a 'strength' or 'influence' value.

I've been struggling to find a designer at the BBC who still has Illustrator installed on their computer to test out exactly how it works, but this is what I understand from my research: the spread works as a weighting factor. According to Copilot:

> Adobe only states that Spread controls the stop's "impact" and does not publish the exact interpolation algorithm or percentage-to-distance conversion.
> 
> My suspicion, based on how it renders, is that Illustrator uses a weighted-distance interpolation field (similar to inverse distance weighting or a metaball-style influence map) and Spread scales that weighting radius. However, Adobe has never publicly documented that implementation detail.

So for example, if there are two colour stops on the left and right of the canvas, and the left has 0% spread and the right has 50% spread, then the right colour stop would cover 50% more of the canvas.

It would be nice if we could make the spread value optional, but if the spread is defined as a length, then it might be difficult for us to define a default value. But if the spread is a percentage representing the weight factor, then all colour stops could have a spread of 0% by default.

<!-- comment id=5295323462 author=faceless2 created=2026-08-14T15:49:14Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5295323462 -->
## @faceless2 — 2026-08-14

> So now we know that freeform gradients are compatible with PDFs after all.

The original syntax for SVG mesh gradients has _always_ been compatible with PDF, as mentioned near the top of this thread - as the SVG syntax was derived from the PDF syntax this shouldn't come as a surprise. So if you have an algorithm that converts freeform mesh to the "Coons Patch" mesh used by the SVG syntax, then representing that in PDF is no problem. And I am very much looking forward to seeing this algorithm specified so we can implement it.

Are there implementation bugs displaying them? _Hell_ yes. I'm not here to defend that, but I will note that while Coons Patches have been in PDF since the mid 90s and are common, patches with overlapping curves are clearly less common. Implementation issues shouldn't impact the specification process of course, but it's good to know what to look for when testing.

* The main problem as you've found is overlapping patches. Behaviour is defined (ISO 32000-2:2020 p246) but not always implemented correctly. Reordering is one approach, another is: split the patches in half to make smaller patches. @bramus has got to the same conclusion already apparently.
* Many PDF viewers will interpolate in RGB even if the corners are specified in (eg) Lab. I realise non-RGB is a way off still, but worth noting now. Solution? Make smaller patches.
* Preview.app has a specific problem relating to the coordinate system of the gradients - _all_ gradients, not just Type 6. Because I know Apple's eyes are on this issue, trivial sample attached: [gradient.pdf](https://github.com/user-attachments/files/30943991/gradient.pdf) - both pages should be identical, but in Preview one is reversed.
* I'd also suggest testing gradients with transparency. The PDF transparency model uses a separate grayscale masking layer rather than combining color/opacity into one sample - it's fairly simple to get right but if you're looking for PDF renderer bugs, it's worth testing.

(Apologies for the issue closure, fat fingers)

<!-- comment id=5295910361 author=JoshTumath created=2026-08-14T16:51:26Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5295910361 -->
## @JoshTumath — 2026-08-14

> > So now we know that freeform gradients are compatible with PDFs after all.
> 
> The original syntax for SVG mesh gradients has _always_ been compatible with PDF, as mentioned near the top of this thread - as the SVG syntax was derived from the PDF syntax this shouldn't come as a surprise. So if you have an algorithm that converts freeform mesh to the "Coons Patch" mesh used by the SVG syntax, then representing that in PDF is no problem. And I am very much looking forward to seeing this algorithm specified so we can implement it.

Bramus said that his tool uses a Tensor-product patch rather than Coons patches to render the freeform gradient, as that's how Illustrator does it. Would it be more beneficial to use an algorithm to convert it into a Coons patch? Is that possible?

But this is exciting because we're now in a very good position to present this at a WG call. I'll be proposing two resolutions:

1. Adopt the mesh gradients proposal with @AmeliaBR's [proposed syntax](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-2174600950), with an algorithm for how to render overlapping patches
   * This is a well-established method of making 2D gradients that is supported by many graphics libraries and PDF
   * Very difficult for authors to write by hand, so tooling will be essential to help authors to create them
2. Adopt the freeform gradient proposal with @LeaVerou's [proposed syntax](https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5295085417) (but agree on how the spread function will work)
   * Supported in PDFs but not in any graphics libraries like Skia or SwiftUI, so requires additional implementation effort
   * Much simpler than mesh gradients for authors to write by hand, but tooling is also very helpful

(And we hope that PDF viewers will implement better support for 2D gradients.)

Mesh gradients should be simpler for UAs to implement

<!-- comment id=5296125043 author=faceless2 created=2026-08-14T17:13:47Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5296125043 -->
## @faceless2 — 2026-08-14

> Bramus said that his tool uses a Tensor-product patch rather than Coons patches to render the freeform gradient, as that's how Illustrator does it. Would it be more beneficial to use an algorithm to convert it into a Coons patch? Is that possible?

Sorry, my error with terminology - a Tensor-product patch is fine for PDF of course (and a Coons patch is just a special subset of Tensor product patch).

<!-- comment id=5296909780 author=tabatkins created=2026-08-14T18:40:45Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5296909780 -->
## @tabatkins — 2026-08-14

I think Amelia's syntax proposal hewed too closely to the Swift syntax, which was shaped the way it was due to how programming 	langauges prefer to take arguments. (They don't really like compound values, as it requires constructing single-use objects.) A more CSS-y syntax that draws on the shape() syntax that has been defined since then is:

```
mesh-gradient( [ <color-interpolation-method> || <color> ]?,
               <mesh-point># [ ';' <mesh-point># ]*
             )
<mesh-point> = <color> [ <mesh-absolute-position> | <mesh-relative-position> ]?
<mesh-absolute-position> = <position> [ with <position> [ / <position> ]{0,3} ]?
<mesh-relative-position> = by <coordinate-pair> [ with <mesh-relative-control-point> [ / <mesh-relative-control-points> ]{0,3} ]?
<mesh-relative-control-point> = <coordinate-pair> [from origin]?
```

* `<color-interpolation-method>` is self-explanatory. The `<color>` defines the color "outside" of the mesh, only visible if your outermost points aren't on the edges of the image; defaulting to `transparent`. (It's not painted "under" the gradient, just "around" it; a mesh with partially-transparent colors will still be transparent even if you specify a color here.)
* The body of the function is comma-separated sequences of mesh points defining a row, with each row separated by a `;`. (Using `;` rather than `/` because I'm borrowing from `shape()` elsewhere and that uses `/`.) Each row needs to have the same number of points, or the function is invalid. Each mesh point has:
	*  a `<color>`
	* positions with optional control points. Cribbing from shape(): if specified as a `<position>`, it and its control points are all absolute coords in the mesh's coordinate space. If given as `by ...`, it and its control points are relative to the trivial NxM even grid position.
	* The outer points don't use all four of the control points, but they're still specified in the grammar; you can supply `0 0` as an easy null value for the unused ones.
	* An omitted control point takes from the value of another one, as usual for CSS's "1-4 values in TRBL order" rules, except we rotate them into position around the point. This gives the desired default behavior where, if you specify just two, you'll get the correct control points on the other side to keep your color ramp smooth. (In Illustrator, opposite-side control points are locked to be half-turn rotations from each other by default, unless you purposely unlock them.)

-----------

For freeform, yes to Lea's syntax - a list of `<color> <position>+` stops. If one position, it defines a point; if multiple it's a catmull-rom curve, a la Adobe Illustrator. If we do want to introduce strength, I recommend a `<number>` immediately after the color, defaulting to 0 with full -inf to +inf range, indicating more or less strength than default.

```
freeform-gradient( <color-interpolation-method>? , <freeform-point># )
<freeform-point> = <color> <number> <position>+
```

<!-- comment id=5298655414 author=JoshTumath created=2026-08-14T22:09:26Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5298655414 -->
## @JoshTumath — 2026-08-14

> Sorry, my error with terminology - a Tensor-product patch is fine for PDF of course (and a Coons patch is just a special subset of Tensor product patch).

Ah I see. Thanks for the clarification.

---

@tabatkins I really like your suggestion for the mesh syntax. Defining the colour with the mesh point feels much better. So if I understand correctly, the simplest possible value for it would be:

```css
background-image: mesh-gradient(
  red top left, blue top right;
  green bottom left, yellow bottom right
);
```

> `<mesh-gradient-method>` is how the colors themselves are interpolated, a la SwiftUI.

Amelia said this would default to `bilinear` - I presume like [SwiftUI's `smoothColors` property?](https://developer.apple.com/documentation/swiftui/meshgradient/smoothscolors). I'd like to bikeshed less technical names for these values, but I don't know what to suggest since I don't know what impact they have in practice. `bicubic` could be called `smooth` _a la_ SwiftUI and the CSS `image-rendering` property.

> For freeform, yes to Lea's syntax - a list of `<color> <position>+` stops. If one position, it defines a point; if multiple it's a catmull-rom curve, a la Adobe Illustrator.

We haven't discussed replicating the line feature in freeform gradients until now, but that seems like a good suggestion to me. For some reason, I thought we had a syntax in CSS for creating lines, similar to the `shape()` function, but we don't.

Strangely, Illustrator doesn't support the spread property on lines. 

> If we do want to introduce strength, I recommend a `<number>` immediately after the color, defaulting to 0 with full -inf to +inf range, indicating more or less strength than default.

I'm not sure what effect a negative spread value would have, though, since the spread does not represent a specific radius. In my mind, that would be like setting a negative number for `flex-grow`.

<!-- comment id=5298817517 author=tabatkins created=2026-08-14T22:32:53Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5298817517 -->
## @tabatkins — 2026-08-14

> the simplest possible value for it would be:

Even simpler, since the positions are optional if you're okay with the default "evenly spaced grid" layout:

```css
background-image: mesh-gradient(red, blue; green, yellow);
```

(Edit: whoops, I had indeed left out the final `?` indicating that the positions are optional in the grammar. Fixed now.)

> bicubic could be called smooth a la SwiftUI and the CSS image-rendering property.

Yeah, that's a good idea. I would like to explore how useful this value even is, tho - can an Apple person contact their SwiftUI people and see what normal vs smooth color interpolation is generally used to achieve? @smfr? @hober?

> I'm not sure what effect a negative spread value would have, though, since the spread does not represent a specific radius. In my mind, that would be like setting a negative number for flex-grow.

Just less power, relative to the surrounding points. You could reproduce it by *raising* the power of everything else, but sometimes it's more convenient to lower just one.

<!-- comment id=5299453194 author=JoshTumath created=2026-08-15T00:20:27Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5299453194 -->
## @JoshTumath — 2026-08-15

> Yeah, that's a good idea. I would like to explore how useful this value even is, tho - can an Apple person contact their SwiftUI people and see what normal vs smooth color interpolation is generally used to achieve? [@smfr](https://github.com/smfr)? [@hober](https://github.com/hober)?

Oh, I've just noticed in the SwiftUI docs that [`smoothsColors` defaults to true in all of the `init` functions.](https://developer.apple.com/documentation/swiftui/meshgradient/init(width:height:bezierpoints:colors:background:smoothscolors:colorspace:))

When I turn it off in Swift Playground, the results are immediately apparent. Wow. I can't imagine why anyone would want `bilinear`.

| `smoothsColors = false` | `smoothsColors = true` |
| --- | --- |
| <img width="616" height="461" alt="Image" src="https://github.com/user-attachments/assets/84ba8701-20fc-47c7-ae23-857fb4c3dbaf" /> | <img width="619" height="462" alt="Image" src="https://github.com/user-attachments/assets/7f40b6c3-5e72-41e1-a4ad-dd294e956841" /> |

<!-- comment id=5299503581 author=tabatkins created=2026-08-15T00:28:58Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5299503581 -->
## @tabatkins — 2026-08-15

Oh yeah that looks kinda awful. I suspect that, if you knew very precisely what you're doing, relying on bilinear color smoothing and then making sure beziers give you smooth continuity might be simpler than mixing bicubic color scaling in?

But, absent a clear use-case or request for it, I think we can just drop the keyword and define the feature as using the bicubic color scaling.

<!-- comment id=5314724209 author=svgeesus created=2026-08-17T10:13:16Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5314724209 -->
## @svgeesus — 2026-08-17

> But, absent a clear use-case or request for it, I think we can just drop the keyword and define the feature as using the bicubic color scaling.

Strong +1 to this.

<!-- comment id=5730651614 author=JoshTumath created=2026-09-18T13:25:46Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5730651614 -->
## @JoshTumath — 2026-09-18

I'm working on an Explainer doc to try to summarise the discussions that we've had and the solutions we've been heading towards. Once that's ready, I'll ask for this issue to be prioritised on one of our next WG call.

---

By the way, I noticed this week that Canva released a new version of Affinity with support for what it calls 'diffusion gradients'. They're somewhat similar to freeform gradients, but only let you specify lines and shapes. You can specify a strength value on the shapes and lines, but the way the colours blend together feels different to Adobe Illustrator's freeform gradients implementation.

However, I like the idea of possibly extending the freeform gradient syntax in the future with support for CSS shapes (I won't include it in the initial proposal, though).

<img width="511" height="324" alt="Image" src="https://github.com/user-attachments/assets/a4d5143c-9f6b-424c-9a33-76a6b8183019" />

<!-- comment id=5835445394 author=JoshTumath created=2026-09-25T16:01:44Z url=https://github.com/w3c/csswg-drafts/issues/7648#issuecomment-5835445394 -->
## @JoshTumath — 2026-09-25

I've finished writing up an Explainer. [**You can read the 2D Gradients Explainer here.**](https://github.com/JoshTumath/Explainers/blob/main/css-2d-gradients/explainer.md)

Please have a read and let me know if I've missed any details. I'm going to request that we add this issue to our Agenda for our call on Wed 7th October (when I'm back from my holiday in Malta 😎), so I hope the doc will provide a useful summary for those who haven't been keeping up with the comments on the issue. I'll move the Explainer to the css-images-5 directory if the proposal is accepted.

Also, I'd just like to call out how this has been a wonderful collaborative effort by so many CSSWG contributors, so thank you everyone! This is what I put in the Acknowledgements section in the doc:

> So many members and contributors to the CSSWG have been involved in the research and feedback for this proposal, whom I am incredibly grateful to. This has truly been a collaborative piece, with very little of the work my own. Many thanks for valuable feedback and advice from:
>
> - Amelia Bellamy-Royds for mesh gradient expertise and opinions and the initial proposal for the mesh gradient function syntax
> - Bramus for doing a lot of research and creating an AI-generated prototype tool that was incredibly helpful in understanding implementation support and exploring API proposals
> - Chris Lilly for explaining how freeform gradients can be rendered and for general advice and support
> - Lea Verou for improving on the freeform gradient syntax
> - Mike Bremford (faceless2) for sharing the SVG2 `MeshGradient` proposal and for general PDF expertise
> - Sebastian Zartner for helping to define the freeform gradient syntax and investigating PDF viewer support for mesh gradients
> - Tab Atkins-Bittner for proposing a more CSS-style API for defining mesh gradients and for general advice and feedback
> 
> (If anyone is missing from this list, please let me know and I will add you.)
