---
number: 7629
title: "[css-backgrounds-3] Use lowercase `currentcolor` for initial property values"
state: "closed"
labels: []
author: "cdoublev"
created_at: "2022-08-19T17:11:19Z"
closed_at: "2022-08-19T18:57:52Z"
url: "https://github.com/w3c/csswg-drafts/issues/7629"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:44:18Z"
---

[`border-<side>-color`](https://drafts.csswg.org/css-backgrounds-3/#border-color) are defined with `currentColor` (camel case) as their initial value, instead of `currentcolor` (lowercase). 

This prevents simplifying the serialization of `border`, `border-color`, `border-<side>`, when making use of the definitions extracted from the spec by `w3c/webref` to compare the `initial` longhand value to the longhand declaration values used to serialize these shorthands.

`currentcolor` always appear lowercase in CSS Color 4. Also, I think it is more accurate to define `initial` fields to a value that matches their serialization as a component of a specified value (keywords serialize to lowercase), not as an input with arbitrary case.

Please tell me if you want me to create a PR, if you agree.

<!-- comment id=1220974371 author=svgeesus created=2022-08-19T18:22:05Z url=https://github.com/w3c/csswg-drafts/issues/7629#issuecomment-1220974371 -->
## @svgeesus — 2022-08-19

The note at the end of https://drafts.csswg.org/css-color-4/#currentcolor-color may be instructive. `camelCase` isn't arbitrary.

> This prevents simplifying the serialization of border, border-color, border-<side>, when making use of the definitions extracted from the spec by w3c/webref to compare the initial longhand value to the longhand declaration values used to serialize these shorthands.

webref should probably be applying the CSS ASCII-case-insensitive rule to keywords.

<!-- comment id=1221006002 author=cdoublev created=2022-08-19T18:57:52Z url=https://github.com/w3c/csswg-drafts/issues/7629#issuecomment-1221006002 -->
## @cdoublev — 2022-08-19

> webref should probably be applying the CSS ASCII-case-insensitive rule to keywords.

This is one of the two reasons why I wanted to close this issue. I was not sure but I think it is safe to lowercase all `initial` field values.

**EDIT 1:** nope, the initial value of `unicode-range` is `U+0-10FFFF` (serialization is not defined, Chrome/Firefox serialize with uppercase).

The second reason is that simplification of values for serialization is not really defined. There is only *If component values can be omitted or replaced with a shorter representation without changing the meaning of the value, omit/replace them.*

Chrome does not simplify `border: medium none currentcolor` whereas Firefox serializes it to `border: medium none`. I think I read somewhere why Firefox removes `currentcolor`. My intention was to omit the initial values in canonical order until only one remains... 

Anyway, thanks for the reply. I still think that the `initial` field should represent a value as if it were serialized, because this is what the user will see with `console.log(style.borderColor)`, but there are too many open issues much more important than this one.

**EDIT 2:** I just came accross [`text-emphasis-color`](https://drafts.csswg.org/css-text-decor-4/#propdef-text-emphasis-color), whose initial value is lowercase `currentcolor`.
