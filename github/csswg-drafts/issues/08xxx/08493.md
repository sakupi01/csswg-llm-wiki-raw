---
number: 8493
title: "[css-grid-2] Subgrid auto repeat calculation when <line-name-list> size isn't a multiple of remaining subgrid tracks"
state: "open"
labels: ["Needs Edits", "css-grid-2"]
author: "KurtCattiSchmidt"
created_at: "2023-02-23T23:08:17Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/8493"
comments: 6
has_resolution: true
synced_at: "2026-07-04T04:45:17Z"
---

https://www.w3.org/TR/css-grid-2/#auto-repeat

If we have a subgrid with the following column definition:

`grid-template-columns: subgrid repeat(auto-fill, [y] [z]); `

...and a subgrid that spans 1 column, should the auto-repeat count be 0? This is how Firefox and Safari have implemented it (and how I'm working on implementing it on Chromium).

Another option would be to just insert the [y] once, and have a half repetition. The spec says "repeats enough times for the name list to match the [subgrid](https://www.w3.org/TR/css-grid-2/#subgrid)’s specified [grid span](https://www.w3.org/TR/css-grid-2/#grid-span) (falling back to 0 if the span is already fulfilled).", but I would argue that in this example, the span isn't fulfilled, so it could be clarified further.

To generalize, this situation involves any time the remaining tracks for calculating a subgrid's auto-repeat is not a multiple of the size of the numbers of lines in the auto repeater's <line-name-list>. Should the number of repetitions be maximized (and thus allow for a non-integer number of repeats), or should it be floored at the largest whole number that will fit? 

Note that the spec linked above indicates that the number of repetitions should be an integer, but only in the standalone grid section. For subgrids, this is ambiguous, but I do think that the current interoperable behavior makes sense.

There are some WPT tests reinforcing that it should be an integer value floored at 0, such as http://[wpt.live/css/css-grid/subgrid/repeat-auto-fill-008.html](http://wpt.live/css/css-grid/subgrid/repeat-auto-fill-008.html) - see sub-case `fill-0t`. An extra half repetition could be used, so instead of:

`grid-template-columns: subgrid [w] repeat(auto-fill, [y] [z]) [x]; /* [w] [y] [z] [y] [z] [x] [] */`

It could be resolved to:

`grid-template-columns: subgrid [w] repeat(auto-fill, [y] [z]) [x]; /* [w] [y] [z] [y] [z] [y] [x] */`

<!-- comment id=1442554849 author=bfgeek created=2023-02-23T23:12:28Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-1442554849 -->
## @bfgeek — 2023-02-23

cc/ @tabatkins @fantasai 

<!-- comment id=1443373445 author=Loirooriol created=2023-02-24T09:49:00Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-1443373445 -->
## @Loirooriol — 2023-02-24

I'd keep it consistent with the standalone axis and do not allow half repetitions.

<!-- comment id=1443771023 author=Loirooriol created=2023-02-24T14:38:37Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-1443771023 -->
## @Loirooriol — 2023-02-24

If authors want partial repetitions, we could add something like `repeat(auto-fit partial, ...)` and use it for both standalone and subgrid cases.

<!-- comment id=1444720306 author=tabatkins created=2023-02-24T23:40:19Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-1444720306 -->
## @tabatkins — 2023-02-24

Yeah, the spec needs fixing, but it definitely is intended to be an integer number of whole repetitions. Partial repetitions are fragile and unlikely to be what the author wants.

<!-- comment id=2762854315 author=fantasai created=2025-03-28T23:34:48Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-2762854315 -->
## @fantasai — 2025-03-28

My reading is actually the opposite: that we should be allowing partial repetitions in this case. That's more likely to be what the author wants than to have a handful of unnamed lines at the end, imho.

<!-- comment id=3184627659 author=css-meeting-bot created=2025-08-13T16:34:50Z url=https://github.com/w3c/csswg-drafts/issues/8493#issuecomment-3184627659 resolution=true -->
## @css-meeting-bot — 2025-08-13

The CSS Working Group just discussed `[css-grid-2] Subgrid auto repeat calculation when <line-name-list> size isn't a multiple of remaining subgrid tracks`, and agreed to the following:

* `RESOLVED: define that subgrid repeat works exactly the same as grid repeat`

<details><summary>The full IRC log of that discussion</summary>
&lt;JoshT> fantasai: there was a question of what to do if the subgrid has a repeat() with multiple line names<br>
&lt;Kurt> q+<br>
&lt;TabAtkins> q+<br>
&lt;JoshT> ... let's say two line names but the number of lines in subgrid is odd. do you just cut the last repetition or just the names don't ger assigned for the last section<br>
&lt;JoshT> ... my intent when I wrote the spec was that we only do whole numbers in repetitions in the repeat functions<br>
&lt;JoshT> ... here the number of lines is set and we just have to give names<br>
&lt;JoshT> ... if we cut off at the last rep, there are some lines that don't get names<br>
&lt;JoshT> ... but if we clamp, they all get names<br>
&lt;astearns> ack Kurt<br>
&lt;JoshT> ... that seems more reasonable than cutting up the last repetition<br>
&lt;JoshT> Kurt: was going to distingush between whether subgrid span size is large enough for a single repeat<br>
&lt;JoshT> ... but I see that's generalised to fantasai's case for splitting the remainder<br>
&lt;JoshT> ... that was my question. i think it's the same problem<br>
&lt;JoshT> ... but my concern with changing this is: is it safe to change at this point?<br>
&lt;astearns> ack TabAtkins<br>
&lt;JoshT> ... subgrid is shipped. are we OK changing how auto-repeat changes in these scenarios?<br>
&lt;JoshT> TabAtkins: I do not think we should change behaviour<br>
&lt;JoshT> ... if you have several repeated lines, you are probably depending on them as a block<br>
&lt;JoshT> ... and putting things next to those lines. so if you have partial repetition, your layout will be broken<br>
&lt;JoshT> ... they'll be too short or ???, etc<br>
&lt;JoshT> ... in normal grid, we don't allow partial repetitions to prevent accidentally getting a partial bit of something the author intended to be a unit<br>
&lt;JoshT> ... so like here if you're just defining line names<br>
&lt;oriol> q+<br>
&lt;JoshT> ... if there was a great reason to diverge, we should be self-consistent when we can. so I think the argument here is to be consistent anyway<br>
&lt;astearns> ack oriol<br>
&lt;JoshT> oriol: I agree with TabAtkins about consistency argumeny<br>
&lt;JoshT> ... I had idea in issue. maybe partial keyword to provide to repeat function to opt into this behaviour<br>
&lt;kizu> +1 to `partial` keyword, including in regular grids as well<br>
&lt;JoshT> fantasai: I'm OK with it!<br>
&lt;JoshT> astearns: resolution for this issue is to keep the subgrid autorepeat consistent with the grid autorepeat, but make a new issue for the partial keyword in the repeat function if we find a use case for it<br>
&lt;JoshT> ... is there any spec change for this issue needed?<br>
&lt;JoshT> fantasai: seems like there should be some clarification<br>
&lt;JoshT> TabAtkins: this was raised on the assumption that something is missing in the spec<br>
&lt;JoshT> RESOLVED: define that subgrid repeat works exactly the same as grid repeat<br>
&lt;JoshT> Kurt: that resolves things<br>
</details>
