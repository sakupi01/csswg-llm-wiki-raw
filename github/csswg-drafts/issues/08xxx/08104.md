---
number: 8104
title: "[css-anchor] Implicit anchors in fallback to enable reuse"
state: "closed"
labels: ["Closed Accepted by Editor Discretion", "css-anchor-position-1"]
author: "tabatkins"
created_at: "2022-11-18T22:38:26Z"
closed_at: "2023-01-30T23:43:20Z"
url: "https://github.com/w3c/csswg-drafts/issues/8104"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:44:25Z"
---

Currently, [position fallback](https://w3c.github.io/csswg-drafts/css-anchor/#fallback) just applies some positioning properties directly from the `@try` blocks. This means that if your element currently anchors to a `--foo` anchor, with your normal positioning written like `top: anchor(--foo bottom);`, you have to write it out similarly in your fallbacks as like `top: auto; bottom: anchor(--foo top);`.

This is fine and easy to understand for unique anchoring styles applied to a single element on the page, but is extremely cumbersome when you're applying the same fallback behavior to many elements on the page (a whole bunch of tooltips, for example). Each one ends up needing a unique `@position-fallback` block which is exactly identical except for the anchor name in the `anchor()` functions.

I propose that we allow the `position-fallback` property to additionally specify an anchor name, and any `anchor()` function using the "implicit anchor element" (omitted anchor name) feature in the `@try` blocks uses the specified anchor name.

That is, you can write something like:

```css
.foo-popup {
  bottom: anchor(--foo top);
  position-fallback: --flip-to-bottom --foo;
}

.bar-popup {
  bottom: anchor(--foo top);
  position-fallback: --flip-to-bottom --bar;
}

@position-fallback --flip-to-bottom {
  @try {
    bottom: auto;
    top: anchor(bottom); // refers to --foo or --bar, depending
  }
}
```

----

Approaching the same problem slightly differently, perhaps we want to generalize and give a way to explicitly specify how the implicit anchor element is resolved? That would allow even more style sharing, like:

```css
.popup {
  bottom: anchor(top); // refers to --foo or --bar, depending
  position-fallback: --flip-to-bottom;
}

.foo-popup {
  anchor-default: --foo;
}
.bar-popup {
  anchor-default: --bar;
}
```

`anchor-default` would also let you refer explicitly to other sources of implicit anchors, so you could switch between them. (Currently only the Popover API provides such, but there's always the possibility of more.) So a grammar like:

```
none | auto | popover | <<dashed-ident>>
```

With `auto` being the initial value, with a well-defined ordering in the spec for deciding which source of implicit anchor elements to choose.

<!-- comment id=1409529122 author=tabatkins created=2023-01-30T23:43:20Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1409529122 -->
## @tabatkins — 2023-01-30

Fixed in 530c6e138. I went with the final approach in my comment - if you omit the anchor name, it'll now take the anchor target from the 'anchor-default' property, which has the same value space as the anchor function itself.

<!-- comment id=1412885820 author=xiaochengh created=2023-02-01T23:15:55Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1412885820 -->
## @xiaochengh — 2023-02-01

How about removing the `popover` keyword? The `implicit` keyword already does it all.

Also we probably don't want to add another keyword if another spec (like selectmenu) also defines an implicit anchor.

<!-- comment id=1412907202 author=tabatkins created=2023-02-01T23:36:06Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1412907202 -->
## @tabatkins — 2023-02-01

My intention is that we *will* add another keyword, indeed, so you can select which implicit anchor to use, just in case your element defines several.

I can remove it if we think that elements will never really run into this situation and just rely on "implicit", tho.

<!-- comment id=1412921507 author=xiaochengh created=2023-02-01T23:55:00Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1412921507 -->
## @xiaochengh — 2023-02-01

I still think we should remove it.

As we [only allow one implicit anchor element](https://drafts4.csswg.org/css-anchor-1/#implicit-anchor-element), the `implicit` keyword is already enough.

And I don't think we should ever allow multiple anchor elements defined by multiple specs.

The only concern is that we are now leaving the responsibility of not redefining implicit anchor element to the other specs, which means there will be an inter-connecting dependency between them. But maybe this is not a big issue at such an early stage.

<!-- comment id=1413031852 author=xiaochengh created=2023-02-02T01:44:50Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1413031852 -->
## @xiaochengh — 2023-02-02

Btw `anchor-scroll` should also be able to use the default anchor

<!-- comment id=1426275225 author=tabatkins created=2023-02-10T20:01:19Z url=https://github.com/w3c/csswg-drafts/issues/8104#issuecomment-1426275225 -->
## @tabatkins — 2023-02-10

Okay, removed `popover`. If we do need to extend the set of possible implicit anchors, we can do that in the future; there's no need to make it generic right now.
