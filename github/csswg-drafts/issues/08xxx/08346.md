---
number: 8346
title: "[css-images] Define `<zero>` as an alternative for `<angle>`"
state: "closed"
labels: ["Needs Edits", "css-images-3", "css-images-4"]
author: "cdoublev"
created_at: "2023-01-21T10:34:53Z"
closed_at: "2025-03-18T13:29:38Z"
url: "https://github.com/w3c/csswg-drafts/issues/8346"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:45:08Z"
---

According to [CSS Values 4](https://drafts.csswg.org/css-values-4/#angles), `0` is an invalid `<angle>` if not otherwise defined explicitly:

  > For legacy reasons, some uses of `<angle>` allow a bare `0` to mean `0deg`. This is not true in general, however, and will not occur in future uses of the `<angle>` type.

[`<conic-gradient()>`](https://drafts.csswg.org/css-images-4/#funcdef-conic-gradient) and [`<linear-gradient()>`](https://drafts.csswg.org/css-images-3/#funcdef-linear-gradient) define `0` in prose as a valid `<angle>` representing a gradient paremeter (origin or direction). But it is not defined valid in the [color stop list](https://drafts.csswg.org/css-images-4/#color-stop-syntax) of `<conic-gradient()>`, whereas it is valid in Chrome/FF.

If I am not mistaken, these would be the two remaining productions defining `0` as a valid `<angle>` in prose instead of in basic syntax. For example, [`<hue>`](https://drafts.csswg.org/css-color-4/#typedef-hue) in color functions accepts `<number>`, [transform functions](https://drafts.csswg.org/css-transforms-1/#two-d-transform-functions) accept `<zero>`, etc.

I suggest to define `<zero>` as an alternative of `<angle>` in `<conic-gradient()>` and `<linear-gradient()>`, which would allow to remove the above note in CSS Values.

```diff
  conic-gradient() = conic-gradient(
-   [ [ [ from <angle> ]? [ at <position> ]? ] || <color-interpolation-method> ]? ,
+   [ [ [ from [ <angle> | <zero> ] ]? [ at <position> ]? ] || <color-interpolation-method> ]? ,
    <angular-color-stop-list>
  )
```

```diff
  <linear-gradient()> = linear-gradient(
-   [ <angle> | to <side-or-corner> ]? ,
+   [ <angle> | <zero> | to <side-or-corner> ]? ,
    <color-stop-list>
  )
```

```diff
-  <angular-color-hint> = <angle-percentage>
-  <color-stop-angle> = <angle-percentage>{1,2}
+  <angular-color-hint> = <angle-percentage> | <zero>
+  <color-stop-angle> = [<angle-percentage> | <zero>]{1,2}
```

<!-- comment id=2731608193 author=yisibl created=2025-03-18T04:36:50Z url=https://github.com/w3c/csswg-drafts/issues/8346#issuecomment-2731608193 -->
## @yisibl — 2025-03-18

Does this need a specification update?

<!-- comment id=2731907310 author=cdoublev created=2025-03-18T07:10:54Z url=https://github.com/w3c/csswg-drafts/issues/8346#issuecomment-2731907310 -->
## @cdoublev — 2025-03-18

Requested in #11952.

While adding `<zero>` to the grammar of conic/linear gradient rotations is an editorial change, adding `<zero>` to the grammar of angular color stop/hint is not. It matches the current output in Chrome and FF though:

```js
el.style.backgroundImage = 'conic-gradient(from 0, blue 0 0, yellow)'
el.style.backgroundImage // conic-gradient(from 0deg, blue 0deg, blue 0deg, yellow)
el.style.backgroundImage = 'linear-gradient(0, blue, yellow)'
el.style.backgroundImage // linear-gradient(0deg, blue, yellow)
```

WPT tests may need to be added/updated:

  - for the [conic/linear gradient rotation](https://github.com/web-platform-tests/wpt/tree/master/css/css-images/parsing)
  - for the (angular) [color stop list](https://github.com/web-platform-tests/wpt/blob/master/css/css-images/gradient/color-stops-parsing.html)

<!-- comment id=2731937391 author=yisibl created=2025-03-18T07:23:40Z url=https://github.com/w3c/csswg-drafts/issues/8346#issuecomment-2731937391 -->
## @yisibl — 2025-03-18

Nice, this is also consistent with the definition of `rotate()`.

```
rotate( [ <angle> | <zero> ] )
```
