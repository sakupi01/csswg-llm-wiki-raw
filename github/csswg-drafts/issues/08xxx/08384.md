---
number: 8384
title: "[css-values-5] Minor problems on `toggle()` and `random-*()` productions"
state: "closed"
labels: ["css-values-5"]
author: "cdoublev"
created_at: "2023-02-01T08:56:00Z"
closed_at: "2023-11-13T20:21:59Z"
url: "https://github.com/w3c/csswg-drafts/issues/8384"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:44:38Z"
---

**[`toggle()`](https://drafts.csswg.org/css-values-5/#funcdef-toggle)**

`toggle()` cannot be extracted by `w3c/reffy` because it is not defined in a production rule. I do not know if you want to declare it as a function or type. I do not know what difference it makes.

---

**[`random-item()`](https://drafts.csswg.org/css-values-5/#funcdef-random-item)**

I think `<any-value>` is inappropriate in `<random-item()>` and should be replaced with `<declaration-value>?`, because `<any-value>` allows top-level `;` and because custom properties accept empty inputs (like in `<mix()>`, see #8556):

`<random-item()> = random-item( <random-caching-options> ';' <any-value> [ ';' <any-value> ]* )`

---

**[`random()`](https://drafts.csswg.org/css-values-5/#funcdef-random)**

Finally, the trailing `;` in the production for `<random()>` seems to be a typo:

  > `<random()> = random( <random-caching-options>? , <calc-sum>, <calc-sum>, [by <calc-sum>]? );`

<!-- comment id=1412202808 author=cdoublev created=2023-02-01T15:01:21Z url=https://github.com/w3c/csswg-drafts/issues/8384#issuecomment-1412202808 -->
## @cdoublev — 2023-02-01

**Aside:** could you please tell me what `q` stands for in `<q-name>` in `attr()` (`q`ualified?), and `<wq-name>` in `<attribute-selector>`, and why different productions are used?

<!-- comment id=1414289494 author=tabatkins created=2023-02-02T19:57:46Z url=https://github.com/w3c/csswg-drafts/issues/8384#issuecomment-1414289494 -->
## @tabatkins — 2023-02-02

`<q-name>` is a typo; it should be `<wq-name>`. "wq" is taken from the XML specs, where it stands for "well-qualified", meaning "a namespace + localname pair".

> Similarly, I think `<any-value>` is inappropriate in `<random-item()>`, and should be replaced by `<declaration-value>`

Yes.

> I do not know if you want to declare it as a function or type. I do not know what difference it makes.

A function. It changes how I do some processing in bikeshed, but most notably how they autolink - you can link to a function with `''foo()''`.

> Finally, the trailing ; in the production for <random()> seems to be a typo:

Not necessarily a typo, I just forget whether we separate production definitions with a semicolon or not. ^_^

<!-- comment id=1793399103 author=cdoublev created=2023-11-04T09:53:55Z url=https://github.com/w3c/csswg-drafts/issues/8384#issuecomment-1793399103 -->
## @cdoublev — 2023-11-04

I edited the initial comment of this issue to remove my suggestion to replace `<toggle-value>` with `<declaration-value>?` in `toggle()`, since it is now replaced with `<whole-value>`.
