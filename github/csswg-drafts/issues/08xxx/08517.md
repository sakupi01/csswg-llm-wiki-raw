---
number: 8517
title: "[css-scoping-1][css-shadow-parts-1] Clarify which selectors can come after `::slotted()` and `::part()`"
state: "closed"
labels: ["css-shadow-parts-1"]
author: "cdoublev"
created_at: "2023-03-02T15:28:41Z"
closed_at: "2024-02-21T17:28:51Z"
url: "https://github.com/w3c/csswg-drafts/issues/8517"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:44:44Z"
---

If I understand correctly, basically, these selectors pierce the corresponding shadow DOM but they cannot read further from the destination DOM (light or shadow) than the selected element.

[`::slotted()`](https://drafts.csswg.org/css-scoping-1/#selectordef-slotted) and [`::part()`](https://drafts.csswg.org/css-shadow-parts-1/#selectordef-part) have different restrictions on the pseudo-classes and pseudo-elements that can be compounded:

  > The `::slotted()` pseudo-element can be followed by a tree-abiding pseudo-element, like `::slotted()::before`, representing the appropriate pseudo-element of the elements represented by the `::slotted()` pseudo-element.

  > The `::part()` pseudo-element can take additional pseudo-classes after it, such as `x-button::part(label):hover`, but never matches the structural pseudo-classes or any other pseudo-classes that match based on tree information rather than local element information.
  >
  > The `::part()` pseudo-element can also take additional pseudo-elements after it, such as `x-button::part(label)::before`, but never match additional `::part()`s.

I would suggest defining both with *they can take any pseudo-class or pseudo-element after it that do not require tree information rather than local element information*. 

This would exclude these pseudo-elements themselves, and structural pseudo-classes.

<sub>Edit 1: I seem to have misunderstood `:host-*` because I thought one could select `:host + div`, for example, but `:host > *` selects elements in the shadow DOM (it seems silly to use such selector but anyway), therefore I removed it from my original question.</sub>

<sub>Edit 2: I made an explicit suggestion.</sub>

<!-- comment id=1460097190 author=cdoublev created=2023-03-08T12:42:20Z url=https://github.com/w3c/csswg-drafts/issues/8517#issuecomment-1460097190 -->
## @cdoublev — 2023-03-08

Actually, I should not have edited my original comment to omit `:host-*` because all corresponding cases in the table below seem to select nothing in Chrome (C) and Firefox (FF), even if they are parsed as valid:

| Selector                   | C  | FF
| -------------------------- | -- | --
| `:host:hover`              | ✔️ | ✔️
| `:host:not(r > t)`         | ✔️ | ✔️
| `:host:is(s > t)`          | ✔️ | ✔️
| `:host:has(c)`             | ✔️ | ❌
| `:host:first-child`        | ✔️ | ✔️
| `::slotted(t):hover`       | ❌ | ❌
| `::slotted(t):not(:hover)` | ❌ | ❌
| `::slotted(t):is(t)`       | ❌ | ✔️
| `::slotted(t):is(s > t)`   | ❌ | ✔️
| `::part(p):hover`          | ✔️ | ✔️
| `::part(p):not(:hover)`    | ✔️ | ✔️
| `::part(p):not(u)`         | ✔️ | ❌
| `::part(p):not(r > t)`     | ✔️ | ❌
| `::part(p):is(s > t)`      | ✔️ | ✔️

I think `:host` cases (except `:host:hover`) match nothing because the following pseudo-classes are evaluated simultaneously therefore the tree information (`r > t`, `s > t`, etc) is unavailable.

I am not sure when a logical combination compounded to `::part()` is based on tree information (when it includes complex selectors?) and should match nothing, but all corresponding cases in the table select the matching elements in Chrome but only those with `:hover` in FF.

In hindsight, I think I would rather let a selector match nothing (like `:host` cases), because validation during parsing can be non-trivial, and because invalid selectors can make the whole rule invalid.

<sub>Edit: fixed my analysis on the validity of cases including logical combinations</sub>

<!-- comment id=1957406427 author=cdoublev created=2024-02-21T17:28:52Z url=https://github.com/w3c/csswg-drafts/issues/8517#issuecomment-1957406427 -->
## @cdoublev — 2024-02-21

Closing because the specs are clear:

  - `::slotted()` accept user actions and logical combination pseudo-classes, and tree-abiding pseudo-elements
  - `::part()` accept all pseudos
