---
number: 8862
title: "[css-syntax-3] `non-ASCII ident code point` might be breaking because it excludes the use of emojis in idents"
state: "closed"
labels: ["Closed Rejected as Invalid", "css-syntax-3"]
author: "romainmenke"
created_at: "2023-05-20T16:23:06Z"
closed_at: "2023-05-22T20:38:12Z"
url: "https://github.com/w3c/csswg-drafts/issues/8862"
comments: 3
has_resolution: false
synced_at: "2026-07-04T04:44:30Z"
---

_As always, I might just have a bug :)_

After implementing https://drafts.csswg.org/css-syntax/#non-ascii-ident-code-point I had a bunch of test failures because emojis are no longer valid in idents.

This works today in all browsers :

```css
:root {
	--💅: hotpink;
}

body {
	background-color: var(--💅);
}
```

I don't think writing CSS like this is a good idea, but I know that we have had reports about it not working for people, so that is at least an indicator that someone did something like this.

https://github.com/postcss/postcss-custom-properties/issues/237

------------

If it is correct that these codepoints are no longer valid in idents it might be needed to check if this will break more than just a few experiments by people.

<!-- comment id=1555946738 author=romainmenke created=2023-05-20T16:28:21Z url=https://github.com/w3c/csswg-drafts/issues/8862#issuecomment-1555946738 -->
## @romainmenke — 2023-05-20

```js
'💅'.charCodeAt(0).toString(16);
// 'd83d'

'💅'.charCodeAt(1).toString(16)
// 'dc85'
```

Seems like there are a lot of these in between these two range :
> between U+3001 and U+D7FF
> between U+F900 and U+FDCF

<!-- comment id=1557953245 author=tabatkins created=2023-05-22T20:38:08Z url=https://github.com/w3c/csswg-drafts/issues/8862#issuecomment-1557953245 -->
## @tabatkins — 2023-05-22

Those are not codepoints that you're looking at.

```js
"💅".codePointAt(0).toString(16)
// '1f485'
```

The Syntax spec runs on a stream of code *points*, but JS naively returns code *units*, which are limited to the range 0-ffff. To encode any higher code points JS splits them into two code units using the reserved surrogate range, from d800-dfff. You have to account for this when parsing strings from JS. ([My parser](https://github.com/tabatkins/parse-css/blob/main/parse-css.js#L38) just eagerly converts the whole string into an array of codepoints, but these days you can do that more easily with an `Array.from(str, cp=>cp.codePointAt(0))` call, since the iterator protocol on strings advances by codepoint.)

<!-- comment id=1558076014 author=romainmenke created=2023-05-22T21:57:31Z url=https://github.com/w3c/csswg-drafts/issues/8862#issuecomment-1558076014 -->
## @romainmenke — 2023-05-22

Awesome!
I was hoping this was a bug on my end.

🙇 
