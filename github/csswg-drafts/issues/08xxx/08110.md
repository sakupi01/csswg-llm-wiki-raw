---
number: 8110
title: "[css-conditional-5] Remove definitions of `<font-format>` and `<font-tech>`"
state: "closed"
labels: ["css-conditional-5"]
author: "cdoublev"
created_at: "2022-11-21T09:07:48Z"
closed_at: "2023-07-01T13:34:48Z"
url: "https://github.com/w3c/csswg-drafts/issues/8110"
comments: 4
has_resolution: false
synced_at: "2026-07-04T04:44:31Z"
---

Would it be possible to remove [the definitions of `<font-format>` and `<font-tech>` from CSS Conditional 5](https://drafts.csswg.org/css-conditional-5/#at-supports-ext)? 

The spec says:

> This level of the specification extends the `<supports-feature>` syntax

But it does not extend these subproductions. Worse: `<font-format>` is only a subset of what is defined in CSS Fonts.

These duplicate definitions must be specifically handled in `w3c/webref`, by defining an "authoritative source" for the production, which is not great.

<!-- comment id=1330516749 author=drott created=2022-11-29T11:58:55Z url=https://github.com/w3c/csswg-drafts/issues/8110#issuecomment-1330516749 -->
## @drott — 2022-11-29

> Worse: <font-format> is only a subset of what is defined in CSS Fonts.

Are you referring to the `<string>` production? This is the legacy part of `format()` which is intentionally not included in the conditionals definition. 

Regarding the PR: What does the spec look like after bikeshed processing - do the font-tech and font-format become clickable references to fonts-4? `font-format` would then no longer be correctly defined.

<!-- comment id=1330621788 author=cdoublev created=2022-11-29T13:03:00Z url=https://github.com/w3c/csswg-drafts/issues/8110#issuecomment-1330621788 -->
## @cdoublev — 2022-11-29

> Are you referring to the `<string>` production?

Yes. Should it be invalid in `@supports` but valid in `src` for `@font-face`? 

If so, I will close my PR but I think it should be defined in prose (more simple) or in the syntax. I think rewriting the syntax would require to not define `font-tech()` as a type (`<font-tech()> = ...`) but as a context-sensitive function (`font-tech() = ...`).

I did not check the result [in Bikeshed](https://api.csswg.org/bikeshed/), which reports an error for duplicate type refs of `<font-tech>` in CSS Conditional 5 and CSS Fonts 5, and generates a link to CSS Fonts 5 for `<font-format>`. I do not know well how it works in details and I do not really understand why it generates a link to CSS Fonts 5 for `<font-format>` but not `<font-tech>`.

<!-- comment id=1522905540 author=cdoublev created=2023-04-26T07:15:54Z url=https://github.com/w3c/csswg-drafts/issues/8110#issuecomment-1522905540 -->
## @cdoublev — 2023-04-26

I updated the PR to import `<font-format>` and `<font-tech>` from CSS Fonts by defining that _"a CSS processor is considered to support a font format when it is capable of utilising the specified font format in layout and rendering, **and this format is not specified as a `<string>`**"_, which is similar to [the prose](https://drafts.csswg.org/css-conditional-4/#dfn-support-selector) excluding an unknown `-webkit` pseudo-element for a supported selector.

<!-- comment id=1538436385 author=vitorroriz created=2023-05-08T14:15:20Z url=https://github.com/w3c/csswg-drafts/issues/8110#issuecomment-1538436385 -->
## @vitorroriz — 2023-05-08

At https://www.w3.org/TR/css-conditional-5/#at-supports-ext, It is confusing for me to read:

> Note: The allowed values for the font-format() function are the same as those for the format() function in the '@font-face' [src](https://www.w3.org/TR/css-fonts-4/#descdef-font-face-src) descriptor.

Given that at @font-face src we accept `<string>` for legacy reasons.
