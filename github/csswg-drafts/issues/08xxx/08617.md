---
number: 8617
title: "[css-cascade-6] Introduce `:scope-end` pseudo to target end boundary"
state: "open"
labels: ["css-cascade-6"]
author: "bramus"
created_at: "2023-03-20T10:05:50Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/8617"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:39Z"
---

In https://github.com/w3c/csswg-drafts/issues/8377 we resolved on allowing `:scope` within `@scope` to refers to the scope root element matched by `<scope-start>`. In that same thread, the idea for a `:scope-end` selector was also proposed. It would target the element(s) matched by the `<scope-end>`.

```css
@scope (.card) to (.content) {

  /* targets the .card scope start element */
  :scope {
    …
  }

  /* targets any img in between .card and .content scope elements */
  img {
    …
  }

  /* targets the .content scope end element */
  :scope-end {
    …
  }

}
```

In case no `<scope-end>` is set, `:scope-end` would match no element.

This tackles earlier questions of _“should the boundaries be included or not?”_, where the hacky fix was to adjust the end boundary selector to `(.content > *)` in order to be able to access `.content` from within `@scope`.

<!-- comment id=1484707417 author=DarkWiiPlayer created=2023-03-27T08:18:24Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-1484707417 -->
## @DarkWiiPlayer — 2023-03-27

I think this solves a different issue than `(.content > *)` lower boundary.

One is useful for including the intended lower boundary in the scoped rules like any other element, so a `p.lower-boundary` would have any scoped styles for `p` applied, but not any nested `p` elements.

`:scope-end` would provide a more explicit way of selecting only the lower boundary, regardless of what rule it matches.

There might also be cases where we might want to combine them by having `(.lower > *)` as our boundary and selecting `:has(> :scope-end)` to get to the "inclusive" lower boundary instead of what CSS would consider the actual boundary.

<!-- comment id=1850790658 author=mirisuzanne created=2023-12-11T19:58:50Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-1850790658 -->
## @mirisuzanne — 2023-12-11

I think it's an interesting proposal. I imagine it would only be allowed as the subject of a selector, so you can't select things nested further past the boundary (tho subject of the has selector might still work).

The main downside I see is maybe what @DarkWiiPlayer is getting at. If you want to a selector that includes (but is not limited to) lower boundaries, this would require duplication:

```css
@scope (.media) to (.content) {
  .my-usual.selector,
  .my-usual.selector:scope-end { /* match boundaries and non-boundaries */ }
}
```

There would be workarounds for that, like `.my-usual.selector:is(:scope-end, *)` - but I'm not sure that's ideal. Still maybe a useful feature? We could bring this to the group for further discussion?

<!-- comment id=2264446580 author=mantou132 created=2024-08-02T03:29:28Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-2264446580 -->
## @mantou132 — 2024-08-02

I'm trying to use `@scope` [instead](https://github.com/mantou132/gem/blob/c2dd5599f4af0a1bb73022b2b9d36f2047291027/packages/gem/src/lib/element.ts#L82) of Shadow DOM style isolation, so I hope to add syntax to scope to include boundaries, e.g: `@scope (my-element) to include (:state(style-scoped))`:

This is convenient for different styles of different boundaries, example:

content:
```html
<my-element #scope>
  <children-element></children-element>
  <other-element></other-element>
```

shadow dom style: 
```css
:host { ... }
children-element { ... }
other-element { ... }
```

`@scope` style(add `@scope (my-element) to  include (:state(style-scoped)) { }` warpper):
```css
:scope {...}
children-element { ... }
other-element { ... }
```

<!-- comment id=2264715385 author=DarkWiiPlayer created=2024-08-02T07:07:23Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-2264715385 -->
## @DarkWiiPlayer — 2024-08-02

@mantou132 This was discussed a while ago over here #6577, iirc the consensus was more or less that `to (:state(style-scoped) > *) { }` is "good enough" and and avoids the bikeshedding of figuring out the perfect keyword for inclusive lower boundaries.

It would be interesting to see some real-world-ish examples though, in case you have any that you could post there.

<!-- comment id=4282423903 author=tomahl created=2026-04-20T16:09:22Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-4282423903 -->
## @tomahl — 2026-04-20

I also think that `:scope-end` would have improved ergonomics and created more semantic, readable code. Today, achieving the same result requires `.my-class > *`—a workaround that feels like a hack rather than a standard feature.

<!-- comment id=4282486783 author=mirisuzanne created=2026-04-20T16:18:40Z url=https://github.com/w3c/csswg-drafts/issues/8617#issuecomment-4282486783 -->
## @mirisuzanne — 2026-04-20

This doesn't have to be past tense. We can still add a scope-end selector, or inclusive variant of scoping. It's just not in the first level of the spec.
