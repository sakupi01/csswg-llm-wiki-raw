---
number: 9064
title: "[css-view-transitions-2] Do a ViewTransition for orientation changes"
state: "open"
labels: ["css-view-transitions-2"]
author: "khushalsagar"
created_at: "2023-07-13T18:56:29Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/9064"
comments: 0
has_resolution: false
synced_at: "2026-07-04T04:44:32Z"
---

If the device is rotated, the page is going to be laid out based on the new orientation. Would be cool to do a ViewTransition there. The following points need to be sorted out.

- How does the transition get initiated? We need to give developers a hook to add `view-transition-names` before the capture is done so we likely need an event which is fired with the new orientation but before the Document is actually resized. Maybe the developer could call startVT in this event to indicate they want to use VT for the rotation.
- If startVT is called then the browser should apply the new size before dispatching the update callback.
- Right now we abort transitions if the snapshot containing block size mismatches before/after the transition. That will need to switch to transposing all the captured state (transform, size, the image itself) to map the captured state to the new orientation.
