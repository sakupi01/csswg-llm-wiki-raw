---
number: 9841
title: "[css-cascade-6] Scoping the subject of a `:has()` selector"
state: "open"
labels: ["css-cascade-6"]
author: "mirisuzanne"
created_at: "2024-01-23T19:18:39Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/9841"
comments: 7
has_resolution: false
synced_at: "2026-07-04T04:45:16Z"
---

This issue was [raised on social media](https://hachyderm.io/@hi_mayank/111801080201900214) by @mayank99. The goal is to select a scope that has a given element _in scope_. However, since `@scope` only requires the 'subject' to be in-scope, and the contents of `has` are not selecting the subject, there is not currently any way to achieve this.

Here's [a code-pen for experimenting](https://codepen.io/miriamsuzanne/pen/bGZrmGx?editors=1100).

It seems like this might be solvable (though not elegant) with a `:scope-end` selector ([already proposed in another thread](https://github.com/w3c/csswg-drafts/issues/8617)) that matches lower boundary elements:

```css
@scope (my-scope) to (my-scope) {
  :scope:has(a:not(:scope-end a)) { … }
}
```

(You can get close to that solution by replacing `:scope-end` with `my-scope` - but it becomes fragile with further nesting.)

Since this issue seems specific to the `:has()` selector, the other approach might be changes on that end:

- Add a `:scope-has()` selector that does a scoped version of the same thing. It's clear and to the point, but maybe a bit of a special case?
- Resolve `:has()` differently when it's used on the `:scope()` selector. This is even more of a heuristic special case, but it really gets to the point of the issue. We want to know if the scope has something.

Other ideas?

The [relevant spec text](https://www.w3.org/TR/css-cascade-6/#scoped-style-rules) is:

> Their selectors can only match elements that are in scope. (This only applies to the subject; the rest of the selector can match unrestricted.)

<!-- comment id=1906796393 author=mirisuzanne created=2024-01-23T19:37:06Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-1906796393 -->
## @mirisuzanne — 2024-01-23

It does seem to me like this should work actually? What am I missing?

```css
@scope (section) to (section) {
  :scope:has(a:not(:scope section *)) {
    background: lavender;
  }
}
```

(at least in Chrome, this does not currently select anything - but I'm not clear why)

<!-- comment id=1906802621 author=kizu created=2024-01-23T19:41:40Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-1906802621 -->
## @kizu — 2024-01-23

> @mirisuzanne 
> (at least in Chrome, this does not currently select anything - but I'm not clear why)

I wonder if this is just a bug with `:scope` inside functional pseudo-classes? I did report recently a similar one with `:scope` inside `:is()`, so maybe it will be worth it reporting this as well: https://bugs.chromium.org/p/chromium/issues/detail?id=1512217

<!-- comment id=1906809797 author=mayank99 created=2024-01-23T19:46:33Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-1906809797 -->
## @mayank99 — 2024-01-23

`:scope:has(a)` working out-of-the-box might make sense for this particular example.

```css
@scope (my-scope) to (my-scope) {
  :scope:has(a) {
    /* … only match if `a` is within scope */
  }
}
```

But there might be cases where you'd want the current unscoped behavior. If it's easy to opt in/out of one or the other behavior, it should be fine?

Maybe to opt out, the rule should just be written outside of `@scope` by itself. Although I do want to mention that nonstandard userland implementations of scoping provide a way to "bail out". For example, [CSS Modules](https://github.com/css-modules/css-modules) provides a `:global()` selector. This is useful when you want part of the selector to be within the scope and other parts to match globally.

```css
@scope (my-scope) to (my-scope) {
  :scope:has(:global(a)) {
    /* … use default unscoped :has behavior */
  }
}
```

I also like the `:scope-end` selector. It could probably be useful for other cases that we haven't thought of yet. Although, since the lower boundary itself is excluded from the scope, maybe that limits its usefulness.

---


> It does seem to me like this should work actually? What am I missing?
> 
> ```css
> @scope (section) to (section) {
>   :scope:has(a:not(:scope section *)) {
>     background: lavender;
>   }
> }
> ```

Wouldn't it break with further nesting, for example, five levels of nested `<section>`s?

<!-- comment id=1906893718 author=mirisuzanne created=2024-01-23T20:48:40Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-1906893718 -->
## @mirisuzanne — 2024-01-23

> Wouldn't it break with further nesting, for example, five levels of nested `<section>`s?

It shouldn't, since `:scope` always refers to the active scope-root, and won't match other potential scopes. That's the difference between `:scope` or `&` in this context. The ampersand represents the scope-selector (and could match out-of-scope sections in this context) - but there is only ever a single relevant matched `:scope` root for a scoped selector.

<!-- comment id=3031942628 author=kizu created=2025-07-03T11:36:50Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-3031942628 -->
## @kizu — 2025-07-03

In https://github.com/w3c/csswg-drafts/issues/9740 it was brought up that there is a similar case, where you could have something like `.foo :scope .bar`, or, likely more often, `:scope .bar:is(.foo *)`, where we'd want to clarify that this `.foo` should be “in scope”.

I propose to introduce something like a `:in-scope` pseudo-class, which could be then used with any selectors, and will only match for elements that are inside the current scope.

The example from the OP will be then

```CSS
@scope (my-scope) to (my-scope) {
  :scope:has(a:in-scope) { … }
}
```

<!-- comment id=3032138446 author=mayank99 created=2025-07-03T12:46:06Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-3032138446 -->
## @mayank99 — 2025-07-03

So `:in-scope` would be an opt-in way to limit any part of the selector to the scope. This is somewhat similar to [CSS Modules `:local()` references](https://github.com/css-modules/css-modules/blob/master/docs%2Fcomposition.md#exceptions).

There is some potential for confusion as you'd have to figure out which parts of the selector are automatically in scope and which ones need `:in-scope`. But I think authors could get used to it.

Do we also need something like `:out-of-scope`? Maybe not, since only the subject of the selector is required to be within scope. e.g. `html :scope` will automatically match.

<!-- comment id=3032382379 author=kizu created=2025-07-03T13:57:24Z url=https://github.com/w3c/csswg-drafts/issues/9841#issuecomment-3032382379 -->
## @kizu — 2025-07-03

> There is some potential for confusion as you'd have to figure out which parts of the selector are automatically in scope and which ones need `:in-scope`. But I think authors could get used to it.

I think right now the only rule is that only the _subject_ is required to be always in scope (well, and the `:scope` itself, of course), otherwise any other parts can be anywhere really.

> Do we also need something like `:out-of-scope`? Maybe not, since only the subject of the selector is required to be within scope. e.g. `html :scope` will automatically match.

I don't think there are many use cases for this, and we could do `:not(:in-scope)` if we'll need this for anything. Although, maybe it could still be useful to introduce a “negative” version, but I don't remember where we stand on this currently.
