---
number: 9289
title: "[css-easings-2] When does a `linear()` includes a point with a null `input`?"
state: "closed"
labels: ["Closed as Question Answered", "css-easing-2"]
author: "cdoublev"
created_at: "2023-09-01T06:34:33Z"
closed_at: "2023-09-01T08:37:46Z"
url: "https://github.com/w3c/csswg-drafts/issues/9289"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:34Z"
---

https://drafts.csswg.org/css-easing-2/#create-a-linear-easing-function

  > 5. For runs of `items` in `function`’s points that have a null `input`, [...]

Can you please add an example when this happens?

<details>
  <summary>I cannot figure it out.</summary>

```js
function createLinearEasingFunction(list) {
  const points = []
  const fn = { points }
  let largestInput = -Infinity
  list.forEach((stop, index) => {
    const [progress, time] = stop
    const point = { input: null, output: progress }
    points.push(point)
    if (isOmitted(time)) {
      if (index === 0) {
        point.input = 0
        largestInput = 0
      } else {
        point.input = Math.max(1, largestInput)
      }
      return
    }
    const [start, end] = time
    largestInput = point.input = Math.max(start / 100, largestInput)
    if (end) {
      const input = largestInput = Math.max(end / 100, largestInput)
      const extraPoint = { input, output: progress }
      points.push(extraPoint)
    }
  })
}
```
</details>

<!-- comment id=1702382054 author=cdoublev created=2023-09-01T08:37:46Z url=https://github.com/w3c/csswg-drafts/issues/9289#issuecomment-1702382054 -->
## @cdoublev — 2023-09-01

Oh, this happens for interleaved points that are specified only with an output value. I guess the coffee did not reached my brain because this seems obvious and not worth a more specific note, therefore I close this issue.
