---
number: 9465
title: "[css-anchor-position-1] A pseudo class for anchor-positioned state?"
state: "open"
labels: ["css-anchor-position-2"]
author: "xiaochengh"
created_at: "2023-10-13T04:37:59Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/9465"
comments: 6
has_resolution: false
synced_at: "2026-07-04T04:45:28Z"
---

(Related Chrome bug: https://crbug.com/1394481)

I'm wondering if a pseudo class that selects anchor-positioned elements is going to be useful. Or maybe weird.

The main motivation is to make popover styling easier. Right now popovers have a UA style sheet like

```css
[popover] {
  position: fixed;
  inset: 0;
  margin: auto;
}

[popover][anchor] {
  inset: auto;
}
```

The first part makes popovers centered in the viewport by default. And to make them easier to work with anchor positioned, the second part resets insets to `auto` if they are supposed to be anchor-positioned, indicated by the HTML `anchor` attribute.

However, this means that if we set a popover's anchor with the `anchor-default` property instead of the HTML attribute, we will also need to manually reset its insets to `auto` -- which isn't obvious to discover, and breaks my mental model of HTML `anchor` attribute and CSS `anchor-default` property are equivalent.

If we have a pseudo class selector, then we can turn the second part into

```css
[popover]:anchor-positioned {
  inset: auto;
}
```

And authors can use `anchor-default` without having to reset the insets.

@mfreed7 @una 

<!-- comment id=1763062685 author=kizu created=2023-10-14T17:46:52Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-1763062685 -->
## @kizu — 2023-10-14

I think that pseudo-class could look weird, I'm not sure if there can be other use cases for it, and if could be weird in a sense that what if we'd override the `anchor-default` within it?

My first idea for this was something like: could we always set the `inset: auto`, but override the `auto` to be something “if the element is in the overlay, and does not have an associated anchor, it resolves to 0”?

What would be the downsides of something like that?

<!-- comment id=1763232342 author=xiaochengh created=2023-10-15T01:48:39Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-1763232342 -->
## @xiaochengh — 2023-10-15

> and if could be weird in a sense that what if we'd override the anchor-default within it?

Right. That would make it super weird. So a pseudo class is probably not a working idea.

> My first idea for this was something like: could we always set the `inset: auto`, but override the auto to be something “if the element is in the overlay, and does not have an associated anchor, it resolves to 0”?

That would be ideal, but we need a more detailed solution here.

<!-- comment id=1763250542 author=bleper created=2023-10-15T03:03:29Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-1763250542 -->
## @bleper — 2023-10-15

Isn't this covered by style container queries? Like this:

```css
[popover] {  
  @container not style(anchor-default : initial) {  
    inset : auto;
   }
}
```

<!-- comment id=1763332289 author=kizu created=2023-10-15T09:21:45Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-1763332289 -->
## @kizu — 2023-10-15

> Isn't this covered by style container queries? Like this:

Similar to regular container queries we couldn't modify the element itself with them, only nested ones (for the same circularity reason).

<!-- comment id=1880650365 author=yisibl created=2024-01-08T09:33:48Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-1880650365 -->
## @yisibl — 2024-01-08

When I first tried to combine a CSS anchor with a popover, the popover was not anchored as I expected when the `anchor` property was not present.

I later realized that the addition of the `anchor` property worked as expected because of the UA's default style `[popover][anchor] { inset: auto; }`. This was confusing.

<!-- comment id=3727708613 author=lilles created=2026-01-09T08:20:19Z url=https://github.com/w3c/csswg-drafts/issues/9465#issuecomment-3727708613 -->
## @lilles — 2026-01-09

Is this issue obsolete?
