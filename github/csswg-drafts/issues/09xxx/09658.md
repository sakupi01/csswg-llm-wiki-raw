---
number: 9658
title: "[css-values] Alternative, weighted mean syntax for `calc-mix()`"
state: "closed"
labels: ["Closed Accepted by CSSWG Resolution", "css-values-5"]
author: "Crissov"
created_at: "2023-11-30T09:21:33Z"
closed_at: "2025-05-29T20:37:25Z"
url: "https://github.com/w3c/csswg-drafts/issues/9658"
comments: 2
has_resolution: true
synced_at: "2026-07-04T04:45:14Z"
---

This started out as a comment to #9343 and is obviously a revival of my suggestion in #4700. 

With their shared use of a single `<progress>` parameter, the [_mix_ functions](https://drafts.csswg.org/css-values-5/#mixing) are currently all limited to two values, _start_ and _end_. While some individual alternative syntaxes support specifying a weight per entry, `calc-mix()` does not.

The alternative syntax of the image mix function `cross-fade()` even supports mixing an arbitrary number of (weighted) images – this includes colors, while the color blending function `color-mix()` is restricted to two values. 

1. If this is useful for images, I would expect that authors would sometimes find a need to mix together multiple values of other types as well. 
2. If the WG agreed with this assumption, `calc-mix()` in particular would need an alternative syntax. Alas, this could not use the established notation for the weights of values, because ”20% 80%“ would be ambiguous.
(Note that the optional weight, `<percentage [0,100]>?`, precedes the value in the grammar for `cross-fade()` and follows it for `color-mix()`, but `&&` means ‘any order’.) 
For linear interpolation of percentages, the order might be irrelevant, but authors may want different ones. 
3. If this was the case indeed, the best way to allow more interpolation methods would probably be to support the various kinds of weighted means (arithmetic, geometric, harmonic, …) – or a generalized mean. #4700
4. If syntax for means was to be introduced, ”calc-mix“ may not the best choice of a name. 

## Compilation of current grammar

~~~~ ebnf
<calc-mix()> = 
  calc-mix( <progress>, <calc-sum>, <calc-sum> )

<color-mix()> =
  color-mix( <progress> && <color-interpolation-method>?, <color>, <color> ) 
  |
  color-mix( <color-interpolation-method>, [<color> && <percentage [0,100]>?]#{2} )

<cross-fade()> =
  cross-fade( <progress>, [ <image> | <color> ], [ <image> | <color> ] ) 
  |
  cross-fade( <cf-image># )
<cf-image> = <percentage [0,100]>? && [ <image> | <color> ]

<transform-mix()> = 
  transform-mix( <progress>, <transform-list>, <transform-list> )

<mix()> =
  mix( <progress> ';' <whole-value> ';' <whole-value> ) 
  |
  mix( <progress> && of <'animation-name'> )
~~~~

<!-- comment id=2770544420 author=css-meeting-bot created=2025-04-01T19:58:29Z url=https://github.com/w3c/csswg-drafts/issues/9658#issuecomment-2770544420 resolution=true -->
## @css-meeting-bot — 2025-04-01

The CSS Working Group just discussed ``[css-values] Alternative, weighted mean syntax for `calc-mix()` ``, and agreed to the following:

* `RESOLVED: Close issue as resolved by https://github.com/w3c/csswg-drafts/issues/6245#issuecomment-2685746280`

<details><summary>The full IRC log of that discussion</summary>
&lt;fantasai> TabAtkins: crissov wanted a weighted average function<br>
&lt;fantasai> TabAtkins: which is doing exactly what he requested (other than differences in grammar)<br>
&lt;fantasai> TabAtkins: Weighted mean of calc() values, and interpolation thing, both are possible now with resolution to https://github.com/w3c/csswg-drafts/issues/6245#issuecomment-2685746280<br>
&lt;fantasai> RESOLVED: Close issue as resolved by https://github.com/w3c/csswg-drafts/issues/6245#issuecomment-2685746280<br>
</details>

<!-- comment id=2920526673 author=LeaVerou created=2025-05-29T20:37:25Z url=https://github.com/w3c/csswg-drafts/issues/9658#issuecomment-2920526673 -->
## @LeaVerou — 2025-05-29

Closing per resolution.
