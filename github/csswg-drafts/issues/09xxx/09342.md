---
number: 9342
title: "[css-values-4] mix() can not represent composition"
state: "open"
labels: ["css-values-5"]
author: "andruud"
created_at: "2023-09-12T09:10:31Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/9342"
comments: 2
has_resolution: false
synced_at: "2026-07-04T04:44:38Z"
---

For example, this requires a mix between _three_ values:

```
  @keyframes anim {
    from {
      offset-distance: min(10px, 10%);
    }
    to {
      animation-composition: add;
      offset-distance: min(20px, 20%);
    }
  }
  div {
    offset-distance: min(30px, 30%);
    animation: anim 10s -1s linear paused;
  }
```
The computed value should be something like `calc(min(30px, 30%) * 0.1 + min(10px, 10%) * 0.9 + min(20px, 20%) * 0.1)`, which `mix()` isn't able to represent.

See also https://github.com/w3c/csswg-drafts/issues/9341.

<!-- comment id=1832293197 author=LeaVerou created=2023-11-29T16:35:23Z url=https://github.com/w3c/csswg-drafts/issues/9342#issuecomment-1832293197 -->
## @LeaVerou — 2023-11-29

Wouldn't `mix(10% of anim)` work? (see https://drafts.csswg.org/css-values-5/#mix for the syntax involving `<animation-name>`). I think it's totally fine if `mix()` handles simple cases as arguments, and has to fallback to referencing a `@keyframes` rule for complex cases.

<!-- comment id=1832696143 author=tabatkins created=2023-11-29T21:03:06Z url=https://github.com/w3c/csswg-drafts/issues/9342#issuecomment-1832696143 -->
## @tabatkins — 2023-11-29

No, we can't use that "get a value from a keyframes" feature to represent an animated value. That would only work for the specific case of animated value produced *by a `@keyframes` animation*; it wouldn't handle transitions, or explicit Web Animations-produced animations.
