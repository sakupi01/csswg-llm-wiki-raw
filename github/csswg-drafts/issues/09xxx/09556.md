---
number: 9556
title: "[css-align] Add percentage values for alignment"
state: "open"
labels: ["css-align-4"]
author: "fantasai"
created_at: "2023-11-02T21:21:09Z"
closed_at: null
url: "https://github.com/w3c/csswg-drafts/issues/9556"
comments: 6
has_resolution: false
synced_at: "2026-08-06T00:47:06Z"
---

As @annevk and I were looking into styling of switch and range controls, it became obvious that it would be useful to be able to position things partway between `start` and `end`. So opening this issue to add `<percentage>` values to `align-self` and `justify-self` (and maybe also `align-content` and `justify-content`, although I'm less convinced that's necessary). These would interpolate between 0% = start and 100% = end, just like for `background-position`.

Open questions:
- Do we want to allow keywords to combine with percentages, to allow interpolation along the various directional axes besides `start`/`end`?
- Do we want to allow values outside of the 0-100% range (which has pretty weird results in some cases, but might be useful) or clamp to that range?

(This has been on my "potential extensions to alignment" list for awhile, but now there's an obvious use case, so filing it for consideration...)

<!-- comment id=1793048371 author=SebastianZ created=2023-11-03T20:21:18Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-1793048371 -->
## @SebastianZ — 2023-11-03

> * Do we want to allow keywords to combine with percentages, to allow interpolation along the various directional axes besides `start`/`end`?

Clear yes from my side.

> * Do we want to allow values outside of the 0-100% range (which has pretty weird results in some cases, but might be useful) or clamp to that range?

I can imagine cases in which the elements may want to overflow their container, for example for the mentioned switch and range controls. Though I don't have a strong opinion on that.

Sebastian

<!-- comment id=1793084095 author=tabatkins created=2023-11-03T20:54:31Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-1793084095 -->
## @tabatkins — 2023-11-03

+1 on this in general. I similarly allow %s for anchor(), meaning the same thing (% between start and end).

> Do we want to allow keywords to combine with percentages, to allow interpolation along the various directional axes besides start/end?

Yeah, since we have multiple possible alignment axises/directions (start/end vs sefl-start/self-end, at minimum), this makes sense. It should match the way background-position works, where `start 10%` and `end 90%` are the same thing (and both represent 10% off of start-aligned).

> Do we want to allow values outside of the 0-100% range (which has pretty weird results in some cases, but might be useful) or clamp to that range?

Yeah, I don't see any reason not to. I dunno use-cases, but again, background-position allows it, and it's well-defined.

<!-- comment id=1798433569 author=Que-tin created=2023-11-07T12:47:03Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-1798433569 -->
## @Que-tin — 2023-11-07

Will it then be possible to transition these values without view transitions?

<!-- comment id=1809131436 author=tabatkins created=2023-11-13T21:12:49Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-1809131436 -->
## @tabatkins — 2023-11-13

Yes.

<!-- comment id=5190835027 author=jakearchibald created=2026-08-05T10:53:30Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-5190835027 -->
## @jakearchibald — 2026-08-05

I've hit this exact issues with range controls so +1 to solving.

<!-- comment id=5190932112 author=css-meeting-bot created=2026-08-05T11:04:26Z url=https://github.com/w3c/csswg-drafts/issues/9556#issuecomment-5190932112 -->
## @css-meeting-bot — 2026-08-05

The CSS Working Group just discussed `[css-align] Add percentage values for alignment`, and agreed to the following:

* ``RESOLVED: Allow percentages to combine with the edge keywords of alignment properties, with a bare percent being relative to the `flex-start`, with syntax consitent with background-position``

<details><summary>The full IRC log of that discussion</summary>
&lt;emilio> TabAtkins: fantasai proposed to do alignment but not precise alignment<br>
&lt;emilio> ... start would be 0%, end 100%, center 50%<br>
&lt;emilio> ... anchor() already allows to do this<br>
&lt;emilio> fantasai: same way background-position does<br>
&lt;emilio> ... additional complications are the different directionalities we have<br>
&lt;emilio> ... so we'd need a default<br>
&lt;emilio> ... and probably combine with a keyword<br>
&lt;emilio> ... in order to get the different axes<br>
&lt;emilio> ... that's a follow-up question<br>
&lt;lea> `inline-end(100%)`<br>
&lt;emilio> ... the other question is what to do for out of range values<br>
&lt;emilio> astearns: what do we do for background-position?<br>
&lt;emilio> fantasai: non-obvious results<br>
&lt;emilio> JakeA: the weird results, are they weird in the case of "animation that had a spring", it'd overshoot? That'd be a use case for this<br>
&lt;emilio> lea: can we render that?<br>
&lt;emilio> TabAtkins: sure<br>
&lt;emilio> lea: why not<br>
&lt;emilio> JakeA: right, but is it weird?<br>
&lt;emilio> ChrisL: it's continuous<br>
&lt;lea> q?<br>
&lt;lea> q+<br>
&lt;emilio> TabAtkins: it's not obvious but for small values the behavior should be reasonable<br>
&lt;astearns> ack lea<br>
&lt;emilio> JakeA: seems like a good reason not to clamp it<br>
&lt;emilio> lea: huge +1 for this<br>
&lt;emilio> ... I've needed this myself<br>
&lt;emilio> ... for the syntax, what if instead of a single percentage<br>
&lt;emilio> ... what if we turn the existing edge keywords into functions that took a percentage?<br>
&lt;emilio> ... then inline-end(100%) would be the same as inline-start(0%)<br>
&lt;emilio> ... but it'd be weird if we did only one edge<br>
&lt;astearns> ack fantasai<br>
&lt;emilio> ... it's a bit more verbose but more obvious, intent is clearer<br>
&lt;emilio> fantasai: we should be consistent with bg-position<br>
&lt;emilio> +1<br>
&lt;emilio> ... we should define and interpret the syntax like background-position<br>
&lt;emilio> lea: how does it doe logical?<br>
&lt;emilio> fantasai: keyword then percentage<br>
&lt;emilio> lea: oh that's nicer<br>
&lt;emilio> fantasai: we won't add lengths<br>
&lt;emilio> ... but to the extent this exist in background-position it should be consistent<br>
&lt;emilio> lea: what would raw percentage mean?<br>
&lt;kbabbitt> q+<br>
&lt;emilio> emilio: we need a default<br>
&lt;emilio> q+<br>
&lt;astearns> ack kbabbitt<br>
&lt;emilio> kbabbitt: what's the different start/end, self-start/self-end, etc?<br>
&lt;emilio> fantasai: writing-mode of the value<br>
&lt;astearns> q+<br>
&lt;emilio> kbabbitt: can we do a transition between start and self-start?<br>
&lt;emilio> TabAtkins: that wouldn't work but as long as it's the same axis it could, e.g. transition from start 10% to end 10% because the later is just start 90%<br>
&lt;emilio> ... reminds me that anchor() takes a percentage but also has two axes<br>
&lt;emilio> ... so needs to do the same thing<br>
&lt;astearns> ack emilio<br>
&lt;emilio> ... will open an issue for that<br>
&lt;emilio> ack emilio<br>
&lt;fantasai> TabAtkins: We should be consistent with bg-position<br>
&lt;fantasai> s/TabAtkins/Emilio/<br>
&lt;emilio> astearns: do we need to allow bare percentages? Or can we require a keyword?<br>
&lt;astearns> ack astearns<br>
&lt;emilio> TabAtkins: for existing usage both anchor and bg-position have a default<br>
&lt;emilio> astearns: I don't mind<br>
&lt;TabAtkins> emilio: Is the reason to... why not add lengths?<br>
&lt;TabAtkins> emilio: if you say "start 1px", that's 1px further down from the start<br>
&lt;TabAtkins> fantasai: we have margins, just use those. we don't need yet another way<br>
&lt;oriol> q+<br>
&lt;TabAtkins> astearns: does anchor() allow lengths?<br>
&lt;astearns> ack oriol<br>
&lt;TabAtkins> TabAtkins: no, just keywords and %, exactly this<br>
&lt;lea> +1 emilio, `&lt;length-percentage>` seems useful and is more consistent with other places<br>
&lt;emilio> oriol: are we doing it for self-alignment only or also others?<br>
&lt;emilio> fantasai: not reason not to do it everywhere<br>
&lt;kbabbitt> q+<br>
&lt;emilio> astearns: so both align-* and justify-*<br>
&lt;emilio> fantasai: yes<br>
&lt;astearns> ack kbabbitt<br>
&lt;emilio> astearns: maybe punt on the lengths?<br>
&lt;emilio> emilio: I think it's fine not to have lengths, margins also support calc() as needed<br>
&lt;sgill> q+<br>
&lt;astearns> s/punt/open a separate issue/<br>
&lt;emilio> kbabbitt: I think it's fine not to have lengths for now<br>
&lt;emilio> ... but bg-position does allow lengths<br>
&lt;emilio> ... happy either way<br>
&lt;emilio> fantasai: there are other differences, they will have different keywords<br>
&lt;emilio> ... but for where they overlap we should share syntax<br>
&lt;astearns> ack sgill<br>
&lt;emilio> TabAtkins: we don't have margins for your background<br>
&lt;emilio> sgill: so if you do 10% start what does that mean?<br>
&lt;emilio> fantasai: 0% is the start edge, 100% is the end, you interpolate betwee<br>
&lt;emilio> sgill: what about center?<br>
&lt;emilio> fantasai: center will not combine with percentages<br>
&lt;emilio> astearns: and default would be start?<br>
&lt;emilio> TabAtkins: yeah that's the anchor default<br>
&lt;emilio> PROPOSED: Allow percentages to combine with the edge keywords of alignment properties, with a bare percent being relative to the `start`<br>
&lt;SebastianZ> q+<br>
&lt;emilio> PROPOSED: Allow percentages to combine with the edge keywords of alignment properties, with a bare percent being relative to the `start`, with syntax consitent with background-position<br>
&lt;emilio> SebastianZ: quick note, seems fine but differs with background-position<br>
&lt;emilio> ... bg-position percents align to the top left<br>
&lt;oriol> q+<br>
&lt;emilio> TabAtkins: right but background-position is a shorthand for the physical properties<br>
&lt;astearns> ack SebastianZ<br>
&lt;astearns> ack oriol<br>
&lt;emilio> fantasai: yeah bg-position has always been physical<br>
&lt;emilio> oriol: so safe alignment falls back to flex-start<br>
&lt;fantasai> whereas alignment has always defaulted to logical<br>
&lt;emilio> ... so could we use `flex-start` rather than `start` as the default?<br>
&lt;emilio> ... maybe it could be better<br>
&lt;emilio> fantasai: sure<br>
&lt;emilio> PROPOSED: Allow percentages to combine with the edge keywords of alignment properties, with a bare percent being relative to the `flex-start`, with syntax consitent with background-position<br>
&lt;emilio> TabAtkins: making everything relative to the flex directions was a mistake<br>
&lt;emilio> RESOLVED: Allow percentages to combine with the edge keywords of alignment properties, with a bare percent being relative to the `flex-start`, with syntax consitent with background-position<br>
</details>
