---
number: 9417
title: "[css-fonts-4] `@font-palette-values` lacking declaration for `font-family`"
state: "closed"
labels: ["Closed as Question Answered", "css-fonts-4"]
author: "cdoublev"
created_at: "2023-09-27T13:37:07Z"
closed_at: "2023-10-18T12:14:45Z"
url: "https://github.com/w3c/csswg-drafts/issues/9417"
comments: 1
has_resolution: false
synced_at: "2026-07-04T04:44:37Z"
---

I would like to get a confirmation that there should be no consistency between `@font-palette-values` (and possibly `@property`) on one side, and `@counter-style` and `@font-face` on the other, when they are missing mandatory descriptors.

For `@counter-style` and `@font-face`, it was decided (#1682, #1133) to consider them as *not declaring a resource* instead of as *invalid* (*ignored*), which means they appear in the CSSOM and make any following `@import` or `@namespace` invalid .

<details>
  <summary>Nit: some texts in the corresponding specs still hints at considering them as invalid.</summary>

For `@counter-style`, cf. #9258.

For `@font-face`, the spec says...:

  > [...] `@font-face` rules require a `font-family` and `src` descriptor; if either of these are missing, the `@font-face` rule must not be considered when performing the font matching algorithm.

... but later:

  > **Name:** `font-family`
  >
  > [...] It is required for the `@font-face` rule to be valid.
  >
  > **Name:** `src`
  >
  > [...] It is required for the `@font-face` rule to be valid.

I am willing to open a separate issue but I think it can be fixed more quickly by a spec author.

***edit:*** fixed in https://github.com/w3c/csswg-drafts/commit/47b1147fdc2d417d9e8d469fb2d5f0c8e971133a.
</details>

But FF currently *ignores* `@counter-style` when it is missing these descriptors. It preserves `@font-face` when it is missing a declaration for `src` or `font-family` (but it prevents declaring values via `fontFace.style`).

Chrome preserves `@counter-style` and `@font-face` missing the required descriptors, and later apply the resources after declaring the missing descriptors via CSSOM.

For `@property` and `@font-palette-values`, the corresponding specs want them to be invalid when they are missing the required descriptors, although implementations still diverge for `@property` (cf. [css-houdini-drafts#1098](https://github.com/w3c/css-houdini-drafts/issues/1098)). This seems to be expected because their descriptors are read-only in the CSSOM.

Chrome and FF preserve `@font-palette-values` missing a declaration for `font-family`.

Finally, I think `@color-profile` may also be considered *invalid* or *not declaring a resource* when missing a declaration for `src`.

<!-- comment id=1768323268 author=cdoublev created=2023-10-18T12:14:45Z url=https://github.com/w3c/csswg-drafts/issues/9417#issuecomment-1768323268 -->
## @cdoublev — 2023-10-18

Browser vendors [agreed](https://github.com/web-platform-tests/interop/issues/575) to follow the specification for `@property`, which means the inconsistency is considered ok:

  - `@counter-style` and `@font-face` lacking mandatory descriptors are **ignored**
  - `@font-palette-values` and `@property` lacking mandatory descriptors are **invalid**

The last two cannot be updated via the CSSOM so this seems logical, assuming they will remain immutable.

I consider all non-conformant behaviors described in my initial comment to be bugs (at the time of writing this comment):

  - FF should not discard `@counter-style` lacking mandatory descriptors
  - Chrome/FF should discard `@font-palette-values` lacking mandatory descriptors
